# SPDX-FileCopyrightText: 2025 QDX Technologies. Authored by Ryan Stocks <ryan.stocks00@gmail.com>
# SPDX-License-Identifier: Apache-2.0
---
name: Linux Clang
on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
env:
  CMAKE_CXX_STANDARD: 20
  CMAKE_VERBOSE_MAKEFILE: ON
jobs:
  linux-clang:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      max-parallel: 5
      matrix:
        clang-version: [14, 15, 16, 17, 18]
        build-type: [Debug, Release]
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
      - name: Configure apt mirror fallbacks
        uses: ./.github/actions/apt-mirror
      - name: Install Clang ${{ matrix.clang-version }}
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            cmake \
            clang-${{ matrix.clang-version }} \
            libc++-${{ matrix.clang-version }}-dev \
            libc++abi-${{ matrix.clang-version }}-dev
          echo "CC=clang-${{ matrix.clang-version }}" >> $GITHUB_ENV
          echo "CXX=clang++-${{ matrix.clang-version }}" >> $GITHUB_ENV
      - name: Install MPI
        run: |
          case "${{ matrix.clang-version }}" in
            14|15)
              sudo apt-get install -y --no-install-recommends libopenmpi-dev openmpi-bin
              echo "MPI_TYPE=OpenMPI" >> $GITHUB_ENV
              ;;
            16|17)
              sudo apt-get install -y --no-install-recommends libmpich-dev mpich
              echo "MPI_TYPE=MPICH" >> $GITHUB_ENV
              # GH runners have no IB; UCX must not probe mlx5/verbs or MPI_Init fails.
              echo "UCX_TLS=tcp,sm,self" >> $GITHUB_ENV
              echo "USE_LIBCXX=OFF" >> $GITHUB_ENV
              ;;
            18)
              wget -O- https://apt.repos.intel.com/intel-gpg-keys/GPG-PUB-KEY-INTEL-SW-PRODUCTS.PUB \
                | gpg --dearmor | sudo tee /usr/share/keyrings/oneapi-archive-keyring.gpg > /dev/null
              echo "deb [signed-by=/usr/share/keyrings/oneapi-archive-keyring.gpg] \
                https://apt.repos.intel.com/oneapi all main" | \
                sudo tee /etc/apt/sources.list.d/oneAPI.list
              sudo apt-get update
              sudo apt-get install -y --no-install-recommends intel-oneapi-mpi-devel
              echo "MPI_TYPE=IntelMPI" >> $GITHUB_ENV
              ;;
          esac
      - name: Configure CMake
        run: |
          if [[ "${{ matrix.clang-version }}" -eq 18 ]]; then
            source /opt/intel/oneapi/setvars.sh
          fi

          echo "MPI_TYPE: $MPI_TYPE"
          echo "MPI_HOME: $MPI_HOME"
          echo "CC: $CC"
          echo "CXX: $CXX"

          if [ "$MPI_TYPE" == "MPICH" ]; then
            export MPICH_CC=$CC
            export MPICH_CXX=$CXX
            export CC=mpicc
            export CXX=mpicxx
          fi

          CMAKE_ARGS="-DCMAKE_BUILD_TYPE=${{ matrix.build-type }} \
            -DCMAKE_C_COMPILER=$CC \
            -DCMAKE_CXX_COMPILER=$CXX \
            -DDYNAMPI_BUILD_TESTS=ON \
            -DDYNAMPI_BUILD_BENCHMARKS=ON \
            -DDYNAMPI_MAX_MPI_RANK=8"

          if [ "$MPI_TYPE" == "MPICH" ]; then
            CMAKE_ARGS="$CMAKE_ARGS -DMPI_C_COMPILER=$CC -DMPI_CXX_COMPILER=$CXX"
          else
            CMAKE_ARGS="$CMAKE_ARGS \
              -DCMAKE_CXX_FLAGS='-stdlib=libc++' \
              -DCMAKE_EXE_LINKER_FLAGS='-stdlib=libc++' \
              -DCMAKE_MODULE_LINKER_FLAGS='-stdlib=libc++'"
          fi

          echo "CMAKE_ARGS: $CMAKE_ARGS"

          eval "cmake -B build $CMAKE_ARGS"
      - name: Build
        run: |
          if [[ "${{ matrix.clang-version }}" -eq 18 ]]; then
            source /opt/intel/oneapi/setvars.sh
          fi
          cmake --build build --config ${{ matrix.build-type }} --parallel
      - name: Test
        run: |
          if [[ "${{ matrix.clang-version }}" -eq 18 ]]; then
            source /opt/intel/oneapi/setvars.sh
          fi
          cd build
          echo "Testing with $MPI_TYPE"
          export DYNAMPI_MAX_MPI_RANK=8
          ctest --output-on-failure -j 1 --timeout 180 -C ${{ matrix.build-type }}

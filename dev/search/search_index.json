{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":""},{"location":"#dynampi","title":"DynaMPI","text":"<p>Header-only C++20 library for dynamic MPI task distribution.</p> <p>DynaMPI distributes tasks across MPI ranks at runtime.  A single manager rank holds a queue of tasks; worker ranks pull work as they become available.  Result ordering depends on the distributor (naive and lock-free return results in task-index order; hierarchical does not).</p>"},{"location":"#quick-start","title":"Quick Start","text":"<pre><code>#include &lt;cmath&gt;\n#include &lt;iostream&gt;\n\n#include &lt;dynampi/dynampi.hpp&gt;\n\nauto work = [](size_t task) -&gt; double {\n  return std::sqrt(static_cast&lt;double&gt;(task));\n};\n\n// Single function call \u2014 manager gets results, workers return nullopt\nauto results = dynampi::mpi_manager_worker_distribution&lt;double&gt;(\n    100,    // number of tasks (indices 0 .. 99)\n    work    // worker function\n);\n\nif (results.has_value()) {\n    for (double r : *results) {\n        std::cout &lt;&lt; r &lt;&lt; \"\\n\";\n    }\n}\n</code></pre>"},{"location":"#api-reference","title":"API Reference","text":""},{"location":"#dynampimpi_manager_worker_distribution","title":"<code>dynampi::mpi_manager_worker_distribution</code>","text":"<pre><code>template &lt;typename ResultT,\n          template &lt;typename, typename, typename...&gt; typename Distributor =\n              HierarchicalMPIWorkDistributor&gt;\nstd::optional&lt;std::vector&lt;ResultT&gt;&gt; mpi_manager_worker_distribution(\n    size_t n_tasks,\n    std::function&lt;ResultT(size_t)&gt; worker_function,\n    MPI_Comm comm = MPI_COMM_WORLD,\n    int manager_rank = 0);\n</code></pre> <p>Distributes <code>n_tasks</code> tasks (indices <code>0 .. n_tasks-1</code>) across the MPI communicator.  Returns the results on the manager rank, <code>std::nullopt</code> on workers.</p> Parameter Default Description <code>n_tasks</code> \u2014 Number of tasks to distribute <code>worker_function</code> \u2014 <code>size_t \u2192 ResultT</code> \u2014 called with the task index <code>comm</code> <code>MPI_COMM_WORLD</code> MPI communicator <code>manager_rank</code> <code>0</code> Rank that manages distribution and collects results <p>Template parameters: - <code>ResultT</code> \u2014 the type each task produces - <code>Distributor</code> \u2014 implementation to use (default template argument is   <code>HierarchicalMPIWorkDistributor</code>, the same type as the public alias   <code>MPIDynamicWorkDistributor</code>)</p>"},{"location":"#dynampimpidynamicworkdistributor","title":"<code>dynampi::MPIDynamicWorkDistributor</code>","text":"<p>The default distributor (hierarchical tree of coordinators). Construct it directly when you need incremental <code>insert_task(s)</code> / <code>run_tasks</code>:</p> <pre><code>template &lt;typename TaskT, typename ResultT, typename... Options&gt;\nusing MPIDynamicWorkDistributor = HierarchicalMPIWorkDistributor&lt;TaskT, ResultT, Options...&gt;;\n</code></pre>"},{"location":"#direct-distributor-usage","title":"Direct Distributor Usage","text":"<p>For fine-grained control (incremental task insertion, batch result collection, statistics):</p> <pre><code>#include &lt;cmath&gt;\n\n#include &lt;dynampi/impl/naive_distributor.hpp&gt;\n\nusing Distributor = dynampi::NaiveMPIWorkDistributor&lt;int, double&gt;;\n\nauto work = [](int task) -&gt; double { return std::sqrt(static_cast&lt;double&gt;(task)); };\nDistributor::Config cfg;\ncfg.comm = MPI_COMM_WORLD;\ncfg.auto_run_workers = false;\n\nDistributor dist(work, cfg);\n\nif (dist.is_root_manager()) {\n    dist.insert_tasks({1, 2, 3, 4, 5});\n\n    // Collect first 3 results, leave remaining tasks in-flight\n    auto batch = dist.run_tasks({\n        .target_num_tasks = 3,\n        .allow_more_than_target_tasks = false\n    });\n\n    // Collect remaining results\n    auto rest = dist.finish_remaining_tasks();\n} else {\n    dist.run_worker();\n}\n</code></pre>"},{"location":"#common-config-fields","title":"Common Config Fields","text":"<p>All full distributors share these configuration fields:</p> Field Type Default Description <code>comm</code> <code>MPI_Comm</code> <code>MPI_COMM_WORLD</code> MPI communicator <code>manager_rank</code> <code>int</code> <code>0</code> Manager rank <code>auto_run_workers</code> <code>bool</code> <code>true</code> If true, workers start automatically in the constructor"},{"location":"#common-runconfig-fields","title":"Common RunConfig Fields","text":"Field Type Default Description <code>target_num_tasks</code> <code>size_t</code> <code>max</code> Stop once this many results are ready <code>allow_more_than_target_tasks</code> <code>bool</code> <code>true</code> If false, clip return to <code>target_num_tasks</code> <code>max_seconds</code> <code>optional&lt;double&gt;</code> <code>nullopt</code> Stop if this many seconds have elapsed"},{"location":"#common-methods","title":"Common Methods","text":"Method Returns Description <code>is_root_manager()</code> <code>bool</code> Whether this rank is the manager <code>insert_task(TaskT)</code> <code>void</code> Add one task to the queue (manager only) <code>insert_tasks(vector)</code> <code>void</code> Add multiple tasks (manager only) <code>run_tasks(RunConfig)</code> <code>vector&lt;ResultT&gt;</code> Process tasks, return completed results (manager) <code>finish_remaining_tasks()</code> <code>vector&lt;ResultT&gt;</code> Process all remaining tasks (manager) <code>run_worker()</code> <code>void</code> Enter worker loop (non-manager ranks) <code>remaining_tasks_count()</code> <code>size_t</code> Tasks still in the queue (manager) <code>finalize()</code> <code>void</code> Signal shutdown to workers <code>get_statistics()</code> <code>Statistics</code> Communication statistics (requires <code>track_statistics</code>)"},{"location":"#options","title":"Options","text":"<p>Template options customise distributor behaviour:</p> <pre><code>// Enable task prioritisation (NaiveMPIWorkDistributor)\nusing Dist = NaiveMPIWorkDistributor&lt;int, double, dynampi::enable_prioritization&gt;;\n\n// Enable statistics tracking\nusing Dist = NaiveMPIWorkDistributor&lt;int, double,\n    dynampi::track_statistics&lt;dynampi::StatisticsMode::Detailed&gt;&gt;;\n\n// Statistics on the default distributor\nusing Dist = MPIDynamicWorkDistributor&lt;int, double,\n    dynampi::track_statistics&lt;dynampi::StatisticsMode::Aggregated&gt;&gt;;\n</code></pre> Option Values Description <code>prioritize_tasks_t</code> <code>enable_prioritization</code> <code>insert_task(task, priority)</code> on distributors that support it (naive) <code>track_statistics_t</code> <code>track_statistics&lt;None&gt;</code>, <code>&lt;Aggregated&gt;</code>, <code>&lt;Detailed&gt;</code> Track communication volume and per-rank task counts"},{"location":"#version-info","title":"Version Info","text":"<pre><code>#include &lt;dynampi/dynampi.hpp&gt;\n\ndynampi::version::string;           // \"v0.0.1\"\ndynampi::version::major;            // 0\ndynampi::version::is_at_least(0,0,1); // true\ndynampi::version::compile_date();   // \"Jun  6 2026 12:34:56\"\ndynampi::version::commit_hash();    // \"abc1234\" or \"abc1234-dirty\"\n</code></pre>"},{"location":"api/","title":"API Reference","text":""},{"location":"api/#api-reference","title":"API Reference","text":""},{"location":"api/#dynampimpi_manager_worker_distribution","title":"<code>dynampi::mpi_manager_worker_distribution</code>","text":"<pre><code>template &lt;typename ResultT,\n          template &lt;typename, typename, typename...&gt; typename Distributor =\n              HierarchicalMPIWorkDistributor&gt;\nstd::optional&lt;std::vector&lt;ResultT&gt;&gt; mpi_manager_worker_distribution(\n    size_t n_tasks,\n    std::function&lt;ResultT(size_t)&gt; worker_function,\n    MPI_Comm comm = MPI_COMM_WORLD,\n    int manager_rank = 0);\n</code></pre> <p>The primary entry point.  Distributes <code>n_tasks</code> tasks (indices <code>0 .. n_tasks-1</code>) across the MPI communicator.  The default <code>Distributor</code> template argument is <code>HierarchicalMPIWorkDistributor</code> (same type as <code>MPIDynamicWorkDistributor</code>).</p> Parameter Default Description <code>n_tasks</code> \u2014 Number of tasks (0, 1, 2, \u2026) <code>worker_function</code> \u2014 <code>size_t \u2192 ResultT</code> <code>comm</code> <code>MPI_COMM_WORLD</code> MPI communicator <code>manager_rank</code> <code>0</code> Manager rank Returns <code>vector&lt;ResultT&gt;</code> on manager, <code>nullopt</code> on workers"},{"location":"api/#dynampimpidynamicworkdistributor","title":"<code>dynampi::MPIDynamicWorkDistributor</code>","text":"<p>The default distributor used by <code>mpi_manager_worker_distribution</code>. Hierarchical tree of coordinators; use this type for incremental task insertion:</p> <pre><code>template &lt;typename TaskT, typename ResultT, typename... Options&gt;\nusing MPIDynamicWorkDistributor =\n    HierarchicalMPIWorkDistributor&lt;TaskT, ResultT, Options...&gt;;\n</code></pre>"},{"location":"api/#distributor-classes","title":"Distributor Classes","text":"<p>Full distributors (<code>NaiveMPIWorkDistributor</code>, <code>MPIDynamicWorkDistributor</code>, <code>LockFreeMPIWorkDistributor</code>) share a common interface:</p> <pre><code>template &lt;typename TaskT, typename ResultT, typename... Options&gt;\nclass Distributor;\n</code></pre> <p><code>MinimalLockFreeMPIWorkDistributor&lt;ResultT&gt;</code> is a separate, smaller API for index-only parallel-for (<code>run(n_tasks)</code>); see Implementations.</p>"},{"location":"api/#common-config-fields","title":"Common <code>Config</code> Fields","text":"Field Type Default Description <code>comm</code> <code>MPI_Comm</code> <code>MPI_COMM_WORLD</code> MPI communicator <code>manager_rank</code> <code>int</code> <code>0</code> Rank that manages distribution <code>auto_run_workers</code> <code>bool</code> <code>true</code> Start workers automatically in constructor"},{"location":"api/#common-runconfig-fields","title":"Common <code>RunConfig</code> Fields","text":"Field Type Default Description <code>target_num_tasks</code> <code>size_t</code> <code>SIZE_MAX</code> Stop once this many results are ready <code>allow_more_than_target_tasks</code> <code>bool</code> <code>true</code> If false, clip return to <code>target_num_tasks</code> <code>max_seconds</code> <code>optional&lt;double&gt;</code> <code>nullopt</code> Time limit for this call"},{"location":"api/#common-methods","title":"Common Methods","text":"Method Returns Description <code>is_root_manager()</code> <code>bool</code> Whether this rank is the manager <code>insert_task(TaskT)</code> <code>void</code> Add one task (manager only) <code>insert_tasks(vector)</code> <code>void</code> Add multiple tasks (manager only) <code>run_tasks(RunConfig)</code> <code>vector&lt;ResultT&gt;</code> Process tasks, collect results (manager) <code>finish_remaining_tasks()</code> <code>vector&lt;ResultT&gt;</code> Process all remaining tasks (manager) <code>run_worker()</code> <code>void</code> Enter worker loop (non-manager) <code>remaining_tasks_count()</code> <code>size_t</code> Tasks still in the queue (manager) <code>finalize()</code> <code>void</code> Signal shutdown to all workers <code>get_statistics()</code> <code>Statistics</code> Communication statistics <p>Distributor-Specific Config</p> <p>See Implementations for additional configuration fields on each distributor class.</p>"},{"location":"api/#options","title":"Options","text":"<p>Template options customise distributor behaviour via the <code>Options...</code> parameter pack:</p> <pre><code>// Task prioritisation (NaiveMPIWorkDistributor)\nusing Dist = NaiveMPIWorkDistributor&lt;int, double,\n    dynampi::enable_prioritization&gt;;\n// Tasks inserted with insert_task(task, priority) are processed in\n// descending priority order.\n\n// Statistics tracking\nusing Dist = NaiveMPIWorkDistributor&lt;int, double,\n    dynampi::track_statistics&lt;dynampi::StatisticsMode::Detailed&gt;&gt;;\n// Tracks bytes sent/received, message counts, per-rank task counts.\n</code></pre> Option Effect <code>enable_prioritization</code> Enables <code>insert_task(task, priority)</code> where supported (naive) <code>track_statistics&lt;None&gt;</code> No statistics (default) <code>track_statistics&lt;Aggregated&gt;</code> Per-rank task counts <code>track_statistics&lt;Detailed&gt;</code> Full communication statistics"},{"location":"api/#version","title":"Version","text":"<pre><code>#include &lt;dynampi/dynampi.hpp&gt;\n\nnamespace dv = dynampi::version;\n\ndv::string;                    // \"v0.0.1\"\ndv::major;  dv::minor;  dv::patch;  // 0, 0, 1\ndv::is_at_least(0, 0, 1);    // true\ndv::compile_date();           // \"Jun  6 2026 12:34:56\"\ndv::commit_hash();            // \"abc1234\" or \"abc1234-dirty\"\n</code></pre>"},{"location":"api/#statistics","title":"Statistics","text":"<p><code>CommStatistics</code> is shared.  Each distributor defines its own nested <code>Statistics</code> type returned by <code>get_statistics()</code>:</p> <pre><code>struct CommStatistics {\n    int send_count, recv_count, collective_count;\n    size_t bytes_sent, bytes_received;\n    double send_time, recv_time;\n    double average_send_size() const;\n    double average_receive_size() const;\n};\n\n// NaiveMPIWorkDistributor / HierarchicalMPIWorkDistributor\nstruct Statistics {\n    const CommStatistics&amp; comm_statistics;\n    std::vector&lt;size_t&gt; worker_task_counts;              // naive\n    // std::optional&lt;std::vector&lt;size_t&gt;&gt; worker_task_counts;  // hierarchical\n};\n\n// LockFreeMPIWorkDistributor\nstruct Statistics {\n    CommStatistics comm_statistics;                      // by value\n    std::vector&lt;size_t&gt; worker_task_counts;\n};\n</code></pre> <p>Prefer <code>auto&amp; stats = dist.get_statistics();</code> and read the fields you need rather than depending on a single shared <code>Statistics</code> layout.</p>"},{"location":"api/#utilities","title":"Utilities","text":""},{"location":"api/#timer","title":"Timer","text":"<pre><code>#include &lt;dynampi/utilities/timer.hpp&gt;\n\ndynampi::Timer timer;                       // starts automatically\ntimer.elapsed();                             // std::chrono::duration&lt;double&gt;\ntimer.stop();                                // pause\ntimer.reset();                               // restart from zero\nstd::cout &lt;&lt; timer;                          // \"12.345 seconds\"\n</code></pre>"},{"location":"api/#assertions","title":"Assertions","text":"<pre><code>#include &lt;dynampi/utilities/assert.hpp&gt;\n\n// These are no-ops in release builds (-DNDEBUG):\nDYNAMPI_ASSERT(condition);                   // abort with file:line on failure\nDYNAMPI_ASSERT_EQ(a, b);                    // assert a == b, print both values\nDYNAMPI_ASSERT_NE(a, b);                    // assert a != b\nDYNAMPI_ASSERT_GT(a, b);                    // assert a &gt; b\nDYNAMPI_ASSERT_GE(a, b);                    // assert a &gt;= b\nDYNAMPI_ASSERT_LT(a, b);                    // assert a &lt; b\nDYNAMPI_ASSERT_LE(a, b);                    // assert a &lt;= b\nDYNAMPI_UNIMPLEMENTED();                    // always fails \u2014 marks unimplemented paths\n</code></pre>"},{"location":"api/#mpi-error-checking","title":"MPI Error Checking","text":"<pre><code>// Wraps MPI calls and throws std::runtime_error with file:line on failure:\nDYNAMPI_MPI_CHECK(MPI_Send, (buf, count, type, dest, tag, comm));\n</code></pre>"},{"location":"api/#printing","title":"Printing","text":"<pre><code>#include &lt;dynampi/utilities/printing.hpp&gt;\n\n// std::ostream operators for STL containers:\nstd::vector&lt;int&gt; v{1, 2, 3};\nstd::cout &lt;&lt; v;  // \"[1, 2, 3]\"\n\nstd::optional&lt;int&gt; o = 42;\nstd::cout &lt;&lt; o;  // \"Some(42)\"\n</code></pre>"},{"location":"implementations/","title":"Implementations","text":""},{"location":"implementations/#distributor-implementations","title":"Distributor Implementations","text":"<p>DynaMPI provides three full-featured distributors plus a minimal lock-free parallel-for helper.</p>"},{"location":"implementations/#naivempiworkdistributor","title":"NaiveMPIWorkDistributor","text":"<p>Best for: small-to-medium process counts, simplicity, ordered results.</p> <p>Two-sided <code>MPI_Send</code> / <code>MPI_Recv</code> between the manager and each worker. Workers send an initial REQUEST, then the manager assigns one task at a time. Each incoming RESULT signals that the worker is ready for the next task.</p>"},{"location":"implementations/#protocol","title":"Protocol","text":"<pre><code>Worker:                              Manager:\n  send REQUEST \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2192   (worker queued as free)\n  probe() \u2190\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500   send TASK\n  recv TASK\n  execute task\n  send RESULT \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2192   recv RESULT\n                                     store result\n                                     (worker queued as free again)\n  probe() \u2190\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500   send TASK\n  ...repeat...\n  probe() \u2190\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500   send DONE (no more tasks)\n</code></pre> <ul> <li>Communication: Two-sided <code>MPI_Send</code> / <code>MPI_Recv</code></li> <li>Ordering: Strictly ordered by task ID (<code>ordered = true</code>)</li> <li>Prioritization: Supported with <code>enable_prioritization</code></li> </ul>"},{"location":"implementations/#mpidynamicworkdistributor","title":"MPIDynamicWorkDistributor","text":"<p>Best for: large process counts (100+ ranks), multi-node clusters.  This is the default distributor (<code>mpi_manager_worker_distribution</code> and the type to construct for dynamic workloads).</p> <p>Implemented by <code>HierarchicalMPIWorkDistributor</code> (same type).</p> <p>Organises ranks into a tree.  Leaf workers communicate only with their local node coordinator; coordinators batch requests and results to/from the manager.  Two topology modes:</p>"},{"location":"implementations/#topology","title":"Topology","text":"<pre><code>  Manager \u2500\u2500\u252c\u2500\u2500 Coordinator 0 \u2500\u2500\u252c\u2500\u2500 Worker\u2080\n            \u2502                   \u251c\u2500\u2500 Worker\u2081\n            \u2502                   \u2514\u2500\u2500 Worker\u2082\n            \u251c\u2500\u2500 Coordinator 1 \u2500\u2500\u252c\u2500\u2500 Worker\u2083\n            \u2502                   \u2514\u2500\u2500 Worker\u2084\n            \u2514\u2500\u2500 Coordinator 2 \u2500\u2500\u252c\u2500\u2500 Worker\u2085\n                                \u2514\u2500\u2500 Worker\u2086\n</code></pre>"},{"location":"implementations/#coordinator_per_node-true-default","title":"<code>coordinator_per_node = true</code> (default)","text":"<p>Uses <code>MPI_Comm_split_type(MPI_COMM_TYPE_SHARED)</code> to discover physical nodes. One node coordinator per node (local rank 0).  Manager + coordinators form a leader group.  Workers talk to their coordinator via shared memory; the manager is excluded from its own node's local group.</p>"},{"location":"implementations/#coordinator_per_node-false","title":"<code>coordinator_per_node = false</code>","text":"<p>Virtual tree built from rank ordering.  Fan-out defaults to <code>max(2, \u221aN)</code>.</p>"},{"location":"implementations/#protocol-node-coordinator","title":"Protocol (node coordinator)","text":"<pre><code>while not done:\n    send REQUEST_BATCH(n) to parent   // n = children \u00d7 multiplier\n    while task_queue not empty:\n        if free children available:\n            dequeue child, send TASK_BATCH\n        else:\n            recv from anyone:\n                REQUEST  \u2192 push child to free stack\n                RESULT   \u2192 return batch to parent, request more\n                TASK     \u2192 execute locally, send RESULT up\n                DONE     \u2192 propagate to children, exit\n</code></pre> <ul> <li>Communication: Two-sided <code>MPI_Send</code> / <code>MPI_Recv</code> with batching</li> <li>Ordering: Not guaranteed (<code>ordered = false</code>)</li> <li>Prioritization: Not yet implemented</li> <li>Batching: Coordinators batch requests and results to amortise overhead</li> </ul>"},{"location":"implementations/#configuration","title":"Configuration","text":"Field Type Default Description <code>message_batch_size</code> <code>optional&lt;size_t&gt;</code> auto Tasks per batch <code>max_workers_per_coordinator</code> <code>optional&lt;int&gt;</code> <code>max(2, \u221aN)</code> Children per node <code>batch_size_multiplier</code> <code>int</code> <code>2</code> Prefetch = children \u00d7 multiplier <code>coordinator_per_node</code> <code>bool</code> <code>true</code> Physical-node topology"},{"location":"implementations/#lockfreempiworkdistributor","title":"LockFreeMPIWorkDistributor","text":"<p>Best for: fine-grained tasks where passive-target RMA progress is strong, and you want to avoid a two-sided request/response handshake per task.</p> <p>Workers claim work by atomically advancing a shared counter on the manager's MPI window (<code>MPI_Fetch_and_op</code>) and deposit results with <code>MPI_Put</code> under <code>MPI_Win_lock_all</code>.  Supports arbitrary <code>TaskT</code> / <code>ResultT</code> (with fixed capacity limits), incremental <code>insert_task(s)</code> / <code>run_tasks</code>, and ordered results.</p> <p>There is also <code>MinimalLockFreeMPIWorkDistributor&lt;ResultT&gt;</code>: a smaller API for embarrassingly parallel index loops (<code>size_t \u2192 ResultT</code>) that claims indices with one atomic counter and gathers results once at the end.</p>"},{"location":"implementations/#protocol-sketch","title":"Protocol (sketch)","text":"<pre><code>Workers (lock_all once):\n  while true:\n    idx \u2190 Fetch_and_op(+1, head)\n    if idx &gt;= total_tasks: exit (or wait for more / shutdown)\n    result = worker_function(task[idx])\n    Put(result) \u2192 manager; signal completion\n\nManager:\n  insert_task(s) bumps total_tasks / publishes payloads in the window\n  poll / gather completed results into run_tasks() return value\n  finalize() sets finished flag and drains remaining work\n</code></pre> <ul> <li>Communication: Passive-target RMA (<code>MPI_Win_lock_all</code>, <code>Fetch_and_op</code>, <code>Put</code>)</li> <li>Ordering: Ordered by task ID (<code>ordered = true</code>)</li> <li>Prioritization: Not supported (priority argument is ignored if enabled)</li> </ul>"},{"location":"implementations/#configuration_1","title":"Configuration","text":"Field Type Default Description <code>max_tasks</code> <code>int</code> <code>8192</code> Lifetime capacity of task/result tables <code>max_task_count</code> <code>int</code> <code>256</code> Max elements per resizable <code>TaskT</code> <code>max_result_count</code> <code>int</code> <code>256</code> Max elements per resizable <code>ResultT</code> <p>Design background (historical fence-based prototype and MPICH notes): see Lock-Free Design.</p>"},{"location":"implementations/#choosing-a-distributor","title":"Choosing a Distributor","text":"Scenario Use &lt; ~64 ranks, need ordered results / prioritization <code>NaiveMPIWorkDistributor</code> 100+ ranks, multi-node (default) <code>MPIDynamicWorkDistributor</code> Fine-grained tasks, good RMA progress <code>LockFreeMPIWorkDistributor</code> Static index parallel-for only <code>MinimalLockFreeMPIWorkDistributor</code>"},{"location":"implementations/#comparison","title":"Comparison","text":"Feature Naive MPIDynamic (hierarchical) LockFree Communication Two-sided Two-sided + batching Passive RMA Ordered results Yes No Yes Task prioritisation Yes No No Statistics Yes Yes Yes Node-aware topology No Yes No Manager bottleneck O(W) messages O(coordinators) Atomic claim + Put Max practical ranks ~64 ~1000+ MPI-RMA dependent"},{"location":"install/","title":"Install","text":""},{"location":"install/#installation","title":"Installation","text":"<p>DynaMPI is a header-only C++20 library.  It requires an MPI implementation (MPICH, Open MPI, Intel MPI, or MS-MPI) and a C++20 compiler.</p>"},{"location":"install/#requirements","title":"Requirements","text":"Dependency Minimum Version C++ compiler GCC 11+, Clang 14+, MSVC 2022+ MPI MPICH 3.4+, Open MPI 4.0+, Intel MPI 2021+, MS-MPI 10+ CMake 3.14+ (for building tests)"},{"location":"install/#cmake","title":"CMake","text":"<pre><code># From a subdirectory\nadd_subdirectory(path/to/DynaMPI)\ntarget_link_libraries(my_target PRIVATE dynampi)\n\n# Or via FetchContent\ninclude(FetchContent)\nFetchContent_Declare(\n    DynaMPI\n    GIT_REPOSITORY https://github.com/ryanstocks00/DynaMPI.git\n    GIT_TAG main\n)\nFetchContent_MakeAvailable(DynaMPI)\ntarget_link_libraries(my_target PRIVATE dynampi)\n</code></pre>"},{"location":"install/#manual-header-only","title":"Manual (header-only)","text":"<p>DynaMPI can be used without CMake \u2014 add the <code>include/</code> directory to your compiler's include path and link against MPI:</p> <pre><code>g++ -std=c++20 -I/path/to/DynaMPI/include my_program.cpp -lmpi\n</code></pre>"},{"location":"install/#building-tests","title":"Building Tests","text":"<pre><code>git clone https://github.com/ryanstocks00/DynaMPI.git\ncd DynaMPI\ncmake -B build -DDYNAMPI_BUILD_TESTS=ON\ncmake --build build\n\n# Run unit tests\n./build/test/unit_test\n\n# Run MPI tests with 4 ranks\nmpirun -n 4 ./build/test/mpi_test\n</code></pre>"},{"location":"mpi_compare_and_swap_design/","title":"Lock-Free Design","text":""},{"location":"mpi_compare_and_swap_design/#lock-free-rma-distributor-design-notes","title":"Lock-Free RMA Distributor \u2014 Design Notes","text":"<p>These notes document the design that became <code>LockFreeMPIWorkDistributor</code> / <code>MinimalLockFreeMPIWorkDistributor</code>. For the user-facing summary, see Implementations.</p> <p>Historical context</p> <p>An earlier fence-based one-sided prototype (<code>OneSidedMPIWorkDistributor</code>) used three <code>MPI_Win_fence</code> barriers per round.  That class is not in the current tree; passive-target <code>MPI_Win_lock_all</code> is the shipped RMA path.</p> <p>MPICH compatibility</p> <p>Early <code>MPI_Win_sync</code> + <code>MPI_Fetch_and_op</code> experiments under <code>MPI_Win_lock_all</code> did not make remote atomic updates visible to local loads on some MPICH 4.0 (ch4:ofi) configurations without async progress. The current lock-free distributors use flush/gather patterns that have been exercised in CI; still validate RMA progress on your target MPI before relying on lock-free at scale.</p>"},{"location":"mpi_compare_and_swap_design/#rationale","title":"Rationale","text":"<p>Passive-target RMA avoids a two-sided request/response handshake per task. Workers claim indices (or task slots) with <code>MPI_Fetch_and_op</code> and publish results with <code>MPI_Put</code>.</p> <p><code>MinimalLockFreeMPIWorkDistributor</code> restricts tasks to loop indices (<code>size_t</code>).  <code>LockFreeMPIWorkDistributor</code> stores arbitrary task/result payloads in fixed-capacity window slots.</p>"},{"location":"mpi_compare_and_swap_design/#data-layout-conceptual","title":"Data Layout (conceptual)","text":"<p>Manager window (simplified):</p> <pre><code>  Offset 0:   head_idx / claim counter   (int64)  \u2190 Fetch_and_op by workers\n  Offset 8:   total_tasks                 (int64)  \u2190 written by manager\n  Offset 16:  finished / control flags    (int64)\n  \u2026         task and result slot tables\n</code></pre> <p>Exact offsets and gather sequencing live in <code>include/dynampi/impl/lockfree_distributor.hpp</code>.</p>"},{"location":"mpi_compare_and_swap_design/#synchronisation","title":"Synchronisation","text":"<p><code>MPI_Win_lock_all(MPI_MODE_NOCHECK)</code> (or equivalent) once at startup. Workers use <code>MPI_Win_flush</code> to complete their own RMA.  The manager gathers completed results and eventually signals shutdown via a finished flag.</p>"},{"location":"mpi_compare_and_swap_design/#protocol-minimal-index-parallel-for","title":"Protocol (minimal index parallel-for)","text":"<pre><code>lock_all once\n\nwhile true:\n    idx \u2190 Fetch_and_op(+1, head)\n    if idx &gt;= n_tasks: break\n    local_results.append(idx, worker_function(idx))\n\ngather_sorted(local_results) on manager\n</code></pre>"},{"location":"mpi_compare_and_swap_design/#comparison-fence-prototype-vs-lock-free","title":"Comparison (fence prototype vs lock-free)","text":"Fence prototype (removed) Lock-free (current) Barriers per round 3 collective fences 0 Lock/unlock per task 0 0 (<code>lock_all</code> once) RMA per task Puts in fence epochs Fetch_and_op + Put (+ gather) Arbitrary task types Yes (slot based) Yes (<code>LockFreeMPIWorkDistributor</code>) Index-only fast path \u2014 <code>MinimalLockFreeMPIWorkDistributor</code>"},{"location":"mpi_compare_and_swap_design/#implementation-checklist-shipped","title":"Implementation checklist (shipped)","text":"<ol> <li><code>LockFreeMPIWorkDistributor&lt;TaskT, ResultT, Options...&gt;</code> with    <code>insert_task(s)</code> / <code>run_tasks</code> / <code>finish_remaining_tasks</code>.</li> <li><code>MinimalLockFreeMPIWorkDistributor&lt;ResultT&gt;</code> for index parallel-for.</li> <li>Manager window created on the root; workers attach with a placeholder base.</li> <li>Capacity limits via <code>Config::max_tasks</code>, <code>max_task_count</code>, <code>max_result_count</code>.</li> </ol>"},{"location":"dynampi/annotated/","title":"Class List","text":"<p>Here are the classes, structs, unions and interfaces with brief descriptions:</p> <ul> <li>namespace dynampi <ul> <li>class AsyncPutLockFreeMPIWorkDistributor <ul> <li>struct Config </li> <li>struct RunConfig </li> <li>struct Statistics </li> </ul> </li> <li>class BaseMPIWorkDistributor <ul> <li>struct Config </li> </ul> </li> <li>struct CommStatistics </li> <li>class HierarchicalAsyncPutLockFreeMPIWorkDistributor <ul> <li>struct Config </li> <li>struct RunConfig </li> </ul> </li> <li>class HierarchicalLockFreeMPIWorkDistributor <ul> <li>struct Config </li> <li>struct RunConfig </li> </ul> </li> <li>class HierarchicalMPIWorkDistributor <ul> <li>struct Config </li> <li>struct RunConfig </li> </ul> </li> <li>class HierarchicalNonBlockingMPIWorkDistributor <ul> <li>struct Config </li> <li>struct RunConfig </li> </ul> </li> <li>class LockFreeMPIWorkDistributor <ul> <li>struct Config </li> <li>struct RunConfig </li> <li>struct Statistics </li> </ul> </li> <li>class MPICommunicator </li> <li>class MPIGroup </li> <li>struct MPI_Type </li> <li>struct MPI_Type&lt; std::nullptr_t &gt; </li> <li>struct MPI_Type&lt; std::string &gt; </li> <li>struct MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt; </li> <li>class MinimalLockFreeMPIWorkDistributor <ul> <li>struct Config </li> </ul> </li> <li>class NaiveMPIWorkDistributor <ul> <li>struct Config </li> <li>struct RunConfig </li> <li>struct Statistics </li> </ul> </li> <li>class Timer </li> <li>namespace detail <ul> <li>class AsyncPutLevel <ul> <li>struct ClaimedRange </li> <li>struct Config </li> </ul> </li> <li>class LockFreeLevel <ul> <li>struct Config </li> </ul> </li> </ul> </li> <li>struct enable_prioritization </li> <li>struct has_dynampi_mpi_type </li> <li>struct has_dynampi_mpi_type&lt; U, std::void_t&lt; decltype(MPI_Type&lt; U &gt;::value)&gt; &gt; </li> <li>struct prioritize_tasks_t </li> <li>struct track_statistics </li> <li>struct track_statistics_t </li> <li>namespace version </li> </ul> </li> <li>struct BridgeHop </li> <li>struct PendingRelay </li> <li>struct Statistics </li> <li>struct TaskRequest </li> <li>class AsyncSendPool </li> <li>struct Statistics </li> <li>struct TaskRequest </li> <li>struct option_value </li> <li>struct option_value&lt; Option, Head, Tail... &gt; </li> <li>namespace std </li> </ul>"},{"location":"dynampi/files/","title":"File List","text":"<p>Here is a list of all files with brief descriptions:</p> <ul> <li>dir include <ul> <li>dir dynampi <ul> <li>dir impl <ul> <li>file async_put_lockfree_distributor.hpp </li> <li>file base_distributor.hpp </li> <li>file hierarchical_async_put_lockfree_distributor.hpp </li> <li>file hierarchical_distributor.hpp </li> <li>file hierarchical_lockfree_distributor.hpp </li> <li>file hierarchical_nonblocking_distributor.hpp </li> <li>file lockfree_distributor.hpp </li> <li>file naive_distributor.hpp </li> </ul> </li> <li>dir mpi <ul> <li>file mpi_communicator.hpp </li> <li>file mpi_error.hpp </li> <li>file mpi_group.hpp </li> <li>file mpi_types.hpp </li> </ul> </li> <li>dir utilities <ul> <li>file assert.hpp </li> <li>file printing.hpp </li> <li>file template_options.hpp </li> <li>file timer.hpp </li> </ul> </li> <li>file dynampi.hpp </li> </ul> </li> </ul> </li> </ul>"},{"location":"dynampi/namespacedynampi/","title":"Namespace dynampi","text":"<p>Namespace List &gt; dynampi</p>"},{"location":"dynampi/namespacedynampi/#namespaces","title":"Namespaces","text":"Type Name namespace detail namespace version"},{"location":"dynampi/namespacedynampi/#classes","title":"Classes","text":"Type Name class AsyncPutLockFreeMPIWorkDistributor &lt;typename TaskT, typename ResultT, Options&gt; class BaseMPIWorkDistributor &lt;typename TaskT, typename ResultT, Options&gt; struct CommStatistics class HierarchicalAsyncPutLockFreeMPIWorkDistributor &lt;typename TaskT, typename ResultT, Options&gt; class HierarchicalLockFreeMPIWorkDistributor &lt;typename TaskT, typename ResultT, Options&gt; class HierarchicalMPIWorkDistributor &lt;typename TaskT, typename ResultT, Options&gt; class HierarchicalNonBlockingMPIWorkDistributor &lt;typename TaskT, typename ResultT, Options&gt; class LockFreeMPIWorkDistributor &lt;typename TaskT, typename ResultT, Options&gt; class MPICommunicator &lt;Options&gt; class MPIGroup struct MPI_Type &lt;typename T, typename&gt; struct MPI_Type&lt; std::nullptr_t &gt; &lt;&gt; struct MPI_Type&lt; std::string &gt; &lt;&gt; struct MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt; &lt;typename T&gt; class MinimalLockFreeMPIWorkDistributor &lt;typename ResultT&gt; class NaiveMPIWorkDistributor &lt;typename TaskT, typename ResultT, Options&gt; class Timer struct enable_prioritization struct has_dynampi_mpi_type &lt;typename, typename&gt; struct has_dynampi_mpi_type&lt; U, std::void_t&lt; decltype(MPI_Type&lt; U &gt;::value)&gt; &gt; &lt;typename U&gt; struct prioritize_tasks_t struct track_statistics &lt;Mode&gt; struct track_statistics_t"},{"location":"dynampi/namespacedynampi/#public-types","title":"Public Types","text":"Type Name typedef HierarchicalMPIWorkDistributor&lt; TaskT, ResultT, Options... &gt; MPIDynamicWorkDistributor enum StatisticsMode"},{"location":"dynampi/namespacedynampi/#public-functions","title":"Public Functions","text":"Type Name DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (char, MPI_CHAR)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (std::byte, MPI_BYTE)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (signed char, MPI_SIGNED_CHAR)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (unsigned char, MPI_UNSIGNED_CHAR)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (short, MPI_SHORT)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (unsigned short, MPI_UNSIGNED_SHORT)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (int, MPI_INT)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (unsigned int, MPI_UNSIGNED)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (long, MPI_LONG)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (unsigned long, MPI_UNSIGNED_LONG)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (long long, MPI_LONG_LONG_INT)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (unsigned long long, MPI_UNSIGNED_LONG_LONG)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (float, MPI_FLOAT)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (double, MPI_DOUBLE)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (long double, MPI_LONG_DOUBLE)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (bool, MPI_C_BOOL)  std::optional&lt; std::string &gt; OptionalString (Args &amp;&amp;... args)  void _DYNAMPI_FAILBinOp (const A &amp; a, const B &amp; b, const std::string &amp; a_str, const std::string &amp; b_str, const std::string &amp; nop, const std::optional&lt; std::string &gt; &amp; message)  void _DYNAMPI_FAIL_ASSERT (const std::string &amp; condition_str, const std::optional&lt; std::string &gt; &amp; message)  void mpi_fail (int err, std::string_view command)  std::optional&lt; std::vector&lt; ResultT &gt; &gt; mpi_manager_worker_distribution (size_t n_tasks, std::function&lt; ResultT(size_t)&gt; worker_function, MPI_Comm comm=MPI_COMM_WORLD, int manager_rank=0)  std::ostream &amp; operator&lt;&lt; (std::ostream &amp; os, const std::set&lt; T &gt; &amp; set)  std::ostream &amp; operator&lt;&lt; (std::ostream &amp; os, const std::vector&lt; T &gt; &amp; vec)  std::ostream &amp; operator&lt;&lt; (std::ostream &amp; os, const std::array&lt; T, N &gt; &amp; arr)  std::ostream &amp; operator&lt;&lt; (std::ostream &amp; os, const std::span&lt; T &gt; &amp; vec)  std::ostream &amp; operator&lt;&lt; (std::ostream &amp; os, const std::optional&lt; T &gt; &amp; op)  std::ostream &amp; operator&lt;&lt; (std::ostream &amp; os, const std::tuple&lt; Args... &gt; &amp; tup)  std::ostream &amp; operator&lt;&lt; (std::ostream &amp; os, const std::pair&lt; T, U &gt; &amp; pair)  std::ostream &amp; operator&lt;&lt; (std::ostream &amp; os, const std::byte &amp; b)"},{"location":"dynampi/namespacedynampi/#public-types-documentation","title":"Public Types Documentation","text":""},{"location":"dynampi/namespacedynampi/#typedef-mpidynamicworkdistributor","title":"typedef MPIDynamicWorkDistributor","text":"<pre><code>using dynampi::MPIDynamicWorkDistributor = typedef HierarchicalMPIWorkDistributor&lt;TaskT, ResultT, Options...&gt;;\n</code></pre>"},{"location":"dynampi/namespacedynampi/#enum-statisticsmode","title":"enum StatisticsMode","text":"<pre><code>enum dynampi::StatisticsMode {\n    None,\n    Aggregated,\n    Detailed\n};\n</code></pre>"},{"location":"dynampi/namespacedynampi/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    char,\n    MPI_CHAR\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_1","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    std::byte,\n    MPI_BYTE\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_2","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    signed char,\n    MPI_SIGNED_CHAR\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_3","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    unsigned char,\n    MPI_UNSIGNED_CHAR\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_4","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    short,\n    MPI_SHORT\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_5","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    unsigned short,\n    MPI_UNSIGNED_SHORT\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_6","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    int,\n    MPI_INT\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_7","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    unsigned int,\n    MPI_UNSIGNED\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_8","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    long,\n    MPI_LONG\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_9","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    unsigned long,\n    MPI_UNSIGNED_LONG\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_10","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    long long,\n    MPI_LONG_LONG_INT\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_11","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    unsigned long long,\n    MPI_UNSIGNED_LONG_LONG\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_12","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    float,\n    MPI_FLOAT\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_13","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    double,\n    MPI_DOUBLE\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_14","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    long double,\n    MPI_LONG_DOUBLE\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_15","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    bool,\n    MPI_C_BOOL\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-optionalstring","title":"function OptionalString","text":"<pre><code>template&lt;typename... Args&gt;\nstd::optional&lt; std::string &gt; dynampi::OptionalString (\n    Args &amp;&amp;... args\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-_dynampi_failbinop","title":"function _DYNAMPI_FAILBinOp","text":"<pre><code>template&lt;typename A, typename B&gt;\ninline void dynampi::_DYNAMPI_FAILBinOp (\n    const A &amp; a,\n    const B &amp; b,\n    const std::string &amp; a_str,\n    const std::string &amp; b_str,\n    const std::string &amp; nop,\n    const std::optional&lt; std::string &gt; &amp; message\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-_dynampi_fail_assert","title":"function _DYNAMPI_FAIL_ASSERT","text":"<pre><code>inline void dynampi::_DYNAMPI_FAIL_ASSERT (\n    const std::string &amp; condition_str,\n    const std::optional&lt; std::string &gt; &amp; message\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-mpi_fail","title":"function mpi_fail","text":"<pre><code>inline void dynampi::mpi_fail (\n    int err,\n    std::string_view command\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-mpi_manager_worker_distribution","title":"function mpi_manager_worker_distribution","text":"<pre><code>template&lt;typename ResultT, template&lt; typename, typename, typename... &gt; typename T&gt;\nstd::optional&lt; std::vector&lt; ResultT &gt; &gt; dynampi::mpi_manager_worker_distribution (\n    size_t n_tasks,\n    std::function&lt; ResultT(size_t)&gt; worker_function,\n    MPI_Comm comm=MPI_COMM_WORLD,\n    int manager_rank=0\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-operator","title":"function operator&lt;&lt;","text":"<pre><code>template&lt;typename T&gt;\ninline std::ostream &amp; dynampi::operator&lt;&lt; (\n    std::ostream &amp; os,\n    const std::set&lt; T &gt; &amp; set\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-operator_1","title":"function operator&lt;&lt;","text":"<pre><code>template&lt;typename T&gt;\ninline std::ostream &amp; dynampi::operator&lt;&lt; (\n    std::ostream &amp; os,\n    const std::vector&lt; T &gt; &amp; vec\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-operator_2","title":"function operator&lt;&lt;","text":"<pre><code>template&lt;typename T, std::size_t N&gt;\ninline std::ostream &amp; dynampi::operator&lt;&lt; (\n    std::ostream &amp; os,\n    const std::array&lt; T, N &gt; &amp; arr\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-operator_3","title":"function operator&lt;&lt;","text":"<pre><code>template&lt;typename T&gt;\ninline std::ostream &amp; dynampi::operator&lt;&lt; (\n    std::ostream &amp; os,\n    const std::span&lt; T &gt; &amp; vec\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-operator_4","title":"function operator&lt;&lt;","text":"<pre><code>template&lt;typename T&gt;\ninline std::ostream &amp; dynampi::operator&lt;&lt; (\n    std::ostream &amp; os,\n    const std::optional&lt; T &gt; &amp; op\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-operator_5","title":"function operator&lt;&lt;","text":"<pre><code>template&lt;typename... Args&gt;\ninline std::ostream &amp; dynampi::operator&lt;&lt; (\n    std::ostream &amp; os,\n    const std::tuple&lt; Args... &gt; &amp; tup\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-operator_6","title":"function operator&lt;&lt;","text":"<pre><code>template&lt;typename T, typename U&gt;\ninline std::ostream &amp; dynampi::operator&lt;&lt; (\n    std::ostream &amp; os,\n    const std::pair&lt; T, U &gt; &amp; pair\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-operator_7","title":"function operator&lt;&lt;","text":"<pre><code>inline std::ostream &amp; dynampi::operator&lt;&lt; (\n    std::ostream &amp; os,\n    const std::byte &amp; b\n) \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/dynampi.hpp</code></p>"},{"location":"dynampi/classdynampi_1_1AsyncPutLockFreeMPIWorkDistributor/","title":"Class dynampi::AsyncPutLockFreeMPIWorkDistributor","text":"<p>template &lt;typename TaskT, typename ResultT, typename... Options&gt;</p> <p>ClassList &gt; dynampi &gt; AsyncPutLockFreeMPIWorkDistributor</p> <ul> <li><code>#include &lt;async_put_lockfree_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/classdynampi_1_1AsyncPutLockFreeMPIWorkDistributor/#classes","title":"Classes","text":"Type Name struct Config struct RunConfig struct Statistics"},{"location":"dynampi/classdynampi_1_1AsyncPutLockFreeMPIWorkDistributor/#public-types","title":"Public Types","text":"Type Name typedef std::conditional_t&lt; statistics_mode !=StatisticsMode::None, Statistics, std::monostate &gt; StatisticsT"},{"location":"dynampi/classdynampi_1_1AsyncPutLockFreeMPIWorkDistributor/#public-static-attributes","title":"Public Static Attributes","text":"Type Name const bool ordered   = <code>false</code>"},{"location":"dynampi/classdynampi_1_1AsyncPutLockFreeMPIWorkDistributor/#public-functions","title":"Public Functions","text":"Type Name AsyncPutLockFreeMPIWorkDistributor (std::function&lt; ResultT(TaskT)&gt; worker_function, Config config={})  void finalize ()  std::vector&lt; ResultT &gt; finish_remaining_tasks ()  std::vector&lt; ResultT &gt; gather_once ()  const StatisticsT &amp; get_statistics () const void insert_task (TaskT task)  void insert_task (const TaskT &amp; task, double)  void insert_tasks (const std::vector&lt; TaskT &gt; &amp; tasks)  bool is_root_manager () const int num_workers () const size_t remaining_tasks_count () const std::vector&lt; ResultT &gt; run_tasks (RunConfig config={})  void run_worker ()  ~AsyncPutLockFreeMPIWorkDistributor ()"},{"location":"dynampi/classdynampi_1_1AsyncPutLockFreeMPIWorkDistributor/#public-types-documentation","title":"Public Types Documentation","text":""},{"location":"dynampi/classdynampi_1_1AsyncPutLockFreeMPIWorkDistributor/#typedef-statisticst","title":"typedef StatisticsT","text":"<pre><code>using dynampi::AsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::StatisticsT =  std::conditional_t&lt;statistics_mode != StatisticsMode::None, Statistics, std::monostate&gt;;\n</code></pre>"},{"location":"dynampi/classdynampi_1_1AsyncPutLockFreeMPIWorkDistributor/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/classdynampi_1_1AsyncPutLockFreeMPIWorkDistributor/#variable-ordered","title":"variable ordered","text":"<pre><code>const bool dynampi::AsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::ordered;\n</code></pre>"},{"location":"dynampi/classdynampi_1_1AsyncPutLockFreeMPIWorkDistributor/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/classdynampi_1_1AsyncPutLockFreeMPIWorkDistributor/#function-asyncputlockfreempiworkdistributor","title":"function AsyncPutLockFreeMPIWorkDistributor","text":"<pre><code>inline explicit dynampi::AsyncPutLockFreeMPIWorkDistributor::AsyncPutLockFreeMPIWorkDistributor (\n    std::function&lt; ResultT(TaskT)&gt; worker_function,\n    Config config={}\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1AsyncPutLockFreeMPIWorkDistributor/#function-finalize","title":"function finalize","text":"<pre><code>inline void dynampi::AsyncPutLockFreeMPIWorkDistributor::finalize () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1AsyncPutLockFreeMPIWorkDistributor/#function-finish_remaining_tasks","title":"function finish_remaining_tasks","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::AsyncPutLockFreeMPIWorkDistributor::finish_remaining_tasks () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1AsyncPutLockFreeMPIWorkDistributor/#function-gather_once","title":"function gather_once","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::AsyncPutLockFreeMPIWorkDistributor::gather_once () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1AsyncPutLockFreeMPIWorkDistributor/#function-get_statistics","title":"function get_statistics","text":"<pre><code>inline const StatisticsT &amp; dynampi::AsyncPutLockFreeMPIWorkDistributor::get_statistics () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1AsyncPutLockFreeMPIWorkDistributor/#function-insert_task-12","title":"function insert_task [1/2]","text":"<pre><code>inline void dynampi::AsyncPutLockFreeMPIWorkDistributor::insert_task (\n    TaskT task\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1AsyncPutLockFreeMPIWorkDistributor/#function-insert_task-22","title":"function insert_task [2/2]","text":"<pre><code>inline void dynampi::AsyncPutLockFreeMPIWorkDistributor::insert_task (\n    const TaskT &amp; task,\n    double\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1AsyncPutLockFreeMPIWorkDistributor/#function-insert_tasks","title":"function insert_tasks","text":"<pre><code>inline void dynampi::AsyncPutLockFreeMPIWorkDistributor::insert_tasks (\n    const std::vector&lt; TaskT &gt; &amp; tasks\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1AsyncPutLockFreeMPIWorkDistributor/#function-is_root_manager","title":"function is_root_manager","text":"<pre><code>inline bool dynampi::AsyncPutLockFreeMPIWorkDistributor::is_root_manager () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1AsyncPutLockFreeMPIWorkDistributor/#function-num_workers","title":"function num_workers","text":"<pre><code>inline int dynampi::AsyncPutLockFreeMPIWorkDistributor::num_workers () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1AsyncPutLockFreeMPIWorkDistributor/#function-remaining_tasks_count","title":"function remaining_tasks_count","text":"<pre><code>inline size_t dynampi::AsyncPutLockFreeMPIWorkDistributor::remaining_tasks_count () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1AsyncPutLockFreeMPIWorkDistributor/#function-run_tasks","title":"function run_tasks","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::AsyncPutLockFreeMPIWorkDistributor::run_tasks (\n    RunConfig config={}\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1AsyncPutLockFreeMPIWorkDistributor/#function-run_worker","title":"function run_worker","text":"<pre><code>inline void dynampi::AsyncPutLockFreeMPIWorkDistributor::run_worker () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1AsyncPutLockFreeMPIWorkDistributor/#function-asyncputlockfreempiworkdistributor_1","title":"function ~AsyncPutLockFreeMPIWorkDistributor","text":"<pre><code>inline dynampi::AsyncPutLockFreeMPIWorkDistributor::~AsyncPutLockFreeMPIWorkDistributor () \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/async_put_lockfree_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1AsyncPutLockFreeMPIWorkDistributor_1_1Config/","title":"Struct dynampi::AsyncPutLockFreeMPIWorkDistributor::Config","text":"<p>ClassList &gt; dynampi &gt; AsyncPutLockFreeMPIWorkDistributor &gt; Config</p> <ul> <li><code>#include &lt;async_put_lockfree_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1AsyncPutLockFreeMPIWorkDistributor_1_1Config/#public-attributes","title":"Public Attributes","text":"Type Name bool auto_run_workers   = <code>true</code> int claim_batch_size   = <code>8</code> MPI_Comm comm   = <code>MPI\\_COMM\\_WORLD</code> int manager_rank   = <code>0</code> int max_result_count   = <code>256</code> int max_task_count   = <code>256</code> int max_tasks   = <code>8192</code>"},{"location":"dynampi/structdynampi_1_1AsyncPutLockFreeMPIWorkDistributor_1_1Config/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1AsyncPutLockFreeMPIWorkDistributor_1_1Config/#variable-auto_run_workers","title":"variable auto_run_workers","text":"<pre><code>bool dynampi::AsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::auto_run_workers;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1AsyncPutLockFreeMPIWorkDistributor_1_1Config/#variable-claim_batch_size","title":"variable claim_batch_size","text":"<pre><code>int dynampi::AsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::claim_batch_size;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1AsyncPutLockFreeMPIWorkDistributor_1_1Config/#variable-comm","title":"variable comm","text":"<pre><code>MPI_Comm dynampi::AsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::comm;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1AsyncPutLockFreeMPIWorkDistributor_1_1Config/#variable-manager_rank","title":"variable manager_rank","text":"<pre><code>int dynampi::AsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::manager_rank;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1AsyncPutLockFreeMPIWorkDistributor_1_1Config/#variable-max_result_count","title":"variable max_result_count","text":"<pre><code>int dynampi::AsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_result_count;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1AsyncPutLockFreeMPIWorkDistributor_1_1Config/#variable-max_task_count","title":"variable max_task_count","text":"<pre><code>int dynampi::AsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_task_count;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1AsyncPutLockFreeMPIWorkDistributor_1_1Config/#variable-max_tasks","title":"variable max_tasks","text":"<pre><code>int dynampi::AsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_tasks;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/async_put_lockfree_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1AsyncPutLockFreeMPIWorkDistributor_1_1RunConfig/","title":"Struct dynampi::AsyncPutLockFreeMPIWorkDistributor::RunConfig","text":"<p>ClassList &gt; dynampi &gt; AsyncPutLockFreeMPIWorkDistributor &gt; RunConfig</p> <ul> <li><code>#include &lt;async_put_lockfree_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1AsyncPutLockFreeMPIWorkDistributor_1_1RunConfig/#public-attributes","title":"Public Attributes","text":"Type Name bool allow_more_than_target_tasks   = <code>true</code> std::optional&lt; double &gt; max_seconds   = <code>std::nullopt</code> size_t target_num_tasks   = <code>std::numeric\\_limits&amp;lt;size\\_t&amp;gt;::max()</code>"},{"location":"dynampi/structdynampi_1_1AsyncPutLockFreeMPIWorkDistributor_1_1RunConfig/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1AsyncPutLockFreeMPIWorkDistributor_1_1RunConfig/#variable-allow_more_than_target_tasks","title":"variable allow_more_than_target_tasks","text":"<pre><code>bool dynampi::AsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::allow_more_than_target_tasks;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1AsyncPutLockFreeMPIWorkDistributor_1_1RunConfig/#variable-max_seconds","title":"variable max_seconds","text":"<pre><code>std::optional&lt;double&gt; dynampi::AsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::max_seconds;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1AsyncPutLockFreeMPIWorkDistributor_1_1RunConfig/#variable-target_num_tasks","title":"variable target_num_tasks","text":"<pre><code>size_t dynampi::AsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::target_num_tasks;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/async_put_lockfree_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1AsyncPutLockFreeMPIWorkDistributor_1_1Statistics/","title":"Struct dynampi::AsyncPutLockFreeMPIWorkDistributor::Statistics","text":"<p>ClassList &gt; dynampi &gt; AsyncPutLockFreeMPIWorkDistributor &gt; Statistics</p> <ul> <li><code>#include &lt;async_put_lockfree_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1AsyncPutLockFreeMPIWorkDistributor_1_1Statistics/#public-attributes","title":"Public Attributes","text":"Type Name CommStatistics comm_statistics"},{"location":"dynampi/structdynampi_1_1AsyncPutLockFreeMPIWorkDistributor_1_1Statistics/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1AsyncPutLockFreeMPIWorkDistributor_1_1Statistics/#variable-comm_statistics","title":"variable comm_statistics","text":"<pre><code>CommStatistics dynampi::AsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Statistics::comm_statistics;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/async_put_lockfree_distributor.hpp</code></p>"},{"location":"dynampi/classdynampi_1_1BaseMPIWorkDistributor/","title":"Class dynampi::BaseMPIWorkDistributor","text":"<p>template &lt;typename TaskT, typename ResultT, typename... Options&gt;</p> <p>ClassList &gt; dynampi &gt; BaseMPIWorkDistributor</p> <ul> <li><code>#include &lt;base_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/classdynampi_1_1BaseMPIWorkDistributor/#classes","title":"Classes","text":"Type Name struct Config"},{"location":"dynampi/classdynampi_1_1BaseMPIWorkDistributor/#protected-types","title":"Protected Types","text":"Type Name typedef std::conditional_t&lt; prioritize_tasks, std::priority_queue&lt; std::pair&lt; double, TaskT &gt; &gt;, std::deque&lt; TaskT &gt; &gt; QueueT"},{"location":"dynampi/classdynampi_1_1BaseMPIWorkDistributor/#protected-static-attributes","title":"Protected Static Attributes","text":"Type Name constexpr bool prioritize_tasks   = <code>[**get\\_option\\_value**](template__options_8hpp.md#function-get_option_value)&amp;lt;[**prioritize\\_tasks\\_t**](structdynampi_1_1prioritize__tasks__t.md), Options...&amp;gt;()</code>"},{"location":"dynampi/classdynampi_1_1BaseMPIWorkDistributor/#protected-types-documentation","title":"Protected Types Documentation","text":""},{"location":"dynampi/classdynampi_1_1BaseMPIWorkDistributor/#typedef-queuet","title":"typedef QueueT","text":"<pre><code>using dynampi::BaseMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::QueueT =  std::conditional_t&lt;prioritize_tasks, std::priority_queue&lt;std::pair&lt;double, TaskT&gt; &gt;, std::deque&lt;TaskT&gt; &gt;;\n</code></pre>"},{"location":"dynampi/classdynampi_1_1BaseMPIWorkDistributor/#protected-static-attributes-documentation","title":"Protected Static Attributes Documentation","text":""},{"location":"dynampi/classdynampi_1_1BaseMPIWorkDistributor/#variable-prioritize_tasks","title":"variable prioritize_tasks","text":"<pre><code>constexpr bool dynampi::BaseMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::prioritize_tasks;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/base_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1BaseMPIWorkDistributor_1_1Config/","title":"Struct dynampi::BaseMPIWorkDistributor::Config","text":"<p>ClassList &gt; dynampi &gt; BaseMPIWorkDistributor &gt; Config</p> <ul> <li><code>#include &lt;base_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1BaseMPIWorkDistributor_1_1Config/#public-attributes","title":"Public Attributes","text":"Type Name bool auto_run_workers   = <code>true</code> MPI_Comm comm   = <code>MPI\\_COMM\\_WORLD</code> int manager_rank   = <code>0</code>"},{"location":"dynampi/structdynampi_1_1BaseMPIWorkDistributor_1_1Config/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1BaseMPIWorkDistributor_1_1Config/#variable-auto_run_workers","title":"variable auto_run_workers","text":"<pre><code>bool dynampi::BaseMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::auto_run_workers;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1BaseMPIWorkDistributor_1_1Config/#variable-comm","title":"variable comm","text":"<pre><code>MPI_Comm dynampi::BaseMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::comm;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1BaseMPIWorkDistributor_1_1Config/#variable-manager_rank","title":"variable manager_rank","text":"<pre><code>int dynampi::BaseMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::manager_rank;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/base_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1CommStatistics/","title":"Struct dynampi::CommStatistics","text":"<p>ClassList &gt; dynampi &gt; CommStatistics</p> <ul> <li><code>#include &lt;mpi_communicator.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1CommStatistics/#public-attributes","title":"Public Attributes","text":"Type Name size_t bytes_received   = <code>0</code> size_t bytes_sent   = <code>0</code> int collective_count   = <code>0</code> int recv_count   = <code>0</code> double recv_time   = <code>0.0</code> int send_count   = <code>0</code> double send_time   = <code>0.0</code>"},{"location":"dynampi/structdynampi_1_1CommStatistics/#public-functions","title":"Public Functions","text":"Type Name double average_receive_size () const double average_send_size () const void reset ()"},{"location":"dynampi/structdynampi_1_1CommStatistics/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1CommStatistics/#variable-bytes_received","title":"variable bytes_received","text":"<pre><code>size_t dynampi::CommStatistics::bytes_received;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1CommStatistics/#variable-bytes_sent","title":"variable bytes_sent","text":"<pre><code>size_t dynampi::CommStatistics::bytes_sent;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1CommStatistics/#variable-collective_count","title":"variable collective_count","text":"<pre><code>int dynampi::CommStatistics::collective_count;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1CommStatistics/#variable-recv_count","title":"variable recv_count","text":"<pre><code>int dynampi::CommStatistics::recv_count;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1CommStatistics/#variable-recv_time","title":"variable recv_time","text":"<pre><code>double dynampi::CommStatistics::recv_time;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1CommStatistics/#variable-send_count","title":"variable send_count","text":"<pre><code>int dynampi::CommStatistics::send_count;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1CommStatistics/#variable-send_time","title":"variable send_time","text":"<pre><code>double dynampi::CommStatistics::send_time;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1CommStatistics/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/structdynampi_1_1CommStatistics/#function-average_receive_size","title":"function average_receive_size","text":"<pre><code>inline double dynampi::CommStatistics::average_receive_size () const\n</code></pre>"},{"location":"dynampi/structdynampi_1_1CommStatistics/#function-average_send_size","title":"function average_send_size","text":"<pre><code>inline double dynampi::CommStatistics::average_send_size () const\n</code></pre>"},{"location":"dynampi/structdynampi_1_1CommStatistics/#function-reset","title":"function reset","text":"<pre><code>inline void dynampi::CommStatistics::reset () \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_communicator.hpp</code></p>"},{"location":"dynampi/classdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor/","title":"Class dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor","text":"<p>template &lt;typename TaskT, typename ResultT, typename... Options&gt;</p> <p>ClassList &gt; dynampi &gt; HierarchicalAsyncPutLockFreeMPIWorkDistributor</p> <ul> <li><code>#include &lt;hierarchical_async_put_lockfree_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/classdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor/#classes","title":"Classes","text":"Type Name struct Config struct RunConfig"},{"location":"dynampi/classdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor/#public-static-attributes","title":"Public Static Attributes","text":"Type Name const bool ordered   = <code>false</code>"},{"location":"dynampi/classdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor/#public-functions","title":"Public Functions","text":"Type Name HierarchicalAsyncPutLockFreeMPIWorkDistributor (std::function&lt; ResultT(TaskT)&gt; worker_function, Config config={})  void finalize ()  std::vector&lt; ResultT &gt; finish_remaining_tasks ()  std::vector&lt; ResultT &gt; gather_once ()  void insert_task (TaskT task)  void insert_tasks (const std::vector&lt; TaskT &gt; &amp; tasks)  bool is_root_manager () const size_t remaining_tasks_count () const std::vector&lt; ResultT &gt; run_tasks (const RunConfig &amp; config=RunConfig{})  void run_worker ()  ~HierarchicalAsyncPutLockFreeMPIWorkDistributor ()"},{"location":"dynampi/classdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/classdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor/#variable-ordered","title":"variable ordered","text":"<pre><code>const bool dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::ordered;\n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/classdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor/#function-hierarchicalasyncputlockfreempiworkdistributor","title":"function HierarchicalAsyncPutLockFreeMPIWorkDistributor","text":"<pre><code>inline explicit dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::HierarchicalAsyncPutLockFreeMPIWorkDistributor (\n    std::function&lt; ResultT(TaskT)&gt; worker_function,\n    Config config={}\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor/#function-finalize","title":"function finalize","text":"<pre><code>inline void dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::finalize () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor/#function-finish_remaining_tasks","title":"function finish_remaining_tasks","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::finish_remaining_tasks () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor/#function-gather_once","title":"function gather_once","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::gather_once () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor/#function-insert_task","title":"function insert_task","text":"<pre><code>inline void dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::insert_task (\n    TaskT task\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor/#function-insert_tasks","title":"function insert_tasks","text":"<pre><code>inline void dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::insert_tasks (\n    const std::vector&lt; TaskT &gt; &amp; tasks\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor/#function-is_root_manager","title":"function is_root_manager","text":"<pre><code>inline bool dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::is_root_manager () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor/#function-remaining_tasks_count","title":"function remaining_tasks_count","text":"<pre><code>inline size_t dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::remaining_tasks_count () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor/#function-run_tasks","title":"function run_tasks","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::run_tasks (\n    const RunConfig &amp; config=RunConfig {}\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor/#function-run_worker","title":"function run_worker","text":"<pre><code>inline void dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::run_worker () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor/#function-hierarchicalasyncputlockfreempiworkdistributor_1","title":"function ~HierarchicalAsyncPutLockFreeMPIWorkDistributor","text":"<pre><code>inline dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::~HierarchicalAsyncPutLockFreeMPIWorkDistributor () \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_async_put_lockfree_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1Config/","title":"Struct dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::Config","text":"<p>ClassList &gt; dynampi &gt; HierarchicalAsyncPutLockFreeMPIWorkDistributor &gt; Config</p> <ul> <li><code>#include &lt;hierarchical_async_put_lockfree_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1Config/#public-attributes","title":"Public Attributes","text":"Type Name bool auto_run_workers   = <code>true</code> MPI_Comm comm   = <code>MPI\\_COMM\\_WORLD</code> int leader_batch_multiplier   = <code>2</code> int local_batch_size   = <code>8</code> int manager_rank   = <code>0</code> int max_local_tasks   = <code>8192</code> int max_result_count   = <code>256</code> int max_task_count   = <code>256</code> int max_tasks   = <code>8192</code> int max_upper_fanout   = <code>0</code>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1Config/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1Config/#variable-auto_run_workers","title":"variable auto_run_workers","text":"<pre><code>bool dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::auto_run_workers;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1Config/#variable-comm","title":"variable comm","text":"<pre><code>MPI_Comm dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::comm;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1Config/#variable-leader_batch_multiplier","title":"variable leader_batch_multiplier","text":"<pre><code>int dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::leader_batch_multiplier;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1Config/#variable-local_batch_size","title":"variable local_batch_size","text":"<pre><code>int dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::local_batch_size;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1Config/#variable-manager_rank","title":"variable manager_rank","text":"<pre><code>int dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::manager_rank;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1Config/#variable-max_local_tasks","title":"variable max_local_tasks","text":"<pre><code>int dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_local_tasks;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1Config/#variable-max_result_count","title":"variable max_result_count","text":"<pre><code>int dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_result_count;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1Config/#variable-max_task_count","title":"variable max_task_count","text":"<pre><code>int dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_task_count;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1Config/#variable-max_tasks","title":"variable max_tasks","text":"<pre><code>int dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_tasks;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1Config/#variable-max_upper_fanout","title":"variable max_upper_fanout","text":"<pre><code>int dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_upper_fanout;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_async_put_lockfree_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1RunConfig/","title":"Struct dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::RunConfig","text":"<p>ClassList &gt; dynampi &gt; HierarchicalAsyncPutLockFreeMPIWorkDistributor &gt; RunConfig</p> <ul> <li><code>#include &lt;hierarchical_async_put_lockfree_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1RunConfig/#public-attributes","title":"Public Attributes","text":"Type Name bool allow_more_than_target_tasks   = <code>true</code> std::optional&lt; double &gt; max_seconds   = <code>std::nullopt</code> size_t target_num_tasks   = <code>std::numeric\\_limits&amp;lt;size\\_t&amp;gt;::max()</code>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1RunConfig/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1RunConfig/#variable-allow_more_than_target_tasks","title":"variable allow_more_than_target_tasks","text":"<pre><code>bool dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::allow_more_than_target_tasks;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1RunConfig/#variable-max_seconds","title":"variable max_seconds","text":"<pre><code>std::optional&lt;double&gt; dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::max_seconds;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1RunConfig/#variable-target_num_tasks","title":"variable target_num_tasks","text":"<pre><code>size_t dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::target_num_tasks;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_async_put_lockfree_distributor.hpp</code></p>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeMPIWorkDistributor/","title":"Class dynampi::HierarchicalLockFreeMPIWorkDistributor","text":"<p>template &lt;typename TaskT, typename ResultT, typename... Options&gt;</p> <p>ClassList &gt; dynampi &gt; HierarchicalLockFreeMPIWorkDistributor</p> <ul> <li><code>#include &lt;hierarchical_lockfree_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeMPIWorkDistributor/#classes","title":"Classes","text":"Type Name struct Config struct RunConfig"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeMPIWorkDistributor/#public-static-attributes","title":"Public Static Attributes","text":"Type Name const bool ordered   = <code>false</code>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeMPIWorkDistributor/#public-functions","title":"Public Functions","text":"Type Name HierarchicalLockFreeMPIWorkDistributor (std::function&lt; ResultT(TaskT)&gt; worker_function, Config config={})  void finalize ()  std::vector&lt; ResultT &gt; finish_remaining_tasks ()  void insert_task (TaskT task)  void insert_tasks (const std::vector&lt; TaskT &gt; &amp; tasks)  bool is_root_manager () const size_t remaining_tasks_count () const std::vector&lt; ResultT &gt; run_tasks (const RunConfig &amp; config=RunConfig{})  void run_worker ()  ~HierarchicalLockFreeMPIWorkDistributor ()"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeMPIWorkDistributor/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeMPIWorkDistributor/#variable-ordered","title":"variable ordered","text":"<pre><code>const bool dynampi::HierarchicalLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::ordered;\n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeMPIWorkDistributor/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeMPIWorkDistributor/#function-hierarchicallockfreempiworkdistributor","title":"function HierarchicalLockFreeMPIWorkDistributor","text":"<pre><code>inline explicit dynampi::HierarchicalLockFreeMPIWorkDistributor::HierarchicalLockFreeMPIWorkDistributor (\n    std::function&lt; ResultT(TaskT)&gt; worker_function,\n    Config config={}\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeMPIWorkDistributor/#function-finalize","title":"function finalize","text":"<pre><code>inline void dynampi::HierarchicalLockFreeMPIWorkDistributor::finalize () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeMPIWorkDistributor/#function-finish_remaining_tasks","title":"function finish_remaining_tasks","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::HierarchicalLockFreeMPIWorkDistributor::finish_remaining_tasks () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeMPIWorkDistributor/#function-insert_task","title":"function insert_task","text":"<pre><code>inline void dynampi::HierarchicalLockFreeMPIWorkDistributor::insert_task (\n    TaskT task\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeMPIWorkDistributor/#function-insert_tasks","title":"function insert_tasks","text":"<pre><code>inline void dynampi::HierarchicalLockFreeMPIWorkDistributor::insert_tasks (\n    const std::vector&lt; TaskT &gt; &amp; tasks\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeMPIWorkDistributor/#function-is_root_manager","title":"function is_root_manager","text":"<pre><code>inline bool dynampi::HierarchicalLockFreeMPIWorkDistributor::is_root_manager () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeMPIWorkDistributor/#function-remaining_tasks_count","title":"function remaining_tasks_count","text":"<pre><code>inline size_t dynampi::HierarchicalLockFreeMPIWorkDistributor::remaining_tasks_count () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeMPIWorkDistributor/#function-run_tasks","title":"function run_tasks","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::HierarchicalLockFreeMPIWorkDistributor::run_tasks (\n    const RunConfig &amp; config=RunConfig {}\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeMPIWorkDistributor/#function-run_worker","title":"function run_worker","text":"<pre><code>inline void dynampi::HierarchicalLockFreeMPIWorkDistributor::run_worker () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeMPIWorkDistributor/#function-hierarchicallockfreempiworkdistributor_1","title":"function ~HierarchicalLockFreeMPIWorkDistributor","text":"<pre><code>inline dynampi::HierarchicalLockFreeMPIWorkDistributor::~HierarchicalLockFreeMPIWorkDistributor () \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_lockfree_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeMPIWorkDistributor_1_1Config/","title":"Struct dynampi::HierarchicalLockFreeMPIWorkDistributor::Config","text":"<p>ClassList &gt; dynampi &gt; HierarchicalLockFreeMPIWorkDistributor &gt; Config</p> <ul> <li><code>#include &lt;hierarchical_lockfree_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeMPIWorkDistributor_1_1Config/#public-attributes","title":"Public Attributes","text":"Type Name bool auto_run_workers   = <code>true</code> MPI_Comm comm   = <code>MPI\\_COMM\\_WORLD</code> int leader_batch_multiplier   = <code>2</code> int local_batch_size   = <code>1</code> int manager_rank   = <code>0</code> int max_local_tasks   = <code>8192</code> int max_result_count   = <code>256</code> int max_task_count   = <code>256</code> int max_tasks   = <code>8192</code>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeMPIWorkDistributor_1_1Config/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeMPIWorkDistributor_1_1Config/#variable-auto_run_workers","title":"variable auto_run_workers","text":"<pre><code>bool dynampi::HierarchicalLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::auto_run_workers;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeMPIWorkDistributor_1_1Config/#variable-comm","title":"variable comm","text":"<pre><code>MPI_Comm dynampi::HierarchicalLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::comm;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeMPIWorkDistributor_1_1Config/#variable-leader_batch_multiplier","title":"variable leader_batch_multiplier","text":"<pre><code>int dynampi::HierarchicalLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::leader_batch_multiplier;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeMPIWorkDistributor_1_1Config/#variable-local_batch_size","title":"variable local_batch_size","text":"<pre><code>int dynampi::HierarchicalLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::local_batch_size;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeMPIWorkDistributor_1_1Config/#variable-manager_rank","title":"variable manager_rank","text":"<pre><code>int dynampi::HierarchicalLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::manager_rank;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeMPIWorkDistributor_1_1Config/#variable-max_local_tasks","title":"variable max_local_tasks","text":"<pre><code>int dynampi::HierarchicalLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_local_tasks;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeMPIWorkDistributor_1_1Config/#variable-max_result_count","title":"variable max_result_count","text":"<pre><code>int dynampi::HierarchicalLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_result_count;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeMPIWorkDistributor_1_1Config/#variable-max_task_count","title":"variable max_task_count","text":"<pre><code>int dynampi::HierarchicalLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_task_count;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeMPIWorkDistributor_1_1Config/#variable-max_tasks","title":"variable max_tasks","text":"<pre><code>int dynampi::HierarchicalLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_tasks;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_lockfree_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeMPIWorkDistributor_1_1RunConfig/","title":"Struct dynampi::HierarchicalLockFreeMPIWorkDistributor::RunConfig","text":"<p>ClassList &gt; dynampi &gt; HierarchicalLockFreeMPIWorkDistributor &gt; RunConfig</p> <ul> <li><code>#include &lt;hierarchical_lockfree_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeMPIWorkDistributor_1_1RunConfig/#public-attributes","title":"Public Attributes","text":"Type Name bool allow_more_than_target_tasks   = <code>true</code> std::optional&lt; double &gt; max_seconds   = <code>std::nullopt</code> size_t target_num_tasks   = <code>std::numeric\\_limits&amp;lt;size\\_t&amp;gt;::max()</code>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeMPIWorkDistributor_1_1RunConfig/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeMPIWorkDistributor_1_1RunConfig/#variable-allow_more_than_target_tasks","title":"variable allow_more_than_target_tasks","text":"<pre><code>bool dynampi::HierarchicalLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::allow_more_than_target_tasks;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeMPIWorkDistributor_1_1RunConfig/#variable-max_seconds","title":"variable max_seconds","text":"<pre><code>std::optional&lt;double&gt; dynampi::HierarchicalLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::max_seconds;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeMPIWorkDistributor_1_1RunConfig/#variable-target_num_tasks","title":"variable target_num_tasks","text":"<pre><code>size_t dynampi::HierarchicalLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::target_num_tasks;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_lockfree_distributor.hpp</code></p>"},{"location":"dynampi/classdynampi_1_1HierarchicalMPIWorkDistributor/","title":"Class dynampi::HierarchicalMPIWorkDistributor","text":"<p>template &lt;typename TaskT, typename ResultT, typename... Options&gt;</p> <p>ClassList &gt; dynampi &gt; HierarchicalMPIWorkDistributor</p> <ul> <li><code>#include &lt;hierarchical_distributor.hpp&gt;</code></li> </ul> <p>Inherits the following classes: dynampi::BaseMPIWorkDistributor</p>"},{"location":"dynampi/classdynampi_1_1HierarchicalMPIWorkDistributor/#classes","title":"Classes","text":"Type Name struct Config struct RunConfig"},{"location":"dynampi/classdynampi_1_1HierarchicalMPIWorkDistributor/#public-static-attributes","title":"Public Static Attributes","text":"Type Name const bool ordered   = <code>false</code> constexpr bool prioritize_tasks   = <code>Base::prioritize\\_tasks</code>"},{"location":"dynampi/classdynampi_1_1HierarchicalMPIWorkDistributor/#public-functions","title":"Public Functions","text":"Type Name HierarchicalMPIWorkDistributor (std::function&lt; ResultT(TaskT)&gt; worker_function, Config runtime_config=Config{})  void allocate_task_to_child ()  void finalize ()  std::vector&lt; ResultT &gt; finish_remaining_tasks ()  const StatisticsT &amp; get_statistics () const void insert_task (TaskT task)  void insert_task (const TaskT &amp; task, double priority)  void insert_tasks (const Range &amp; tasks)  void insert_tasks (const std::vector&lt; TaskT &gt; &amp; tasks)  bool is_root_manager () const size_t remaining_tasks_count () const std::vector&lt; ResultT &gt; run_tasks (const RunConfig &amp; config=RunConfig{})  void run_worker ()  void send_results_to_parent ()  ~HierarchicalMPIWorkDistributor ()"},{"location":"dynampi/classdynampi_1_1HierarchicalMPIWorkDistributor/#protected-types-inherited-from-dynampibasempiworkdistributor","title":"Protected Types inherited from dynampi::BaseMPIWorkDistributor","text":"<p>See dynampi::BaseMPIWorkDistributor</p> Type Name typedef std::conditional_t&lt; prioritize_tasks, std::priority_queue&lt; std::pair&lt; double, TaskT &gt; &gt;, std::deque&lt; TaskT &gt; &gt; QueueT"},{"location":"dynampi/classdynampi_1_1HierarchicalMPIWorkDistributor/#protected-static-attributes-inherited-from-dynampibasempiworkdistributor","title":"Protected Static Attributes inherited from dynampi::BaseMPIWorkDistributor","text":"<p>See dynampi::BaseMPIWorkDistributor</p> Type Name constexpr bool prioritize_tasks   = <code>[**get\\_option\\_value**](template__options_8hpp.md#function-get_option_value)&amp;lt;[**prioritize\\_tasks\\_t**](structdynampi_1_1prioritize__tasks__t.md), Options...&amp;gt;()</code>"},{"location":"dynampi/classdynampi_1_1HierarchicalMPIWorkDistributor/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/classdynampi_1_1HierarchicalMPIWorkDistributor/#variable-ordered","title":"variable ordered","text":"<pre><code>const bool dynampi::HierarchicalMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::ordered;\n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalMPIWorkDistributor/#variable-prioritize_tasks","title":"variable prioritize_tasks","text":"<pre><code>constexpr bool dynampi::HierarchicalMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::prioritize_tasks;\n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalMPIWorkDistributor/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/classdynampi_1_1HierarchicalMPIWorkDistributor/#function-hierarchicalmpiworkdistributor","title":"function HierarchicalMPIWorkDistributor","text":"<pre><code>inline explicit dynampi::HierarchicalMPIWorkDistributor::HierarchicalMPIWorkDistributor (\n    std::function&lt; ResultT(TaskT)&gt; worker_function,\n    Config runtime_config=Config {}\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalMPIWorkDistributor/#function-allocate_task_to_child","title":"function allocate_task_to_child","text":"<pre><code>inline void dynampi::HierarchicalMPIWorkDistributor::allocate_task_to_child () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalMPIWorkDistributor/#function-finalize","title":"function finalize","text":"<pre><code>inline void dynampi::HierarchicalMPIWorkDistributor::finalize () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalMPIWorkDistributor/#function-finish_remaining_tasks","title":"function finish_remaining_tasks","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::HierarchicalMPIWorkDistributor::finish_remaining_tasks () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalMPIWorkDistributor/#function-get_statistics","title":"function get_statistics","text":"<pre><code>inline const StatisticsT &amp; dynampi::HierarchicalMPIWorkDistributor::get_statistics () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalMPIWorkDistributor/#function-insert_task-12","title":"function insert_task [1/2]","text":"<pre><code>inline void dynampi::HierarchicalMPIWorkDistributor::insert_task (\n    TaskT task\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalMPIWorkDistributor/#function-insert_task-22","title":"function insert_task [2/2]","text":"<pre><code>inline void dynampi::HierarchicalMPIWorkDistributor::insert_task (\n    const TaskT &amp; task,\n    double priority\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalMPIWorkDistributor/#function-insert_tasks-12","title":"function insert_tasks [1/2]","text":"<pre><code>template&lt;typename Range&gt;\ninline void dynampi::HierarchicalMPIWorkDistributor::insert_tasks (\n    const Range &amp; tasks\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalMPIWorkDistributor/#function-insert_tasks-22","title":"function insert_tasks [2/2]","text":"<pre><code>inline void dynampi::HierarchicalMPIWorkDistributor::insert_tasks (\n    const std::vector&lt; TaskT &gt; &amp; tasks\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalMPIWorkDistributor/#function-is_root_manager","title":"function is_root_manager","text":"<pre><code>inline bool dynampi::HierarchicalMPIWorkDistributor::is_root_manager () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalMPIWorkDistributor/#function-remaining_tasks_count","title":"function remaining_tasks_count","text":"<pre><code>inline size_t dynampi::HierarchicalMPIWorkDistributor::remaining_tasks_count () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalMPIWorkDistributor/#function-run_tasks","title":"function run_tasks","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::HierarchicalMPIWorkDistributor::run_tasks (\n    const RunConfig &amp; config=RunConfig {}\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalMPIWorkDistributor/#function-run_worker","title":"function run_worker","text":"<pre><code>inline void dynampi::HierarchicalMPIWorkDistributor::run_worker () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalMPIWorkDistributor/#function-send_results_to_parent","title":"function send_results_to_parent","text":"<pre><code>inline void dynampi::HierarchicalMPIWorkDistributor::send_results_to_parent () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalMPIWorkDistributor/#function-hierarchicalmpiworkdistributor_1","title":"function ~HierarchicalMPIWorkDistributor","text":"<pre><code>inline dynampi::HierarchicalMPIWorkDistributor::~HierarchicalMPIWorkDistributor () \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1Config/","title":"Struct dynampi::HierarchicalMPIWorkDistributor::Config","text":"<p>ClassList &gt; dynampi &gt; HierarchicalMPIWorkDistributor &gt; Config</p> <ul> <li><code>#include &lt;hierarchical_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1Config/#public-attributes","title":"Public Attributes","text":"Type Name bool auto_run_workers   = <code>true</code> int batch_size_multiplier   = <code>2</code> MPI_Comm comm   = <code>MPI\\_COMM\\_WORLD</code> bool coordinator_per_node   = <code>true</code> int manager_rank   = <code>0</code> std::optional&lt; int &gt; max_workers_per_coordinator   = <code>std::nullopt</code> std::optional&lt; size_t &gt; message_batch_size   = <code>std::nullopt</code> int pipeline_depth   = <code>2</code>"},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1Config/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1Config/#variable-auto_run_workers","title":"variable auto_run_workers","text":"<pre><code>bool dynampi::HierarchicalMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::auto_run_workers;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1Config/#variable-batch_size_multiplier","title":"variable batch_size_multiplier","text":"<pre><code>int dynampi::HierarchicalMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::batch_size_multiplier;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1Config/#variable-comm","title":"variable comm","text":"<pre><code>MPI_Comm dynampi::HierarchicalMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::comm;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1Config/#variable-coordinator_per_node","title":"variable coordinator_per_node","text":"<pre><code>bool dynampi::HierarchicalMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::coordinator_per_node;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1Config/#variable-manager_rank","title":"variable manager_rank","text":"<pre><code>int dynampi::HierarchicalMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::manager_rank;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1Config/#variable-max_workers_per_coordinator","title":"variable max_workers_per_coordinator","text":"<pre><code>std::optional&lt;int&gt; dynampi::HierarchicalMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_workers_per_coordinator;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1Config/#variable-message_batch_size","title":"variable message_batch_size","text":"<pre><code>std::optional&lt;size_t&gt; dynampi::HierarchicalMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::message_batch_size;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1Config/#variable-pipeline_depth","title":"variable pipeline_depth","text":"<pre><code>int dynampi::HierarchicalMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::pipeline_depth;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1RunConfig/","title":"Struct dynampi::HierarchicalMPIWorkDistributor::RunConfig","text":"<p>ClassList &gt; dynampi &gt; HierarchicalMPIWorkDistributor &gt; RunConfig</p> <ul> <li><code>#include &lt;hierarchical_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1RunConfig/#public-attributes","title":"Public Attributes","text":"Type Name bool allow_more_than_target_tasks   = <code>true</code> std::optional&lt; double &gt; max_seconds   = <code>std::nullopt</code> size_t target_num_tasks   = <code>std::numeric\\_limits&amp;lt;size\\_t&amp;gt;::max()</code>"},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1RunConfig/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1RunConfig/#variable-allow_more_than_target_tasks","title":"variable allow_more_than_target_tasks","text":"<pre><code>bool dynampi::HierarchicalMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::allow_more_than_target_tasks;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1RunConfig/#variable-max_seconds","title":"variable max_seconds","text":"<pre><code>std::optional&lt;double&gt; dynampi::HierarchicalMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::max_seconds;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1RunConfig/#variable-target_num_tasks","title":"variable target_num_tasks","text":"<pre><code>size_t dynampi::HierarchicalMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::target_num_tasks;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_distributor.hpp</code></p>"},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor/","title":"Class dynampi::HierarchicalNonBlockingMPIWorkDistributor","text":"<p>template &lt;typename TaskT, typename ResultT, typename... Options&gt;</p> <p>ClassList &gt; dynampi &gt; HierarchicalNonBlockingMPIWorkDistributor</p> <ul> <li><code>#include &lt;hierarchical_nonblocking_distributor.hpp&gt;</code></li> </ul> <p>Inherits the following classes: dynampi::BaseMPIWorkDistributor</p>"},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor/#classes","title":"Classes","text":"Type Name struct Config struct RunConfig"},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor/#public-static-attributes","title":"Public Static Attributes","text":"Type Name const bool ordered   = <code>false</code> constexpr bool prioritize_tasks   = <code>Base::prioritize\\_tasks</code>"},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor/#public-functions","title":"Public Functions","text":"Type Name HierarchicalNonBlockingMPIWorkDistributor (std::function&lt; ResultT(TaskT)&gt; worker_function, Config runtime_config=Config{})  void allocate_task_to_child ()  void finalize ()  std::vector&lt; ResultT &gt; finish_remaining_tasks ()  const StatisticsT &amp; get_statistics () const void insert_task (TaskT task)  void insert_task (const TaskT &amp; task, double priority)  void insert_tasks (const Range &amp; tasks)  void insert_tasks (const std::vector&lt; TaskT &gt; &amp; tasks)  bool is_root_manager () const size_t remaining_tasks_count () const std::vector&lt; ResultT &gt; run_tasks (const RunConfig &amp; config=RunConfig{})  void run_worker ()  void send_results_to_parent ()  ~HierarchicalNonBlockingMPIWorkDistributor ()"},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor/#protected-types-inherited-from-dynampibasempiworkdistributor","title":"Protected Types inherited from dynampi::BaseMPIWorkDistributor","text":"<p>See dynampi::BaseMPIWorkDistributor</p> Type Name typedef std::conditional_t&lt; prioritize_tasks, std::priority_queue&lt; std::pair&lt; double, TaskT &gt; &gt;, std::deque&lt; TaskT &gt; &gt; QueueT"},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor/#protected-static-attributes-inherited-from-dynampibasempiworkdistributor","title":"Protected Static Attributes inherited from dynampi::BaseMPIWorkDistributor","text":"<p>See dynampi::BaseMPIWorkDistributor</p> Type Name constexpr bool prioritize_tasks   = <code>[**get\\_option\\_value**](template__options_8hpp.md#function-get_option_value)&amp;lt;[**prioritize\\_tasks\\_t**](structdynampi_1_1prioritize__tasks__t.md), Options...&amp;gt;()</code>"},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor/#variable-ordered","title":"variable ordered","text":"<pre><code>const bool dynampi::HierarchicalNonBlockingMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::ordered;\n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor/#variable-prioritize_tasks","title":"variable prioritize_tasks","text":"<pre><code>constexpr bool dynampi::HierarchicalNonBlockingMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::prioritize_tasks;\n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor/#function-hierarchicalnonblockingmpiworkdistributor","title":"function HierarchicalNonBlockingMPIWorkDistributor","text":"<pre><code>inline explicit dynampi::HierarchicalNonBlockingMPIWorkDistributor::HierarchicalNonBlockingMPIWorkDistributor (\n    std::function&lt; ResultT(TaskT)&gt; worker_function,\n    Config runtime_config=Config {}\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor/#function-allocate_task_to_child","title":"function allocate_task_to_child","text":"<pre><code>inline void dynampi::HierarchicalNonBlockingMPIWorkDistributor::allocate_task_to_child () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor/#function-finalize","title":"function finalize","text":"<pre><code>inline void dynampi::HierarchicalNonBlockingMPIWorkDistributor::finalize () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor/#function-finish_remaining_tasks","title":"function finish_remaining_tasks","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::HierarchicalNonBlockingMPIWorkDistributor::finish_remaining_tasks () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor/#function-get_statistics","title":"function get_statistics","text":"<pre><code>inline const StatisticsT &amp; dynampi::HierarchicalNonBlockingMPIWorkDistributor::get_statistics () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor/#function-insert_task-12","title":"function insert_task [1/2]","text":"<pre><code>inline void dynampi::HierarchicalNonBlockingMPIWorkDistributor::insert_task (\n    TaskT task\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor/#function-insert_task-22","title":"function insert_task [2/2]","text":"<pre><code>inline void dynampi::HierarchicalNonBlockingMPIWorkDistributor::insert_task (\n    const TaskT &amp; task,\n    double priority\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor/#function-insert_tasks-12","title":"function insert_tasks [1/2]","text":"<pre><code>template&lt;typename Range&gt;\ninline void dynampi::HierarchicalNonBlockingMPIWorkDistributor::insert_tasks (\n    const Range &amp; tasks\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor/#function-insert_tasks-22","title":"function insert_tasks [2/2]","text":"<pre><code>inline void dynampi::HierarchicalNonBlockingMPIWorkDistributor::insert_tasks (\n    const std::vector&lt; TaskT &gt; &amp; tasks\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor/#function-is_root_manager","title":"function is_root_manager","text":"<pre><code>inline bool dynampi::HierarchicalNonBlockingMPIWorkDistributor::is_root_manager () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor/#function-remaining_tasks_count","title":"function remaining_tasks_count","text":"<pre><code>inline size_t dynampi::HierarchicalNonBlockingMPIWorkDistributor::remaining_tasks_count () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor/#function-run_tasks","title":"function run_tasks","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::HierarchicalNonBlockingMPIWorkDistributor::run_tasks (\n    const RunConfig &amp; config=RunConfig {}\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor/#function-run_worker","title":"function run_worker","text":"<pre><code>inline void dynampi::HierarchicalNonBlockingMPIWorkDistributor::run_worker () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor/#function-send_results_to_parent","title":"function send_results_to_parent","text":"<pre><code>inline void dynampi::HierarchicalNonBlockingMPIWorkDistributor::send_results_to_parent () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor/#function-hierarchicalnonblockingmpiworkdistributor_1","title":"function ~HierarchicalNonBlockingMPIWorkDistributor","text":"<pre><code>inline dynampi::HierarchicalNonBlockingMPIWorkDistributor::~HierarchicalNonBlockingMPIWorkDistributor () \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_nonblocking_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1Config/","title":"Struct dynampi::HierarchicalNonBlockingMPIWorkDistributor::Config","text":"<p>ClassList &gt; dynampi &gt; HierarchicalNonBlockingMPIWorkDistributor &gt; Config</p> <ul> <li><code>#include &lt;hierarchical_nonblocking_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1Config/#public-attributes","title":"Public Attributes","text":"Type Name bool auto_run_workers   = <code>true</code> int batch_size_multiplier   = <code>2</code> MPI_Comm comm   = <code>MPI\\_COMM\\_WORLD</code> bool coordinator_per_node   = <code>true</code> int manager_rank   = <code>0</code> std::optional&lt; int &gt; max_workers_per_coordinator   = <code>std::nullopt</code> std::optional&lt; size_t &gt; message_batch_size   = <code>std::nullopt</code> int pipeline_depth   = <code>2</code>"},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1Config/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1Config/#variable-auto_run_workers","title":"variable auto_run_workers","text":"<pre><code>bool dynampi::HierarchicalNonBlockingMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::auto_run_workers;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1Config/#variable-batch_size_multiplier","title":"variable batch_size_multiplier","text":"<pre><code>int dynampi::HierarchicalNonBlockingMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::batch_size_multiplier;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1Config/#variable-comm","title":"variable comm","text":"<pre><code>MPI_Comm dynampi::HierarchicalNonBlockingMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::comm;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1Config/#variable-coordinator_per_node","title":"variable coordinator_per_node","text":"<pre><code>bool dynampi::HierarchicalNonBlockingMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::coordinator_per_node;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1Config/#variable-manager_rank","title":"variable manager_rank","text":"<pre><code>int dynampi::HierarchicalNonBlockingMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::manager_rank;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1Config/#variable-max_workers_per_coordinator","title":"variable max_workers_per_coordinator","text":"<pre><code>std::optional&lt;int&gt; dynampi::HierarchicalNonBlockingMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_workers_per_coordinator;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1Config/#variable-message_batch_size","title":"variable message_batch_size","text":"<pre><code>std::optional&lt;size_t&gt; dynampi::HierarchicalNonBlockingMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::message_batch_size;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1Config/#variable-pipeline_depth","title":"variable pipeline_depth","text":"<pre><code>int dynampi::HierarchicalNonBlockingMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::pipeline_depth;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_nonblocking_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1RunConfig/","title":"Struct dynampi::HierarchicalNonBlockingMPIWorkDistributor::RunConfig","text":"<p>ClassList &gt; dynampi &gt; HierarchicalNonBlockingMPIWorkDistributor &gt; RunConfig</p> <ul> <li><code>#include &lt;hierarchical_nonblocking_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1RunConfig/#public-attributes","title":"Public Attributes","text":"Type Name bool allow_more_than_target_tasks   = <code>true</code> std::optional&lt; double &gt; max_seconds   = <code>std::nullopt</code> size_t target_num_tasks   = <code>std::numeric\\_limits&amp;lt;size\\_t&amp;gt;::max()</code>"},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1RunConfig/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1RunConfig/#variable-allow_more_than_target_tasks","title":"variable allow_more_than_target_tasks","text":"<pre><code>bool dynampi::HierarchicalNonBlockingMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::allow_more_than_target_tasks;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1RunConfig/#variable-max_seconds","title":"variable max_seconds","text":"<pre><code>std::optional&lt;double&gt; dynampi::HierarchicalNonBlockingMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::max_seconds;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1RunConfig/#variable-target_num_tasks","title":"variable target_num_tasks","text":"<pre><code>size_t dynampi::HierarchicalNonBlockingMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::target_num_tasks;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_nonblocking_distributor.hpp</code></p>"},{"location":"dynampi/classdynampi_1_1LockFreeMPIWorkDistributor/","title":"Class dynampi::LockFreeMPIWorkDistributor","text":"<p>template &lt;typename TaskT, typename ResultT, typename... Options&gt;</p> <p>ClassList &gt; dynampi &gt; LockFreeMPIWorkDistributor</p> <ul> <li><code>#include &lt;lockfree_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/classdynampi_1_1LockFreeMPIWorkDistributor/#classes","title":"Classes","text":"Type Name struct Config struct RunConfig struct Statistics"},{"location":"dynampi/classdynampi_1_1LockFreeMPIWorkDistributor/#public-types","title":"Public Types","text":"Type Name typedef std::conditional_t&lt; statistics_mode==StatisticsMode::Detailed, Statistics, std::monostate &gt; StatisticsT"},{"location":"dynampi/classdynampi_1_1LockFreeMPIWorkDistributor/#public-static-attributes","title":"Public Static Attributes","text":"Type Name const bool ordered   = <code>true</code>"},{"location":"dynampi/classdynampi_1_1LockFreeMPIWorkDistributor/#public-functions","title":"Public Functions","text":"Type Name LockFreeMPIWorkDistributor (std::function&lt; ResultT(TaskT)&gt; worker_function, Config config={})  void finalize ()  std::vector&lt; ResultT &gt; finish_remaining_tasks ()  std::vector&lt; ResultT &gt; gather_once ()  const StatisticsT &amp; get_statistics () const void insert_task (TaskT task)  void insert_task (const TaskT &amp; task, double)  void insert_tasks (const std::vector&lt; TaskT &gt; &amp; tasks)  bool is_root_manager () const size_t remaining_tasks_count () const std::vector&lt; ResultT &gt; run_tasks (RunConfig config={})  void run_worker ()  ~LockFreeMPIWorkDistributor ()"},{"location":"dynampi/classdynampi_1_1LockFreeMPIWorkDistributor/#public-types-documentation","title":"Public Types Documentation","text":""},{"location":"dynampi/classdynampi_1_1LockFreeMPIWorkDistributor/#typedef-statisticst","title":"typedef StatisticsT","text":"<pre><code>using dynampi::LockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::StatisticsT =  std::conditional_t&lt;statistics_mode == StatisticsMode::Detailed, Statistics, std::monostate&gt;;\n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeMPIWorkDistributor/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/classdynampi_1_1LockFreeMPIWorkDistributor/#variable-ordered","title":"variable ordered","text":"<pre><code>const bool dynampi::LockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::ordered;\n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeMPIWorkDistributor/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/classdynampi_1_1LockFreeMPIWorkDistributor/#function-lockfreempiworkdistributor","title":"function LockFreeMPIWorkDistributor","text":"<pre><code>inline explicit dynampi::LockFreeMPIWorkDistributor::LockFreeMPIWorkDistributor (\n    std::function&lt; ResultT(TaskT)&gt; worker_function,\n    Config config={}\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeMPIWorkDistributor/#function-finalize","title":"function finalize","text":"<pre><code>inline void dynampi::LockFreeMPIWorkDistributor::finalize () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeMPIWorkDistributor/#function-finish_remaining_tasks","title":"function finish_remaining_tasks","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::LockFreeMPIWorkDistributor::finish_remaining_tasks () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeMPIWorkDistributor/#function-gather_once","title":"function gather_once","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::LockFreeMPIWorkDistributor::gather_once () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeMPIWorkDistributor/#function-get_statistics","title":"function get_statistics","text":"<pre><code>inline const StatisticsT &amp; dynampi::LockFreeMPIWorkDistributor::get_statistics () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeMPIWorkDistributor/#function-insert_task-12","title":"function insert_task [1/2]","text":"<pre><code>inline void dynampi::LockFreeMPIWorkDistributor::insert_task (\n    TaskT task\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeMPIWorkDistributor/#function-insert_task-22","title":"function insert_task [2/2]","text":"<pre><code>inline void dynampi::LockFreeMPIWorkDistributor::insert_task (\n    const TaskT &amp; task,\n    double\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeMPIWorkDistributor/#function-insert_tasks","title":"function insert_tasks","text":"<pre><code>inline void dynampi::LockFreeMPIWorkDistributor::insert_tasks (\n    const std::vector&lt; TaskT &gt; &amp; tasks\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeMPIWorkDistributor/#function-is_root_manager","title":"function is_root_manager","text":"<pre><code>inline bool dynampi::LockFreeMPIWorkDistributor::is_root_manager () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeMPIWorkDistributor/#function-remaining_tasks_count","title":"function remaining_tasks_count","text":"<pre><code>inline size_t dynampi::LockFreeMPIWorkDistributor::remaining_tasks_count () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeMPIWorkDistributor/#function-run_tasks","title":"function run_tasks","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::LockFreeMPIWorkDistributor::run_tasks (\n    RunConfig config={}\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeMPIWorkDistributor/#function-run_worker","title":"function run_worker","text":"<pre><code>inline void dynampi::LockFreeMPIWorkDistributor::run_worker () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeMPIWorkDistributor/#function-lockfreempiworkdistributor_1","title":"function ~LockFreeMPIWorkDistributor","text":"<pre><code>inline dynampi::LockFreeMPIWorkDistributor::~LockFreeMPIWorkDistributor () \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/lockfree_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1LockFreeMPIWorkDistributor_1_1Config/","title":"Struct dynampi::LockFreeMPIWorkDistributor::Config","text":"<p>ClassList &gt; dynampi &gt; LockFreeMPIWorkDistributor &gt; Config</p> <ul> <li><code>#include &lt;lockfree_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1LockFreeMPIWorkDistributor_1_1Config/#public-attributes","title":"Public Attributes","text":"Type Name bool auto_run_workers   = <code>true</code> int claim_batch_size   = <code>8</code> MPI_Comm comm   = <code>MPI\\_COMM\\_WORLD</code> int manager_rank   = <code>0</code> int max_result_count   = <code>256</code> int max_task_count   = <code>256</code> int max_tasks   = <code>8192</code>"},{"location":"dynampi/structdynampi_1_1LockFreeMPIWorkDistributor_1_1Config/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1LockFreeMPIWorkDistributor_1_1Config/#variable-auto_run_workers","title":"variable auto_run_workers","text":"<pre><code>bool dynampi::LockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::auto_run_workers;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1LockFreeMPIWorkDistributor_1_1Config/#variable-claim_batch_size","title":"variable claim_batch_size","text":"<pre><code>int dynampi::LockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::claim_batch_size;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1LockFreeMPIWorkDistributor_1_1Config/#variable-comm","title":"variable comm","text":"<pre><code>MPI_Comm dynampi::LockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::comm;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1LockFreeMPIWorkDistributor_1_1Config/#variable-manager_rank","title":"variable manager_rank","text":"<pre><code>int dynampi::LockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::manager_rank;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1LockFreeMPIWorkDistributor_1_1Config/#variable-max_result_count","title":"variable max_result_count","text":"<pre><code>int dynampi::LockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_result_count;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1LockFreeMPIWorkDistributor_1_1Config/#variable-max_task_count","title":"variable max_task_count","text":"<pre><code>int dynampi::LockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_task_count;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1LockFreeMPIWorkDistributor_1_1Config/#variable-max_tasks","title":"variable max_tasks","text":"<pre><code>int dynampi::LockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_tasks;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/lockfree_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1LockFreeMPIWorkDistributor_1_1RunConfig/","title":"Struct dynampi::LockFreeMPIWorkDistributor::RunConfig","text":"<p>ClassList &gt; dynampi &gt; LockFreeMPIWorkDistributor &gt; RunConfig</p> <ul> <li><code>#include &lt;lockfree_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1LockFreeMPIWorkDistributor_1_1RunConfig/#public-attributes","title":"Public Attributes","text":"Type Name bool allow_more_than_target_tasks   = <code>true</code> std::optional&lt; double &gt; max_seconds   = <code>std::nullopt</code> size_t target_num_tasks   = <code>std::numeric\\_limits&amp;lt;size\\_t&amp;gt;::max()</code>"},{"location":"dynampi/structdynampi_1_1LockFreeMPIWorkDistributor_1_1RunConfig/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1LockFreeMPIWorkDistributor_1_1RunConfig/#variable-allow_more_than_target_tasks","title":"variable allow_more_than_target_tasks","text":"<pre><code>bool dynampi::LockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::allow_more_than_target_tasks;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1LockFreeMPIWorkDistributor_1_1RunConfig/#variable-max_seconds","title":"variable max_seconds","text":"<pre><code>std::optional&lt;double&gt; dynampi::LockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::max_seconds;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1LockFreeMPIWorkDistributor_1_1RunConfig/#variable-target_num_tasks","title":"variable target_num_tasks","text":"<pre><code>size_t dynampi::LockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::target_num_tasks;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/lockfree_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1LockFreeMPIWorkDistributor_1_1Statistics/","title":"Struct dynampi::LockFreeMPIWorkDistributor::Statistics","text":"<p>ClassList &gt; dynampi &gt; LockFreeMPIWorkDistributor &gt; Statistics</p> <ul> <li><code>#include &lt;lockfree_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1LockFreeMPIWorkDistributor_1_1Statistics/#public-attributes","title":"Public Attributes","text":"Type Name CommStatistics comm_statistics std::vector&lt; size_t &gt; worker_task_counts"},{"location":"dynampi/structdynampi_1_1LockFreeMPIWorkDistributor_1_1Statistics/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1LockFreeMPIWorkDistributor_1_1Statistics/#variable-comm_statistics","title":"variable comm_statistics","text":"<pre><code>CommStatistics dynampi::LockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Statistics::comm_statistics;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1LockFreeMPIWorkDistributor_1_1Statistics/#variable-worker_task_counts","title":"variable worker_task_counts","text":"<pre><code>std::vector&lt;size_t&gt; dynampi::LockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Statistics::worker_task_counts;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/lockfree_distributor.hpp</code></p>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/","title":"Class dynampi::MPICommunicator","text":"<p>template &lt;typename... Options&gt;</p> <p>ClassList &gt; dynampi &gt; MPICommunicator</p> <ul> <li><code>#include &lt;mpi_communicator.hpp&gt;</code></li> </ul>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#public-types","title":"Public Types","text":"Type Name enum Ownership"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#public-functions","title":"Public Functions","text":"Type Name MPICommunicator (MPI_Comm comm, Ownership ownership=Duplicate)  MPICommunicator (const MPICommunicator &amp; other) = delete MPICommunicator (MPICommunicator &amp;&amp; other) noexcept void broadcast (T &amp; data, int root=0)  void gather (const T &amp; data, std::vector&lt; T &gt; * result, int root=0)  MPI_Comm get () const MPIGroup get_group () const const CommStatistics &amp; get_statistics () const std::optional&lt; MPI_Status &gt; iprobe (int source=MPI_ANY_SOURCE, int tag=MPI_ANY_TAG)  operator MPI_Comm () const MPICommunicator &amp; operator= (const MPICommunicator &amp; other) = delete MPICommunicator &amp; operator= (MPICommunicator &amp;&amp; other) = delete MPI_Status probe (int source=MPI_ANY_SOURCE, int tag=MPI_ANY_TAG)  int rank () const void recv (T &amp; data, int source, int tag=0)  MPI_Status recv_any (T &amp; data, int source=MPI_ANY_SOURCE, int tag=MPI_ANY_TAG)  void recv_empty (int source, int tag=0) Receives 0 elements of type T. Use when the sender used send_empty&lt;T&gt;. void recv_empty_message (int source, int tag=0)  void send (const T &amp; data, int dest, int tag=0)  void send_empty (int dest, int tag=0)  int size () const std::optional&lt; MPICommunicator &gt; split (int color, int key=0) const MPICommunicator split_by_node () const ~MPICommunicator ()"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#public-types-documentation","title":"Public Types Documentation","text":""},{"location":"dynampi/classdynampi_1_1MPICommunicator/#enum-ownership","title":"enum Ownership","text":"<pre><code>enum dynampi::MPICommunicator::Ownership {\n    Reference,\n    Move,\n    Duplicate\n};\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-mpicommunicator-13","title":"function MPICommunicator [1/3]","text":"<pre><code>inline dynampi::MPICommunicator::MPICommunicator (\n    MPI_Comm comm,\n    Ownership ownership=Duplicate\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-mpicommunicator-23","title":"function MPICommunicator [2/3]","text":"<pre><code>dynampi::MPICommunicator::MPICommunicator (\n    const MPICommunicator &amp; other\n) = delete\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-mpicommunicator-33","title":"function MPICommunicator [3/3]","text":"<pre><code>inline dynampi::MPICommunicator::MPICommunicator (\n    MPICommunicator &amp;&amp; other\n) noexcept\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-broadcast","title":"function broadcast","text":"<pre><code>template&lt;typename T&gt;\ninline void dynampi::MPICommunicator::broadcast (\n    T &amp; data,\n    int root=0\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-gather","title":"function gather","text":"<pre><code>template&lt;typename T&gt;\ninline void dynampi::MPICommunicator::gather (\n    const T &amp; data,\n    std::vector&lt; T &gt; * result,\n    int root=0\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-get","title":"function get","text":"<pre><code>inline MPI_Comm dynampi::MPICommunicator::get () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-get_group","title":"function get_group","text":"<pre><code>inline MPIGroup dynampi::MPICommunicator::get_group () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-get_statistics","title":"function get_statistics","text":"<pre><code>inline const CommStatistics &amp; dynampi::MPICommunicator::get_statistics () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-iprobe","title":"function iprobe","text":"<pre><code>inline std::optional&lt; MPI_Status &gt; dynampi::MPICommunicator::iprobe (\n    int source=MPI_ANY_SOURCE,\n    int tag=MPI_ANY_TAG\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-operator-mpi_comm","title":"function operator MPI_Comm","text":"<pre><code>inline dynampi::MPICommunicator::operator MPI_Comm () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-operator","title":"function operator=","text":"<pre><code>MPICommunicator &amp; dynampi::MPICommunicator::operator= (\n    const MPICommunicator &amp; other\n) = delete\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-operator_1","title":"function operator=","text":"<pre><code>MPICommunicator &amp; dynampi::MPICommunicator::operator= (\n    MPICommunicator &amp;&amp; other\n) = delete\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-probe","title":"function probe","text":"<pre><code>inline MPI_Status dynampi::MPICommunicator::probe (\n    int source=MPI_ANY_SOURCE,\n    int tag=MPI_ANY_TAG\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-rank","title":"function rank","text":"<pre><code>inline int dynampi::MPICommunicator::rank () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-recv","title":"function recv","text":"<pre><code>template&lt;typename T&gt;\ninline void dynampi::MPICommunicator::recv (\n    T &amp; data,\n    int source,\n    int tag=0\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-recv_any","title":"function recv_any","text":"<pre><code>template&lt;typename T&gt;\ninline MPI_Status dynampi::MPICommunicator::recv_any (\n    T &amp; data,\n    int source=MPI_ANY_SOURCE,\n    int tag=MPI_ANY_TAG\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-recv_empty","title":"function recv_empty","text":"<p>Receives 0 elements of type T. Use when the sender used send_empty&lt;T&gt;. <pre><code>template&lt;typename T&gt;\ninline void dynampi::MPICommunicator::recv_empty (\n    int source,\n    int tag=0\n) \n</code></pre></p>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-recv_empty_message","title":"function recv_empty_message","text":"<pre><code>inline void dynampi::MPICommunicator::recv_empty_message (\n    int source,\n    int tag=0\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-send","title":"function send","text":"<pre><code>template&lt;typename T&gt;\ninline void dynampi::MPICommunicator::send (\n    const T &amp; data,\n    int dest,\n    int tag=0\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-send_empty","title":"function send_empty","text":"<pre><code>template&lt;typename T&gt;\ninline void dynampi::MPICommunicator::send_empty (\n    int dest,\n    int tag=0\n) \n</code></pre> <p>Sends 0 elements of type T (same type as recv buffer) so that recv_any(T&amp;) can receive any worker message (REQUEST or RESULT) into a single buffer type. </p>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-size","title":"function size","text":"<pre><code>inline int dynampi::MPICommunicator::size () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-split","title":"function split","text":"<pre><code>inline std::optional&lt; MPICommunicator &gt; dynampi::MPICommunicator::split (\n    int color,\n    int key=0\n) const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-split_by_node","title":"function split_by_node","text":"<pre><code>inline MPICommunicator dynampi::MPICommunicator::split_by_node () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-mpicommunicator","title":"function ~MPICommunicator","text":"<pre><code>inline dynampi::MPICommunicator::~MPICommunicator () \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_communicator.hpp</code></p>"},{"location":"dynampi/classdynampi_1_1MPIGroup/","title":"Class dynampi::MPIGroup","text":"<p>ClassList &gt; dynampi &gt; MPIGroup</p> <ul> <li><code>#include &lt;mpi_group.hpp&gt;</code></li> </ul>"},{"location":"dynampi/classdynampi_1_1MPIGroup/#public-functions","title":"Public Functions","text":"Type Name MPIGroup (const MPICommunicator&lt; Options... &gt; &amp; comm)  MPIGroup (const MPIGroup &amp; other) = delete MPIGroup (MPIGroup &amp;&amp; other) noexcept MPI_Group get () const operator MPI_Group () const MPIGroup &amp; operator= (const MPIGroup &amp; other) = delete MPIGroup &amp; operator= (MPIGroup &amp;&amp; other) noexcept int rank () const int size () const int translate_rank (int rank, const MPIGroup &amp; to_group) const void translate_ranks (const MPIGroup &amp; to_group, int n, const int ranks, int translated_ranks) const ~MPIGroup ()"},{"location":"dynampi/classdynampi_1_1MPIGroup/#public-static-functions","title":"Public Static Functions","text":"Type Name int contains_rank_in_group (int rank_in_reference, const MPIGroup &amp; reference_group, const MPIGroup &amp; target_group)"},{"location":"dynampi/classdynampi_1_1MPIGroup/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/classdynampi_1_1MPIGroup/#function-mpigroup-13","title":"function MPIGroup [1/3]","text":"<pre><code>template&lt;typename... Options&gt;\ninline explicit dynampi::MPIGroup::MPIGroup (\n    const MPICommunicator &lt; Options... &gt; &amp; comm\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPIGroup/#function-mpigroup-23","title":"function MPIGroup [2/3]","text":"<pre><code>dynampi::MPIGroup::MPIGroup (\n    const MPIGroup &amp; other\n) = delete\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPIGroup/#function-mpigroup-33","title":"function MPIGroup [3/3]","text":"<pre><code>inline dynampi::MPIGroup::MPIGroup (\n    MPIGroup &amp;&amp; other\n) noexcept\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPIGroup/#function-get","title":"function get","text":"<pre><code>inline MPI_Group dynampi::MPIGroup::get () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPIGroup/#function-operator-mpi_group","title":"function operator MPI_Group","text":"<pre><code>inline dynampi::MPIGroup::operator MPI_Group () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPIGroup/#function-operator","title":"function operator=","text":"<pre><code>MPIGroup &amp; dynampi::MPIGroup::operator= (\n    const MPIGroup &amp; other\n) = delete\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPIGroup/#function-operator_1","title":"function operator=","text":"<pre><code>inline MPIGroup &amp; dynampi::MPIGroup::operator= (\n    MPIGroup &amp;&amp; other\n) noexcept\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPIGroup/#function-rank","title":"function rank","text":"<pre><code>inline int dynampi::MPIGroup::rank () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPIGroup/#function-size","title":"function size","text":"<pre><code>inline int dynampi::MPIGroup::size () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPIGroup/#function-translate_rank","title":"function translate_rank","text":"<pre><code>inline int dynampi::MPIGroup::translate_rank (\n    int rank,\n    const MPIGroup &amp; to_group\n) const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPIGroup/#function-translate_ranks","title":"function translate_ranks","text":"<pre><code>inline void dynampi::MPIGroup::translate_ranks (\n    const MPIGroup &amp; to_group,\n    int n,\n    const int ranks,\n    int translated_ranks\n) const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPIGroup/#function-mpigroup","title":"function ~MPIGroup","text":"<pre><code>inline dynampi::MPIGroup::~MPIGroup () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPIGroup/#public-static-functions-documentation","title":"Public Static Functions Documentation","text":""},{"location":"dynampi/classdynampi_1_1MPIGroup/#function-contains_rank_in_group","title":"function contains_rank_in_group","text":"<pre><code>static inline int dynampi::MPIGroup::contains_rank_in_group (\n    int rank_in_reference,\n    const MPIGroup &amp; reference_group,\n    const MPIGroup &amp; target_group\n) \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_group.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1MPI__Type/","title":"Struct dynampi::MPI_Type","text":"<p>template &lt;typename T, typename&gt;</p> <p>ClassList &gt; dynampi &gt; MPI_Type</p> <ul> <li><code>#include &lt;mpi_types.hpp&gt;</code></li> </ul> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_types.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1nullptr__t_01_4/","title":"Struct dynampi::MPI_Type&lt; std::nullptr_t &gt;","text":"<p>template &lt;&gt;</p> <p>ClassList &gt; dynampi &gt; MPI_Type&lt; std::nullptr_t &gt;</p> <ul> <li><code>#include &lt;mpi_types.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1nullptr__t_01_4/#public-static-attributes","title":"Public Static Attributes","text":"Type Name const bool resize_required   = <code>false</code> const MPI_Datatype value   = <code>MPI\\_PACKED</code>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1nullptr__t_01_4/#public-static-functions","title":"Public Static Functions","text":"Type Name int count (const std::nullptr_t &amp;) noexcept void * ptr (std::nullptr_t &amp;) noexcept const void * ptr (const std::nullptr_t &amp;) noexcept void resize (std::nullptr_t &amp;, int new_size) noexcept"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1nullptr__t_01_4/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1nullptr__t_01_4/#variable-resize_required","title":"variable resize_required","text":"<pre><code>const bool dynampi::MPI_Type&lt; std::nullptr_t &gt;::resize_required;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1nullptr__t_01_4/#variable-value","title":"variable value","text":"<pre><code>const MPI_Datatype dynampi::MPI_Type&lt; std::nullptr_t &gt;::value;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1nullptr__t_01_4/#public-static-functions-documentation","title":"Public Static Functions Documentation","text":""},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1nullptr__t_01_4/#function-count","title":"function count","text":"<pre><code>static inline int dynampi::MPI_Type&lt; std::nullptr_t &gt;::count (\n    const std::nullptr_t &amp;\n) noexcept\n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1nullptr__t_01_4/#function-ptr-12","title":"function ptr [1/2]","text":"<pre><code>static inline void * dynampi::MPI_Type&lt; std::nullptr_t &gt;::ptr (\n    std::nullptr_t &amp;\n) noexcept\n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1nullptr__t_01_4/#function-ptr-22","title":"function ptr [2/2]","text":"<pre><code>static inline const void * dynampi::MPI_Type&lt; std::nullptr_t &gt;::ptr (\n    const std::nullptr_t &amp;\n) noexcept\n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1nullptr__t_01_4/#function-resize","title":"function resize","text":"<pre><code>static inline void dynampi::MPI_Type&lt; std::nullptr_t &gt;::resize (\n    std::nullptr_t &amp;,\n    int new_size\n) noexcept\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_types.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1string_01_4/","title":"Struct dynampi::MPI_Type&lt; std::string &gt;","text":"<p>template &lt;&gt;</p> <p>ClassList &gt; dynampi &gt; MPI_Type&lt; std::string &gt;</p> <ul> <li><code>#include &lt;mpi_types.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1string_01_4/#public-static-attributes","title":"Public Static Attributes","text":"Type Name const bool resize_required   = <code>true</code> const MPI_Datatype value   = <code>MPI\\_CHAR</code>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1string_01_4/#public-static-functions","title":"Public Static Functions","text":"Type Name int count (const std::string &amp; str)  void * ptr (std::string &amp; str) noexcept const void * ptr (const std::string &amp; str) noexcept void resize (std::string &amp; str, int new_size)"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1string_01_4/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1string_01_4/#variable-resize_required","title":"variable resize_required","text":"<pre><code>const bool dynampi::MPI_Type&lt; std::string &gt;::resize_required;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1string_01_4/#variable-value","title":"variable value","text":"<pre><code>const MPI_Datatype dynampi::MPI_Type&lt; std::string &gt;::value;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1string_01_4/#public-static-functions-documentation","title":"Public Static Functions Documentation","text":""},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1string_01_4/#function-count","title":"function count","text":"<pre><code>static inline int dynampi::MPI_Type&lt; std::string &gt;::count (\n    const std::string &amp; str\n) \n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1string_01_4/#function-ptr-12","title":"function ptr [1/2]","text":"<pre><code>static inline void * dynampi::MPI_Type&lt; std::string &gt;::ptr (\n    std::string &amp; str\n) noexcept\n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1string_01_4/#function-ptr-22","title":"function ptr [2/2]","text":"<pre><code>static inline const void * dynampi::MPI_Type&lt; std::string &gt;::ptr (\n    const std::string &amp; str\n) noexcept\n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1string_01_4/#function-resize","title":"function resize","text":"<pre><code>static inline void dynampi::MPI_Type&lt; std::string &gt;::resize (\n    std::string &amp; str,\n    int new_size\n) \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_types.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1vector_3_01T_01_4_00_01std_1_1enable__if__t_3_01has__dynam0c05b0754f90b71498257126104ee051/","title":"Struct dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;","text":"<p>template &lt;typename T&gt;</p> <p>ClassList &gt; dynampi &gt; MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;</p> <ul> <li><code>#include &lt;mpi_types.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1vector_3_01T_01_4_00_01std_1_1enable__if__t_3_01has__dynam0c05b0754f90b71498257126104ee051/#public-static-attributes","title":"Public Static Attributes","text":"Type Name const bool resize_required   = <code>true</code> const MPI_Datatype value   = <code>[**MPI\\_Type**](structdynampi_1_1MPI__Type.md)&amp;lt;T&amp;gt;::value</code>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1vector_3_01T_01_4_00_01std_1_1enable__if__t_3_01has__dynam0c05b0754f90b71498257126104ee051/#public-static-functions","title":"Public Static Functions","text":"Type Name int count (const std::vector&lt; T &gt; &amp; vec)  void * ptr (std::vector&lt; T &gt; &amp; vec) noexcept const void * ptr (const std::vector&lt; T &gt; &amp; vec) noexcept void resize (std::vector&lt; T &gt; &amp; vec, int new_size)"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1vector_3_01T_01_4_00_01std_1_1enable__if__t_3_01has__dynam0c05b0754f90b71498257126104ee051/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1vector_3_01T_01_4_00_01std_1_1enable__if__t_3_01has__dynam0c05b0754f90b71498257126104ee051/#variable-resize_required","title":"variable resize_required","text":"<pre><code>const bool dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;::resize_required;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1vector_3_01T_01_4_00_01std_1_1enable__if__t_3_01has__dynam0c05b0754f90b71498257126104ee051/#variable-value","title":"variable value","text":"<pre><code>const MPI_Datatype dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;::value;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1vector_3_01T_01_4_00_01std_1_1enable__if__t_3_01has__dynam0c05b0754f90b71498257126104ee051/#public-static-functions-documentation","title":"Public Static Functions Documentation","text":""},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1vector_3_01T_01_4_00_01std_1_1enable__if__t_3_01has__dynam0c05b0754f90b71498257126104ee051/#function-count","title":"function count","text":"<pre><code>static inline int dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;::count (\n    const std::vector&lt; T &gt; &amp; vec\n) \n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1vector_3_01T_01_4_00_01std_1_1enable__if__t_3_01has__dynam0c05b0754f90b71498257126104ee051/#function-ptr-12","title":"function ptr [1/2]","text":"<pre><code>static inline void * dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;::ptr (\n    std::vector&lt; T &gt; &amp; vec\n) noexcept\n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1vector_3_01T_01_4_00_01std_1_1enable__if__t_3_01has__dynam0c05b0754f90b71498257126104ee051/#function-ptr-22","title":"function ptr [2/2]","text":"<pre><code>static inline const void * dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;::ptr (\n    const std::vector&lt; T &gt; &amp; vec\n) noexcept\n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1vector_3_01T_01_4_00_01std_1_1enable__if__t_3_01has__dynam0c05b0754f90b71498257126104ee051/#function-resize","title":"function resize","text":"<pre><code>static inline void dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;::resize (\n    std::vector&lt; T &gt; &amp; vec,\n    int new_size\n) \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_types.hpp</code></p>"},{"location":"dynampi/classdynampi_1_1MinimalLockFreeMPIWorkDistributor/","title":"Class dynampi::MinimalLockFreeMPIWorkDistributor","text":"<p>template &lt;typename ResultT&gt;</p> <p>ClassList &gt; dynampi &gt; MinimalLockFreeMPIWorkDistributor</p> <ul> <li><code>#include &lt;lockfree_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/classdynampi_1_1MinimalLockFreeMPIWorkDistributor/#classes","title":"Classes","text":"Type Name struct Config"},{"location":"dynampi/classdynampi_1_1MinimalLockFreeMPIWorkDistributor/#public-functions","title":"Public Functions","text":"Type Name MinimalLockFreeMPIWorkDistributor (std::function&lt; ResultT(size_t)&gt; worker_function, Config config={})  bool is_root_manager () const std::vector&lt; ResultT &gt; run (size_t n_tasks)  ~MinimalLockFreeMPIWorkDistributor ()"},{"location":"dynampi/classdynampi_1_1MinimalLockFreeMPIWorkDistributor/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/classdynampi_1_1MinimalLockFreeMPIWorkDistributor/#function-minimallockfreempiworkdistributor","title":"function MinimalLockFreeMPIWorkDistributor","text":"<pre><code>inline explicit dynampi::MinimalLockFreeMPIWorkDistributor::MinimalLockFreeMPIWorkDistributor (\n    std::function&lt; ResultT(size_t)&gt; worker_function,\n    Config config={}\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1MinimalLockFreeMPIWorkDistributor/#function-is_root_manager","title":"function is_root_manager","text":"<pre><code>inline bool dynampi::MinimalLockFreeMPIWorkDistributor::is_root_manager () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MinimalLockFreeMPIWorkDistributor/#function-run","title":"function run","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::MinimalLockFreeMPIWorkDistributor::run (\n    size_t n_tasks\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1MinimalLockFreeMPIWorkDistributor/#function-minimallockfreempiworkdistributor_1","title":"function ~MinimalLockFreeMPIWorkDistributor","text":"<pre><code>inline dynampi::MinimalLockFreeMPIWorkDistributor::~MinimalLockFreeMPIWorkDistributor () \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/lockfree_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1MinimalLockFreeMPIWorkDistributor_1_1Config/","title":"Struct dynampi::MinimalLockFreeMPIWorkDistributor::Config","text":"<p>ClassList &gt; dynampi &gt; MinimalLockFreeMPIWorkDistributor &gt; Config</p> <ul> <li><code>#include &lt;lockfree_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1MinimalLockFreeMPIWorkDistributor_1_1Config/#public-attributes","title":"Public Attributes","text":"Type Name MPI_Comm comm   = <code>MPI\\_COMM\\_WORLD</code> int manager_rank   = <code>0</code>"},{"location":"dynampi/structdynampi_1_1MinimalLockFreeMPIWorkDistributor_1_1Config/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1MinimalLockFreeMPIWorkDistributor_1_1Config/#variable-comm","title":"variable comm","text":"<pre><code>MPI_Comm dynampi::MinimalLockFreeMPIWorkDistributor&lt; ResultT &gt;::Config::comm;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1MinimalLockFreeMPIWorkDistributor_1_1Config/#variable-manager_rank","title":"variable manager_rank","text":"<pre><code>int dynampi::MinimalLockFreeMPIWorkDistributor&lt; ResultT &gt;::Config::manager_rank;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/lockfree_distributor.hpp</code></p>"},{"location":"dynampi/classdynampi_1_1NaiveMPIWorkDistributor/","title":"Class dynampi::NaiveMPIWorkDistributor","text":"<p>template &lt;typename TaskT, typename ResultT, typename... Options&gt;</p> <p>ClassList &gt; dynampi &gt; NaiveMPIWorkDistributor</p> <ul> <li><code>#include &lt;naive_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/classdynampi_1_1NaiveMPIWorkDistributor/#classes","title":"Classes","text":"Type Name struct Config struct RunConfig struct Statistics"},{"location":"dynampi/classdynampi_1_1NaiveMPIWorkDistributor/#public-types","title":"Public Types","text":"Type Name typedef std::conditional_t&lt; statistics_mode==StatisticsMode::Detailed, Statistics, std::monostate &gt; StatisticsT"},{"location":"dynampi/classdynampi_1_1NaiveMPIWorkDistributor/#public-static-attributes","title":"Public Static Attributes","text":"Type Name const bool ordered   = <code>true</code>"},{"location":"dynampi/classdynampi_1_1NaiveMPIWorkDistributor/#public-functions","title":"Public Functions","text":"Type Name NaiveMPIWorkDistributor (std::function&lt; ResultT(TaskT)&gt; worker_function, Config runtime_config=Config{})  void finalize ()  std::vector&lt; ResultT &gt; finish_remaining_tasks ()  const StatisticsT &amp; get_statistics () const void insert_task (TaskT task)  void insert_task (const TaskT &amp; task, double priority)  void insert_tasks (const std::vector&lt; TaskT &gt; &amp; tasks)  bool is_root_manager () const size_t remaining_tasks_count () const std::vector&lt; ResultT &gt; run_tasks (RunConfig config=RunConfig{})  void run_worker ()  ~NaiveMPIWorkDistributor ()"},{"location":"dynampi/classdynampi_1_1NaiveMPIWorkDistributor/#public-types-documentation","title":"Public Types Documentation","text":""},{"location":"dynampi/classdynampi_1_1NaiveMPIWorkDistributor/#typedef-statisticst","title":"typedef StatisticsT","text":"<pre><code>using dynampi::NaiveMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::StatisticsT =  std::conditional_t&lt;statistics_mode == StatisticsMode::Detailed, Statistics, std::monostate&gt;;\n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveMPIWorkDistributor/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/classdynampi_1_1NaiveMPIWorkDistributor/#variable-ordered","title":"variable ordered","text":"<pre><code>const bool dynampi::NaiveMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::ordered;\n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveMPIWorkDistributor/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/classdynampi_1_1NaiveMPIWorkDistributor/#function-naivempiworkdistributor","title":"function NaiveMPIWorkDistributor","text":"<pre><code>inline explicit dynampi::NaiveMPIWorkDistributor::NaiveMPIWorkDistributor (\n    std::function&lt; ResultT(TaskT)&gt; worker_function,\n    Config runtime_config=Config {}\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveMPIWorkDistributor/#function-finalize","title":"function finalize","text":"<pre><code>inline void dynampi::NaiveMPIWorkDistributor::finalize () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveMPIWorkDistributor/#function-finish_remaining_tasks","title":"function finish_remaining_tasks","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::NaiveMPIWorkDistributor::finish_remaining_tasks () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveMPIWorkDistributor/#function-get_statistics","title":"function get_statistics","text":"<pre><code>inline const StatisticsT &amp; dynampi::NaiveMPIWorkDistributor::get_statistics () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveMPIWorkDistributor/#function-insert_task-12","title":"function insert_task [1/2]","text":"<pre><code>inline void dynampi::NaiveMPIWorkDistributor::insert_task (\n    TaskT task\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveMPIWorkDistributor/#function-insert_task-22","title":"function insert_task [2/2]","text":"<pre><code>inline void dynampi::NaiveMPIWorkDistributor::insert_task (\n    const TaskT &amp; task,\n    double priority\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveMPIWorkDistributor/#function-insert_tasks","title":"function insert_tasks","text":"<pre><code>inline void dynampi::NaiveMPIWorkDistributor::insert_tasks (\n    const std::vector&lt; TaskT &gt; &amp; tasks\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveMPIWorkDistributor/#function-is_root_manager","title":"function is_root_manager","text":"<pre><code>inline bool dynampi::NaiveMPIWorkDistributor::is_root_manager () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveMPIWorkDistributor/#function-remaining_tasks_count","title":"function remaining_tasks_count","text":"<pre><code>inline size_t dynampi::NaiveMPIWorkDistributor::remaining_tasks_count () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveMPIWorkDistributor/#function-run_tasks","title":"function run_tasks","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::NaiveMPIWorkDistributor::run_tasks (\n    RunConfig config=RunConfig {}\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveMPIWorkDistributor/#function-run_worker","title":"function run_worker","text":"<pre><code>inline void dynampi::NaiveMPIWorkDistributor::run_worker () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveMPIWorkDistributor/#function-naivempiworkdistributor_1","title":"function ~NaiveMPIWorkDistributor","text":"<pre><code>inline dynampi::NaiveMPIWorkDistributor::~NaiveMPIWorkDistributor () \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/naive_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1NaiveMPIWorkDistributor_1_1Config/","title":"Struct dynampi::NaiveMPIWorkDistributor::Config","text":"<p>ClassList &gt; dynampi &gt; NaiveMPIWorkDistributor &gt; Config</p> <ul> <li><code>#include &lt;naive_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1NaiveMPIWorkDistributor_1_1Config/#public-attributes","title":"Public Attributes","text":"Type Name bool auto_run_workers   = <code>true</code> MPI_Comm comm   = <code>MPI\\_COMM\\_WORLD</code> int manager_rank   = <code>0</code> int max_result_size   = <code>1024</code> bool use_immediate_recv   = <code>false</code>"},{"location":"dynampi/structdynampi_1_1NaiveMPIWorkDistributor_1_1Config/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1NaiveMPIWorkDistributor_1_1Config/#variable-auto_run_workers","title":"variable auto_run_workers","text":"<pre><code>bool dynampi::NaiveMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::auto_run_workers;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1NaiveMPIWorkDistributor_1_1Config/#variable-comm","title":"variable comm","text":"<pre><code>MPI_Comm dynampi::NaiveMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::comm;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1NaiveMPIWorkDistributor_1_1Config/#variable-manager_rank","title":"variable manager_rank","text":"<pre><code>int dynampi::NaiveMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::manager_rank;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1NaiveMPIWorkDistributor_1_1Config/#variable-max_result_size","title":"variable max_result_size","text":"<pre><code>int dynampi::NaiveMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_result_size;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1NaiveMPIWorkDistributor_1_1Config/#variable-use_immediate_recv","title":"variable use_immediate_recv","text":"<pre><code>bool dynampi::NaiveMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::use_immediate_recv;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/naive_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1NaiveMPIWorkDistributor_1_1RunConfig/","title":"Struct dynampi::NaiveMPIWorkDistributor::RunConfig","text":"<p>ClassList &gt; dynampi &gt; NaiveMPIWorkDistributor &gt; RunConfig</p> <ul> <li><code>#include &lt;naive_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1NaiveMPIWorkDistributor_1_1RunConfig/#public-attributes","title":"Public Attributes","text":"Type Name bool allow_more_than_target_tasks   = <code>true</code> std::optional&lt; double &gt; max_seconds   = <code>std::nullopt</code> size_t target_num_tasks   = <code>std::numeric\\_limits&amp;lt;size\\_t&amp;gt;::max()</code>"},{"location":"dynampi/structdynampi_1_1NaiveMPIWorkDistributor_1_1RunConfig/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1NaiveMPIWorkDistributor_1_1RunConfig/#variable-allow_more_than_target_tasks","title":"variable allow_more_than_target_tasks","text":"<pre><code>bool dynampi::NaiveMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::allow_more_than_target_tasks;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1NaiveMPIWorkDistributor_1_1RunConfig/#variable-max_seconds","title":"variable max_seconds","text":"<pre><code>std::optional&lt;double&gt; dynampi::NaiveMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::max_seconds;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1NaiveMPIWorkDistributor_1_1RunConfig/#variable-target_num_tasks","title":"variable target_num_tasks","text":"<pre><code>size_t dynampi::NaiveMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::target_num_tasks;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/naive_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1NaiveMPIWorkDistributor_1_1Statistics/","title":"Struct dynampi::NaiveMPIWorkDistributor::Statistics","text":"<p>ClassList &gt; dynampi &gt; NaiveMPIWorkDistributor &gt; Statistics</p> <ul> <li><code>#include &lt;naive_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1NaiveMPIWorkDistributor_1_1Statistics/#public-attributes","title":"Public Attributes","text":"Type Name const CommStatistics &amp; comm_statistics std::vector&lt; size_t &gt; worker_task_counts"},{"location":"dynampi/structdynampi_1_1NaiveMPIWorkDistributor_1_1Statistics/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1NaiveMPIWorkDistributor_1_1Statistics/#variable-comm_statistics","title":"variable comm_statistics","text":"<pre><code>const CommStatistics&amp; dynampi::NaiveMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Statistics::comm_statistics;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1NaiveMPIWorkDistributor_1_1Statistics/#variable-worker_task_counts","title":"variable worker_task_counts","text":"<pre><code>std::vector&lt;size_t&gt; dynampi::NaiveMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Statistics::worker_task_counts;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/naive_distributor.hpp</code></p>"},{"location":"dynampi/classdynampi_1_1Timer/","title":"Class dynampi::Timer","text":"<p>ClassList &gt; dynampi &gt; Timer</p> <ul> <li><code>#include &lt;timer.hpp&gt;</code></li> </ul>"},{"location":"dynampi/classdynampi_1_1Timer/#public-types","title":"Public Types","text":"Type Name enum AutoStart"},{"location":"dynampi/classdynampi_1_1Timer/#public-functions","title":"Public Functions","text":"Type Name Timer (AutoStart auto_start=AutoStart::Yes)  std::chrono::duration&lt; double &gt; elapsed () const void reset (AutoStart auto_start=AutoStart::Yes)  void start ()  std::chrono::duration&lt; double &gt; stop ()"},{"location":"dynampi/classdynampi_1_1Timer/#public-types-documentation","title":"Public Types Documentation","text":""},{"location":"dynampi/classdynampi_1_1Timer/#enum-autostart","title":"enum AutoStart","text":"<pre><code>enum dynampi::Timer::AutoStart {\n    Yes,\n    No\n};\n</code></pre>"},{"location":"dynampi/classdynampi_1_1Timer/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/classdynampi_1_1Timer/#function-timer","title":"function Timer","text":"<pre><code>inline dynampi::Timer::Timer (\n    AutoStart auto_start=AutoStart::Yes\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1Timer/#function-elapsed","title":"function elapsed","text":"<pre><code>inline std::chrono::duration&lt; double &gt; dynampi::Timer::elapsed () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1Timer/#function-reset","title":"function reset","text":"<pre><code>inline void dynampi::Timer::reset (\n    AutoStart auto_start=AutoStart::Yes\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1Timer/#function-start","title":"function start","text":"<pre><code>inline void dynampi::Timer::start () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1Timer/#function-stop","title":"function stop","text":"<pre><code>inline std::chrono::duration&lt; double &gt; dynampi::Timer::stop () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1Timer/#friends-documentation","title":"Friends Documentation","text":""},{"location":"dynampi/classdynampi_1_1Timer/#friend-operator","title":"friend operator&lt;&lt;","text":"<pre><code>inline std::ostream &amp; dynampi::Timer::operator&lt;&lt; (\n    std::ostream &amp; os,\n    const Timer &amp; timer\n) \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/utilities/timer.hpp</code></p>"},{"location":"dynampi/namespacedynampi_1_1detail/","title":"Namespace dynampi::detail","text":"<p>Namespace List &gt; dynampi &gt; detail</p>"},{"location":"dynampi/namespacedynampi_1_1detail/#classes","title":"Classes","text":"Type Name class AsyncPutLevel &lt;typename TaskT, typename ResultT&gt; class LockFreeLevel &lt;typename TaskT, typename ResultT&gt;"},{"location":"dynampi/namespacedynampi_1_1detail/#public-functions","title":"Public Functions","text":"Type Name int mpi_type_size_bytes ()  int64_t read_i64 (const std::byte * buffer, size_t buffer_size, size_t offset)  void read_result_bytes (const std::byte * buffer, size_t buffer_size, size_t offset, T &amp; value, size_t data_bytes)  void rma_wait_idle (MPI_Win window)  constexpr size_t round_up_8 (size_t bytes)  void write_bytes (std::byte * buffer, size_t buffer_size, size_t offset, const void * src, size_t nbytes)  void write_i64 (std::byte * buffer, size_t buffer_size, size_t offset, int64_t value)"},{"location":"dynampi/namespacedynampi_1_1detail/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/namespacedynampi_1_1detail/#function-mpi_type_size_bytes","title":"function mpi_type_size_bytes","text":"<pre><code>template&lt;typename T&gt;\ninline int dynampi::detail::mpi_type_size_bytes () \n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1detail/#function-read_i64","title":"function read_i64","text":"<pre><code>inline int64_t dynampi::detail::read_i64 (\n    const std::byte * buffer,\n    size_t buffer_size,\n    size_t offset\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1detail/#function-read_result_bytes","title":"function read_result_bytes","text":"<pre><code>template&lt;typename T&gt;\ninline void dynampi::detail::read_result_bytes (\n    const std::byte * buffer,\n    size_t buffer_size,\n    size_t offset,\n    T &amp; value,\n    size_t data_bytes\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1detail/#function-rma_wait_idle","title":"function rma_wait_idle","text":"<pre><code>inline void dynampi::detail::rma_wait_idle (\n    MPI_Win window\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1detail/#function-round_up_8","title":"function round_up_8","text":"<pre><code>inline constexpr  size_t dynampi::detail::round_up_8 (\n    size_t bytes\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1detail/#function-write_bytes","title":"function write_bytes","text":"<pre><code>inline void dynampi::detail::write_bytes (\n    std::byte * buffer,\n    size_t buffer_size,\n    size_t offset,\n    const  void * src,\n    size_t nbytes\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1detail/#function-write_i64","title":"function write_i64","text":"<pre><code>inline void dynampi::detail::write_i64 (\n    std::byte * buffer,\n    size_t buffer_size,\n    size_t offset,\n    int64_t value\n) \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_async_put_lockfree_distributor.hpp</code></p>"},{"location":"dynampi/classdynampi_1_1detail_1_1AsyncPutLevel/","title":"Class dynampi::detail::AsyncPutLevel","text":"<p>template &lt;typename TaskT, typename ResultT&gt;</p> <p>ClassList &gt; dynampi &gt; detail &gt; AsyncPutLevel</p> <ul> <li><code>#include &lt;hierarchical_async_put_lockfree_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/classdynampi_1_1detail_1_1AsyncPutLevel/#classes","title":"Classes","text":"Type Name struct ClaimedRange struct Config"},{"location":"dynampi/classdynampi_1_1detail_1_1AsyncPutLevel/#public-functions","title":"Public Functions","text":"Type Name AsyncPutLevel (Config config)  AsyncPutLevel (const AsyncPutLevel &amp;) = delete bool check_finished ()  int claim_batch_size () const int comm_rank () const int comm_size () const bool drained ()  std::vector&lt; ResultT &gt; harvest_ready_results ()  std::vector&lt; ResultT &gt; harvest_ready_results_throttled ()  void idle_wait ()  bool is_owner () const void mark_finished ()  AsyncPutLevel &amp; operator= (const AsyncPutLevel &amp;) = delete size_t owner_collected_count () const bool owner_marked_finished () const size_t owner_published_count () const void publish_tasks (const std::vector&lt; TaskT &gt; &amp; tasks)  ClaimedRange try_claim ()  void write_result_range (int64_t start, const std::vector&lt; ResultT &gt; &amp; results)  ~AsyncPutLevel ()"},{"location":"dynampi/classdynampi_1_1detail_1_1AsyncPutLevel/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/classdynampi_1_1detail_1_1AsyncPutLevel/#function-asyncputlevel-12","title":"function AsyncPutLevel [1/2]","text":"<pre><code>inline explicit dynampi::detail::AsyncPutLevel::AsyncPutLevel (\n    Config config\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1AsyncPutLevel/#function-asyncputlevel-22","title":"function AsyncPutLevel [2/2]","text":"<pre><code>dynampi::detail::AsyncPutLevel::AsyncPutLevel (\n    const  AsyncPutLevel &amp;\n) = delete\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1AsyncPutLevel/#function-check_finished","title":"function check_finished","text":"<pre><code>inline bool dynampi::detail::AsyncPutLevel::check_finished () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1AsyncPutLevel/#function-claim_batch_size","title":"function claim_batch_size","text":"<pre><code>inline int dynampi::detail::AsyncPutLevel::claim_batch_size () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1AsyncPutLevel/#function-comm_rank","title":"function comm_rank","text":"<pre><code>inline int dynampi::detail::AsyncPutLevel::comm_rank () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1AsyncPutLevel/#function-comm_size","title":"function comm_size","text":"<pre><code>inline int dynampi::detail::AsyncPutLevel::comm_size () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1AsyncPutLevel/#function-drained","title":"function drained","text":"<pre><code>inline bool dynampi::detail::AsyncPutLevel::drained () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1AsyncPutLevel/#function-harvest_ready_results","title":"function harvest_ready_results","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::detail::AsyncPutLevel::harvest_ready_results () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1AsyncPutLevel/#function-harvest_ready_results_throttled","title":"function harvest_ready_results_throttled","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::detail::AsyncPutLevel::harvest_ready_results_throttled () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1AsyncPutLevel/#function-idle_wait","title":"function idle_wait","text":"<pre><code>inline void dynampi::detail::AsyncPutLevel::idle_wait () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1AsyncPutLevel/#function-is_owner","title":"function is_owner","text":"<pre><code>inline bool dynampi::detail::AsyncPutLevel::is_owner () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1AsyncPutLevel/#function-mark_finished","title":"function mark_finished","text":"<pre><code>inline void dynampi::detail::AsyncPutLevel::mark_finished () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1AsyncPutLevel/#function-operator","title":"function operator=","text":"<pre><code>AsyncPutLevel &amp; dynampi::detail::AsyncPutLevel::operator= (\n    const  AsyncPutLevel &amp;\n) = delete\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1AsyncPutLevel/#function-owner_collected_count","title":"function owner_collected_count","text":"<pre><code>inline size_t dynampi::detail::AsyncPutLevel::owner_collected_count () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1AsyncPutLevel/#function-owner_marked_finished","title":"function owner_marked_finished","text":"<pre><code>inline bool dynampi::detail::AsyncPutLevel::owner_marked_finished () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1AsyncPutLevel/#function-owner_published_count","title":"function owner_published_count","text":"<pre><code>inline size_t dynampi::detail::AsyncPutLevel::owner_published_count () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1AsyncPutLevel/#function-publish_tasks","title":"function publish_tasks","text":"<pre><code>inline void dynampi::detail::AsyncPutLevel::publish_tasks (\n    const std::vector&lt; TaskT &gt; &amp; tasks\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1AsyncPutLevel/#function-try_claim","title":"function try_claim","text":"<pre><code>inline ClaimedRange dynampi::detail::AsyncPutLevel::try_claim () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1AsyncPutLevel/#function-write_result_range","title":"function write_result_range","text":"<pre><code>inline void dynampi::detail::AsyncPutLevel::write_result_range (\n    int64_t start,\n    const std::vector&lt; ResultT &gt; &amp; results\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1AsyncPutLevel/#function-asyncputlevel","title":"function ~AsyncPutLevel","text":"<pre><code>inline dynampi::detail::AsyncPutLevel::~AsyncPutLevel () \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_async_put_lockfree_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1detail_1_1AsyncPutLevel_1_1ClaimedRange/","title":"Struct dynampi::detail::AsyncPutLevel::ClaimedRange","text":"<p>ClassList &gt; dynampi &gt; detail &gt; AsyncPutLevel &gt; ClaimedRange</p> <ul> <li><code>#include &lt;hierarchical_async_put_lockfree_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1detail_1_1AsyncPutLevel_1_1ClaimedRange/#public-attributes","title":"Public Attributes","text":"Type Name int64_t start   = <code>-1</code> std::vector&lt; TaskT &gt; tasks"},{"location":"dynampi/structdynampi_1_1detail_1_1AsyncPutLevel_1_1ClaimedRange/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1detail_1_1AsyncPutLevel_1_1ClaimedRange/#variable-start","title":"variable start","text":"<pre><code>int64_t dynampi::detail::AsyncPutLevel&lt; TaskT, ResultT &gt;::ClaimedRange::start;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1detail_1_1AsyncPutLevel_1_1ClaimedRange/#variable-tasks","title":"variable tasks","text":"<pre><code>std::vector&lt;TaskT&gt; dynampi::detail::AsyncPutLevel&lt; TaskT, ResultT &gt;::ClaimedRange::tasks;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_async_put_lockfree_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1detail_1_1AsyncPutLevel_1_1Config/","title":"Struct dynampi::detail::AsyncPutLevel::Config","text":"<p>ClassList &gt; dynampi &gt; detail &gt; AsyncPutLevel &gt; Config</p> <ul> <li><code>#include &lt;hierarchical_async_put_lockfree_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1detail_1_1AsyncPutLevel_1_1Config/#public-attributes","title":"Public Attributes","text":"Type Name int claim_batch_size   = <code>8</code> MPI_Comm comm   = <code>[**MPI\\_COMM\\_NULL**](namespacedynampi_1_1detail.md#function-mpi_type_size_bytes)</code> int max_result_count   = <code>256</code> int max_task_count   = <code>256</code> int max_tasks   = <code>8192</code> int owner_rank   = <code>0</code>"},{"location":"dynampi/structdynampi_1_1detail_1_1AsyncPutLevel_1_1Config/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1detail_1_1AsyncPutLevel_1_1Config/#variable-claim_batch_size","title":"variable claim_batch_size","text":"<pre><code>int dynampi::detail::AsyncPutLevel&lt; TaskT, ResultT &gt;::Config::claim_batch_size;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1detail_1_1AsyncPutLevel_1_1Config/#variable-comm","title":"variable comm","text":"<pre><code>MPI_Comm dynampi::detail::AsyncPutLevel&lt; TaskT, ResultT &gt;::Config::comm;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1detail_1_1AsyncPutLevel_1_1Config/#variable-max_result_count","title":"variable max_result_count","text":"<pre><code>int dynampi::detail::AsyncPutLevel&lt; TaskT, ResultT &gt;::Config::max_result_count;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1detail_1_1AsyncPutLevel_1_1Config/#variable-max_task_count","title":"variable max_task_count","text":"<pre><code>int dynampi::detail::AsyncPutLevel&lt; TaskT, ResultT &gt;::Config::max_task_count;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1detail_1_1AsyncPutLevel_1_1Config/#variable-max_tasks","title":"variable max_tasks","text":"<pre><code>int dynampi::detail::AsyncPutLevel&lt; TaskT, ResultT &gt;::Config::max_tasks;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1detail_1_1AsyncPutLevel_1_1Config/#variable-owner_rank","title":"variable owner_rank","text":"<pre><code>int dynampi::detail::AsyncPutLevel&lt; TaskT, ResultT &gt;::Config::owner_rank;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_async_put_lockfree_distributor.hpp</code></p>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeLevel/","title":"Class dynampi::detail::LockFreeLevel","text":"<p>template &lt;typename TaskT, typename ResultT&gt;</p> <p>ClassList &gt; dynampi &gt; detail &gt; LockFreeLevel</p> <ul> <li><code>#include &lt;hierarchical_lockfree_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeLevel/#classes","title":"Classes","text":"Type Name struct Config"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeLevel/#public-functions","title":"Public Functions","text":"Type Name LockFreeLevel (Config config)  LockFreeLevel (const LockFreeLevel &amp;) = delete int comm_rank () const int comm_size () const bool drained ()  bool gather_fully_done ()  bool has_pending_results () const void idle_wait ()  bool is_owner () const void mark_finished ()  void mark_gather_done ()  bool maybe_participate_in_gather ()  LockFreeLevel &amp; operator= (const LockFreeLevel &amp;) = delete size_t owner_collected_count () const bool owner_marked_finished () const size_t owner_published_count () const void publish_tasks (const std::vector&lt; TaskT &gt; &amp; tasks)  std::vector&lt; ResultT &gt; request_gather ()  std::vector&lt; ResultT &gt; request_gather_throttled ()  void stage_result (ResultT result)  std::vector&lt; TaskT &gt; try_claim_batch (int want)  ~LockFreeLevel ()"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeLevel/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeLevel/#function-lockfreelevel-12","title":"function LockFreeLevel [1/2]","text":"<pre><code>inline explicit dynampi::detail::LockFreeLevel::LockFreeLevel (\n    Config config\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeLevel/#function-lockfreelevel-22","title":"function LockFreeLevel [2/2]","text":"<pre><code>dynampi::detail::LockFreeLevel::LockFreeLevel (\n    const  LockFreeLevel &amp;\n) = delete\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeLevel/#function-comm_rank","title":"function comm_rank","text":"<pre><code>inline int dynampi::detail::LockFreeLevel::comm_rank () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeLevel/#function-comm_size","title":"function comm_size","text":"<pre><code>inline int dynampi::detail::LockFreeLevel::comm_size () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeLevel/#function-drained","title":"function drained","text":"<pre><code>inline bool dynampi::detail::LockFreeLevel::drained () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeLevel/#function-gather_fully_done","title":"function gather_fully_done","text":"<pre><code>inline bool dynampi::detail::LockFreeLevel::gather_fully_done () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeLevel/#function-has_pending_results","title":"function has_pending_results","text":"<pre><code>inline bool dynampi::detail::LockFreeLevel::has_pending_results () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeLevel/#function-idle_wait","title":"function idle_wait","text":"<pre><code>inline void dynampi::detail::LockFreeLevel::idle_wait () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeLevel/#function-is_owner","title":"function is_owner","text":"<pre><code>inline bool dynampi::detail::LockFreeLevel::is_owner () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeLevel/#function-mark_finished","title":"function mark_finished","text":"<pre><code>inline void dynampi::detail::LockFreeLevel::mark_finished () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeLevel/#function-mark_gather_done","title":"function mark_gather_done","text":"<pre><code>inline void dynampi::detail::LockFreeLevel::mark_gather_done () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeLevel/#function-maybe_participate_in_gather","title":"function maybe_participate_in_gather","text":"<pre><code>inline bool dynampi::detail::LockFreeLevel::maybe_participate_in_gather () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeLevel/#function-operator","title":"function operator=","text":"<pre><code>LockFreeLevel &amp; dynampi::detail::LockFreeLevel::operator= (\n    const  LockFreeLevel &amp;\n) = delete\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeLevel/#function-owner_collected_count","title":"function owner_collected_count","text":"<pre><code>inline size_t dynampi::detail::LockFreeLevel::owner_collected_count () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeLevel/#function-owner_marked_finished","title":"function owner_marked_finished","text":"<pre><code>inline bool dynampi::detail::LockFreeLevel::owner_marked_finished () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeLevel/#function-owner_published_count","title":"function owner_published_count","text":"<pre><code>inline size_t dynampi::detail::LockFreeLevel::owner_published_count () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeLevel/#function-publish_tasks","title":"function publish_tasks","text":"<pre><code>inline void dynampi::detail::LockFreeLevel::publish_tasks (\n    const std::vector&lt; TaskT &gt; &amp; tasks\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeLevel/#function-request_gather","title":"function request_gather","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::detail::LockFreeLevel::request_gather () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeLevel/#function-request_gather_throttled","title":"function request_gather_throttled","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::detail::LockFreeLevel::request_gather_throttled () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeLevel/#function-stage_result","title":"function stage_result","text":"<pre><code>inline void dynampi::detail::LockFreeLevel::stage_result (\n    ResultT result\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeLevel/#function-try_claim_batch","title":"function try_claim_batch","text":"<pre><code>inline std::vector&lt; TaskT &gt; dynampi::detail::LockFreeLevel::try_claim_batch (\n    int want\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeLevel/#function-lockfreelevel","title":"function ~LockFreeLevel","text":"<pre><code>inline dynampi::detail::LockFreeLevel::~LockFreeLevel () \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_lockfree_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1detail_1_1LockFreeLevel_1_1Config/","title":"Struct dynampi::detail::LockFreeLevel::Config","text":"<p>ClassList &gt; dynampi &gt; detail &gt; LockFreeLevel &gt; Config</p> <ul> <li><code>#include &lt;hierarchical_lockfree_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1detail_1_1LockFreeLevel_1_1Config/#public-attributes","title":"Public Attributes","text":"Type Name MPI_Comm comm   = <code>[**MPI\\_COMM\\_NULL**](namespacedynampi_1_1detail.md#function-mpi_type_size_bytes)</code> int max_result_count   = <code>256</code> int max_task_count   = <code>256</code> int max_tasks   = <code>8192</code> int owner_rank   = <code>0</code>"},{"location":"dynampi/structdynampi_1_1detail_1_1LockFreeLevel_1_1Config/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1detail_1_1LockFreeLevel_1_1Config/#variable-comm","title":"variable comm","text":"<pre><code>MPI_Comm dynampi::detail::LockFreeLevel&lt; TaskT, ResultT &gt;::Config::comm;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1detail_1_1LockFreeLevel_1_1Config/#variable-max_result_count","title":"variable max_result_count","text":"<pre><code>int dynampi::detail::LockFreeLevel&lt; TaskT, ResultT &gt;::Config::max_result_count;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1detail_1_1LockFreeLevel_1_1Config/#variable-max_task_count","title":"variable max_task_count","text":"<pre><code>int dynampi::detail::LockFreeLevel&lt; TaskT, ResultT &gt;::Config::max_task_count;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1detail_1_1LockFreeLevel_1_1Config/#variable-max_tasks","title":"variable max_tasks","text":"<pre><code>int dynampi::detail::LockFreeLevel&lt; TaskT, ResultT &gt;::Config::max_tasks;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1detail_1_1LockFreeLevel_1_1Config/#variable-owner_rank","title":"variable owner_rank","text":"<pre><code>int dynampi::detail::LockFreeLevel&lt; TaskT, ResultT &gt;::Config::owner_rank;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_lockfree_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1enable__prioritization/","title":"Struct dynampi::enable_prioritization","text":"<p>ClassList &gt; dynampi &gt; enable_prioritization</p> <ul> <li><code>#include &lt;base_distributor.hpp&gt;</code></li> </ul> <p>Inherits the following classes: dynampi::prioritize_tasks_t</p>"},{"location":"dynampi/structdynampi_1_1enable__prioritization/#public-static-attributes","title":"Public Static Attributes","text":"Type Name constexpr bool value   = <code>true</code>"},{"location":"dynampi/structdynampi_1_1enable__prioritization/#public-static-attributes-inherited-from-dynampiprioritize_tasks_t","title":"Public Static Attributes inherited from dynampi::prioritize_tasks_t","text":"<p>See dynampi::prioritize_tasks_t</p> Type Name constexpr bool value   = <code>false</code>"},{"location":"dynampi/structdynampi_1_1enable__prioritization/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1enable__prioritization/#variable-value","title":"variable value","text":"<pre><code>constexpr bool dynampi::enable_prioritization::value;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/base_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1has__dynampi__mpi__type/","title":"Struct dynampi::has_dynampi_mpi_type","text":"<p>template &lt;typename, typename&gt;</p> <p>ClassList &gt; dynampi &gt; has_dynampi_mpi_type</p> <ul> <li><code>#include &lt;mpi_types.hpp&gt;</code></li> </ul> <p>Inherits the following classes: std::false_type</p> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_types.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1has__dynampi__mpi__type_3_01U_00_01std_1_1void__t_3_01decltype_07MPI__Type_3_01U_01_4_1_1value_08_4_01_4/","title":"Struct dynampi::has_dynampi_mpi_type&lt; U, std::void_t&lt; decltype(MPI_Type&lt; U &gt;::value)&gt; &gt;","text":"<p>template &lt;typename U&gt;</p> <p>ClassList &gt; dynampi &gt; has_dynampi_mpi_type&lt; U, std::void_t&lt; decltype(MPI_Type&lt; U &gt;::value)&gt; &gt;</p> <ul> <li><code>#include &lt;mpi_types.hpp&gt;</code></li> </ul> <p>Inherits the following classes: std::true_type</p> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_types.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1prioritize__tasks__t/","title":"Struct dynampi::prioritize_tasks_t","text":"<p>ClassList &gt; dynampi &gt; prioritize_tasks_t</p> <ul> <li><code>#include &lt;base_distributor.hpp&gt;</code></li> </ul> <p>Inherited by the following classes: dynampi::enable_prioritization</p>"},{"location":"dynampi/structdynampi_1_1prioritize__tasks__t/#public-static-attributes","title":"Public Static Attributes","text":"Type Name constexpr bool value   = <code>false</code>"},{"location":"dynampi/structdynampi_1_1prioritize__tasks__t/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1prioritize__tasks__t/#variable-value","title":"variable value","text":"<pre><code>constexpr bool dynampi::prioritize_tasks_t::value;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/base_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1track__statistics/","title":"Struct dynampi::track_statistics","text":"<p>template &lt;StatisticsMode Mode&gt;</p> <p>ClassList &gt; dynampi &gt; track_statistics</p> <ul> <li><code>#include &lt;mpi_communicator.hpp&gt;</code></li> </ul> <p>Inherits the following classes: dynampi::track_statistics_t</p>"},{"location":"dynampi/structdynampi_1_1track__statistics/#public-static-attributes","title":"Public Static Attributes","text":"Type Name constexpr StatisticsMode value   = <code>Mode</code>"},{"location":"dynampi/structdynampi_1_1track__statistics/#public-static-attributes-inherited-from-dynampitrack_statistics_t","title":"Public Static Attributes inherited from dynampi::track_statistics_t","text":"<p>See dynampi::track_statistics_t</p> Type Name constexpr StatisticsMode value   = <code>StatisticsMode::None</code>"},{"location":"dynampi/structdynampi_1_1track__statistics/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1track__statistics/#variable-value","title":"variable value","text":"<pre><code>constexpr StatisticsMode dynampi::track_statistics&lt; Mode &gt;::value;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_communicator.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1track__statistics__t/","title":"Struct dynampi::track_statistics_t","text":"<p>ClassList &gt; dynampi &gt; track_statistics_t</p> <ul> <li><code>#include &lt;mpi_communicator.hpp&gt;</code></li> </ul> <p>Inherited by the following classes: dynampi::track_statistics</p>"},{"location":"dynampi/structdynampi_1_1track__statistics__t/#public-static-attributes","title":"Public Static Attributes","text":"Type Name constexpr StatisticsMode value   = <code>StatisticsMode::None</code>"},{"location":"dynampi/structdynampi_1_1track__statistics__t/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1track__statistics__t/#variable-value","title":"variable value","text":"<pre><code>constexpr StatisticsMode dynampi::track_statistics_t::value;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_communicator.hpp</code></p>"},{"location":"dynampi/namespacedynampi_1_1version/","title":"Namespace dynampi::version","text":"<p>Namespace List &gt; dynampi &gt; version</p>"},{"location":"dynampi/namespacedynampi_1_1version/#public-attributes","title":"Public Attributes","text":"Type Name constexpr int major   = <code>DYNAMPI\\_VERSION\\_MAJOR</code> constexpr int minor   = <code>DYNAMPI\\_VERSION\\_MINOR</code> constexpr int patch   = <code>DYNAMPI\\_VERSION\\_PATCH</code> constexpr std::string_view string   = <code>[**DYNAMPI\\_VERSION\\_STRING**](dynampi_8hpp.md#define-dynampi_version_string)</code>"},{"location":"dynampi/namespacedynampi_1_1version/#public-functions","title":"Public Functions","text":"Type Name constexpr std::string_view commit_hash ()  constexpr std::string_view compile_date ()  constexpr bool is_at_least (int v_major, int v_minor, int v_patch)"},{"location":"dynampi/namespacedynampi_1_1version/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/namespacedynampi_1_1version/#variable-major","title":"variable major","text":"<pre><code>constexpr int dynampi::version::major;\n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1version/#variable-minor","title":"variable minor","text":"<pre><code>constexpr int dynampi::version::minor;\n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1version/#variable-patch","title":"variable patch","text":"<pre><code>constexpr int dynampi::version::patch;\n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1version/#variable-string","title":"variable string","text":"<pre><code>constexpr std::string_view dynampi::version::string;\n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1version/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/namespacedynampi_1_1version/#function-commit_hash","title":"function commit_hash","text":"<pre><code>inline constexpr std::string_view dynampi::version::commit_hash () \n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1version/#function-compile_date","title":"function compile_date","text":"<pre><code>inline constexpr std::string_view dynampi::version::compile_date () \n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1version/#function-is_at_least","title":"function is_at_least","text":"<pre><code>constexpr bool dynampi::version::is_at_least (\n    int v_major,\n    int v_minor,\n    int v_patch\n) \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/dynampi.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1BridgeHop/","title":"Struct dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::BridgeHop","text":"<p>ClassList &gt; BridgeHop</p>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1BridgeHop/#public-attributes","title":"Public Attributes","text":"Type Name detail::AsyncPutLevel&lt; TaskT, ResultT &gt; * child bool finish_marked   = <code>false</code> detail::AsyncPutLevel&lt; TaskT, ResultT &gt; * parent std::deque&lt; PendingRelay &gt; pending_relays int64_t pending_task_count   = <code>0</code> std::vector&lt; ResultT &gt; relay_buffer"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1BridgeHop/#public-functions","title":"Public Functions","text":"Type Name BridgeHop (detail::AsyncPutLevel&lt; TaskT, ResultT &gt; * p, detail::AsyncPutLevel&lt; TaskT, ResultT &gt; * c)"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1BridgeHop/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1BridgeHop/#variable-child","title":"variable child","text":"<pre><code>detail::AsyncPutLevel&lt;TaskT, ResultT&gt;* dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::BridgeHop::child;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1BridgeHop/#variable-finish_marked","title":"variable finish_marked","text":"<pre><code>bool dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::BridgeHop::finish_marked;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1BridgeHop/#variable-parent","title":"variable parent","text":"<pre><code>detail::AsyncPutLevel&lt;TaskT, ResultT&gt;* dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::BridgeHop::parent;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1BridgeHop/#variable-pending_relays","title":"variable pending_relays","text":"<pre><code>std::deque&lt;PendingRelay&gt; dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::BridgeHop::pending_relays;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1BridgeHop/#variable-pending_task_count","title":"variable pending_task_count","text":"<pre><code>int64_t dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::BridgeHop::pending_task_count;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1BridgeHop/#variable-relay_buffer","title":"variable relay_buffer","text":"<pre><code>std::vector&lt;ResultT&gt; dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::BridgeHop::relay_buffer;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1BridgeHop/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1BridgeHop/#function-bridgehop","title":"function BridgeHop","text":"<pre><code>inline BridgeHop::BridgeHop (\n    detail::AsyncPutLevel &lt; TaskT, ResultT &gt; * p,\n    detail::AsyncPutLevel &lt; TaskT, ResultT &gt; * c\n) \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_async_put_lockfree_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1PendingRelay/","title":"Struct dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::PendingRelay","text":"<p>ClassList &gt; PendingRelay</p>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1PendingRelay/#public-attributes","title":"Public Attributes","text":"Type Name int64_t child_len int64_t parent_start"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1PendingRelay/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1PendingRelay/#variable-child_len","title":"variable child_len","text":"<pre><code>int64_t dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::PendingRelay::child_len;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalAsyncPutLockFreeMPIWorkDistributor_1_1PendingRelay/#variable-parent_start","title":"variable parent_start","text":"<pre><code>int64_t dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::PendingRelay::parent_start;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_async_put_lockfree_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1Statistics/","title":"Struct dynampi::HierarchicalMPIWorkDistributor::Statistics","text":"<p>ClassList &gt; Statistics</p>"},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1Statistics/#public-attributes","title":"Public Attributes","text":"Type Name const CommStatistics &amp; comm_statistics std::optional&lt; std::vector&lt; size_t &gt; &gt; worker_task_counts   = <code>{}</code>"},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1Statistics/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1Statistics/#variable-comm_statistics","title":"variable comm_statistics","text":"<pre><code>const CommStatistics&amp; dynampi::HierarchicalMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Statistics::comm_statistics;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1Statistics/#variable-worker_task_counts","title":"variable worker_task_counts","text":"<pre><code>std::optional&lt;std::vector&lt;size_t&gt; &gt; dynampi::HierarchicalMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Statistics::worker_task_counts;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1TaskRequest/","title":"Struct dynampi::HierarchicalMPIWorkDistributor::TaskRequest","text":"<p>ClassList &gt; TaskRequest</p>"},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1TaskRequest/#public-attributes","title":"Public Attributes","text":"Type Name std::optional&lt; int &gt; num_tasks_requested   = <code>std::nullopt</code> CommLayer source_layer   = <code>CommLayer::Global</code> int worker_rank"},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1TaskRequest/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1TaskRequest/#variable-num_tasks_requested","title":"variable num_tasks_requested","text":"<pre><code>std::optional&lt;int&gt; dynampi::HierarchicalMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::TaskRequest::num_tasks_requested;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1TaskRequest/#variable-source_layer","title":"variable source_layer","text":"<pre><code>CommLayer dynampi::HierarchicalMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::TaskRequest::source_layer;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalMPIWorkDistributor_1_1TaskRequest/#variable-worker_rank","title":"variable worker_rank","text":"<pre><code>int dynampi::HierarchicalMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::TaskRequest::worker_rank;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_distributor.hpp</code></p>"},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1AsyncSendPool/","title":"Class dynampi::HierarchicalNonBlockingMPIWorkDistributor::AsyncSendPool","text":"<p>template &lt;typename T&gt;</p> <p>ClassList &gt; AsyncSendPool</p>"},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1AsyncSendPool/#public-functions","title":"Public Functions","text":"Type Name void post (MPICommunicator &amp; comm, T value, int dest, int tag)  void wait_all ()"},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1AsyncSendPool/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1AsyncSendPool/#function-post","title":"function post","text":"<pre><code>inline void AsyncSendPool::post (\n    MPICommunicator &amp; comm,\n    T value,\n    int dest,\n    int tag\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1AsyncSendPool/#function-wait_all","title":"function wait_all","text":"<pre><code>inline void AsyncSendPool::wait_all () \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_nonblocking_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1Statistics/","title":"Struct dynampi::HierarchicalNonBlockingMPIWorkDistributor::Statistics","text":"<p>ClassList &gt; Statistics</p>"},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1Statistics/#public-attributes","title":"Public Attributes","text":"Type Name const CommStatistics &amp; comm_statistics std::optional&lt; std::vector&lt; size_t &gt; &gt; worker_task_counts   = <code>{}</code>"},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1Statistics/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1Statistics/#variable-comm_statistics","title":"variable comm_statistics","text":"<pre><code>const CommStatistics&amp; dynampi::HierarchicalNonBlockingMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Statistics::comm_statistics;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1Statistics/#variable-worker_task_counts","title":"variable worker_task_counts","text":"<pre><code>std::optional&lt;std::vector&lt;size_t&gt; &gt; dynampi::HierarchicalNonBlockingMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::Statistics::worker_task_counts;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_nonblocking_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1TaskRequest/","title":"Struct dynampi::HierarchicalNonBlockingMPIWorkDistributor::TaskRequest","text":"<p>ClassList &gt; TaskRequest</p>"},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1TaskRequest/#public-attributes","title":"Public Attributes","text":"Type Name std::optional&lt; int &gt; num_tasks_requested   = <code>std::nullopt</code> CommLayer source_layer   = <code>CommLayer::Global</code> int worker_rank"},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1TaskRequest/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1TaskRequest/#variable-num_tasks_requested","title":"variable num_tasks_requested","text":"<pre><code>std::optional&lt;int&gt; dynampi::HierarchicalNonBlockingMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::TaskRequest::num_tasks_requested;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1TaskRequest/#variable-source_layer","title":"variable source_layer","text":"<pre><code>CommLayer dynampi::HierarchicalNonBlockingMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::TaskRequest::source_layer;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalNonBlockingMPIWorkDistributor_1_1TaskRequest/#variable-worker_rank","title":"variable worker_rank","text":"<pre><code>int dynampi::HierarchicalNonBlockingMPIWorkDistributor&lt; TaskT, ResultT, Options &gt;::TaskRequest::worker_rank;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_nonblocking_distributor.hpp</code></p>"},{"location":"dynampi/structoption__value/","title":"Struct option_value","text":"<p>template &lt;typename Option, typename... Options&gt;</p> <p>ClassList &gt; option_value</p> <ul> <li><code>#include &lt;template_options.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structoption__value/#public-static-attributes","title":"Public Static Attributes","text":"Type Name constexpr decltype(Option::value) value   = <code>Option::value</code>"},{"location":"dynampi/structoption__value/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/structoption__value/#variable-value","title":"variable value","text":"<pre><code>constexpr decltype(Option::value) option_value&lt; Option, Options &gt;::value;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/utilities/template_options.hpp</code></p>"},{"location":"dynampi/structoption__value_3_01Option_00_01Head_00_01Tail_8_8_8_01_4/","title":"Struct option_value&lt; Option, Head, Tail... &gt;","text":"<p>template &lt;typename Option, typename Head, typename... Tail&gt;</p> <p>ClassList &gt; option_value&lt; Option, Head, Tail... &gt;</p> <ul> <li><code>#include &lt;template_options.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structoption__value_3_01Option_00_01Head_00_01Tail_8_8_8_01_4/#public-static-attributes","title":"Public Static Attributes","text":"Type Name constexpr decltype(Option::value) value   = <code>/* multi line expression */</code>"},{"location":"dynampi/structoption__value_3_01Option_00_01Head_00_01Tail_8_8_8_01_4/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/structoption__value_3_01Option_00_01Head_00_01Tail_8_8_8_01_4/#variable-value","title":"variable value","text":"<pre><code>constexpr decltype(Option::value) option_value&lt; Option, Head, Tail... &gt;::value;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/utilities/template_options.hpp</code></p>"},{"location":"dynampi/namespacestd/","title":"Namespace std","text":"<p>Namespace List &gt; std</p> <p>The documentation for this class was generated from the following file <code>[generated]</code></p>"},{"location":"dynampi/dir_d44c64559bbebec7f509842c48db8b23/","title":"Dir /home/runner/work/DynaMPI/DynaMPI/include","text":"<p>FileList &gt; include</p>"},{"location":"dynampi/dir_d44c64559bbebec7f509842c48db8b23/#directories","title":"Directories","text":"Type Name dir dynampi <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/</code></p>"},{"location":"dynampi/dir_f8db417ebd5c3d89eea80c24e3fd4369/","title":"Dir /home/runner/work/DynaMPI/DynaMPI/include/dynampi","text":"<p>FileList &gt; dynampi</p>"},{"location":"dynampi/dir_f8db417ebd5c3d89eea80c24e3fd4369/#files","title":"Files","text":"Type Name file dynampi.hpp"},{"location":"dynampi/dir_f8db417ebd5c3d89eea80c24e3fd4369/#directories","title":"Directories","text":"Type Name dir impl dir mpi dir utilities <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/</code></p>"},{"location":"dynampi/dir_87365955bfd9c1869b83a1fbd3fdb221/","title":"Dir /home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl","text":"<p>FileList &gt; dynampi &gt; impl</p>"},{"location":"dynampi/dir_87365955bfd9c1869b83a1fbd3fdb221/#files","title":"Files","text":"Type Name file async_put_lockfree_distributor.hpp file base_distributor.hpp file hierarchical_async_put_lockfree_distributor.hpp file hierarchical_distributor.hpp file hierarchical_lockfree_distributor.hpp file hierarchical_nonblocking_distributor.hpp file lockfree_distributor.hpp file naive_distributor.hpp <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/</code></p>"},{"location":"dynampi/async__put__lockfree__distributor_8hpp/","title":"File async_put_lockfree_distributor.hpp","text":"<p>FileList &gt; dynampi &gt; impl &gt; async_put_lockfree_distributor.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;algorithm&gt;</code></li> <li><code>#include &lt;cassert&gt;</code></li> <li><code>#include &lt;cstddef&gt;</code></li> <li><code>#include &lt;cstdint&gt;</code></li> <li><code>#include &lt;functional&gt;</code></li> <li><code>#include &lt;limits&gt;</code></li> <li><code>#include &lt;optional&gt;</code></li> <li><code>#include &lt;span&gt;</code></li> <li><code>#include &lt;vector&gt;</code></li> <li><code>#include \"../mpi/mpi_communicator.hpp\"</code></li> <li><code>#include \"../mpi/mpi_types.hpp\"</code></li> <li><code>#include \"dynampi/impl/lockfree_distributor.hpp\"</code></li> <li><code>#include \"dynampi/mpi/mpi_error.hpp\"</code></li> <li><code>#include \"dynampi/utilities/timer.hpp\"</code></li> </ul>"},{"location":"dynampi/async__put__lockfree__distributor_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi"},{"location":"dynampi/async__put__lockfree__distributor_8hpp/#classes","title":"Classes","text":"Type Name class AsyncPutLockFreeMPIWorkDistributor &lt;typename TaskT, typename ResultT, Options&gt; struct Config struct RunConfig struct Statistics <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/async_put_lockfree_distributor.hpp</code></p>"},{"location":"dynampi/async__put__lockfree__distributor_8hpp_source/","title":"File async_put_lockfree_distributor.hpp","text":"<p>File List &gt; dynampi &gt; impl &gt; async_put_lockfree_distributor.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2026 Ryan Stocks\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;algorithm&gt;\n#include &lt;cassert&gt;\n#include &lt;cstddef&gt;\n#include &lt;cstdint&gt;\n#include &lt;functional&gt;\n#include &lt;limits&gt;\n#include &lt;optional&gt;\n#include &lt;span&gt;\n#include &lt;vector&gt;\n\n#include \"../mpi/mpi_communicator.hpp\"\n#include \"../mpi/mpi_types.hpp\"\n#include \"dynampi/impl/lockfree_distributor.hpp\"  // reuses dynampi::detail byte-packing helpers\n#include \"dynampi/mpi/mpi_error.hpp\"\n#include \"dynampi/utilities/timer.hpp\"\n\nnamespace dynampi {\n\n// ---------------------------------------------------------------------------\n// AsyncPutLockFreeMPIWorkDistributor\n//\n// Same lock-free task claiming as LockFreeMPIWorkDistributor (fetch-and-add\n// against HEAD_OFF, bounded by a cached read of TOTAL_OFF), but a completely\n// different result path: no MPI_Gather/MPI_Gatherv, ever, and -- as of this\n// revision -- no per-task RMA call on either side either. Every round trip\n// in the hot path covers a whole claimed *batch*, not one task:\n//\n//   worker, per batch of up to claim_batch_size tasks:\n//     1 fetch_add(HEAD_OFF)      -- claim the range\n//     1 bulk Get                 -- read all tasks in the range\n//     1 bulk Put                 -- write all results in the range\n//     1 atomic_set               -- publish one \"[start,count) done\" entry\n//                                    to a completion log, keyed by `start`\n//                                    (collision-free: fetch_add already\n//                                    guarantees this worker uniquely owns\n//                                    `start`, so no separate slot-allocation\n//                                    round trip is needed for the log either)\n//\n//   manager, per harvest call (however many batches are ready):\n//     1 atomic_read(HEAD_OFF)    -- how far claiming has progressed\n//     1 bulk Get of the completion log over the unscanned range\n//     1 bulk Get of the result table over however much of that range\n//       turned out to be a contiguous run of completed entries\n//\n// i.e. O(1) round trips per batch and per harvest call, not O(batch size) or\n// O(results found). This is what actually gets close to the raw one-sided\n// atomic ceiling rma_atomic_microbench measures (~900K ops/s against a\n// single target): the earlier per-task version paid 2 extra round trips\n// *per task* on the write side and 3 *per result* on the harvest side, which\n// swamped the win from dropping the collective entirely. Every RMA call\n// here still goes through the real MPI RMA API (Fetch_and_op/Put/Get), even\n// the manager's self-targeted ones -- see harvest_ready_results() for why\n// plain local loads on the owner's own window buffer are not safe to\n// substitute (MPI_WIN_SEPARATE).\n//\n// Results are explicitly NOT ordered: harvested in completion order (which,\n// via the completion log's contiguous-prefix scan, is close to submission\n// order but not guaranteed to be exactly that under a straggler). Ordering\n// would require a Sort or an index-keyed result table -- either reintroduces\n// per-task cost this design specifically avoids.\n// ---------------------------------------------------------------------------\n// Options currently only recognizes track_statistics&lt;...&gt; (no\n// prioritization support in this class); it exists so this type fits the\n// same Distributor&lt;TaskT, ResultT, Options...&gt; shape every other\n// distributor here uses, for generic test/benchmark code that instantiates\n// uniformly.\ntemplate &lt;typename TaskT, typename ResultT, typename... Options&gt;\nclass AsyncPutLockFreeMPIWorkDistributor {\n private:\n  static constexpr StatisticsMode statistics_mode =\n      get_option_value&lt;track_statistics_t, Options...&gt;();\n  using Comm = dynampi::MPICommunicator&lt;track_statistics&lt;statistics_mode&gt;&gt;;\n\n public:\n  struct Config {\n    MPI_Comm comm = MPI_COMM_WORLD;\n    int manager_rank = 0;\n    bool auto_run_workers = true;\n    int max_tasks = 8192;\n    int max_task_count = 256;\n    int max_result_count = 256;\n    int claim_batch_size = 8;\n  };\n\n  struct RunConfig {\n    size_t target_num_tasks = std::numeric_limits&lt;size_t&gt;::max();\n    bool allow_more_than_target_tasks = true;\n    std::optional&lt;double&gt; max_seconds = std::nullopt;\n  };\n\n  // Not ordered -- see the class comment. Every other distributor's\n  // ordered=false path already sorts before comparing in generic test code,\n  // so this needs no special-casing there.\n  static const bool ordered = false;\n\n  struct Statistics {\n    CommStatistics comm_statistics;\n  };\n  using StatisticsT =\n      std::conditional_t&lt;statistics_mode != StatisticsMode::None, Statistics, std::monostate&gt;;\n\n  const StatisticsT&amp; get_statistics() const\n    requires(statistics_mode != StatisticsMode::None)\n  {\n    assert(is_root_manager());\n    return m_statistics;\n  }\n\n  explicit AsyncPutLockFreeMPIWorkDistributor(std::function&lt;ResultT(TaskT)&gt; worker_function,\n                                              Config config = {})\n      : m_config(config),\n        m_comm(config.comm, Comm::Duplicate),\n        m_worker_function(std::move(worker_function)) {\n    initialize_window();\n    if (m_config.auto_run_workers &amp;&amp; !is_root_manager()) run_worker();\n  }\n\n  ~AsyncPutLockFreeMPIWorkDistributor() {\n    if (!m_finalized) finalize();\n    if (m_window != MPI_WIN_NULL) {\n      DYNAMPI_MPI_CHECK(MPI_Win_unlock_all, (m_window));\n      MPI_Barrier(m_comm.get());\n      DYNAMPI_MPI_CHECK(MPI_Win_free, (&amp;m_window));\n      m_window = MPI_WIN_NULL;\n    }\n  }\n\n  bool is_root_manager() const { return m_comm.rank() == m_config.manager_rank; }\n  int num_workers() const { return m_comm.size() - 1; }\n\n  size_t remaining_tasks_count() const {\n    assert(is_root_manager());\n    return static_cast&lt;size_t&gt;(m_total_tasks) - m_returned_count;\n  }\n\n  void insert_task(TaskT task) {\n    assert(is_root_manager());\n    publish_tasks(std::span&lt;const TaskT&gt;(&amp;task, 1));\n  }\n\n  // Priority is accepted but ignored (no prioritization support -- see the\n  // Options note above); exists only for template-shape compatibility with\n  // generic test/benchmark code exercised uniformly across distributors.\n  void insert_task(const TaskT&amp; task, double /*priority*/) {\n    assert(is_root_manager());\n    publish_tasks(std::span&lt;const TaskT&gt;(&amp;task, 1));\n  }\n\n  void insert_tasks(const std::vector&lt;TaskT&gt;&amp; tasks) {\n    assert(is_root_manager());\n    publish_tasks(std::span&lt;const TaskT&gt;(tasks));\n  }\n\n  [[nodiscard]] std::vector&lt;ResultT&gt; run_tasks(RunConfig config = {}) {\n    assert(is_root_manager());\n    Timer timer;\n\n    if (num_workers() == 0) {\n      while (m_collected_count &lt; static_cast&lt;size_t&gt;(m_total_tasks)) {\n        if (m_results.size() &gt;= config.target_num_tasks) break;\n        if (config.max_seconds &amp;&amp; timer.elapsed().count() &gt;= *config.max_seconds) break;\n        run_one_task_locally();\n      }\n    } else {\n      while (true) {\n        if (m_results.size() &gt;= config.target_num_tasks) break;\n        if (config.max_seconds &amp;&amp; timer.elapsed().count() &gt;= *config.max_seconds) break;\n        if (m_collected_count &gt;= static_cast&lt;size_t&gt;(m_total_tasks)) break;\n        const size_t before = m_collected_count;\n        harvest_ready_results();\n        if (m_collected_count == before) detail::rma_wait_idle(m_window);\n      }\n    }\n\n    const size_t limit = config.allow_more_than_target_tasks ? std::numeric_limits&lt;size_t&gt;::max()\n                                                              : config.target_num_tasks;\n    return drain_results(limit);\n  }\n\n  [[nodiscard]] std::vector&lt;ResultT&gt; finish_remaining_tasks() { return run_tasks({}); }\n\n  // Requests exactly one harvest pass and returns whatever's ready --\n  // possibly none, possibly everything outstanding, depending on how far\n  // workers have gotten. Unlike run_tasks()/finish_remaining_tasks(), this\n  // does not loop retrying until every outstanding task is collected: those\n  // block until m_collected_count reaches m_total_tasks (or a target/\n  // timeout), which is the wrong shape for \"take one snapshot and return\n  // control\" callers -- e.g. a benchmark driver measuring throughput of an\n  // uninterrupted worker-side spin, which wants to publish a batch, let\n  // workers run completely undisturbed for a fixed window, then harvest\n  // once at the end (see AsyncPutLockFree's benchmark path in\n  // strong_scaling_distribution_rate.cpp, mirroring\n  // LockFreeMPIWorkDistributor::gather_once() for the same reason).\n  [[nodiscard]] std::vector&lt;ResultT&gt; gather_once() {\n    assert(is_root_manager());\n    if (num_workers() == 0) {\n      while (m_collected_count &lt; static_cast&lt;size_t&gt;(m_total_tasks)) run_one_task_locally();\n    } else {\n      harvest_ready_results();\n    }\n    return drain_results(std::numeric_limits&lt;size_t&gt;::max());\n  }\n\n  void finalize() {\n    assert(!m_finalized);\n    if (is_root_manager()) {\n      if (num_workers() == 0) {\n        while (m_collected_count &lt; static_cast&lt;size_t&gt;(m_total_tasks)) run_one_task_locally();\n      } else {\n        while (m_collected_count &lt; static_cast&lt;size_t&gt;(m_total_tasks)) {\n          const size_t before = m_collected_count;\n          harvest_ready_results();\n          if (m_collected_count == before) detail::rma_wait_idle(m_window);\n        }\n        atomic_set(FINISHED_OFF, 1);  // tell workers to stop\n        detail::rma_wait_idle(m_window);\n      }\n    }\n    m_finalized = true;\n  }\n\n  void run_worker() {\n    assert(!is_root_manager());\n\n    // Same bounded fetch-and-add claim loop as LockFreeMPIWorkDistributor\n    // (see that class's run_worker() for the full derivation of why the\n    // request size must be bounded by a cached TOTAL_OFF read, why the\n    // claim branch must NOT be gated on !finished_seen -- a hang, found and\n    // fixed via gdb backtraces earlier -- and why pending_start/pending_end\n    // + processing the ready prefix immediately is required to avoid a\n    // claim-vs-collection deadlock). The only difference here: no\n    // maybe_participate_in_gather() at all -- there is no gather protocol\n    // in this class, results go out via one-sided Put the moment a whole\n    // claimed batch (or its ready prefix) finishes.\n    int64_t cached_head = 0;\n    int64_t cached_total = 0;\n    int64_t pending_start = -1;\n    int64_t pending_end = -1;\n    bool finished_seen = false;\n\n    auto process_range = [&amp;](int64_t start, int64_t count) {\n      std::vector&lt;TaskT&gt; tasks = read_task_batch(start, count);\n      std::vector&lt;ResultT&gt; results;\n      results.reserve(static_cast&lt;size_t&gt;(count));\n      for (int64_t i = 0; i &lt; count; ++i) {\n        results.push_back(m_worker_function(std::move(tasks[static_cast&lt;size_t&gt;(i)])));\n      }\n      write_result_range(start, results);\n    };\n\n    while (true) {\n      bool made_progress = false;\n\n      if (pending_start != -1) {\n        const int64_t total = atomic_read(TOTAL_OFF);\n        const int64_t ready = std::min(pending_end, total);\n        if (ready &gt; pending_start) {\n          process_range(pending_start, ready - pending_start);\n          pending_start = ready;\n          made_progress = true;\n        }\n        if (pending_start &gt;= pending_end) {\n          pending_start = -1;\n        } else if (finished_seen) {\n          pending_start = -1;\n        }\n      } else {\n        // Deliberately NOT gated on !finished_seen: see the comment on the\n        // exit check below -- gating this branch on !finished_seen was the\n        // exact bug caught via gdb (15/16 ranks parked in the destructor's\n        // teardown barrier, the 16th spinning forever re-discovering a real\n        // unclaimed gap it had no way left to act on).\n        if (cached_head &gt;= cached_total &amp;&amp; !finished_seen) cached_total = atomic_read(TOTAL_OFF);\n\n        if (cached_head &lt; cached_total) {\n          const int64_t claim =\n              std::min&lt;int64_t&gt;(m_config.claim_batch_size, cached_total - cached_head);\n          const int64_t start = fetch_add(HEAD_OFF, claim);\n          const int64_t end = start + claim;\n          cached_head = end;\n\n          const int64_t total = (end &lt;= cached_total) ? cached_total : atomic_read(TOTAL_OFF);\n          // Clamped into [start, end], not just capped at end: total can be\n          // *less than start* here, not only less than end -- cached_total\n          // (used above to size the claim) can be stale enough that this\n          // worker's whole claim lands beyond what's actually published yet,\n          // not merely straddling the boundary. Before this clamp, an\n          // unclamped `ready = std::min(end, total)` could come out below\n          // `start`, and the pending_start assignment below would then set\n          // pending_start to that sub-start value -- an index this worker\n          // never claimed and does not own, since fetch_add already handed\n          // it to whichever earlier claimant's range covers it. This was a\n          // real, confirmed bug: two ranks ended up both writing results\n          // for the same task index (one legitimately, one via this\n          // corrupted pending_start), and whichever write landed last in\n          // the completion log silently clobbered the other's entry,\n          // permanently stalling the manager's harvest (confirmed via\n          // instrumented logging on a caught hang -- see git history).\n          const int64_t ready = std::clamp(total, start, end);\n          if (ready &gt; start) {\n            process_range(start, ready - start);\n            made_progress = true;\n          }\n          if (ready &lt; end) {\n            pending_start = ready;\n            pending_end = end;\n          }\n        }\n      }\n\n      if (!finished_seen &amp;&amp; atomic_read(FINISHED_OFF) != 0) finished_seen = true;\n\n      if (pending_start == -1 &amp;&amp; finished_seen) {\n        const int64_t final_total = atomic_read(TOTAL_OFF);\n        if (cached_head &gt;= final_total) break;\n        cached_total = final_total;\n      }\n      if (!made_progress) detail::rma_wait_idle(m_window);\n    }\n  }\n\n private:\n  // Window layout: [head][total][finished] then the task table, the result\n  // table, and the completion log, each sized for max_tasks entries. No\n  // gather-sequence field -- there is no gather protocol.\n  static constexpr MPI_Aint HEAD_OFF = 0;\n  static constexpr MPI_Aint TOTAL_OFF = 8;\n  static constexpr MPI_Aint FINISHED_OFF = 16;\n  static constexpr size_t CONTROL_BYTES = 24;\n\n  // Task slot: [count][data].\n  static constexpr size_t T_COUNT = 0;\n  static constexpr size_t T_DATA = 8;\n  // Result slot: [count][data]. No valid flag here -- validity lives in the\n  // completion log instead (see write_result_range), one entry per whole\n  // claimed batch rather than one flag per task.\n  static constexpr size_t R_COUNT = 0;\n  static constexpr size_t R_DATA = 8;\n  // Completion log entry: a single int64. 0 = untouched (sentinel; a real\n  // entry always has count &gt;= 1, see process_range's `ready &gt; start` guard\n  // before it ever calls write_result_range). count &gt; 0 at log index `s`\n  // means \"the batch claimed at task index s, covering [s, s+count), is\n  // fully written to the result table\". Keyed by the claim's own start\n  // index -- collision-free for free, since fetch_add already guarantees\n  // this worker is the sole owner of that start index, so no separate\n  // slot-allocation round trip is needed to place this entry.\n  static constexpr size_t LOG_ENTRY_BYTES = 8;\n\n  Config m_config;\n  Comm m_comm;\n  std::function&lt;ResultT(TaskT)&gt; m_worker_function;\n\n  MPI_Win m_window = MPI_WIN_NULL;\n  std::vector&lt;std::byte&gt; m_window_buffer;  // manager: control + task table + result table + log\n  alignas(int64_t) std::byte m_worker_window[sizeof(int64_t)]{};\n  bool m_finalized = false;\n\n  size_t m_task_elem = 0;\n  size_t m_result_elem = 0;\n  size_t m_max_task_count = 1;\n  size_t m_max_result_count = 1;\n  size_t m_task_slot_stride = 0;\n  size_t m_result_slot_stride = 0;\n  size_t m_task_base = 0;\n  size_t m_result_base = 0;\n  size_t m_log_base = 0;\n\n  int64_t m_total_tasks = 0;\n  // Doubles as the completion-log scan frontier: the manager only ever\n  // advances it by exactly the number of newly-confirmed results each\n  // harvest, so \"task indices below m_collected_count are fully collected\"\n  // and \"the log/result tables are unscanned above it\" stay equivalent by\n  // construction -- no separate frontier variable needed.\n  size_t m_collected_count = 0;\n  size_t m_returned_count = 0;\n  std::vector&lt;ResultT&gt; m_results;         // manager only: harvested, ready to return\n  std::vector&lt;TaskT&gt; m_task_store;        // manager only, solo-world fallback\n  StatisticsT m_statistics;\n\n  void initialize_window() {\n    m_task_elem = static_cast&lt;size_t&gt;(detail::mpi_type_size_bytes&lt;TaskT&gt;());\n    m_result_elem = static_cast&lt;size_t&gt;(detail::mpi_type_size_bytes&lt;ResultT&gt;());\n    m_max_task_count =\n        MPI_Type&lt;TaskT&gt;::resize_required ? static_cast&lt;size_t&gt;(m_config.max_task_count) : 1;\n    m_max_result_count =\n        MPI_Type&lt;ResultT&gt;::resize_required ? static_cast&lt;size_t&gt;(m_config.max_result_count) : 1;\n\n    m_task_slot_stride = detail::round_up_8(T_DATA + m_max_task_count * m_task_elem);\n    m_result_slot_stride = detail::round_up_8(R_DATA + m_max_result_count * m_result_elem);\n\n    const size_t capacity = static_cast&lt;size_t&gt;(m_config.max_tasks);\n    m_task_base = CONTROL_BYTES;\n    m_result_base = m_task_base + capacity * m_task_slot_stride;\n    m_log_base = m_result_base + capacity * m_result_slot_stride;\n    const size_t manager_window_bytes = m_log_base + capacity * LOG_ENTRY_BYTES;\n\n    if (num_workers() == 0) return;  // solo world: no RMA window needed at all\n\n    if (is_root_manager()) {\n      m_window_buffer.resize(manager_window_bytes);\n    }\n\n    void* base = nullptr;\n    MPI_Aint bsize = 0;\n    if (is_root_manager()) {\n      base = m_window_buffer.data();\n      bsize = static_cast&lt;MPI_Aint&gt;(m_window_buffer.size());\n    } else {\n      base = m_worker_window;\n      bsize = static_cast&lt;MPI_Aint&gt;(sizeof(m_worker_window));\n    }\n    DYNAMPI_MPI_CHECK(MPI_Win_create, (base, bsize, 1, MPI_INFO_NULL, m_comm.get(), &amp;m_window));\n    DYNAMPI_MPI_CHECK(MPI_Win_lock_all, (MPI_MODE_NOCHECK, m_window));\n  }\n\n  MPI_Aint task_slot(int64_t index) const {\n    return static_cast&lt;MPI_Aint&gt;(m_task_base + static_cast&lt;size_t&gt;(index) * m_task_slot_stride);\n  }\n  MPI_Aint result_slot(int64_t index) const {\n    return static_cast&lt;MPI_Aint&gt;(m_result_base + static_cast&lt;size_t&gt;(index) * m_result_slot_stride);\n  }\n  MPI_Aint log_slot(int64_t index) const {\n    return static_cast&lt;MPI_Aint&gt;(m_log_base + static_cast&lt;size_t&gt;(index) * LOG_ENTRY_BYTES);\n  }\n\n  // --- RMA primitives (claimant side) ---\n\n  void flush(int rank) { DYNAMPI_MPI_CHECK(MPI_Win_flush, (rank, m_window)); }\n\n  int64_t atomic_read(MPI_Aint offset) {\n    int64_t in = 0, out;\n    DYNAMPI_MPI_CHECK(MPI_Fetch_and_op,\n                      (&amp;in, &amp;out, MPI_INT64_T, m_config.manager_rank, offset, MPI_NO_OP, m_window));\n    flush(m_config.manager_rank);\n    return out;\n  }\n  void atomic_set(MPI_Aint offset, int64_t value) {\n    int64_t in = value, out;\n    DYNAMPI_MPI_CHECK(MPI_Fetch_and_op, (&amp;in, &amp;out, MPI_INT64_T, m_config.manager_rank, offset,\n                                         MPI_REPLACE, m_window));\n    flush(m_config.manager_rank);\n  }\n  int64_t fetch_add(MPI_Aint offset, int64_t increment) {\n    int64_t in = increment, out;\n    DYNAMPI_MPI_CHECK(MPI_Fetch_and_op,\n                      (&amp;in, &amp;out, MPI_INT64_T, m_config.manager_rank, offset, MPI_SUM, m_window));\n    flush(m_config.manager_rank);\n    return out;\n  }\n  // MPI_Put/MPI_Get take a plain `int` count, but n (a byte length derived\n  // from task/result-table capacities that can legitimately reach into the\n  // hundreds of millions of slots) can exceed INT_MAX. static_cast&lt;int&gt;(n)\n  // on an over-INT_MAX n silently wraps -- see the identical fix (and its\n  // fuller rationale) in hierarchical_async_put_lockfree_distributor.hpp's\n  // put_bytes()/get_bytes(), which share this exact helper shape. Chunk\n  // into INT_MAX-bounded pieces so no single MPI_Put/MPI_Get call is ever\n  // handed a count that doesn't fit in `int`.\n  static constexpr size_t kMaxRmaChunkBytes = static_cast&lt;size_t&gt;(std::numeric_limits&lt;int&gt;::max());\n\n  void put_bytes(const void* src, size_t n, MPI_Aint offset) {\n    if (n == 0) return;\n    const auto* bytes = static_cast&lt;const std::byte*&gt;(src);\n    size_t done = 0;\n    while (done &lt; n) {\n      const size_t chunk = std::min(kMaxRmaChunkBytes, n - done);\n      DYNAMPI_MPI_CHECK(MPI_Put, (bytes + done, static_cast&lt;int&gt;(chunk), MPI_BYTE,\n                                  m_config.manager_rank, offset + static_cast&lt;MPI_Aint&gt;(done),\n                                  static_cast&lt;int&gt;(chunk), MPI_BYTE, m_window));\n      done += chunk;\n    }\n    flush(m_config.manager_rank);\n  }\n  void get_bytes(void* dst, size_t n, MPI_Aint offset) {\n    if (n == 0) return;\n    auto* bytes = static_cast&lt;std::byte*&gt;(dst);\n    size_t done = 0;\n    while (done &lt; n) {\n      const size_t chunk = std::min(kMaxRmaChunkBytes, n - done);\n      DYNAMPI_MPI_CHECK(MPI_Get, (bytes + done, static_cast&lt;int&gt;(chunk), MPI_BYTE,\n                                  m_config.manager_rank, offset + static_cast&lt;MPI_Aint&gt;(done),\n                                  static_cast&lt;int&gt;(chunk), MPI_BYTE, m_window));\n      done += chunk;\n    }\n    flush(m_config.manager_rank);\n  }\n\n  // --- Task publish (manager side) / read (claimant side) ---\n\n  // Publishes tasks.size() tasks in two round trips total, regardless of\n  // how many: one bulk Put for the whole range's [count][data] slots, then\n  // one atomic_set for TOTAL_OFF at the end -- the same batching this class\n  // already applies on the claim side (fetch_add) and the result-write side\n  // (write_result_range). Before this, insert_task()/insert_tasks() called\n  // a per-task publish_task() that did its own Put + atomic_set per task (2\n  // round trips per task, unbatched) -- confirmed via measurement to be the\n  // actual bottleneck once the claim and result paths were already batched:\n  // near-zero-compute-time throughput (isolating publish+claim+write from\n  // real work) only reached ~18K tasks/s against rma_atomic_microbench's\n  // ~900K/s raw ceiling, and this per-task publish loop is exactly the same\n  // shape of problem already fixed twice elsewhere in this file.\n  //\n  // TOTAL_OFF is only bumped once, after every task's data is durably in\n  // place -- unlike the old per-task version, a claimant can no longer\n  // observe a bumped TOTAL_OFF for some prefix of this batch while the rest\n  // is still being Put; that's fine because TOTAL_OFF is the only signal\n  // that gates claiming, and this batch was never claimable mid-flight\n  // either way (compare to write_result_range's ordering requirement, which\n  // is about a *different* pair of fields).\n  void publish_tasks(std::span&lt;const TaskT&gt; tasks) {\n    if (tasks.empty()) return;\n    const int64_t start = m_total_tasks;\n    assert(static_cast&lt;size_t&gt;(start) + tasks.size() &lt;= static_cast&lt;size_t&gt;(m_config.max_tasks) &amp;&amp;\n           \"AsyncPutLockFree: exceeded max_tasks capacity\");\n    if (num_workers() == 0) {\n      m_task_store.insert(m_task_store.end(), tasks.begin(), tasks.end());\n      m_total_tasks += static_cast&lt;int64_t&gt;(tasks.size());\n      return;\n    }\n    std::vector&lt;std::byte&gt; buffer(tasks.size() * m_task_slot_stride);\n    size_t total_data_bytes = 0;\n    for (size_t i = 0; i &lt; tasks.size(); ++i) {\n      const TaskT&amp; task = tasks[i];\n      const int count = MPI_Type&lt;TaskT&gt;::count(task);\n      assert(static_cast&lt;size_t&gt;(count) &lt;= m_max_task_count &amp;&amp;\n             \"AsyncPutLockFree: task exceeds max_task_count\");\n      const size_t data_bytes = static_cast&lt;size_t&gt;(count) * m_task_elem;\n      const size_t off = i * m_task_slot_stride;\n      detail::write_i64(buffer.data(), buffer.size(), off + T_COUNT, count);\n      if (data_bytes &gt; 0) {\n        detail::write_bytes(buffer.data(), buffer.size(), off + T_DATA, MPI_Type&lt;TaskT&gt;::ptr(task),\n                            data_bytes);\n      }\n      total_data_bytes += data_bytes;\n    }\n    put_bytes(buffer.data(), buffer.size(), task_slot(start));\n    m_total_tasks += static_cast&lt;int64_t&gt;(tasks.size());\n    atomic_set(TOTAL_OFF, m_total_tasks);\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      m_statistics.comm_statistics.bytes_sent += total_data_bytes;\n      m_statistics.comm_statistics.send_count++;\n    }\n  }\n\n  std::vector&lt;TaskT&gt; read_task_batch(int64_t index, int64_t count) {\n    const size_t bytes = static_cast&lt;size_t&gt;(count) * m_task_slot_stride;\n    std::vector&lt;std::byte&gt; buf(bytes);\n    get_bytes(buf.data(), bytes, task_slot(index));\n\n    std::vector&lt;TaskT&gt; tasks;\n    tasks.reserve(static_cast&lt;size_t&gt;(count));\n    for (int64_t i = 0; i &lt; count; ++i) {\n      const size_t slot_offset = static_cast&lt;size_t&gt;(i) * m_task_slot_stride;\n      const int64_t elem_count = detail::read_i64(buf.data(), buf.size(), slot_offset + T_COUNT);\n      TaskT task{};\n      if constexpr (MPI_Type&lt;TaskT&gt;::resize_required)\n        MPI_Type&lt;TaskT&gt;::resize(task, static_cast&lt;int&gt;(elem_count));\n      const size_t data_bytes = static_cast&lt;size_t&gt;(elem_count) * m_task_elem;\n      detail::read_result_bytes(buf.data(), buf.size(), slot_offset + T_DATA, task, data_bytes);\n      tasks.push_back(std::move(task));\n    }\n    return tasks;\n  }\n\n  // --- Result write (claimant side) / harvest (manager side) ---\n\n  // Writes results.size() results starting at task index `start` in two\n  // round trips total, regardless of how many: one bulk Put for the whole\n  // range's [count][data] slots, then one completion-log entry covering the\n  // whole range. The log entry must land after the data Put's flush (data\n  // durably in place before anything can observe the log entry) -- exactly\n  // the same ordering argument as the single-result version this replaced,\n  // just amortized over a whole batch instead of one flag per task.\n  void write_result_range(int64_t start, const std::vector&lt;ResultT&gt;&amp; results) {\n    const int64_t count = static_cast&lt;int64_t&gt;(results.size());\n    assert(count &gt; 0);\n    std::vector&lt;std::byte&gt; buffer(static_cast&lt;size_t&gt;(count) * m_result_slot_stride);\n    for (int64_t i = 0; i &lt; count; ++i) {\n      const ResultT&amp; result = results[static_cast&lt;size_t&gt;(i)];\n      const int elem_count = MPI_Type&lt;ResultT&gt;::count(result);\n      assert(elem_count &gt;= 0);\n      const size_t data_bytes =\n          elem_count &gt; 0 ? static_cast&lt;size_t&gt;(elem_count) * m_result_elem : size_t{0};\n      const size_t off = static_cast&lt;size_t&gt;(i) * m_result_slot_stride;\n      detail::write_i64(buffer.data(), buffer.size(), off + R_COUNT, elem_count);\n      if (data_bytes &gt; 0) {\n        detail::write_bytes(buffer.data(), buffer.size(), off + R_DATA,\n                            MPI_Type&lt;ResultT&gt;::ptr(result), data_bytes);\n      }\n    }\n    put_bytes(buffer.data(), buffer.size(), result_slot(start));\n    atomic_set(log_slot(start), count);\n  }\n\n  // Owner-only. Three round trips regardless of how many batches turn out\n  // to be ready: one to see how far claiming has progressed (HEAD_OFF), one\n  // bulk read of the completion log over the unscanned range, one bulk read\n  // of the result table over whatever contiguous prefix of that range the\n  // log confirms is done. A gap (a still-in-flight or straggling batch)\n  // simply stops the contiguous-prefix scan there for this call; already-\n  // confirmed entries past a gap are picked up on a later call once the gap\n  // fills in, at the cost of re-scanning that stretch of the log -- cheap,\n  // since the scan is one bulk Get either way, not a per-entry round trip.\n  //\n  // Goes through the real RMA API (Fetch_and_op/Get), even though the\n  // target is this same rank: MPI only guarantees a window owner sees\n  // another rank's completed RMA writes via its *local* loads under\n  // MPI_WIN_UNIFIED, not MPI_WIN_SEPARATE, and the RMA API is correct\n  // either way. Confirmed necessary via a caught hang earlier: plain local\n  // loads on m_window_buffer never observed workers' writes at all.\n  void harvest_ready_results() {\n    assert(is_root_manager());\n    const int64_t head_now = atomic_read(HEAD_OFF);\n    const int64_t frontier = static_cast&lt;int64_t&gt;(m_collected_count);\n    if (head_now &lt;= frontier) return;\n\n    const size_t scan_count = static_cast&lt;size_t&gt;(head_now - frontier);\n    std::vector&lt;std::byte&gt; log_buf(scan_count * LOG_ENTRY_BYTES);\n    get_bytes(log_buf.data(), log_buf.size(), log_slot(frontier));\n\n    int64_t confirmed_end = frontier;\n    while (confirmed_end &lt; head_now) {\n      const size_t off = static_cast&lt;size_t&gt;(confirmed_end - frontier) * LOG_ENTRY_BYTES;\n      const int64_t count = detail::read_i64(log_buf.data(), log_buf.size(), off);\n      if (count == 0) break;  // gap: not yet written; stop the contiguous prefix here\n      confirmed_end += count;\n    }\n    if (confirmed_end &lt;= frontier) return;\n\n    const int64_t n = confirmed_end - frontier;\n    std::vector&lt;std::byte&gt; result_buf(static_cast&lt;size_t&gt;(n) * m_result_slot_stride);\n    get_bytes(result_buf.data(), result_buf.size(), result_slot(frontier));\n\n    m_results.reserve(m_results.size() + static_cast&lt;size_t&gt;(n));\n    for (int64_t i = 0; i &lt; n; ++i) {\n      const size_t off = static_cast&lt;size_t&gt;(i) * m_result_slot_stride;\n      const int64_t count = detail::read_i64(result_buf.data(), result_buf.size(), off + R_COUNT);\n      ResultT result{};\n      if constexpr (MPI_Type&lt;ResultT&gt;::resize_required)\n        MPI_Type&lt;ResultT&gt;::resize(result, static_cast&lt;int&gt;(count));\n      const size_t data_bytes = count &gt; 0 ? static_cast&lt;size_t&gt;(count) * m_result_elem : size_t{0};\n      detail::read_result_bytes(result_buf.data(), result_buf.size(), off + R_DATA, result,\n                                data_bytes);\n      m_results.push_back(std::move(result));\n    }\n    m_collected_count = static_cast&lt;size_t&gt;(confirmed_end);\n  }\n\n  void run_one_task_locally() {\n    m_results.push_back(m_worker_function(m_task_store[m_collected_count]));\n    m_collected_count++;\n  }\n\n  std::vector&lt;ResultT&gt; drain_results(size_t limit) {\n    const size_t count = std::min(limit, m_results.size());\n    std::vector&lt;ResultT&gt; output;\n    output.reserve(count);\n    for (size_t i = 0; i &lt; count; ++i) output.push_back(std::move(m_results[i]));\n    m_results.erase(m_results.begin(), m_results.begin() + static_cast&lt;ptrdiff_t&gt;(count));\n    m_returned_count += count;\n    return output;\n  }\n};\n\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/base__distributor_8hpp/","title":"File base_distributor.hpp","text":"<p>FileList &gt; dynampi &gt; impl &gt; base_distributor.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;mpi.h&gt;</code></li> <li><code>#include &lt;deque&gt;</code></li> <li><code>#include &lt;queue&gt;</code></li> <li><code>#include &lt;type_traits&gt;</code></li> <li><code>#include &lt;utility&gt;</code></li> </ul>"},{"location":"dynampi/base__distributor_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi"},{"location":"dynampi/base__distributor_8hpp/#classes","title":"Classes","text":"Type Name class BaseMPIWorkDistributor &lt;typename TaskT, typename ResultT, Options&gt; struct Config struct enable_prioritization struct prioritize_tasks_t <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/base_distributor.hpp</code></p>"},{"location":"dynampi/base__distributor_8hpp_source/","title":"File base_distributor.hpp","text":"<p>File List &gt; dynampi &gt; impl &gt; base_distributor.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2025 QDX Technologies. Authored by Ryan Stocks &lt;ryan.stocks00@gmail.com&gt;\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;mpi.h&gt;\n\n#include &lt;deque&gt;\n#include &lt;queue&gt;\n#include &lt;type_traits&gt;\n#include &lt;utility&gt;\n\nnamespace dynampi {\n\nstruct prioritize_tasks_t {\n  static constexpr bool value = false;\n};\n\nstruct enable_prioritization : public prioritize_tasks_t {\n  static constexpr bool value = true;\n};\n\ntemplate &lt;typename TaskT, typename ResultT, typename... Options&gt;\nclass BaseMPIWorkDistributor {\n public:\n  struct Config {\n    MPI_Comm comm = MPI_COMM_WORLD;\n    int manager_rank = 0;\n    bool auto_run_workers = true;\n  };\n\n protected:\n  static constexpr bool prioritize_tasks = get_option_value&lt;prioritize_tasks_t, Options...&gt;();\n  using QueueT = std::conditional_t&lt;prioritize_tasks, std::priority_queue&lt;std::pair&lt;double, TaskT&gt;&gt;,\n                                    std::deque&lt;TaskT&gt;&gt;;\n};\n\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/hierarchical__async__put__lockfree__distributor_8hpp/","title":"File hierarchical_async_put_lockfree_distributor.hpp","text":"<p>FileList &gt; dynampi &gt; impl &gt; hierarchical_async_put_lockfree_distributor.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;algorithm&gt;</code></li> <li><code>#include &lt;cassert&gt;</code></li> <li><code>#include &lt;cstddef&gt;</code></li> <li><code>#include &lt;cstdint&gt;</code></li> <li><code>#include &lt;deque&gt;</code></li> <li><code>#include &lt;functional&gt;</code></li> <li><code>#include &lt;limits&gt;</code></li> <li><code>#include &lt;optional&gt;</code></li> <li><code>#include &lt;vector&gt;</code></li> <li><code>#include \"../mpi/mpi_communicator.hpp\"</code></li> <li><code>#include \"../mpi/mpi_group.hpp\"</code></li> <li><code>#include \"../mpi/mpi_types.hpp\"</code></li> <li><code>#include \"dynampi/impl/lockfree_distributor.hpp\"</code></li> <li><code>#include \"dynampi/mpi/mpi_error.hpp\"</code></li> <li><code>#include \"dynampi/utilities/timer.hpp\"</code></li> </ul>"},{"location":"dynampi/hierarchical__async__put__lockfree__distributor_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi namespace detail"},{"location":"dynampi/hierarchical__async__put__lockfree__distributor_8hpp/#classes","title":"Classes","text":"Type Name class HierarchicalAsyncPutLockFreeMPIWorkDistributor &lt;typename TaskT, typename ResultT, Options&gt; struct Config struct RunConfig class AsyncPutLevel &lt;typename TaskT, typename ResultT&gt; struct ClaimedRange struct Config <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_async_put_lockfree_distributor.hpp</code></p>"},{"location":"dynampi/hierarchical__async__put__lockfree__distributor_8hpp_source/","title":"File hierarchical_async_put_lockfree_distributor.hpp","text":"<p>File List &gt; dynampi &gt; impl &gt; hierarchical_async_put_lockfree_distributor.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2026 Ryan Stocks\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;algorithm&gt;\n#include &lt;cassert&gt;\n#include &lt;cstddef&gt;\n#include &lt;cstdint&gt;\n#include &lt;deque&gt;\n#include &lt;functional&gt;\n#include &lt;limits&gt;\n#include &lt;optional&gt;\n#include &lt;vector&gt;\n\n#include \"../mpi/mpi_communicator.hpp\"\n#include \"../mpi/mpi_group.hpp\"\n#include \"../mpi/mpi_types.hpp\"\n#include \"dynampi/impl/lockfree_distributor.hpp\"  // reuses dynampi::detail byte-packing helpers\n#include \"dynampi/mpi/mpi_error.hpp\"\n#include \"dynampi/utilities/timer.hpp\"\n\nnamespace dynampi {\n\nnamespace detail {\n\n// ---------------------------------------------------------------------------\n// AsyncPutLevel\n//\n// One level of AsyncPutLockFreeMPIWorkDistributor's protocol (fetch-and-add\n// task claiming, one-sided Put of results plus a completion log, no\n// collective calls anywhere in the hot path), scoped to an arbitrary\n// communicator and factored out here so it can be composed at multiple\n// levels of a tree topology -- see HierarchicalAsyncPutLockFreeMPIWorkDistributor\n// below, which instantiates one AsyncPutLevel per tree level: manager&lt;-&gt;node\n// coordinators, and, independently, node coordinator&lt;-&gt;its local workers.\n//\n// Unlike detail::LockFreeLevel (hierarchical_lockfree_distributor.hpp), there\n// is no gather-round/barrier/broadcast protocol here at all: publishing,\n// claiming, writing results, and harvesting are each purely one-sided,\n// independent operations with no requirement that every claimant\n// participate in lockstep. This makes the state machine considerably\n// simpler than LockFreeLevel's, and is exactly why the flat\n// AsyncPutLockFreeMPIWorkDistributor reaches such a high throughput ceiling\n// in the first place (see that file's class comment) -- composing it\n// hierarchically doesn't need to reintroduce any of that.\n//\n// The claim/write-result API is split into discrete non-blocking steps\n// (try_claim() / write_result_range()) rather than a single \"claim, compute,\n// write\" loop like the flat distributor's run_worker(): a coordinator using\n// this level as a claimant doesn't compute results itself, it republishes\n// claimed tasks into a *different* AsyncPutLevel (the local level) and only\n// later, once those results come back, writes them here -- so the caller\n// needs to be able to interleave claim and write independently. A plain leaf\n// worker (see run_local_worker()/run_leaf_leader_worker() below) just calls\n// both back-to-back in its own loop, which is exactly the flat class's\n// pattern reassembled from the same two primitives.\n// ---------------------------------------------------------------------------\ntemplate &lt;typename TaskT, typename ResultT&gt;\nclass AsyncPutLevel {\n public:\n  struct Config {\n    MPI_Comm comm = MPI_COMM_NULL;\n    int owner_rank = 0;\n    int max_tasks = 8192;\n    int max_task_count = 256;\n    int max_result_count = 256;\n    int claim_batch_size = 8;\n  };\n\n  struct ClaimedRange {\n    int64_t start = -1;  // -1: nothing claimable right now (not necessarily drained -- see drained())\n    std::vector&lt;TaskT&gt; tasks;\n  };\n\n  explicit AsyncPutLevel(Config config) : m_config(config) { initialize_window(); }\n\n  AsyncPutLevel(const AsyncPutLevel&amp;) = delete;\n  AsyncPutLevel&amp; operator=(const AsyncPutLevel&amp;) = delete;\n\n  ~AsyncPutLevel() {\n    if (m_window != MPI_WIN_NULL) {\n      DYNAMPI_MPI_CHECK(MPI_Win_unlock_all, (m_window));\n      MPI_Barrier(m_config.comm);\n      DYNAMPI_MPI_CHECK(MPI_Win_free, (&amp;m_window));\n      m_window = MPI_WIN_NULL;\n    }\n  }\n\n  bool is_owner() const { return comm_rank() == m_config.owner_rank; }\n  int comm_rank() const {\n    int r = 0;\n    MPI_Comm_rank(m_config.comm, &amp;r);\n    return r;\n  }\n  int comm_size() const {\n    int s = 0;\n    MPI_Comm_size(m_config.comm, &amp;s);\n    return s;\n  }\n\n  void idle_wait() { detail::rma_wait_idle(m_window); }\n\n  // How many tasks a single try_claim() asks for at a time -- exposed so a\n  // caller managing several levels (see HierarchicalAsyncPutLockFreeMPIWork\n  // Distributor's step_bridge_hop()) can size a claim-ahead backpressure cap\n  // relative to this level's own configured granularity.\n  int claim_batch_size() const { return m_config.claim_batch_size; }\n\n  // --- Owner-side API ---\n\n  // Publishes tasks.size() tasks in two round trips total regardless of how\n  // many: one bulk Put, then one atomic_set(TOTAL_OFF) -- see\n  // AsyncPutLockFreeMPIWorkDistributor::publish_tasks() for the full\n  // rationale (this is the same fix applied there).\n  void publish_tasks(const std::vector&lt;TaskT&gt;&amp; tasks) {\n    assert(is_owner());\n    if (tasks.empty()) return;\n    const int64_t start = m_total_tasks;\n    assert(static_cast&lt;size_t&gt;(start) + tasks.size() &lt;= static_cast&lt;size_t&gt;(m_config.max_tasks) &amp;&amp;\n           \"AsyncPutLevel: exceeded max_tasks capacity\");\n    if (comm_size() &gt; 1) {\n      std::vector&lt;std::byte&gt; buffer(tasks.size() * m_task_slot_stride);\n      for (size_t i = 0; i &lt; tasks.size(); ++i) {\n        const TaskT&amp; task = tasks[i];\n        const int count = MPI_Type&lt;TaskT&gt;::count(task);\n        assert(static_cast&lt;size_t&gt;(count) &lt;= m_max_task_count &amp;&amp;\n               \"AsyncPutLevel: task exceeds max_task_count\");\n        const size_t data_bytes = static_cast&lt;size_t&gt;(count) * m_task_elem;\n        const size_t off = i * m_task_slot_stride;\n        detail::write_i64(buffer.data(), buffer.size(), off + T_COUNT, count);\n        if (data_bytes &gt; 0) {\n          detail::write_bytes(buffer.data(), buffer.size(), off + T_DATA, MPI_Type&lt;TaskT&gt;::ptr(task),\n                              data_bytes);\n        }\n      }\n      put_bytes(buffer.data(), buffer.size(), task_slot(start));\n      m_total_tasks += static_cast&lt;int64_t&gt;(tasks.size());\n      atomic_set(TOTAL_OFF, m_total_tasks);\n    } else {\n      m_total_tasks += static_cast&lt;int64_t&gt;(tasks.size());\n    }\n  }\n\n  void mark_finished() {\n    assert(is_owner());\n    m_owner_marked_finished = true;\n    if (comm_size() &gt; 1) {\n      atomic_set(FINISHED_OFF, 1);\n      idle_wait();\n    }\n  }\n\n  bool owner_marked_finished() const {\n    assert(is_owner());\n    return m_owner_marked_finished;\n  }\n\n  size_t owner_published_count() const {\n    assert(is_owner());\n    return static_cast&lt;size_t&gt;(m_total_tasks);\n  }\n  size_t owner_collected_count() const {\n    assert(is_owner());\n    return m_owner_collected_count;\n  }\n\n  // Returns whatever newly-confirmed contiguous prefix of results is ready\n  // right now (possibly empty) in at most three round trips: one to see how\n  // far claiming has progressed, one bulk read of the unscanned completion\n  // log, one bulk read of the result table over the confirmed prefix. See\n  // AsyncPutLockFreeMPIWorkDistributor::harvest_ready_results() for the full\n  // derivation (this is the same logic, factored out so it isn't tied to a\n  // single top-level result vector -- see this level's callers, which each\n  // route the returned batch somewhere different: the top-level manager\n  // appends it straight to its own result buffer, while a node coordinator\n  // routes it into a relay queue bound for the leader level).\n  std::vector&lt;ResultT&gt; harvest_ready_results() {\n    assert(is_owner());\n    if (comm_size() == 1) return {};\n    const int64_t head_now = atomic_read(HEAD_OFF);\n    const int64_t frontier = static_cast&lt;int64_t&gt;(m_owner_collected_count);\n    if (head_now &lt;= frontier) return {};\n\n    const size_t scan_count = static_cast&lt;size_t&gt;(head_now - frontier);\n    std::vector&lt;std::byte&gt; log_buf(scan_count * LOG_ENTRY_BYTES);\n    get_bytes(log_buf.data(), log_buf.size(), log_slot(frontier));\n\n    int64_t confirmed_end = frontier;\n    while (confirmed_end &lt; head_now) {\n      const size_t off = static_cast&lt;size_t&gt;(confirmed_end - frontier) * LOG_ENTRY_BYTES;\n      const int64_t count = detail::read_i64(log_buf.data(), log_buf.size(), off);\n      if (count == 0) break;  // gap: not yet written; stop the contiguous prefix here\n      confirmed_end += count;\n    }\n    if (confirmed_end &lt;= frontier) return {};\n\n    const int64_t n = confirmed_end - frontier;\n    std::vector&lt;std::byte&gt; result_buf(static_cast&lt;size_t&gt;(n) * m_result_slot_stride);\n    get_bytes(result_buf.data(), result_buf.size(), result_slot(frontier));\n\n    std::vector&lt;ResultT&gt; output;\n    output.reserve(static_cast&lt;size_t&gt;(n));\n    for (int64_t i = 0; i &lt; n; ++i) {\n      const size_t off = static_cast&lt;size_t&gt;(i) * m_result_slot_stride;\n      const int64_t count = detail::read_i64(result_buf.data(), result_buf.size(), off + R_COUNT);\n      ResultT result{};\n      if constexpr (MPI_Type&lt;ResultT&gt;::resize_required)\n        MPI_Type&lt;ResultT&gt;::resize(result, static_cast&lt;int&gt;(count));\n      const size_t data_bytes = count &gt; 0 ? static_cast&lt;size_t&gt;(count) * m_result_elem : size_t{0};\n      detail::read_result_bytes(result_buf.data(), result_buf.size(), off + R_DATA, result,\n                                data_bytes);\n      output.push_back(std::move(result));\n    }\n    m_owner_collected_count = static_cast&lt;size_t&gt;(confirmed_end);\n    return output;\n  }\n\n  // Like harvest_ready_results(), but backs off briefly if nothing new was\n  // found, instead of being called back-to-back in a tight owner loop --\n  // matching request_gather_throttled()'s role in LockFreeLevel, though the\n  // cost being avoided here is pure CPU/RMA-poll churn rather than gather-\n  // round flooding (there's no round to flood: a throttle here is purely a\n  // politeness backoff, not a correctness requirement).\n  std::vector&lt;ResultT&gt; harvest_ready_results_throttled() {\n    if (comm_size() == 1) return {};\n    const size_t before = owner_collected_count();\n    auto results = harvest_ready_results();\n    if (owner_collected_count() == before) idle_wait();\n    return results;\n  }\n\n  // --- Claimant-side API ---\n  //\n  // NOTE: unlike the owner-side API above, these methods do NOT assert\n  // !is_owner(). A rank promoted to lead a group in\n  // HierarchicalAsyncPutLockFreeMPIWorkDistributor's N-level mode owns this\n  // level (hosts its window) AND must also claim from it (its own subtree\n  // still needs feeding, and there's no other mechanism to get tasks down to\n  // it) -- i.e. the owner can validly be one of its own claimants too, via\n  // self-targeted RMA. This is mechanically identical to any other claimant:\n  // every RMA helper below (atomic_read/atomic_set/fetch_add/put_bytes/\n  // get_bytes) targets m_config.owner_rank unconditionally, so a self-\n  // targeted call behaves exactly like a remote one, just over a loopback --\n  // the same reasoning that already lets an owner read its own window via\n  // harvest_ready_results() instead of a plain memory load (MPI_WIN_SEPARATE).\n\n  // Attempts one non-blocking claim step (same state machine as\n  // AsyncPutLockFreeMPIWorkDistributor::run_worker(), exposed here as a\n  // discrete callable rather than an internal loop -- see the class\n  // comment). Returns start=-1 if nothing is claimable right this instant\n  // (could be a genuine claim miss, a wait for TOTAL_OFF to catch up to an\n  // already-reserved range, or true exhaustion -- see drained() for the\n  // latter).\n  ClaimedRange try_claim() {\n    if (m_pending_start != -1) {\n      const int64_t total = atomic_read(TOTAL_OFF);\n      const int64_t ready = std::min(m_pending_end, total);\n      if (ready &gt; m_pending_start) {\n        const int64_t start = m_pending_start;\n        const int64_t count = ready - m_pending_start;\n        m_pending_start = ready;\n        if (m_pending_start &gt;= m_pending_end || m_seen_finished) m_pending_start = -1;\n        return ClaimedRange{start, read_task_batch(start, count)};\n      }\n      if (m_pending_start &gt;= m_pending_end || m_seen_finished) m_pending_start = -1;\n      return ClaimedRange{};\n    }\n\n    // Deliberately NOT gated on !m_seen_finished when re-checking\n    // cached_total below the threshold -- see drained()'s exit-check\n    // symmetric handling and AsyncPutLockFreeMPIWorkDistributor::run_worker()'s\n    // identical comment: once finished_seen is true, a stale cached_total\n    // must still be correctable by drained()'s own fresh read, not frozen.\n    if (m_cached_head &gt;= m_cached_total &amp;&amp; !m_seen_finished) m_cached_total = atomic_read(TOTAL_OFF);\n\n    if (m_cached_head &lt; m_cached_total) {\n      const int64_t claim =\n          std::min&lt;int64_t&gt;(m_config.claim_batch_size, m_cached_total - m_cached_head);\n      const int64_t start = fetch_add(HEAD_OFF, claim);\n      const int64_t end = start + claim;\n      m_cached_head = end;\n\n      const int64_t total = (end &lt;= m_cached_total) ? m_cached_total : atomic_read(TOTAL_OFF);\n      // Clamped into [start, end]: total can land below start (this whole\n      // claim landed beyond what's published yet), not just below end. See\n      // AsyncPutLockFreeMPIWorkDistributor::run_worker()'s comment on the\n      // exact bug this guards -- an unclamped `min(end, total)` here could\n      // produce a pending_start below this claim's own start, an index this\n      // rank never actually owns, corrupting another claimant's range.\n      const int64_t ready = std::clamp(total, start, end);\n      if (ready &lt; end) {\n        m_pending_start = ready;\n        m_pending_end = end;\n      }\n      if (ready &gt; start) {\n        return ClaimedRange{start, read_task_batch(start, ready - start)};\n      }\n    }\n    return ClaimedRange{};\n  }\n\n  // Refreshes the cached finished-observation; call periodically (try_claim()\n  // does not do this itself, since a caller managing multiple levels --\n  // see run_node_coordinator() -- may want to control polling cadence\n  // explicitly rather than pay a FINISHED_OFF read on every try_claim()).\n  bool check_finished() {\n    if (!m_seen_finished &amp;&amp; atomic_read(FINISHED_OFF) != 0) m_seen_finished = true;\n    return m_seen_finished;\n  }\n\n  // True once nothing more will ever be claimable here: no pending\n  // remainder outstanding, FINISHED_OFF observed, and this rank's own\n  // cached_head has caught up to the true final TOTAL_OFF (a fresh read,\n  // not the possibly-stale cached_total -- mirrors\n  // AsyncPutLockFreeMPIWorkDistributor::run_worker()'s exit check exactly,\n  // including the same reason: cached_total can be stale relative to when\n  // TOTAL_OFF actually stopped growing, since publishing finishes well\n  // before FINISHED_OFF becomes visible to this claimant).\n  bool drained() {\n    // Unconditionally refreshed BEFORE the pending-remainder early return,\n    // not after -- a real, confirmed bug otherwise: a claimant whose\n    // speculative claim landed beyond what's published yet\n    // (m_pending_start != -1, see try_claim()) would return false here\n    // every time without ever reaching the check_finished() call below it,\n    // so m_seen_finished would never become true while a pending remainder\n    // is outstanding. try_claim()'s own pending-resolution branch relies on\n    // m_seen_finished to know when to give up on a remainder that will\n    // never resolve (nothing beyond it will ever be published) -- with\n    // m_seen_finished stuck false, that branch could never clear the\n    // remainder either, deadlocking this claimant forever. Confirmed via a\n    // caught 4-rank hang (two local workers, both stuck with real pending\n    // ranges and m_seen_finished permanently false).\n    check_finished();\n    if (m_pending_start != -1) return false;\n    if (!m_seen_finished) return false;\n    const int64_t final_total = atomic_read(TOTAL_OFF);\n    if (m_cached_head &gt;= final_total) return true;\n    m_cached_total = final_total;  // real unclaimed work remains; try_claim() will pick it up\n    return false;\n  }\n\n  // Writes results.size() results starting at task index `start` in two\n  // round trips total: one bulk Put for [count][data], then one completion-\n  // log entry covering the whole range -- see\n  // AsyncPutLockFreeMPIWorkDistributor::write_result_range() for the\n  // ordering rationale (identical here).\n  void write_result_range(int64_t start, const std::vector&lt;ResultT&gt;&amp; results) {\n    const int64_t count = static_cast&lt;int64_t&gt;(results.size());\n    assert(count &gt; 0);\n    std::vector&lt;std::byte&gt; buffer(static_cast&lt;size_t&gt;(count) * m_result_slot_stride);\n    for (int64_t i = 0; i &lt; count; ++i) {\n      const ResultT&amp; result = results[static_cast&lt;size_t&gt;(i)];\n      const int elem_count = MPI_Type&lt;ResultT&gt;::count(result);\n      assert(elem_count &gt;= 0);\n      const size_t data_bytes =\n          elem_count &gt; 0 ? static_cast&lt;size_t&gt;(elem_count) * m_result_elem : size_t{0};\n      const size_t off = static_cast&lt;size_t&gt;(i) * m_result_slot_stride;\n      detail::write_i64(buffer.data(), buffer.size(), off + R_COUNT, elem_count);\n      if (data_bytes &gt; 0) {\n        detail::write_bytes(buffer.data(), buffer.size(), off + R_DATA,\n                            MPI_Type&lt;ResultT&gt;::ptr(result), data_bytes);\n      }\n    }\n    put_bytes(buffer.data(), buffer.size(), result_slot(start));\n    atomic_set(log_slot(start), count);\n  }\n\n private:\n  // Window layout: [head][total][finished] then the task table, the result\n  // table, and the completion log, each sized for max_tasks entries -- same\n  // layout as AsyncPutLockFreeMPIWorkDistributor.\n  static constexpr MPI_Aint HEAD_OFF = 0;\n  static constexpr MPI_Aint TOTAL_OFF = 8;\n  static constexpr MPI_Aint FINISHED_OFF = 16;\n  static constexpr size_t CONTROL_BYTES = 24;\n\n  static constexpr size_t T_COUNT = 0;\n  static constexpr size_t T_DATA = 8;\n  static constexpr size_t R_COUNT = 0;\n  static constexpr size_t R_DATA = 8;\n  static constexpr size_t LOG_ENTRY_BYTES = 8;\n\n  Config m_config;\n  MPI_Win m_window = MPI_WIN_NULL;\n  std::vector&lt;std::byte&gt; m_window_buffer;\n  alignas(int64_t) std::byte m_peer_window[sizeof(int64_t)]{};\n\n  size_t m_task_elem = 0;\n  size_t m_result_elem = 0;\n  size_t m_max_task_count = 1;\n  size_t m_max_result_count = 1;\n  size_t m_task_slot_stride = 0;\n  size_t m_result_slot_stride = 0;\n  size_t m_task_base = 0;\n  size_t m_result_base = 0;\n  size_t m_log_base = 0;\n\n  int64_t m_total_tasks = 0;\n  bool m_owner_marked_finished = false;\n  size_t m_owner_collected_count = 0;\n\n  // Claimant-only state (see try_claim()/drained()).\n  int64_t m_cached_head = 0;\n  int64_t m_cached_total = 0;\n  int64_t m_pending_start = -1;\n  int64_t m_pending_end = -1;\n  bool m_seen_finished = false;\n\n  void initialize_window() {\n    m_task_elem = static_cast&lt;size_t&gt;(detail::mpi_type_size_bytes&lt;TaskT&gt;());\n    m_result_elem = static_cast&lt;size_t&gt;(detail::mpi_type_size_bytes&lt;ResultT&gt;());\n    m_max_task_count =\n        MPI_Type&lt;TaskT&gt;::resize_required ? static_cast&lt;size_t&gt;(m_config.max_task_count) : 1;\n    m_max_result_count =\n        MPI_Type&lt;ResultT&gt;::resize_required ? static_cast&lt;size_t&gt;(m_config.max_result_count) : 1;\n\n    m_task_slot_stride = detail::round_up_8(T_DATA + m_max_task_count * m_task_elem);\n    m_result_slot_stride = detail::round_up_8(R_DATA + m_max_result_count * m_result_elem);\n\n    const size_t capacity = static_cast&lt;size_t&gt;(m_config.max_tasks);\n    m_task_base = CONTROL_BYTES;\n    m_result_base = m_task_base + capacity * m_task_slot_stride;\n    m_log_base = m_result_base + capacity * m_result_slot_stride;\n    const size_t owner_window_bytes = m_log_base + capacity * LOG_ENTRY_BYTES;\n\n    if (is_owner() &amp;&amp; comm_size() &gt; 1) {\n      m_window_buffer.resize(owner_window_bytes);\n    }\n    if (comm_size() == 1) return;\n\n    void* base = nullptr;\n    MPI_Aint bsize = 0;\n    if (is_owner()) {\n      base = m_window_buffer.data();\n      bsize = static_cast&lt;MPI_Aint&gt;(m_window_buffer.size());\n    } else {\n      base = m_peer_window;\n      bsize = static_cast&lt;MPI_Aint&gt;(sizeof(m_peer_window));\n    }\n    DYNAMPI_MPI_CHECK(MPI_Win_create, (base, bsize, 1, MPI_INFO_NULL, m_config.comm, &amp;m_window));\n    DYNAMPI_MPI_CHECK(MPI_Win_lock_all, (MPI_MODE_NOCHECK, m_window));\n  }\n\n  MPI_Aint task_slot(int64_t index) const {\n    return static_cast&lt;MPI_Aint&gt;(m_task_base + static_cast&lt;size_t&gt;(index) * m_task_slot_stride);\n  }\n  MPI_Aint result_slot(int64_t index) const {\n    return static_cast&lt;MPI_Aint&gt;(m_result_base + static_cast&lt;size_t&gt;(index) * m_result_slot_stride);\n  }\n  MPI_Aint log_slot(int64_t index) const {\n    return static_cast&lt;MPI_Aint&gt;(m_log_base + static_cast&lt;size_t&gt;(index) * LOG_ENTRY_BYTES);\n  }\n\n  void flush(int rank) { DYNAMPI_MPI_CHECK(MPI_Win_flush, (rank, m_window)); }\n\n  int64_t atomic_read(MPI_Aint offset) {\n    int64_t in = 0, out;\n    DYNAMPI_MPI_CHECK(MPI_Fetch_and_op,\n                      (&amp;in, &amp;out, MPI_INT64_T, m_config.owner_rank, offset, MPI_NO_OP, m_window));\n    flush(m_config.owner_rank);\n    return out;\n  }\n  void atomic_set(MPI_Aint offset, int64_t value) {\n    int64_t in = value, out;\n    DYNAMPI_MPI_CHECK(MPI_Fetch_and_op, (&amp;in, &amp;out, MPI_INT64_T, m_config.owner_rank, offset,\n                                         MPI_REPLACE, m_window));\n    flush(m_config.owner_rank);\n  }\n  int64_t fetch_add(MPI_Aint offset, int64_t increment) {\n    int64_t in = increment, out;\n    DYNAMPI_MPI_CHECK(MPI_Fetch_and_op,\n                      (&amp;in, &amp;out, MPI_INT64_T, m_config.owner_rank, offset, MPI_SUM, m_window));\n    flush(m_config.owner_rank);\n    return out;\n  }\n  // MPI_Put/MPI_Get take a plain `int` count, but n (a byte length derived\n  // from task/result-table capacities that can legitimately reach into the\n  // hundreds of millions of slots -- see kLockFreeMaxTasks-style configs in\n  // the benchmarks) can exceed INT_MAX. static_cast&lt;int&gt;(n) on an\n  // over-INT_MAX n silently wraps (confirmed: a single-node run pushing\n  // ~100M-capacity tables produced a bulk get_bytes() call whose byte count\n  // overflowed to a small/negative int, which MPI_Get then reinterpreted as\n  // a huge unsigned transfer -- observed downstream as a relay buffer\n  // reporting a \"size\" near 2^32 that took hours to (never) drain, and is\n  // the leading suspect for this class's other unexplained bad_alloc/\n  // segfault crashes at high throughput). Chunk into MPI_Aint-addressed,\n  // INT_MAX-bounded pieces so no single MPI_Put/MPI_Get call is ever handed\n  // a count that doesn't fit in `int`.\n  static constexpr size_t kMaxRmaChunkBytes = static_cast&lt;size_t&gt;(std::numeric_limits&lt;int&gt;::max());\n\n  void put_bytes(const void* src, size_t n, MPI_Aint offset) {\n    if (n == 0) return;\n    const auto* bytes = static_cast&lt;const std::byte*&gt;(src);\n    size_t done = 0;\n    while (done &lt; n) {\n      const size_t chunk = std::min(kMaxRmaChunkBytes, n - done);\n      DYNAMPI_MPI_CHECK(MPI_Put, (bytes + done, static_cast&lt;int&gt;(chunk), MPI_BYTE,\n                                  m_config.owner_rank, offset + static_cast&lt;MPI_Aint&gt;(done),\n                                  static_cast&lt;int&gt;(chunk), MPI_BYTE, m_window));\n      done += chunk;\n    }\n    flush(m_config.owner_rank);\n  }\n  void get_bytes(void* dst, size_t n, MPI_Aint offset) {\n    if (n == 0) return;\n    auto* bytes = static_cast&lt;std::byte*&gt;(dst);\n    size_t done = 0;\n    while (done &lt; n) {\n      const size_t chunk = std::min(kMaxRmaChunkBytes, n - done);\n      DYNAMPI_MPI_CHECK(MPI_Get, (bytes + done, static_cast&lt;int&gt;(chunk), MPI_BYTE,\n                                  m_config.owner_rank, offset + static_cast&lt;MPI_Aint&gt;(done),\n                                  static_cast&lt;int&gt;(chunk), MPI_BYTE, m_window));\n      done += chunk;\n    }\n    flush(m_config.owner_rank);\n  }\n\n  std::vector&lt;TaskT&gt; read_task_batch(int64_t index, int64_t count) {\n    const size_t bytes = static_cast&lt;size_t&gt;(count) * m_task_slot_stride;\n    std::vector&lt;std::byte&gt; buf(bytes);\n    get_bytes(buf.data(), bytes, task_slot(index));\n\n    std::vector&lt;TaskT&gt; tasks;\n    tasks.reserve(static_cast&lt;size_t&gt;(count));\n    for (int64_t i = 0; i &lt; count; ++i) {\n      const size_t slot_offset = static_cast&lt;size_t&gt;(i) * m_task_slot_stride;\n      const int64_t elem_count = detail::read_i64(buf.data(), buf.size(), slot_offset + T_COUNT);\n      TaskT task{};\n      if constexpr (MPI_Type&lt;TaskT&gt;::resize_required)\n        MPI_Type&lt;TaskT&gt;::resize(task, static_cast&lt;int&gt;(elem_count));\n      const size_t data_bytes = static_cast&lt;size_t&gt;(elem_count) * m_task_elem;\n      detail::read_result_bytes(buf.data(), buf.size(), slot_offset + T_DATA, task, data_bytes);\n      tasks.push_back(std::move(task));\n    }\n    return tasks;\n  }\n};\n\n}  // namespace detail\n\n// ---------------------------------------------------------------------------\n// HierarchicalAsyncPutLockFreeMPIWorkDistributor\n//\n// Combines HierarchicalMPIWorkDistributor's node-aware tree topology\n// (manager &lt;-&gt; per-node coordinators &lt;-&gt; per-node local workers) with\n// AsyncPutLockFreeMPIWorkDistributor's one-sided, collective-free RMA\n// protocol (fetch-and-add claiming, batched Put-based result return via a\n// completion log), applied independently at each level of the tree -- the\n// same relationship HierarchicalLockFreeMPIWorkDistributor has to\n// LockFreeMPIWorkDistributor, but built on the async-put protocol instead\n// of the CAS+Gatherv one.\n//\n// Motivation: the flat AsyncPutLockFreeMPIWorkDistributor reaches ~600K-2M+\n// tasks/s (see that file, and strong_scaling_distribution_rate.cpp's\n// async_put_lockfree path) by removing all collective calls from its hot\n// path -- but every claim, publish, and harvest still targets the *same one*\n// manager-owned window, so at large node counts the manager rank's own\n// capacity to service that RMA traffic becomes the ceiling (measured\n// plateauing around 2.1-2.26M tasks/s from 32 nodes onward, while\n// HierarchicalMPIWorkDistributor's tree topology kept climbing to 5.2M/s at\n// 128 nodes by spreading coordination load across many coordinators). This\n// class applies that same fix -- one independent AsyncPutLevel window per\n// node coordinator (the local level) plus one at the leader level -- to the\n// async-put protocol specifically, aiming to combine both distributors'\n// advantages: no collective-call overhead *and* no single-window ceiling.\n//\n// A node coordinator plays two roles at once, same as in\n// HierarchicalLockFreeMPIWorkDistributor: owner of its local level, and\n// claimant of the leader level. The one genuinely new piece of bookkeeping\n// versus that sibling class: a coordinator doesn't compute claimed tasks\n// itself, it republishes them into its local level and only later, once\n// local results are confirmed, relays them upward -- and because local\n// harvesting returns whatever contiguous prefix is ready (which can span\n// multiple leader-level claims, or only partially cover one), the\n// coordinator tracks a small FIFO of {leader_start, local_start, local_end}\n// relay entries (m_pending_relays) and slices harvested local-result batches\n// against that queue's boundaries to know when and where to write each\n// completed leader-level range back. See run_node_coordinator().\n//\n// Results are unordered (see AsyncPutLevel); use a different distributor if\n// task-index-ordered output is required. Task prioritization and detailed\n// statistics are not supported (matching HierarchicalLockFreeMPIWorkDistributor).\n// ---------------------------------------------------------------------------\ntemplate &lt;typename TaskT, typename ResultT, typename... Options&gt;\nclass HierarchicalAsyncPutLockFreeMPIWorkDistributor {\n public:\n  struct Config {\n    MPI_Comm comm = MPI_COMM_WORLD;\n    int manager_rank = 0;\n    bool auto_run_workers = true;\n    int leader_batch_multiplier = 2;  // coordinators claim (local peers)*multiplier tasks at once\n    int local_batch_size = 8;         // local workers claim this many tasks at once from their coordinator\n    int max_tasks = 8192;             // leader-level task table capacity (lifetime total)\n    int max_local_tasks = 8192;       // per-node local task table capacity (lifetime total, per node)\n    int max_task_count = 256;\n    int max_result_count = 256;\n    // 0 (default): exactly today's two-level tree -- manager talks directly\n    // to every node coordinator. &gt;0: caps how many direct claimants any\n    // single upper-level window may have. If the node coordinator count\n    // exceeds this, coordinators are grouped (recursively, as many times as\n    // needed) into a tree of intermediate levels so no single window -- not\n    // even the manager's -- ever has more than max_upper_fanout direct\n    // claimants. See setup_upper_chain() for the construction and the class\n    // comment for why this matters at large scale.\n    int max_upper_fanout = 0;\n  };\n\n  struct RunConfig {\n    size_t target_num_tasks = std::numeric_limits&lt;size_t&gt;::max();\n    bool allow_more_than_target_tasks = true;\n    std::optional&lt;double&gt; max_seconds = std::nullopt;\n  };\n\n  static const bool ordered = false;\n\n  explicit HierarchicalAsyncPutLockFreeMPIWorkDistributor(\n      std::function&lt;ResultT(TaskT)&gt; worker_function, Config config = {})\n      : m_config(config),\n        m_world_comm(config.comm, MPICommunicator&lt;&gt;::Duplicate),\n        m_worker_function(std::move(worker_function)) {\n    setup_topology();\n    setup_levels();\n\n    if (m_config.auto_run_workers &amp;&amp; !is_root_manager()) run_worker();\n  }\n\n  ~HierarchicalAsyncPutLockFreeMPIWorkDistributor() {\n    if (!m_finalized) finalize();\n  }\n\n  bool is_root_manager() const { return m_world_comm.rank() == m_config.manager_rank; }\n\n  size_t remaining_tasks_count() const {\n    assert(is_root_manager());\n    if (m_solo) return m_local_task_store.size() - m_local_collected_count;\n    const auto&amp; top = m_owned_upper_levels.front();  // manager always owns exactly one top-of-chain level\n    return top.owner_published_count() - top.owner_collected_count() - m_results.size();\n  }\n\n  void insert_task(TaskT task) {\n    assert(is_root_manager());\n    insert_tasks(std::vector&lt;TaskT&gt;{std::move(task)});\n  }\n\n  void insert_tasks(const std::vector&lt;TaskT&gt;&amp; tasks) {\n    assert(is_root_manager());\n    if (m_solo) {\n      m_local_task_store.insert(m_local_task_store.end(), tasks.begin(), tasks.end());\n    } else {\n      m_owned_upper_levels.front().publish_tasks(tasks);  // manager's top-of-chain level\n    }\n  }\n\n  [[nodiscard]] std::vector&lt;ResultT&gt; run_tasks(const RunConfig&amp; config = RunConfig{}) {\n    assert(is_root_manager());\n    Timer timer;\n\n    if (m_solo) {\n      while (m_local_collected_count &lt; m_local_task_store.size()) {\n        if (m_results.size() &gt;= config.target_num_tasks) break;\n        if (config.max_seconds &amp;&amp; timer.elapsed().count() &gt;= *config.max_seconds) break;\n        m_results.push_back(m_worker_function(m_local_task_store[m_local_collected_count]));\n        m_local_collected_count++;\n      }\n    } else {\n      auto&amp; top = m_owned_upper_levels.front();  // manager's top-of-chain level\n      while (true) {\n        if (m_results.size() &gt;= config.target_num_tasks) break;\n        if (config.max_seconds &amp;&amp; timer.elapsed().count() &gt;= *config.max_seconds) break;\n        if (top.owner_collected_count() &gt;= top.owner_published_count()) break;\n        auto results = top.harvest_ready_results_throttled();\n        m_results.insert(m_results.end(), std::make_move_iterator(results.begin()),\n                         std::make_move_iterator(results.end()));\n      }\n    }\n\n    return drain_results(config.allow_more_than_target_tasks\n                             ? std::numeric_limits&lt;size_t&gt;::max()\n                             : config.target_num_tasks);\n  }\n\n  [[nodiscard]] std::vector&lt;ResultT&gt; finish_remaining_tasks() { return run_tasks({}); }\n\n  // One non-looping harvest snapshot -- see\n  // AsyncPutLockFreeMPIWorkDistributor::gather_once() for the full\n  // rationale (this mirrors it exactly, against the leader level instead\n  // of a single flat window).\n  [[nodiscard]] std::vector&lt;ResultT&gt; gather_once() {\n    assert(is_root_manager());\n    if (m_solo) {\n      while (m_local_collected_count &lt; m_local_task_store.size()) {\n        m_results.push_back(m_worker_function(m_local_task_store[m_local_collected_count]));\n        m_local_collected_count++;\n      }\n    } else {\n      auto results = m_owned_upper_levels.front().harvest_ready_results();  // manager's top-of-chain level\n      m_results.insert(m_results.end(), std::make_move_iterator(results.begin()),\n                       std::make_move_iterator(results.end()));\n    }\n    return drain_results(std::numeric_limits&lt;size_t&gt;::max());\n  }\n\n  void finalize() {\n    assert(!m_finalized);\n    if (is_root_manager()) {\n      if (m_solo) {\n        while (m_local_collected_count &lt; m_local_task_store.size()) {\n          m_results.push_back(m_worker_function(m_local_task_store[m_local_collected_count]));\n          m_local_collected_count++;\n        }\n      } else {\n        auto&amp; top = m_owned_upper_levels.front();  // manager's top-of-chain level\n        while (top.owner_collected_count() &lt; top.owner_published_count()) {\n          auto results = top.harvest_ready_results_throttled();\n          m_results.insert(m_results.end(), std::make_move_iterator(results.begin()),\n                           std::make_move_iterator(results.end()));\n        }\n        // Only the manager's own level needs an explicit mark_finished()\n        // call: every level below it in the chain learns \"finished\" by\n        // observing ITS OWN parent drain (see run_bridge_chain()'s\n        // leader_drained handling), cascading down automatically.\n        top.mark_finished();\n      }\n    }\n    m_finalized = true;\n  }\n\n  void run_worker() {\n    assert(!is_root_manager());\n    const bool has_upper_chain = m_parent_level.has_value() || !m_owned_upper_levels.empty();\n    if (m_local_level &amp;&amp; m_local_comm-&gt;size() &gt; 1) {\n      if (has_upper_chain) {\n        run_node_coordinator();\n      } else {\n        run_local_worker();\n      }\n    } else if (has_upper_chain) {\n      run_leaf_leader_worker();\n    }\n  }\n\n private:\n  Config m_config;\n  MPICommunicator&lt;&gt; m_world_comm;\n  std::function&lt;ResultT(TaskT)&gt; m_worker_function;\n  const bool m_solo = (m_world_comm.size() == 1);\n\n  std::optional&lt;MPICommunicator&lt;&gt;&gt; m_local_comm;\n  std::optional&lt;detail::AsyncPutLevel&lt;TaskT, ResultT&gt;&gt; m_local_level;\n\n  // Upper hierarchy (everything above the node-local level) -- see\n  // setup_upper_chain() for how this is built.\n  //\n  // m_upper_comms keeps every comm this rank uses up there alive for the\n  // distributor's lifetime (AsyncPutLevel only stores a raw MPI_Comm\n  // handle, it doesn't own one).\n  std::vector&lt;MPICommunicator&lt;&gt;&gt; m_upper_comms;\n\n  // Levels this rank OWNS in the upper chain, ordered top (closest to the\n  // manager) to bottom (closest to m_local_level). The manager always has\n  // exactly one entry (see manager_level()). A node coordinator promoted to\n  // lead one or more groups (max_upper_fanout mode) has one entry per level\n  // it was promoted at, each one also self-claimed (see AsyncPutLevel's\n  // claimant-side API comment). A plain (non-promoted) coordinator or leaf\n  // leader-worker has none.\n  //\n  // std::deque, not std::vector: AsyncPutLevel manages an MPI_Win exposing\n  // its own member's address, and is neither copyable nor movable (see its\n  // deleted copy constructor) -- growing this container must never relocate\n  // existing elements the way a vector reallocation could.\n  std::deque&lt;detail::AsyncPutLevel&lt;TaskT, ResultT&gt;&gt; m_owned_upper_levels;\n\n  // The one level this rank is a pure claimant of (never owns) -- its\n  // immediate parent in the tree. Unset only for the manager (which owns\n  // the top of the chain instead, in m_owned_upper_levels) and for a rank\n  // with no upper-chain participation at all (a plain local worker).\n  std::optional&lt;detail::AsyncPutLevel&lt;TaskT, ResultT&gt;&gt; m_parent_level;\n\n  bool m_finalized = false;\n  std::vector&lt;ResultT&gt; m_results;\n  size_t m_returned_count = 0;\n\n  std::vector&lt;TaskT&gt; m_local_task_store;\n  size_t m_local_collected_count = 0;\n\n  void setup_topology() {\n    if (m_solo) return;\n\n    MPICommunicator&lt;&gt; node_comm = m_world_comm.split_by_node();\n\n    const int local_color = (m_world_comm.rank() == m_config.manager_rank) ? MPI_UNDEFINED : 0;\n    auto local_opt = node_comm.split(local_color, m_world_comm.rank());\n    if (local_opt.has_value()) m_local_comm.emplace(std::move(*local_opt));\n\n    const bool is_manager = (m_world_comm.rank() == m_config.manager_rank);\n    bool is_node_coordinator = false;\n    if (m_local_comm.has_value()) is_node_coordinator = (m_local_comm-&gt;rank() == 0);\n\n    const int leader_color = (is_manager || is_node_coordinator) ? 0 : MPI_UNDEFINED;\n    auto leader_opt = m_world_comm.split(leader_color, m_world_comm.rank());\n    if (!leader_opt.has_value()) return;  // plain local worker: no upper-chain role at all\n\n    setup_upper_chain(std::move(*leader_opt), is_manager);\n  }\n\n  // Appends one AsyncPutLevel this rank OWNS (and, per the class comment on\n  // AsyncPutLevel's claimant-side API, will also claim from -- its own\n  // subtree still needs feeding) to m_owned_upper_levels.\n  void emplace_owned_upper_level(MPICommunicator&lt;&gt; comm, int owner_rank, int claim_batch_size) {\n    m_upper_comms.push_back(std::move(comm));\n    typename detail::AsyncPutLevel&lt;TaskT, ResultT&gt;::Config cfg;\n    cfg.comm = m_upper_comms.back().get();\n    cfg.owner_rank = owner_rank;\n    cfg.max_tasks = m_config.max_tasks;\n    cfg.max_task_count = m_config.max_task_count;\n    cfg.max_result_count = m_config.max_result_count;\n    cfg.claim_batch_size = claim_batch_size;\n    m_owned_upper_levels.emplace_back(cfg);\n  }\n\n  // Sets m_parent_level: the one level this rank is a pure claimant of\n  // (never owns) -- its immediate parent in the tree.\n  void emplace_parent_level(MPICommunicator&lt;&gt; comm, int owner_rank, int claim_batch_size) {\n    m_upper_comms.push_back(std::move(comm));\n    typename detail::AsyncPutLevel&lt;TaskT, ResultT&gt;::Config cfg;\n    cfg.comm = m_upper_comms.back().get();\n    cfg.owner_rank = owner_rank;\n    cfg.max_tasks = m_config.max_tasks;\n    cfg.max_task_count = m_config.max_task_count;\n    cfg.max_result_count = m_config.max_result_count;\n    cfg.claim_batch_size = claim_batch_size;\n    m_parent_level.emplace(cfg);\n  }\n\n  // Builds this rank's view of the upper hierarchy (everything above the\n  // node-local level): a chain of AsyncPutLevel windows from the manager\n  // down to this rank's immediate parent, filling in m_upper_comms (kept\n  // alive for the AsyncPutLevel objects' lifetime, since AsyncPutLevel only\n  // stores a raw MPI_Comm handle) / m_owned_upper_levels / m_parent_level.\n  //\n  // `flat_comm` is manager + every node coordinator -- exactly the single\n  // flat level this class used before max_upper_fanout existed. If\n  // max_upper_fanout is disabled (0) or the coordinator count already fits\n  // under it, that flat comm IS the whole chain: one level, owned by the\n  // manager, claimed directly by every coordinator -- byte-for-byte today's\n  // behavior.\n  //\n  // Otherwise, coordinators (manager excluded) are grouped into a k-ary tree\n  // with branching factor max_upper_fanout: repeatedly split the current\n  // \"round\" of coordinator-like ranks into groups of at most\n  // max_upper_fanout, promote each group's local rank 0 to own that group's\n  // level, and treat the promoted set as the next round's input -- until a\n  // round's membership already fits directly under the manager. A rank\n  // promoted at round r ends up BOTH owning round r's group level AND being\n  // one of its own claimants (its own node/subtree still needs feeding, and\n  // there's no other mechanism to get tasks down to it) -- see\n  // AsyncPutLevel's claimant-side API comment for why that's safe\n  // (self-targeted RMA, mechanically no different from any other claimant).\n  // A rank never promoted just ends up with a single entry in m_parent_level\n  // (the one group it's a member of) and nothing in m_owned_upper_levels --\n  // identical in shape to today's single-level design.\n  //\n  // Every MPI_Comm_split call below is collective over its *input* comm's\n  // full membership; the control flow is written so every rank in that\n  // membership reaches the matching call, even ranks that stop being\n  // promoted early (see the inline notes at each split).\n  void setup_upper_chain(MPICommunicator&lt;&gt; flat_comm, bool is_manager) {\n    MPIGroup world_group(m_world_comm);\n    const int local_peers = m_local_comm.has_value() ? std::max(0, m_local_comm-&gt;size() - 1) : 0;\n    const int multiplier = std::max(1, m_config.leader_batch_multiplier);\n    // The level feeding this rank's own local_level (or its own direct\n    // compute, for a leaf leader-worker) is sized by that rank's own\n    // downstream fan-out -- see the old setup_levels()'s claim_batch_size\n    // comment (unchanged reasoning, just renamed). Anything feeding an\n    // *intermediate* group level instead is sized by that group's raw\n    // membership -- see feed_size's updates below.\n    const int leaf_claim = local_peers &gt; 0 ? std::max(1, local_peers * multiplier) : multiplier;\n\n    const int effective_fanout =\n        m_config.max_upper_fanout &gt; 0 ? m_config.max_upper_fanout : std::numeric_limits&lt;int&gt;::max();\n    const int coordinator_count = flat_comm.size() - 1;\n\n    if (coordinator_count &lt;= effective_fanout) {\n      // Fits directly under the manager: exactly today's single flat level\n      // (also always true when max_upper_fanout is disabled).\n      MPIGroup flat_group(flat_comm);\n      const int owner_rank = world_group.translate_rank(m_config.manager_rank, flat_group);\n      if (flat_comm.rank() == owner_rank) {\n        emplace_owned_upper_level(std::move(flat_comm), owner_rank, leaf_claim);\n      } else {\n        emplace_parent_level(std::move(flat_comm), owner_rank, leaf_claim);\n      }\n      return;\n    }\n\n    // Real grouping needed. Carve \"coordinators only\" out of flat_comm --\n    // every member of flat_comm (manager included) calls this split\n    // together, even though only coordinators use the result.\n    auto coordinators_opt = flat_comm.split(is_manager ? MPI_UNDEFINED : 0, flat_comm.rank());\n\n    bool is_final_round_leader = false;\n    int feed_size = leaf_claim;  // claim size for the NEXT level to be discovered, updated per round\n    if (!is_manager) {\n      // std::optional, not a bare MPICommunicator: MPICommunicator has no\n      // move assignment (only move construction -- see its deleted\n      // operator=), so replacing round_comm each iteration needs\n      // emplace()'s in-place construction rather than `round_comm = ...`.\n      std::optional&lt;MPICommunicator&lt;&gt;&gt; round_comm(std::move(*coordinators_opt));\n      while (true) {\n        if (round_comm-&gt;size() &lt;= effective_fanout) {\n          // This round's membership (this rank included) already fits\n          // directly under the manager -- stop promoting; round_comm itself\n          // was only ever bookkeeping (the manager isn't part of it), so it\n          // just falls out of scope here.\n          is_final_round_leader = true;\n          break;\n        }\n        const int color = round_comm-&gt;rank() / effective_fanout;\n        auto group_opt = round_comm-&gt;split(color, round_comm-&gt;rank());\n        MPICommunicator&lt;&gt; group_comm = std::move(*group_opt);\n        const bool is_group_leader = (group_comm.rank() == 0);\n        const int group_size = group_comm.size();\n\n        // Collective over round_comm: every member (leader or not) calls\n        // this together, before acting on their differing result below.\n        auto leaders_opt = round_comm-&gt;split(is_group_leader ? 0 : MPI_UNDEFINED, round_comm-&gt;rank());\n\n        if (!is_group_leader) {\n          emplace_parent_level(std::move(group_comm), 0, feed_size);\n          break;\n        }\n        emplace_owned_upper_level(std::move(group_comm), 0, feed_size);\n        // Compound by this round's group_size rather than resetting to\n        // group_size*multiplier: feed_size is the claim_batch_size every\n        // level ABOVE this one will use (see step_bridge_hop()'s\n        // pending_cap = claim_batch_size*kMaxPendingRounds), so it must\n        // reflect the full subtree feeding through this leader, not just\n        // this one group's immediate fanout. A flat reset here was a real,\n        // confirmed bug: it pinned every intermediate hop's flow-control\n        // window at a tiny constant (group_size*multiplier, e.g. 4 for\n        // fanout=2) regardless of how many leaf workers actually feed it,\n        // collapsing throughput by orders of magnitude as depth increased\n        // (observed: an 8-node/816-rank run's top-level drain rate fell to\n        // ~5,370 tasks/s -- turning a 44M-task run that should finish in\n        // seconds into a multi-hour drain that reads as a hang).\n        feed_size = std::max(1, group_size * feed_size);\n        round_comm.emplace(std::move(*leaders_opt));\n      }\n    }\n\n    // Attach to the manager: every ORIGINAL member of flat_comm (manager +\n    // every coordinator, whether promoted zero, one, or many times) reaches\n    // this exact call.\n    auto top_opt =\n        flat_comm.split((is_manager || is_final_round_leader) ? 0 : MPI_UNDEFINED, flat_comm.rank());\n    if (top_opt.has_value()) {\n      MPIGroup top_group(*top_opt);\n      const int owner_rank = world_group.translate_rank(m_config.manager_rank, top_group);\n      if (is_manager) {\n        emplace_owned_upper_level(std::move(*top_opt), owner_rank, feed_size);\n      } else {\n        emplace_parent_level(std::move(*top_opt), owner_rank, feed_size);\n      }\n    }\n  }\n\n  void setup_levels() {\n    if (m_local_comm.has_value()) {\n      typename detail::AsyncPutLevel&lt;TaskT, ResultT&gt;::Config local_cfg;\n      local_cfg.comm = m_local_comm-&gt;get();\n      local_cfg.owner_rank = 0;\n      local_cfg.max_tasks = m_config.max_local_tasks;\n      local_cfg.max_task_count = m_config.max_task_count;\n      local_cfg.max_result_count = m_config.max_result_count;\n      local_cfg.claim_batch_size = m_config.local_batch_size;\n      m_local_level.emplace(local_cfg);\n    }\n  }\n\n  // One (parent, child) bridge relationship: claim task batches from\n  // `parent` and republish them into `child` (a *different* AsyncPutLevel\n  // this rank owns), relaying completed results back to `parent` once\n  // they're confirmed. This is the single-hop unit run_node_coordinator()\n  // used to run exactly once, inline, before max_upper_fanout existed --\n  // factored out here so a rank promoted through several grouping rounds\n  // (see setup_upper_chain()) can run several of these, chained, in one\n  // combined loop, without duplicating the logic per hop.\n  //\n  // Because harvesting from `child` can return a batch that spans multiple\n  // `parent`-level claims (or only part of one), pending_relays +\n  // relay_buffer track the FIFO mapping from child-side result positions\n  // back to the parent-side range they belong to: relays are contiguous and\n  // gap-free by construction (every child-level index belongs to exactly\n  // one queued relay, since a claim is always immediately republished with\n  // its matching relay entry queued in the same step), so a prefix of\n  // relay_buffer always aligns exactly with the front relay's boundary once\n  // enough has accumulated.\n  struct PendingRelay {\n    int64_t parent_start;\n    int64_t child_len;\n  };\n  struct BridgeHop {\n    BridgeHop(detail::AsyncPutLevel&lt;TaskT, ResultT&gt;* p, detail::AsyncPutLevel&lt;TaskT, ResultT&gt;* c)\n        : parent(p), child(c) {}\n    detail::AsyncPutLevel&lt;TaskT, ResultT&gt;* parent;\n    detail::AsyncPutLevel&lt;TaskT, ResultT&gt;* child;\n    std::deque&lt;PendingRelay&gt; pending_relays;\n    std::vector&lt;ResultT&gt; relay_buffer;  // child results collected but not yet fully relayed upward\n    int64_t pending_task_count = 0;     // sum of child_len across pending_relays -- see step_bridge_hop()\n    bool finish_marked = false;\n  };\n\n  // Runs one non-blocking step of a single hop. Returns whether this step\n  // accomplished anything against `parent` (claimed, relayed, or hit the\n  // finish transition) -- callers use this, per hop, to decide whether that\n  // hop's OWN parent-level polling should back off, independent of every\n  // other hop's state and independent of child-harvest activity (see\n  // run_node_coordinator()'s idle_wait() comment for why conflating the two\n  // was a real, confirmed bottleneck: a rank bridging levels has no natural\n  // compute-time delay between parent polls the way a rank that actually\n  // computes tasks does, so without an explicit and correctly-scoped\n  // backoff it spins its parent-level polling as fast as RMA round-trips\n  // allow).\n  bool step_bridge_hop(BridgeHop&amp; hop) {\n    bool made_parent_progress = false;\n\n    // Read once per call and reuse for both the claim-guard and (via the\n    // caller's bridge_hop_done()) the exit check -- AsyncPutLevel::drained()\n    // is not a pure query (it does fresh RMA reads and can flip from false\n    // to true between two calls made moments apart), so calling it\n    // separately for those two decisions was a real, confirmed bug: if it\n    // flipped true *between* them, the exit check could fire having never\n    // gone through the \"else\" branch below at all -- the only place that\n    // calls hop.child-&gt;mark_finished(). That could let this rank exit (and\n    // reach its destructor's teardown barrier) without ever telling its\n    // child's claimants to stop, hanging them forever. Reusing one cached\n    // value and gating exit on hop.finish_marked itself (not a re-derived\n    // drained() call) makes \"mark_finished() happens before this hop can\n    // possibly be done\" true by construction, not by hoping two nearby\n    // calls agree.\n    const bool parent_drained = hop.parent-&gt;drained();\n\n    // 1. Claim from parent and republish into child, queueing a relay entry\n    // recording where this batch's results must eventually be written back.\n    //\n    // Gated by backpressure: pending_relays is a strict FIFO (relay N can't\n    // be written back to parent until every relay before it has been), so\n    // claiming far ahead of what's actually been relayed doesn't just add\n    // buffering -- it adds *ordering* latency, since a fully-caught-up\n    // relay near the back of the queue still can't flush until everything\n    // ahead of it does. Confirmed as a real, severe bottleneck at 32+ nodes:\n    // without this cap, a coordinator claims from parent every iteration\n    // whenever parent isn't drained (no natural throttle, same reasoning as\n    // the leader-poll backoff above, just for the claim side instead of the\n    // idle side), racing far ahead of what child's claimants can actually\n    // drain -- one observed coordinator had 670 queued relay entries (roughly\n    // 9,300 tasks) after one second, and since flushing is strictly\n    // FIFO-ordered, that whole backlog has to clear before parent ever sees\n    // the tail end of it confirmed, regardless of how fast child itself is\n    // running. Capping outstanding tasks at a small multiple of parent's own\n    // claim granularity keeps the pipeline fed (still several rounds of\n    // slack) without letting the queue -- and therefore the worst-case relay\n    // latency -- grow without bound.\n    constexpr int64_t kMaxPendingRounds = 8;\n    const int64_t pending_cap =\n        static_cast&lt;int64_t&gt;(hop.parent-&gt;claim_batch_size()) * kMaxPendingRounds;\n    if (!parent_drained &amp;&amp; hop.pending_task_count &lt; pending_cap) {\n      auto claimed = hop.parent-&gt;try_claim();\n      if (claimed.start != -1) {\n        const int64_t child_len = static_cast&lt;int64_t&gt;(claimed.tasks.size());\n        hop.child-&gt;publish_tasks(claimed.tasks);\n        hop.pending_relays.push_back(PendingRelay{claimed.start, child_len});\n        hop.pending_task_count += child_len;\n        made_parent_progress = true;\n      }\n    } else if (parent_drained &amp;&amp; !hop.finish_marked) {\n      // Parent is fully drained: nothing more will ever be published to\n      // child either (every task this rank will ever see from parent has\n      // already been claimed and republished above).\n      hop.child-&gt;mark_finished();\n      hop.finish_marked = true;\n      made_parent_progress = true;\n    }\n\n    // 2. Harvest whatever's ready in child and append to the relay buffer.\n    // Deliberately NOT counted towards made_parent_progress: this is\n    // same-owner-rank RMA against child (this rank's own window, no\n    // upstream contention), and it succeeds almost every iteration once\n    // child's claimants are active -- counting it here is exactly what\n    // previously masked parent-level polling from ever backing off.\n    auto child_results = hop.child-&gt;harvest_ready_results();\n    if (!child_results.empty()) {\n      hop.relay_buffer.insert(hop.relay_buffer.end(), std::make_move_iterator(child_results.begin()),\n                              std::make_move_iterator(child_results.end()));\n    }\n\n    // 3. Flush any relay entries the buffer now fully covers, in FIFO\n    // order.\n    size_t consumed = 0;\n    while (!hop.pending_relays.empty() &amp;&amp;\n           hop.relay_buffer.size() - consumed &gt;=\n               static_cast&lt;size_t&gt;(hop.pending_relays.front().child_len)) {\n      const auto&amp; front = hop.pending_relays.front();\n      std::vector&lt;ResultT&gt; slice(\n          std::make_move_iterator(hop.relay_buffer.begin() + static_cast&lt;ptrdiff_t&gt;(consumed)),\n          std::make_move_iterator(hop.relay_buffer.begin() + static_cast&lt;ptrdiff_t&gt;(consumed) +\n                                  front.child_len));\n      hop.parent-&gt;write_result_range(front.parent_start, slice);\n      consumed += static_cast&lt;size_t&gt;(front.child_len);\n      hop.pending_task_count -= front.child_len;\n      hop.pending_relays.pop_front();\n      made_parent_progress = true;\n    }\n    if (consumed &gt; 0) {\n      hop.relay_buffer.erase(hop.relay_buffer.begin(),\n                             hop.relay_buffer.begin() + static_cast&lt;ptrdiff_t&gt;(consumed));\n    }\n    return made_parent_progress;\n  }\n\n  // A hop is done once child's claimants have been told to stop\n  // (finish_marked, not a re-derived parent-&gt;drained() -- see\n  // step_bridge_hop()'s comment) and every claimed batch has been relayed\n  // back upward.\n  static bool bridge_hop_done(const BridgeHop&amp; hop) {\n    return hop.finish_marked &amp;&amp; hop.pending_relays.empty();\n  }\n\n  // Builds the chain of bridge hops this rank participates in, from its\n  // immediate parent down to (but not including) `terminal_extra` (pass\n  // m_local_level for a node coordinator with real local peers, or nullptr\n  // for a leaf leader-worker, which claims+computes directly against the\n  // last upper level instead -- see run_leaf_leader_worker()). Most ranks\n  // (never promoted -- see setup_upper_chain()) get exactly one hop here,\n  // identical in shape to this class's original single-level design.\n  std::vector&lt;BridgeHop&gt; build_upper_hops() {\n    std::vector&lt;detail::AsyncPutLevel&lt;TaskT, ResultT&gt;*&gt; chain;\n    chain.push_back(&amp;*m_parent_level);\n    for (auto&amp; level : m_owned_upper_levels) chain.push_back(&amp;level);\n    std::vector&lt;BridgeHop&gt; hops;\n    for (size_t i = 0; i + 1 &lt; chain.size(); ++i) hops.push_back(BridgeHop{chain[i], chain[i + 1]});\n    return hops;\n  }\n\n  // The bottom-most level in this rank's own upper chain: its last owned\n  // group level if it was promoted at least once, otherwise its immediate\n  // parent. This is what a terminal action (feeding a real local_level in\n  // run_node_coordinator(), or claiming+computing directly in\n  // run_leaf_leader_worker()) attaches to.\n  detail::AsyncPutLevel&lt;TaskT, ResultT&gt;&amp; last_upper_level() {\n    return m_owned_upper_levels.empty() ? *m_parent_level : m_owned_upper_levels.back();\n  }\n\n  // A node coordinator: bridges its parent level (claiming task batches\n  // from the manager or an intermediate group level -- see\n  // setup_upper_chain()) down to its local level (its own window, published\n  // to for its local workers to claim from), and, if promoted to lead one\n  // or more groups, one or more intermediate hops in between. Unlike a\n  // plain worker, this rank never computes a task itself -- see\n  // step_bridge_hop() and the class comment for the full rationale.\n  void run_node_coordinator() {\n    std::vector&lt;BridgeHop&gt; hops = build_upper_hops();\n    hops.push_back(BridgeHop{&amp;last_upper_level(), &amp;*m_local_level});\n\n    while (true) {\n      bool any_progress = false;\n      bool all_done = true;\n      for (auto&amp; hop : hops) {\n        if (step_bridge_hop(hop)) any_progress = true;\n        if (!bridge_hop_done(hop)) all_done = false;\n      }\n      if (all_done) break;\n\n      // See step_bridge_hop()'s comment: only back off when NONE of this\n      // rank's hops made progress against their own parent -- any single\n      // hop's window works fine as the backoff's flush target (see\n      // AsyncPutLevel::idle_wait(); the window argument is unused outside\n      // MS-MPI, where every window needs the same periodic flush anyway).\n      if (!any_progress) hops.front().parent-&gt;idle_wait();\n    }\n  }\n\n  // A plain local worker: claims batches from its node coordinator, computes\n  // them inline, writes results back -- the same claim/write pattern\n  // AsyncPutLockFreeMPIWorkDistributor::run_worker() uses against its one\n  // flat window, here against the local level instead.\n  void run_local_worker() {\n    while (true) {\n      // Cache once per iteration -- see step_bridge_hop()'s comment on why\n      // calling AsyncPutLevel::drained() separately for the claim guard and\n      // the exit check is a real bug class (it does fresh RMA reads and is\n      // not guaranteed to agree with itself moments apart), not just a\n      // style preference.\n      const bool is_drained = m_local_level-&gt;drained();\n      if (!is_drained) {\n        auto claimed = m_local_level-&gt;try_claim();\n        if (claimed.start != -1) {\n          std::vector&lt;ResultT&gt; results;\n          results.reserve(claimed.tasks.size());\n          for (auto&amp; task : claimed.tasks) results.push_back(m_worker_function(std::move(task)));\n          m_local_level-&gt;write_result_range(claimed.start, results);\n          continue;\n        }\n      }\n      if (is_drained) break;\n      m_local_level-&gt;idle_wait();\n    }\n  }\n\n  // A leaf leader-worker (no local peers -- alone on its node): bridges\n  // whatever upper hops this rank is promoted through (usually none), then\n  // claims and executes tasks directly against the bottom of its own chain\n  // (its parent level, or its own last owned group level if promoted),\n  // instead of publishing further down to a local_level it doesn't have.\n  void run_leaf_leader_worker() {\n    std::vector&lt;BridgeHop&gt; hops = build_upper_hops();\n    detail::AsyncPutLevel&lt;TaskT, ResultT&gt;&amp; terminal = last_upper_level();\n\n    while (true) {\n      bool any_progress = false;\n      for (auto&amp; hop : hops) {\n        if (step_bridge_hop(hop)) any_progress = true;\n      }\n\n      const bool is_drained = terminal.drained();\n      if (!is_drained) {\n        auto claimed = terminal.try_claim();\n        if (claimed.start != -1) {\n          std::vector&lt;ResultT&gt; results;\n          results.reserve(claimed.tasks.size());\n          for (auto&amp; task : claimed.tasks) results.push_back(m_worker_function(std::move(task)));\n          terminal.write_result_range(claimed.start, results);\n          any_progress = true;\n        }\n      }\n\n      bool all_hops_done = true;\n      for (auto&amp; hop : hops) {\n        if (!bridge_hop_done(hop)) {\n          all_hops_done = false;\n          break;\n        }\n      }\n      if (all_hops_done &amp;&amp; is_drained) break;\n\n      if (!any_progress) terminal.idle_wait();\n    }\n  }\n\n  std::vector&lt;ResultT&gt; drain_results(size_t limit) {\n    const size_t count = std::min(limit, m_results.size());\n    std::vector&lt;ResultT&gt; output;\n    output.reserve(count);\n    for (size_t i = 0; i &lt; count; ++i) output.push_back(std::move(m_results[i]));\n    m_results.erase(m_results.begin(), m_results.begin() + static_cast&lt;ptrdiff_t&gt;(count));\n    m_returned_count += count;\n    return output;\n  }\n};\n\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/hierarchical__distributor_8hpp/","title":"File hierarchical_distributor.hpp","text":"<p>FileList &gt; dynampi &gt; impl &gt; hierarchical_distributor.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;algorithm&gt;</code></li> <li><code>#include &lt;cassert&gt;</code></li> <li><code>#include &lt;cmath&gt;</code></li> <li><code>#include &lt;deque&gt;</code></li> <li><code>#include &lt;functional&gt;</code></li> <li><code>#include &lt;iostream&gt;</code></li> <li><code>#include &lt;iterator&gt;</code></li> <li><code>#include &lt;limits&gt;</code></li> <li><code>#include &lt;ranges&gt;</code></li> <li><code>#include &lt;set&gt;</code></li> <li><code>#include &lt;span&gt;</code></li> <li><code>#include &lt;stack&gt;</code></li> <li><code>#include &lt;type_traits&gt;</code></li> <li><code>#include &lt;vector&gt;</code></li> <li><code>#include \"../mpi/mpi_communicator.hpp\"</code></li> <li><code>#include \"../mpi/mpi_types.hpp\"</code></li> <li><code>#include \"dynampi/impl/base_distributor.hpp\"</code></li> <li><code>#include \"dynampi/utilities/assert.hpp\"</code></li> <li><code>#include \"dynampi/utilities/timer.hpp\"</code></li> </ul>"},{"location":"dynampi/hierarchical__distributor_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi"},{"location":"dynampi/hierarchical__distributor_8hpp/#classes","title":"Classes","text":"Type Name class HierarchicalMPIWorkDistributor &lt;typename TaskT, typename ResultT, Options&gt; struct Config struct RunConfig <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_distributor.hpp</code></p>"},{"location":"dynampi/hierarchical__distributor_8hpp_source/","title":"File hierarchical_distributor.hpp","text":"<p>File List &gt; dynampi &gt; impl &gt; hierarchical_distributor.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2025 QDX Technologies. Authored by Ryan Stocks &lt;ryan.stocks00@gmail.com&gt;\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;algorithm&gt;\n#include &lt;cassert&gt;\n#include &lt;cmath&gt;\n#include &lt;deque&gt;\n#include &lt;functional&gt;\n#include &lt;iostream&gt;\n#include &lt;iterator&gt;\n#include &lt;limits&gt;\n#include &lt;ranges&gt;\n#include &lt;set&gt;\n#include &lt;span&gt;\n#include &lt;stack&gt;\n#include &lt;type_traits&gt;\n#include &lt;vector&gt;\n\n#include \"../mpi/mpi_communicator.hpp\"\n#include \"../mpi/mpi_types.hpp\"\n#include \"dynampi/impl/base_distributor.hpp\"\n#include \"dynampi/utilities/assert.hpp\"\n#include \"dynampi/utilities/timer.hpp\"\n\nnamespace dynampi {\n\ntemplate &lt;typename TaskT, typename ResultT, typename... Options&gt;\nclass HierarchicalMPIWorkDistributor : public BaseMPIWorkDistributor&lt;TaskT, ResultT, Options...&gt; {\n  using Base = BaseMPIWorkDistributor&lt;TaskT, ResultT, Options...&gt;;\n\n public:\n  struct Config {\n    MPI_Comm comm = MPI_COMM_WORLD;\n    int manager_rank = 0;\n    bool auto_run_workers = true;\n    std::optional&lt;size_t&gt; message_batch_size = std::nullopt;\n    std::optional&lt;int&gt; max_workers_per_coordinator = std::nullopt;\n    int batch_size_multiplier = 2;\n\n    // How many batches' worth of tasks/requests a coordinator tries to keep\n    // in the pipeline at once, including the one currently being\n    // distributed to its children. 1 disables prefetching entirely (a\n    // coordinator only asks for its next batch after fully finishing the\n    // current one). 2 is double-buffering: one batch active, one already\n    // requested (or in flight) so children never wait a full round trip\n    // between batches. Values above 2 keep additional batches' worth of\n    // requests outstanding simultaneously, trading more slack against\n    // round-trip latency variance for coarser load-balancing granularity\n    // (tasks committed this far ahead can't be reassigned to an idle\n    // sibling coordinator).\n    int pipeline_depth = 2;\n\n    // If true, topology is strictly mapped to physical nodes:\n    // Manager &lt;-&gt; Node Coordinators &lt;-&gt; Local Workers\n    // Note: Manager is excluded from its node's Local Comm to separate duties.\n    bool coordinator_per_node = true;\n  };\n\n  struct RunConfig {\n    // Stop once we have at least this many results ready to return.\n    size_t target_num_tasks = std::numeric_limits&lt;size_t&gt;::max();\n\n    // If false, strictly clips the return vector to `target_num_tasks`.\n    // Excess results are buffered for the next call.\n    bool allow_more_than_target_tasks = true;\n\n    // Stop if this much time has passed.\n    std::optional&lt;double&gt; max_seconds = std::nullopt;\n  };\n\n  static constexpr bool prioritize_tasks = Base::prioritize_tasks;\n  static const bool ordered = false;\n\n private:\n  typename Base::QueueT m_unallocated_task_queue;\n  std::vector&lt;ResultT&gt; m_results;\n\n  enum class CommLayer { Global, Local, Leader };\n\n  struct TaskRequest {\n    int worker_rank;\n    CommLayer source_layer = CommLayer::Global;  // Which comm did this come from?\n    std::optional&lt;int&gt; num_tasks_requested = std::nullopt;\n  };\n  static constexpr int kMaxTasksRequested = 1'000'000;  // guard against pathological reserve()\n  std::stack&lt;TaskRequest, std::vector&lt;TaskRequest&gt;&gt; m_free_worker_indices;\n\n  size_t m_tasks_sent_to_child = 0;\n  size_t m_results_received_from_child = 0;\n  size_t m_results_sent_to_parent = 0;\n  size_t m_tasks_received_from_parent = 0;\n  size_t m_tasks_executed = 0;\n  size_t m_results_returned = 0;\n\n  bool m_finalized = false;\n  bool m_done = false;\n\n  // Pipelining (see run_worker()'s intermediate-coordinator branch):\n  // tasks that arrive as a reply to a proactive next-batch request sent\n  // while the current round is still active are quarantined here and only\n  // released into m_unallocated_task_queue at the next round boundary, so\n  // the current round's distribution loop can never overshoot into the\n  // next round's tasks. Replies stay in arrival order (FIFO) regardless of\n  // which of possibly several outstanding requests they answer.\n  bool m_round_active = false;\n  std::deque&lt;TaskT&gt; m_prefetched_tasks;\n  // Requests sent to our parent (Tag::REQUEST_BATCH) whose reply --\n  // Tag::TASK_BATCH into m_prefetched_tasks/m_unallocated_task_queue, or\n  // Tag::DONE -- hasn't arrived yet. Kept below Config::pipeline_depth (see\n  // top_up_pipeline()).\n  int m_outstanding_requests = 0;\n\n  static constexpr StatisticsMode statistics_mode =\n      get_option_value&lt;track_statistics_t, Options...&gt;();\n\n  using MPICommunicator = dynampi::MPICommunicator&lt;track_statistics&lt;statistics_mode&gt;&gt;;\n\n  MPICommunicator m_communicator;  // Global communicator\n  MPIGroup m_world_group;          // Group for the global communicator (for rank translation)\n  std::optional&lt;MPIGroup&gt; m_local_group;  // Intra-node group (Shared Memory, excludes manager)\n  std::optional&lt;MPIGroup&gt;\n      m_leader_group;  // Inter-node group (Leaders only: manager + node coordinators)\n\n  std::function&lt;ResultT(TaskT)&gt; m_worker_function;\n  Config m_config;\n\n  // Cached parent target to avoid repeated MPI_Group_translate_ranks calls\n  mutable std::optional&lt;std::pair&lt;int, CommLayer&gt;&gt; m_cached_parent_target;\n\n  // --- Topology Helper Methods ---\n\n  inline int max_workers_per_coordinator() const {\n    const int default_value = std::max(2, static_cast&lt;int&gt;(std::sqrt(m_communicator.size())));\n    const int configured = m_config.max_workers_per_coordinator.value_or(default_value);\n    return std::max(1, configured);\n  }\n\n  // Returns {parent_rank, communicator_layer}\n  inline std::pair&lt;int, CommLayer&gt; get_parent_target() const {\n    // Return cached value if available\n    if (m_cached_parent_target.has_value()) {\n      return m_cached_parent_target.value();\n    }\n\n    std::pair&lt;int, CommLayer&gt; result;\n    DYNAMPI_ASSERT(!is_root_manager(), \"Root manager should not have a parent\");\n    if (m_config.coordinator_per_node) {\n      DYNAMPI_ASSERT(m_local_group.has_value() || m_leader_group.has_value(),\n                     \"Local or leader group should be present\");\n      if (m_local_group &amp;&amp; m_local_group-&gt;rank() &gt; 0) {\n        // Case 1: I am a Local Worker (Rank &gt; 0 in Local Group)\n        // Parent is the Node Coordinator (Local Rank 0).\n        // Translate local rank 0 to world rank\n        int node_coord_world_rank = m_local_group-&gt;translate_rank(0, m_world_group);\n        result = {node_coord_world_rank, CommLayer::Local};\n      } else {\n        // Case 2: I am a Node Coordinator (Local Rank 0).\n        // Parent is the Global Manager.\n        // With the new topology, Manager is ALWAYS in the leader group.\n        // We need the manager's world rank, which we already have\n        int global_manager = m_config.manager_rank;\n        result = std::make_pair(global_manager, CommLayer::Leader);\n      }\n    } else {\n      // Original Logic\n      int rank = m_communicator.rank();\n      int virtual_rank = rank == m_config.manager_rank ? 0 : idx_for_worker(rank) + 1;\n      int virtual_parent = (virtual_rank - 1) / max_workers_per_coordinator();\n      int parent_rank =\n          virtual_parent == 0 ? m_config.manager_rank : worker_for_idx(virtual_parent - 1);\n      result = {parent_rank, CommLayer::Global};\n    }\n\n    // Cache the result\n    m_cached_parent_target = result;\n    return result;\n  }\n\n  inline int total_num_children(int rank) const {\n    if (m_config.coordinator_per_node) {\n      DYNAMPI_UNIMPLEMENTED(\"Recursive child counting not supported/needed in Node topology mode\");\n      return 0;\n    }\n    int virtual_rank = rank == m_config.manager_rank ? 0 : idx_for_worker(rank) + 1;\n    int num_children = 0;\n    int max_children = max_workers_per_coordinator();\n    for (int i = 0; i &lt; max_children; ++i) {\n      int child = virtual_rank * max_children + i + 1;\n      if (child &gt;= m_communicator.size()) break;  // No more children\n      num_children += 1 + total_num_children(worker_for_idx(child - 1));\n    }\n    return num_children;\n  }\n\n  // Calculate number of direct children based on active topology\n  inline int num_direct_children() const {\n    if (m_config.coordinator_per_node) {\n      int count = 0;\n      // 1. Local Children: Everyone in local group except me (Rank 0)\n      if (m_local_group &amp;&amp; m_local_group-&gt;rank() == 0) {\n        count += (m_local_group-&gt;size() - 1);\n      }\n      // 2. Remote Children: If I am Manager, other Leaders are my children.\n      // Note: In this topology, Manager is IN leader group, but NOT in local group.\n      if (is_root_manager() &amp;&amp; m_leader_group) {\n        count += (m_leader_group-&gt;size() - 1);\n      }\n      return count;\n    } else {\n      // Original Logic\n      int rank = m_communicator.rank();\n      int num_children = 0;\n      int max_children = max_workers_per_coordinator();\n      for (int i = 0; i &lt; max_children; ++i) {\n        int virtual_rank = rank == m_config.manager_rank ? 0 : idx_for_worker(rank) + 1;\n        int virtual_child = virtual_rank * max_children + i + 1;\n        if (virtual_child &lt; m_communicator.size()) {\n          num_children++;\n        }\n      }\n      return num_children;\n    }\n  }\n\n  bool is_leaf_worker() const {\n    if (m_config.coordinator_per_node) {\n      if (is_root_manager()) return false;\n\n      // If I am NOT in local group (should only be Manager, handled above), panic?\n      // Actually, with this topology, everyone except Manager is in local group.\n      if (!m_local_group) return true;  // Safety fallback\n\n      // Standard Worker: Rank &gt; 0 in Local Group\n      if (m_local_group-&gt;rank() &gt; 0) return true;\n\n      // Node Coordinator: Rank 0 in Local Comm.\n      // Leaf only if single-core node (no children).\n      return num_direct_children() == 0;\n    } else {\n      int rank = m_communicator.rank();\n      int max_children = max_workers_per_coordinator();\n      int virtual_rank = rank == m_config.manager_rank ? 0 : idx_for_worker(rank) + 1;\n      int first_child_virtual = virtual_rank * max_children + 1;\n      return first_child_virtual &gt;= m_communicator.size();\n    }\n  }\n\n  enum Tag : int {\n    TASK = 0,\n    DONE = 1,\n    RESULT = 2,\n    REQUEST = 3,\n    TASK_BATCH = 4,\n    RESULT_BATCH = 5,\n    REQUEST_BATCH = 6\n  };\n\n  struct Statistics {\n    const CommStatistics&amp; comm_statistics;\n    std::optional&lt;std::vector&lt;size_t&gt;&gt; worker_task_counts = {};\n  };\n\n  using StatisticsT =\n      std::conditional_t&lt;statistics_mode == StatisticsMode::Detailed, Statistics, std::monostate&gt;;\n\n  StatisticsT _statistics;\n\n  static StatisticsT create_statistics(const MPICommunicator&amp; comm) {\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      return Statistics{.comm_statistics = comm.get_statistics()};\n    } else {\n      return {};\n    }\n  }\n\n public:\n  explicit HierarchicalMPIWorkDistributor(std::function&lt;ResultT(TaskT)&gt; worker_function,\n                                          Config runtime_config = Config{})\n      : m_communicator(runtime_config.comm, MPICommunicator::Duplicate),\n        m_world_group(m_communicator),\n        m_worker_function(worker_function),\n        m_config(runtime_config),\n        _statistics{create_statistics(m_communicator)} {\n    // --- Initialize Topology Groups ---\n    if (m_config.coordinator_per_node) {\n      // 1. Identify physical nodes via split_by_node\n      MPICommunicator node_comm = m_communicator.split_by_node();\n\n      // 2. Create Local Group: Exclude Manager!\n      // If I am Manager, color is Undefined (I don't participate in local worker pool).\n      // Everyone else participates.\n      int local_color = (m_communicator.rank() == m_config.manager_rank) ? MPI_UNDEFINED : 0;\n\n      auto local_comm_opt = node_comm.split(local_color, m_communicator.rank());\n      if (local_comm_opt.has_value()) {\n        // Extract group from the temporary communicator, then let it be freed\n        m_local_group.emplace(*local_comm_opt);\n      }\n\n      // 3. Create Leader Group\n      // Who joins?\n      // A: The Manager (Always)\n      // B: The Node Coordinators (Rank 0 of the *Local* Comm)\n      bool is_manager = (m_communicator.rank() == m_config.manager_rank);\n      // Check if we're rank 0 in the local group (node coordinator)\n      bool is_node_coordinator = false;\n      if (m_local_group.has_value()) {\n        int my_local_rank = m_local_group-&gt;rank();\n        is_node_coordinator = (my_local_rank == 0);\n      }\n\n      int leader_color = (is_manager || is_node_coordinator) ? 0 : MPI_UNDEFINED;\n\n      // Key is global rank to maintain global ordering among leaders\n      auto leader_comm_opt = m_communicator.split(leader_color, m_communicator.rank());\n      if (leader_comm_opt.has_value()) {\n        // Extract group from the temporary communicator, then let it be freed\n        m_leader_group.emplace(*leader_comm_opt);\n      }\n    }\n\n    if (m_config.auto_run_workers &amp;&amp; m_communicator.rank() != m_config.manager_rank) {\n      run_worker();\n    }\n  }\n\n  const StatisticsT&amp; get_statistics() const\n    requires(statistics_mode != StatisticsMode::None)\n  {\n    DYNAMPI_ASSERT(is_root_manager(), \"Only the manager can access statistics\");\n    return _statistics;\n  }\n\n  void run_worker() {\n    DYNAMPI_ASSERT(m_communicator.rank() != m_config.manager_rank,\n                   \"Worker cannot run on the manager rank\");\n    if (is_leaf_worker()) {\n      // Leaf workers (usually local ranks &gt; 0) just request from parent\n      send_to_parent(nullptr, Tag::REQUEST);\n      while (!m_done) {\n        receive_from_anyone();\n      }\n    } else {\n      // Intermediate nodes (Node Coordinators)\n      int num_children = num_direct_children();\n      int prefetch = num_children * m_config.batch_size_multiplier;\n      const int pipeline_depth = std::max(1, m_config.pipeline_depth);\n\n      // Keeps up to `target` batches' worth of requests outstanding with\n      // our parent at once (see Config::pipeline_depth), sending only as\n      // many new ones as needed to close the gap -- so calling this\n      // repeatedly as replies trickle in and reduce m_outstanding_requests\n      // never over-sends. No-ops once m_done is known: the manager has\n      // stopped listening (finalize() only answers each child once -- see\n      // send_done_to_children_when_free()), so a request sent after that\n      // would never be received.\n      auto top_up_pipeline = [&amp;](int target) {\n        while (!m_done &amp;&amp; m_outstanding_requests &lt; target) {\n          send_to_parent(prefetch, Tag::REQUEST_BATCH);\n          m_outstanding_requests++;\n        }\n      };\n\n      // Prime with exactly one request, not the full pipeline_depth: while\n      // we're waiting for this very first reply, m_round_active is still\n      // false, so receive_task_batch_from() has no round to quarantine\n      // against yet and routes any reply straight into\n      // m_unallocated_task_queue (see below). Priming with more than one\n      // would risk several replies landing there before we ever set\n      // m_round_active, merging what should be separate rounds into one\n      // oversized first batch. Sending only one guarantees at most one\n      // reply is pending during this wait; the rest of the pipeline_depth\n      // is built up below, entirely after m_round_active is true.\n      top_up_pipeline(1);\n\n      while (!m_done) {\n        // A batch prefetched during the previous round (see below) is\n        // already sitting here, ready to go, with no round trip needed.\n        // (Prioritized task queues don't support batch prefetching; see\n        // receive_task_batch_from.)\n        if constexpr (!prioritize_tasks) {\n          if (!m_prefetched_tasks.empty()) {\n            for (auto&amp; task : m_prefetched_tasks) {\n              m_unallocated_task_queue.push_back(std::move(task));\n            }\n            m_prefetched_tasks.clear();\n          }\n        }\n\n        // If we have no tasks to give, wait for tasks from parent. Flush\n        // any results gained while waiting -- a real, confirmed deadlock\n        // otherwise: once this coordinator has distributed everything it\n        // currently has, this loop is the ONLY place it spends time until\n        // its parent sends more (which may never happen, e.g. once the\n        // parent's own task supply is exhausted but m_done hasn't arrived\n        // yet). Straggler results trickling in via receive_from_anyone()\n        // here would sit in m_results forever with no other flush point to\n        // reach -- and the parent's own exhaustion check depends on\n        // eventually seeing them, so both ends would wait on each other\n        // permanently. Flushing here closes that gap the same way the\n        // round-boundary flush does for the normal case.\n        while (!m_done &amp;&amp; m_unallocated_task_queue.empty()) {\n          receive_from_anyone();\n          if (!m_results.empty()) {\n            send_results_to_parent();\n          }\n        }\n        if (m_done) break;\n\n        // While this round is active, any TASK_BATCH that arrives (a reply\n        // to a pipelined request) is quarantined in m_prefetched_tasks\n        // rather than merged into m_unallocated_task_queue, so this round's\n        // distribution loop can never overshoot past this round's own\n        // starting queue size into next round's tasks.\n        m_round_active = true;\n\n        // Pipelining: top up outstanding requests to pipeline_depth-1 (the\n        // \"-1\" is this now-active round itself) as soon as we start working\n        // it, rather than only after its results have been fully collected\n        // and sent upstream. Requests and result returns are independent\n        // messages (see receive_request_batch_from / receive_result_batch_from),\n        // so replies can already be arriving while we're still distributing\n        // or awaiting results for this round. Without this, children sit\n        // idle for a full parent round trip between batches.\n        top_up_pipeline(pipeline_depth - 1);\n\n        // Process tasks: Give to workers or execute ourselves if needed.\n        // Deliberately does NOT break on m_done: a pipelined request can be\n        // answered with DONE (if the manager has nothing left and\n        // finalize() runs) while THIS round is still being handed out to\n        // our own children. Since m_round_active already quarantines any\n        // newly-arriving TASK_BATCH into m_prefetched_tasks, nothing new\n        // can leak into m_unallocated_task_queue here -- so finishing it\n        // out is always safe, and required: abandoning it would silently\n        // drop this round's tasks.\n        while (!m_unallocated_task_queue.empty()) {\n          if (m_free_worker_indices.empty()) {\n            // Must wait for a worker to become free\n            receive_from_anyone();\n          } else {\n            allocate_task_to_child();\n          }\n        }\n\n        m_round_active = false;\n\n        // Deliberately NOT gated on every result from this round having\n        // arrived: this class is unordered (see `static const bool ordered\n        // = false` above), so nothing downstream cares which round a result\n        // came from. Waiting here for a round's slowest straggler used to\n        // block send_results_to_parent() entirely -- even results from\n        // OTHER children that finished long ago sat unsent the whole time.\n        // Sending whatever has accumulated so far (possibly nothing, if\n        // every child in this round is still working) and moving straight\n        // on to distributing the next round lets fast children's results\n        // flow upward immediately and lets those same children pick up\n        // next-round work right away (allocate_task_to_child() only needs a\n        // free worker, not a fully-drained round) instead of sitting idle\n        // behind one slow sibling. Any straggler's result still gets\n        // collected and forwarded eventually -- either by a later round's\n        // flush below, or by the final drain after send_done_to_children_\n        // when_free() if it lands after the last round.\n        if (!m_results.empty()) {\n          send_results_to_parent();\n        }\n\n        // Minimum-progress guarantee: ensure at least one request is\n        // outstanding before we loop back to wait for the next round.\n        // Without this, pipeline_depth == 1 (top_up_pipeline(pipeline_depth\n        // - 1) above is a no-op at 0) would never ask for a next batch at\n        // all -- unlike the old implicit protocol this replaced, where\n        // sending RESULT_BATCH itself doubled as a request (see\n        // receive_result_batch_from), nothing here sends a request except\n        // top_up_pipeline. This is also not just a depth==1 special case:\n        // even at higher depths, a long round can fully drain\n        // m_outstanding_requests to 0 (every previously-sent request\n        // already answered and parked in m_prefetched_tasks) before the\n        // round finishes, and needs the same top-up to keep the pipeline\n        // fed afterward.\n        top_up_pipeline(1);\n        // Safe to honor m_done now: this round is fully distributed and\n        // whatever was ready has been flushed to our parent. Any results\n        // still outstanding (stragglers) are picked up by\n        // send_done_to_children_when_free()'s own draining below, and\n        // flushed by the final send_results_to_parent() after it.\n        if (m_done) break;\n      }\n      send_done_to_children_when_free();\n      // send_done_to_children_when_free() only returns once every child is\n      // free, which (see receive_result_from/receive_result_batch_from)\n      // requires having received that child's result first -- so by this\n      // point every result this coordinator will ever collect has arrived.\n      // The round loop above only flushes at round boundaries, so a\n      // straggler landing after the last round's flush (including one only\n      // observed here, during the done-notification drain) would otherwise\n      // never be forwarded, permanently violating finalize()'s\n      // m_results_sent_to_parent == m_tasks_received_from_parent invariant.\n      if (!m_results.empty()) {\n        send_results_to_parent();\n      }\n    }\n  }\n\n  void send_results_to_parent() {\n    DYNAMPI_ASSERT(!is_leaf_worker(), \"Leaf workers should not return results directly\");\n    DYNAMPI_ASSERT_NE(m_communicator.rank(), m_config.manager_rank,\n                      \"Manager should not request tasks from itself\");\n    std::vector&lt;ResultT&gt; results = m_results;\n    m_results.clear();\n\n    send_to_parent(results, Tag::RESULT_BATCH);\n    m_results_sent_to_parent += results.size();\n  }\n\n  bool is_root_manager() const { return m_communicator.rank() == m_config.manager_rank; }\n\n  size_t remaining_tasks_count() const {\n    DYNAMPI_ASSERT_EQ(m_communicator.rank(), m_config.manager_rank,\n                      \"Only the manager can check remaining tasks\");\n    return m_unallocated_task_queue.size();\n  }\n\n  void insert_task(TaskT task)\n    requires(!prioritize_tasks)\n  {\n    DYNAMPI_ASSERT_EQ(m_communicator.rank(), m_config.manager_rank,\n                      \"Only the manager can distribute tasks\");\n    m_unallocated_task_queue.push_back(task);\n    m_tasks_received_from_parent++;\n  }\n  void insert_task(const TaskT&amp; task, double priority)\n    requires(prioritize_tasks)\n  {\n    DYNAMPI_ASSERT_EQ(m_communicator.rank(), m_config.manager_rank,\n                      \"Only the manager can distribute tasks\");\n    m_unallocated_task_queue.emplace(priority, task);\n    m_tasks_received_from_parent++;\n  }\n\n  template &lt;typename Range&gt;\n    requires std::ranges::input_range&lt;Range&gt; &amp;&amp; (!prioritize_tasks)\n  void insert_tasks(const Range&amp; tasks) {\n    DYNAMPI_ASSERT_EQ(m_communicator.rank(), m_config.manager_rank,\n                      \"Only the manager can distribute tasks\");\n    std::copy(std::ranges::begin(tasks), std::ranges::end(tasks),\n              std::back_inserter(m_unallocated_task_queue));\n    m_tasks_received_from_parent +=\n        std::distance(std::ranges::begin(tasks), std::ranges::end(tasks));\n  }\n  void insert_tasks(const std::vector&lt;TaskT&gt;&amp; tasks)\n    requires(!prioritize_tasks)\n  {\n    insert_tasks(std::span&lt;const TaskT&gt;(tasks));\n  }\n\n  void allocate_task_to_child() {\n    if (m_communicator.size() &gt; 1) {\n      DYNAMPI_ASSERT(!m_free_worker_indices.empty(), \"Cannot allocate task with no free workers\");\n\n      TaskRequest request = m_free_worker_indices.top();\n      m_free_worker_indices.pop();\n\n      // Determine target and communicator based on request source\n      int worker_rank = request.worker_rank;\n      CommLayer layer = request.source_layer;\n\n      if (request.num_tasks_requested.has_value()) {\n        std::vector&lt;TaskT&gt; tasks;\n        int num_tasks = request.num_tasks_requested.value();\n\n        const int actual_num_tasks =\n            std::min&lt;int&gt;(num_tasks, static_cast&lt;int&gt;(m_unallocated_task_queue.size()));\n        tasks.reserve(actual_num_tasks);\n        if constexpr (std::is_same_v&lt;decltype(m_unallocated_task_queue), std::deque&lt;TaskT&gt;&gt;) {\n          tasks.assign(m_unallocated_task_queue.begin(),\n                       m_unallocated_task_queue.begin() + actual_num_tasks);\n          m_unallocated_task_queue.erase(m_unallocated_task_queue.begin(),\n                                         m_unallocated_task_queue.begin() + actual_num_tasks);\n        } else {\n          for (int i = 0; i &lt; actual_num_tasks; ++i) {\n            tasks.push_back(std::move(m_unallocated_task_queue.top().second));\n            m_unallocated_task_queue.pop();\n          }\n        }\n\n        send_to_worker(tasks, worker_rank, Tag::TASK_BATCH, layer);\n        m_tasks_sent_to_child += tasks.size();\n      } else {\n        const TaskT task = get_next_task_to_send();\n        send_to_worker(task, worker_rank, Tag::TASK, layer);\n        m_tasks_sent_to_child++;\n      }\n    } else {\n      const TaskT task = get_next_task_to_send();\n      m_results.emplace_back(m_worker_function(task));\n      m_tasks_executed++;\n    }\n  }\n\n  [[nodiscard]] std::vector&lt;ResultT&gt; run_tasks(const RunConfig&amp; config = RunConfig{}) {\n    DYNAMPI_ASSERT_EQ(m_communicator.rank(), m_config.manager_rank,\n                      \"Only the manager can finish remaining tasks\");\n    Timer timer;\n\n    while (true) {\n      // A. Target reached\n      if (m_results.size() &gt;= config.target_num_tasks) {\n        break;\n      }\n\n      // B. Time limit\n      if (config.max_seconds &amp;&amp; timer.elapsed().count() &gt;= *config.max_seconds) {\n        break;\n      }\n\n      // C. Exhaustion\n      size_t active_tasks = m_tasks_sent_to_child - m_results_received_from_child;\n      if (m_unallocated_task_queue.empty() &amp;&amp; active_tasks == 0) {\n        break;\n      }\n\n      bool tasks_available = !m_unallocated_task_queue.empty();\n      bool workers_available = !m_free_worker_indices.empty();\n      bool is_single_proc = (m_communicator.size() == 1);\n\n      if (tasks_available &amp;&amp; (is_single_proc || workers_available)) {\n        allocate_task_to_child();\n      } else if (active_tasks &gt; 0 || (tasks_available &amp;&amp; !workers_available)) {\n        receive_from_anyone();\n      }\n    }\n\n    // --- Return Logic ---\n    std::vector&lt;ResultT&gt; batch;\n\n    size_t available = m_results.size();\n    size_t count_to_return = available;\n\n    if (!config.allow_more_than_target_tasks) {\n      count_to_return = std::min(available, config.target_num_tasks);\n    }\n\n    batch.reserve(count_to_return);\n    auto end_it = m_results.begin() + count_to_return;\n    std::move(m_results.begin(), end_it, std::back_inserter(batch));\n    m_results.erase(m_results.begin(), end_it);\n\n    m_results_sent_to_parent += batch.size();\n    return batch;\n  }\n\n  [[nodiscard]] std::vector&lt;ResultT&gt; finish_remaining_tasks() {\n    RunConfig cfg;\n    cfg.target_num_tasks = std::numeric_limits&lt;size_t&gt;::max();\n    return run_tasks(cfg);\n  }\n\n  void finalize() {\n    DYNAMPI_ASSERT(!m_finalized, \"Work distribution already finalized\");\n    if (is_root_manager()) {\n      send_done_to_children_when_free();\n    }\n    m_finalized = true;\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      if (is_root_manager()) {\n        _statistics.worker_task_counts = std::vector&lt;size_t&gt;(m_communicator.size(), 0);\n      }\n      m_communicator.gather(m_tasks_executed,\n                            _statistics.worker_task_counts.has_value()\n                                ? &amp;_statistics.worker_task_counts.value()\n                                : nullptr,\n                            m_config.manager_rank);\n    }\n  }\n\n  ~HierarchicalMPIWorkDistributor() {\n    if (!m_finalized) {\n      finalize();\n    }\n    DYNAMPI_ASSERT_EQ(m_results_received_from_child, m_tasks_sent_to_child,\n                      \"All tasks should have been processed by workers before finalizing\");\n    DYNAMPI_ASSERT_EQ(m_results_sent_to_parent, m_tasks_received_from_parent,\n                      \"All results should have been sent to the parent before finalizing\");\n    if (is_leaf_worker())\n      DYNAMPI_ASSERT_EQ(m_results_received_from_child, 0,\n                        \"Leaf workers should not receive results from children\");\n    else if (m_communicator.size() &gt; 1)\n      DYNAMPI_ASSERT_EQ(m_results_received_from_child + m_tasks_executed, m_results_sent_to_parent,\n                        \"Results received from children should match results sent to parent\");\n  }\n\n private:\n  TaskT get_next_task_to_send() {\n    DYNAMPI_ASSERT(is_root_manager() || !is_leaf_worker(),\n                   \"Leaf workers should not send tasks directly\");\n    DYNAMPI_ASSERT(!m_unallocated_task_queue.empty(), \"There should be tasks available to send\");\n    TaskT task;\n    if constexpr (std::is_same_v&lt;decltype(m_unallocated_task_queue), std::deque&lt;TaskT&gt;&gt;) {\n      task = m_unallocated_task_queue.front();\n      m_unallocated_task_queue.pop_front();\n    } else {\n      task = m_unallocated_task_queue.top().second;\n      m_unallocated_task_queue.pop();\n    }\n    return task;\n  }\n\n  int idx_for_worker(int worker_rank) const {\n    DYNAMPI_ASSERT_NE(worker_rank, m_config.manager_rank,\n                      \"Manager rank should not be used as a worker rank\");\n    if (worker_rank &lt; m_config.manager_rank) {\n      return worker_rank;\n    } else {\n      return worker_rank - 1;\n    }\n  }\n\n  int worker_for_idx(int idx) const { return (idx &lt; m_config.manager_rank) ? idx : (idx + 1); }\n\n  // --- Helper: Determine which layer a world rank belongs to ---\n  CommLayer determine_layer_from_world_rank(int world_rank) const {\n    DYNAMPI_ASSERT(m_config.coordinator_per_node);\n    // Check if rank is in local group (and not manager)\n    if (m_local_group) {\n      int local_rank = m_world_group.translate_rank(world_rank, *m_local_group);\n      if (local_rank != MPI_UNDEFINED) {\n        return CommLayer::Local;\n      }\n    }\n    DYNAMPI_ASSERT(m_leader_group.has_value(), \"Leader group should be present\");\n    [[maybe_unused]] int leader_rank = m_world_group.translate_rank(world_rank, *m_leader_group);\n    DYNAMPI_ASSERT_NE(leader_rank, MPI_UNDEFINED, \"Rank should be in leader group\");\n    return CommLayer::Leader;\n  }\n\n  // --- Abstract Send Wrappers ---\n\n  template &lt;typename T&gt;\n  void send_to_parent(const T&amp; data, Tag tag) {\n    auto [target, layer] = get_parent_target();\n    DYNAMPI_ASSERT_NE(target, -1, \"Root cannot send to parent\");\n\n    // With groups, target is always a world rank, so use global communicator\n    m_communicator.send(data, target, tag);\n  }\n\n  template &lt;typename T&gt;\n  void send_to_worker(const T&amp; data, int rank, Tag tag, [[maybe_unused]] CommLayer layer) {\n    // With groups, rank is stored as world rank in TaskRequest, so use global communicator\n    m_communicator.send(data, rank, tag);\n  }\n\n  void send_done_to_children_when_free() {\n    const int direct_children = num_direct_children();\n    // Tracks unique children notified, not requests answered: with\n    // pipeline_depth &gt; 1 (see run_worker()) a single child can have several\n    // outstanding requests queued here at once, all requesting the same\n    // \"no more work\" answer. Counting requests instead of children would let\n    // one over-represented child consume the whole direct_children budget,\n    // leaving others never told to stop -- a real deadlock, not a\n    // theoretical one, once more than one request per child is possible.\n    std::set&lt;int&gt; notified;\n    while (static_cast&lt;int&gt;(notified.size()) &lt; direct_children) {\n      if (m_free_worker_indices.empty()) {\n        receive_from_anyone();\n        continue;\n      }\n      TaskRequest request = m_free_worker_indices.top();\n      m_free_worker_indices.pop();\n\n      if (notified.contains(request.worker_rank)) {\n        // Already told this child; it doesn't need (or expect) a separate\n        // reply per request it happened to have outstanding.\n        continue;\n      }\n      send_to_worker(nullptr, request.worker_rank, Tag::DONE, request.source_layer);\n      notified.insert(request.worker_rank);\n    }\n  }\n\n  using result_mpi_type = MPI_Type&lt;ResultT&gt;;\n  using task_mpi_type = MPI_Type&lt;TaskT&gt;;\n\n  void receive_result_from(MPI_Status status, [[maybe_unused]] MPICommunicator&amp; source_comm,\n                           CommLayer layer) {\n    m_results.push_back(ResultT{});\n    if (result_mpi_type::resize_required) {\n      DYNAMPI_UNIMPLEMENTED(  // LCOV_EXCL_LINE\n          \"Dynamic resizing of results is not supported in hierarchical distribution\");\n    }\n    // With groups, always use global communicator and determine layer from source rank\n    int world_source = status.MPI_SOURCE;\n    if (m_config.coordinator_per_node) {\n      layer = determine_layer_from_world_rank(world_source);\n    }\n    m_communicator.recv(m_results.back(), world_source, Tag::RESULT);\n    m_results_received_from_child++;\n    m_free_worker_indices.push(TaskRequest{.worker_rank = world_source, .source_layer = layer});\n  }\n\n  void receive_result_batch_from(MPI_Status status, [[maybe_unused]] MPICommunicator&amp; source_comm,\n                                 [[maybe_unused]] CommLayer layer) {\n    using message_type = MPI_Type&lt;std::vector&lt;ResultT&gt;&gt;;\n    int count;\n    DYNAMPI_MPI_CHECK(MPI_Get_count, (&amp;status, message_type::value, &amp;count));\n    std::vector&lt;ResultT&gt; results;\n    message_type::resize(results, count);\n    int world_source = status.MPI_SOURCE;\n    m_communicator.recv(results, world_source, Tag::RESULT_BATCH);\n    // Results and next-batch requests are independent messages (see the\n    // double-buffering refill in run_worker()), so receiving results here\n    // has no side effect on task allocation for this child.\n    std::copy(results.begin(), results.end(), std::back_inserter(m_results));\n    m_results_received_from_child += results.size();\n  }\n\n  void receive_execute_return_task_from(MPI_Status status,\n                                        [[maybe_unused]] MPICommunicator&amp; source_comm,\n                                        [[maybe_unused]] CommLayer layer) {\n    int count;\n    DYNAMPI_MPI_CHECK(MPI_Get_count, (&amp;status, task_mpi_type::value, &amp;count));\n    TaskT message;\n    task_mpi_type::resize(message, count);\n    // With groups, always use global communicator\n    int world_source = status.MPI_SOURCE;\n    m_communicator.recv(message, world_source, Tag::TASK);\n    m_tasks_received_from_parent++;\n    ResultT result = m_worker_function(message);\n    m_tasks_executed++;\n    // Reply on the global communicator\n    m_communicator.send(result, world_source, Tag::RESULT);\n    m_results_sent_to_parent++;\n  }\n\n  void receive_task_batch_from(MPI_Status status, [[maybe_unused]] MPICommunicator&amp; source_comm,\n                               [[maybe_unused]] CommLayer layer) {\n    if constexpr (prioritize_tasks) {\n      DYNAMPI_UNIMPLEMENTED(\"Prioritized hierarchical distribution\");\n    } else {\n      using message_type = MPI_Type&lt;std::vector&lt;TaskT&gt;&gt;;\n      int count;\n      DYNAMPI_MPI_CHECK(MPI_Get_count, (&amp;status, message_type::value, &amp;count));\n      std::vector&lt;TaskT&gt; tasks;\n      message_type::resize(tasks, count);\n      // With groups, always use global communicator\n      int world_source = status.MPI_SOURCE;\n      m_communicator.recv(tasks, world_source, Tag::TASK_BATCH);\n      m_tasks_received_from_parent += tasks.size();\n      // This reply fulfills one of our outstanding pipelined requests (see\n      // run_worker()'s top_up_pipeline()); only intermediate coordinators\n      // (never leaf workers, which use the unbatched TASK/RESULT protocol)\n      // send REQUEST_BATCH, so only they receive TASK_BATCH replies here.\n      if (m_outstanding_requests &gt; 0) --m_outstanding_requests;\n      // While a round is active, this is the reply to a request for a\n      // *future* round: quarantine it so the current round's distribution\n      // loop can't overshoot into it (see run_worker()).\n      auto&amp; target = m_round_active ? m_prefetched_tasks : m_unallocated_task_queue;\n      for (const auto&amp; task : tasks) {\n        target.push_back(task);\n      }\n    }\n  }\n\n  void receive_request_from(MPI_Status status, [[maybe_unused]] MPICommunicator&amp; source_comm,\n                            CommLayer layer) {\n    // With groups, always use global communicator and determine layer from source rank\n    int world_source = status.MPI_SOURCE;\n    if (m_config.coordinator_per_node) {\n      layer = determine_layer_from_world_rank(world_source);\n    }\n    m_communicator.recv_empty_message(world_source, Tag::REQUEST);\n    m_free_worker_indices.push(TaskRequest{.worker_rank = world_source, .source_layer = layer});\n  }\n\n  void receive_request_batch_from(MPI_Status status, [[maybe_unused]] MPICommunicator&amp; source_comm,\n                                  CommLayer layer) {\n    // With groups, always use global communicator and determine layer from source rank\n    int world_source = status.MPI_SOURCE;\n    if (m_config.coordinator_per_node) {\n      layer = determine_layer_from_world_rank(world_source);\n    }\n    int request_count;\n    m_communicator.recv(request_count, world_source, Tag::REQUEST_BATCH);\n    DYNAMPI_ASSERT_GT(request_count, 0, \"Invalid request count\");\n    DYNAMPI_ASSERT_LE(request_count, kMaxTasksRequested, \"Request count exceeds maximum allowed\");\n    m_free_worker_indices.push(TaskRequest{\n        .worker_rank = world_source, .source_layer = layer, .num_tasks_requested = request_count});\n  }\n\n  void receive_done_from(MPI_Status status, [[maybe_unused]] MPICommunicator&amp; source_comm,\n                         [[maybe_unused]] CommLayer layer) {\n    // With groups, always use global communicator\n    int world_source = status.MPI_SOURCE;\n    m_communicator.recv_empty_message(world_source, Tag::DONE);\n    m_done = true;\n  }\n\n  void receive_from_anyone() {\n    DYNAMPI_ASSERT_GT(m_communicator.size(), 1,\n                      \"There should be at least one worker to receive results from\");\n\n    MPI_Status status{};\n    CommLayer layer = CommLayer::Global;\n\n    // All messages now route through the global communicator regardless of topology,\n    // so a blocking probe is sufficient. The CommLayer is determined from the source\n    // rank in each receive method via determine_layer_from_world_rank.\n    status = m_communicator.probe();\n\n    // Assert that the tag is a valid Tag enum value before casting\n    DYNAMPI_ASSERT(status.MPI_TAG &gt;= static_cast&lt;int&gt;(Tag::TASK) &amp;&amp;\n                       status.MPI_TAG &lt;= static_cast&lt;int&gt;(Tag::REQUEST_BATCH),\n                   \"Received invalid MPI tag: \" + std::to_string(status.MPI_TAG));\n    Tag tag = static_cast&lt;Tag&gt;(status.MPI_TAG);\n    // Note: receive methods now use global communicator and determine layer from source rank\n    switch (tag) {\n      case Tag::TASK:\n        return receive_execute_return_task_from(status, m_communicator, layer);\n      case Tag::TASK_BATCH:\n        return receive_task_batch_from(status, m_communicator, layer);\n      case Tag::RESULT:\n        return receive_result_from(status, m_communicator, layer);\n      case Tag::RESULT_BATCH:\n        return receive_result_batch_from(status, m_communicator, layer);\n      case Tag::REQUEST:\n        return receive_request_from(status, m_communicator, layer);\n      case Tag::REQUEST_BATCH:\n        return receive_request_batch_from(status, m_communicator, layer);\n      case Tag::DONE:\n        return receive_done_from(status, m_communicator, layer);\n    }\n  }\n};\n\n};  // namespace dynampi\n</code></pre>"},{"location":"dynampi/hierarchical__lockfree__distributor_8hpp/","title":"File hierarchical_lockfree_distributor.hpp","text":"<p>FileList &gt; dynampi &gt; impl &gt; hierarchical_lockfree_distributor.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;algorithm&gt;</code></li> <li><code>#include &lt;cassert&gt;</code></li> <li><code>#include &lt;cstddef&gt;</code></li> <li><code>#include &lt;cstdint&gt;</code></li> <li><code>#include &lt;functional&gt;</code></li> <li><code>#include &lt;limits&gt;</code></li> <li><code>#include &lt;optional&gt;</code></li> <li><code>#include &lt;vector&gt;</code></li> <li><code>#include \"../mpi/mpi_communicator.hpp\"</code></li> <li><code>#include \"../mpi/mpi_group.hpp\"</code></li> <li><code>#include \"../mpi/mpi_types.hpp\"</code></li> <li><code>#include \"dynampi/impl/lockfree_distributor.hpp\"</code></li> <li><code>#include \"dynampi/mpi/mpi_error.hpp\"</code></li> <li><code>#include \"dynampi/utilities/timer.hpp\"</code></li> </ul>"},{"location":"dynampi/hierarchical__lockfree__distributor_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi namespace detail"},{"location":"dynampi/hierarchical__lockfree__distributor_8hpp/#classes","title":"Classes","text":"Type Name class HierarchicalLockFreeMPIWorkDistributor &lt;typename TaskT, typename ResultT, Options&gt; struct Config struct RunConfig class LockFreeLevel &lt;typename TaskT, typename ResultT&gt; struct Config <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_lockfree_distributor.hpp</code></p>"},{"location":"dynampi/hierarchical__lockfree__distributor_8hpp_source/","title":"File hierarchical_lockfree_distributor.hpp","text":"<p>File List &gt; dynampi &gt; impl &gt; hierarchical_lockfree_distributor.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2026 Ryan Stocks\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;algorithm&gt;\n#include &lt;cassert&gt;\n#include &lt;cstddef&gt;\n#include &lt;cstdint&gt;\n#include &lt;functional&gt;\n#include &lt;limits&gt;\n#include &lt;optional&gt;\n#include &lt;vector&gt;\n\n#include \"../mpi/mpi_communicator.hpp\"\n#include \"../mpi/mpi_group.hpp\"\n#include \"../mpi/mpi_types.hpp\"\n#include \"dynampi/impl/lockfree_distributor.hpp\"  // reuses dynampi::detail byte-packing helpers\n#include \"dynampi/mpi/mpi_error.hpp\"\n#include \"dynampi/utilities/timer.hpp\"\n\nnamespace dynampi {\n\nnamespace detail {\n\n// ---------------------------------------------------------------------------\n// LockFreeLevel\n//\n// One level of lock-free RMA task claiming plus Gatherv-based result\n// collection, scoped to an arbitrary communicator. The rank at `owner_rank`\n// (within `comm`) hosts the task table window; every other rank in `comm`\n// claims tasks from it in batches via atomic fetch-and-add/CAS (no messages,\n// no owner-side receive loop), executes them, and returns results via\n// periodic Gatherv rounds triggered by the owner bumping a gather-sequence\n// counter -- the same mechanism LockFreeMPIWorkDistributor uses at global\n// scope, factored out here so it can be composed at multiple levels of a\n// tree topology (see HierarchicalLockFreeMPIWorkDistributor below, which\n// instantiates one LockFreeLevel per tree level: manager&lt;-&gt;node coordinators,\n// and, independently, node coordinator&lt;-&gt;its local workers).\n//\n// Results are unordered: this level only tracks how many results came back,\n// not which original task each belongs to.\n// ---------------------------------------------------------------------------\ntemplate &lt;typename TaskT, typename ResultT&gt;\nclass LockFreeLevel {\n public:\n  struct Config {\n    MPI_Comm comm = MPI_COMM_NULL;\n    int owner_rank = 0;\n    int max_tasks = 8192;       // capacity of the task table (lifetime total for this level)\n    int max_task_count = 256;   // max elements per task (only for resizable TaskT)\n    int max_result_count = 256;  // unused by this level directly, kept for API symmetry\n  };\n\n  explicit LockFreeLevel(Config config) : m_config(config) { initialize_window(); }\n\n  LockFreeLevel(const LockFreeLevel&amp;) = delete;\n  LockFreeLevel&amp; operator=(const LockFreeLevel&amp;) = delete;\n\n  ~LockFreeLevel() {\n    if (m_window != MPI_WIN_NULL) {\n      DYNAMPI_MPI_CHECK(MPI_Win_unlock_all, (m_window));\n      MPI_Barrier(m_config.comm);\n      DYNAMPI_MPI_CHECK(MPI_Win_free, (&amp;m_window));\n      m_window = MPI_WIN_NULL;\n    }\n  }\n\n  bool is_owner() const { return comm_rank() == m_config.owner_rank; }\n  int comm_rank() const {\n    int r = 0;\n    MPI_Comm_rank(m_config.comm, &amp;r);\n    return r;\n  }\n  int comm_size() const {\n    int s = 0;\n    MPI_Comm_size(m_config.comm, &amp;s);\n    return s;\n  }\n\n  void idle_wait() { detail::rma_wait_idle(m_window); }\n\n  // --- Owner-side API ---\n\n  void publish_tasks(const std::vector&lt;TaskT&gt;&amp; tasks) {\n    assert(is_owner());\n    for (const auto&amp; task : tasks) publish_task(task);\n  }\n\n  void mark_finished() {\n    assert(is_owner());\n    m_owner_marked_finished = true;\n    if (comm_size() &gt; 1) {\n      atomic_set(FINISHED_OFF, 1);\n      idle_wait();\n    }\n  }\n\n  bool owner_marked_finished() const {\n    assert(is_owner());\n    return m_owner_marked_finished;\n  }\n\n  // Call only once the owner's own collection loop has fully finished\n  // (owner_collected_count() &gt;= owner_published_count(), with nothing else\n  // that could ever add to owner_published_count()). Drives one final\n  // announce/gather round (its Ibcast payload carries is_final=1) all the\n  // way to completion -- see advance_gather_round() -- so every claimant is\n  // guaranteed to see it via the same broadcast-driven mechanism it already\n  // polls with, rather than a separately-polled flag. finalize()'s drain\n  // loop above has already confirmed nothing remains to collect, so this is\n  // purely the termination handshake: it must still be a real, completed\n  // round (not a fire-and-forget flag write) because a claimant only learns\n  // \"no more rounds are coming\" by observing this exact broadcast.\n  void mark_gather_done() {\n    assert(is_owner());\n    if (comm_size() &gt; 1) {\n      assert(m_stage == GatherStage::Idle);\n      while (!m_owner_gather_finished) {\n        auto results = advance_gather_round(/*start_final=*/true);\n        assert(results.empty());\n        if (!m_owner_gather_finished) idle_wait();\n      }\n    } else {\n      m_owner_gather_finished = true;\n    }\n  }\n  size_t owner_published_count() const {\n    assert(is_owner());\n    return static_cast&lt;size_t&gt;(m_total_tasks);\n  }\n  size_t owner_collected_count() const {\n    assert(is_owner());\n    return m_owner_collected_count;\n  }\n\n  // Owner: advance the round state machine by one non-blocking step and\n  // return whatever a completed round just produced (possibly empty --\n  // most calls land mid-round and return nothing). Safe to call anytime,\n  // including with nothing new pending: an Idle call always starts a new\n  // round (see advance_gather_round()), matching the old code's \"every call\n  // announces a round\" behavior, just spread asynchronously over however\n  // many calls it takes claimants to catch up instead of blocking here for\n  // all of them at once.\n  std::vector&lt;ResultT&gt; request_gather() {\n    assert(is_owner());\n    std::vector&lt;ResultT&gt; results;\n    if (comm_size() &gt; 1) {\n      results = advance_gather_round();\n    } else {\n      results = std::move(m_pending_results);\n      m_pending_results.clear();\n    }\n    m_owner_collected_count += results.size();\n    return results;\n  }\n\n  // Like request_gather(), but backs off briefly if the round came back\n  // empty, instead of immediately announcing another one. Without this, an\n  // owner polling in a tight loop (run_tasks()/finalize()) floods claimants\n  // with back-to-back gather rounds; claimants must service every round\n  // they observe (see run_local_worker()/run_leaf_leader_worker()), so a\n  // relentless owner starves them of time to actually claim and execute\n  // tasks between rounds.\n  std::vector&lt;ResultT&gt; request_gather_throttled() {\n    const size_t before = owner_collected_count();\n    auto results = request_gather();\n    if (owner_collected_count() == before) idle_wait();\n    return results;\n  }\n\n  // --- Claimant-side API ---\n\n  // Attempts to claim up to `want` tasks from the current publish frontier.\n  // Returns fewer (possibly zero) if the owner hasn't published that many\n  // yet, if another claimant won the race for this slice, or if the level\n  // has been marked finished with nothing left.\n  std::vector&lt;TaskT&gt; try_claim_batch(int want) {\n    assert(!is_owner());\n    if (want &lt;= 0) return {};\n    const int64_t total = atomic_read(TOTAL_OFF);\n    const int64_t head = atomic_read(HEAD_OFF);\n    if (head &gt;= total) return {};\n    const int64_t claim = std::min&lt;int64_t&gt;(want, total - head);\n    const int64_t start = compare_and_swap(HEAD_OFF, head, head + claim);\n    if (start != head) return {};  // lost the race; caller retries next call\n    std::vector&lt;TaskT&gt; tasks;\n    tasks.reserve(static_cast&lt;size_t&gt;(claim));\n    for (int64_t i = start; i &lt; start + claim; ++i) tasks.push_back(read_task(i));\n    return tasks;\n  }\n\n  // True once FINISHED has been observed and no tasks remain to claim. Use\n  // this to decide whether to keep trying to claim work -- NOT as a loop\n  // exit condition (see gather_fully_done()).\n  bool drained() {\n    assert(!is_owner());\n    if (!m_seen_finished &amp;&amp; atomic_read(FINISHED_OFF) != 0) m_seen_finished = true;\n    if (!m_seen_finished) return false;\n    return atomic_read(HEAD_OFF) &gt;= atomic_read(TOTAL_OFF);\n  }\n\n  // True once this claimant has fully processed the owner's final round\n  // (its Ibcast announce carried is_final=1, and this rank's own\n  // announce/count/data phases for that round have all completed -- see\n  // advance_gather_round()). This -- not drained() -- is the only condition\n  // under which a claimant may safely stop polling and let its process move\n  // toward destruction: MPI_Igatherv is collective, so any round the owner\n  // still announces after this claimant stops polling would hang the owner\n  // forever waiting for a participant that will never arrive. Unlike the\n  // RMA-flag version this replaced, there is nothing to read here -- this\n  // rank set the flag itself, locally, the moment it finished that round.\n  bool gather_fully_done() {\n    assert(!is_owner());\n    return m_gather_fully_done;\n  }\n\n  // Buffers a completed result for the next gather round this rank joins.\n  void stage_result(ResultT result) {\n    assert(!is_owner());\n    m_pending_results.push_back(std::move(result));\n  }\n\n  // True if this rank has staged results not yet flushed to the owner via a\n  // gather round. A claimant must not stop polling for gather rounds (and\n  // must not let its process move on toward destruction) while this is\n  // true: staging and reporting are decoupled (stage_result() only buffers\n  // locally; a result is only sent once this rank participates in a round\n  // the owner initiates), so exiting with pending results here strands\n  // them forever -- the owner has no way to know they exist, and this\n  // rank's own process is by then past the point where it could still\n  // respond to a gather request. See the fix this guards in\n  // run_local_worker() / run_leaf_leader_worker() / run_node_coordinator().\n  bool has_pending_results() const {\n    assert(!is_owner());\n    return !m_pending_results.empty();\n  }\n\n  // Call periodically (whenever otherwise idle, and after claiming/staging)\n  // to notice and join a gather round the owner initiated, or to advance\n  // one this rank already joined. Non-blocking: each call does at most one\n  // MPI_Test plus, if that just completed, posting the next phase -- never\n  // waits on a peer. Returns true if this call made progress.\n  bool maybe_participate_in_gather() {\n    assert(!is_owner());\n    const GatherStage before = m_stage;\n    advance_gather_round();\n    return m_stage != before;\n  }\n\n private:\n  // Window layout: [head][total][finished] then task slots. This window\n  // only carries the lock-free task-claiming state now -- gather-round\n  // announcement and completion are no longer RMA-polled at all (see\n  // GatherStage below); they travel over an MPI_Ibcast/Igather/Igatherv\n  // pipeline instead, which is why FINISHED is the only \"done\" flag left\n  // here. (A GATHER_DONE flag used to live here too; it's now carried as\n  // the is_final element of the Ibcast payload in m_bcast_buf, delivered by\n  // the same broadcast a claimant already polls to learn a round exists at\n  // all, rather than a second thing to separately poll for.)\n  static constexpr MPI_Aint HEAD_OFF = 0;\n  static constexpr MPI_Aint TOTAL_OFF = 8;\n  static constexpr MPI_Aint FINISHED_OFF = 16;\n  static constexpr size_t CONTROL_BYTES = 24;\n  // Per-task slot: [int64 count][data].\n  static constexpr size_t T_COUNT = 0;\n  static constexpr size_t T_DATA = 8;\n\n  // Gather-round state machine, driven one non-blocking step per call to\n  // advance_gather_round() (see below). Shared by owner and claimant: the\n  // Idle and AwaitingAnnounce phases behave differently per role, but\n  // AwaitingCounts/AwaitingData are structurally identical (just\n  // recvbuf-null-or-not), so both roles drive the same m_stage through the\n  // same function.\n  enum class GatherStage { Idle, AwaitingAnnounce, AwaitingCounts, AwaitingData };\n\n  Config m_config;\n  MPI_Win m_window = MPI_WIN_NULL;\n  std::vector&lt;std::byte&gt; m_window_buffer;  // owner: control + task table\n  alignas(int64_t) std::byte m_peer_window[sizeof(int64_t)]{};  // non-owner: Win_create placeholder\n\n  size_t m_task_elem = 0;\n  size_t m_max_task_count = 1;\n  size_t m_task_slot_stride = 0;\n  size_t m_task_base = 0;\n\n  int64_t m_total_tasks = 0;\n  bool m_seen_finished = false;\n  bool m_owner_marked_finished = false;\n  size_t m_owner_collected_count = 0;\n  std::vector&lt;ResultT&gt; m_pending_results;  // claimant: staged, not yet sent; owner (size==1): local store\n\n  GatherStage m_stage = GatherStage::Idle;\n  MPI_Request m_bcast_request = MPI_REQUEST_NULL;\n  MPI_Request m_gather_request = MPI_REQUEST_NULL;\n  MPI_Request m_gatherv_request = MPI_REQUEST_NULL;\n  int64_t m_bcast_buf[2] = {0, 0};  // [0]=target task id (informational), [1]=is_final\n  int m_exchange_send_count = 0;\n  std::vector&lt;std::byte&gt; m_exchange_send_buf;   // this rank's serialized contribution to the in-flight round\n  std::vector&lt;int&gt; m_exchange_byte_counts;      // owner only\n  std::vector&lt;int&gt; m_exchange_displacements;    // owner only\n  std::vector&lt;std::byte&gt; m_exchange_recv_buf;   // owner only\n  bool m_gather_fully_done = false;   // claimant only: seen and finished the final round\n  bool m_owner_gather_finished = false;  // owner only: final round fully completed\n\n  void initialize_window() {\n    m_task_elem = static_cast&lt;size_t&gt;(detail::mpi_type_size_bytes&lt;TaskT&gt;());\n    m_max_task_count =\n        MPI_Type&lt;TaskT&gt;::resize_required ? static_cast&lt;size_t&gt;(m_config.max_task_count) : 1;\n    m_task_slot_stride = detail::round_up_8(T_DATA + m_max_task_count * m_task_elem);\n\n    const size_t capacity = static_cast&lt;size_t&gt;(m_config.max_tasks);\n    m_task_base = CONTROL_BYTES;\n    const size_t owner_window_bytes = m_task_base + capacity * m_task_slot_stride;\n\n    if (is_owner() &amp;&amp; comm_size() &gt; 1) {\n      m_window_buffer.resize(owner_window_bytes);\n    }\n    if (comm_size() == 1) return;  // no peers: nothing to expose over RMA\n\n    void* base = nullptr;\n    MPI_Aint bsize = 0;\n    if (is_owner()) {\n      base = m_window_buffer.data();\n      bsize = static_cast&lt;MPI_Aint&gt;(m_window_buffer.size());\n    } else {\n      base = m_peer_window;\n      bsize = static_cast&lt;MPI_Aint&gt;(sizeof(m_peer_window));\n    }\n    DYNAMPI_MPI_CHECK(MPI_Win_create, (base, bsize, 1, MPI_INFO_NULL, m_config.comm, &amp;m_window));\n    DYNAMPI_MPI_CHECK(MPI_Win_lock_all, (MPI_MODE_NOCHECK, m_window));\n  }\n\n  MPI_Aint task_slot(int64_t index) const {\n    return static_cast&lt;MPI_Aint&gt;(m_task_base + static_cast&lt;size_t&gt;(index) * m_task_slot_stride);\n  }\n\n  void flush(int rank) { DYNAMPI_MPI_CHECK(MPI_Win_flush, (rank, m_window)); }\n\n  int64_t atomic_read(MPI_Aint offset) {\n    int64_t in = 0, out;\n    DYNAMPI_MPI_CHECK(MPI_Fetch_and_op,\n                      (&amp;in, &amp;out, MPI_INT64_T, m_config.owner_rank, offset, MPI_NO_OP, m_window));\n    flush(m_config.owner_rank);\n    return out;\n  }\n  void atomic_set(MPI_Aint offset, int64_t value) {\n    int64_t in = value, out;\n    DYNAMPI_MPI_CHECK(MPI_Fetch_and_op, (&amp;in, &amp;out, MPI_INT64_T, m_config.owner_rank, offset,\n                                         MPI_REPLACE, m_window));\n    flush(m_config.owner_rank);\n  }\n  int64_t compare_and_swap(MPI_Aint offset, int64_t expected, int64_t desired) {\n    int64_t comp = expected, des = desired, out;\n    DYNAMPI_MPI_CHECK(MPI_Compare_and_swap,\n                      (&amp;des, &amp;comp, &amp;out, MPI_INT64_T, m_config.owner_rank, offset, m_window));\n    flush(m_config.owner_rank);\n    return out;\n  }\n  // MPI_Put/MPI_Get take a plain `int` count, but n (a byte length derived\n  // from task/result-table capacities that can legitimately reach into the\n  // hundreds of millions of slots) can exceed INT_MAX. static_cast&lt;int&gt;(n)\n  // on an over-INT_MAX n silently wraps -- see the identical fix (and its\n  // fuller rationale) in hierarchical_async_put_lockfree_distributor.hpp's\n  // put_bytes()/get_bytes(). Chunk into INT_MAX-bounded pieces so no single\n  // MPI_Put/MPI_Get call is ever handed a count that doesn't fit in `int`.\n  static constexpr size_t kMaxRmaChunkBytes = static_cast&lt;size_t&gt;(std::numeric_limits&lt;int&gt;::max());\n\n  void put_bytes(const void* src, size_t n, MPI_Aint offset) {\n    if (n == 0) return;\n    const auto* bytes = static_cast&lt;const std::byte*&gt;(src);\n    size_t done = 0;\n    while (done &lt; n) {\n      const size_t chunk = std::min(kMaxRmaChunkBytes, n - done);\n      DYNAMPI_MPI_CHECK(MPI_Put, (bytes + done, static_cast&lt;int&gt;(chunk), MPI_BYTE,\n                                  m_config.owner_rank, offset + static_cast&lt;MPI_Aint&gt;(done),\n                                  static_cast&lt;int&gt;(chunk), MPI_BYTE, m_window));\n      done += chunk;\n    }\n    flush(m_config.owner_rank);\n  }\n  void get_bytes(void* dst, size_t n, MPI_Aint offset) {\n    if (n == 0) return;\n    auto* bytes = static_cast&lt;std::byte*&gt;(dst);\n    size_t done = 0;\n    while (done &lt; n) {\n      const size_t chunk = std::min(kMaxRmaChunkBytes, n - done);\n      DYNAMPI_MPI_CHECK(MPI_Get, (bytes + done, static_cast&lt;int&gt;(chunk), MPI_BYTE,\n                                  m_config.owner_rank, offset + static_cast&lt;MPI_Aint&gt;(done),\n                                  static_cast&lt;int&gt;(chunk), MPI_BYTE, m_window));\n      done += chunk;\n    }\n    flush(m_config.owner_rank);\n  }\n\n  void publish_task(const TaskT&amp; task) {\n    const int64_t index = m_total_tasks;\n    assert(static_cast&lt;size_t&gt;(index) &lt; static_cast&lt;size_t&gt;(m_config.max_tasks) &amp;&amp;\n           \"LockFreeLevel: exceeded max_tasks capacity\");\n    if (comm_size() &gt; 1) {\n      const int count = MPI_Type&lt;TaskT&gt;::count(task);\n      assert(static_cast&lt;size_t&gt;(count) &lt;= m_max_task_count &amp;&amp;\n             \"LockFreeLevel: task exceeds max_task_count\");\n      const size_t data_bytes = static_cast&lt;size_t&gt;(count) * m_task_elem;\n      std::vector&lt;std::byte&gt; buffer(T_DATA + data_bytes);\n      detail::write_i64(buffer.data(), buffer.size(), T_COUNT, count);\n      if (count &gt; 0) {\n        detail::write_bytes(buffer.data(), buffer.size(), T_DATA, MPI_Type&lt;TaskT&gt;::ptr(task),\n                            data_bytes);\n      }\n      put_bytes(buffer.data(), buffer.size(), task_slot(index));\n      m_total_tasks++;\n      atomic_set(TOTAL_OFF, m_total_tasks);  // publish to claimants\n    } else {\n      m_total_tasks++;\n    }\n  }\n\n  TaskT read_task(int64_t index) {\n    int64_t count = 0;\n    get_bytes(&amp;count, 8, task_slot(index) + static_cast&lt;MPI_Aint&gt;(T_COUNT));\n    TaskT task{};\n    if constexpr (MPI_Type&lt;TaskT&gt;::resize_required) MPI_Type&lt;TaskT&gt;::resize(task, static_cast&lt;int&gt;(count));\n    get_bytes(MPI_Type&lt;TaskT&gt;::ptr(task), static_cast&lt;size_t&gt;(count) * m_task_elem,\n              task_slot(index) + static_cast&lt;MPI_Aint&gt;(T_DATA));\n    return task;\n  }\n\n  void serialize_pending_results(std::vector&lt;std::byte&gt;&amp; out) const {\n    const int elem = detail::mpi_type_size_bytes&lt;ResultT&gt;();\n    out.clear();\n    for (auto&amp; result : m_pending_results) {\n      const int count = MPI_Type&lt;ResultT&gt;::count(result);\n      assert(count &gt;= 0);\n      const size_t data_bytes =\n          count &gt; 0 ? static_cast&lt;size_t&gt;(count) * static_cast&lt;size_t&gt;(elem) : size_t{0};\n      const size_t offset = out.size();\n      out.resize(offset + 8 + data_bytes);\n      detail::write_i64(out.data(), out.size(), offset, count);\n      if (data_bytes &gt; 0) {\n        detail::write_bytes(out.data(), out.size(), offset + 8, MPI_Type&lt;ResultT&gt;::ptr(result),\n                            data_bytes);\n      }\n    }\n  }\n\n  static std::vector&lt;ResultT&gt; deserialize_results(const std::vector&lt;std::byte&gt;&amp; in) {\n    const int elem = detail::mpi_type_size_bytes&lt;ResultT&gt;();\n    std::vector&lt;ResultT&gt; output;\n    size_t pos = 0;\n    while (pos &lt; in.size()) {\n      assert(pos + 8 &lt;= in.size());\n      const int64_t count = detail::read_i64(in.data(), in.size(), pos);\n      pos += 8;\n      ResultT result{};\n      if constexpr (MPI_Type&lt;ResultT&gt;::resize_required)\n        MPI_Type&lt;ResultT&gt;::resize(result, static_cast&lt;int&gt;(count));\n      assert(count &gt;= 0);\n      const size_t data_bytes =\n          count &gt; 0 ? static_cast&lt;size_t&gt;(count) * static_cast&lt;size_t&gt;(elem) : size_t{0};\n      detail::read_result_bytes(in.data(), in.size(), pos, result, data_bytes);\n      pos += data_bytes;\n      output.push_back(std::move(result));\n    }\n    return output;\n  }\n\n  // Phase 1-&gt;2: snapshot this rank's staged results (claimant only -- the\n  // owner never stages any, see stage_result()'s assert) and post the\n  // byte-count Igather every rank must participate in before displacements\n  // for the data phase can be computed.\n  void post_count_gather() {\n    const bool owner = is_owner();\n    if (!owner) {\n      // Snapshot now: m_exchange_send_buf must stay valid and unmodified\n      // until post_data_gatherv()'s Igatherv completes. Anything staged\n      // after this point accumulates fresh into m_pending_results for\n      // whichever round comes after this one.\n      serialize_pending_results(m_exchange_send_buf);\n      m_pending_results.clear();\n    }\n    m_exchange_send_count = static_cast&lt;int&gt;(m_exchange_send_buf.size());\n    if (owner) m_exchange_byte_counts.resize(static_cast&lt;size_t&gt;(comm_size()));\n    DYNAMPI_MPI_CHECK(MPI_Igather,\n                      (&amp;m_exchange_send_count, 1, MPI_INT,\n                       owner ? m_exchange_byte_counts.data() : nullptr, 1, MPI_INT,\n                       m_config.owner_rank, m_config.comm, &amp;m_gather_request));\n  }\n\n  // Phase 2-&gt;3: owner now knows every rank's byte count, so it can size its\n  // receive buffer and displacements; post the data Igatherv every rank\n  // must also participate in, sending its snapshot from post_count_gather().\n  void post_data_gatherv() {\n    const bool owner = is_owner();\n    if (owner) {\n      const int size = comm_size();\n      m_exchange_displacements.resize(static_cast&lt;size_t&gt;(size));\n      int total = 0;\n      for (int r = 0; r &lt; size; ++r) {\n        m_exchange_displacements[static_cast&lt;size_t&gt;(r)] = total;\n        total += m_exchange_byte_counts[static_cast&lt;size_t&gt;(r)];\n      }\n      m_exchange_recv_buf.resize(static_cast&lt;size_t&gt;(total));\n    }\n    DYNAMPI_MPI_CHECK(\n        MPI_Igatherv, (m_exchange_send_buf.data(), m_exchange_send_count, MPI_BYTE,\n                      owner ? m_exchange_recv_buf.data() : nullptr,\n                      owner ? m_exchange_byte_counts.data() : nullptr,\n                      owner ? m_exchange_displacements.data() : nullptr, MPI_BYTE,\n                      m_config.owner_rank, m_config.comm, &amp;m_gatherv_request));\n  }\n\n  // Advances this rank's participation in the current (or next) gather\n  // round by exactly one non-blocking step: one MPI_Test (or, only when\n  // starting a round, one Ibcast post) per call, never a wait on a peer.\n  // Returns any results the owner just finished harvesting -- always empty\n  // except on the call whose MPI_Test observes the data phase complete.\n  //\n  // `start_final` (owner only): when true and currently Idle, the round\n  // this starts carries is_final=1 in its Ibcast payload, telling every\n  // claimant this is the last round they'll ever need to join (see\n  // mark_gather_done() and gather_fully_done()). Claimants ignore the\n  // parameter entirely -- they only ever react to what's broadcast to them.\n  std::vector&lt;ResultT&gt; advance_gather_round(bool start_final = false) {\n    switch (m_stage) {\n      case GatherStage::Idle: {\n        if (is_owner()) {\n          m_bcast_buf[0] = m_total_tasks;\n          m_bcast_buf[1] = start_final ? 1 : 0;\n          DYNAMPI_MPI_CHECK(MPI_Ibcast, (m_bcast_buf, 2, MPI_INT64_T, m_config.owner_rank,\n                                         m_config.comm, &amp;m_bcast_request));\n          // Root's own local completion of a broadcast doesn't depend on\n          // any claimant -- it's this rank's own send, expected to finish\n          // promptly regardless of how far behind a straggler claimant is\n          // (unlike the old barrier, this is not a whole-comm rendezvous).\n          // Reaping it inline here, rather than adding a fourth polled\n          // phase, keeps the state machine to the two genuinely\n          // peer-dependent stages below.\n          DYNAMPI_MPI_CHECK(MPI_Wait, (&amp;m_bcast_request, MPI_STATUS_IGNORE));\n          post_count_gather();\n          m_stage = GatherStage::AwaitingCounts;\n        } else {\n          if (m_gather_fully_done) return {};\n          DYNAMPI_MPI_CHECK(MPI_Ibcast, (m_bcast_buf, 2, MPI_INT64_T, m_config.owner_rank,\n                                         m_config.comm, &amp;m_bcast_request));\n          m_stage = GatherStage::AwaitingAnnounce;\n        }\n        return {};\n      }\n      case GatherStage::AwaitingAnnounce: {\n        assert(!is_owner());\n        int done = 0;\n        DYNAMPI_MPI_CHECK(MPI_Test, (&amp;m_bcast_request, &amp;done, MPI_STATUS_IGNORE));\n        if (!done) return {};\n        post_count_gather();\n        m_stage = GatherStage::AwaitingCounts;\n        return {};\n      }\n      case GatherStage::AwaitingCounts: {\n        int done = 0;\n        DYNAMPI_MPI_CHECK(MPI_Test, (&amp;m_gather_request, &amp;done, MPI_STATUS_IGNORE));\n        if (!done) return {};\n        post_data_gatherv();\n        m_stage = GatherStage::AwaitingData;\n        return {};\n      }\n      case GatherStage::AwaitingData: {\n        int done = 0;\n        DYNAMPI_MPI_CHECK(MPI_Test, (&amp;m_gatherv_request, &amp;done, MPI_STATUS_IGNORE));\n        if (!done) return {};\n        std::vector&lt;ResultT&gt; results = is_owner() ? deserialize_results(m_exchange_recv_buf)\n                                                   : std::vector&lt;ResultT&gt;{};\n        const bool was_final = (m_bcast_buf[1] != 0);\n        m_exchange_send_buf.clear();\n        m_stage = GatherStage::Idle;\n        if (was_final) {\n          if (is_owner()) {\n            m_owner_gather_finished = true;\n          } else {\n            m_gather_fully_done = true;\n          }\n        }\n        return results;\n      }\n    }\n    return {};\n  }\n};\n\n}  // namespace detail\n\n// ---------------------------------------------------------------------------\n// HierarchicalLockFreeMPIWorkDistributor\n//\n// Combines HierarchicalMPIWorkDistributor's node-aware tree topology\n// (manager &lt;-&gt; per-node coordinators &lt;-&gt; per-node local workers) with\n// LockFreeMPIWorkDistributor's one-sided RMA task dispatch, applied\n// independently at each level of the tree:\n//\n//   - Leader level: the manager's task table window is claimed, in\n//     batches, only by node coordinators (fan-in bounded by node count,\n//     not total rank count).\n//   - Local level: each node coordinator hosts its own task table window\n//     that only its own node's workers claim from (fan-in bounded by\n//     ranks-per-node).\n//\n// This avoids both bottlenecks of the two distributors it combines: the\n// flat LockFreeMPIWorkDistributor's single window fielding RMA traffic from\n// every rank in the job, and HierarchicalMPIWorkDistributor's blocking\n// message-passing receive loop at each coordinator.\n//\n// Results are unordered (see LockFreeLevel); use a different distributor if\n// task-index-ordered output is required. Task prioritization and detailed\n// statistics are not supported (matching LockFreeMPIWorkDistributor).\n// ---------------------------------------------------------------------------\ntemplate &lt;typename TaskT, typename ResultT, typename... Options&gt;\nclass HierarchicalLockFreeMPIWorkDistributor {\n public:\n  struct Config {\n    MPI_Comm comm = MPI_COMM_WORLD;\n    int manager_rank = 0;\n    bool auto_run_workers = true;\n    int leader_batch_multiplier = 2;  // coordinators claim (local peers)*multiplier tasks at once\n    int local_batch_size = 1;         // local workers claim this many tasks at once from their coordinator\n    int max_tasks = 8192;             // leader-level task table capacity (lifetime total)\n    int max_local_tasks = 8192;       // per-node local task table capacity (lifetime total, per node)\n    int max_task_count = 256;\n    int max_result_count = 256;\n  };\n\n  struct RunConfig {\n    size_t target_num_tasks = std::numeric_limits&lt;size_t&gt;::max();\n    bool allow_more_than_target_tasks = true;\n    std::optional&lt;double&gt; max_seconds = std::nullopt;\n  };\n\n  static const bool ordered = false;\n\n  explicit HierarchicalLockFreeMPIWorkDistributor(std::function&lt;ResultT(TaskT)&gt; worker_function,\n                                                   Config config = {})\n      : m_config(config),\n        m_world_comm(config.comm, MPICommunicator&lt;&gt;::Duplicate),\n        m_worker_function(std::move(worker_function)) {\n    setup_topology();\n    setup_levels();\n\n    if (m_config.auto_run_workers &amp;&amp; !is_root_manager()) run_worker();\n  }\n\n  ~HierarchicalLockFreeMPIWorkDistributor() {\n    if (!m_finalized) finalize();\n  }\n\n  bool is_root_manager() const { return m_world_comm.rank() == m_config.manager_rank; }\n\n  size_t remaining_tasks_count() const {\n    assert(is_root_manager());\n    if (m_solo) return m_local_task_store.size() - m_local_collected_count;\n    return m_leader_level-&gt;owner_published_count() - m_leader_level-&gt;owner_collected_count() -\n           m_results.size();\n  }\n\n  void insert_task(TaskT task) {\n    assert(is_root_manager());\n    insert_tasks(std::vector&lt;TaskT&gt;{std::move(task)});\n  }\n\n  void insert_tasks(const std::vector&lt;TaskT&gt;&amp; tasks) {\n    assert(is_root_manager());\n    if (m_solo) {\n      m_local_task_store.insert(m_local_task_store.end(), tasks.begin(), tasks.end());\n    } else {\n      m_leader_level-&gt;publish_tasks(tasks);\n    }\n  }\n\n  [[nodiscard]] std::vector&lt;ResultT&gt; run_tasks(const RunConfig&amp; config = RunConfig{}) {\n    assert(is_root_manager());\n    Timer timer;\n\n    if (m_solo) {\n      // No other ranks at all: run everything inline. (m_leader_level would\n      // otherwise still get constructed for the manager -- see setup_levels\n      // -- so this must be gated on true world size, not level presence.)\n      while (m_local_collected_count &lt; m_local_task_store.size()) {\n        if (m_results.size() &gt;= config.target_num_tasks) break;\n        if (config.max_seconds &amp;&amp; timer.elapsed().count() &gt;= *config.max_seconds) break;\n        m_results.push_back(m_worker_function(m_local_task_store[m_local_collected_count]));\n        m_local_collected_count++;\n      }\n    } else {\n      while (true) {\n        if (m_results.size() &gt;= config.target_num_tasks) break;\n        if (config.max_seconds &amp;&amp; timer.elapsed().count() &gt;= *config.max_seconds) break;\n        if (m_leader_level-&gt;owner_collected_count() &gt;= m_leader_level-&gt;owner_published_count()) break;\n        auto results = m_leader_level-&gt;request_gather_throttled();\n        m_results.insert(m_results.end(), std::make_move_iterator(results.begin()),\n                         std::make_move_iterator(results.end()));\n      }\n    }\n\n    return drain_results(config.allow_more_than_target_tasks\n                             ? std::numeric_limits&lt;size_t&gt;::max()\n                             : config.target_num_tasks);\n  }\n\n  [[nodiscard]] std::vector&lt;ResultT&gt; finish_remaining_tasks() { return run_tasks({}); }\n\n  void finalize() {\n    assert(!m_finalized);\n    if (is_root_manager()) {\n      if (m_solo) {\n        while (m_local_collected_count &lt; m_local_task_store.size()) {\n          m_results.push_back(m_worker_function(m_local_task_store[m_local_collected_count]));\n          m_local_collected_count++;\n        }\n      } else {\n        while (m_leader_level-&gt;owner_collected_count() &lt; m_leader_level-&gt;owner_published_count()) {\n          auto results = m_leader_level-&gt;request_gather_throttled();\n          m_results.insert(m_results.end(), std::make_move_iterator(results.begin()),\n                           std::make_move_iterator(results.end()));\n        }\n        m_leader_level-&gt;mark_finished();\n        // Strictly after mark_finished(), and only now that the drain loop\n        // above confirms nothing more will ever be collected: see the\n        // window-layout comment on GATHER_DONE_OFF in LockFreeLevel.\n        m_leader_level-&gt;mark_gather_done();\n      }\n    }\n    m_finalized = true;\n  }\n\n  void run_worker() {\n    assert(!is_root_manager());\n    if (m_local_level &amp;&amp; m_local_comm-&gt;size() &gt; 1) {\n      if (m_leader_level) {\n        run_node_coordinator();\n      } else {\n        run_local_worker();\n      }\n    } else if (m_leader_level) {\n      // Sole rank on this node besides being a leader: no local peers, so\n      // this rank claims and executes directly at the leader level.\n      run_leaf_leader_worker();\n    }\n  }\n\n private:\n  Config m_config;\n  MPICommunicator&lt;&gt; m_world_comm;\n  std::function&lt;ResultT(TaskT)&gt; m_worker_function;\n  const bool m_solo = (m_world_comm.size() == 1);  // true world size 1: no levels are usable\n\n  std::optional&lt;MPICommunicator&lt;&gt;&gt; m_local_comm;   // this node's coordinator + local workers\n  std::optional&lt;MPICommunicator&lt;&gt;&gt; m_leader_comm;  // manager + all node coordinators\n\n  std::optional&lt;detail::LockFreeLevel&lt;TaskT, ResultT&gt;&gt; m_leader_level;\n  std::optional&lt;detail::LockFreeLevel&lt;TaskT, ResultT&gt;&gt; m_local_level;\n\n  bool m_finalized = false;\n  std::vector&lt;ResultT&gt; m_results;         // manager only: results ready to return\n  size_t m_returned_count = 0;            // manager only: results already handed to the caller\n\n  std::vector&lt;TaskT&gt; m_local_task_store;  // manager only, solo-world fallback\n  size_t m_local_collected_count = 0;     // manager only, solo-world fallback\n\n  void setup_topology() {\n    if (m_solo) return;  // nothing to split; LockFreeLevel can't run tasks itself (see m_solo)\n\n    MPICommunicator&lt;&gt; node_comm = m_world_comm.split_by_node();\n\n    const int local_color = (m_world_comm.rank() == m_config.manager_rank) ? MPI_UNDEFINED : 0;\n    auto local_opt = node_comm.split(local_color, m_world_comm.rank());\n    if (local_opt.has_value()) m_local_comm.emplace(std::move(*local_opt));\n\n    const bool is_manager = (m_world_comm.rank() == m_config.manager_rank);\n    bool is_node_coordinator = false;\n    if (m_local_comm.has_value()) is_node_coordinator = (m_local_comm-&gt;rank() == 0);\n\n    const int leader_color = (is_manager || is_node_coordinator) ? 0 : MPI_UNDEFINED;\n    auto leader_opt = m_world_comm.split(leader_color, m_world_comm.rank());\n    if (leader_opt.has_value()) m_leader_comm.emplace(std::move(*leader_opt));\n  }\n\n  void setup_levels() {\n    if (m_leader_comm.has_value()) {\n      MPIGroup world_group(m_world_comm);\n      MPIGroup leader_group(*m_leader_comm);\n      const int leader_owner_rank = world_group.translate_rank(m_config.manager_rank, leader_group);\n      typename detail::LockFreeLevel&lt;TaskT, ResultT&gt;::Config leader_cfg;\n      leader_cfg.comm = m_leader_comm-&gt;get();\n      leader_cfg.owner_rank = leader_owner_rank;\n      leader_cfg.max_tasks = m_config.max_tasks;\n      leader_cfg.max_task_count = m_config.max_task_count;\n      m_leader_level.emplace(leader_cfg);\n    }\n\n    if (m_local_comm.has_value()) {\n      typename detail::LockFreeLevel&lt;TaskT, ResultT&gt;::Config local_cfg;\n      local_cfg.comm = m_local_comm-&gt;get();\n      local_cfg.owner_rank = 0;  // node coordinator is always local rank 0\n      local_cfg.max_tasks = m_config.max_local_tasks;\n      local_cfg.max_task_count = m_config.max_task_count;\n      m_local_level.emplace(local_cfg);\n    }\n  }\n\n  // A node coordinator: bridges the leader level (claiming batches from the\n  // manager) and the local level (its own window, published to for its\n  // local workers to claim from). This rank plays both roles at once: owner\n  // of the local level, and claimant of the leader level -- so its exit\n  // condition is the AND of both sides' termination signals (see\n  // LockFreeLevel::gather_fully_done() for why \"no more tasks\" is not\n  // enough on its own).\n  void run_node_coordinator() {\n    const int local_peers = std::max(0, m_local_comm-&gt;size() - 1);\n    const int leader_claim_size = std::max(1, local_peers * m_config.leader_batch_multiplier);\n    bool local_gather_done_marked = false;\n\n    while (true) {\n      m_leader_level-&gt;maybe_participate_in_gather();\n\n      if (!m_local_level-&gt;owner_marked_finished()) {\n        auto tasks = m_leader_level-&gt;try_claim_batch(leader_claim_size);\n        if (!tasks.empty()) {\n          m_local_level-&gt;publish_tasks(tasks);\n        } else if (m_leader_level-&gt;drained()) {\n          m_local_level-&gt;mark_finished();\n        }\n      }\n\n      // Stop initiating local-level rounds once this coordinator's own\n      // collection there is confirmed complete -- there's nothing left to\n      // gather, and continuing would just needlessly force every local\n      // worker still polling into another synchronization for no reason.\n      if (!local_gather_done_marked) {\n        auto local_results = m_local_level-&gt;request_gather_throttled();\n        for (auto&amp; result : local_results) m_leader_level-&gt;stage_result(std::move(result));\n\n        if (m_local_level-&gt;owner_marked_finished() &amp;&amp;\n            m_local_level-&gt;owner_collected_count() &gt;= m_local_level-&gt;owner_published_count()) {\n          // Local level: this coordinator (as its owner) has now collected\n          // everything it ever will. Local workers waiting on\n          // gather_fully_done() can now safely exit.\n          m_local_level-&gt;mark_gather_done();\n          local_gather_done_marked = true;\n        }\n      }\n\n      // Leader level: this coordinator (as a claimant) may only exit once\n      // the manager has announced it will never call another gather round\n      // -- and, symmetrically to the local level above, only once any of\n      // this coordinator's own still-unflushed leader-level results have\n      // gone out (which they always will before the manager can possibly\n      // reach that point: it can't see everything collected while this\n      // coordinator still has something staged that hasn't reached it).\n      if (local_gather_done_marked &amp;&amp; m_leader_level-&gt;gather_fully_done() &amp;&amp;\n          !m_leader_level-&gt;has_pending_results()) {\n        break;\n      }\n\n      m_leader_level-&gt;idle_wait();\n    }\n  }\n\n  // A plain local worker: claims individual tasks from its node coordinator.\n  void run_local_worker() {\n    while (true) {\n      // Deliberately ignore the return value here (unlike a naive \"if\n      // pending round: continue\" short-circuit). If the coordinator is\n      // issuing gather rounds back-to-back (e.g. every iteration of\n      // run_node_coordinator(), which calls request_gather()\n      // unconditionally), skipping straight back to the top of this loop\n      // whenever a round was just run starves this rank of ever reaching\n      // try_claim_batch() again -- a real livelock, not merely a\n      // theoretical one: reproduced with 2 ranks (see repro_hlf.cpp in the\n      // session that found this). Always fall through to attempt a claim.\n      m_local_level-&gt;maybe_participate_in_gather();\n      if (!m_local_level-&gt;drained()) {\n        auto tasks = m_local_level-&gt;try_claim_batch(m_config.local_batch_size);\n        if (!tasks.empty()) {\n          for (auto&amp; task : tasks) m_local_level-&gt;stage_result(m_worker_function(std::move(task)));\n          continue;\n        }\n      }\n      // gather_fully_done() -- not drained() -- gates exit: see the\n      // window-layout comment on GATHER_DONE_OFF in LockFreeLevel. Confirmed\n      // via gdb backtraces on hung 8/9-rank runs that drained() alone lets\n      // this rank exit and stop polling while the coordinator is still\n      // waiting on other stragglers and will announce more rounds --\n      // MPI_Gather is collective, so this rank not showing up for one of\n      // those hangs the coordinator forever.\n      if (m_local_level-&gt;gather_fully_done()) break;\n      m_local_level-&gt;idle_wait();\n    }\n  }\n\n  // A leader-level rank with no local peers (alone on its node): claims and\n  // executes tasks directly at the leader level, no local level involved.\n  void run_leaf_leader_worker() {\n    while (true) {\n      // See run_local_worker(): must not skip claiming just because a\n      // gather round was serviced this iteration, or a manager hammering\n      // request_gather() in a tight loop starves this rank forever.\n      m_leader_level-&gt;maybe_participate_in_gather();\n      if (!m_leader_level-&gt;drained()) {\n        auto tasks = m_leader_level-&gt;try_claim_batch(m_config.leader_batch_multiplier);\n        if (!tasks.empty()) {\n          for (auto&amp; task : tasks) m_leader_level-&gt;stage_result(m_worker_function(std::move(task)));\n          continue;\n        }\n      }\n      // See run_local_worker(): gather_fully_done() (not drained()) gates\n      // exit -- same bug, same fix, at the leader level instead of local.\n      if (m_leader_level-&gt;gather_fully_done()) break;\n      m_leader_level-&gt;idle_wait();\n    }\n  }\n\n  std::vector&lt;ResultT&gt; drain_results(size_t limit) {\n    const size_t count = std::min(limit, m_results.size());\n    std::vector&lt;ResultT&gt; output;\n    output.reserve(count);\n    for (size_t i = 0; i &lt; count; ++i) output.push_back(std::move(m_results[i]));\n    m_results.erase(m_results.begin(), m_results.begin() + static_cast&lt;ptrdiff_t&gt;(count));\n    m_returned_count += count;\n    return output;\n  }\n};\n\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/hierarchical__nonblocking__distributor_8hpp/","title":"File hierarchical_nonblocking_distributor.hpp","text":"<p>FileList &gt; dynampi &gt; impl &gt; hierarchical_nonblocking_distributor.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;algorithm&gt;</code></li> <li><code>#include &lt;cassert&gt;</code></li> <li><code>#include &lt;cmath&gt;</code></li> <li><code>#include &lt;deque&gt;</code></li> <li><code>#include &lt;functional&gt;</code></li> <li><code>#include &lt;iostream&gt;</code></li> <li><code>#include &lt;iterator&gt;</code></li> <li><code>#include &lt;limits&gt;</code></li> <li><code>#include &lt;ranges&gt;</code></li> <li><code>#include &lt;set&gt;</code></li> <li><code>#include &lt;span&gt;</code></li> <li><code>#include &lt;stack&gt;</code></li> <li><code>#include &lt;type_traits&gt;</code></li> <li><code>#include &lt;vector&gt;</code></li> <li><code>#include \"../mpi/mpi_communicator.hpp\"</code></li> <li><code>#include \"../mpi/mpi_types.hpp\"</code></li> <li><code>#include \"dynampi/impl/base_distributor.hpp\"</code></li> <li><code>#include \"dynampi/utilities/assert.hpp\"</code></li> <li><code>#include \"dynampi/utilities/timer.hpp\"</code></li> </ul>"},{"location":"dynampi/hierarchical__nonblocking__distributor_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi"},{"location":"dynampi/hierarchical__nonblocking__distributor_8hpp/#classes","title":"Classes","text":"Type Name class HierarchicalNonBlockingMPIWorkDistributor &lt;typename TaskT, typename ResultT, Options&gt; struct Config struct RunConfig <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_nonblocking_distributor.hpp</code></p>"},{"location":"dynampi/hierarchical__nonblocking__distributor_8hpp_source/","title":"File hierarchical_nonblocking_distributor.hpp","text":"<p>File List &gt; dynampi &gt; impl &gt; hierarchical_nonblocking_distributor.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2025 QDX Technologies. Authored by Ryan Stocks &lt;ryan.stocks00@gmail.com&gt;\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;algorithm&gt;\n#include &lt;cassert&gt;\n#include &lt;cmath&gt;\n#include &lt;deque&gt;\n#include &lt;functional&gt;\n#include &lt;iostream&gt;\n#include &lt;iterator&gt;\n#include &lt;limits&gt;\n#include &lt;ranges&gt;\n#include &lt;set&gt;\n#include &lt;span&gt;\n#include &lt;stack&gt;\n#include &lt;type_traits&gt;\n#include &lt;vector&gt;\n\n#include \"../mpi/mpi_communicator.hpp\"\n#include \"../mpi/mpi_types.hpp\"\n#include \"dynampi/impl/base_distributor.hpp\"\n#include \"dynampi/utilities/assert.hpp\"\n#include \"dynampi/utilities/timer.hpp\"\n\nnamespace dynampi {\n\n// ---------------------------------------------------------------------------\n// HierarchicalNonBlockingMPIWorkDistributor\n//\n// Identical topology, protocol, and pipelining logic to\n// HierarchicalMPIWorkDistributor (see that file) -- this is a copy with one\n// change: every outgoing message (TASK, TASK_BATCH, RESULT, RESULT_BATCH,\n// REQUEST_BATCH) is sent via MPI_Isend instead of a blocking MPI_Send, so a\n// coordinator posting a reply to one child never stalls before it can move\n// on to serve the next. Each async send's buffer is kept alive in a small\n// per-type pool (AsyncSendPool below) until MPI confirms it, reaped\n// opportunistically whenever a new send of that type is posted.\n//\n// Receiving is unchanged (still a blocking MPI_Probe + matching MPI_Recv):\n// every call site that waits on it already has nothing else productive to\n// do at that exact point (see run_worker()), so converting it to a\n// proactively-posted MPI_Irecv would need a byte-count-then-data staging\n// protocol for the variable-length TASK_BATCH/RESULT_BATCH messages (sizes\n// aren't known until the sender tells you) without a clear benefit here --\n// this variant isolates the question of whether the *send* side blocking\n// was costing anything.\n//\n// Empty messages (REQUEST, DONE -- zero payload bytes) skip the pool\n// entirely: with no buffer to keep alive, MPI_Isend + MPI_Request_free is a\n// safe, genuine fire-and-forget.\n// ---------------------------------------------------------------------------\ntemplate &lt;typename TaskT, typename ResultT, typename... Options&gt;\nclass HierarchicalNonBlockingMPIWorkDistributor\n    : public BaseMPIWorkDistributor&lt;TaskT, ResultT, Options...&gt; {\n  using Base = BaseMPIWorkDistributor&lt;TaskT, ResultT, Options...&gt;;\n\n public:\n  struct Config {\n    MPI_Comm comm = MPI_COMM_WORLD;\n    int manager_rank = 0;\n    bool auto_run_workers = true;\n    std::optional&lt;size_t&gt; message_batch_size = std::nullopt;\n    std::optional&lt;int&gt; max_workers_per_coordinator = std::nullopt;\n    int batch_size_multiplier = 2;\n\n    // How many batches' worth of tasks/requests a coordinator tries to keep\n    // in the pipeline at once, including the one currently being\n    // distributed to its children. 1 disables prefetching entirely (a\n    // coordinator only asks for its next batch after fully finishing the\n    // current one). 2 is double-buffering: one batch active, one already\n    // requested (or in flight) so children never wait a full round trip\n    // between batches. Values above 2 keep additional batches' worth of\n    // requests outstanding simultaneously, trading more slack against\n    // round-trip latency variance for coarser load-balancing granularity\n    // (tasks committed this far ahead can't be reassigned to an idle\n    // sibling coordinator).\n    int pipeline_depth = 2;\n\n    // If true, topology is strictly mapped to physical nodes:\n    // Manager &lt;-&gt; Node Coordinators &lt;-&gt; Local Workers\n    // Note: Manager is excluded from its node's Local Comm to separate duties.\n    bool coordinator_per_node = true;\n  };\n\n  struct RunConfig {\n    // Stop once we have at least this many results ready to return.\n    size_t target_num_tasks = std::numeric_limits&lt;size_t&gt;::max();\n\n    // If false, strictly clips the return vector to `target_num_tasks`.\n    // Excess results are buffered for the next call.\n    bool allow_more_than_target_tasks = true;\n\n    // Stop if this much time has passed.\n    std::optional&lt;double&gt; max_seconds = std::nullopt;\n  };\n\n  static constexpr bool prioritize_tasks = Base::prioritize_tasks;\n  static const bool ordered = false;\n\n private:\n  typename Base::QueueT m_unallocated_task_queue;\n  std::vector&lt;ResultT&gt; m_results;\n\n  enum class CommLayer { Global, Local, Leader };\n\n  struct TaskRequest {\n    int worker_rank;\n    CommLayer source_layer = CommLayer::Global;  // Which comm did this come from?\n    std::optional&lt;int&gt; num_tasks_requested = std::nullopt;\n  };\n  static constexpr int kMaxTasksRequested = 1'000'000;  // guard against pathological reserve()\n  std::stack&lt;TaskRequest, std::vector&lt;TaskRequest&gt;&gt; m_free_worker_indices;\n\n  size_t m_tasks_sent_to_child = 0;\n  size_t m_results_received_from_child = 0;\n  size_t m_results_sent_to_parent = 0;\n  size_t m_tasks_received_from_parent = 0;\n  size_t m_tasks_executed = 0;\n  size_t m_results_returned = 0;\n\n  bool m_finalized = false;\n  bool m_done = false;\n\n  // Pipelining (see run_worker()'s intermediate-coordinator branch):\n  // tasks that arrive as a reply to a proactive next-batch request sent\n  // while the current round is still active are quarantined here and only\n  // released into m_unallocated_task_queue at the next round boundary, so\n  // the current round's distribution loop can never overshoot into the\n  // next round's tasks. Replies stay in arrival order (FIFO) regardless of\n  // which of possibly several outstanding requests they answer.\n  bool m_round_active = false;\n  std::deque&lt;TaskT&gt; m_prefetched_tasks;\n  // Requests sent to our parent (Tag::REQUEST_BATCH) whose reply --\n  // Tag::TASK_BATCH into m_prefetched_tasks/m_unallocated_task_queue, or\n  // Tag::DONE -- hasn't arrived yet. Kept below Config::pipeline_depth (see\n  // top_up_pipeline()).\n  int m_outstanding_requests = 0;\n\n  static constexpr StatisticsMode statistics_mode =\n      get_option_value&lt;track_statistics_t, Options...&gt;();\n\n  using MPICommunicator = dynampi::MPICommunicator&lt;track_statistics&lt;statistics_mode&gt;&gt;;\n\n  MPICommunicator m_communicator;  // Global communicator\n  MPIGroup m_world_group;          // Group for the global communicator (for rank translation)\n  std::optional&lt;MPIGroup&gt; m_local_group;  // Intra-node group (Shared Memory, excludes manager)\n  std::optional&lt;MPIGroup&gt;\n      m_leader_group;  // Inter-node group (Leaders only: manager + node coordinators)\n\n  std::function&lt;ResultT(TaskT)&gt; m_worker_function;\n  Config m_config;\n\n  // Cached parent target to avoid repeated MPI_Group_translate_ranks calls\n  mutable std::optional&lt;std::pair&lt;int, CommLayer&gt;&gt; m_cached_parent_target;\n\n  // Keeps a non-blocking send's buffer alive until MPI confirms the send,\n  // without ever blocking the poster on that confirmation. post() reaps\n  // already-completed entries (via MPI_Test, not Wait) before adding the\n  // new one -- an amortized, non-blocking cleanup rather than a separate\n  // sweep -- so the pool stays close to \"however many of this message type\n  // are genuinely in flight right now,\" not unboundedly growing.\n  template &lt;typename T&gt;\n  class AsyncSendPool {\n   public:\n    void post(MPICommunicator&amp; comm, T value, int dest, int tag) {\n      reap();\n      pending.emplace_back(std::move(value), MPI_Request{});\n      auto&amp; [buf, req] = pending.back();\n      using mpi_type = MPI_Type&lt;T&gt;;\n      DYNAMPI_MPI_CHECK(MPI_Isend, (mpi_type::ptr(buf), mpi_type::count(buf), mpi_type::value,\n                                    dest, tag, comm.get(), &amp;req));\n    }\n\n    // Must be called before this rank's process moves toward destruction:\n    // an Isend's buffer has to stay valid and its request has to be\n    // completed (Wait or Test-until-true) before it's safe to let go of\n    // either -- see the call sites in run_worker()/finalize().\n    void wait_all() {\n      for (auto&amp; [buf, req] : pending) {\n        DYNAMPI_MPI_CHECK(MPI_Wait, (&amp;req, MPI_STATUS_IGNORE));\n      }\n      pending.clear();\n    }\n\n   private:\n    void reap() {\n      for (auto it = pending.begin(); it != pending.end();) {\n        int flag = 0;\n        DYNAMPI_MPI_CHECK(MPI_Test, (&amp;it-&gt;second, &amp;flag, MPI_STATUS_IGNORE));\n        if (flag) {\n          it = pending.erase(it);\n        } else {\n          ++it;\n        }\n      }\n    }\n    std::deque&lt;std::pair&lt;T, MPI_Request&gt;&gt; pending;\n  };\n\n  AsyncSendPool&lt;int&gt; m_pending_int_sends;                        // REQUEST_BATCH\n  AsyncSendPool&lt;TaskT&gt; m_pending_task_sends;                      // TASK\n  AsyncSendPool&lt;ResultT&gt; m_pending_result_sends;                  // RESULT\n  AsyncSendPool&lt;std::vector&lt;TaskT&gt;&gt; m_pending_task_batch_sends;   // TASK_BATCH\n  AsyncSendPool&lt;std::vector&lt;ResultT&gt;&gt; m_pending_result_batch_sends;  // RESULT_BATCH\n\n  void wait_all_pending_sends() {\n    m_pending_int_sends.wait_all();\n    m_pending_task_sends.wait_all();\n    m_pending_result_sends.wait_all();\n    m_pending_task_batch_sends.wait_all();\n    m_pending_result_batch_sends.wait_all();\n  }\n\n  // --- Topology Helper Methods ---\n\n  inline int max_workers_per_coordinator() const {\n    const int default_value = std::max(2, static_cast&lt;int&gt;(std::sqrt(m_communicator.size())));\n    const int configured = m_config.max_workers_per_coordinator.value_or(default_value);\n    return std::max(1, configured);\n  }\n\n  // Returns {parent_rank, communicator_layer}\n  inline std::pair&lt;int, CommLayer&gt; get_parent_target() const {\n    // Return cached value if available\n    if (m_cached_parent_target.has_value()) {\n      return m_cached_parent_target.value();\n    }\n\n    std::pair&lt;int, CommLayer&gt; result;\n    DYNAMPI_ASSERT(!is_root_manager(), \"Root manager should not have a parent\");\n    if (m_config.coordinator_per_node) {\n      DYNAMPI_ASSERT(m_local_group.has_value() || m_leader_group.has_value(),\n                     \"Local or leader group should be present\");\n      if (m_local_group &amp;&amp; m_local_group-&gt;rank() &gt; 0) {\n        // Case 1: I am a Local Worker (Rank &gt; 0 in Local Group)\n        // Parent is the Node Coordinator (Local Rank 0).\n        // Translate local rank 0 to world rank\n        int node_coord_world_rank = m_local_group-&gt;translate_rank(0, m_world_group);\n        result = {node_coord_world_rank, CommLayer::Local};\n      } else {\n        // Case 2: I am a Node Coordinator (Local Rank 0).\n        // Parent is the Global Manager.\n        // With the new topology, Manager is ALWAYS in the leader group.\n        // We need the manager's world rank, which we already have\n        int global_manager = m_config.manager_rank;\n        result = std::make_pair(global_manager, CommLayer::Leader);\n      }\n    } else {\n      // Original Logic\n      int rank = m_communicator.rank();\n      int virtual_rank = rank == m_config.manager_rank ? 0 : idx_for_worker(rank) + 1;\n      int virtual_parent = (virtual_rank - 1) / max_workers_per_coordinator();\n      int parent_rank =\n          virtual_parent == 0 ? m_config.manager_rank : worker_for_idx(virtual_parent - 1);\n      result = {parent_rank, CommLayer::Global};\n    }\n\n    // Cache the result\n    m_cached_parent_target = result;\n    return result;\n  }\n\n  inline int total_num_children(int rank) const {\n    if (m_config.coordinator_per_node) {\n      DYNAMPI_UNIMPLEMENTED(\"Recursive child counting not supported/needed in Node topology mode\");\n      return 0;\n    }\n    int virtual_rank = rank == m_config.manager_rank ? 0 : idx_for_worker(rank) + 1;\n    int num_children = 0;\n    int max_children = max_workers_per_coordinator();\n    for (int i = 0; i &lt; max_children; ++i) {\n      int child = virtual_rank * max_children + i + 1;\n      if (child &gt;= m_communicator.size()) break;  // No more children\n      num_children += 1 + total_num_children(worker_for_idx(child - 1));\n    }\n    return num_children;\n  }\n\n  // Calculate number of direct children based on active topology\n  inline int num_direct_children() const {\n    if (m_config.coordinator_per_node) {\n      int count = 0;\n      // 1. Local Children: Everyone in local group except me (Rank 0)\n      if (m_local_group &amp;&amp; m_local_group-&gt;rank() == 0) {\n        count += (m_local_group-&gt;size() - 1);\n      }\n      // 2. Remote Children: If I am Manager, other Leaders are my children.\n      // Note: In this topology, Manager is IN leader group, but NOT in local group.\n      if (is_root_manager() &amp;&amp; m_leader_group) {\n        count += (m_leader_group-&gt;size() - 1);\n      }\n      return count;\n    } else {\n      // Original Logic\n      int rank = m_communicator.rank();\n      int num_children = 0;\n      int max_children = max_workers_per_coordinator();\n      for (int i = 0; i &lt; max_children; ++i) {\n        int virtual_rank = rank == m_config.manager_rank ? 0 : idx_for_worker(rank) + 1;\n        int virtual_child = virtual_rank * max_children + i + 1;\n        if (virtual_child &lt; m_communicator.size()) {\n          num_children++;\n        }\n      }\n      return num_children;\n    }\n  }\n\n  bool is_leaf_worker() const {\n    if (m_config.coordinator_per_node) {\n      if (is_root_manager()) return false;\n\n      // If I am NOT in local group (should only be Manager, handled above), panic?\n      // Actually, with this topology, everyone except Manager is in local group.\n      if (!m_local_group) return true;  // Safety fallback\n\n      // Standard Worker: Rank &gt; 0 in Local Group\n      if (m_local_group-&gt;rank() &gt; 0) return true;\n\n      // Node Coordinator: Rank 0 in Local Comm.\n      // Leaf only if single-core node (no children).\n      return num_direct_children() == 0;\n    } else {\n      int rank = m_communicator.rank();\n      int max_children = max_workers_per_coordinator();\n      int virtual_rank = rank == m_config.manager_rank ? 0 : idx_for_worker(rank) + 1;\n      int first_child_virtual = virtual_rank * max_children + 1;\n      return first_child_virtual &gt;= m_communicator.size();\n    }\n  }\n\n  enum Tag : int {\n    TASK = 0,\n    DONE = 1,\n    RESULT = 2,\n    REQUEST = 3,\n    TASK_BATCH = 4,\n    RESULT_BATCH = 5,\n    REQUEST_BATCH = 6\n  };\n\n  struct Statistics {\n    const CommStatistics&amp; comm_statistics;\n    std::optional&lt;std::vector&lt;size_t&gt;&gt; worker_task_counts = {};\n  };\n\n  using StatisticsT =\n      std::conditional_t&lt;statistics_mode == StatisticsMode::Detailed, Statistics, std::monostate&gt;;\n\n  StatisticsT _statistics;\n\n  static StatisticsT create_statistics(const MPICommunicator&amp; comm) {\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      return Statistics{.comm_statistics = comm.get_statistics()};\n    } else {\n      return {};\n    }\n  }\n\n public:\n  explicit HierarchicalNonBlockingMPIWorkDistributor(std::function&lt;ResultT(TaskT)&gt; worker_function,\n                                          Config runtime_config = Config{})\n      : m_communicator(runtime_config.comm, MPICommunicator::Duplicate),\n        m_world_group(m_communicator),\n        m_worker_function(worker_function),\n        m_config(runtime_config),\n        _statistics{create_statistics(m_communicator)} {\n    // --- Initialize Topology Groups ---\n    if (m_config.coordinator_per_node) {\n      // 1. Identify physical nodes via split_by_node\n      MPICommunicator node_comm = m_communicator.split_by_node();\n\n      // 2. Create Local Group: Exclude Manager!\n      // If I am Manager, color is Undefined (I don't participate in local worker pool).\n      // Everyone else participates.\n      int local_color = (m_communicator.rank() == m_config.manager_rank) ? MPI_UNDEFINED : 0;\n\n      auto local_comm_opt = node_comm.split(local_color, m_communicator.rank());\n      if (local_comm_opt.has_value()) {\n        // Extract group from the temporary communicator, then let it be freed\n        m_local_group.emplace(*local_comm_opt);\n      }\n\n      // 3. Create Leader Group\n      // Who joins?\n      // A: The Manager (Always)\n      // B: The Node Coordinators (Rank 0 of the *Local* Comm)\n      bool is_manager = (m_communicator.rank() == m_config.manager_rank);\n      // Check if we're rank 0 in the local group (node coordinator)\n      bool is_node_coordinator = false;\n      if (m_local_group.has_value()) {\n        int my_local_rank = m_local_group-&gt;rank();\n        is_node_coordinator = (my_local_rank == 0);\n      }\n\n      int leader_color = (is_manager || is_node_coordinator) ? 0 : MPI_UNDEFINED;\n\n      // Key is global rank to maintain global ordering among leaders\n      auto leader_comm_opt = m_communicator.split(leader_color, m_communicator.rank());\n      if (leader_comm_opt.has_value()) {\n        // Extract group from the temporary communicator, then let it be freed\n        m_leader_group.emplace(*leader_comm_opt);\n      }\n    }\n\n    if (m_config.auto_run_workers &amp;&amp; m_communicator.rank() != m_config.manager_rank) {\n      run_worker();\n    }\n  }\n\n  const StatisticsT&amp; get_statistics() const\n    requires(statistics_mode != StatisticsMode::None)\n  {\n    DYNAMPI_ASSERT(is_root_manager(), \"Only the manager can access statistics\");\n    return _statistics;\n  }\n\n  void run_worker() {\n    DYNAMPI_ASSERT(m_communicator.rank() != m_config.manager_rank,\n                   \"Worker cannot run on the manager rank\");\n    if (is_leaf_worker()) {\n      // Leaf workers (usually local ranks &gt; 0) just request from parent\n      send_to_parent(nullptr, Tag::REQUEST);\n      while (!m_done) {\n        receive_from_anyone();\n      }\n      wait_all_pending_sends();\n    } else {\n      // Intermediate nodes (Node Coordinators)\n      int num_children = num_direct_children();\n      int prefetch = num_children * m_config.batch_size_multiplier;\n      const int pipeline_depth = std::max(1, m_config.pipeline_depth);\n\n      // Keeps up to `target` batches' worth of requests outstanding with\n      // our parent at once (see Config::pipeline_depth), sending only as\n      // many new ones as needed to close the gap -- so calling this\n      // repeatedly as replies trickle in and reduce m_outstanding_requests\n      // never over-sends. No-ops once m_done is known: the manager has\n      // stopped listening (finalize() only answers each child once -- see\n      // send_done_to_children_when_free()), so a request sent after that\n      // would never be received.\n      auto top_up_pipeline = [&amp;](int target) {\n        while (!m_done &amp;&amp; m_outstanding_requests &lt; target) {\n          send_to_parent(prefetch, Tag::REQUEST_BATCH);\n          m_outstanding_requests++;\n        }\n      };\n\n      // Prime with exactly one request, not the full pipeline_depth: while\n      // we're waiting for this very first reply, m_round_active is still\n      // false, so receive_task_batch_from() has no round to quarantine\n      // against yet and routes any reply straight into\n      // m_unallocated_task_queue (see below). Priming with more than one\n      // would risk several replies landing there before we ever set\n      // m_round_active, merging what should be separate rounds into one\n      // oversized first batch. Sending only one guarantees at most one\n      // reply is pending during this wait; the rest of the pipeline_depth\n      // is built up below, entirely after m_round_active is true.\n      top_up_pipeline(1);\n\n      while (!m_done) {\n        // A batch prefetched during the previous round (see below) is\n        // already sitting here, ready to go, with no round trip needed.\n        // (Prioritized task queues don't support batch prefetching; see\n        // receive_task_batch_from.)\n        if constexpr (!prioritize_tasks) {\n          if (!m_prefetched_tasks.empty()) {\n            for (auto&amp; task : m_prefetched_tasks) {\n              m_unallocated_task_queue.push_back(std::move(task));\n            }\n            m_prefetched_tasks.clear();\n          }\n        }\n\n        // If we have no tasks to give, wait for tasks from parent\n        while (!m_done &amp;&amp; m_unallocated_task_queue.empty()) {\n          receive_from_anyone();\n        }\n        if (m_done) break;\n\n        size_t num_tasks_should_be_received = m_unallocated_task_queue.size();\n        // While this round is active, any TASK_BATCH that arrives (a reply\n        // to a pipelined request) is quarantined in m_prefetched_tasks\n        // rather than merged into m_unallocated_task_queue, so this round's\n        // distribution loop can never overshoot past\n        // num_tasks_should_be_received into next round's tasks.\n        m_round_active = true;\n\n        // Pipelining: top up outstanding requests to pipeline_depth-1 (the\n        // \"-1\" is this now-active round itself) as soon as we start working\n        // it, rather than only after its results have been fully collected\n        // and sent upstream. Requests and result returns are independent\n        // messages (see receive_request_batch_from / receive_result_batch_from),\n        // so replies can already be arriving while we're still distributing\n        // or awaiting results for this round. Without this, children sit\n        // idle for a full parent round trip between batches.\n        top_up_pipeline(pipeline_depth - 1);\n\n        // Process tasks: Give to workers or execute ourselves if needed.\n        // Deliberately does NOT break on m_done: a pipelined request can be\n        // answered with DONE (if the manager has nothing left and\n        // finalize() runs) while THIS round is still being handed out to\n        // our own children. Since m_round_active already quarantines any\n        // newly-arriving TASK_BATCH into m_prefetched_tasks, nothing new\n        // can leak into m_unallocated_task_queue here -- so finishing it\n        // out is always safe, and required: abandoning it would silently\n        // drop this round's tasks (DYNAMPI_ASSERT_EQ below compiles to\n        // nothing under NDEBUG/Release, so this would fail silently, not\n        // loudly).\n        while (!m_unallocated_task_queue.empty()) {\n          if (m_free_worker_indices.empty()) {\n            // Must wait for a worker to become free\n            receive_from_anyone();\n          } else {\n            allocate_task_to_child();\n          }\n        }\n\n        // Wait for results from children -- also not gated on m_done, for\n        // the same reason as above: this round's already-dispatched tasks\n        // must be collected before we can honor a DONE observed mid-round.\n        while (m_tasks_sent_to_child &gt; m_results_received_from_child) {\n          receive_from_anyone();\n        }\n\n        m_round_active = false;\n\n        (void)num_tasks_should_be_received;\n        DYNAMPI_ASSERT_EQ(m_results.size(), num_tasks_should_be_received);\n\n        send_results_to_parent();\n\n        // Minimum-progress guarantee: ensure at least one request is\n        // outstanding before we loop back to wait for the next round.\n        // Without this, pipeline_depth == 1 (top_up_pipeline(pipeline_depth\n        // - 1) above is a no-op at 0) would never ask for a next batch at\n        // all -- unlike the old implicit protocol this replaced, where\n        // sending RESULT_BATCH itself doubled as a request (see\n        // receive_result_batch_from), nothing here sends a request except\n        // top_up_pipeline. This is also not just a depth==1 special case:\n        // even at higher depths, a long round can fully drain\n        // m_outstanding_requests to 0 (every previously-sent request\n        // already answered and parked in m_prefetched_tasks) before the\n        // round finishes, and needs the same top-up to keep the pipeline\n        // fed afterward.\n        top_up_pipeline(1);\n        // Safe to honor m_done now: this round is fully distributed,\n        // collected, and flushed to our parent.\n        if (m_done) break;\n      }\n      send_done_to_children_when_free();\n      wait_all_pending_sends();\n    }\n  }\n\n  void send_results_to_parent() {\n    DYNAMPI_ASSERT(!is_leaf_worker(), \"Leaf workers should not return results directly\");\n    DYNAMPI_ASSERT_NE(m_communicator.rank(), m_config.manager_rank,\n                      \"Manager should not request tasks from itself\");\n    std::vector&lt;ResultT&gt; results = m_results;\n    m_results.clear();\n\n    send_to_parent(results, Tag::RESULT_BATCH);\n    m_results_sent_to_parent += results.size();\n  }\n\n  bool is_root_manager() const { return m_communicator.rank() == m_config.manager_rank; }\n\n  size_t remaining_tasks_count() const {\n    DYNAMPI_ASSERT_EQ(m_communicator.rank(), m_config.manager_rank,\n                      \"Only the manager can check remaining tasks\");\n    return m_unallocated_task_queue.size();\n  }\n\n  void insert_task(TaskT task)\n    requires(!prioritize_tasks)\n  {\n    DYNAMPI_ASSERT_EQ(m_communicator.rank(), m_config.manager_rank,\n                      \"Only the manager can distribute tasks\");\n    m_unallocated_task_queue.push_back(task);\n    m_tasks_received_from_parent++;\n  }\n  void insert_task(const TaskT&amp; task, double priority)\n    requires(prioritize_tasks)\n  {\n    DYNAMPI_ASSERT_EQ(m_communicator.rank(), m_config.manager_rank,\n                      \"Only the manager can distribute tasks\");\n    m_unallocated_task_queue.emplace(priority, task);\n    m_tasks_received_from_parent++;\n  }\n\n  template &lt;typename Range&gt;\n    requires std::ranges::input_range&lt;Range&gt; &amp;&amp; (!prioritize_tasks)\n  void insert_tasks(const Range&amp; tasks) {\n    DYNAMPI_ASSERT_EQ(m_communicator.rank(), m_config.manager_rank,\n                      \"Only the manager can distribute tasks\");\n    std::copy(std::ranges::begin(tasks), std::ranges::end(tasks),\n              std::back_inserter(m_unallocated_task_queue));\n    m_tasks_received_from_parent +=\n        std::distance(std::ranges::begin(tasks), std::ranges::end(tasks));\n  }\n  void insert_tasks(const std::vector&lt;TaskT&gt;&amp; tasks)\n    requires(!prioritize_tasks)\n  {\n    insert_tasks(std::span&lt;const TaskT&gt;(tasks));\n  }\n\n  void allocate_task_to_child() {\n    if (m_communicator.size() &gt; 1) {\n      DYNAMPI_ASSERT(!m_free_worker_indices.empty(), \"Cannot allocate task with no free workers\");\n\n      TaskRequest request = m_free_worker_indices.top();\n      m_free_worker_indices.pop();\n\n      // Determine target and communicator based on request source\n      int worker_rank = request.worker_rank;\n      CommLayer layer = request.source_layer;\n\n      if (request.num_tasks_requested.has_value()) {\n        std::vector&lt;TaskT&gt; tasks;\n        int num_tasks = request.num_tasks_requested.value();\n\n        const int actual_num_tasks =\n            std::min&lt;int&gt;(num_tasks, static_cast&lt;int&gt;(m_unallocated_task_queue.size()));\n        tasks.reserve(actual_num_tasks);\n        if constexpr (std::is_same_v&lt;decltype(m_unallocated_task_queue), std::deque&lt;TaskT&gt;&gt;) {\n          tasks.assign(m_unallocated_task_queue.begin(),\n                       m_unallocated_task_queue.begin() + actual_num_tasks);\n          m_unallocated_task_queue.erase(m_unallocated_task_queue.begin(),\n                                         m_unallocated_task_queue.begin() + actual_num_tasks);\n        } else {\n          for (int i = 0; i &lt; actual_num_tasks; ++i) {\n            tasks.push_back(std::move(m_unallocated_task_queue.top().second));\n            m_unallocated_task_queue.pop();\n          }\n        }\n\n        send_to_worker(tasks, worker_rank, Tag::TASK_BATCH, layer);\n        m_tasks_sent_to_child += tasks.size();\n      } else {\n        const TaskT task = get_next_task_to_send();\n        send_to_worker(task, worker_rank, Tag::TASK, layer);\n        m_tasks_sent_to_child++;\n      }\n    } else {\n      const TaskT task = get_next_task_to_send();\n      m_results.emplace_back(m_worker_function(task));\n      m_tasks_executed++;\n    }\n  }\n\n  [[nodiscard]] std::vector&lt;ResultT&gt; run_tasks(const RunConfig&amp; config = RunConfig{}) {\n    DYNAMPI_ASSERT_EQ(m_communicator.rank(), m_config.manager_rank,\n                      \"Only the manager can finish remaining tasks\");\n    Timer timer;\n\n    while (true) {\n      // A. Target reached\n      if (m_results.size() &gt;= config.target_num_tasks) {\n        break;\n      }\n\n      // B. Time limit\n      if (config.max_seconds &amp;&amp; timer.elapsed().count() &gt;= *config.max_seconds) {\n        break;\n      }\n\n      // C. Exhaustion\n      size_t active_tasks = m_tasks_sent_to_child - m_results_received_from_child;\n      if (m_unallocated_task_queue.empty() &amp;&amp; active_tasks == 0) {\n        break;\n      }\n\n      bool tasks_available = !m_unallocated_task_queue.empty();\n      bool workers_available = !m_free_worker_indices.empty();\n      bool is_single_proc = (m_communicator.size() == 1);\n\n      if (tasks_available &amp;&amp; (is_single_proc || workers_available)) {\n        allocate_task_to_child();\n      } else if (active_tasks &gt; 0 || (tasks_available &amp;&amp; !workers_available)) {\n        receive_from_anyone();\n      }\n    }\n\n    // --- Return Logic ---\n    std::vector&lt;ResultT&gt; batch;\n\n    size_t available = m_results.size();\n    size_t count_to_return = available;\n\n    if (!config.allow_more_than_target_tasks) {\n      count_to_return = std::min(available, config.target_num_tasks);\n    }\n\n    batch.reserve(count_to_return);\n    auto end_it = m_results.begin() + count_to_return;\n    std::move(m_results.begin(), end_it, std::back_inserter(batch));\n    m_results.erase(m_results.begin(), end_it);\n\n    m_results_sent_to_parent += batch.size();\n    return batch;\n  }\n\n  [[nodiscard]] std::vector&lt;ResultT&gt; finish_remaining_tasks() {\n    RunConfig cfg;\n    cfg.target_num_tasks = std::numeric_limits&lt;size_t&gt;::max();\n    return run_tasks(cfg);\n  }\n\n  void finalize() {\n    DYNAMPI_ASSERT(!m_finalized, \"Work distribution already finalized\");\n    if (is_root_manager()) {\n      send_done_to_children_when_free();\n      wait_all_pending_sends();\n    }\n    m_finalized = true;\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      if (is_root_manager()) {\n        _statistics.worker_task_counts = std::vector&lt;size_t&gt;(m_communicator.size(), 0);\n      }\n      m_communicator.gather(m_tasks_executed,\n                            _statistics.worker_task_counts.has_value()\n                                ? &amp;_statistics.worker_task_counts.value()\n                                : nullptr,\n                            m_config.manager_rank);\n    }\n  }\n\n  ~HierarchicalNonBlockingMPIWorkDistributor() {\n    if (!m_finalized) {\n      finalize();\n    }\n    DYNAMPI_ASSERT_EQ(m_results_received_from_child, m_tasks_sent_to_child,\n                      \"All tasks should have been processed by workers before finalizing\");\n    DYNAMPI_ASSERT_EQ(m_results_sent_to_parent, m_tasks_received_from_parent,\n                      \"All results should have been sent to the parent before finalizing\");\n    if (is_leaf_worker())\n      DYNAMPI_ASSERT_EQ(m_results_received_from_child, 0,\n                        \"Leaf workers should not receive results from children\");\n    else if (m_communicator.size() &gt; 1)\n      DYNAMPI_ASSERT_EQ(m_results_received_from_child + m_tasks_executed, m_results_sent_to_parent,\n                        \"Results received from children should match results sent to parent\");\n  }\n\n private:\n  TaskT get_next_task_to_send() {\n    DYNAMPI_ASSERT(is_root_manager() || !is_leaf_worker(),\n                   \"Leaf workers should not send tasks directly\");\n    DYNAMPI_ASSERT(!m_unallocated_task_queue.empty(), \"There should be tasks available to send\");\n    TaskT task;\n    if constexpr (std::is_same_v&lt;decltype(m_unallocated_task_queue), std::deque&lt;TaskT&gt;&gt;) {\n      task = m_unallocated_task_queue.front();\n      m_unallocated_task_queue.pop_front();\n    } else {\n      task = m_unallocated_task_queue.top().second;\n      m_unallocated_task_queue.pop();\n    }\n    return task;\n  }\n\n  int idx_for_worker(int worker_rank) const {\n    DYNAMPI_ASSERT_NE(worker_rank, m_config.manager_rank,\n                      \"Manager rank should not be used as a worker rank\");\n    if (worker_rank &lt; m_config.manager_rank) {\n      return worker_rank;\n    } else {\n      return worker_rank - 1;\n    }\n  }\n\n  int worker_for_idx(int idx) const { return (idx &lt; m_config.manager_rank) ? idx : (idx + 1); }\n\n  // --- Helper: Determine which layer a world rank belongs to ---\n  CommLayer determine_layer_from_world_rank(int world_rank) const {\n    DYNAMPI_ASSERT(m_config.coordinator_per_node);\n    // Check if rank is in local group (and not manager)\n    if (m_local_group) {\n      int local_rank = m_world_group.translate_rank(world_rank, *m_local_group);\n      if (local_rank != MPI_UNDEFINED) {\n        return CommLayer::Local;\n      }\n    }\n    DYNAMPI_ASSERT(m_leader_group.has_value(), \"Leader group should be present\");\n    [[maybe_unused]] int leader_rank = m_world_group.translate_rank(world_rank, *m_leader_group);\n    DYNAMPI_ASSERT_NE(leader_rank, MPI_UNDEFINED, \"Rank should be in leader group\");\n    return CommLayer::Leader;\n  }\n\n  // --- Abstract Send Wrappers ---\n\n  template &lt;typename T&gt;\n  void send_to_parent(const T&amp; data, Tag tag) {\n    auto [target, layer] = get_parent_target();\n    DYNAMPI_ASSERT_NE(target, -1, \"Root cannot send to parent\");\n    send_async(data, target, tag);\n  }\n\n  template &lt;typename T&gt;\n  void send_to_worker(const T&amp; data, int rank, Tag tag, [[maybe_unused]] CommLayer layer) {\n    send_async(data, rank, tag);\n  }\n\n  // Routes to the pool matching T (see AsyncSendPool above), or a genuine\n  // fire-and-forget Isend+Request_free for zero-payload messages. If\n  // TaskT == ResultT (e.g. both uint32_t, as in the benchmarks), a TaskT\n  // send and a ResultT send are the same instantiation and both land in\n  // m_pending_task_sends -- harmless: pool identity is only a buffer-\n  // lifetime bookkeeping detail, not part of what goes out on the wire\n  // (dest/tag/data are whatever the caller passed regardless of which pool\n  // holds the buffer).\n  template &lt;typename T&gt;\n  void send_async(const T&amp; data, int dest, Tag tag) {\n    if constexpr (std::is_same_v&lt;T, std::nullptr_t&gt;) {\n      using mpi_type = MPI_Type&lt;std::nullptr_t&gt;;\n      MPI_Request req;\n      DYNAMPI_MPI_CHECK(MPI_Isend, (nullptr, mpi_type::count(nullptr), mpi_type::value, dest,\n                                    static_cast&lt;int&gt;(tag), m_communicator.get(), &amp;req));\n      DYNAMPI_MPI_CHECK(MPI_Request_free, (&amp;req));\n    } else if constexpr (std::is_same_v&lt;T, std::vector&lt;TaskT&gt;&gt;) {\n      m_pending_task_batch_sends.post(m_communicator, data, dest, static_cast&lt;int&gt;(tag));\n    } else if constexpr (std::is_same_v&lt;T, std::vector&lt;ResultT&gt;&gt;) {\n      m_pending_result_batch_sends.post(m_communicator, data, dest, static_cast&lt;int&gt;(tag));\n    } else if constexpr (std::is_same_v&lt;T, TaskT&gt;) {\n      m_pending_task_sends.post(m_communicator, data, dest, static_cast&lt;int&gt;(tag));\n    } else if constexpr (std::is_same_v&lt;T, ResultT&gt;) {\n      m_pending_result_sends.post(m_communicator, data, dest, static_cast&lt;int&gt;(tag));\n    } else if constexpr (std::is_same_v&lt;T, int&gt;) {\n      m_pending_int_sends.post(m_communicator, data, dest, static_cast&lt;int&gt;(tag));\n    } else {\n      static_assert(!sizeof(T*), \"send_async: unsupported type\");\n    }\n  }\n\n  void send_done_to_children_when_free() {\n    const int direct_children = num_direct_children();\n    // Tracks unique children notified, not requests answered: with\n    // pipeline_depth &gt; 1 (see run_worker()) a single child can have several\n    // outstanding requests queued here at once, all requesting the same\n    // \"no more work\" answer. Counting requests instead of children would let\n    // one over-represented child consume the whole direct_children budget,\n    // leaving others never told to stop -- a real deadlock, not a\n    // theoretical one, once more than one request per child is possible.\n    std::set&lt;int&gt; notified;\n    while (static_cast&lt;int&gt;(notified.size()) &lt; direct_children) {\n      if (m_free_worker_indices.empty()) {\n        receive_from_anyone();\n        continue;\n      }\n      TaskRequest request = m_free_worker_indices.top();\n      m_free_worker_indices.pop();\n\n      if (notified.contains(request.worker_rank)) {\n        // Already told this child; it doesn't need (or expect) a separate\n        // reply per request it happened to have outstanding.\n        continue;\n      }\n      send_to_worker(nullptr, request.worker_rank, Tag::DONE, request.source_layer);\n      notified.insert(request.worker_rank);\n    }\n  }\n\n  using result_mpi_type = MPI_Type&lt;ResultT&gt;;\n  using task_mpi_type = MPI_Type&lt;TaskT&gt;;\n\n  void receive_result_from(MPI_Status status, [[maybe_unused]] MPICommunicator&amp; source_comm,\n                           CommLayer layer) {\n    m_results.push_back(ResultT{});\n    if (result_mpi_type::resize_required) {\n      DYNAMPI_UNIMPLEMENTED(  // LCOV_EXCL_LINE\n          \"Dynamic resizing of results is not supported in hierarchical distribution\");\n    }\n    // With groups, always use global communicator and determine layer from source rank\n    int world_source = status.MPI_SOURCE;\n    if (m_config.coordinator_per_node) {\n      layer = determine_layer_from_world_rank(world_source);\n    }\n    m_communicator.recv(m_results.back(), world_source, Tag::RESULT);\n    m_results_received_from_child++;\n    m_free_worker_indices.push(TaskRequest{.worker_rank = world_source, .source_layer = layer});\n  }\n\n  void receive_result_batch_from(MPI_Status status, [[maybe_unused]] MPICommunicator&amp; source_comm,\n                                 [[maybe_unused]] CommLayer layer) {\n    using message_type = MPI_Type&lt;std::vector&lt;ResultT&gt;&gt;;\n    int count;\n    DYNAMPI_MPI_CHECK(MPI_Get_count, (&amp;status, message_type::value, &amp;count));\n    std::vector&lt;ResultT&gt; results;\n    message_type::resize(results, count);\n    int world_source = status.MPI_SOURCE;\n    m_communicator.recv(results, world_source, Tag::RESULT_BATCH);\n    // Results and next-batch requests are independent messages (see the\n    // double-buffering refill in run_worker()), so receiving results here\n    // has no side effect on task allocation for this child.\n    std::copy(results.begin(), results.end(), std::back_inserter(m_results));\n    m_results_received_from_child += results.size();\n  }\n\n  void receive_execute_return_task_from(MPI_Status status,\n                                        [[maybe_unused]] MPICommunicator&amp; source_comm,\n                                        [[maybe_unused]] CommLayer layer) {\n    int count;\n    DYNAMPI_MPI_CHECK(MPI_Get_count, (&amp;status, task_mpi_type::value, &amp;count));\n    TaskT message;\n    task_mpi_type::resize(message, count);\n    // With groups, always use global communicator\n    int world_source = status.MPI_SOURCE;\n    m_communicator.recv(message, world_source, Tag::TASK);\n    m_tasks_received_from_parent++;\n    ResultT result = m_worker_function(message);\n    m_tasks_executed++;\n    // Reply on the global communicator\n    send_async(result, world_source, Tag::RESULT);\n    m_results_sent_to_parent++;\n  }\n\n  void receive_task_batch_from(MPI_Status status, [[maybe_unused]] MPICommunicator&amp; source_comm,\n                               [[maybe_unused]] CommLayer layer) {\n    if constexpr (prioritize_tasks) {\n      DYNAMPI_UNIMPLEMENTED(\"Prioritized hierarchical distribution\");\n    } else {\n      using message_type = MPI_Type&lt;std::vector&lt;TaskT&gt;&gt;;\n      int count;\n      DYNAMPI_MPI_CHECK(MPI_Get_count, (&amp;status, message_type::value, &amp;count));\n      std::vector&lt;TaskT&gt; tasks;\n      message_type::resize(tasks, count);\n      // With groups, always use global communicator\n      int world_source = status.MPI_SOURCE;\n      m_communicator.recv(tasks, world_source, Tag::TASK_BATCH);\n      m_tasks_received_from_parent += tasks.size();\n      // This reply fulfills one of our outstanding pipelined requests (see\n      // run_worker()'s top_up_pipeline()); only intermediate coordinators\n      // (never leaf workers, which use the unbatched TASK/RESULT protocol)\n      // send REQUEST_BATCH, so only they receive TASK_BATCH replies here.\n      if (m_outstanding_requests &gt; 0) --m_outstanding_requests;\n      // While a round is active, this is the reply to a request for a\n      // *future* round: quarantine it so the current round's distribution\n      // loop can't overshoot into it (see run_worker()).\n      auto&amp; target = m_round_active ? m_prefetched_tasks : m_unallocated_task_queue;\n      for (const auto&amp; task : tasks) {\n        target.push_back(task);\n      }\n    }\n  }\n\n  void receive_request_from(MPI_Status status, [[maybe_unused]] MPICommunicator&amp; source_comm,\n                            CommLayer layer) {\n    // With groups, always use global communicator and determine layer from source rank\n    int world_source = status.MPI_SOURCE;\n    if (m_config.coordinator_per_node) {\n      layer = determine_layer_from_world_rank(world_source);\n    }\n    m_communicator.recv_empty_message(world_source, Tag::REQUEST);\n    m_free_worker_indices.push(TaskRequest{.worker_rank = world_source, .source_layer = layer});\n  }\n\n  void receive_request_batch_from(MPI_Status status, [[maybe_unused]] MPICommunicator&amp; source_comm,\n                                  CommLayer layer) {\n    // With groups, always use global communicator and determine layer from source rank\n    int world_source = status.MPI_SOURCE;\n    if (m_config.coordinator_per_node) {\n      layer = determine_layer_from_world_rank(world_source);\n    }\n    int request_count;\n    m_communicator.recv(request_count, world_source, Tag::REQUEST_BATCH);\n    DYNAMPI_ASSERT_GT(request_count, 0, \"Invalid request count\");\n    DYNAMPI_ASSERT_LE(request_count, kMaxTasksRequested, \"Request count exceeds maximum allowed\");\n    m_free_worker_indices.push(TaskRequest{\n        .worker_rank = world_source, .source_layer = layer, .num_tasks_requested = request_count});\n  }\n\n  void receive_done_from(MPI_Status status, [[maybe_unused]] MPICommunicator&amp; source_comm,\n                         [[maybe_unused]] CommLayer layer) {\n    // With groups, always use global communicator\n    int world_source = status.MPI_SOURCE;\n    m_communicator.recv_empty_message(world_source, Tag::DONE);\n    m_done = true;\n  }\n\n  void receive_from_anyone() {\n    DYNAMPI_ASSERT_GT(m_communicator.size(), 1,\n                      \"There should be at least one worker to receive results from\");\n\n    MPI_Status status{};\n    CommLayer layer = CommLayer::Global;\n\n    // All messages now route through the global communicator regardless of topology,\n    // so a blocking probe is sufficient. The CommLayer is determined from the source\n    // rank in each receive method via determine_layer_from_world_rank.\n    status = m_communicator.probe();\n\n    // Assert that the tag is a valid Tag enum value before casting\n    DYNAMPI_ASSERT(status.MPI_TAG &gt;= static_cast&lt;int&gt;(Tag::TASK) &amp;&amp;\n                       status.MPI_TAG &lt;= static_cast&lt;int&gt;(Tag::REQUEST_BATCH),\n                   \"Received invalid MPI tag: \" + std::to_string(status.MPI_TAG));\n    Tag tag = static_cast&lt;Tag&gt;(status.MPI_TAG);\n    // Note: receive methods now use global communicator and determine layer from source rank\n    switch (tag) {\n      case Tag::TASK:\n        return receive_execute_return_task_from(status, m_communicator, layer);\n      case Tag::TASK_BATCH:\n        return receive_task_batch_from(status, m_communicator, layer);\n      case Tag::RESULT:\n        return receive_result_from(status, m_communicator, layer);\n      case Tag::RESULT_BATCH:\n        return receive_result_batch_from(status, m_communicator, layer);\n      case Tag::REQUEST:\n        return receive_request_from(status, m_communicator, layer);\n      case Tag::REQUEST_BATCH:\n        return receive_request_batch_from(status, m_communicator, layer);\n      case Tag::DONE:\n        return receive_done_from(status, m_communicator, layer);\n    }\n  }\n};\n\n};  // namespace dynampi\n</code></pre>"},{"location":"dynampi/lockfree__distributor_8hpp/","title":"File lockfree_distributor.hpp","text":"<p>FileList &gt; dynampi &gt; impl &gt; lockfree_distributor.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;algorithm&gt;</code></li> <li><code>#include &lt;cassert&gt;</code></li> <li><code>#include &lt;chrono&gt;</code></li> <li><code>#include &lt;cstddef&gt;</code></li> <li><code>#include &lt;cstdint&gt;</code></li> <li><code>#include &lt;cstring&gt;</code></li> <li><code>#include &lt;functional&gt;</code></li> <li><code>#include &lt;limits&gt;</code></li> <li><code>#include &lt;map&gt;</code></li> <li><code>#include &lt;optional&gt;</code></li> <li><code>#include &lt;thread&gt;</code></li> <li><code>#include &lt;type_traits&gt;</code></li> <li><code>#include &lt;utility&gt;</code></li> <li><code>#include &lt;vector&gt;</code></li> <li><code>#include \"../mpi/mpi_communicator.hpp\"</code></li> <li><code>#include \"../mpi/mpi_types.hpp\"</code></li> <li><code>#include \"dynampi/impl/base_distributor.hpp\"</code></li> <li><code>#include \"dynampi/mpi/mpi_error.hpp\"</code></li> <li><code>#include \"dynampi/utilities/timer.hpp\"</code></li> </ul>"},{"location":"dynampi/lockfree__distributor_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi namespace detail"},{"location":"dynampi/lockfree__distributor_8hpp/#classes","title":"Classes","text":"Type Name class LockFreeMPIWorkDistributor &lt;typename TaskT, typename ResultT, Options&gt; struct Config struct RunConfig struct Statistics class MinimalLockFreeMPIWorkDistributor &lt;typename ResultT&gt; struct Config <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/lockfree_distributor.hpp</code></p>"},{"location":"dynampi/lockfree__distributor_8hpp_source/","title":"File lockfree_distributor.hpp","text":"<p>File List &gt; dynampi &gt; impl &gt; lockfree_distributor.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2026 Ryan Stocks\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;algorithm&gt;\n#include &lt;cassert&gt;\n#include &lt;chrono&gt;\n#include &lt;cstddef&gt;\n#include &lt;cstdint&gt;\n#include &lt;cstring&gt;\n#include &lt;functional&gt;\n#include &lt;limits&gt;\n#include &lt;map&gt;\n#include &lt;optional&gt;\n#include &lt;thread&gt;\n#include &lt;type_traits&gt;\n#include &lt;utility&gt;\n#include &lt;vector&gt;\n\n#include \"../mpi/mpi_communicator.hpp\"\n#include \"../mpi/mpi_types.hpp\"\n#include \"dynampi/impl/base_distributor.hpp\"\n#include \"dynampi/mpi/mpi_error.hpp\"\n#include \"dynampi/utilities/timer.hpp\"\n\nnamespace dynampi {\n\nnamespace detail {\n\n// Byte size of a single element of the MPI datatype backing T (e.g. 4 for int,\n// 4 for the element type of std::vector&lt;int&gt;).\ntemplate &lt;typename T&gt;\ninline int mpi_type_size_bytes() {\n  int size = 0;\n  DYNAMPI_MPI_CHECK(MPI_Type_size, (MPI_Type&lt;T&gt;::value, &amp;size));\n  return size;\n}\n\ninline constexpr size_t round_up_8(size_t bytes) { return (bytes + 7) &amp; ~static_cast&lt;size_t&gt;(7); }\n\ninline void write_bytes(std::byte* buffer, [[maybe_unused]] size_t buffer_size, size_t offset,\n                        const void* src, size_t nbytes) {\n  if (nbytes == 0) return;\n  assert(offset &lt;= buffer_size &amp;&amp; nbytes &lt;= buffer_size - offset);\n  std::memcpy(buffer + offset, src, nbytes);\n}\n\ninline int64_t read_i64(const std::byte* buffer, [[maybe_unused]] size_t buffer_size,\n                        size_t offset) {\n  assert(offset + sizeof(int64_t) &lt;= buffer_size);\n  int64_t value{};\n  std::memcpy(&amp;value, buffer + offset, sizeof(int64_t));\n  return value;\n}\n\ninline void write_i64(std::byte* buffer, size_t buffer_size, size_t offset, int64_t value) {\n  write_bytes(buffer, buffer_size, offset, &amp;value, sizeof(int64_t));\n}\n\ntemplate &lt;typename T&gt;\ninline void read_result_bytes(const std::byte* buffer, [[maybe_unused]] size_t buffer_size,\n                              size_t offset, T&amp; value, size_t data_bytes) {\n  if (data_bytes == 0) return;\n  assert(offset &lt;= buffer_size &amp;&amp; data_bytes &lt;= buffer_size - offset);\n  if constexpr (MPI_Type&lt;T&gt;::resize_required) {\n    // cppcheck-suppress invalidPointerCast\n    std::memcpy(MPI_Type&lt;T&gt;::ptr(value), buffer + offset, data_bytes);\n  } else {\n    assert(data_bytes == sizeof(T));\n    std::memcpy(&amp;value, buffer + offset, sizeof(T));\n  }\n}\n\n// Passive-target RMA on MS-MPI needs explicit flush progress while spinning.\ninline void rma_wait_idle(MPI_Win window) {\n#if defined(_WIN32)\n  if (window != MPI_WIN_NULL) {\n    DYNAMPI_MPI_CHECK(MPI_Win_flush_all, (window));\n  }\n  std::this_thread::yield();\n#else\n  (void)window;\n  std::this_thread::sleep_for(std::chrono::microseconds(50));\n#endif\n}\n\n}  // namespace detail\n\n// ---------------------------------------------------------------------------\n// MinimalLockFreeMPIWorkDistributor\n//\n// The simplest possible lock-free distributor: a parallel-for over the index\n// range [0, n_tasks). The task *is* its global index, and every rank pulls the\n// next index by atomically incrementing a single shared counter in the\n// manager's RMA window. Results are gathered once at the end.\n//\n// This is genuinely lock-free (no manager bottleneck, one shared atomic) and\n// deliberately tiny. Use it when the work is an embarrassingly parallel loop\n// and the task payload is just the loop index. For arbitrary task payloads,\n// priorities, incremental result collection or statistics, use\n// LockFreeMPIWorkDistributor (or one of the message-based distributors).\n//\n// Usage (collective: every rank must call run() with the same n_tasks):\n//   MinimalLockFreeMPIWorkDistributor&lt;double&gt; dist([](size_t i){ return f(i); });\n//   std::vector&lt;double&gt; results = dist.run(n);  // populated on the manager only\n// ---------------------------------------------------------------------------\ntemplate &lt;typename ResultT&gt;\nclass MinimalLockFreeMPIWorkDistributor {\n public:\n  struct Config {\n    MPI_Comm comm = MPI_COMM_WORLD;\n    int manager_rank = 0;\n  };\n\n  explicit MinimalLockFreeMPIWorkDistributor(std::function&lt;ResultT(size_t)&gt; worker_function,\n                                             Config config = {})\n      : m_config(config),\n        m_comm(config.comm, MPICommunicator&lt;&gt;::Duplicate),\n        m_worker_function(std::move(worker_function)) {\n    if (m_comm.size() == 1) {\n      // Manager-only: no workers to share the claim counter with.\n      return;\n    }\n\n    void* base = is_root_manager() ? static_cast&lt;void*&gt;(&amp;m_counter) : m_worker_window;\n    MPI_Aint size = static_cast&lt;MPI_Aint&gt;(sizeof(int64_t));\n    DYNAMPI_MPI_CHECK(MPI_Win_create, (base, size, 1, MPI_INFO_NULL, m_comm.get(), &amp;m_window));\n    DYNAMPI_MPI_CHECK(MPI_Win_lock_all, (MPI_MODE_NOCHECK, m_window));\n  }\n\n  ~MinimalLockFreeMPIWorkDistributor() {\n    if (m_window != MPI_WIN_NULL) {\n      DYNAMPI_MPI_CHECK(MPI_Win_unlock_all, (m_window));\n      DYNAMPI_MPI_CHECK(MPI_Win_free, (&amp;m_window));\n      m_window = MPI_WIN_NULL;\n    }\n  }\n\n  bool is_root_manager() const { return m_comm.rank() == m_config.manager_rank; }\n\n  // Collective. Every rank must call with the same n_tasks. Returns the results\n  // ordered by task index on the manager, and an empty vector on workers.\n  [[nodiscard]] std::vector&lt;ResultT&gt; run(size_t n_tasks) {\n    unsigned long long n = n_tasks;\n    DYNAMPI_MPI_CHECK(MPI_Bcast,\n                      (&amp;n, 1, MPI_UNSIGNED_LONG_LONG, m_config.manager_rank, m_comm.get()));\n\n    if (m_comm.size() == 1) {\n      assert(is_root_manager());\n      std::vector&lt;ResultT&gt; results;\n      results.reserve(static_cast&lt;size_t&gt;(n));\n      for (unsigned long long i = 0; i &lt; n; ++i) {\n        results.push_back(m_worker_function(static_cast&lt;size_t&gt;(i)));\n      }\n      return results;\n    }\n\n    if (is_root_manager()) set_counter(0);\n    DYNAMPI_MPI_CHECK(MPI_Barrier, (m_comm.get()));  // reset visible + synchronized start\n\n    std::vector&lt;std::pair&lt;int64_t, ResultT&gt;&gt; local;\n    while (true) {\n      int64_t index = fetch_add(1);\n      if (index &gt;= static_cast&lt;int64_t&gt;(n)) break;\n      local.emplace_back(index, m_worker_function(static_cast&lt;size_t&gt;(index)));\n    }\n\n    return gather_sorted(local);\n  }\n\n private:\n  Config m_config;\n  MPICommunicator&lt;&gt; m_comm;\n  std::function&lt;ResultT(size_t)&gt; m_worker_function;\n  MPI_Win m_window = MPI_WIN_NULL;\n  int64_t m_counter = 0;  // window-exposed claim counter (manager only)\n  alignas(int64_t) std::byte m_worker_window[sizeof(int64_t)]{};\n\n  void set_counter(int64_t value) {\n    int64_t in = value, out;\n    DYNAMPI_MPI_CHECK(MPI_Fetch_and_op,\n                      (&amp;in, &amp;out, MPI_INT64_T, m_config.manager_rank, 0, MPI_REPLACE, m_window));\n    DYNAMPI_MPI_CHECK(MPI_Win_flush, (m_config.manager_rank, m_window));\n  }\n\n  int64_t fetch_add(int64_t increment) {\n    int64_t in = increment, out;\n    DYNAMPI_MPI_CHECK(MPI_Fetch_and_op,\n                      (&amp;in, &amp;out, MPI_INT64_T, m_config.manager_rank, 0, MPI_SUM, m_window));\n    DYNAMPI_MPI_CHECK(MPI_Win_flush, (m_config.manager_rank, m_window));\n    return out;\n  }\n\n  std::vector&lt;ResultT&gt; gather_sorted(std::vector&lt;std::pair&lt;int64_t, ResultT&gt;&gt;&amp; local) {\n    const int elem = detail::mpi_type_size_bytes&lt;ResultT&gt;();\n    const bool manager = is_root_manager();\n    const int size = m_comm.size();\n\n    // Pack: per result [int64 index][int64 count][count * elem bytes].\n    std::vector&lt;std::byte&gt; send_buf;\n    for (auto&amp; [index, result] : local) {\n      const int count = MPI_Type&lt;ResultT&gt;::count(result);\n      assert(count &gt;= 0);\n      const size_t data_bytes =\n          count &gt; 0 ? static_cast&lt;size_t&gt;(count) * static_cast&lt;size_t&gt;(elem) : size_t{0};\n      const size_t offset = send_buf.size();\n      send_buf.resize(offset + 16 + data_bytes);\n      detail::write_i64(send_buf.data(), send_buf.size(), offset, index);\n      detail::write_i64(send_buf.data(), send_buf.size(), offset + 8, count);\n      if (data_bytes &gt; 0) {\n        detail::write_bytes(send_buf.data(), send_buf.size(), offset + 16,\n                            MPI_Type&lt;ResultT&gt;::ptr(result), data_bytes);\n      }\n    }\n\n    const int send_count = static_cast&lt;int&gt;(send_buf.size());\n    std::vector&lt;int&gt; byte_counts(manager ? static_cast&lt;size_t&gt;(size) : 0);\n    DYNAMPI_MPI_CHECK(MPI_Gather, (&amp;send_count, 1, MPI_INT, manager ? byte_counts.data() : nullptr,\n                                   1, MPI_INT, m_config.manager_rank, m_comm.get()));\n\n    std::vector&lt;int&gt; displacements;\n    std::vector&lt;std::byte&gt; recv_buf;\n    int total_bytes = 0;\n    if (manager) {\n      displacements.resize(static_cast&lt;size_t&gt;(size));\n      for (int r = 0; r &lt; size; ++r) {\n        displacements[static_cast&lt;size_t&gt;(r)] = total_bytes;\n        total_bytes += byte_counts[static_cast&lt;size_t&gt;(r)];\n      }\n      recv_buf.resize(static_cast&lt;size_t&gt;(total_bytes));\n    }\n\n    DYNAMPI_MPI_CHECK(\n        MPI_Gatherv,\n        (send_buf.data(), send_count, MPI_BYTE, manager ? recv_buf.data() : nullptr,\n         manager ? byte_counts.data() : nullptr, manager ? displacements.data() : nullptr, MPI_BYTE,\n         m_config.manager_rank, m_comm.get()));\n\n    std::vector&lt;ResultT&gt; output;\n    if (!manager) return output;\n\n    std::vector&lt;std::pair&lt;int64_t, ResultT&gt;&gt; all;\n    size_t pos = 0;\n    while (pos &lt; static_cast&lt;size_t&gt;(total_bytes)) {\n      assert(pos + 16 &lt;= static_cast&lt;size_t&gt;(total_bytes));\n      const int64_t index = detail::read_i64(recv_buf.data(), recv_buf.size(), pos);\n      const int64_t count = detail::read_i64(recv_buf.data(), recv_buf.size(), pos + 8);\n      pos += 16;\n      ResultT result{};\n      if constexpr (MPI_Type&lt;ResultT&gt;::resize_required)\n        MPI_Type&lt;ResultT&gt;::resize(result, static_cast&lt;int&gt;(count));\n      assert(count &gt;= 0);\n      const size_t data_bytes =\n          count &gt; 0 ? static_cast&lt;size_t&gt;(count) * static_cast&lt;size_t&gt;(elem) : size_t{0};\n      detail::read_result_bytes(recv_buf.data(), recv_buf.size(), pos, result, data_bytes);\n      pos += data_bytes;\n      all.emplace_back(index, std::move(result));\n    }\n    std::sort(all.begin(), all.end(),\n              [](const auto&amp; a, const auto&amp; b) { return a.first &lt; b.first; });\n    output.reserve(all.size());\n    for (auto&amp; [index, result] : all) output.push_back(std::move(result));\n    return output;\n  }\n};\n\n// ---------------------------------------------------------------------------\n// LockFreeMPIWorkDistributor\n//\n// Task dispatch uses passive one-sided RMA on the manager's window (no\n// MPI_Send/MPI_Recv on the hot path). Workers claim tasks via compare-and-swap,\n// read task payloads from that window, execute, and buffer results locally.\n// The manager collects via MPI_Gatherv rounds (same packing as\n// MinimalLockFreeMPIWorkDistributor), triggered when workers report progress.\n//\n// Microsoft MPI always uses MPI_WIN_SEPARATE: a rank cannot observe remote RMA\n// updates to its own window. Task dispatch stays on the manager window; results\n// never use RMA Put/Get on the manager's own memory.\n// ---------------------------------------------------------------------------\ntemplate &lt;typename TaskT, typename ResultT, typename... Options&gt;\nclass LockFreeMPIWorkDistributor {\n public:\n  struct Config {\n    MPI_Comm comm = MPI_COMM_WORLD;\n    int manager_rank = 0;\n    bool auto_run_workers = true;\n    int max_tasks = 8192;        // capacity of the task/result tables (lifetime total)\n    int max_task_count = 256;    // max elements per task (only for resizable TaskT)\n    int max_result_count = 256;  // max elements per result (only for resizable ResultT)\n    int claim_batch_size = 8;    // tasks claimed per RMA round trip (see run_worker())\n  };\n\n  struct RunConfig {\n    size_t target_num_tasks = std::numeric_limits&lt;size_t&gt;::max();\n    bool allow_more_than_target_tasks = true;\n    std::optional&lt;double&gt; max_seconds = std::nullopt;\n  };\n\n  static const bool ordered = true;\n\n private:\n  static constexpr bool prioritize_tasks = get_option_value&lt;prioritize_tasks_t, Options...&gt;();\n  static constexpr StatisticsMode statistics_mode =\n      get_option_value&lt;track_statistics_t, Options...&gt;();\n  using Comm = dynampi::MPICommunicator&lt;track_statistics&lt;statistics_mode&gt;&gt;;\n\n  // Manager window: [head][total][finished][gather_seq] then task slots.\n  static constexpr MPI_Aint HEAD_OFF = 0;\n  static constexpr MPI_Aint TOTAL_OFF = 8;\n  static constexpr MPI_Aint FINISHED_OFF = 16;\n  static constexpr MPI_Aint GATHER_SEQ_OFF = 24;\n  static constexpr size_t CONTROL_BYTES = 32;\n\n  // Manager per-task slot: [int64 count][data].\n  static constexpr size_t T_COUNT = 0;\n  static constexpr size_t T_DATA = 8;\n\n public:\n  struct Statistics {\n    CommStatistics comm_statistics;\n    std::vector&lt;size_t&gt; worker_task_counts;\n  };\n  using StatisticsT =\n      std::conditional_t&lt;statistics_mode == StatisticsMode::Detailed, Statistics, std::monostate&gt;;\n\n  explicit LockFreeMPIWorkDistributor(std::function&lt;ResultT(TaskT)&gt; worker_function,\n                                      Config config = {})\n      : m_config(config),\n        m_comm(config.comm, Comm::Duplicate),\n        m_worker_function(std::move(worker_function)),\n        m_statistics{make_statistics()} {\n    initialize_window();\n\n    if constexpr (statistics_mode &gt;= StatisticsMode::Aggregated) {\n      if (is_root_manager())\n        m_statistics.worker_task_counts.assign(static_cast&lt;size_t&gt;(m_comm.size()), 0);\n    }\n\n    if (m_config.auto_run_workers &amp;&amp; !is_root_manager()) run_worker();\n  }\n\n  ~LockFreeMPIWorkDistributor() {\n    if (!m_finalized) finalize();\n    if (m_window != MPI_WIN_NULL) {\n      DYNAMPI_MPI_CHECK(MPI_Win_unlock_all, (m_window));\n      MPI_Barrier(m_comm.get());\n      DYNAMPI_MPI_CHECK(MPI_Win_free, (&amp;m_window));\n      m_window = MPI_WIN_NULL;\n    }\n  }\n\n  // --- Public API ---\n\n  [[nodiscard]] std::vector&lt;ResultT&gt; run_tasks(RunConfig config = {}) {\n    assert(is_root_manager());\n    Timer timer;\n\n    if (num_workers() == 0) {\n      // No workers: the manager runs everything itself.\n      while (m_collected_count &lt; static_cast&lt;size_t&gt;(m_total_tasks)) {\n        if (available() &gt;= config.target_num_tasks) break;\n        if (config.max_seconds &amp;&amp; timer.elapsed().count() &gt;= *config.max_seconds) break;\n        run_one_task_locally();\n      }\n    } else {\n      while (true) {\n        if (available() &gt;= config.target_num_tasks) break;\n        if (config.max_seconds &amp;&amp; timer.elapsed().count() &gt;= *config.max_seconds) break;\n        if (m_collected_count &gt;= static_cast&lt;size_t&gt;(m_total_tasks)) break;\n\n        try_gather_results();\n      }\n    }\n\n    size_t limit = config.allow_more_than_target_tasks ? std::numeric_limits&lt;size_t&gt;::max()\n                                                       : config.target_num_tasks;\n    return drain_results(limit);\n  }\n\n  [[nodiscard]] std::vector&lt;ResultT&gt; finish_remaining_tasks() { return run_tasks({}); }\n\n  // Requests exactly one gather round and returns whatever results have been\n  // staged by workers so far -- possibly none, possibly all outstanding\n  // tasks, depending on how far workers have gotten. Unlike run_tasks() /\n  // finish_remaining_tasks(), this does not loop retrying until every\n  // outstanding task is collected: each call to those pays for a\n  // MPI_Barrier + MPI_Gather/MPI_Gatherv across the whole communicator on\n  // *every* internal retry (see try_gather_results()), which at high rank\n  // counts can dominate over actual task execution time for fine-grained\n  // tasks. Call this instead when the caller wants to control synchronization\n  // frequency directly -- e.g. insert a large batch, let workers churn\n  // uninterrupted for a while, then take a single snapshot with one call\n  // here rather than one call per outstanding task.\n  [[nodiscard]] std::vector&lt;ResultT&gt; gather_once() {\n    assert(is_root_manager());\n    if (num_workers() == 0) {\n      while (m_collected_count &lt; static_cast&lt;size_t&gt;(m_total_tasks)) run_one_task_locally();\n    } else {\n      request_gather();\n    }\n    return drain_results(std::numeric_limits&lt;size_t&gt;::max());\n  }\n\n  void finalize() {\n    assert(!m_finalized);\n    if (is_root_manager()) {\n      if (num_workers() == 0) {\n        while (m_collected_count &lt; static_cast&lt;size_t&gt;(m_total_tasks)) run_one_task_locally();\n      } else {\n        while (m_collected_count &lt; static_cast&lt;size_t&gt;(m_total_tasks)) {\n          try_gather_results();\n        }\n        atomic_set(FINISHED_OFF, 1);  // tell workers to stop\n        detail::rma_wait_idle(m_window);\n      }\n    }\n    m_finalized = true;\n  }\n\n  bool is_root_manager() const { return m_comm.rank() == m_config.manager_rank; }\n\n  size_t remaining_tasks_count() const {\n    assert(is_root_manager());\n    return static_cast&lt;size_t&gt;(m_total_tasks) - m_returned_count;\n  }\n\n  const StatisticsT&amp; get_statistics() const\n    requires(statistics_mode != StatisticsMode::None)\n  {\n    assert(is_root_manager());\n    return m_statistics;\n  }\n\n  void insert_task(TaskT task)\n    requires(!prioritize_tasks)\n  {\n    assert(is_root_manager());\n    publish_task(task);\n  }\n\n  // LockFree does not support prioritisation; the priority is ignored.\n  void insert_task(const TaskT&amp; task, double)\n    requires(prioritize_tasks)\n  {\n    assert(is_root_manager());\n    publish_task(task);\n  }\n\n  void insert_tasks(const std::vector&lt;TaskT&gt;&amp; tasks)\n    requires(!prioritize_tasks)\n  {\n    assert(is_root_manager());\n    for (const auto&amp; task : tasks) publish_task(task);\n  }\n\n  void run_worker() {\n    assert(!is_root_manager());\n\n    // Fetch-and-add claiming instead of compare-and-swap: FAA can't lose a\n    // race (there's nothing to compare against), so under contention it\n    // wastes none of the throughput CAS gives up to failed attempts --\n    // rma_atomic_microbench measured CAS success rates as low as 3%\n    // (32-way contention) to under 1% (128-way), while FAA always succeeds.\n    //\n    // Unlike compare_and_swap, FAA can't be told \"only add up to what's\n    // published\" -- it always grants exactly what you ask for, with no way\n    // to verify that against TOTAL_OFF atomically. The naive fix (always\n    // request claim_batch_size, unconditionally) was tried and made things\n    // *worse*, not better: every worker instantly races HEAD far ahead of\n    // TOTAL regardless of real publish rate, so nearly every claim lands in\n    // the slow wait-and-poll path instead of the fast immediate-process\n    // path -- the CAS version's habit of bounding its request to\n    // min(claim_batch_size, total-head) *before* claiming was quietly doing\n    // most of the work, not the compare-and-swap itself. So this still\n    // bounds the requested size the same way CAS did (cached_head/\n    // cached_total, refreshed lazily) -- the difference is only that FAA\n    // can't *verify* cached_head is still accurate the way CAS's compare\n    // does, so a claim can occasionally still straddle the published\n    // boundary if another worker advanced HEAD since our last observation\n    // of it. That's rare and small (bounded by how stale cached_head is),\n    // not the systematic full-batch overshoot of the unconditional version.\n    //\n    // pending_start/pending_end track the unresolved remainder of such a\n    // straddling claim across iterations. The one subtlety that isn't\n    // optional: a worker with a pending tail MUST still process whatever\n    // *prefix* of its claim is already valid immediately, rather than\n    // blocking until the whole range resolves. finalize() can't set\n    // FINISHED_OFF until it has collected every result -- including this\n    // worker's already-ready prefix -- so blocking on the unready remainder\n    // before staging that prefix would deadlock: the manager waiting on\n    // results this worker refuses to compute until FINISHED, which the\n    // manager won't set until it has them. Processing the ready prefix on\n    // the spot (both in the fresh-claim branch and the pending-tail branch\n    // below) avoids that entirely -- only the genuinely-not-yet-published\n    // remainder ever waits.\n    int64_t cached_head = 0;\n    int64_t cached_total = 0;\n    int64_t pending_start = -1;\n    int64_t pending_end = -1;\n    bool finished_seen = false;\n\n    auto process_range = [&amp;](int64_t start, int64_t count) {\n      std::vector&lt;TaskT&gt; tasks = read_task_batch(start, count);\n      for (int64_t i = 0; i &lt; count; ++i) {\n        ResultT result = m_worker_function(std::move(tasks[static_cast&lt;size_t&gt;(i)]));\n        store_result(start + i, std::move(result));\n      }\n    };\n\n    while (true) {\n      maybe_participate_in_gather();\n      bool made_progress = false;\n\n      if (pending_start != -1) {\n        const int64_t total = atomic_read(TOTAL_OFF);\n        const int64_t ready = std::min(pending_end, total);\n        if (ready &gt; pending_start) {\n          process_range(pending_start, ready - pending_start);\n          pending_start = ready;\n          made_progress = true;\n        }\n        if (pending_start &gt;= pending_end) {\n          pending_start = -1;  // fully resolved\n        } else if (finished_seen) {\n          pending_start = -1;  // total is final and short of pending_end: that remainder never existed\n        }\n      } else {\n        // Deliberately NOT gated on !finished_seen: finished_seen only means\n        // \"TOTAL_OFF won't grow anymore\", not \"stop claiming what's already\n        // known to be available\". Gating this whole branch on !finished_seen\n        // meant that once finished_seen went true, nothing could ever claim\n        // again -- a worker sitting on a real, never-claimed gap between\n        // cached_head and a just-learned final_total (see the exit check\n        // below, which discovers and records that gap into cached_total)\n        // would spin forever re-discovering it with no way to act on it.\n        // Confirmed via gdb backtraces on a caught hang in the sibling\n        // AsyncPutLockFreeMPIWorkDistributor, which shares this exact\n        // claim-loop structure: 15/16 ranks parked in the destructor's\n        // teardown barrier while the 16th sat in an idle-wait loop forever.\n        if (cached_head &gt;= cached_total &amp;&amp; !finished_seen) cached_total = atomic_read(TOTAL_OFF);\n\n        if (cached_head &lt; cached_total) {\n          const int64_t claim =\n              std::min&lt;int64_t&gt;(m_config.claim_batch_size, cached_total - cached_head);\n          const int64_t start = fetch_add(HEAD_OFF, claim);\n          const int64_t end = start + claim;\n          cached_head = end;  // best local estimate; may be stale vs. concurrent claimants, see above\n\n          // Usually ready == end (cached_total was accurate); only re-reads\n          // TOTAL_OFF when start turned out higher than expected (another\n          // worker claimed since our last observation), which is the rare\n          // case this whole scheme is built to tolerate rather than prevent.\n          const int64_t total = (start + claim &lt;= cached_total) ? cached_total : atomic_read(TOTAL_OFF);\n          const int64_t ready = std::min(end, total);\n          if (ready &gt; start) {\n            process_range(start, ready - start);\n            made_progress = true;\n          }\n          if (ready &lt; end) {\n            pending_start = ready;\n            pending_end = end;\n          }\n        }\n      }\n\n      if (!finished_seen &amp;&amp; atomic_read(FINISHED_OFF) != 0) finished_seen = true;\n\n      if (pending_start == -1 &amp;&amp; finished_seen) {\n        // cached_total can be stale relative to when TOTAL_OFF actually\n        // stopped growing (publishing finishes well before FINISHED_OFF\n        // becomes visible here), so a fresh read is needed before trusting\n        // \"nothing left\" -- otherwise this could exit while real,\n        // never-claimed work still exists.\n        const int64_t final_total = atomic_read(TOTAL_OFF);\n        if (cached_head &gt;= final_total) break;\n        cached_total = final_total;  // real unclaimed work remains; go claim it next iteration\n      }\n      if (!made_progress) {\n        maybe_participate_in_gather();\n        detail::rma_wait_idle(m_window);\n      }\n    }\n  }\n\n private:\n  Config m_config;\n  Comm m_comm;\n  std::function&lt;ResultT(TaskT)&gt; m_worker_function;\n\n  MPI_Win m_window = MPI_WIN_NULL;\n  std::vector&lt;std::byte&gt; m_window_buffer;                         // manager: control + task table\n  alignas(int64_t) std::byte m_worker_window[sizeof(int64_t)]{};  // workers: Win_create placeholder\n  bool m_finalized = false;\n\n  // Layout, computed once in initialize_window().\n  size_t m_task_elem = 0;\n  size_t m_result_elem = 0;\n  size_t m_max_task_count = 1;\n  size_t m_max_result_count = 1;\n  size_t m_task_slot_stride = 0;\n  size_t m_task_base = 0;\n\n  int64_t m_total_tasks = 0;\n  int64_t m_gather_seq = 0;\n  int64_t m_seen_gather_seq = 0;\n  size_t m_collected_count = 0;\n  size_t m_returned_count = 0;\n  std::vector&lt;ResultT&gt; m_results;\n  std::map&lt;int64_t, ResultT&gt; m_staging;\n  std::vector&lt;TaskT&gt; m_task_store;\n  std::vector&lt;std::pair&lt;int64_t, ResultT&gt;&gt; m_local_results;  // workers only\n\n  StatisticsT m_statistics;\n\n  // --- Setup ---\n\n  int num_workers() const { return m_comm.size() - 1; }\n\n  void initialize_window() {\n    m_task_elem = static_cast&lt;size_t&gt;(detail::mpi_type_size_bytes&lt;TaskT&gt;());\n    m_result_elem = static_cast&lt;size_t&gt;(detail::mpi_type_size_bytes&lt;ResultT&gt;());\n    m_max_task_count =\n        MPI_Type&lt;TaskT&gt;::resize_required ? static_cast&lt;size_t&gt;(m_config.max_task_count) : 1;\n    m_max_result_count =\n        MPI_Type&lt;ResultT&gt;::resize_required ? static_cast&lt;size_t&gt;(m_config.max_result_count) : 1;\n\n    m_task_slot_stride = detail::round_up_8(T_DATA + m_max_task_count * m_task_elem);\n\n    const size_t capacity = static_cast&lt;size_t&gt;(m_config.max_tasks);\n    m_task_base = CONTROL_BYTES;\n    const size_t manager_window_bytes = m_task_base + capacity * m_task_slot_stride;\n\n    if (is_root_manager() &amp;&amp; num_workers() &gt; 0) {\n      m_window_buffer.resize(manager_window_bytes);\n    }\n\n    if (is_root_manager() &amp;&amp; num_workers() == 0) {\n      return;\n    }\n\n    void* base = nullptr;\n    MPI_Aint bsize = 0;\n    if (is_root_manager()) {\n      base = m_window_buffer.data();\n      bsize = static_cast&lt;MPI_Aint&gt;(m_window_buffer.size());\n    } else {\n      base = m_worker_window;\n      bsize = static_cast&lt;MPI_Aint&gt;(sizeof(m_worker_window));\n    }\n    DYNAMPI_MPI_CHECK(MPI_Win_create, (base, bsize, 1, MPI_INFO_NULL, m_comm.get(), &amp;m_window));\n    DYNAMPI_MPI_CHECK(MPI_Win_lock_all, (MPI_MODE_NOCHECK, m_window));\n  }\n\n  MPI_Aint task_slot(int64_t index) const {\n    return static_cast&lt;MPI_Aint&gt;(m_task_base + static_cast&lt;size_t&gt;(index) * m_task_slot_stride);\n  }\n\n  size_t available() const { return m_results.size(); }\n\n  // --- RMA primitives ---\n\n  void flush(int rank) { DYNAMPI_MPI_CHECK(MPI_Win_flush, (rank, m_window)); }\n\n  // Remote ranks use Fetch_and_op to read/update the manager's window.\n  int64_t atomic_read(MPI_Aint offset) {\n    int64_t in = 0, out;\n    DYNAMPI_MPI_CHECK(MPI_Fetch_and_op,\n                      (&amp;in, &amp;out, MPI_INT64_T, m_config.manager_rank, offset, MPI_NO_OP, m_window));\n    flush(m_config.manager_rank);\n    return out;\n  }\n\n  void atomic_set(MPI_Aint offset, int64_t value) {\n    int64_t in = value, out;\n    DYNAMPI_MPI_CHECK(MPI_Fetch_and_op, (&amp;in, &amp;out, MPI_INT64_T, m_config.manager_rank, offset,\n                                         MPI_REPLACE, m_window));\n    flush(m_config.manager_rank);\n  }\n\n  // Unlike compare_and_swap (see run_worker()'s doc comment for why it was\n  // dropped), fetch_add can't lose a race -- there's nothing to compare, it\n  // unconditionally reserves `increment` slots starting at whatever HEAD_OFF\n  // currently is and hands that starting point back.\n  int64_t fetch_add(MPI_Aint offset, int64_t increment) {\n    int64_t in = increment, out;\n    DYNAMPI_MPI_CHECK(MPI_Fetch_and_op,\n                      (&amp;in, &amp;out, MPI_INT64_T, m_config.manager_rank, offset, MPI_SUM, m_window));\n    flush(m_config.manager_rank);\n    return out;\n  }\n\n  // MPI_Put/MPI_Get take a plain `int` count, but n (a byte length derived\n  // from task/result-table capacities that can legitimately reach into the\n  // hundreds of millions of slots) can exceed INT_MAX. static_cast&lt;int&gt;(n)\n  // on an over-INT_MAX n silently wraps -- see the identical fix (and its\n  // fuller rationale) in hierarchical_async_put_lockfree_distributor.hpp's\n  // put_bytes()/get_bytes(). Chunk into INT_MAX-bounded pieces so no single\n  // MPI_Put/MPI_Get call is ever handed a count that doesn't fit in `int`.\n  static constexpr size_t kMaxRmaChunkBytes = static_cast&lt;size_t&gt;(std::numeric_limits&lt;int&gt;::max());\n\n  void put_bytes(const void* src, size_t n, MPI_Aint offset) {\n    if (n == 0) return;\n    const auto* bytes = static_cast&lt;const std::byte*&gt;(src);\n    size_t done = 0;\n    while (done &lt; n) {\n      const size_t chunk = std::min(kMaxRmaChunkBytes, n - done);\n      DYNAMPI_MPI_CHECK(MPI_Put, (bytes + done, static_cast&lt;int&gt;(chunk), MPI_BYTE,\n                                  m_config.manager_rank, offset + static_cast&lt;MPI_Aint&gt;(done),\n                                  static_cast&lt;int&gt;(chunk), MPI_BYTE, m_window));\n      done += chunk;\n    }\n    flush(m_config.manager_rank);\n  }\n\n  // Workers read the manager's window.\n  void get_bytes(void* dst, size_t n, MPI_Aint offset) {\n    if (n == 0) return;\n    auto* bytes = static_cast&lt;std::byte*&gt;(dst);\n    size_t done = 0;\n    while (done &lt; n) {\n      const size_t chunk = std::min(kMaxRmaChunkBytes, n - done);\n      DYNAMPI_MPI_CHECK(MPI_Get, (bytes + done, static_cast&lt;int&gt;(chunk), MPI_BYTE,\n                                  m_config.manager_rank, offset + static_cast&lt;MPI_Aint&gt;(done),\n                                  static_cast&lt;int&gt;(chunk), MPI_BYTE, m_window));\n      done += chunk;\n    }\n    flush(m_config.manager_rank);\n  }\n\n  // --- Task / result transfer ---\n\n  void publish_task(const TaskT&amp; task) {\n    const int64_t index = m_total_tasks;\n    assert(static_cast&lt;size_t&gt;(index) &lt; static_cast&lt;size_t&gt;(m_config.max_tasks) &amp;&amp;\n           \"LockFree: exceeded max_tasks capacity\");\n\n    if (num_workers() == 0) {\n      m_task_store.push_back(task);\n      m_total_tasks++;\n      return;\n    }\n\n    const int count = MPI_Type&lt;TaskT&gt;::count(task);\n    assert(static_cast&lt;size_t&gt;(count) &lt;= m_max_task_count &amp;&amp;\n           \"LockFree: task exceeds max_task_count\");\n    const size_t data_bytes = static_cast&lt;size_t&gt;(count) * m_task_elem;\n\n    std::vector&lt;std::byte&gt; buffer(T_DATA + data_bytes);\n    detail::write_i64(buffer.data(), buffer.size(), T_COUNT, count);\n    if (count &gt; 0) {\n      detail::write_bytes(buffer.data(), buffer.size(), T_DATA, MPI_Type&lt;TaskT&gt;::ptr(task),\n                          data_bytes);\n    }\n    put_bytes(buffer.data(), buffer.size(), task_slot(index));\n\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      m_statistics.comm_statistics.bytes_sent += data_bytes;\n      m_statistics.comm_statistics.send_count++;\n    }\n\n    m_total_tasks++;\n    atomic_set(TOTAL_OFF, m_total_tasks);  // publish to workers\n  }\n\n  // Reads `count` contiguous task slots starting at `index` in a single RMA\n  // Get spanning the whole range (rather than one Get per task, or two for\n  // resizable TaskT), then parses each fixed-stride slot out of that one\n  // buffer locally. Transfers some unused padding for resizable TaskT whose\n  // actual size is below max_task_count, but for a claimed batch that's a\n  // bandwidth cost, not a round-trip cost -- round trips are what dominate\n  // here (see run_worker()).\n  std::vector&lt;TaskT&gt; read_task_batch(int64_t index, int64_t count) {\n    const size_t bytes = static_cast&lt;size_t&gt;(count) * m_task_slot_stride;\n    std::vector&lt;std::byte&gt; buf(bytes);\n    get_bytes(buf.data(), bytes, task_slot(index));\n\n    std::vector&lt;TaskT&gt; tasks;\n    tasks.reserve(static_cast&lt;size_t&gt;(count));\n    for (int64_t i = 0; i &lt; count; ++i) {\n      const size_t slot_offset = static_cast&lt;size_t&gt;(i) * m_task_slot_stride;\n      const int64_t elem_count = detail::read_i64(buf.data(), buf.size(), slot_offset + T_COUNT);\n      TaskT task{};\n      if constexpr (MPI_Type&lt;TaskT&gt;::resize_required)\n        MPI_Type&lt;TaskT&gt;::resize(task, static_cast&lt;int&gt;(elem_count));\n      const size_t data_bytes = static_cast&lt;size_t&gt;(elem_count) * m_task_elem;\n      detail::read_result_bytes(buf.data(), buf.size(), slot_offset + T_DATA, task, data_bytes);\n      tasks.push_back(std::move(task));\n    }\n    return tasks;\n  }\n\n  void store_result(int64_t index, ResultT result) {\n    assert(static_cast&lt;size_t&gt;(MPI_Type&lt;ResultT&gt;::count(result)) &lt;= m_max_result_count &amp;&amp;\n           \"LockFree: result exceeds max_result_count\");\n    m_local_results.emplace_back(index, std::move(result));\n  }\n\n  // --- Result collection via Gatherv (all ranks) ---\n\n  void maybe_participate_in_gather() {\n    const int64_t seq = atomic_read(GATHER_SEQ_OFF);\n    if (seq == m_seen_gather_seq) return;\n    m_seen_gather_seq = seq;\n    DYNAMPI_MPI_CHECK(MPI_Barrier, (m_comm.get()));\n    exchange_gathered_results();\n  }\n\n  void request_gather() {\n    atomic_set(GATHER_SEQ_OFF, ++m_gather_seq);\n    detail::rma_wait_idle(m_window);\n    maybe_participate_in_gather();\n  }\n\n  void try_gather_results() {\n    const size_t before = m_collected_count;\n    request_gather();\n    if (m_collected_count == before) detail::rma_wait_idle(m_window);\n  }\n\n  void exchange_gathered_results() {\n    const int elem = detail::mpi_type_size_bytes&lt;ResultT&gt;();\n    const bool manager = is_root_manager();\n    const int size = m_comm.size();\n\n    std::vector&lt;std::byte&gt; send_buf;\n    for (auto&amp; [index, result] : m_local_results) {\n      const int count = MPI_Type&lt;ResultT&gt;::count(result);\n      assert(count &gt;= 0);\n      const size_t data_bytes =\n          count &gt; 0 ? static_cast&lt;size_t&gt;(count) * static_cast&lt;size_t&gt;(elem) : size_t{0};\n      const size_t offset = send_buf.size();\n      send_buf.resize(offset + 16 + data_bytes);\n      detail::write_i64(send_buf.data(), send_buf.size(), offset, index);\n      detail::write_i64(send_buf.data(), send_buf.size(), offset + 8, count);\n      if (data_bytes &gt; 0) {\n        detail::write_bytes(send_buf.data(), send_buf.size(), offset + 16,\n                            MPI_Type&lt;ResultT&gt;::ptr(result), data_bytes);\n      }\n    }\n    m_local_results.clear();\n\n    const int send_count = static_cast&lt;int&gt;(send_buf.size());\n    std::vector&lt;int&gt; byte_counts(manager ? static_cast&lt;size_t&gt;(size) : 0);\n    DYNAMPI_MPI_CHECK(MPI_Gather, (&amp;send_count, 1, MPI_INT, manager ? byte_counts.data() : nullptr,\n                                   1, MPI_INT, m_config.manager_rank, m_comm.get()));\n\n    std::vector&lt;int&gt; displacements;\n    std::vector&lt;std::byte&gt; recv_buf;\n    int total_bytes = 0;\n    if (manager) {\n      displacements.resize(static_cast&lt;size_t&gt;(size));\n      for (int r = 0; r &lt; size; ++r) {\n        displacements[static_cast&lt;size_t&gt;(r)] = total_bytes;\n        total_bytes += byte_counts[static_cast&lt;size_t&gt;(r)];\n      }\n      recv_buf.resize(static_cast&lt;size_t&gt;(total_bytes));\n    }\n\n    DYNAMPI_MPI_CHECK(\n        MPI_Gatherv,\n        (send_buf.data(), send_count, MPI_BYTE, manager ? recv_buf.data() : nullptr,\n         manager ? byte_counts.data() : nullptr, manager ? displacements.data() : nullptr, MPI_BYTE,\n         m_config.manager_rank, m_comm.get()));\n\n    if (!manager) return;\n\n    for (int r = 0; r &lt; size; ++r) {\n      if (r == m_config.manager_rank) continue;\n      size_t pos = static_cast&lt;size_t&gt;(displacements[static_cast&lt;size_t&gt;(r)]);\n      const size_t end = pos + static_cast&lt;size_t&gt;(byte_counts[static_cast&lt;size_t&gt;(r)]);\n      size_t result_count = 0;\n      while (pos &lt; end) {\n        assert(pos + 16 &lt;= end);\n        const int64_t index = detail::read_i64(recv_buf.data(), recv_buf.size(), pos);\n        const int64_t count = detail::read_i64(recv_buf.data(), recv_buf.size(), pos + 8);\n        pos += 16;\n        ResultT result{};\n        if constexpr (MPI_Type&lt;ResultT&gt;::resize_required)\n          MPI_Type&lt;ResultT&gt;::resize(result, static_cast&lt;int&gt;(count));\n        assert(count &gt;= 0);\n        const size_t data_bytes =\n            count &gt; 0 ? static_cast&lt;size_t&gt;(count) * static_cast&lt;size_t&gt;(elem) : size_t{0};\n        detail::read_result_bytes(recv_buf.data(), recv_buf.size(), pos, result, data_bytes);\n        pos += data_bytes;\n        m_staging[index] = std::move(result);\n        result_count++;\n\n        if constexpr (statistics_mode != StatisticsMode::None) {\n          m_statistics.comm_statistics.bytes_received += data_bytes;\n          m_statistics.comm_statistics.recv_count++;\n        }\n      }\n      if constexpr (statistics_mode &gt;= StatisticsMode::Aggregated) {\n        if (static_cast&lt;size_t&gt;(r) &lt; m_statistics.worker_task_counts.size())\n          m_statistics.worker_task_counts[static_cast&lt;size_t&gt;(r)] += result_count;\n      }\n    }\n\n    while (m_staging.contains(static_cast&lt;int64_t&gt;(m_collected_count))) {\n      m_results.push_back(std::move(m_staging[static_cast&lt;int64_t&gt;(m_collected_count)]));\n      m_staging.erase(static_cast&lt;int64_t&gt;(m_collected_count));\n      m_collected_count++;\n    }\n  }\n\n  void run_one_task_locally() {\n    m_results.push_back(m_worker_function(m_task_store[m_collected_count]));\n    if constexpr (statistics_mode &gt;= StatisticsMode::Aggregated) {\n      if (static_cast&lt;size_t&gt;(m_config.manager_rank) &lt; m_statistics.worker_task_counts.size())\n        m_statistics.worker_task_counts[static_cast&lt;size_t&gt;(m_config.manager_rank)]++;\n    }\n    m_collected_count++;\n  }\n\n  std::vector&lt;ResultT&gt; drain_results(size_t limit) {\n    const size_t count = std::min(limit, m_results.size());\n    std::vector&lt;ResultT&gt; output;\n    output.reserve(count);\n    for (size_t i = 0; i &lt; count; ++i) output.push_back(std::move(m_results[i]));\n    m_results.erase(m_results.begin(), m_results.begin() + static_cast&lt;ptrdiff_t&gt;(count));\n    m_returned_count += count;\n    return output;\n  }\n\n  static StatisticsT make_statistics() {\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      return Statistics{};\n    } else {\n      return {};\n    }\n  }\n};\n\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/naive__distributor_8hpp/","title":"File naive_distributor.hpp","text":"<p>FileList &gt; dynampi &gt; impl &gt; naive_distributor.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;algorithm&gt;</code></li> <li><code>#include &lt;cassert&gt;</code></li> <li><code>#include &lt;cstdint&gt;</code></li> <li><code>#include &lt;deque&gt;</code></li> <li><code>#include &lt;functional&gt;</code></li> <li><code>#include &lt;limits&gt;</code></li> <li><code>#include &lt;optional&gt;</code></li> <li><code>#include &lt;queue&gt;</code></li> <li><code>#include &lt;stack&gt;</code></li> <li><code>#include &lt;type_traits&gt;</code></li> <li><code>#include &lt;variant&gt;</code></li> <li><code>#include &lt;vector&gt;</code></li> <li><code>#include \"../mpi/mpi_communicator.hpp\"</code></li> <li><code>#include \"../mpi/mpi_types.hpp\"</code></li> <li><code>#include \"dynampi/impl/base_distributor.hpp\"</code></li> <li><code>#include \"dynampi/utilities/timer.hpp\"</code></li> </ul>"},{"location":"dynampi/naive__distributor_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi"},{"location":"dynampi/naive__distributor_8hpp/#classes","title":"Classes","text":"Type Name class NaiveMPIWorkDistributor &lt;typename TaskT, typename ResultT, Options&gt; struct Config struct RunConfig struct Statistics <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/naive_distributor.hpp</code></p>"},{"location":"dynampi/naive__distributor_8hpp_source/","title":"File naive_distributor.hpp","text":"<p>File List &gt; dynampi &gt; impl &gt; naive_distributor.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2025 QDX Technologies. Authored by Ryan Stocks &lt;ryan.stocks00@gmail.com&gt;\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;algorithm&gt;\n#include &lt;cassert&gt;\n#include &lt;cstdint&gt;\n#include &lt;deque&gt;\n#include &lt;functional&gt;\n#include &lt;limits&gt;\n#include &lt;optional&gt;\n#include &lt;queue&gt;\n#include &lt;stack&gt;\n#include &lt;type_traits&gt;\n#include &lt;variant&gt;\n#include &lt;vector&gt;\n\n#include \"../mpi/mpi_communicator.hpp\"\n#include \"../mpi/mpi_types.hpp\"\n#include \"dynampi/impl/base_distributor.hpp\"\n#include \"dynampi/utilities/timer.hpp\"\n\nnamespace dynampi {\n\ntemplate &lt;typename TaskT, typename ResultT, typename... Options&gt;\nclass NaiveMPIWorkDistributor {\n public:\n  struct Config {\n    MPI_Comm comm = MPI_COMM_WORLD;\n    int manager_rank = 0;\n    bool auto_run_workers = true;\n    bool use_immediate_recv = false;\n    int max_result_size = 1024;  // Maximum expected size for RESULT messages when using immediate\n                                 // recv. Must be large enough to hold the largest expected RESULT\n                                 // message. If a message exceeds this size, behavior is undefined.\n  };\n\n  struct RunConfig {\n    // Stop once we have at least this many contiguous results ready to return.\n    size_t target_num_tasks = std::numeric_limits&lt;size_t&gt;::max();\n\n    // If false, strictly clips the return vector to `target_num_tasks`.\n    // Excess results remain in the internal buffer for the next call.\n    bool allow_more_than_target_tasks = true;\n\n    // Stop if this much time has passed.\n    std::optional&lt;double&gt; max_seconds = std::nullopt;\n  };\n\n  static const bool ordered = true;\n\n private:\n  static constexpr bool prioritize_tasks = get_option_value&lt;prioritize_tasks_t, Options...&gt;();\n  static constexpr StatisticsMode statistics_mode =\n      get_option_value&lt;track_statistics_t, Options...&gt;();\n\n  using QueueT = std::conditional_t&lt;prioritize_tasks, std::priority_queue&lt;std::pair&lt;double, TaskT&gt;&gt;,\n                                    std::deque&lt;TaskT&gt;&gt;;\n  using MPICommunicator = dynampi::MPICommunicator&lt;track_statistics&lt;statistics_mode&gt;&gt;;\n\n  // --- Member Variables ---\n  Config m_config;\n  MPICommunicator m_communicator;\n  std::function&lt;ResultT(TaskT)&gt; m_worker_function;\n\n  QueueT m_unallocated_task_queue;\n\n  // State tracking\n  std::vector&lt;int64_t&gt; m_worker_current_task_indices;  // Maps worker_idx -&gt; task_id\n  std::stack&lt;int&gt; m_free_worker_ranks;\n\n  // Transient Storage:\n  // We use a vector to store results by task ID, with a bitmap to track validity.\n  // Items are marked invalid as soon as they become contiguous and ready to return.\n  std::vector&lt;ResultT&gt; m_pending_results;\n  std::vector&lt;bool&gt; m_pending_results_valid;\n\n  // Counters\n  size_t m_tasks_sent = 0;        // Total tasks ever sent (acts as the unique ID for the next task)\n  size_t m_front_result_idx = 0;  // The task ID of the result at the front of the vector (index 0)\n  size_t m_known_contiguous_results =\n      0;  // Number of contiguous valid results starting from m_front_result_idx\n  bool m_finalized = false;\n\n  enum Tag : int { TASK = 0, DONE = 1, RESULT = 2, REQUEST = 3, ERROR = 4 };\n\n public:\n  struct Statistics {\n    const CommStatistics&amp; comm_statistics;\n    std::vector&lt;size_t&gt; worker_task_counts;\n  };\n\n  using StatisticsT =\n      std::conditional_t&lt;statistics_mode == StatisticsMode::Detailed, Statistics, std::monostate&gt;;\n\n private:\n  StatisticsT m_statistics;\n\n public:\n  explicit NaiveMPIWorkDistributor(std::function&lt;ResultT(TaskT)&gt; worker_function,\n                                   Config runtime_config = Config{})\n      : m_config(runtime_config),\n        m_communicator(runtime_config.comm, MPICommunicator::Duplicate),\n        m_worker_function(worker_function),\n        m_statistics{create_statistics(m_communicator)} {\n    if (is_root_manager()) {\n      m_worker_current_task_indices.resize(num_workers(), -1);\n    }\n\n    if (m_config.auto_run_workers &amp;&amp; !is_root_manager()) {\n      run_worker();\n    }\n\n    if constexpr (statistics_mode &gt;= StatisticsMode::Aggregated) {\n      if (is_root_manager()) m_statistics.worker_task_counts.resize(m_communicator.size(), 0);\n    }\n  }\n\n  ~NaiveMPIWorkDistributor() {\n    if (!m_finalized) finalize();\n  }\n\n  // --- Main Interface ---\n\n  [[nodiscard]] std::vector&lt;ResultT&gt; run_tasks(RunConfig config = RunConfig{}) {\n    assert(is_root_manager() &amp;&amp; \"Only the manager can distribute tasks\");\n\n    Timer timer;\n\n    // We loop until one of the exit conditions is met.\n    while (true) {\n      // --- 1. Check Exit Conditions ---\n\n      // A. Have we collected enough contiguous results?\n      if (m_known_contiguous_results &gt;= config.target_num_tasks) {\n        break;\n      }\n\n      // B. Time limit check\n      if (config.max_seconds &amp;&amp; timer.elapsed().count() &gt;= *config.max_seconds) {\n        break;\n      }\n\n      // C. Total exhaustion check\n      if (m_unallocated_task_queue.empty() &amp;&amp; active_worker_count() == 0) {\n        break;\n      }\n\n      // --- 2. Action Logic (Send vs Receive) ---\n\n      // Priority: Keep workers busy\n      if (!m_unallocated_task_queue.empty() &amp;&amp; !m_free_worker_ranks.empty()) {\n        send_next_task_to_worker(m_free_worker_ranks.top());\n        m_free_worker_ranks.pop();\n      } else {\n        // Single process mode fallback\n        if (num_workers() == 0 &amp;&amp; !m_unallocated_task_queue.empty()) {\n          run_task_locally();\n        }\n        // Standard MPI wait\n        else if (active_worker_count() &gt; 0) {\n          process_incoming_message();\n        }\n      }\n    }\n\n    // --- 3. Return Logic ---\n    size_t limit = std::numeric_limits&lt;size_t&gt;::max();\n    if (!config.allow_more_than_target_tasks) {\n      limit = config.target_num_tasks;\n    }\n\n    return collect_available_results(limit);\n  }\n\n  [[nodiscard]] std::vector&lt;ResultT&gt; finish_remaining_tasks() {\n    RunConfig cfg;\n    cfg.target_num_tasks = std::numeric_limits&lt;size_t&gt;::max();\n    return run_tasks(cfg);\n  }\n\n  void finalize() {\n    assert(!m_finalized &amp;&amp; \"Work distribution already finalized\");\n    if (is_root_manager()) {\n      broadcast_done();\n    }\n    m_finalized = true;\n  }\n\n  // --- Public Accessors ---\n\n  bool is_root_manager() const { return m_communicator.rank() == m_config.manager_rank; }\n\n  size_t remaining_tasks_count() const {\n    assert(is_root_manager() &amp;&amp; \"Only the manager can check remaining tasks\");\n    return m_unallocated_task_queue.size();\n  }\n\n  const StatisticsT&amp; get_statistics() const\n    requires(statistics_mode != StatisticsMode::None)\n  {\n    assert(is_root_manager() &amp;&amp; \"Only the manager can access statistics\");\n    return m_statistics;\n  }\n\n  // --- Task Insertion ---\n\n  void insert_task(TaskT task)\n    requires(!prioritize_tasks)\n  {\n    assert(is_root_manager());\n    m_unallocated_task_queue.push_back(std::move(task));\n  }\n\n  void insert_task(const TaskT&amp; task, double priority)\n    requires(prioritize_tasks)\n  {\n    assert(is_root_manager());\n    m_unallocated_task_queue.emplace(priority, task);\n  }\n\n  void insert_tasks(const std::vector&lt;TaskT&gt;&amp; tasks)\n    requires(!prioritize_tasks)\n  {\n    assert(is_root_manager());\n    for (const auto&amp; t : tasks) m_unallocated_task_queue.push_back(t);\n  }\n\n  // --- Worker Logic ---\n\n  void run_worker() {\n    assert(!is_root_manager());\n    using task_type = MPI_Type&lt;TaskT&gt;;\n\n    // Handshake: send REQUEST as 0 elements of ResultT so manager can recv_any(ResultT&amp;) for both\n    // REQUEST and RESULT\n    m_communicator.template send_empty&lt;ResultT&gt;(m_config.manager_rank, Tag::REQUEST);\n\n    while (true) {\n      MPI_Status status = m_communicator.probe();\n\n      if (status.MPI_TAG == Tag::DONE) {\n        m_communicator.recv_empty_message(m_config.manager_rank, Tag::DONE);\n        break;\n      }\n\n      int count;\n      DYNAMPI_MPI_CHECK(MPI_Get_count, (&amp;status, task_type::value, &amp;count));\n      TaskT message;\n      task_type::resize(message, count);\n      m_communicator.recv(message, m_config.manager_rank, Tag::TASK);\n\n      ResultT result = m_worker_function(std::move(message));\n\n      m_communicator.send(result, m_config.manager_rank, Tag::RESULT);\n    }\n  }\n\n private:\n  // --- Helpers ---\n\n  int num_workers() const { return m_communicator.size() - 1; }\n\n  size_t active_worker_count() const {\n    return static_cast&lt;size_t&gt;(num_workers()) - m_free_worker_ranks.size();\n  }\n\n  int rank_to_worker_idx(int rank) const {\n    return (rank &lt; m_config.manager_rank) ? rank : (rank - 1);\n  }\n\n  int worker_idx_to_rank(int idx) const { return (idx &lt; m_config.manager_rank) ? idx : (idx + 1); }\n\n  TaskT pop_next_task() {\n    TaskT task;\n    if constexpr (prioritize_tasks) {\n      task = m_unallocated_task_queue.top().second;\n      m_unallocated_task_queue.pop();\n    } else {\n      task = std::move(m_unallocated_task_queue.front());\n      m_unallocated_task_queue.pop_front();\n    }\n    return task;\n  }\n\n  void run_task_locally() {\n    TaskT task = pop_next_task();\n    // Store result directly in vector (using relative indexing)\n    int64_t task_id = static_cast&lt;int64_t&gt;(m_tasks_sent);\n    ensure_result_capacity(task_id - m_front_result_idx + 1);\n    size_t vector_idx = task_id - m_front_result_idx;\n    m_pending_results[vector_idx] = m_worker_function(std::move(task));\n    m_pending_results_valid[vector_idx] = true;\n    m_tasks_sent++;\n    update_contiguous_results_count(task_id);\n  }\n\n  void send_next_task_to_worker(int worker_rank) {\n    TaskT task = pop_next_task();\n    int64_t task_id = static_cast&lt;int64_t&gt;(m_tasks_sent);\n\n    m_worker_current_task_indices[rank_to_worker_idx(worker_rank)] = task_id;\n    if constexpr (statistics_mode &gt;= StatisticsMode::Aggregated) {\n      m_statistics.worker_task_counts[rank_to_worker_idx(worker_rank)]++;\n    }\n\n    m_communicator.send(task, worker_rank, Tag::TASK);\n    m_tasks_sent++;\n  }\n\n  void process_incoming_message() {\n    MPI_Status status = m_communicator.probe(MPI_ANY_SOURCE, MPI_ANY_TAG);\n    int source = status.MPI_SOURCE;\n    if (status.MPI_TAG == Tag::RESULT) {\n      handle_result_message(source, status);\n    } else {\n      DYNAMPI_ASSERT_EQ(status.MPI_TAG, Tag::REQUEST, \"Unexpected tag received\");\n      m_communicator.template recv_empty&lt;ResultT&gt;(source, Tag::REQUEST);\n    }\n    m_free_worker_ranks.push(source);\n  }\n\n  void handle_result_message(int source, MPI_Status&amp; probe_status) {\n    int worker_idx = rank_to_worker_idx(source);\n    int64_t task_id = m_worker_current_task_indices[worker_idx];\n    m_worker_current_task_indices[worker_idx] = -1;\n\n    using result_type = MPI_Type&lt;ResultT&gt;;\n    int count;\n    DYNAMPI_MPI_CHECK(MPI_Get_count, (&amp;probe_status, result_type::value, &amp;count));\n\n    ResultT result_data;\n    result_type::resize(result_data, count);\n    m_communicator.recv(result_data, source, Tag::RESULT);\n\n    // Store in vector (using relative indexing)\n    size_t vector_idx = task_id - m_front_result_idx;\n    ensure_result_capacity(vector_idx + 1);\n    m_pending_results[vector_idx] = std::move(result_data);\n    m_pending_results_valid[vector_idx] = true;\n    update_contiguous_results_count(task_id);\n  }\n\n  std::vector&lt;ResultT&gt; collect_available_results(size_t limit) {\n    std::vector&lt;ResultT&gt; batch;\n    size_t num_results_to_return = std::min(limit, m_known_contiguous_results);\n    if (num_results_to_return == 0) {\n      return batch;\n    }\n\n    batch.reserve(num_results_to_return);\n    // Extract from the beginning of the vectors (which contain contiguous results starting from\n    // m_front_result_idx)\n    batch.insert(batch.end(), std::make_move_iterator(m_pending_results.begin()),\n                 std::make_move_iterator(m_pending_results.begin() + num_results_to_return));\n\n    // Erase the collected results from the beginning\n    m_pending_results_valid.erase(m_pending_results_valid.begin(),\n                                  m_pending_results_valid.begin() + num_results_to_return);\n    m_pending_results.erase(m_pending_results.begin(),\n                            m_pending_results.begin() + num_results_to_return);\n\n    // Update counters: increment m_front_result_idx to reflect the new starting point,\n    // and decrement the contiguous count. The vectors now use relative indexing\n    // where index 0 corresponds to task_id = m_front_result_idx.\n    m_front_result_idx += num_results_to_return;\n    m_known_contiguous_results -= num_results_to_return;\n\n    return batch;\n  }\n\n  void broadcast_done() {\n    for (int i = 0; i &lt; num_workers(); i++) {\n      m_communicator.send(nullptr, worker_idx_to_rank(i), Tag::DONE);\n    }\n  }\n\n  void ensure_result_capacity(size_t required_size) {\n    // required_size is relative to m_front_result_idx\n    if (m_pending_results.size() &lt; required_size) {\n      m_pending_results.resize(required_size);\n      m_pending_results_valid.resize(required_size, false);\n    }\n  }\n\n  // Updates m_known_contiguous_results when a new result arrives.\n  // If the result extends the contiguous sequence, increment and check forward.\n  void update_contiguous_results_count(int64_t task_id) {\n    int64_t expected_task_id =\n        static_cast&lt;int64_t&gt;(m_front_result_idx + m_known_contiguous_results);\n\n    // Only update if this result extends the contiguous sequence\n    if (task_id == expected_task_id) {\n      // Extend the contiguous sequence forward as far as possible\n      // Use relative indexing for vector access\n      size_t vector_idx = expected_task_id - m_front_result_idx;\n      while (vector_idx &lt; m_pending_results.size() &amp;&amp; m_pending_results_valid[vector_idx]) {\n        m_known_contiguous_results++;\n        vector_idx++;\n      }\n    }\n  }\n\n  static StatisticsT create_statistics(const MPICommunicator&amp; comm) {\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      return Statistics{comm.get_statistics(), {}};\n    } else {\n      return {};\n    }\n  }\n};\n\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/dir_70f9944cf42b7c83e40875b744e30ff7/","title":"Dir /home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi","text":"<p>FileList &gt; dynampi &gt; mpi</p>"},{"location":"dynampi/dir_70f9944cf42b7c83e40875b744e30ff7/#files","title":"Files","text":"Type Name file mpi_communicator.hpp file mpi_error.hpp file mpi_group.hpp file mpi_types.hpp <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/</code></p>"},{"location":"dynampi/mpi__communicator_8hpp/","title":"File mpi_communicator.hpp","text":"<p>FileList &gt; dynampi &gt; mpi &gt; mpi_communicator.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;mpi.h&gt;</code></li> <li><code>#include &lt;optional&gt;</code></li> <li><code>#include &lt;variant&gt;</code></li> <li><code>#include \"dynampi/mpi/mpi_group.hpp\"</code></li> <li><code>#include \"dynampi/mpi/mpi_types.hpp\"</code></li> <li><code>#include \"dynampi/utilities/assert.hpp\"</code></li> <li><code>#include \"dynampi/utilities/template_options.hpp\"</code></li> <li><code>#include \"mpi_error.hpp\"</code></li> </ul>"},{"location":"dynampi/mpi__communicator_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi"},{"location":"dynampi/mpi__communicator_8hpp/#classes","title":"Classes","text":"Type Name struct CommStatistics class MPICommunicator &lt;Options&gt; struct track_statistics &lt;Mode&gt; struct track_statistics_t <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_communicator.hpp</code></p>"},{"location":"dynampi/mpi__communicator_8hpp_source/","title":"File mpi_communicator.hpp","text":"<p>File List &gt; dynampi &gt; mpi &gt; mpi_communicator.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2025 QDX Technologies. Authored by Ryan Stocks &lt;ryan.stocks00@gmail.com&gt;\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;mpi.h&gt;\n\n#include &lt;optional&gt;\n#include &lt;variant&gt;\n\n#include \"dynampi/mpi/mpi_group.hpp\"\n#include \"dynampi/mpi/mpi_types.hpp\"\n#include \"dynampi/utilities/assert.hpp\"\n#include \"dynampi/utilities/template_options.hpp\"\n#include \"mpi_error.hpp\"\n\nnamespace dynampi {\n\nenum class StatisticsMode {\n  None,\n  Aggregated,\n  Detailed,\n};\n\nstruct track_statistics_t {\n  static constexpr StatisticsMode value = StatisticsMode::None;\n};\n\ntemplate &lt;StatisticsMode Mode = StatisticsMode::Detailed&gt;\nstruct track_statistics : public track_statistics_t {\n  static constexpr StatisticsMode value = Mode;\n};\n\nstruct CommStatistics {\n  int send_count = 0;\n  int recv_count = 0;\n  int collective_count = 0;\n  size_t bytes_sent = 0;\n  size_t bytes_received = 0;\n  double send_time = 0.0;\n  double recv_time = 0.0;\n\n  void reset() {\n    send_count = 0;\n    recv_count = 0;\n    send_time = 0.0;\n    recv_time = 0.0;\n  }\n\n  double average_send_size() const {\n    if (send_count == 0) return 0.0;\n    return static_cast&lt;double&gt;(bytes_sent) / send_count;\n  }\n\n  double average_receive_size() const {\n    if (recv_count == 0) return 0.0;\n    return static_cast&lt;double&gt;(bytes_received) / recv_count;\n  }\n};\n\ntemplate &lt;typename... Options&gt;\nclass MPICommunicator {\n public:\n  enum Ownership {\n    Reference,  // The communicator is not owned by this class and should not be freed.\n    Move,       // The communicator is moved into this class and will be freed in the destructor.\n    Duplicate,  // The communicator is duplicated by this class and will be freed in the destructor.\n  };\n\n private:\n  MPI_Comm m_comm;\n  Ownership m_ownership;\n\n  static constexpr StatisticsMode statistics_mode =\n      get_option_value&lt;track_statistics_t, Options...&gt;();\n  using StatisticsT =\n      std::conditional_t&lt;statistics_mode != StatisticsMode::None, CommStatistics, std::monostate&gt;;\n\n  StatisticsT _statistics;\n\n public:\n  MPICommunicator(MPI_Comm comm, Ownership ownership = Duplicate)\n      : m_comm(comm), m_ownership(ownership) {\n    if (m_ownership == Duplicate) {\n      DYNAMPI_MPI_CHECK(MPI_Comm_dup, (comm, &amp;m_comm));\n    }\n  }\n\n  MPICommunicator(const MPICommunicator&amp; other) = delete;\n  MPICommunicator&amp; operator=(const MPICommunicator&amp; other) = delete;\n  MPICommunicator(MPICommunicator&amp;&amp; other) noexcept\n      : m_comm(other.m_comm),\n        m_ownership(other.m_ownership),\n        _statistics(std::move(other._statistics)) {\n    other.m_comm = MPI_COMM_NULL;\n    other.m_ownership = Reference;\n  }\n  MPICommunicator&amp; operator=(MPICommunicator&amp;&amp; other) = delete;\n\n  ~MPICommunicator() {\n    if (m_ownership != Reference) {\n      MPI_Comm_free(&amp;m_comm);\n    }\n  }\n\n  MPICommunicator split_by_node() const {\n    MPI_Comm node_comm;\n    DYNAMPI_MPI_CHECK(MPI_Comm_split_type,\n                      (m_comm, MPI_COMM_TYPE_SHARED, 0, MPI_INFO_NULL, &amp;node_comm));\n    return MPICommunicator(node_comm, Move);\n  }\n\n  std::optional&lt;MPICommunicator&gt; split(int color, int key = 0) const {\n    MPI_Comm new_comm;\n    DYNAMPI_MPI_CHECK(MPI_Comm_split, (m_comm, color, key, &amp;new_comm));\n    if (new_comm == MPI_COMM_NULL) {\n      return std::nullopt;\n    }\n    assert(color != MPI_UNDEFINED &amp;&amp; \"Undefined color should not result in a valid communicator\");\n    return MPICommunicator(new_comm, Move);\n  }\n\n  operator MPI_Comm() const { return m_comm; }\n\n  const CommStatistics&amp; get_statistics() const\n    requires(statistics_mode != StatisticsMode::None)\n  {\n    return _statistics;\n  }\n\n  int rank() const {\n    int rank;\n    DYNAMPI_MPI_CHECK(MPI_Comm_rank, (m_comm, &amp;rank));\n    return rank;\n  }\n\n  int size() const {\n    int size;\n    DYNAMPI_MPI_CHECK(MPI_Comm_size, (m_comm, &amp;size));\n    return size;\n  }\n\n  template &lt;typename T&gt;\n  inline void send(const T&amp; data, int dest, int tag = 0) {\n    using mpi_type = MPI_Type&lt;T&gt;;\n    DYNAMPI_MPI_CHECK(\n        MPI_Send, (mpi_type::ptr(data), mpi_type::count(data), mpi_type::value, dest, tag, m_comm));\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      _statistics.send_count++;\n      int size;\n      MPI_Type_size(mpi_type::value, &amp;size);\n      _statistics.bytes_sent += mpi_type::count(data) * size;\n    }\n  }\n\n  inline MPI_Status probe(int source = MPI_ANY_SOURCE, int tag = MPI_ANY_TAG) {\n    MPI_Status status;\n    DYNAMPI_MPI_CHECK(MPI_Probe, (source, tag, m_comm, &amp;status));\n    return status;\n  }\n\n  inline std::optional&lt;MPI_Status&gt; iprobe(int source = MPI_ANY_SOURCE, int tag = MPI_ANY_TAG) {\n    MPI_Status status;\n    int flag;\n    DYNAMPI_MPI_CHECK(MPI_Iprobe, (source, tag, m_comm, &amp;flag, &amp;status));\n    if (flag) {\n      return status;\n    }\n    return std::nullopt;\n  }\n\n  template &lt;typename T&gt;\n  inline void recv(T&amp; data, int source, int tag = 0) {\n    using mpi_type = MPI_Type&lt;T&gt;;\n    MPI_Status status;\n    DYNAMPI_MPI_CHECK(MPI_Recv, (mpi_type::ptr(data), mpi_type::count(data), mpi_type::value,\n                                 source, tag, m_comm, &amp;status));\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      _statistics.recv_count++;\n      int actual_count;\n      DYNAMPI_MPI_CHECK(MPI_Get_count, (&amp;status, mpi_type::value, &amp;actual_count));\n      int size;\n      MPI_Type_size(mpi_type::value, &amp;size);\n      _statistics.bytes_received += actual_count * size;\n    }\n  }\n\n  // Receive with MPI_ANY_SOURCE/MPI_ANY_TAG and return status\n  template &lt;typename T&gt;\n  inline MPI_Status recv_any(T&amp; data, int source = MPI_ANY_SOURCE, int tag = MPI_ANY_TAG) {\n    using mpi_type = MPI_Type&lt;T&gt;;\n    MPI_Status status;\n    DYNAMPI_MPI_CHECK(MPI_Recv, (mpi_type::ptr(data), mpi_type::count(data), mpi_type::value,\n                                 source, tag, m_comm, &amp;status));\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      _statistics.recv_count++;\n      int actual_count;\n      DYNAMPI_MPI_CHECK(MPI_Get_count, (&amp;status, mpi_type::value, &amp;actual_count));\n      int size;\n      MPI_Type_size(mpi_type::value, &amp;size);\n      _statistics.bytes_received += actual_count * size;\n    }\n    return status;\n  }\n\n  template &lt;typename T&gt;\n  inline void broadcast(T&amp; data, int root = 0) {\n    using mpi_type = MPI_Type&lt;T&gt;;\n    if constexpr (mpi_type::resize_required) {\n      int size = mpi_type::count(data);\n      broadcast(size, root);\n      if (rank() != root) {\n        mpi_type::resize(data, size);\n      }\n    }\n    DYNAMPI_MPI_CHECK(MPI_Bcast,\n                      (mpi_type::ptr(data), mpi_type::count(data), mpi_type::value, root, m_comm));\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      _statistics.collective_count++;\n    }\n  }\n\n  inline void recv_empty_message(int source, int tag = 0) {\n    using mpi_type = MPI_Type&lt;std::nullptr_t&gt;;\n    DYNAMPI_MPI_CHECK(MPI_Recv, (nullptr, mpi_type::count(nullptr), mpi_type::value, source, tag,\n                                 m_comm, MPI_STATUS_IGNORE));\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      _statistics.recv_count++;\n    }\n  }\n\n  template &lt;typename T&gt;\n  inline void send_empty(int dest, int tag = 0) {\n    using mpi_type = MPI_Type&lt;T&gt;;\n    DYNAMPI_MPI_CHECK(MPI_Send, (nullptr, 0, mpi_type::value, dest, tag, m_comm));\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      _statistics.send_count++;\n    }\n  }\n\n  template &lt;typename T&gt;\n  inline void recv_empty(int source, int tag = 0) {\n    using mpi_type = MPI_Type&lt;T&gt;;\n    DYNAMPI_MPI_CHECK(MPI_Recv,\n                      (nullptr, 0, mpi_type::value, source, tag, m_comm, MPI_STATUS_IGNORE));\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      _statistics.recv_count++;\n    }\n  }\n\n  template &lt;typename T&gt;\n  inline void gather(const T&amp; data, std::vector&lt;T&gt;* result, int root = 0) {\n    DYNAMPI_ASSERT_EQ(result != nullptr, root == rank(),\n                      \"Gather result must be provided only on the root rank\");\n    using mpi_type = MPI_Type&lt;T&gt;;\n    DYNAMPI_MPI_CHECK(MPI_Gather, (mpi_type::ptr(data), mpi_type::count(data), mpi_type::value,\n                                   result == nullptr ? nullptr : result-&gt;data(),\n                                   mpi_type::count(data), mpi_type::value, root, m_comm));\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      _statistics.collective_count++;\n    }\n  }\n\n  [[nodiscard]] MPI_Comm get() const { return m_comm; }\n\n  // Get the group associated with this communicator\n  [[nodiscard]] MPIGroup get_group() const { return MPIGroup(*this); }\n};\n\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/mpi__error_8hpp/","title":"File mpi_error.hpp","text":"<p>FileList &gt; dynampi &gt; mpi &gt; mpi_error.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;mpi.h&gt;</code></li> <li><code>#include &lt;string&gt;</code></li> <li><code>#include &lt;stdexcept&gt;</code></li> </ul>"},{"location":"dynampi/mpi__error_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi"},{"location":"dynampi/mpi__error_8hpp/#macros","title":"Macros","text":"Type Name define DYNAMPI_MPI_CHECK (func, args) <code>/* multi line expression */</code>"},{"location":"dynampi/mpi__error_8hpp/#macro-definition-documentation","title":"Macro Definition Documentation","text":""},{"location":"dynampi/mpi__error_8hpp/#define-dynampi_mpi_check","title":"define DYNAMPI_MPI_CHECK","text":"<pre><code>#define DYNAMPI_MPI_CHECK (\n    func,\n    args\n) `/* multi line expression */`\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_error.hpp</code></p>"},{"location":"dynampi/mpi__error_8hpp_source/","title":"File mpi_error.hpp","text":"<p>File List &gt; dynampi &gt; mpi &gt; mpi_error.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2025 QDX Technologies. Authored by Ryan Stocks &lt;ryan.stocks00@gmail.com&gt;\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;mpi.h&gt;\n\n#include &lt;string&gt;\n\n// cppcheck-suppress preprocessorErrorDirective\n#if __has_include(&lt;source_location&gt;)\n#include &lt;source_location&gt;\n#if defined(__cpp_lib_source_location)\n#define DYNAMPI_HAS_SOURCE_LOCATION\n#endif\n#elif __has_include(&lt;experimental/source_location&gt;)\n#include &lt;experimental/source_location&gt;\n#define DYNAMPI_HAS_SOURCE_LOCATION\n#endif\n#include &lt;stdexcept&gt;\n\n#define DYNAMPI_MPI_CHECK(func, args)      \\\n  do {                                     \\\n    int err = func args;                   \\\n    if (err != MPI_SUCCESS) {              \\\n      dynampi::mpi_fail(err, #func #args); \\\n    }                                      \\\n  } while (false)\n\nnamespace dynampi {\ninline void mpi_fail(int err, std::string_view command\n#ifdef DYNAMPI_HAS_SOURCE_LOCATION\n                     ,\n                     std::source_location loc = std::source_location::current()\n#endif\n) {\n  char error_string[MPI_MAX_ERROR_STRING];\n  int length_of_error_string;\n  MPI_Error_string(err, error_string, &amp;length_of_error_string);\n  throw std::runtime_error(std::string(\"MPI error in \") + std::string(command) + \": \" +\n                           std::string(error_string, length_of_error_string)\n#ifdef DYNAMPI_HAS_SOURCE_LOCATION\n                           + \" at \" + loc.file_name() + \":\" + std::to_string(loc.line())\n#endif\n  );\n}\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/mpi__group_8hpp/","title":"File mpi_group.hpp","text":"<p>FileList &gt; dynampi &gt; mpi &gt; mpi_group.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;mpi.h&gt;</code></li> <li><code>#include \"dynampi/utilities/assert.hpp\"</code></li> <li><code>#include \"mpi_error.hpp\"</code></li> </ul>"},{"location":"dynampi/mpi__group_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi"},{"location":"dynampi/mpi__group_8hpp/#classes","title":"Classes","text":"Type Name class MPIGroup <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_group.hpp</code></p>"},{"location":"dynampi/mpi__group_8hpp_source/","title":"File mpi_group.hpp","text":"<p>File List &gt; dynampi &gt; mpi &gt; mpi_group.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2025 QDX Technologies. Authored by Ryan Stocks &lt;ryan.stocks00@gmail.com&gt;\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;mpi.h&gt;\n\n#include \"dynampi/utilities/assert.hpp\"\n#include \"mpi_error.hpp\"\n\nnamespace dynampi {\n\n// Forward declaration\ntemplate &lt;typename... Options&gt;\nclass MPICommunicator;\n\nclass MPIGroup {\n private:\n  MPI_Group m_group;\n\n public:\n  // Create from a communicator (extracts the group)\n  template &lt;typename... Options&gt;\n  explicit MPIGroup(const MPICommunicator&lt;Options...&gt;&amp; comm) {\n    DYNAMPI_MPI_CHECK(MPI_Comm_group, (comm.get(), &amp;m_group));\n  }\n\n  // Non-copyable\n  MPIGroup(const MPIGroup&amp; other) = delete;\n  MPIGroup&amp; operator=(const MPIGroup&amp; other) = delete;\n\n  // Movable\n  MPIGroup(MPIGroup&amp;&amp; other) noexcept : m_group(other.m_group) { other.m_group = MPI_GROUP_NULL; }\n  MPIGroup&amp; operator=(MPIGroup&amp;&amp; other) noexcept {\n    if (this != &amp;other) {\n      if (m_group != MPI_GROUP_NULL) {\n        MPI_Group_free(&amp;m_group);\n      }\n      m_group = other.m_group;\n      other.m_group = MPI_GROUP_NULL;\n    }\n    return *this;\n  }\n\n  ~MPIGroup() {\n    if (m_group != MPI_GROUP_NULL) {\n      MPI_Group_free(&amp;m_group);\n    }\n  }\n\n  // Translate ranks from this group to another group\n  void translate_ranks(const MPIGroup&amp; to_group, int n, const int ranks[],\n                       int translated_ranks[]) const {\n    DYNAMPI_MPI_CHECK(MPI_Group_translate_ranks,\n                      (m_group, n, ranks, to_group.m_group, translated_ranks));\n  }\n\n  // Convenience method for single rank translation\n  int translate_rank(int rank, const MPIGroup&amp; to_group) const {\n    int translated_rank;\n    translate_ranks(to_group, 1, &amp;rank, &amp;translated_rank);\n    return translated_rank;\n  }\n\n  // Get the size of this group\n  int size() const {\n    int size;\n    DYNAMPI_MPI_CHECK(MPI_Group_size, (m_group, &amp;size));\n    return size;\n  }\n\n  // Get the rank of the calling process in this group (MPI_UNDEFINED if not in group)\n  int rank() const {\n    int rank;\n    DYNAMPI_MPI_CHECK(MPI_Group_rank, (m_group, &amp;rank));\n    return rank;\n  }\n\n  // Check if a rank (from a reference group, typically the world group) is in this group\n  // Returns the rank in this group, or MPI_UNDEFINED if not found\n  // Note: This translates FROM reference_group TO this group\n  static int contains_rank_in_group(int rank_in_reference, const MPIGroup&amp; reference_group,\n                                    const MPIGroup&amp; target_group) {\n    return reference_group.translate_rank(rank_in_reference, target_group);\n  }\n\n  operator MPI_Group() const { return m_group; }\n\n  [[nodiscard]] MPI_Group get() const { return m_group; }\n};\n\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/mpi__types_8hpp/","title":"File mpi_types.hpp","text":"<p>FileList &gt; dynampi &gt; mpi &gt; mpi_types.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;mpi.h&gt;</code></li> <li><code>#include &lt;cassert&gt;</code></li> <li><code>#include &lt;cstddef&gt;</code></li> <li><code>#include &lt;string&gt;</code></li> <li><code>#include &lt;type_traits&gt;</code></li> <li><code>#include &lt;vector&gt;</code></li> </ul>"},{"location":"dynampi/mpi__types_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi"},{"location":"dynampi/mpi__types_8hpp/#classes","title":"Classes","text":"Type Name struct MPI_Type &lt;typename T, typename&gt; struct MPI_Type&lt; std::nullptr_t &gt; &lt;&gt; struct MPI_Type&lt; std::string &gt; &lt;&gt; struct MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt; &lt;typename T&gt; struct has_dynampi_mpi_type &lt;typename, typename&gt; struct has_dynampi_mpi_type&lt; U, std::void_t&lt; decltype(MPI_Type&lt; U &gt;::value)&gt; &gt; &lt;typename U&gt;"},{"location":"dynampi/mpi__types_8hpp/#macros","title":"Macros","text":"Type Name define DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (type, mpi_type) <code>/* multi line expression */</code>"},{"location":"dynampi/mpi__types_8hpp/#macro-definition-documentation","title":"Macro Definition Documentation","text":""},{"location":"dynampi/mpi__types_8hpp/#define-dynampi_define_primitive_mpi_type","title":"define DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>#define DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    type,\n    mpi_type\n) `/* multi line expression */`\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_types.hpp</code></p>"},{"location":"dynampi/mpi__types_8hpp_source/","title":"File mpi_types.hpp","text":"<p>File List &gt; dynampi &gt; mpi &gt; mpi_types.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2025 QDX Technologies\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;mpi.h&gt;\n\n#include &lt;cassert&gt;\n#include &lt;cstddef&gt;\n#include &lt;string&gt;\n#include &lt;type_traits&gt;\n#include &lt;vector&gt;\n\nnamespace dynampi {\n\ntemplate &lt;typename T, typename = void&gt;\nstruct MPI_Type {\n  static_assert(sizeof(T) == 0,\n                \"dynampi::MPI_Type&lt;T&gt; is not defined for this T. \"\n                \"Provide a specialization or use a supported primitive.\");\n};\n\n#define DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(type, mpi_type)         \\\n  template &lt;&gt;                                                     \\\n  struct MPI_Type&lt;type, void&gt; {                                   \\\n    inline static const MPI_Datatype value = mpi_type;            \\\n    inline static const bool resize_required = false;             \\\n    static int count(const type&amp;) noexcept { return 1; }          \\\n    static void resize(type&amp;, int new_size) noexcept {            \\\n      (void)new_size;                                             \\\n      assert(new_size == 1);                                      \\\n    }                                                             \\\n    static void* ptr(type&amp; t) noexcept { return &amp;t; }             \\\n    static const void* ptr(const type&amp; t) noexcept { return &amp;t; } \\\n  }\n\n// Primitives\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(char, MPI_CHAR);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(std::byte, MPI_BYTE);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(signed char, MPI_SIGNED_CHAR);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(unsigned char, MPI_UNSIGNED_CHAR);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(short, MPI_SHORT);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(unsigned short, MPI_UNSIGNED_SHORT);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(int, MPI_INT);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(unsigned int, MPI_UNSIGNED);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(long, MPI_LONG);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(unsigned long, MPI_UNSIGNED_LONG);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(long long, MPI_LONG_LONG_INT);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(unsigned long long, MPI_UNSIGNED_LONG_LONG);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(float, MPI_FLOAT);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(double, MPI_DOUBLE);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(long double, MPI_LONG_DOUBLE);\n#if defined(MPI_CXX_BOOL)\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(bool, MPI_CXX_BOOL);\n#else\n// Fallback for when MPI_CXX_BOOL is not available (e.g. Microsoft-MPI)\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(bool, MPI_C_BOOL);\n#endif\n\ntemplate &lt;&gt;\nstruct MPI_Type&lt;std::nullptr_t&gt; {\n  inline static const MPI_Datatype value = MPI_PACKED;\n  inline static const bool resize_required = false;\n\n  static int count(const std::nullptr_t&amp;) noexcept { return 0; }\n  static void resize(std::nullptr_t&amp;, int new_size) noexcept {\n    (void)new_size;  // No-op, nullptr cannot be resized\n  }\n  static void* ptr(std::nullptr_t&amp;) noexcept { return nullptr; }\n  static const void* ptr(const std::nullptr_t&amp;) noexcept { return nullptr; }\n};\n\n// Helper trait: is there a dynampi::MPI_Type&lt;U&gt; specialization?\ntemplate &lt;typename, typename = void&gt;\nstruct has_dynampi_mpi_type : std::false_type {};\ntemplate &lt;typename U&gt;\nstruct has_dynampi_mpi_type&lt;U, std::void_t&lt;decltype(MPI_Type&lt;U&gt;::value)&gt;&gt; : std::true_type {};\n\n// std::vector&lt;T&gt; specialization (contiguous storage). Excludes vector&lt;bool&gt;.\ntemplate &lt;typename T&gt;\nstruct MPI_Type&lt;std::vector&lt;T&gt;, std::enable_if_t&lt;has_dynampi_mpi_type&lt;T&gt;::value&gt;&gt; {\n  inline static const MPI_Datatype value = MPI_Type&lt;T&gt;::value;\n  inline static const bool resize_required = true;\n\n  static int count(const std::vector&lt;T&gt;&amp; vec) {\n    // Traditional MPI calls take 'int' counts; very large vectors require MPI-4 large-count APIs.\n    // Caller responsibility if vec.size() exceeds INT_MAX.\n    return static_cast&lt;int&gt;(vec.size());\n  }\n  static void resize(std::vector&lt;T&gt;&amp; vec, int new_size) {\n    vec.resize(static_cast&lt;size_t&gt;(new_size));\n  }\n  static void* ptr(std::vector&lt;T&gt;&amp; vec) noexcept { return vec.data(); }\n  static const void* ptr(const std::vector&lt;T&gt;&amp; vec) noexcept { return vec.data(); }\n\n  static_assert(!std::is_same_v&lt;bool, T&gt;,\n                \"dynampi::MPI_Type&lt;std::vector&lt;bool&gt;&gt; is not supported: \"\n                \"std::vector&lt;bool&gt; is bit-packed and not contiguous. \"\n                \"Use std::vector&lt;unsigned char&gt; or a custom container.\");\n};\n\n// std::string specialization\ntemplate &lt;&gt;\nstruct MPI_Type&lt;std::string&gt; {\n  inline static const MPI_Datatype value = MPI_CHAR;\n  inline static const bool resize_required = true;\n\n  static int count(const std::string&amp; str) { return static_cast&lt;int&gt;(str.size()); }\n  static void resize(std::string&amp; str, int new_size) { str.resize(static_cast&lt;size_t&gt;(new_size)); }\n  static void* ptr(std::string&amp; str) noexcept { return str.data(); }\n  static const void* ptr(const std::string&amp; str) noexcept { return str.data(); }\n};\n\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/dir_23e51883c93568b92bc8806003dcc116/","title":"Dir /home/runner/work/DynaMPI/DynaMPI/include/dynampi/utilities","text":"<p>FileList &gt; dynampi &gt; utilities</p>"},{"location":"dynampi/dir_23e51883c93568b92bc8806003dcc116/#files","title":"Files","text":"Type Name file assert.hpp file printing.hpp file template_options.hpp file timer.hpp <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/utilities/</code></p>"},{"location":"dynampi/assert_8hpp/","title":"File assert.hpp","text":"<p>FileList &gt; dynampi &gt; utilities &gt; assert.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;mpi.h&gt;</code></li> <li><code>#include &lt;exception&gt;</code></li> <li><code>#include &lt;iostream&gt;</code></li> <li><code>#include &lt;optional&gt;</code></li> <li><code>#include &lt;sstream&gt;</code></li> <li><code>#include &lt;stdexcept&gt;</code></li> <li><code>#include &lt;string&gt;</code></li> <li><code>#include &lt;string_view&gt;</code></li> <li><code>#include \"printing.hpp\"</code></li> </ul>"},{"location":"dynampi/assert_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi"},{"location":"dynampi/assert_8hpp/#macros","title":"Macros","text":"Type Name define DYNAMPI_ASSERT (condition, ...) <code>/* multi line expression */</code> define DYNAMPI_ASSERT_BIN_OP (a, b, op, nop, ...) <code>/* multi line expression */</code> define DYNAMPI_ASSERT_EQ (expr, val, ...) <code>[**DYNAMPI\\_ASSERT\\_BIN\\_OP**](assert_8hpp.md#define-dynampi_assert_bin_op)(expr, val, ==, !=, \\_\\_VA\\_ARGS\\_\\_)</code> define DYNAMPI_ASSERT_GE (expr, val, ...) <code>[**DYNAMPI\\_ASSERT\\_BIN\\_OP**](assert_8hpp.md#define-dynampi_assert_bin_op)(expr, val, &amp;gt;=, &amp;lt;, \\_\\_VA\\_ARGS\\_\\_)</code> define DYNAMPI_ASSERT_GT (expr, val, ...) <code>[**DYNAMPI\\_ASSERT\\_BIN\\_OP**](assert_8hpp.md#define-dynampi_assert_bin_op)(expr, val, &amp;gt;, &amp;lt;=, \\_\\_VA\\_ARGS\\_\\_)</code> define DYNAMPI_ASSERT_LE (expr, val, ...) <code>[**DYNAMPI\\_ASSERT\\_BIN\\_OP**](assert_8hpp.md#define-dynampi_assert_bin_op)(expr, val, &amp;lt;=, &amp;gt;, \\_\\_VA\\_ARGS\\_\\_)</code> define DYNAMPI_ASSERT_LT (expr, val, ...) <code>[**DYNAMPI\\_ASSERT\\_BIN\\_OP**](assert_8hpp.md#define-dynampi_assert_bin_op)(expr, val, &amp;lt;, &amp;gt;=, \\_\\_VA\\_ARGS\\_\\_)</code> define DYNAMPI_ASSERT_NE (expr, val, ...) <code>[**DYNAMPI\\_ASSERT\\_BIN\\_OP**](assert_8hpp.md#define-dynampi_assert_bin_op)(expr, val, !=, ==, \\_\\_VA\\_ARGS\\_\\_)</code> define DYNAMPI_FAIL (...) <code>/* multi line expression */</code> define DYNAMPI_HAS_BUILTIN (x) <code>\\_\\_has\\_builtin(x)</code> define DYNAMPI_UNIMPLEMENTED (...) <code>[**DYNAMPI\\_FAIL**](assert_8hpp.md#define-dynampi_fail)(\"DYNAMPI\\_UNIMPLEMENTED\")</code> define DYNAMPI_UNREACHABLE () <code>\\_\\_builtin\\_unreachable()</code>"},{"location":"dynampi/assert_8hpp/#macro-definition-documentation","title":"Macro Definition Documentation","text":""},{"location":"dynampi/assert_8hpp/#define-dynampi_assert","title":"define DYNAMPI_ASSERT","text":"<pre><code>#define DYNAMPI_ASSERT (\n    condition,\n    ...\n) `/* multi line expression */`\n</code></pre>"},{"location":"dynampi/assert_8hpp/#define-dynampi_assert_bin_op","title":"define DYNAMPI_ASSERT_BIN_OP","text":"<pre><code>#define DYNAMPI_ASSERT_BIN_OP (\n    a,\n    b,\n    op,\n    nop,\n    ...\n) `/* multi line expression */`\n</code></pre>"},{"location":"dynampi/assert_8hpp/#define-dynampi_assert_eq","title":"define DYNAMPI_ASSERT_EQ","text":"<pre><code>#define DYNAMPI_ASSERT_EQ (\n    expr,\n    val,\n    ...\n) `DYNAMPI_ASSERT_BIN_OP (expr, val, ==, !=, __VA_ARGS__)`\n</code></pre>"},{"location":"dynampi/assert_8hpp/#define-dynampi_assert_ge","title":"define DYNAMPI_ASSERT_GE","text":"<pre><code>#define DYNAMPI_ASSERT_GE (\n    expr,\n    val,\n    ...\n) `DYNAMPI_ASSERT_BIN_OP (expr, val, &gt;=, &lt;, __VA_ARGS__)`\n</code></pre>"},{"location":"dynampi/assert_8hpp/#define-dynampi_assert_gt","title":"define DYNAMPI_ASSERT_GT","text":"<pre><code>#define DYNAMPI_ASSERT_GT (\n    expr,\n    val,\n    ...\n) `DYNAMPI_ASSERT_BIN_OP (expr, val, &gt;, &lt;=, __VA_ARGS__)`\n</code></pre>"},{"location":"dynampi/assert_8hpp/#define-dynampi_assert_le","title":"define DYNAMPI_ASSERT_LE","text":"<pre><code>#define DYNAMPI_ASSERT_LE (\n    expr,\n    val,\n    ...\n) `DYNAMPI_ASSERT_BIN_OP (expr, val, &lt;=, &gt;, __VA_ARGS__)`\n</code></pre>"},{"location":"dynampi/assert_8hpp/#define-dynampi_assert_lt","title":"define DYNAMPI_ASSERT_LT","text":"<pre><code>#define DYNAMPI_ASSERT_LT (\n    expr,\n    val,\n    ...\n) `DYNAMPI_ASSERT_BIN_OP (expr, val, &lt;, &gt;=, __VA_ARGS__)`\n</code></pre>"},{"location":"dynampi/assert_8hpp/#define-dynampi_assert_ne","title":"define DYNAMPI_ASSERT_NE","text":"<pre><code>#define DYNAMPI_ASSERT_NE (\n    expr,\n    val,\n    ...\n) `DYNAMPI_ASSERT_BIN_OP (expr, val, !=, ==, __VA_ARGS__)`\n</code></pre>"},{"location":"dynampi/assert_8hpp/#define-dynampi_fail","title":"define DYNAMPI_FAIL","text":"<pre><code>#define DYNAMPI_FAIL (\n    ...\n) `DYNAMPI_ASSERT (false, __VA_ARGS__); \\ DYNAMPI_UNREACHABLE ()`\n</code></pre>"},{"location":"dynampi/assert_8hpp/#define-dynampi_has_builtin","title":"define DYNAMPI_HAS_BUILTIN","text":"<pre><code>#define DYNAMPI_HAS_BUILTIN (\n    x\n) `__has_builtin(x)`\n</code></pre>"},{"location":"dynampi/assert_8hpp/#define-dynampi_unimplemented","title":"define DYNAMPI_UNIMPLEMENTED","text":"<pre><code>#define DYNAMPI_UNIMPLEMENTED (\n    ...\n) `DYNAMPI_FAIL (\"DYNAMPI_UNIMPLEMENTED\")`\n</code></pre>"},{"location":"dynampi/assert_8hpp/#define-dynampi_unreachable","title":"define DYNAMPI_UNREACHABLE","text":"<pre><code>#define DYNAMPI_UNREACHABLE (\n\n) `__builtin_unreachable()`\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/utilities/assert.hpp</code></p>"},{"location":"dynampi/assert_8hpp_source/","title":"File assert.hpp","text":"<p>File List &gt; dynampi &gt; utilities &gt; assert.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2025 QDX Technologies. Authored by Ryan Stocks &lt;ryan.stocks00@gmail.com&gt;\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;mpi.h&gt;\n\n#include &lt;exception&gt;\n#ifndef _MSC_VER\n#define DYNAMPI_HAS_BUILTIN(x) __has_builtin(x)\n#else\n#define DYNAMPI_HAS_BUILTIN(x) 0\n#endif\n\n#ifndef NDEBUG\n#include &lt;iostream&gt;\n#include &lt;optional&gt;\n\n// cppcheck-suppress preprocessorErrorDirective\n#if __has_include(&lt;source_location&gt;)\n#include &lt;source_location&gt;\n#if defined(__cpp_lib_source_location)\n#define DYNAMPI_HAS_SOURCE_LOCATION\n#endif\n#elif __has_include(&lt;experimental/source_location&gt;)\n#include &lt;experimental/source_location&gt;\nnamespace std {\nusing source_location = std::experimental::source_location;\n}\n#define DYNAMPI_HAS_SOURCE_LOCATION\n#endif\n\n#include &lt;sstream&gt;\n#include &lt;stdexcept&gt;\n#include &lt;string&gt;\n#include &lt;string_view&gt;\n\n#include \"printing.hpp\"\n#endif\n\nnamespace dynampi {\n\n#ifndef NDEBUG\ntemplate &lt;typename... Args&gt;\nstd::optional&lt;std::string&gt; OptionalString(Args &amp;&amp;...args) {\n  if constexpr (sizeof...(args) == 0) {\n    return std::nullopt;\n  } else {\n    std::stringstream ss;\n    (ss &lt;&lt; ... &lt;&lt; args);\n    return ss.str();\n  }\n}\n\n#define DYNAMPI_ASSERT(condition, ...)                                                 \\\n  do {                                                                                 \\\n    if (!(condition))                                                                  \\\n      dynampi::_DYNAMPI_FAIL_ASSERT(#condition, dynampi::OptionalString(__VA_ARGS__)); \\\n  } while (false)\n\ninline void _DYNAMPI_FAIL_ASSERT(const std::string &amp;condition_str,\n                                 const std::optional&lt;std::string&gt; &amp;message\n#ifdef DYNAMPI_HAS_SOURCE_LOCATION\n                                 ,\n                                 const std::source_location &amp;loc = std::source_location::current()\n#endif\n) {\n  if (!std::uncaught_exceptions()) {\n    std::stringstream ss;\n#ifdef DYNAMPI_HAS_SOURCE_LOCATION\n    std::string_view s = loc.file_name();\n    std::string_view filename = s.substr(s.find_last_of('/') + 1);\n#endif\n    int rank = 0;\n    MPI_Comm_rank(MPI_COMM_WORLD, &amp;rank);\n    ss &lt;&lt; \"DynaMPI assertion failed on rank \" &lt;&lt; rank &lt;&lt; \": \" &lt;&lt; condition_str\n       &lt;&lt; (message ? \" \" + *message : \"\")\n\n#ifdef DYNAMPI_HAS_SOURCE_LOCATION\n       &lt;&lt; \"\\n in \" &lt;&lt; loc.function_name() &lt;&lt; \" at \" &lt;&lt; filename &lt;&lt; \":\" &lt;&lt; loc.line()\n#endif\n       &lt;&lt; std::endl;\n    std::cerr &lt;&lt; ss.str();\n    throw std::runtime_error(ss.str());\n  }\n}\n\n#define DYNAMPI_ASSERT_BIN_OP(a, b, op, nop, ...)                        \\\n  do {                                                                   \\\n    const auto A = a;                                                    \\\n    const auto B = b;                                                    \\\n    if (!((A)op(B)))                                                     \\\n      dynampi::_DYNAMPI_FAILBinOp((A), (B), (#a), (#b), (#nop),          \\\n                                  dynampi::OptionalString(__VA_ARGS__)); \\\n  } while (false)\n\ntemplate &lt;typename A, typename B&gt;\ninline void _DYNAMPI_FAILBinOp(const A &amp;a, const B &amp;b, const std::string &amp;a_str,\n                               const std::string &amp;b_str, const std::string &amp;nop,\n                               const std::optional&lt;std::string&gt; &amp;message\n#ifdef DYNAMPI_HAS_SOURCE_LOCATION\n                               ,\n                               const std::source_location &amp;loc = std::source_location::current()\n#endif\n) {\n  std::stringstream ss;\n  ss &lt;&lt; a &lt;&lt; \" \" &lt;&lt; nop &lt;&lt; \" \" &lt;&lt; b;\n  dynampi::_DYNAMPI_FAIL_ASSERT(a_str + \" \" + nop + \" \" + b_str,\n                                message ? (ss.str() + \" \" + *message) : ss.str()\n#ifdef DYNAMPI_HAS_SOURCE_LOCATION\n                                    ,\n                                loc\n#endif\n  );\n}\n\n#else\n#define DYNAMPI_ASSERT(condition, ...) \\\n  do {                                 \\\n  } while (false)\n#define DYNAMPI_ASSERT_BIN_OP(a, b, op, nop, ...) \\\n  do {                                            \\\n  } while (false)\n#endif\n\n#define DYNAMPI_FAIL(...)             \\\n  DYNAMPI_ASSERT(false, __VA_ARGS__); \\\n  DYNAMPI_UNREACHABLE()  // LCOV_EXCL_LINE\n\n#define DYNAMPI_UNIMPLEMENTED(...) DYNAMPI_FAIL(\"DYNAMPI_UNIMPLEMENTED\")\n\n#define DYNAMPI_ASSERT_GE(expr, val, ...) DYNAMPI_ASSERT_BIN_OP(expr, val, &gt;=, &lt;, __VA_ARGS__)\n#define DYNAMPI_ASSERT_LE(expr, val, ...) DYNAMPI_ASSERT_BIN_OP(expr, val, &lt;=, &gt;, __VA_ARGS__)\n#define DYNAMPI_ASSERT_GT(expr, val, ...) DYNAMPI_ASSERT_BIN_OP(expr, val, &gt;, &lt;=, __VA_ARGS__)\n#define DYNAMPI_ASSERT_LT(expr, val, ...) DYNAMPI_ASSERT_BIN_OP(expr, val, &lt;, &gt;=, __VA_ARGS__)\n#define DYNAMPI_ASSERT_EQ(expr, val, ...) DYNAMPI_ASSERT_BIN_OP(expr, val, ==, !=, __VA_ARGS__)\n#define DYNAMPI_ASSERT_NE(expr, val, ...) DYNAMPI_ASSERT_BIN_OP(expr, val, !=, ==, __VA_ARGS__)\n\n#if defined(_MSC_VER) &amp;&amp; !defined(__clang__)  // MSVC\n#define DYNAMPI_UNREACHABLE() __assume(false)\n#else  // GCC, Clang\n#define DYNAMPI_UNREACHABLE() __builtin_unreachable()\n#endif\n\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/printing_8hpp/","title":"File printing.hpp","text":"<p>FileList &gt; dynampi &gt; utilities &gt; printing.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;array&gt;</code></li> <li><code>#include &lt;cstddef&gt;</code></li> <li><code>#include &lt;cstdint&gt;</code></li> <li><code>#include &lt;iostream&gt;</code></li> <li><code>#include &lt;optional&gt;</code></li> <li><code>#include &lt;set&gt;</code></li> <li><code>#include &lt;span&gt;</code></li> <li><code>#include &lt;tuple&gt;</code></li> <li><code>#include &lt;utility&gt;</code></li> <li><code>#include &lt;vector&gt;</code></li> </ul>"},{"location":"dynampi/printing_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/utilities/printing.hpp</code></p>"},{"location":"dynampi/printing_8hpp_source/","title":"File printing.hpp","text":"<p>File List &gt; dynampi &gt; utilities &gt; printing.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2025 QDX Technologies. Authored by Ryan Stocks &lt;ryan.stocks00@gmail.com&gt;\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;array&gt;\n#include &lt;cstddef&gt;\n#include &lt;cstdint&gt;\n#include &lt;iostream&gt;\n#include &lt;optional&gt;\n#include &lt;set&gt;\n#include &lt;span&gt;\n#include &lt;tuple&gt;\n#include &lt;utility&gt;\n#include &lt;vector&gt;\n\nnamespace dynampi {\n\ntemplate &lt;typename T&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::set&lt;T&gt;&amp; set);\ntemplate &lt;typename T&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::vector&lt;T&gt;&amp; vec);\ntemplate &lt;typename T, std::size_t N&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::array&lt;T, N&gt;&amp; arr);\ntemplate &lt;typename T&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::span&lt;T&gt;&amp; vec);\ntemplate &lt;typename T&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::optional&lt;T&gt;&amp; op);\ntemplate &lt;typename... Args&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::tuple&lt;Args...&gt;&amp; tup);\ntemplate &lt;typename T, typename U&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::pair&lt;T, U&gt;&amp; pair);\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::byte&amp; b);\n\n// --------------- IMPLEMENTATIONS ---------------\n\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::byte&amp; b) {\n  return os &lt;&lt; static_cast&lt;uint32_t&gt;(b);\n}\n\ntemplate &lt;typename T&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::span&lt;T&gt;&amp; vec) {\n  os &lt;&lt; \"[\";\n  for (std::size_t i = 0; i &lt; vec.size(); i++) {\n    os &lt;&lt; vec[i];\n    if (i &lt; vec.size() - 1) {\n      os &lt;&lt; \", \";\n    }\n  }\n  return os &lt;&lt; \"]\";\n}\n\ntemplate &lt;typename T&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::vector&lt;T&gt;&amp; vec) {\n  return os &lt;&lt; std::span&lt;const T&gt;(vec.data(), vec.size());\n}\n\ntemplate &lt;typename T, std::size_t N&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::array&lt;T, N&gt;&amp; arr) {\n  os &lt;&lt; \"[\";\n  for (std::size_t i = 0; i &lt; arr.size(); i++) {\n    os &lt;&lt; arr[i];\n    if (i &lt; arr.size() - 1) {\n      os &lt;&lt; \", \";\n    }\n  }\n  return os &lt;&lt; \"]\";\n}\n\ntemplate &lt;typename T&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::set&lt;T&gt;&amp; set) {\n  os &lt;&lt; \"{\";\n  auto it = set.begin();\n  while (it != set.end()) {\n    os &lt;&lt; *it;\n    ++it;\n    if (it != set.end()) {\n      os &lt;&lt; \", \";\n    }\n  }\n  return os &lt;&lt; \"}\";\n}\n\ntemplate &lt;typename T, typename U&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::pair&lt;T, U&gt;&amp; pair) {\n  return os &lt;&lt; \"(\" &lt;&lt; pair.first &lt;&lt; \", \" &lt;&lt; pair.second &lt;&lt; \")\";\n}\n\ntemplate &lt;typename T&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::optional&lt;T&gt;&amp; op) {\n  if (op.has_value()) {\n    return os &lt;&lt; \"Some(\" &lt;&lt; op.value() &lt;&lt; \")\";\n  }\n  return os &lt;&lt; \"None\";\n}\n\ntemplate &lt;typename... Args&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::tuple&lt;Args...&gt;&amp; tup) {\n  os &lt;&lt; \"(\";\n  std::apply(\n      [&amp;os](const Args&amp;... args) {\n        std::size_t i = 0;\n        ((os &lt;&lt; args &lt;&lt; (++i &lt; sizeof...(Args) ? \", \" : \"\")), ...);\n      },\n      tup);\n  return os &lt;&lt; \")\";\n}\n\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/template__options_8hpp/","title":"File template_options.hpp","text":"<p>FileList &gt; dynampi &gt; utilities &gt; template_options.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;type_traits&gt;</code></li> </ul>"},{"location":"dynampi/template__options_8hpp/#classes","title":"Classes","text":"Type Name struct option_value &lt;typename Option, Options&gt; struct option_value&lt; Option, Head, Tail... &gt; &lt;typename Option, typename Head, Tail&gt;"},{"location":"dynampi/template__options_8hpp/#public-functions","title":"Public Functions","text":"Type Name consteval decltype(Option::value) get_option_value ()"},{"location":"dynampi/template__options_8hpp/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/template__options_8hpp/#function-get_option_value","title":"function get_option_value","text":"<pre><code>template&lt;typename Option, typename... Options&gt;\nconsteval decltype(Option::value) get_option_value () \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/utilities/template_options.hpp</code></p>"},{"location":"dynampi/template__options_8hpp_source/","title":"File template_options.hpp","text":"<p>File List &gt; dynampi &gt; utilities &gt; template_options.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2025 QDX Technologies. Authored by Ryan Stocks &lt;ryan.stocks00@gmail.com&gt;\n * SPDX-License-Identifier: Apache-2.0\n */\n#pragma once\n\n#include &lt;type_traits&gt;\n\ntemplate &lt;typename Option, typename... Options&gt;\nstruct option_value {\n  static constexpr decltype(Option::value) value = Option::value;\n};\n\ntemplate &lt;typename Option, typename Head, typename... Tail&gt;\nstruct option_value&lt;Option, Head, Tail...&gt; {\n  static constexpr decltype(Option::value) value = [] {\n    if constexpr (std::is_base_of_v&lt;Option, Head&gt;) {\n      // Use Head\u2019s value only if it really is derived from Option\n      return static_cast&lt;decltype(Option::value)&gt;(Head::value);\n    } else {\n      return option_value&lt;Option, Tail...&gt;::value;\n    }\n  }();\n};\n\ntemplate &lt;typename Option, typename... Options&gt;\nconsteval decltype(Option::value) get_option_value() {\n  return option_value&lt;Option, Options...&gt;::value;\n}\n</code></pre>"},{"location":"dynampi/timer_8hpp/","title":"File timer.hpp","text":"<p>FileList &gt; dynampi &gt; utilities &gt; timer.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;cassert&gt;</code></li> <li><code>#include &lt;chrono&gt;</code></li> <li><code>#include &lt;optional&gt;</code></li> <li><code>#include &lt;ostream&gt;</code></li> </ul>"},{"location":"dynampi/timer_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi"},{"location":"dynampi/timer_8hpp/#classes","title":"Classes","text":"Type Name class Timer <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/utilities/timer.hpp</code></p>"},{"location":"dynampi/timer_8hpp_source/","title":"File timer.hpp","text":"<p>File List &gt; dynampi &gt; utilities &gt; timer.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2025 QDX Technologies. Authored by Ryan Stocks &lt;ryan.stocks00@gmail.com&gt;\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;cassert&gt;\n#include &lt;chrono&gt;\n#include &lt;optional&gt;\n#include &lt;ostream&gt;\n\nnamespace dynampi {\n\nclass Timer {\n  std::optional&lt;std::chrono::time_point&lt;std::chrono::high_resolution_clock&gt;&gt; _start_time;\n  std::chrono::nanoseconds _elapsed_time{0};\n\n public:\n  enum class AutoStart { Yes, No };\n\n  Timer(AutoStart auto_start = AutoStart::Yes) {\n    if (auto_start == AutoStart::Yes) {\n      start();\n    }\n  }\n\n  void start() {\n    assert(!_start_time.has_value() &amp;&amp; \"Timer already started\");\n    _start_time = std::chrono::high_resolution_clock::now();\n  }\n\n  std::chrono::duration&lt;double&gt; stop() {\n    assert(_start_time.has_value() &amp;&amp; \"Timer not started\");\n    auto end_time = std::chrono::high_resolution_clock::now();\n    _elapsed_time +=\n        std::chrono::duration_cast&lt;std::chrono::nanoseconds&gt;(end_time - _start_time.value());\n    _start_time.reset();\n    return std::chrono::duration&lt;double&gt;(_elapsed_time);\n  }\n\n  void reset(AutoStart auto_start = AutoStart::Yes) {\n    _start_time.reset();\n    _elapsed_time = std::chrono::nanoseconds{0};\n    if (auto_start == AutoStart::Yes) {\n      start();\n    }\n  }\n\n  [[nodiscard]] std::chrono::duration&lt;double&gt; elapsed() const {\n    if (_start_time.has_value()) {\n      auto current_elapsed =\n          _elapsed_time + std::chrono::duration_cast&lt;std::chrono::nanoseconds&gt;(\n                              std::chrono::high_resolution_clock::now() - _start_time.value());\n      return std::chrono::duration&lt;double&gt;(current_elapsed);\n    }\n    return std::chrono::duration&lt;double&gt;(_elapsed_time);\n  }\n\n  friend std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const Timer&amp; timer) {\n    return os &lt;&lt; timer.elapsed().count() &lt;&lt; \" seconds\";\n  }\n};\n\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/dynampi_8hpp/","title":"File dynampi.hpp","text":"<p>FileList &gt; dynampi &gt; dynampi.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;mpi.h&gt;</code></li> <li><code>#include &lt;cassert&gt;</code></li> <li><code>#include &lt;functional&gt;</code></li> <li><code>#include &lt;optional&gt;</code></li> <li><code>#include &lt;string_view&gt;</code></li> <li><code>#include &lt;tuple&gt;</code></li> <li><code>#include &lt;vector&gt;</code></li> <li><code>#include \"dynampi/impl/hierarchical_distributor.hpp\"</code></li> <li><code>#include \"dynampi/impl/hierarchical_lockfree_distributor.hpp\"</code></li> <li><code>#include \"dynampi/impl/lockfree_distributor.hpp\"</code></li> <li><code>#include \"dynampi/impl/naive_distributor.hpp\"</code></li> </ul>"},{"location":"dynampi/dynampi_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi namespace version"},{"location":"dynampi/dynampi_8hpp/#macros","title":"Macros","text":"Type Name define DYNAMPI_STR (x) <code>[**DYNAMPI\\_STR\\_HELPER**](dynampi_8hpp.md#define-dynampi_str_helper)(x)</code> define DYNAMPI_STR_HELPER (x) <code>#x</code> define DYNAMPI_VERSION_STRING <code>/* multi line expression */</code>"},{"location":"dynampi/dynampi_8hpp/#macro-definition-documentation","title":"Macro Definition Documentation","text":""},{"location":"dynampi/dynampi_8hpp/#define-dynampi_str","title":"define DYNAMPI_STR","text":"<pre><code>#define DYNAMPI_STR (\n    x\n) `DYNAMPI_STR_HELPER (x)`\n</code></pre>"},{"location":"dynampi/dynampi_8hpp/#define-dynampi_str_helper","title":"define DYNAMPI_STR_HELPER","text":"<pre><code>#define DYNAMPI_STR_HELPER (\n    x\n) `#x`\n</code></pre>"},{"location":"dynampi/dynampi_8hpp/#define-dynampi_version_string","title":"define DYNAMPI_VERSION_STRING","text":"<pre><code>#define DYNAMPI_VERSION_STRING `/* multi line expression */`\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/dynampi.hpp</code></p>"},{"location":"dynampi/dynampi_8hpp_source/","title":"File dynampi.hpp","text":"<p>File List &gt; dynampi &gt; dynampi.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2025 QDX Technologies. Authored by Ryan Stocks &lt;ryan.stocks00@gmail.com&gt;\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;mpi.h&gt;\n\n#include &lt;cassert&gt;\n#include &lt;functional&gt;\n#include &lt;optional&gt;\n#include &lt;string_view&gt;\n#include &lt;tuple&gt;\n#include &lt;vector&gt;\n\n#include \"dynampi/impl/hierarchical_distributor.hpp\"\n#include \"dynampi/impl/hierarchical_lockfree_distributor.hpp\"\n#include \"dynampi/impl/lockfree_distributor.hpp\"\n#include \"dynampi/impl/naive_distributor.hpp\"\n\nnamespace dynampi {\n\nnamespace version {\n\ninline constexpr int major = DYNAMPI_VERSION_MAJOR;\ninline constexpr int minor = DYNAMPI_VERSION_MINOR;\ninline constexpr int patch = DYNAMPI_VERSION_PATCH;\n\n// Macros for compile-time version string\n#define DYNAMPI_STR_HELPER(x) #x\n#define DYNAMPI_STR(x) DYNAMPI_STR_HELPER(x)\n#define DYNAMPI_VERSION_STRING                                                                   \\\n  \"v\" DYNAMPI_STR(DYNAMPI_VERSION_MAJOR) \".\" DYNAMPI_STR(DYNAMPI_VERSION_MINOR) \".\" DYNAMPI_STR( \\\n      DYNAMPI_VERSION_PATCH)\n\ninline constexpr std::string_view string = DYNAMPI_VERSION_STRING;\n\n[[nodiscard]] constexpr bool is_at_least(int v_major, int v_minor, int v_patch) {\n  return std::tie(major, minor, patch) &gt;= std::tie(v_major, v_minor, v_patch);\n}\n\n[[nodiscard]] inline constexpr std::string_view compile_date() { return __DATE__ \" \" __TIME__; }\n\n[[nodiscard]] inline constexpr std::string_view commit_hash() { return DYNAMPI_COMMIT_HASH; }\n\n}  // namespace version\n\ntemplate &lt;typename ResultT,\n          template &lt;typename, typename, typename...&gt; typename T = HierarchicalMPIWorkDistributor&gt;\nstd::optional&lt;std::vector&lt;ResultT&gt;&gt; mpi_manager_worker_distribution(\n    size_t n_tasks, std::function&lt;ResultT(size_t)&gt; worker_function, MPI_Comm comm = MPI_COMM_WORLD,\n    int manager_rank = 0) {\n  T&lt;size_t, ResultT&gt; distributor(worker_function, {.comm = comm, .manager_rank = manager_rank});\n  if (distributor.is_root_manager()) {\n    for (size_t i = 0; i &lt; n_tasks; ++i) {\n      distributor.insert_task(i);\n    }\n    return distributor.finish_remaining_tasks();\n  }\n  return {};\n}\n\ntemplate &lt;typename TaskT, typename ResultT, typename... Options&gt;\nusing MPIDynamicWorkDistributor = HierarchicalMPIWorkDistributor&lt;TaskT, ResultT, Options...&gt;;\n\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/namespaces/","title":"Namespace List","text":"<p>Here is a list of all namespaces with brief descriptions:</p> <ul> <li>namespace dynampi <ul> <li>namespace detail </li> <li>namespace version </li> </ul> </li> <li>namespace std </li> </ul>"},{"location":"dynampi/classes/","title":"Class Index","text":""},{"location":"dynampi/classes/#a","title":"a","text":"<ul> <li>AsyncPutLevel (dynampi::detail)</li> <li>AsyncPutLockFreeMPIWorkDistributor (dynampi)</li> <li>AsyncSendPool</li> </ul>"},{"location":"dynampi/classes/#b","title":"b","text":"<ul> <li>BaseMPIWorkDistributor (dynampi)</li> <li>BridgeHop</li> </ul>"},{"location":"dynampi/classes/#c","title":"c","text":"<ul> <li>ClaimedRange (dynampi::detail::AsyncPutLevel)</li> <li>CommStatistics (dynampi)</li> <li>Config (dynampi::AsyncPutLockFreeMPIWorkDistributor)</li> <li>Config (dynampi::BaseMPIWorkDistributor)</li> <li>Config (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor)</li> <li>Config (dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>Config (dynampi::HierarchicalMPIWorkDistributor)</li> <li>Config (dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>Config (dynampi::LockFreeMPIWorkDistributor)</li> <li>Config (dynampi::MinimalLockFreeMPIWorkDistributor)</li> <li>Config (dynampi::NaiveMPIWorkDistributor)</li> <li>Config (dynampi::detail::AsyncPutLevel)</li> <li>Config (dynampi::detail::LockFreeLevel)</li> </ul>"},{"location":"dynampi/classes/#e","title":"e","text":"<ul> <li>enable_prioritization (dynampi)</li> </ul>"},{"location":"dynampi/classes/#h","title":"h","text":"<ul> <li>has_dynampi_mpi_type (dynampi)</li> <li>has_dynampi_mpi_type&lt; U, std::void_t&lt; decltype(MPI_Type&lt; U &gt;::value)&gt; &gt; (dynampi)</li> <li>HierarchicalAsyncPutLockFreeMPIWorkDistributor (dynampi)</li> <li>HierarchicalLockFreeMPIWorkDistributor (dynampi)</li> <li>HierarchicalMPIWorkDistributor (dynampi)</li> <li>HierarchicalNonBlockingMPIWorkDistributor (dynampi)</li> </ul>"},{"location":"dynampi/classes/#l","title":"l","text":"<ul> <li>LockFreeLevel (dynampi::detail)</li> <li>LockFreeMPIWorkDistributor (dynampi)</li> </ul>"},{"location":"dynampi/classes/#m","title":"m","text":"<ul> <li>MinimalLockFreeMPIWorkDistributor (dynampi)</li> <li>MPI_Type (dynampi)</li> <li>MPI_Type&lt; std::nullptr_t &gt; (dynampi)</li> <li>MPI_Type&lt; std::string &gt; (dynampi)</li> <li>MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt; (dynampi)</li> <li>MPICommunicator (dynampi)</li> <li>MPIGroup (dynampi)</li> </ul>"},{"location":"dynampi/classes/#n","title":"n","text":"<ul> <li>NaiveMPIWorkDistributor (dynampi)</li> </ul>"},{"location":"dynampi/classes/#o","title":"o","text":"<ul> <li>option_value</li> <li>option_value&lt; Option, Head, Tail... &gt;</li> </ul>"},{"location":"dynampi/classes/#p","title":"p","text":"<ul> <li>PendingRelay</li> <li>prioritize_tasks_t (dynampi)</li> </ul>"},{"location":"dynampi/classes/#r","title":"r","text":"<ul> <li>RunConfig (dynampi::AsyncPutLockFreeMPIWorkDistributor)</li> <li>RunConfig (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor)</li> <li>RunConfig (dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>RunConfig (dynampi::HierarchicalMPIWorkDistributor)</li> <li>RunConfig (dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>RunConfig (dynampi::LockFreeMPIWorkDistributor)</li> <li>RunConfig (dynampi::NaiveMPIWorkDistributor)</li> </ul>"},{"location":"dynampi/classes/#s","title":"s","text":"<ul> <li>Statistics (dynampi::AsyncPutLockFreeMPIWorkDistributor)</li> <li>Statistics (dynampi::LockFreeMPIWorkDistributor)</li> <li>Statistics (dynampi::NaiveMPIWorkDistributor)</li> <li>Statistics</li> <li>Statistics</li> </ul>"},{"location":"dynampi/classes/#t","title":"t","text":"<ul> <li>TaskRequest</li> <li>TaskRequest</li> <li>Timer (dynampi)</li> <li>track_statistics (dynampi)</li> <li>track_statistics_t (dynampi)</li> </ul>"},{"location":"dynampi/hierarchy/","title":"Class Hierarchy","text":"<p>This inheritance list is sorted roughly, but not completely, alphabetically:</p> <ul> <li>class dynampi::AsyncPutLockFreeMPIWorkDistributor </li> <li>class dynampi::BaseMPIWorkDistributor </li> <li>class dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor </li> <li>class dynampi::HierarchicalLockFreeMPIWorkDistributor </li> <li>class dynampi::LockFreeMPIWorkDistributor </li> <li>class dynampi::MPICommunicator </li> <li>class dynampi::MPIGroup </li> <li>class dynampi::MinimalLockFreeMPIWorkDistributor </li> <li>class dynampi::NaiveMPIWorkDistributor </li> <li>class dynampi::Timer </li> <li>class dynampi::detail::AsyncPutLevel </li> <li>class dynampi::detail::LockFreeLevel </li> <li>class dynampi::HierarchicalNonBlockingMPIWorkDistributor::AsyncSendPool </li> <li>struct dynampi::AsyncPutLockFreeMPIWorkDistributor::Config </li> <li>struct dynampi::AsyncPutLockFreeMPIWorkDistributor::RunConfig </li> <li>struct dynampi::AsyncPutLockFreeMPIWorkDistributor::Statistics </li> <li>struct dynampi::BaseMPIWorkDistributor::Config </li> <li>struct dynampi::CommStatistics </li> <li>struct dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::Config </li> <li>struct dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::RunConfig </li> <li>struct dynampi::HierarchicalLockFreeMPIWorkDistributor::Config </li> <li>struct dynampi::HierarchicalLockFreeMPIWorkDistributor::RunConfig </li> <li>struct dynampi::HierarchicalMPIWorkDistributor::Config </li> <li>struct dynampi::HierarchicalMPIWorkDistributor::RunConfig </li> <li>struct dynampi::HierarchicalNonBlockingMPIWorkDistributor::Config </li> <li>struct dynampi::HierarchicalNonBlockingMPIWorkDistributor::RunConfig </li> <li>struct dynampi::LockFreeMPIWorkDistributor::Config </li> <li>struct dynampi::LockFreeMPIWorkDistributor::RunConfig </li> <li>struct dynampi::LockFreeMPIWorkDistributor::Statistics </li> <li>struct dynampi::MPI_Type </li> <li>struct dynampi::MPI_Type&lt; std::nullptr_t &gt; </li> <li>struct dynampi::MPI_Type&lt; std::string &gt; </li> <li>struct dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt; </li> <li>struct dynampi::MinimalLockFreeMPIWorkDistributor::Config </li> <li>struct dynampi::NaiveMPIWorkDistributor::Config </li> <li>struct dynampi::NaiveMPIWorkDistributor::RunConfig </li> <li>struct dynampi::NaiveMPIWorkDistributor::Statistics </li> <li>struct dynampi::detail::AsyncPutLevel::ClaimedRange </li> <li>struct dynampi::detail::AsyncPutLevel::Config </li> <li>struct dynampi::detail::LockFreeLevel::Config </li> <li>struct dynampi::prioritize_tasks_t <ul> <li>struct dynampi::enable_prioritization </li> </ul> </li> <li>struct dynampi::track_statistics_t <ul> <li>struct dynampi::track_statistics </li> </ul> </li> <li>struct dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::BridgeHop </li> <li>struct dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::PendingRelay </li> <li>struct dynampi::HierarchicalMPIWorkDistributor::Statistics </li> <li>struct dynampi::HierarchicalMPIWorkDistributor::TaskRequest </li> <li>struct dynampi::HierarchicalNonBlockingMPIWorkDistributor::Statistics </li> <li>struct dynampi::HierarchicalNonBlockingMPIWorkDistributor::TaskRequest </li> <li>struct option_value </li> <li>struct option_value&lt; Option, Head, Tail... &gt; </li> <li>class std::false_type <ul> <li>struct dynampi::has_dynampi_mpi_type </li> </ul> </li> <li>class std::true_type <ul> <li>struct dynampi::has_dynampi_mpi_type&lt; U, std::void_t&lt; decltype(MPI_Type&lt; U &gt;::value)&gt; &gt; </li> </ul> </li> </ul>"},{"location":"dynampi/modules/","title":"Modules","text":"<p>No modules found.</p>"},{"location":"dynampi/pages/","title":"Related Pages","text":"<p>Here is a list of all related documentation pages:</p>"},{"location":"dynampi/class_members/","title":"Class Members","text":""},{"location":"dynampi/class_members/#a","title":"a","text":"<ul> <li>AsyncPutLockFreeMPIWorkDistributor (dynampi::AsyncPutLockFreeMPIWorkDistributor)</li> <li>atomic_read (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>atomic_set (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>auto_run_workers (dynampi::AsyncPutLockFreeMPIWorkDistributor::Config, dynampi::BaseMPIWorkDistributor::Config, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalMPIWorkDistributor::Config, dynampi::HierarchicalNonBlockingMPIWorkDistributor::Config, dynampi::LockFreeMPIWorkDistributor::Config, dynampi::NaiveMPIWorkDistributor::Config)</li> <li>allow_more_than_target_tasks (dynampi::AsyncPutLockFreeMPIWorkDistributor::RunConfig, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::RunConfig, dynampi::HierarchicalLockFreeMPIWorkDistributor::RunConfig, dynampi::HierarchicalMPIWorkDistributor::RunConfig, dynampi::HierarchicalNonBlockingMPIWorkDistributor::RunConfig, dynampi::LockFreeMPIWorkDistributor::RunConfig, dynampi::NaiveMPIWorkDistributor::RunConfig)</li> <li>average_receive_size (dynampi::CommStatistics)</li> <li>average_send_size (dynampi::CommStatistics)</li> <li>allocate_task_to_child (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>available (dynampi::LockFreeMPIWorkDistributor)</li> <li>active_worker_count (dynampi::NaiveMPIWorkDistributor)</li> <li>AutoStart (dynampi::Timer)</li> <li>AsyncPutLevel (dynampi::detail::AsyncPutLevel)</li> <li>advance_gather_round (dynampi::detail::LockFreeLevel)</li> </ul>"},{"location":"dynampi/class_members/#b","title":"b","text":"<ul> <li>bytes_received (dynampi::CommStatistics)</li> <li>bytes_sent (dynampi::CommStatistics)</li> <li>bridge_hop_done (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor)</li> <li>build_upper_hops (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor)</li> <li>Base (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>batch_size_multiplier (dynampi::HierarchicalMPIWorkDistributor::Config, dynampi::HierarchicalNonBlockingMPIWorkDistributor::Config)</li> <li>broadcast (dynampi::MPICommunicator)</li> <li>broadcast_done (dynampi::NaiveMPIWorkDistributor)</li> <li>BridgeHop (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::BridgeHop)</li> </ul>"},{"location":"dynampi/class_members/#c","title":"c","text":"<ul> <li>CONTROL_BYTES (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>Comm (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor)</li> <li>claim_batch_size (dynampi::AsyncPutLockFreeMPIWorkDistributor::Config, dynampi::LockFreeMPIWorkDistributor::Config, dynampi::detail::AsyncPutLevel, dynampi::detail::AsyncPutLevel::Config)</li> <li>comm (dynampi::AsyncPutLockFreeMPIWorkDistributor::Config, dynampi::BaseMPIWorkDistributor::Config, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalMPIWorkDistributor::Config, dynampi::HierarchicalNonBlockingMPIWorkDistributor::Config, dynampi::LockFreeMPIWorkDistributor::Config, dynampi::MinimalLockFreeMPIWorkDistributor::Config, dynampi::NaiveMPIWorkDistributor::Config, dynampi::detail::AsyncPutLevel::Config, dynampi::detail::LockFreeLevel::Config)</li> <li>comm_statistics (dynampi::AsyncPutLockFreeMPIWorkDistributor::Statistics, dynampi::LockFreeMPIWorkDistributor::Statistics, dynampi::NaiveMPIWorkDistributor::Statistics, dynampi::HierarchicalMPIWorkDistributor::Statistics, dynampi::HierarchicalNonBlockingMPIWorkDistributor::Statistics)</li> <li>collective_count (dynampi::CommStatistics)</li> <li>CommLayer (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>create_statistics (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>coordinator_per_node (dynampi::HierarchicalMPIWorkDistributor::Config, dynampi::HierarchicalNonBlockingMPIWorkDistributor::Config)</li> <li>contains_rank_in_group (dynampi::MPIGroup)</li> <li>count (dynampi::MPI_Type&lt; std::nullptr_t &gt;, dynampi::MPI_Type&lt; std::string &gt;, dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;)</li> <li>collect_available_results (dynampi::NaiveMPIWorkDistributor)</li> <li>check_finished (dynampi::detail::AsyncPutLevel)</li> <li>comm_rank (dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>comm_size (dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>compare_and_swap (dynampi::detail::LockFreeLevel)</li> <li>child (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::BridgeHop)</li> <li>child_len (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::PendingRelay)</li> </ul>"},{"location":"dynampi/class_members/#d","title":"d","text":"<ul> <li>drain_results (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor)</li> <li>determine_layer_from_world_rank (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>drained (dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>deserialize_results (dynampi::detail::LockFreeLevel)</li> </ul>"},{"location":"dynampi/class_members/#e","title":"e","text":"<ul> <li>emplace_owned_upper_level (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor)</li> <li>emplace_parent_level (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor)</li> <li>exchange_gathered_results (dynampi::LockFreeMPIWorkDistributor)</li> <li>ensure_result_capacity (dynampi::NaiveMPIWorkDistributor)</li> <li>elapsed (dynampi::Timer)</li> </ul>"},{"location":"dynampi/class_members/#f","title":"f","text":"<ul> <li>FINISHED_OFF (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>fetch_add (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::MinimalLockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>finalize (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>finish_remaining_tasks (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>flush (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>finish_marked (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::BridgeHop)</li> </ul>"},{"location":"dynampi/class_members/#g","title":"g","text":"<ul> <li>gather_once (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor)</li> <li>get_bytes (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>get_statistics (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::MPICommunicator, dynampi::NaiveMPIWorkDistributor)</li> <li>get_next_task_to_send (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>get_parent_target (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>GATHER_SEQ_OFF (dynampi::LockFreeMPIWorkDistributor)</li> <li>gather (dynampi::MPICommunicator)</li> <li>get (dynampi::MPICommunicator, dynampi::MPIGroup)</li> <li>get_group (dynampi::MPICommunicator)</li> <li>gather_sorted (dynampi::MinimalLockFreeMPIWorkDistributor)</li> <li>GatherStage (dynampi::detail::LockFreeLevel)</li> <li>gather_fully_done (dynampi::detail::LockFreeLevel)</li> </ul>"},{"location":"dynampi/class_members/#h","title":"h","text":"<ul> <li>HEAD_OFF (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>harvest_ready_results (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>HierarchicalAsyncPutLockFreeMPIWorkDistributor (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor)</li> <li>HierarchicalLockFreeMPIWorkDistributor (dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>HierarchicalMPIWorkDistributor (dynampi::HierarchicalMPIWorkDistributor)</li> <li>HierarchicalNonBlockingMPIWorkDistributor (dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>handle_result_message (dynampi::NaiveMPIWorkDistributor)</li> <li>harvest_ready_results_throttled (dynampi::detail::AsyncPutLevel)</li> <li>has_pending_results (dynampi::detail::LockFreeLevel)</li> </ul>"},{"location":"dynampi/class_members/#i","title":"i","text":"<ul> <li>initialize_window (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>insert_task (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>insert_tasks (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>is_root_manager (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::MinimalLockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>idx_for_worker (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>is_leaf_worker (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>iprobe (dynampi::MPICommunicator)</li> <li>idle_wait (dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>is_owner (dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> </ul>"},{"location":"dynampi/class_members/#k","title":"k","text":"<ul> <li>kMaxRmaChunkBytes (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>kMaxTasksRequested (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> </ul>"},{"location":"dynampi/class_members/#l","title":"l","text":"<ul> <li>LOG_ENTRY_BYTES (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>log_slot (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>leader_batch_multiplier (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalLockFreeMPIWorkDistributor::Config)</li> <li>local_batch_size (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalLockFreeMPIWorkDistributor::Config)</li> <li>last_upper_level (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor)</li> <li>LockFreeMPIWorkDistributor (dynampi::LockFreeMPIWorkDistributor)</li> <li>LockFreeLevel (dynampi::detail::LockFreeLevel)</li> </ul>"},{"location":"dynampi/class_members/#m","title":"m","text":"<ul> <li>manager_rank (dynampi::AsyncPutLockFreeMPIWorkDistributor::Config, dynampi::BaseMPIWorkDistributor::Config, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalMPIWorkDistributor::Config, dynampi::HierarchicalNonBlockingMPIWorkDistributor::Config, dynampi::LockFreeMPIWorkDistributor::Config, dynampi::MinimalLockFreeMPIWorkDistributor::Config, dynampi::NaiveMPIWorkDistributor::Config)</li> <li>max_result_count (dynampi::AsyncPutLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalLockFreeMPIWorkDistributor::Config, dynampi::LockFreeMPIWorkDistributor::Config, dynampi::detail::AsyncPutLevel::Config, dynampi::detail::LockFreeLevel::Config)</li> <li>max_task_count (dynampi::AsyncPutLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalLockFreeMPIWorkDistributor::Config, dynampi::LockFreeMPIWorkDistributor::Config, dynampi::detail::AsyncPutLevel::Config, dynampi::detail::LockFreeLevel::Config)</li> <li>max_tasks (dynampi::AsyncPutLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalLockFreeMPIWorkDistributor::Config, dynampi::LockFreeMPIWorkDistributor::Config, dynampi::detail::AsyncPutLevel::Config, dynampi::detail::LockFreeLevel::Config)</li> <li>max_seconds (dynampi::AsyncPutLockFreeMPIWorkDistributor::RunConfig, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::RunConfig, dynampi::HierarchicalLockFreeMPIWorkDistributor::RunConfig, dynampi::HierarchicalMPIWorkDistributor::RunConfig, dynampi::HierarchicalNonBlockingMPIWorkDistributor::RunConfig, dynampi::LockFreeMPIWorkDistributor::RunConfig, dynampi::NaiveMPIWorkDistributor::RunConfig)</li> <li>m_collected_count (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor)</li> <li>m_comm (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::MPICommunicator, dynampi::MinimalLockFreeMPIWorkDistributor)</li> <li>m_config (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::MinimalLockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>m_finalized (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>m_log_base (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>m_max_result_count (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>m_max_task_count (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>m_result_base (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>m_result_elem (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>m_result_slot_stride (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>m_results (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor)</li> <li>m_returned_count (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor)</li> <li>m_statistics (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>m_task_base (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>m_task_elem (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>m_task_slot_stride (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>m_task_store (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor)</li> <li>m_total_tasks (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>m_window (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::MinimalLockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>m_window_buffer (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>m_worker_function (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::MinimalLockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>m_worker_window (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::MinimalLockFreeMPIWorkDistributor)</li> <li>max_local_tasks (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalLockFreeMPIWorkDistributor::Config)</li> <li>max_upper_fanout (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::Config)</li> <li>m_local_collected_count (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>m_local_comm (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>m_local_level (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>m_local_task_store (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>m_owned_upper_levels (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor)</li> <li>m_parent_level (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor)</li> <li>m_solo (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>m_upper_comms (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor)</li> <li>m_world_comm (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>m_leader_comm (dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>m_leader_level (dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>MPICommunicator (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::MPICommunicator, dynampi::NaiveMPIWorkDistributor)</li> <li>max_workers_per_coordinator (dynampi::HierarchicalMPIWorkDistributor::Config, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor::Config, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>message_batch_size (dynampi::HierarchicalMPIWorkDistributor::Config, dynampi::HierarchicalNonBlockingMPIWorkDistributor::Config)</li> <li>m_cached_parent_target (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_communicator (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>m_done (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_free_worker_indices (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_leader_group (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_local_group (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_outstanding_requests (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_prefetched_tasks (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_results_received_from_child (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_results_returned (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_results_sent_to_parent (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_round_active (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_tasks_executed (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_tasks_received_from_parent (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_tasks_sent_to_child (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_unallocated_task_queue (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>m_world_group (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_pending_int_sends (dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_pending_result_batch_sends (dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_pending_result_sends (dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_pending_task_batch_sends (dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_pending_task_sends (dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_gather_seq (dynampi::LockFreeMPIWorkDistributor)</li> <li>m_local_results (dynampi::LockFreeMPIWorkDistributor)</li> <li>m_seen_gather_seq (dynampi::LockFreeMPIWorkDistributor)</li> <li>m_staging (dynampi::LockFreeMPIWorkDistributor)</li> <li>make_statistics (dynampi::LockFreeMPIWorkDistributor)</li> <li>maybe_participate_in_gather (dynampi::LockFreeMPIWorkDistributor, dynampi::detail::LockFreeLevel)</li> <li>m_ownership (dynampi::MPICommunicator)</li> <li>MPIGroup (dynampi::MPIGroup)</li> <li>m_group (dynampi::MPIGroup)</li> <li>MinimalLockFreeMPIWorkDistributor (dynampi::MinimalLockFreeMPIWorkDistributor)</li> <li>m_counter (dynampi::MinimalLockFreeMPIWorkDistributor)</li> <li>max_result_size (dynampi::NaiveMPIWorkDistributor::Config)</li> <li>m_free_worker_ranks (dynampi::NaiveMPIWorkDistributor)</li> <li>m_front_result_idx (dynampi::NaiveMPIWorkDistributor)</li> <li>m_known_contiguous_results (dynampi::NaiveMPIWorkDistributor)</li> <li>m_pending_results (dynampi::NaiveMPIWorkDistributor, dynampi::detail::LockFreeLevel)</li> <li>m_pending_results_valid (dynampi::NaiveMPIWorkDistributor)</li> <li>m_tasks_sent (dynampi::NaiveMPIWorkDistributor)</li> <li>m_worker_current_task_indices (dynampi::NaiveMPIWorkDistributor)</li> <li>m_cached_head (dynampi::detail::AsyncPutLevel)</li> <li>m_cached_total (dynampi::detail::AsyncPutLevel)</li> <li>m_owner_collected_count (dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>m_owner_marked_finished (dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>m_peer_window (dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>m_pending_end (dynampi::detail::AsyncPutLevel)</li> <li>m_pending_start (dynampi::detail::AsyncPutLevel)</li> <li>m_seen_finished (dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>mark_finished (dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>m_bcast_buf (dynampi::detail::LockFreeLevel)</li> <li>m_bcast_request (dynampi::detail::LockFreeLevel)</li> <li>m_exchange_byte_counts (dynampi::detail::LockFreeLevel)</li> <li>m_exchange_displacements (dynampi::detail::LockFreeLevel)</li> <li>m_exchange_recv_buf (dynampi::detail::LockFreeLevel)</li> <li>m_exchange_send_buf (dynampi::detail::LockFreeLevel)</li> <li>m_exchange_send_count (dynampi::detail::LockFreeLevel)</li> <li>m_gather_fully_done (dynampi::detail::LockFreeLevel)</li> <li>m_gather_request (dynampi::detail::LockFreeLevel)</li> <li>m_gatherv_request (dynampi::detail::LockFreeLevel)</li> <li>m_owner_gather_finished (dynampi::detail::LockFreeLevel)</li> <li>m_stage (dynampi::detail::LockFreeLevel)</li> <li>mark_gather_done (dynampi::detail::LockFreeLevel)</li> </ul>"},{"location":"dynampi/class_members/#n","title":"n","text":"<ul> <li>num_workers (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>num_direct_children (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>NaiveMPIWorkDistributor (dynampi::NaiveMPIWorkDistributor)</li> <li>num_tasks_requested (dynampi::HierarchicalMPIWorkDistributor::TaskRequest, dynampi::HierarchicalNonBlockingMPIWorkDistributor::TaskRequest)</li> </ul>"},{"location":"dynampi/class_members/#o","title":"o","text":"<ul> <li>ordered (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>Ownership (dynampi::MPICommunicator)</li> <li>operator MPI_Comm (dynampi::MPICommunicator)</li> <li>operator= (dynampi::MPICommunicator, dynampi::MPIGroup, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>operator MPI_Group (dynampi::MPIGroup)</li> <li>owner_rank (dynampi::detail::AsyncPutLevel::Config, dynampi::detail::LockFreeLevel::Config)</li> <li>owner_collected_count (dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>owner_marked_finished (dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>owner_published_count (dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> </ul>"},{"location":"dynampi/class_members/#p","title":"p","text":"<ul> <li>publish_tasks (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>put_bytes (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>prioritize_tasks (dynampi::BaseMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>pipeline_depth (dynampi::HierarchicalMPIWorkDistributor::Config, dynampi::HierarchicalNonBlockingMPIWorkDistributor::Config)</li> <li>publish_task (dynampi::LockFreeMPIWorkDistributor, dynampi::detail::LockFreeLevel)</li> <li>probe (dynampi::MPICommunicator)</li> <li>ptr (dynampi::MPI_Type&lt; std::nullptr_t &gt;, dynampi::MPI_Type&lt; std::string &gt;, dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;)</li> <li>pop_next_task (dynampi::NaiveMPIWorkDistributor)</li> <li>process_incoming_message (dynampi::NaiveMPIWorkDistributor)</li> <li>post_count_gather (dynampi::detail::LockFreeLevel)</li> <li>post_data_gatherv (dynampi::detail::LockFreeLevel)</li> <li>parent (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::BridgeHop)</li> <li>pending_relays (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::BridgeHop)</li> <li>pending_task_count (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::BridgeHop)</li> <li>parent_start (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::PendingRelay)</li> <li>pending (dynampi::HierarchicalNonBlockingMPIWorkDistributor::AsyncSendPool)</li> <li>post (dynampi::HierarchicalNonBlockingMPIWorkDistributor::AsyncSendPool)</li> </ul>"},{"location":"dynampi/class_members/#q","title":"q","text":"<ul> <li>QueueT (dynampi::BaseMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> </ul>"},{"location":"dynampi/class_members/#r","title":"r","text":"<ul> <li>R_COUNT (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>R_DATA (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>read_task_batch (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>remaining_tasks_count (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>result_slot (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>run_one_task_locally (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor)</li> <li>run_tasks (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>run_worker (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>recv_count (dynampi::CommStatistics)</li> <li>recv_time (dynampi::CommStatistics)</li> <li>reset (dynampi::CommStatistics, dynampi::Timer)</li> <li>run_leaf_leader_worker (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>run_local_worker (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>run_node_coordinator (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>receive_done_from (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>receive_execute_return_task_from (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>receive_from_anyone (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>receive_request_batch_from (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>receive_request_from (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>receive_result_batch_from (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>receive_result_from (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>receive_task_batch_from (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>result_mpi_type (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>request_gather (dynampi::LockFreeMPIWorkDistributor, dynampi::detail::LockFreeLevel)</li> <li>rank (dynampi::MPICommunicator, dynampi::MPIGroup)</li> <li>recv (dynampi::MPICommunicator)</li> <li>recv_any (dynampi::MPICommunicator)</li> <li>recv_empty (dynampi::MPICommunicator)</li> <li>recv_empty_message (dynampi::MPICommunicator)</li> <li>resize (dynampi::MPI_Type&lt; std::nullptr_t &gt;, dynampi::MPI_Type&lt; std::string &gt;, dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;)</li> <li>resize_required (dynampi::MPI_Type&lt; std::nullptr_t &gt;, dynampi::MPI_Type&lt; std::string &gt;, dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;)</li> <li>run (dynampi::MinimalLockFreeMPIWorkDistributor)</li> <li>rank_to_worker_idx (dynampi::NaiveMPIWorkDistributor)</li> <li>run_task_locally (dynampi::NaiveMPIWorkDistributor)</li> <li>read_task (dynampi::detail::LockFreeLevel)</li> <li>request_gather_throttled (dynampi::detail::LockFreeLevel)</li> <li>relay_buffer (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::BridgeHop)</li> <li>reap (dynampi::HierarchicalNonBlockingMPIWorkDistributor::AsyncSendPool)</li> </ul>"},{"location":"dynampi/class_members/#s","title":"s","text":"<ul> <li>StatisticsT (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::MPICommunicator, dynampi::NaiveMPIWorkDistributor)</li> <li>statistics_mode (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::MPICommunicator, dynampi::NaiveMPIWorkDistributor)</li> <li>send_count (dynampi::CommStatistics)</li> <li>send_time (dynampi::CommStatistics)</li> <li>setup_levels (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>setup_topology (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>setup_upper_chain (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor)</li> <li>step_bridge_hop (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor)</li> <li>send_done_to_children_when_free (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>send_results_to_parent (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>send_to_parent (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>send_to_worker (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>send_async (dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>store_result (dynampi::LockFreeMPIWorkDistributor)</li> <li>send (dynampi::MPICommunicator)</li> <li>send_empty (dynampi::MPICommunicator)</li> <li>size (dynampi::MPICommunicator, dynampi::MPIGroup)</li> <li>split (dynampi::MPICommunicator)</li> <li>split_by_node (dynampi::MPICommunicator)</li> <li>set_counter (dynampi::MinimalLockFreeMPIWorkDistributor)</li> <li>send_next_task_to_worker (dynampi::NaiveMPIWorkDistributor)</li> <li>start (dynampi::Timer, dynampi::detail::AsyncPutLevel::ClaimedRange)</li> <li>stop (dynampi::Timer)</li> <li>serialize_pending_results (dynampi::detail::LockFreeLevel)</li> <li>stage_result (dynampi::detail::LockFreeLevel)</li> <li>source_layer (dynampi::HierarchicalMPIWorkDistributor::TaskRequest, dynampi::HierarchicalNonBlockingMPIWorkDistributor::TaskRequest)</li> </ul>"},{"location":"dynampi/class_members/#t","title":"t","text":"<ul> <li>TOTAL_OFF (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>T_COUNT (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>T_DATA (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>target_num_tasks (dynampi::AsyncPutLockFreeMPIWorkDistributor::RunConfig, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::RunConfig, dynampi::HierarchicalLockFreeMPIWorkDistributor::RunConfig, dynampi::HierarchicalMPIWorkDistributor::RunConfig, dynampi::HierarchicalNonBlockingMPIWorkDistributor::RunConfig, dynampi::LockFreeMPIWorkDistributor::RunConfig, dynampi::NaiveMPIWorkDistributor::RunConfig)</li> <li>task_slot (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>Tag (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>task_mpi_type (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>total_num_children (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>try_gather_results (dynampi::LockFreeMPIWorkDistributor)</li> <li>translate_rank (dynampi::MPIGroup)</li> <li>translate_ranks (dynampi::MPIGroup)</li> <li>Timer (dynampi::Timer)</li> <li>tasks (dynampi::detail::AsyncPutLevel::ClaimedRange)</li> <li>try_claim (dynampi::detail::AsyncPutLevel)</li> <li>try_claim_batch (dynampi::detail::LockFreeLevel)</li> </ul>"},{"location":"dynampi/class_members/#u","title":"u","text":"<ul> <li>use_immediate_recv (dynampi::NaiveMPIWorkDistributor::Config)</li> <li>update_contiguous_results_count (dynampi::NaiveMPIWorkDistributor)</li> </ul>"},{"location":"dynampi/class_members/#v","title":"v","text":"<ul> <li>value (dynampi::MPI_Type&lt; std::nullptr_t &gt;, dynampi::MPI_Type&lt; std::string &gt;, dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;, dynampi::enable_prioritization, dynampi::prioritize_tasks_t, dynampi::track_statistics, dynampi::track_statistics_t, option_value, option_value&lt; Option, Head, Tail... &gt;)</li> </ul>"},{"location":"dynampi/class_members/#w","title":"w","text":"<ul> <li>write_result_range (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>worker_for_idx (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>wait_all_pending_sends (dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>worker_task_counts (dynampi::LockFreeMPIWorkDistributor::Statistics, dynampi::NaiveMPIWorkDistributor::Statistics, dynampi::HierarchicalMPIWorkDistributor::Statistics, dynampi::HierarchicalNonBlockingMPIWorkDistributor::Statistics)</li> <li>worker_idx_to_rank (dynampi::NaiveMPIWorkDistributor)</li> <li>worker_rank (dynampi::HierarchicalMPIWorkDistributor::TaskRequest, dynampi::HierarchicalNonBlockingMPIWorkDistributor::TaskRequest)</li> <li>wait_all (dynampi::HierarchicalNonBlockingMPIWorkDistributor::AsyncSendPool)</li> </ul>"},{"location":"dynampi/class_members/#_1","title":"~","text":"<ul> <li>~AsyncPutLockFreeMPIWorkDistributor (dynampi::AsyncPutLockFreeMPIWorkDistributor)</li> <li>~HierarchicalAsyncPutLockFreeMPIWorkDistributor (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor)</li> <li>~HierarchicalLockFreeMPIWorkDistributor (dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>~HierarchicalMPIWorkDistributor (dynampi::HierarchicalMPIWorkDistributor)</li> <li>~HierarchicalNonBlockingMPIWorkDistributor (dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>~LockFreeMPIWorkDistributor (dynampi::LockFreeMPIWorkDistributor)</li> <li>~MPICommunicator (dynampi::MPICommunicator)</li> <li>~MPIGroup (dynampi::MPIGroup)</li> <li>~MinimalLockFreeMPIWorkDistributor (dynampi::MinimalLockFreeMPIWorkDistributor)</li> <li>~NaiveMPIWorkDistributor (dynampi::NaiveMPIWorkDistributor)</li> <li>~AsyncPutLevel (dynampi::detail::AsyncPutLevel)</li> <li>~LockFreeLevel (dynampi::detail::LockFreeLevel)</li> </ul>"},{"location":"dynampi/class_members/#_","title":"_","text":"<ul> <li>_statistics (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::MPICommunicator)</li> <li>_elapsed_time (dynampi::Timer)</li> <li>_start_time (dynampi::Timer)</li> </ul>"},{"location":"dynampi/class_member_functions/","title":"Class Member Functions","text":""},{"location":"dynampi/class_member_functions/#a","title":"a","text":"<ul> <li>AsyncPutLockFreeMPIWorkDistributor (dynampi::AsyncPutLockFreeMPIWorkDistributor)</li> <li>atomic_read (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>atomic_set (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>average_receive_size (dynampi::CommStatistics)</li> <li>average_send_size (dynampi::CommStatistics)</li> <li>allocate_task_to_child (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>available (dynampi::LockFreeMPIWorkDistributor)</li> <li>active_worker_count (dynampi::NaiveMPIWorkDistributor)</li> <li>AsyncPutLevel (dynampi::detail::AsyncPutLevel)</li> <li>advance_gather_round (dynampi::detail::LockFreeLevel)</li> </ul>"},{"location":"dynampi/class_member_functions/#b","title":"b","text":"<ul> <li>bridge_hop_done (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor)</li> <li>build_upper_hops (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor)</li> <li>broadcast (dynampi::MPICommunicator)</li> <li>broadcast_done (dynampi::NaiveMPIWorkDistributor)</li> <li>BridgeHop (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::BridgeHop)</li> </ul>"},{"location":"dynampi/class_member_functions/#c","title":"c","text":"<ul> <li>create_statistics (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>contains_rank_in_group (dynampi::MPIGroup)</li> <li>count (dynampi::MPI_Type&lt; std::nullptr_t &gt;, dynampi::MPI_Type&lt; std::string &gt;, dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;)</li> <li>collect_available_results (dynampi::NaiveMPIWorkDistributor)</li> <li>check_finished (dynampi::detail::AsyncPutLevel)</li> <li>claim_batch_size (dynampi::detail::AsyncPutLevel)</li> <li>comm_rank (dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>comm_size (dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>compare_and_swap (dynampi::detail::LockFreeLevel)</li> </ul>"},{"location":"dynampi/class_member_functions/#d","title":"d","text":"<ul> <li>drain_results (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor)</li> <li>determine_layer_from_world_rank (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>drained (dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>deserialize_results (dynampi::detail::LockFreeLevel)</li> </ul>"},{"location":"dynampi/class_member_functions/#e","title":"e","text":"<ul> <li>emplace_owned_upper_level (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor)</li> <li>emplace_parent_level (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor)</li> <li>exchange_gathered_results (dynampi::LockFreeMPIWorkDistributor)</li> <li>ensure_result_capacity (dynampi::NaiveMPIWorkDistributor)</li> <li>elapsed (dynampi::Timer)</li> </ul>"},{"location":"dynampi/class_member_functions/#f","title":"f","text":"<ul> <li>fetch_add (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::MinimalLockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>finalize (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>finish_remaining_tasks (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>flush (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> </ul>"},{"location":"dynampi/class_member_functions/#g","title":"g","text":"<ul> <li>gather_once (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor)</li> <li>get_bytes (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>get_statistics (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::MPICommunicator, dynampi::NaiveMPIWorkDistributor)</li> <li>get_next_task_to_send (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>get_parent_target (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>gather (dynampi::MPICommunicator)</li> <li>get (dynampi::MPICommunicator, dynampi::MPIGroup)</li> <li>get_group (dynampi::MPICommunicator)</li> <li>gather_sorted (dynampi::MinimalLockFreeMPIWorkDistributor)</li> <li>gather_fully_done (dynampi::detail::LockFreeLevel)</li> </ul>"},{"location":"dynampi/class_member_functions/#h","title":"h","text":"<ul> <li>harvest_ready_results (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>HierarchicalAsyncPutLockFreeMPIWorkDistributor (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor)</li> <li>HierarchicalLockFreeMPIWorkDistributor (dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>HierarchicalMPIWorkDistributor (dynampi::HierarchicalMPIWorkDistributor)</li> <li>HierarchicalNonBlockingMPIWorkDistributor (dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>handle_result_message (dynampi::NaiveMPIWorkDistributor)</li> <li>harvest_ready_results_throttled (dynampi::detail::AsyncPutLevel)</li> <li>has_pending_results (dynampi::detail::LockFreeLevel)</li> </ul>"},{"location":"dynampi/class_member_functions/#i","title":"i","text":"<ul> <li>initialize_window (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>insert_task (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>insert_tasks (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>is_root_manager (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::MinimalLockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>idx_for_worker (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>is_leaf_worker (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>iprobe (dynampi::MPICommunicator)</li> <li>idle_wait (dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>is_owner (dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> </ul>"},{"location":"dynampi/class_member_functions/#l","title":"l","text":"<ul> <li>log_slot (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>last_upper_level (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor)</li> <li>LockFreeMPIWorkDistributor (dynampi::LockFreeMPIWorkDistributor)</li> <li>LockFreeLevel (dynampi::detail::LockFreeLevel)</li> </ul>"},{"location":"dynampi/class_member_functions/#m","title":"m","text":"<ul> <li>max_workers_per_coordinator (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>make_statistics (dynampi::LockFreeMPIWorkDistributor)</li> <li>maybe_participate_in_gather (dynampi::LockFreeMPIWorkDistributor, dynampi::detail::LockFreeLevel)</li> <li>MPICommunicator (dynampi::MPICommunicator)</li> <li>MPIGroup (dynampi::MPIGroup)</li> <li>MinimalLockFreeMPIWorkDistributor (dynampi::MinimalLockFreeMPIWorkDistributor)</li> <li>mark_finished (dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>mark_gather_done (dynampi::detail::LockFreeLevel)</li> </ul>"},{"location":"dynampi/class_member_functions/#n","title":"n","text":"<ul> <li>num_workers (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>num_direct_children (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>NaiveMPIWorkDistributor (dynampi::NaiveMPIWorkDistributor)</li> </ul>"},{"location":"dynampi/class_member_functions/#o","title":"o","text":"<ul> <li>operator MPI_Comm (dynampi::MPICommunicator)</li> <li>operator= (dynampi::MPICommunicator, dynampi::MPIGroup, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>operator MPI_Group (dynampi::MPIGroup)</li> <li>owner_collected_count (dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>owner_marked_finished (dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>owner_published_count (dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> </ul>"},{"location":"dynampi/class_member_functions/#p","title":"p","text":"<ul> <li>publish_tasks (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>put_bytes (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>publish_task (dynampi::LockFreeMPIWorkDistributor, dynampi::detail::LockFreeLevel)</li> <li>probe (dynampi::MPICommunicator)</li> <li>ptr (dynampi::MPI_Type&lt; std::nullptr_t &gt;, dynampi::MPI_Type&lt; std::string &gt;, dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;)</li> <li>pop_next_task (dynampi::NaiveMPIWorkDistributor)</li> <li>process_incoming_message (dynampi::NaiveMPIWorkDistributor)</li> <li>post_count_gather (dynampi::detail::LockFreeLevel)</li> <li>post_data_gatherv (dynampi::detail::LockFreeLevel)</li> <li>post (dynampi::HierarchicalNonBlockingMPIWorkDistributor::AsyncSendPool)</li> </ul>"},{"location":"dynampi/class_member_functions/#r","title":"r","text":"<ul> <li>read_task_batch (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>remaining_tasks_count (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>result_slot (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>run_one_task_locally (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor)</li> <li>run_tasks (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>run_worker (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>reset (dynampi::CommStatistics, dynampi::Timer)</li> <li>run_leaf_leader_worker (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>run_local_worker (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>run_node_coordinator (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>receive_done_from (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>receive_execute_return_task_from (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>receive_from_anyone (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>receive_request_batch_from (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>receive_request_from (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>receive_result_batch_from (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>receive_result_from (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>receive_task_batch_from (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>request_gather (dynampi::LockFreeMPIWorkDistributor, dynampi::detail::LockFreeLevel)</li> <li>rank (dynampi::MPICommunicator, dynampi::MPIGroup)</li> <li>recv (dynampi::MPICommunicator)</li> <li>recv_any (dynampi::MPICommunicator)</li> <li>recv_empty (dynampi::MPICommunicator)</li> <li>recv_empty_message (dynampi::MPICommunicator)</li> <li>resize (dynampi::MPI_Type&lt; std::nullptr_t &gt;, dynampi::MPI_Type&lt; std::string &gt;, dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;)</li> <li>run (dynampi::MinimalLockFreeMPIWorkDistributor)</li> <li>rank_to_worker_idx (dynampi::NaiveMPIWorkDistributor)</li> <li>run_task_locally (dynampi::NaiveMPIWorkDistributor)</li> <li>read_task (dynampi::detail::LockFreeLevel)</li> <li>request_gather_throttled (dynampi::detail::LockFreeLevel)</li> <li>reap (dynampi::HierarchicalNonBlockingMPIWorkDistributor::AsyncSendPool)</li> </ul>"},{"location":"dynampi/class_member_functions/#s","title":"s","text":"<ul> <li>setup_levels (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>setup_topology (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>setup_upper_chain (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor)</li> <li>step_bridge_hop (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor)</li> <li>send_done_to_children_when_free (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>send_results_to_parent (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>send_to_parent (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>send_to_worker (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>send_async (dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>store_result (dynampi::LockFreeMPIWorkDistributor)</li> <li>send (dynampi::MPICommunicator)</li> <li>send_empty (dynampi::MPICommunicator)</li> <li>size (dynampi::MPICommunicator, dynampi::MPIGroup)</li> <li>split (dynampi::MPICommunicator)</li> <li>split_by_node (dynampi::MPICommunicator)</li> <li>set_counter (dynampi::MinimalLockFreeMPIWorkDistributor)</li> <li>send_next_task_to_worker (dynampi::NaiveMPIWorkDistributor)</li> <li>start (dynampi::Timer)</li> <li>stop (dynampi::Timer)</li> <li>serialize_pending_results (dynampi::detail::LockFreeLevel)</li> <li>stage_result (dynampi::detail::LockFreeLevel)</li> </ul>"},{"location":"dynampi/class_member_functions/#t","title":"t","text":"<ul> <li>task_slot (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>total_num_children (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>try_gather_results (dynampi::LockFreeMPIWorkDistributor)</li> <li>translate_rank (dynampi::MPIGroup)</li> <li>translate_ranks (dynampi::MPIGroup)</li> <li>Timer (dynampi::Timer)</li> <li>try_claim (dynampi::detail::AsyncPutLevel)</li> <li>try_claim_batch (dynampi::detail::LockFreeLevel)</li> </ul>"},{"location":"dynampi/class_member_functions/#u","title":"u","text":"<ul> <li>update_contiguous_results_count (dynampi::NaiveMPIWorkDistributor)</li> </ul>"},{"location":"dynampi/class_member_functions/#w","title":"w","text":"<ul> <li>write_result_range (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>worker_for_idx (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>wait_all_pending_sends (dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>worker_idx_to_rank (dynampi::NaiveMPIWorkDistributor)</li> <li>wait_all (dynampi::HierarchicalNonBlockingMPIWorkDistributor::AsyncSendPool)</li> </ul>"},{"location":"dynampi/class_member_functions/#_1","title":"~","text":"<ul> <li>~AsyncPutLockFreeMPIWorkDistributor (dynampi::AsyncPutLockFreeMPIWorkDistributor)</li> <li>~HierarchicalAsyncPutLockFreeMPIWorkDistributor (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor)</li> <li>~HierarchicalLockFreeMPIWorkDistributor (dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>~HierarchicalMPIWorkDistributor (dynampi::HierarchicalMPIWorkDistributor)</li> <li>~HierarchicalNonBlockingMPIWorkDistributor (dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>~LockFreeMPIWorkDistributor (dynampi::LockFreeMPIWorkDistributor)</li> <li>~MPICommunicator (dynampi::MPICommunicator)</li> <li>~MPIGroup (dynampi::MPIGroup)</li> <li>~MinimalLockFreeMPIWorkDistributor (dynampi::MinimalLockFreeMPIWorkDistributor)</li> <li>~NaiveMPIWorkDistributor (dynampi::NaiveMPIWorkDistributor)</li> <li>~AsyncPutLevel (dynampi::detail::AsyncPutLevel)</li> <li>~LockFreeLevel (dynampi::detail::LockFreeLevel)</li> </ul>"},{"location":"dynampi/class_member_variables/","title":"Class Member Variables","text":""},{"location":"dynampi/class_member_variables/#a","title":"a","text":"<ul> <li>auto_run_workers (dynampi::AsyncPutLockFreeMPIWorkDistributor::Config, dynampi::BaseMPIWorkDistributor::Config, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalMPIWorkDistributor::Config, dynampi::HierarchicalNonBlockingMPIWorkDistributor::Config, dynampi::LockFreeMPIWorkDistributor::Config, dynampi::NaiveMPIWorkDistributor::Config)</li> <li>allow_more_than_target_tasks (dynampi::AsyncPutLockFreeMPIWorkDistributor::RunConfig, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::RunConfig, dynampi::HierarchicalLockFreeMPIWorkDistributor::RunConfig, dynampi::HierarchicalMPIWorkDistributor::RunConfig, dynampi::HierarchicalNonBlockingMPIWorkDistributor::RunConfig, dynampi::LockFreeMPIWorkDistributor::RunConfig, dynampi::NaiveMPIWorkDistributor::RunConfig)</li> </ul>"},{"location":"dynampi/class_member_variables/#b","title":"b","text":"<ul> <li>bytes_received (dynampi::CommStatistics)</li> <li>bytes_sent (dynampi::CommStatistics)</li> <li>batch_size_multiplier (dynampi::HierarchicalMPIWorkDistributor::Config, dynampi::HierarchicalNonBlockingMPIWorkDistributor::Config)</li> </ul>"},{"location":"dynampi/class_member_variables/#c","title":"c","text":"<ul> <li>CONTROL_BYTES (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>claim_batch_size (dynampi::AsyncPutLockFreeMPIWorkDistributor::Config, dynampi::LockFreeMPIWorkDistributor::Config, dynampi::detail::AsyncPutLevel::Config)</li> <li>comm (dynampi::AsyncPutLockFreeMPIWorkDistributor::Config, dynampi::BaseMPIWorkDistributor::Config, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalMPIWorkDistributor::Config, dynampi::HierarchicalNonBlockingMPIWorkDistributor::Config, dynampi::LockFreeMPIWorkDistributor::Config, dynampi::MinimalLockFreeMPIWorkDistributor::Config, dynampi::NaiveMPIWorkDistributor::Config, dynampi::detail::AsyncPutLevel::Config, dynampi::detail::LockFreeLevel::Config)</li> <li>comm_statistics (dynampi::AsyncPutLockFreeMPIWorkDistributor::Statistics, dynampi::LockFreeMPIWorkDistributor::Statistics, dynampi::NaiveMPIWorkDistributor::Statistics, dynampi::HierarchicalMPIWorkDistributor::Statistics, dynampi::HierarchicalNonBlockingMPIWorkDistributor::Statistics)</li> <li>collective_count (dynampi::CommStatistics)</li> <li>coordinator_per_node (dynampi::HierarchicalMPIWorkDistributor::Config, dynampi::HierarchicalNonBlockingMPIWorkDistributor::Config)</li> <li>child (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::BridgeHop)</li> <li>child_len (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::PendingRelay)</li> </ul>"},{"location":"dynampi/class_member_variables/#f","title":"f","text":"<ul> <li>FINISHED_OFF (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>finish_marked (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::BridgeHop)</li> </ul>"},{"location":"dynampi/class_member_variables/#g","title":"g","text":"<ul> <li>GATHER_SEQ_OFF (dynampi::LockFreeMPIWorkDistributor)</li> </ul>"},{"location":"dynampi/class_member_variables/#h","title":"h","text":"<ul> <li>HEAD_OFF (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> </ul>"},{"location":"dynampi/class_member_variables/#k","title":"k","text":"<ul> <li>kMaxRmaChunkBytes (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>kMaxTasksRequested (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> </ul>"},{"location":"dynampi/class_member_variables/#l","title":"l","text":"<ul> <li>LOG_ENTRY_BYTES (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>leader_batch_multiplier (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalLockFreeMPIWorkDistributor::Config)</li> <li>local_batch_size (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalLockFreeMPIWorkDistributor::Config)</li> </ul>"},{"location":"dynampi/class_member_variables/#m","title":"m","text":"<ul> <li>manager_rank (dynampi::AsyncPutLockFreeMPIWorkDistributor::Config, dynampi::BaseMPIWorkDistributor::Config, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalMPIWorkDistributor::Config, dynampi::HierarchicalNonBlockingMPIWorkDistributor::Config, dynampi::LockFreeMPIWorkDistributor::Config, dynampi::MinimalLockFreeMPIWorkDistributor::Config, dynampi::NaiveMPIWorkDistributor::Config)</li> <li>max_result_count (dynampi::AsyncPutLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalLockFreeMPIWorkDistributor::Config, dynampi::LockFreeMPIWorkDistributor::Config, dynampi::detail::AsyncPutLevel::Config, dynampi::detail::LockFreeLevel::Config)</li> <li>max_task_count (dynampi::AsyncPutLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalLockFreeMPIWorkDistributor::Config, dynampi::LockFreeMPIWorkDistributor::Config, dynampi::detail::AsyncPutLevel::Config, dynampi::detail::LockFreeLevel::Config)</li> <li>max_tasks (dynampi::AsyncPutLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalLockFreeMPIWorkDistributor::Config, dynampi::LockFreeMPIWorkDistributor::Config, dynampi::detail::AsyncPutLevel::Config, dynampi::detail::LockFreeLevel::Config)</li> <li>max_seconds (dynampi::AsyncPutLockFreeMPIWorkDistributor::RunConfig, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::RunConfig, dynampi::HierarchicalLockFreeMPIWorkDistributor::RunConfig, dynampi::HierarchicalMPIWorkDistributor::RunConfig, dynampi::HierarchicalNonBlockingMPIWorkDistributor::RunConfig, dynampi::LockFreeMPIWorkDistributor::RunConfig, dynampi::NaiveMPIWorkDistributor::RunConfig)</li> <li>m_collected_count (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor)</li> <li>m_comm (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::MPICommunicator, dynampi::MinimalLockFreeMPIWorkDistributor)</li> <li>m_config (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::MinimalLockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>m_finalized (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>m_log_base (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>m_max_result_count (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>m_max_task_count (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>m_result_base (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>m_result_elem (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>m_result_slot_stride (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>m_results (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor)</li> <li>m_returned_count (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor)</li> <li>m_statistics (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>m_task_base (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>m_task_elem (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>m_task_slot_stride (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>m_task_store (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor)</li> <li>m_total_tasks (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>m_window (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::MinimalLockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>m_window_buffer (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>m_worker_function (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::MinimalLockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>m_worker_window (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::MinimalLockFreeMPIWorkDistributor)</li> <li>max_local_tasks (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::Config, dynampi::HierarchicalLockFreeMPIWorkDistributor::Config)</li> <li>max_upper_fanout (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::Config)</li> <li>m_local_collected_count (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>m_local_comm (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>m_local_level (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>m_local_task_store (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>m_owned_upper_levels (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor)</li> <li>m_parent_level (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor)</li> <li>m_solo (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>m_upper_comms (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor)</li> <li>m_world_comm (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>m_leader_comm (dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>m_leader_level (dynampi::HierarchicalLockFreeMPIWorkDistributor)</li> <li>max_workers_per_coordinator (dynampi::HierarchicalMPIWorkDistributor::Config, dynampi::HierarchicalNonBlockingMPIWorkDistributor::Config)</li> <li>message_batch_size (dynampi::HierarchicalMPIWorkDistributor::Config, dynampi::HierarchicalNonBlockingMPIWorkDistributor::Config)</li> <li>m_cached_parent_target (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_communicator (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>m_done (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_free_worker_indices (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_leader_group (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_local_group (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_outstanding_requests (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_prefetched_tasks (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_results_received_from_child (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_results_returned (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_results_sent_to_parent (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_round_active (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_tasks_executed (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_tasks_received_from_parent (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_tasks_sent_to_child (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_unallocated_task_queue (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>m_world_group (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_pending_int_sends (dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_pending_result_batch_sends (dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_pending_result_sends (dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_pending_task_batch_sends (dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_pending_task_sends (dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> <li>m_gather_seq (dynampi::LockFreeMPIWorkDistributor)</li> <li>m_local_results (dynampi::LockFreeMPIWorkDistributor)</li> <li>m_seen_gather_seq (dynampi::LockFreeMPIWorkDistributor)</li> <li>m_staging (dynampi::LockFreeMPIWorkDistributor)</li> <li>m_ownership (dynampi::MPICommunicator)</li> <li>m_group (dynampi::MPIGroup)</li> <li>m_counter (dynampi::MinimalLockFreeMPIWorkDistributor)</li> <li>max_result_size (dynampi::NaiveMPIWorkDistributor::Config)</li> <li>m_free_worker_ranks (dynampi::NaiveMPIWorkDistributor)</li> <li>m_front_result_idx (dynampi::NaiveMPIWorkDistributor)</li> <li>m_known_contiguous_results (dynampi::NaiveMPIWorkDistributor)</li> <li>m_pending_results (dynampi::NaiveMPIWorkDistributor, dynampi::detail::LockFreeLevel)</li> <li>m_pending_results_valid (dynampi::NaiveMPIWorkDistributor)</li> <li>m_tasks_sent (dynampi::NaiveMPIWorkDistributor)</li> <li>m_worker_current_task_indices (dynampi::NaiveMPIWorkDistributor)</li> <li>m_cached_head (dynampi::detail::AsyncPutLevel)</li> <li>m_cached_total (dynampi::detail::AsyncPutLevel)</li> <li>m_owner_collected_count (dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>m_owner_marked_finished (dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>m_peer_window (dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>m_pending_end (dynampi::detail::AsyncPutLevel)</li> <li>m_pending_start (dynampi::detail::AsyncPutLevel)</li> <li>m_seen_finished (dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>m_bcast_buf (dynampi::detail::LockFreeLevel)</li> <li>m_bcast_request (dynampi::detail::LockFreeLevel)</li> <li>m_exchange_byte_counts (dynampi::detail::LockFreeLevel)</li> <li>m_exchange_displacements (dynampi::detail::LockFreeLevel)</li> <li>m_exchange_recv_buf (dynampi::detail::LockFreeLevel)</li> <li>m_exchange_send_buf (dynampi::detail::LockFreeLevel)</li> <li>m_exchange_send_count (dynampi::detail::LockFreeLevel)</li> <li>m_gather_fully_done (dynampi::detail::LockFreeLevel)</li> <li>m_gather_request (dynampi::detail::LockFreeLevel)</li> <li>m_gatherv_request (dynampi::detail::LockFreeLevel)</li> <li>m_owner_gather_finished (dynampi::detail::LockFreeLevel)</li> <li>m_stage (dynampi::detail::LockFreeLevel)</li> </ul>"},{"location":"dynampi/class_member_variables/#n","title":"n","text":"<ul> <li>num_tasks_requested (dynampi::HierarchicalMPIWorkDistributor::TaskRequest, dynampi::HierarchicalNonBlockingMPIWorkDistributor::TaskRequest)</li> </ul>"},{"location":"dynampi/class_member_variables/#o","title":"o","text":"<ul> <li>ordered (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>owner_rank (dynampi::detail::AsyncPutLevel::Config, dynampi::detail::LockFreeLevel::Config)</li> </ul>"},{"location":"dynampi/class_member_variables/#p","title":"p","text":"<ul> <li>prioritize_tasks (dynampi::BaseMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> <li>pipeline_depth (dynampi::HierarchicalMPIWorkDistributor::Config, dynampi::HierarchicalNonBlockingMPIWorkDistributor::Config)</li> <li>parent (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::BridgeHop)</li> <li>pending_relays (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::BridgeHop)</li> <li>pending_task_count (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::BridgeHop)</li> <li>parent_start (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::PendingRelay)</li> <li>pending (dynampi::HierarchicalNonBlockingMPIWorkDistributor::AsyncSendPool)</li> </ul>"},{"location":"dynampi/class_member_variables/#r","title":"r","text":"<ul> <li>R_COUNT (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>R_DATA (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel)</li> <li>recv_count (dynampi::CommStatistics)</li> <li>recv_time (dynampi::CommStatistics)</li> <li>resize_required (dynampi::MPI_Type&lt; std::nullptr_t &gt;, dynampi::MPI_Type&lt; std::string &gt;, dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;)</li> <li>relay_buffer (dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::BridgeHop)</li> </ul>"},{"location":"dynampi/class_member_variables/#s","title":"s","text":"<ul> <li>statistics_mode (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::MPICommunicator, dynampi::NaiveMPIWorkDistributor)</li> <li>send_count (dynampi::CommStatistics)</li> <li>send_time (dynampi::CommStatistics)</li> <li>start (dynampi::detail::AsyncPutLevel::ClaimedRange)</li> <li>source_layer (dynampi::HierarchicalMPIWorkDistributor::TaskRequest, dynampi::HierarchicalNonBlockingMPIWorkDistributor::TaskRequest)</li> </ul>"},{"location":"dynampi/class_member_variables/#t","title":"t","text":"<ul> <li>TOTAL_OFF (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>T_COUNT (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>T_DATA (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::detail::AsyncPutLevel, dynampi::detail::LockFreeLevel)</li> <li>target_num_tasks (dynampi::AsyncPutLockFreeMPIWorkDistributor::RunConfig, dynampi::HierarchicalAsyncPutLockFreeMPIWorkDistributor::RunConfig, dynampi::HierarchicalLockFreeMPIWorkDistributor::RunConfig, dynampi::HierarchicalMPIWorkDistributor::RunConfig, dynampi::HierarchicalNonBlockingMPIWorkDistributor::RunConfig, dynampi::LockFreeMPIWorkDistributor::RunConfig, dynampi::NaiveMPIWorkDistributor::RunConfig)</li> <li>tasks (dynampi::detail::AsyncPutLevel::ClaimedRange)</li> </ul>"},{"location":"dynampi/class_member_variables/#u","title":"u","text":"<ul> <li>use_immediate_recv (dynampi::NaiveMPIWorkDistributor::Config)</li> </ul>"},{"location":"dynampi/class_member_variables/#v","title":"v","text":"<ul> <li>value (dynampi::MPI_Type&lt; std::nullptr_t &gt;, dynampi::MPI_Type&lt; std::string &gt;, dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;, dynampi::enable_prioritization, dynampi::prioritize_tasks_t, dynampi::track_statistics, dynampi::track_statistics_t, option_value, option_value&lt; Option, Head, Tail... &gt;)</li> </ul>"},{"location":"dynampi/class_member_variables/#w","title":"w","text":"<ul> <li>worker_task_counts (dynampi::LockFreeMPIWorkDistributor::Statistics, dynampi::NaiveMPIWorkDistributor::Statistics, dynampi::HierarchicalMPIWorkDistributor::Statistics, dynampi::HierarchicalNonBlockingMPIWorkDistributor::Statistics)</li> <li>worker_rank (dynampi::HierarchicalMPIWorkDistributor::TaskRequest, dynampi::HierarchicalNonBlockingMPIWorkDistributor::TaskRequest)</li> </ul>"},{"location":"dynampi/class_member_variables/#_","title":"_","text":"<ul> <li>_statistics (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::MPICommunicator)</li> <li>_elapsed_time (dynampi::Timer)</li> <li>_start_time (dynampi::Timer)</li> </ul>"},{"location":"dynampi/class_member_typedefs/","title":"Class Member Typedefs","text":""},{"location":"dynampi/class_member_typedefs/#b","title":"b","text":"<ul> <li>Base (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> </ul>"},{"location":"dynampi/class_member_typedefs/#c","title":"c","text":"<ul> <li>Comm (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor)</li> </ul>"},{"location":"dynampi/class_member_typedefs/#m","title":"m","text":"<ul> <li>MPICommunicator (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> </ul>"},{"location":"dynampi/class_member_typedefs/#q","title":"q","text":"<ul> <li>QueueT (dynampi::BaseMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> </ul>"},{"location":"dynampi/class_member_typedefs/#r","title":"r","text":"<ul> <li>result_mpi_type (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> </ul>"},{"location":"dynampi/class_member_typedefs/#s","title":"s","text":"<ul> <li>StatisticsT (dynampi::AsyncPutLockFreeMPIWorkDistributor, dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::LockFreeMPIWorkDistributor, dynampi::MPICommunicator, dynampi::NaiveMPIWorkDistributor)</li> </ul>"},{"location":"dynampi/class_member_typedefs/#t","title":"t","text":"<ul> <li>task_mpi_type (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> </ul>"},{"location":"dynampi/class_member_enums/","title":"Class Member Enums","text":""},{"location":"dynampi/class_member_enums/#a","title":"a","text":"<ul> <li>AutoStart (dynampi::Timer)</li> </ul>"},{"location":"dynampi/class_member_enums/#c","title":"c","text":"<ul> <li>CommLayer (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor)</li> </ul>"},{"location":"dynampi/class_member_enums/#g","title":"g","text":"<ul> <li>GatherStage (dynampi::detail::LockFreeLevel)</li> </ul>"},{"location":"dynampi/class_member_enums/#o","title":"o","text":"<ul> <li>Ownership (dynampi::MPICommunicator)</li> </ul>"},{"location":"dynampi/class_member_enums/#t","title":"t","text":"<ul> <li>Tag (dynampi::HierarchicalMPIWorkDistributor, dynampi::HierarchicalNonBlockingMPIWorkDistributor, dynampi::NaiveMPIWorkDistributor)</li> </ul>"},{"location":"dynampi/namespace_members/","title":"Namespace Members","text":""},{"location":"dynampi/namespace_members/#c","title":"c","text":"<ul> <li>commit_hash (dynampi::version)</li> <li>compile_date (dynampi::version)</li> </ul>"},{"location":"dynampi/namespace_members/#d","title":"d","text":"<ul> <li>DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (dynampi)</li> </ul>"},{"location":"dynampi/namespace_members/#i","title":"i","text":"<ul> <li>is_at_least (dynampi::version)</li> </ul>"},{"location":"dynampi/namespace_members/#m","title":"m","text":"<ul> <li>MPIDynamicWorkDistributor (dynampi)</li> <li>mpi_type_size_bytes (dynampi::detail)</li> <li>major (dynampi::version)</li> <li>minor (dynampi::version)</li> <li>mpi_fail (dynampi)</li> <li>mpi_manager_worker_distribution (dynampi)</li> </ul>"},{"location":"dynampi/namespace_members/#o","title":"o","text":"<ul> <li>OptionalString (dynampi)</li> <li>operator&lt;&lt; (dynampi)</li> </ul>"},{"location":"dynampi/namespace_members/#p","title":"p","text":"<ul> <li>patch (dynampi::version)</li> </ul>"},{"location":"dynampi/namespace_members/#r","title":"r","text":"<ul> <li>read_i64 (dynampi::detail)</li> <li>read_result_bytes (dynampi::detail)</li> <li>rma_wait_idle (dynampi::detail)</li> <li>round_up_8 (dynampi::detail)</li> </ul>"},{"location":"dynampi/namespace_members/#s","title":"s","text":"<ul> <li>StatisticsMode (dynampi)</li> <li>string (dynampi::version)</li> </ul>"},{"location":"dynampi/namespace_members/#w","title":"w","text":"<ul> <li>write_bytes (dynampi::detail)</li> <li>write_i64 (dynampi::detail)</li> </ul>"},{"location":"dynampi/namespace_members/#_","title":"_","text":"<ul> <li>_DYNAMPI_FAILBinOp (dynampi)</li> <li>_DYNAMPI_FAIL_ASSERT (dynampi)</li> </ul>"},{"location":"dynampi/namespace_member_functions/","title":"Namespace Member Functions","text":""},{"location":"dynampi/namespace_member_functions/#c","title":"c","text":"<ul> <li>commit_hash (dynampi::version)</li> <li>compile_date (dynampi::version)</li> </ul>"},{"location":"dynampi/namespace_member_functions/#d","title":"d","text":"<ul> <li>DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (dynampi)</li> </ul>"},{"location":"dynampi/namespace_member_functions/#i","title":"i","text":"<ul> <li>is_at_least (dynampi::version)</li> </ul>"},{"location":"dynampi/namespace_member_functions/#m","title":"m","text":"<ul> <li>mpi_type_size_bytes (dynampi::detail)</li> <li>mpi_fail (dynampi)</li> <li>mpi_manager_worker_distribution (dynampi)</li> </ul>"},{"location":"dynampi/namespace_member_functions/#o","title":"o","text":"<ul> <li>OptionalString (dynampi)</li> <li>operator&lt;&lt; (dynampi)</li> </ul>"},{"location":"dynampi/namespace_member_functions/#r","title":"r","text":"<ul> <li>read_i64 (dynampi::detail)</li> <li>read_result_bytes (dynampi::detail)</li> <li>rma_wait_idle (dynampi::detail)</li> <li>round_up_8 (dynampi::detail)</li> </ul>"},{"location":"dynampi/namespace_member_functions/#w","title":"w","text":"<ul> <li>write_bytes (dynampi::detail)</li> <li>write_i64 (dynampi::detail)</li> </ul>"},{"location":"dynampi/namespace_member_functions/#_","title":"_","text":"<ul> <li>_DYNAMPI_FAILBinOp (dynampi)</li> <li>_DYNAMPI_FAIL_ASSERT (dynampi)</li> </ul>"},{"location":"dynampi/namespace_member_variables/","title":"Namespace Member Variables","text":""},{"location":"dynampi/namespace_member_variables/#m","title":"m","text":"<ul> <li>major (dynampi::version)</li> <li>minor (dynampi::version)</li> </ul>"},{"location":"dynampi/namespace_member_variables/#p","title":"p","text":"<ul> <li>patch (dynampi::version)</li> </ul>"},{"location":"dynampi/namespace_member_variables/#s","title":"s","text":"<ul> <li>string (dynampi::version)</li> </ul>"},{"location":"dynampi/namespace_member_typedefs/","title":"Namespace Member Typedefs","text":""},{"location":"dynampi/namespace_member_typedefs/#m","title":"m","text":"<ul> <li>MPIDynamicWorkDistributor (dynampi)</li> </ul>"},{"location":"dynampi/namespace_member_enums/","title":"Namespace Member Enums","text":""},{"location":"dynampi/namespace_member_enums/#s","title":"s","text":"<ul> <li>StatisticsMode (dynampi)</li> </ul>"},{"location":"dynampi/functions/","title":"Functions","text":""},{"location":"dynampi/functions/#g","title":"g","text":"<ul> <li>get_option_value (template_options.hpp)</li> </ul>"},{"location":"dynampi/macros/","title":"Macros","text":""},{"location":"dynampi/macros/#d","title":"d","text":"<ul> <li>DYNAMPI_MPI_CHECK (mpi_error.hpp)</li> <li>DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (mpi_types.hpp)</li> <li>DYNAMPI_ASSERT (assert.hpp)</li> <li>DYNAMPI_ASSERT_BIN_OP (assert.hpp)</li> <li>DYNAMPI_ASSERT_EQ (assert.hpp)</li> <li>DYNAMPI_ASSERT_GE (assert.hpp)</li> <li>DYNAMPI_ASSERT_GT (assert.hpp)</li> <li>DYNAMPI_ASSERT_LE (assert.hpp)</li> <li>DYNAMPI_ASSERT_LT (assert.hpp)</li> <li>DYNAMPI_ASSERT_NE (assert.hpp)</li> <li>DYNAMPI_FAIL (assert.hpp)</li> <li>DYNAMPI_HAS_BUILTIN (assert.hpp)</li> <li>DYNAMPI_UNIMPLEMENTED (assert.hpp)</li> <li>DYNAMPI_UNREACHABLE (assert.hpp)</li> <li>DYNAMPI_STR (dynampi.hpp)</li> <li>DYNAMPI_STR_HELPER (dynampi.hpp)</li> <li>DYNAMPI_VERSION_STRING (dynampi.hpp)</li> </ul>"},{"location":"dynampi/variables/","title":"Variables","text":"<p>Nothing related to Variables found.</p>"},{"location":"dynampi/links/","title":"Links","text":"<ul> <li>Related Pages</li> <li>Modules</li> <li>Class List</li> <li>Namespace ListNamespace List</li> <li>Namespace Members</li> <li>Namespace Member Functions</li> <li>Namespace Member Variables</li> <li>Namespace Member Typedefs</li> <li>Namespace Member Enumerations</li> <li>Class Index</li> <li>Class Hierarchy</li> <li>Class Members</li> <li>Class Member Functions</li> <li>Class Member Variables</li> <li>Class Member Typedefs</li> <li>Class Member Enumerations</li> <li>Files</li> <li>File Variables</li> <li>File Functions</li> <li>File Macros</li> </ul>"}]}
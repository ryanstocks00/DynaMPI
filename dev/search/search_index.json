{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":""},{"location":"#dynampi","title":"DynaMPI","text":"<p>Header-only C++20 library for manager\u2013worker dynamic load distribution over MPI.</p> <p>DynaMPI hands out a stream of tasks to MPI ranks at runtime instead of partitioning them up front.  One rank acts as the manager and owns the task queue; the remaining ranks act as workers and pull work as they finish what they already have.  This keeps every rank busy when task costs are irregular or unknown in advance.</p> <p>Four distributors implement that contract with different communication strategies \u2014 from a 400-line two-sided loop to a tree of one-sided RMA windows that sustains millions of task hand-offs per second.  They share one interface, so switching between them is a single type change.</p>"},{"location":"#quick-start","title":"Quick start","text":""},{"location":"#fixed-set-of-index-tasks","title":"Fixed set of index tasks","text":"<pre><code>#include &lt;cmath&gt;\n#include &lt;iostream&gt;\n\n#include &lt;dynampi/dynampi.hpp&gt;\n\nint main(int argc, char** argv) {\n  MPI_Init(&amp;argc, &amp;argv);\n  {\n    auto work = [](size_t task) -&gt; double {\n      return std::sqrt(static_cast&lt;double&gt;(task));\n    };\n\n    // Collective: every rank calls it. Workers loop internally and return\n    // std::nullopt; the manager returns the results.\n    auto results = dynampi::mpi_manager_worker_distribution&lt;double&gt;(100, work);\n\n    if (results.has_value()) {\n      for (double r : *results) std::cout &lt;&lt; r &lt;&lt; \"\\n\";\n    }\n  }\n  MPI_Finalize();\n}\n</code></pre> <p>Results are unordered by default</p> <p>The default distributor is hierarchical, and hierarchical results come back in completion order \u2014 <code>(*results)[i]</code> is not the result of task <code>i</code>. Either carry the index inside your result type, or use <code>NaiveWorkDistributor</code> (see Result ordering).</p>"},{"location":"#tasks-discovered-as-you-go","title":"Tasks discovered as you go","text":"<p>When the task set is not known up front, construct a distributor directly and alternate insertion with collection:</p> <pre><code>#include &lt;dynampi/dynampi.hpp&gt;\n\nusing Task = int;\nusing Result = double;\n\ndynampi::DynamicWorkDistributor&lt;Task, Result&gt; dist(\n    [](Task t) -&gt; Result { return std::sqrt(static_cast&lt;double&gt;(t)); });\n\nif (dist.is_root_manager()) {\n  dist.insert_tasks({1, 2, 3, 4, 5});\n  auto first = dist.finish_remaining_tasks();   // 5 results\n\n  dist.insert_tasks({6, 7, 8});\n  auto second = dist.finish_remaining_tasks();  // 3 results\n}\n// Non-manager ranks entered the worker loop inside the constructor\n// (auto_run_workers defaults to true) and return here once the\n// manager's distributor is destroyed.\n</code></pre>"},{"location":"#mental-model","title":"Mental model","text":"Concept Meaning Manager Rank <code>Config::manager_rank</code> (default <code>0</code>). Owns the queue, collects results, and is the only rank allowed to call <code>insert_task*</code> / <code>run_tasks</code> / <code>finish_remaining_tasks</code>. Worker Every other rank. Runs <code>run_worker()</code> \u2014 automatically from the constructor unless you set <code>auto_run_workers = false</code>. Node manager In the hierarchical distributors, an intermediate rank (one per node by default) that relays batches of tasks down and results up, so the root manager never talks to every rank directly. Task A <code>TaskT</code> value. Must be sendable \u2014 see task and result types. Result Whatever the worker function returns. Collected on the manager. <p>Construction is collective over <code>Config::comm</code>, and so is destruction: every rank must construct and destroy the distributor.</p>"},{"location":"#choosing-a-distributor","title":"Choosing a distributor","text":"Situation Use Small communicator, need results in task order or task priorities <code>NaiveWorkDistributor</code> General purpose, multi-node <code>DynamicWorkDistributor</code> (default) Fine-grained tasks, want the highest hand-off rate at moderate scale <code>LockFreeRMAWorkDistributor</code> Same, but at large node counts where one manager window saturates <code>HierarchicalLockFreeRMAWorkDistributor</code> <p>The full comparison, including protocol descriptions and the constraints each distributor imposes, is in Implementations.</p>"},{"location":"#where-to-go-next","title":"Where to go next","text":"<ul> <li>Examples \u2014   seven short runnable programs, one per concept.</li> <li>Installation \u2014 requirements, CMake, and the header-only path.</li> <li>API Reference \u2014 every public type, method, config field and option.</li> <li>Implementations \u2014 how each distributor works and when to pick it.</li> <li>Lock-free RMA design \u2014 window layouts and   synchronisation rules behind the RMA distributors.</li> </ul> <p>Licensed under the Apache License 2.0.</p>"},{"location":"api/","title":"API Reference","text":""},{"location":"api/#api-reference","title":"API Reference","text":"<p>Everything lives in namespace <code>dynampi</code>.</p>"},{"location":"api/#headers","title":"Headers","text":"Header Provides <code>&lt;dynampi/task_error.hpp&gt;</code> <code>TaskError</code>, <code>TaskFailure</code>, <code>kMaxTaskErrorMessage</code> (pulled in by every distributor) <code>&lt;dynampi/impl/naive_distributor.hpp&gt;</code> <code>NaiveWorkDistributor</code> <code>&lt;dynampi/impl/hierarchical_distributor.hpp&gt;</code> <code>HierarchicalWorkDistributor</code> <code>&lt;dynampi/impl/lockfree_rma_distributor.hpp&gt;</code> <code>LockFreeRMAWorkDistributor</code> <code>&lt;dynampi/impl/hierarchical_lockfree_rma_distributor.hpp&gt;</code> <code>HierarchicalLockFreeRMAWorkDistributor</code> <code>&lt;dynampi/version.hpp&gt;</code> <code>dynampi::version</code> <code>&lt;dynampi/mpi/mpi_types.hpp&gt;</code> <code>MPI_Type&lt;T&gt;</code> \u2014 the trait you specialise for custom payloads <code>&lt;dynampi/mpi/mpi_communicator.hpp&gt;</code> <code>MPICommunicator</code>, <code>CommStatistics</code>, <code>StatisticsMode</code>, <code>track_statistics</code> <code>&lt;dynampi/mpi/mpi_error.hpp&gt;</code> <code>DYNAMPI_MPI_CHECK</code> <code>&lt;dynampi/utilities/timer.hpp&gt;</code> <code>Timer</code> <code>&lt;dynampi/utilities/assert.hpp&gt;</code> <code>DYNAMPI_ASSERT*</code> <code>&lt;dynampi/utilities/printing.hpp&gt;</code> <code>operator&lt;&lt;</code> for STL containers <p>Every header is usable with a plain <code>-I include</code>; nothing requires CMake or predefined macros.</p>"},{"location":"api/#mpi_manager_worker_distribution","title":"<code>mpi_manager_worker_distribution</code>","text":"<pre><code>template &lt;typename ResultT,\n          template &lt;typename, typename, typename...&gt; typename Distributor =\n              HierarchicalWorkDistributor&gt;\nstd::optional&lt;std::vector&lt;ResultT&gt;&gt; mpi_manager_worker_distribution(\n    size_t n_tasks,\n    std::function&lt;ResultT(size_t)&gt; worker_function,\n    MPI_Comm comm = MPI_COMM_WORLD,\n    int manager_rank = 0);\n</code></pre> <p>The one-call entry point.  Distributes tasks <code>0 .. n_tasks-1</code> across <code>comm</code>. Collective: every rank must call it.  Workers run the worker loop internally and return <code>std::nullopt</code>; the manager returns the results.</p> Parameter Default Description <code>n_tasks</code> \u2014 Number of tasks; the task value is the index itself <code>worker_function</code> \u2014 <code>size_t \u2192 ResultT</code>, called on the worker rank <code>comm</code> <code>MPI_COMM_WORLD</code> Communicator (duplicated internally) <code>manager_rank</code> <code>0</code> Rank that distributes and collects <p><code>Distributor</code> may be any of the five full distributors.  The default is <code>HierarchicalWorkDistributor</code>, so results are unordered \u2014 pass <code>NaiveWorkDistributor</code> if you need <code>(*result)[i]</code> to be task <code>i</code>.</p> <pre><code>auto ordered = dynampi::mpi_manager_worker_distribution&lt;\n    double, dynampi::NaiveWorkDistributor&gt;(100, work);\n</code></pre>"},{"location":"api/#dynamicworkdistributor","title":"<code>DynamicWorkDistributor</code>","text":"<pre><code>template &lt;typename TaskT, typename ResultT, typename... Options&gt;\nusing DynamicWorkDistributor =\n    HierarchicalWorkDistributor&lt;TaskT, ResultT, Options...&gt;;\n</code></pre> <p>An alias, not a distinct type.  Use it when you want \"the recommended default\" to track future changes.</p>"},{"location":"api/#the-distributor-interface","title":"The distributor interface","text":"<p>The five full distributors share this shape:</p> <pre><code>template &lt;typename TaskT, typename ResultT, typename... Options&gt;\nclass Distributor {\n public:\n  struct Config;\n  struct RunConfig;\n  static const bool ordered;\n\n  explicit Distributor(std::function&lt;ResultT(TaskT)&gt; worker_function,\n                       Config config = {});\n  ~Distributor();   // calls finalize() if you have not\n};\n</code></pre>"},{"location":"api/#construction-and-lifecycle","title":"Construction and lifecycle","text":"<p>Construction and destruction are both collective over <code>Config::comm</code>.  The communicator is duplicated internally, so DynaMPI messages never interfere with the caller's own traffic.</p> <p>With <code>auto_run_workers = true</code> (default) non-manager ranks enter <code>run_worker()</code> inside the constructor and only return once shutdown is signalled \u2014 typically when the manager's distributor is destroyed.  Set it to <code>false</code> to control that yourself:</p> <pre><code>Distributor::Config cfg;\ncfg.auto_run_workers = false;\nDistributor dist(work, cfg);\n\nif (dist.is_root_manager()) {\n  dist.insert_tasks(tasks);\n  auto results = dist.finish_remaining_tasks();\n} else {\n  dist.run_worker();      // returns when the manager finalizes\n}\n</code></pre>"},{"location":"api/#common-config-fields","title":"Common <code>Config</code> fields","text":"<p>Present on all five full distributors:</p> Field Type Default Description <code>comm</code> <code>MPI_Comm</code> <code>MPI_COMM_WORLD</code> Communicator to distribute over <code>manager_rank</code> <code>int</code> <code>0</code> Rank that owns the queue and collects results <code>auto_run_workers</code> <code>bool</code> <code>true</code> Non-manager ranks run the worker loop from the constructor <p>Distributor-specific fields \u2014 tree shape, pipelining, RMA window capacities \u2014 are documented per class in Implementations.</p>"},{"location":"api/#runconfig","title":"<code>RunConfig</code>","text":"Field Type Default Description <code>target_num_tasks</code> <code>size_t</code> <code>SIZE_MAX</code> Return once this many results are ready <code>allow_more_than_target_tasks</code> <code>bool</code> <code>true</code> If <code>false</code>, clip the returned vector to <code>target_num_tasks</code>; the excess stays buffered for the next call <code>max_seconds</code> <code>optional&lt;double&gt;</code> <code>nullopt</code> Soft deadline for this call <p>Two semantics worth knowing:</p> <ul> <li>On the ordered distributors (<code>NaiveWorkDistributor</code> and the two RMA   distributors), <code>target_num_tasks</code> counts contiguous results from the front   of the ordered stream.  On <code>HierarchicalWorkDistributor</code> (unordered), it   counts however many have arrived, in any order.</li> <li><code>max_seconds</code> is checked between messages, not asynchronously.  A call already   blocked in a probe or an RMA wait will overshoot the deadline until the next   event arrives.</li> </ul>"},{"location":"api/#methods","title":"Methods","text":"Method Returns Notes <code>is_root_manager()</code> <code>bool</code> Safe on every rank <code>insert_task(TaskT)</code> <code>void</code> Manager only. Disabled when <code>enable_prioritization</code> is set <code>insert_task(const TaskT&amp;, double priority)</code> <code>void</code> Manager only. Requires <code>enable_prioritization</code> (see the availability table) <code>insert_tasks(const std::vector&lt;TaskT&gt;&amp;)</code> <code>void</code> Manager only. Preferred over repeated <code>insert_task</code> on the RMA distributors <code>insert_tasks(Range&amp;&amp;)</code> <code>void</code> Hierarchical variants only; any <code>std::ranges::input_range</code> <code>run_tasks(RunConfig)</code> <code>std::vector&lt;ResultT&gt;</code> Manager only. Drives distribution and returns completed results <code>finish_remaining_tasks()</code> <code>std::vector&lt;ResultT&gt;</code> Manager only. <code>run_tasks({})</code> \u2014 runs until everything outstanding is collected <code>gather_once()</code> <code>std::vector&lt;ResultT&gt;</code> Manager only, lock-free RMA distributors. One harvest pass, no retry loop <code>run_worker()</code> <code>void</code> Non-manager ranks. Returns when shutdown is signalled <code>remaining_tasks_count()</code> <code>size_t</code> Manager only. See the note below <code>finalize()</code> <code>void</code> Signals shutdown. Called by the destructor if you do not <code>get_statistics()</code> <code>const Statistics&amp;</code> Manager only. Requires a <code>track_statistics</code> option <p><code>remaining_tasks_count()</code> is not uniform</p> <p>On the two-sided distributors it is the number of unallocated tasks still in the manager's queue, excluding tasks already dispatched.  On <code>LockFreeRMAWorkDistributor</code> it is published-minus-returned, and on <code>HierarchicalLockFreeRMAWorkDistributor</code> it is the number still outstanding in the tree.  Use it as a progress indicator, not as an exact cross-distributor quantity.</p>"},{"location":"api/#availability","title":"Availability","text":"Naive Hierarchical LockFreeRMA Hier. LockFreeRMA <code>insert_task(TaskT)</code> \u2713 \u2713 \u2713 \u2713 <code>insert_task(task, priority)</code> \u2713 \u2713 \u00b9 \u2713 \u00b2 \u2014 <code>insert_tasks(vector)</code> \u2713 \u2713 \u2713 \u2713 <code>insert_tasks(Range)</code> \u2014 \u2713 \u2014 \u2014 <code>run_tasks</code> / <code>finish_remaining_tasks</code> \u2713 \u2713 \u2713 \u2713 <code>gather_once</code> \u2014 \u2014 \u2713 \u2713 <code>run_worker</code> / <code>finalize</code> \u2713 \u2713 \u2713 \u2713 <code>get_statistics</code> \u2713 \u2713 \u2713 \u2014 <code>num_workers()</code> \u2014 \u2014 \u2713 \u2014 <code>static ordered</code> <code>true</code> <code>false</code> <code>true</code> <code>true</code> <code>static prioritize_tasks</code> \u2014 \u2713 \u2014 \u2014 <p>\u00b9 Compiles, but the hierarchical batch path raises <code>DYNAMPI_UNIMPLEMENTED</code> as soon as a manager has children \u2014 treat prioritization as naive-only. \u00b2 Accepted for interface compatibility; the priority is ignored.</p>"},{"location":"api/#options","title":"Options","text":"<p>Options are empty tag types passed through the <code>Options...</code> pack.  Unrecognised options are ignored, so a generic wrapper can pass the same pack to every distributor.</p> <pre><code>using Prioritised = dynampi::NaiveWorkDistributor&lt;\n    int, double, dynampi::enable_prioritization&gt;;\n\nusing Instrumented = dynampi::DynamicWorkDistributor&lt;\n    int, double, dynampi::track_statistics&lt;dynampi::StatisticsMode::Detailed&gt;&gt;;\n</code></pre> Option Effect <code>enable_prioritization</code> Swaps the FIFO queue for a <code>std::priority_queue</code> and enables <code>insert_task(task, priority)</code> (descending priority). Functional on <code>NaiveWorkDistributor</code> only. <code>track_statistics&lt;Mode&gt;</code> <code>Mode</code> is <code>StatisticsMode::None</code> (default), <code>Aggregated</code>, or <code>Detailed</code>. Defaults to <code>Detailed</code> if written as <code>track_statistics&lt;&gt;</code>."},{"location":"api/#choosing-a-statistics-mode","title":"Choosing a statistics mode","text":"Mode Message and byte counters Per-rank task counts <code>send_time</code> / <code>recv_time</code> <code>None</code> \u2014 \u2014 \u2014 <code>Aggregated</code> \u2713 \u2713 \u2014 <code>Detailed</code> \u2713 \u2713 \u2713 <p><code>Aggregated</code> only increments counters, with no clock reads in the hot path \u2014 that is the mode to use while benchmarking.  <code>Detailed</code> additionally accumulates the wall time spent inside the very calls that bump <code>send_count</code> / <code>recv_count</code>, which costs two <code>MPI_Wtime()</code> calls per message.</p>"},{"location":"api/#statistics","title":"Statistics","text":"<p><code>get_statistics()</code> is only declared when a <code>track_statistics</code> option requests a mode other than <code>None</code>, and may only be called on the manager.  The returned reference stays valid for the distributor's lifetime.</p> <pre><code>struct CommStatistics {\n  int    send_count, recv_count, collective_count, atomic_count;\n  size_t bytes_sent, bytes_received, atomic_bytes;\n  double send_time, recv_time;   // Detailed only; zero under Aggregated\n\n  double average_send_size() const;\n  double average_receive_size() const;\n  void   reset();\n};\n</code></pre> <p><code>CommStatistics</code> counts only the calling rank's own traffic; it is not reduced across the communicator.  For the RMA distributors, <code>put_bytes</code> / <code>get_bytes</code> count one logical transfer each (even when chunked internally), and <code>MPI_Fetch_and_op</code> is counted separately under <code>atomic_count</code> / <code>atomic_bytes</code>.</p> <p><code>send_time</code> / <code>recv_time</code> cover exactly the calls that increment <code>send_count</code> / <code>recv_count</code> \u2014 the blocking sends and receives, the <code>MPI_Isend</code> post, and <code>MPI_Put</code> / <code>MPI_Get</code>.  Time spent blocked in <code>MPI_Probe</code> waiting for work to arrive, and time inside <code>MPI_Fetch_and_op</code>, are not included.</p> <p>Each distributor defines its own nested <code>Statistics</code>:</p> <pre><code>// NaiveWorkDistributor\nstruct Statistics {\n  const CommStatistics&amp; comm_statistics;\n  std::vector&lt;size_t&gt;   worker_task_counts;  // indexed by worker index\n};\n\n// HierarchicalWorkDistributor\nstruct Statistics {\n  const CommStatistics&amp;                     comm_statistics;\n  std::optional&lt;std::vector&lt;size_t&gt;&gt;        worker_task_counts;  // by world rank\n};\n\n// LockFreeRMAWorkDistributor\nstruct Statistics {\n  const CommStatistics&amp; comm_statistics;\n};\n</code></pre> <p>Read the fields you need rather than assuming a shared layout:</p> <pre><code>const auto&amp; stats = dist.get_statistics();\nstd::cout &lt;&lt; stats.comm_statistics.bytes_sent &lt;&lt; \"\\n\";\n</code></pre> <p>Support and caveats:</p> Distributor <code>Aggregated</code> <code>Detailed</code> Notes <code>NaiveWorkDistributor</code> \u2713 \u2713 <code>worker_task_counts</code> is indexed by worker index (this rank's communicator rank with the manager removed) <code>HierarchicalWorkDistributor</code> \u2713 \u2713 <code>worker_task_counts</code> is filled by an <code>MPI_Gather</code> inside <code>finalize()</code>, so it is <code>nullopt</code> before then, and holds locally-executed task counts by world rank <code>LockFreeRMAWorkDistributor</code> \u2713 \u2713 Communication counters only <code>HierarchicalLockFreeRMAWorkDistributor</code> \u2014 \u2014 Options accepted and ignored; no <code>get_statistics()</code> <p>Statistics enable a collective in <code>finalize()</code></p> <p>On the hierarchical two-sided distributors, <code>finalize()</code> performs an <code>MPI_Gather</code> when statistics are enabled.  Every rank must therefore reach <code>finalize()</code> (directly or via its destructor).</p>"},{"location":"api/#task-and-result-types","title":"Task and result types","text":"<p><code>TaskT</code> and <code>ResultT</code> must have a <code>dynampi::MPI_Type&lt;T&gt;</code> specialization.  Shipped out of the box:</p> <ul> <li>all MPI-mappable scalars (<code>char</code>, <code>short</code>, <code>int</code>, <code>long</code>, <code>long long</code> and   unsigned variants, <code>float</code>, <code>double</code>, <code>long double</code>, <code>bool</code>, <code>std::byte</code>);</li> <li><code>std::vector&lt;T&gt;</code> for any supported scalar <code>T</code> (not <code>std::vector&lt;bool&gt;</code>,   which is bit-packed \u2014 a <code>static_assert</code> catches it);</li> <li><code>std::string</code>.</li> </ul> <p><code>MPI_Type&lt;T&gt;::resize_required</code> is <code>false</code> for fixed-size types and <code>true</code> for the variable-length ones.  That flag decides where a type may be used:</p> Payload Naive Hierarchical LockFreeRMA / Hier. LockFreeRMA Fixed-size scalar \u2713 \u2713 \u2713 Fixed-size struct \u2713 \u2713 \u2713 Variable-length \u2713 \u2713 \u2713, up to <code>max_task_count</code> / <code>max_result_count</code> elements"},{"location":"api/#custom-types","title":"Custom types","text":"<p>Specialise <code>MPI_Type</code> for your own type.  DynaMPI moves a value as <code>count()</code> elements of <code>value</code>, read and written through <code>ptr()</code>, so the storage must be contiguous.</p> <pre><code>struct Point { double x, y, z; };\nstatic_assert(sizeof(Point) == 3 * sizeof(double));\n\ntemplate &lt;&gt;\nstruct dynampi::MPI_Type&lt;Point&gt; {\n  inline static const MPI_Datatype value = MPI_DOUBLE;\n  inline static const bool resize_required = false;  // fixed size\n\n  static int   count(const Point&amp;) noexcept { return 3; }\n  static void  resize(Point&amp;, int) noexcept {}       // fixed size: nothing to do\n  static void* ptr(Point&amp; p) noexcept { return &amp;p; }\n  static const void* ptr(const Point&amp; p) noexcept { return &amp;p; }\n};\n</code></pre> <p>That works with every distributor.  A value may span several datatype elements \u2014 three <code>MPI_DOUBLE</code>s here \u2014 and everything that sizes a buffer per value accounts for it: the fixed-width RMA window slots, and the element count of a batched <code>std::vector&lt;T&gt;</code> message.</p> <p>Genuinely variable-length types work the same way with <code>resize_required = true</code>, <code>count()</code> returning the current element count, and <code>resize(value, n)</code> growing the storage to <code>n</code> elements \u2014 exactly how the shipped <code>std::vector&lt;T&gt;</code> and <code>std::string</code> specializations are written.</p> <p>Two requirements to be aware of:</p> <ul> <li>The whole object must be sent. <code>count() * MPI_Type_size(value)</code> has to   equal <code>sizeof(T)</code> for a fixed-size type, and the storage at <code>ptr()</code> has to be   contiguous.</li> <li>The object must be a whole number of elements. A 12-byte struct described   by an 8-byte datatype cannot be expressed as N elements; distributors detect   this in their constructor and throw <code>std::invalid_argument</code> naming the type,   in release builds too.  Describe such a type with a datatype that fits \u2014 three   <code>MPI_FLOAT</code>s rather than one and a half <code>MPI_DOUBLE</code>s \u2014 or with <code>MPI_BYTE</code> and   <code>count() == sizeof(T)</code>.</li> </ul> <p>examples/04_custom_task_type.cpp is a complete working version of this.</p>"},{"location":"api/#version","title":"Version","text":"<pre><code>#include &lt;dynampi/dynampi.hpp&gt;\n\nnamespace dv = dynampi::version;\n\ndv::string;                  // \"v0.0.1\"  (std::string_view)\ndv::major; dv::minor; dv::patch;   // 0, 0, 1\ndv::is_at_least(0, 0, 1);    // constexpr bool\ndv::compile_date();          // __DATE__ \" \" __TIME__ of the translation unit\ndv::commit_hash();           // \"abc1234\" or \"abc1234-dirty\"\n</code></pre> <p><code>major</code>/<code>minor</code>/<code>patch</code> and <code>commit_hash()</code> come from the macros the CMake target defines.</p>"},{"location":"api/#utilities","title":"Utilities","text":""},{"location":"api/#timer","title":"<code>Timer</code>","text":"<pre><code>#include &lt;dynampi/utilities/timer.hpp&gt;\n\ndynampi::Timer timer;                              // starts immediately\ndynampi::Timer paused{dynampi::Timer::AutoStart::No};\n\ntimer.elapsed();   // std::chrono::duration&lt;double&gt;, valid while running\ntimer.stop();      // pause, returns total elapsed\ntimer.start();     // resume (accumulates)\ntimer.reset();     // back to zero, restarts unless AutoStart::No\n\nstd::cout &lt;&lt; timer;  // \"12.345 seconds\"\n</code></pre> <p>Backed by <code>std::chrono::high_resolution_clock</code>.  <code>start()</code> on an already-running timer and <code>stop()</code> on a stopped one trip <code>assert</code>s.</p>"},{"location":"api/#assertions","title":"Assertions","text":"<pre><code>#include &lt;dynampi/utilities/assert.hpp&gt;\n\nDYNAMPI_ASSERT(cond);                    // optional trailing message args\nDYNAMPI_ASSERT(cond, \"context: \", value);\nDYNAMPI_ASSERT_EQ(a, b);                 // also NE, LT, LE, GT, GE\nDYNAMPI_ASSERT_EQ(a, b, \"while merging\");\nDYNAMPI_FAIL(\"unreachable state\");\nDYNAMPI_UNIMPLEMENTED();\n</code></pre> <p>On failure these print <code>DynaMPI assertion failed on rank N: \u2026</code> with the failing expression, both operand values for the binary forms, and (where <code>std::source_location</code> is available) function, file and line \u2014 then throw <code>std::runtime_error</code>.  They are compiled out entirely under <code>NDEBUG</code>, at which point <code>DYNAMPI_FAIL</code> / <code>DYNAMPI_UNIMPLEMENTED</code> degrade to <code>__builtin_unreachable()</code>, so do not rely on them for release-build control flow.</p> <p>Message arguments are streamed with <code>operator&lt;&lt;</code>, so anything printable works \u2014 including the container printers below.</p>"},{"location":"api/#mpi-error-checking","title":"MPI error checking","text":"<pre><code>#include &lt;dynampi/mpi/mpi_error.hpp&gt;\n\nDYNAMPI_MPI_CHECK(MPI_Send, (buf, count, type, dest, tag, comm));\n</code></pre> <p>Calls the function and, on any result other than <code>MPI_SUCCESS</code>, throws <code>std::runtime_error</code> containing the MPI error string, the call site expression, and file:line.  Note the argument list is a single parenthesised group.</p>"},{"location":"api/#container-printing","title":"Container printing","text":"<pre><code>#include &lt;dynampi/utilities/printing.hpp&gt;\n\nusing namespace dynampi;  // required: the operators live in namespace dynampi\n\nstd::cout &lt;&lt; std::vector&lt;int&gt;{1, 2, 3};        // [1, 2, 3]\nstd::cout &lt;&lt; std::optional&lt;int&gt;{42};           // Some(42)\nstd::cout &lt;&lt; std::optional&lt;int&gt;{};             // None\nstd::cout &lt;&lt; std::pair{1, 'a'};                // (1, a)\n</code></pre> <p>Overloads exist for <code>std::vector</code>, <code>std::array</code>, <code>std::span</code>, <code>std::set</code>, <code>std::pair</code>, <code>std::tuple</code>, <code>std::optional</code> and <code>std::byte</code>.</p> <p>Because these operate on standard-library types, argument-dependent lookup will not find them \u2014 bring them in with <code>using namespace dynampi;</code> (or a targeted <code>using dynampi::operator&lt;&lt;;</code>) in the scope where you stream.  Assertion message arguments are an exception: <code>DYNAMPI_ASSERT(cond, some_vector)</code> streams from inside <code>namespace dynampi</code>, so container printing works there with no using-declaration.</p>"},{"location":"api/#mpi-wrappers","title":"MPI wrappers","text":"<p><code>dynampi::MPICommunicator&lt;Options...&gt;</code> and <code>dynampi::MPIGroup</code> are the RAII wrappers the distributors are built on.  <code>MPICommunicator</code> owns (or references) a communicator, offers typed <code>send</code> / <code>recv</code> / <code>probe</code> / <code>broadcast</code> / <code>gather</code> and RMA helpers (<code>put_bytes</code>, <code>get_bytes</code>, <code>fetch_and_op</code>), records <code>CommStatistics</code> when a <code>track_statistics</code> option is supplied, and provides <code>split()</code> / <code>split_by_node()</code>.  <code>MPIGroup</code> wraps <code>MPI_Group</code> for rank translation.  They are public and usable directly, but the distributors are the supported surface.</p>"},{"location":"implementations/","title":"Implementations","text":""},{"location":"implementations/#distributor-implementations","title":"Distributor Implementations","text":"<p>DynaMPI ships six distributors.  Five share the full manager\u2013worker interface (<code>insert_task*</code> / <code>run_tasks</code> / <code>run_worker</code> / <code>finalize</code>); the sixth is a deliberately minimal parallel-for helper with its own small API.</p> Class Header Communication Topology Ordered <code>NaiveWorkDistributor</code> <code>impl/naive_distributor.hpp</code> Two-sided <code>Send</code>/<code>Recv</code> Flat Yes <code>HierarchicalWorkDistributor</code> <code>impl/hierarchical_distributor.hpp</code> Two-sided, batched Tree No <code>LockFreeRMAWorkDistributor</code> <code>impl/lockfree_rma_distributor.hpp</code> Passive-target RMA Flat Yes <code>HierarchicalLockFreeRMAWorkDistributor</code> <code>impl/hierarchical_lockfree_rma_distributor.hpp</code> Passive-target RMA Tree Yes <p>Only <code>NaiveWorkDistributor</code>, <code>HierarchicalWorkDistributor</code> are pulled in by <code>&lt;dynampi/dynampi.hpp&gt;</code>. The other three need their own <code>#include</code> \u2014 see Headers.</p>"},{"location":"implementations/#shared-behaviour","title":"Shared behaviour","text":""},{"location":"implementations/#result-ordering","title":"Result ordering","text":"<p>Ordering is exposed as a compile-time constant, <code>Distributor::ordered</code>:</p> <ul> <li><code>NaiveWorkDistributor</code>, <code>LockFreeRMAWorkDistributor</code>,   <code>HierarchicalLockFreeRMAWorkDistributor</code> (<code>ordered == true</code>) \u2014 the manager   buffers results by task ID (a result table slot for the RMA distributors,   keyed the same way as the task it answers) and only releases a contiguous   prefix, so returned results are always in insertion order.  A slow task   therefore holds back every result behind it, all the way up the tree for the   hierarchical variant.</li> <li><code>HierarchicalWorkDistributor</code> (<code>ordered == false</code>) \u2014 results are   returned as they are confirmed complete, roughly submission order but not   guaranteed.</li> </ul> <p>If you need to know which task a result came from and the distributor is unordered, carry the identity in the result type (e.g. <code>std::pair&lt;size_t, T&gt;</code>).</p>"},{"location":"implementations/#lifecycle","title":"Lifecycle","text":"<ol> <li>Construction is collective over <code>Config::comm</code>.  The communicator is    duplicated, so DynaMPI traffic never collides with the caller's.</li> <li>With <code>auto_run_workers = true</code> (the default), every non-manager rank enters    <code>run_worker()</code> inside the constructor and does not return until shutdown is    signalled.  Set it to <code>false</code> if you want to call <code>run_worker()</code> yourself.</li> <li>The manager drives distribution with <code>run_tasks()</code> /    <code>finish_remaining_tasks()</code>.</li> <li><code>finalize()</code> signals shutdown to the workers.  The destructor calls it if you    have not.</li> <li>Destruction is collective too \u2014 the RMA distributors free windows and    barrier inside their destructors.</li> </ol>"},{"location":"implementations/#failing-tasks","title":"Failing tasks","text":"<p>A task that throws does not abort the job.  Every distributor catches whatever escapes the worker function, substitutes a default-constructed <code>ResultT</code> so the task still occupies its slot in the result set, and carries the message back to the manager out of band \u2014 a <code>TASK_ERROR</code> message alongside the result for the two-sided distributors, a small fixed-size table in the window for the RMA ones.</p> <p>What the manager then does is up to <code>Config::rethrow_task_errors</code>:</p> <ul> <li><code>true</code> (default) \u2014 propagate. <code>run_tasks()</code> / <code>finish_remaining_tasks()</code>   throw <code>dynampi::TaskFailure</code>, carrying the failing rank and message.  It is   thrown before results are drained, so whoever catches it can call again and   still collect everything gathered so far.  Each failure is thrown once;   <code>finalize()</code> and the destructors never throw.</li> <li><code>false</code> \u2014 recover.  Distribution runs to completion and the failures come   back from <code>take_task_errors()</code> as a <code>std::vector&lt;TaskError&gt;</code>, oldest first.</li> </ul> <pre><code>dynampi::DynamicWorkDistributor&lt;int, double&gt;::Config config;\nconfig.rethrow_task_errors = false;\ndynampi::DynamicWorkDistributor&lt;int, double&gt; distributor(work, config);\n\nif (distributor.is_root_manager()) {\n  distributor.insert_tasks({1, 2, 3});\n  auto results = distributor.finish_remaining_tasks();   // one entry per task\n  for (const auto&amp; failure : distributor.take_task_errors()) {\n    std::cerr &lt;&lt; \"rank \" &lt;&lt; failure.worker_rank &lt;&lt; \": \" &lt;&lt; failure.message &lt;&lt; \"\\n\";\n  }\n}\n</code></pre> <p>Messages are truncated to <code>dynampi::kMaxTaskErrorMessage</code> (240) characters, and the RMA distributors keep at most 16 records per window \u2014 beyond that the failures still happen and the run still completes, but the extra messages are dropped.  Anything not thrown or taken by the time the distributor is destroyed is reported to <code>stderr</code> rather than vanishing.</p>"},{"location":"implementations/#single-rank-runs","title":"Single-rank runs","text":"<p>Every distributor handles a communicator of size 1 by executing tasks inline on the manager, with no MPI window or message traffic.  Tests and serial debugging therefore work unchanged.</p>"},{"location":"implementations/#naiveworkdistributor","title":"NaiveWorkDistributor","text":"<p>Best for: small communicators, ordered results, task priorities, and as a reference implementation when debugging a workload.</p> <p>The manager talks to every worker directly.  Workers announce themselves with an empty <code>REQUEST</code>, then receive one task at a time; each <code>RESULT</code> doubles as the signal that the worker is free again.</p> <pre><code>Worker                                Manager\n  send REQUEST \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25ba   push worker onto free stack\n  probe / recv TASK  \u25c4\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500    send TASK (pop free worker)\n  execute\n  send RESULT \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25ba   recv RESULT, store by task ID\n                                      push worker onto free stack\n  probe / recv TASK  \u25c4\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500    send TASK\n  ...\n  probe / recv DONE  \u25c4\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500    finalize(): send DONE to every worker\n</code></pre> <ul> <li>Ordering: strict \u2014 results are buffered by task ID and released as a   contiguous prefix.</li> <li>Prioritization: supported, and this is the only distributor where it   works.  With <code>dynampi::enable_prioritization</code>, <code>insert_task(task, priority)</code>   feeds a <code>std::priority_queue</code> and higher priorities are dispatched first.   Note that <code>insert_tasks(vector)</code> is disabled in this mode.</li> <li>Variable-length payloads: supported for both <code>TaskT</code> and <code>ResultT</code>   (<code>std::vector&lt;T&gt;</code>, <code>std::string</code>, \u2026).</li> <li>Statistics: <code>Aggregated</code> and <code>Detailed</code> both supported (see   Statistics).  <code>worker_task_counts</code> is indexed by worker   index \u2014 the rank with the manager removed.</li> </ul>"},{"location":"implementations/#cost-model","title":"Cost model","text":"<p>The manager handles two messages per task and is the only rank doing so, which makes it the bottleneck at a few hundred ranks.  There is no batching and no prefetching: a worker is idle for one manager round trip between finishing a task and receiving the next.</p>"},{"location":"implementations/#configuration","title":"Configuration","text":"<p>Beyond the common fields there are none that take effect.</p>"},{"location":"implementations/#hierarchicalworkdistributor-dynamicworkdistributor","title":"HierarchicalWorkDistributor (<code>DynamicWorkDistributor</code>)","text":"<p>Best for: the general multi-node case.  This is the default used by <code>mpi_manager_worker_distribution</code>, and <code>dynampi::DynamicWorkDistributor</code> is an alias for it.</p> <p>Ranks are arranged in a tree.  Leaf workers only ever talk to their node manager; node managers exchange batches with their own parent, so the root manager's message count scales with the number of node managers rather than the number of ranks.</p> <pre><code>  Root manager \u2500\u2500\u252c\u2500\u2500 Node manager 0 \u2500\u2500\u252c\u2500\u2500 Worker 0\n                 \u2502       (node 0)     \u251c\u2500\u2500 Worker 1\n                 \u2502                    \u2514\u2500\u2500 Worker 2\n                 \u251c\u2500\u2500 Node manager 1 \u2500\u2500\u252c\u2500\u2500 Worker 3\n                 \u2502       (node 1)     \u2514\u2500\u2500 Worker 4\n                 \u2514\u2500\u2500 Node manager 2 \u2500\u2500\u252c\u2500\u2500 Worker 5\n                         (node 2)     \u2514\u2500\u2500 Worker 6\n</code></pre>"},{"location":"implementations/#topology","title":"Topology","text":"<p><code>manager_per_node = true</code> (default). <code>MPI_Comm_split_type(MPI_COMM_TYPE_SHARED)</code> discovers physical nodes.  Local rank 0 of each node becomes that node's manager; the root manager is excluded from its own node's local group so it is never also a node manager.  The root manager plus the node managers form the leader layer.</p> <p>Two knobs reshape this:</p> <ul> <li><code>max_local_group_size &gt; 0</code> splits large nodes into several contiguous local   groups, producing more (smaller) node managers.  This reduces per-manager   contention, and lets a single-node job exercise the multi-manager paths.</li> <li><code>max_upper_fanout</code> caps how many direct children any leader-layer rank may   have.  If there are more managers than that, they are recursively grouped   into a k-ary tree of intermediate leaders, so not even the manager exceeds the   cap.  <code>-1</code> (default) is auto: stay flat at \u2264 32 managers, otherwise use the   smallest power of two \u2265 <code>sqrt(manager_count)</code>.  <code>0</code> disables grouping   entirely (one flat leader layer).</li> </ul> <p><code>manager_per_node = false</code>.  A virtual tree derived purely from rank order, with fan-out <code>max_workers_per_manager</code> (default <code>max(2, sqrt(N))</code>). Useful when shared-memory splitting is unavailable or misleading (e.g. under simulators).</p>"},{"location":"implementations/#protocol","title":"Protocol","text":"<p>Leaf workers use the unbatched <code>REQUEST</code> / <code>TASK</code> / <code>RESULT</code> exchange. Managers use the batched one:</p> <pre><code>manager:\n  top_up_pipeline(1)                       # one REQUEST_BATCH(n) to parent\n  loop until DONE:\n    release any prefetched batch into the work queue\n    wait for TASK_BATCH if the queue is empty (flushing results while waiting)\n    mark round active                      # later replies are quarantined\n    top_up_pipeline(pipeline_depth - 1)    # ask for the next batches now\n    while queue non-empty:\n      pop a free child, send it TASK_BATCH (or TASK for a leaf)\n      otherwise block until a child reports in\n    end round; send whatever results have accumulated up as RESULT_BATCH\n  propagate DONE to children, drain in-flight results, flush\n</code></pre> <p>Two details matter for throughput:</p> <ul> <li>Pipelining. A manager keeps <code>pipeline_depth</code> batch requests in flight   (default 2 = double buffering) so children never wait a full parent round trip   between batches.  Replies that arrive mid-round are quarantined until the round   boundary, so a round can never overshoot into the next round's tasks.  Deeper   pipelines hide more latency but commit tasks further ahead, which coarsens load   balancing \u2014 tasks already handed to one manager cannot be reassigned to an   idle sibling.</li> <li>Partial result flushing. Results are forwarded upward at every round   boundary, whether or not that round is complete.  Waiting for a round's slowest   straggler would otherwise stall results from children that finished long ago.</li> </ul> <p>Batch size is <code>subtree_leaf_count \u00d7 batch_size_multiplier</code> \u2014 the number of leaf workers a manager is responsible for feeding, including its own node's, not the number of children it sends to.  The two differ at every level above the node managers, since a leader's leader-layer children are themselves managers fronting whole nodes: at 2048 nodes with 7 ranks per node a leader has 69 direct children but 384 leaf workers beneath it, and the gap grows by a factor of the fanout per level.  <code>HierarchicalLockFreeRMAWorkDistributor</code> sizes its claims the same way (<code>setup_upper_chain()</code>'s <code>feed_width</code>).</p>"},{"location":"implementations/#constraints","title":"Constraints","text":"<p>Variable-length payloads are supported</p> <p><code>TaskT</code> and <code>ResultT</code> may be <code>std::vector&lt;T&gt;</code>, <code>std::string</code>, or any other type with <code>MPI_Type&lt;T&gt;::resize_required == true</code>.  Single tasks and results are sent with a probed count; batches are packed into a length-prefixed flat buffer (<code>detail::pack_variable_batch</code>), so no separate code path is needed and there is no per-element cap of the kind the RMA distributors impose.</p> <p>Prioritization is not supported here</p> <p>The <code>enable_prioritization</code> option compiles and <code>insert_task(task, priority)</code> exists, but any manager that receives a <code>TASK_BATCH</code> hits <code>DYNAMPI_UNIMPLEMENTED</code> (undefined behaviour under <code>NDEBUG</code>).  It happens to work only in degenerate topologies where no manager has children.</p>"},{"location":"implementations/#configuration_1","title":"Configuration","text":"Field Type Default Description <code>max_workers_per_manager</code> <code>optional&lt;int&gt;</code> <code>max(2, sqrt(N))</code> Fan-out of the virtual tree. Only used when <code>manager_per_node == false</code>. <code>batch_size_multiplier</code> <code>int</code> <code>1</code> Batch request size = subtree leaf workers \u00d7 this. <code>pipeline_depth</code> <code>int</code> <code>2</code> Batch requests kept outstanding, including the active round. <code>1</code> disables prefetching. <code>manager_per_node</code> <code>bool</code> <code>true</code> Use shared-memory node discovery instead of a rank-order virtual tree. <code>max_local_group_size</code> <code>int</code> <code>0</code> <code>&gt; 0</code> splits nodes into local groups of at most this size. <code>manager_per_node</code> only. <code>max_upper_fanout</code> <code>int</code> <code>-1</code> Max direct leader-layer children. <code>-1</code> auto, <code>0</code> disabled, <code>&gt; 0</code> explicit. <code>manager_per_node</code> only. <p>Statistics: <code>track_statistics&lt;Aggregated&gt;</code> and <code>&lt;Detailed&gt;</code> are both supported.  <code>Statistics::worker_task_counts</code> is populated by an <code>MPI_Gather</code> inside <code>finalize()</code>, so it is <code>std::nullopt</code> until then.</p>"},{"location":"implementations/#lockfreermaworkdistributor","title":"LockFreeRMAWorkDistributor","text":"<p>Best for: fine-grained tasks where two-sided hand-off latency dominates.  No collective calls anywhere in the hot path.</p> <p>The manager exposes one RMA window containing a control block, a task table, a result table and a completion log.  Everything is passive-target (<code>MPI_Win_lock_all(MPI_MODE_NOCHECK)</code> once at construction) \u2014 workers never wait on the manager to make progress.</p> <pre><code>manager publish (insert_tasks):\n  Put[task slots]  \u2192  atomic REPLACE(total)  \u2192  flush          # 2 round trips per batch\n\nworker (per claim, claim width 1):\n  fetch_and_add(head, 1) + flush_local        # claim an index\n  Get(task slot)         + flush_local        # read the payload\n  compute\n  Put(result slot)       + flush              # data durable at the manager\n  Put(log[start] = count)+ flush              # publishes the result, after data\n\nmanager harvest (run_tasks):\n  atomic read(head)                           # how far claiming got\n  Get(log[frontier .. head))                  # one bulk read\n  scan the contiguous prefix; Get(result[frontier .. confirmed))\n</code></pre> <p>The completion log is what makes the result path correct without atomics on the data: <code>fetch_and_add</code> already gives a worker exclusive ownership of its start index, so the log entry is a plain <code>Put</code>, and the intervening flush is what portably orders \"data written\" before \"flag published\".  The manager only advances past a contiguous prefix, so a straggling batch stops that harvest and is picked up on a later pass.</p> <p>Harvesting reads the manager's own window through the RMA API rather than plain loads: under <code>MPI_WIN_SEPARATE</code> (always the case on MS-MPI) local loads are not guaranteed to observe remote writes at all.</p> <ul> <li>Ordering: guaranteed, via the contiguous-prefix harvest above -- same   cost as <code>NaiveWorkDistributor</code>'s ordering, a straggler holds back every   result behind it.</li> <li>Prioritization: not supported.  <code>insert_task(task, priority)</code> exists for   interface compatibility and ignores the priority.</li> <li>Variable-length payloads: supported, within the per-slot capacity below.</li> <li>Statistics: <code>track_statistics&lt;Detailed&gt;</code> supported; <code>Statistics</code> carries   <code>comm_statistics</code> only (no per-worker task counts).</li> <li>Extra API: <code>gather_once()</code> performs exactly one harvest pass and returns   whatever is ready, instead of looping until everything is collected.</li> </ul>"},{"location":"implementations/#capacity","title":"Capacity","text":"<p>The window is preallocated as a ring. Capacities are hard limits on simultaneously published, unharvested work, but harvested slots are reused and there is no lifetime task cap.</p> Field Default Meaning <code>max_tasks</code> <code>8192</code> Reusable task/result/log ring slots. A single insertion batch larger than the ring throws <code>std::length_error</code>; when live work fills it, publication harvests and waits for space. <code>max_task_count</code> <code>256</code> Max element count of a single task, for variable-length <code>TaskT</code>. Ignored for fixed-size types. <code>max_result_count</code> <code>256</code> Same, for <code>ResultT</code>. <p>Exceeding <code>max_task_count</code> / <code>max_result_count</code> trips an assertion in debug builds and corrupts the window in release builds \u2014 size them for your worst case.</p> <p>Manager window bytes are</p> <pre><code>24 + max_tasks \u00d7 (task_stride + result_stride + 8)\n\ntask_stride   = round_up_8(8 + max_task_count   \u00d7 sizeof(task element))\nresult_stride = round_up_8(8 + max_result_count \u00d7 sizeof(result element))\n</code></pre> <p>(For fixed-size types the count factor is 1.)  With <code>int</code> tasks and <code>double</code> results at the defaults that is ~320 KiB; with <code>std::vector&lt;int&gt;</code> payloads at <code>max_task_count = max_result_count = 256</code> it is ~16 MiB.</p>"},{"location":"implementations/#batching-guidance","title":"Batching guidance","text":"<p><code>insert_task()</code> publishes immediately \u2014 two RMA round trips for one task. <code>insert_tasks(vector)</code> publishes the whole span in two round trips regardless of size.  Always prefer the batch form; per-task publishing is the measured bottleneck otherwise.</p>"},{"location":"implementations/#hierarchicallockfreermaworkdistributor","title":"HierarchicalLockFreeRMAWorkDistributor","text":"<p>Best for: large node counts where a single manager-owned window becomes the ceiling.</p> <p>Takes the lock-free RMA protocol above and instantiates it once per level of the node-aware tree: one window at the leader level (root manager \u2194 node managers), plus an independent window per node (manager \u2194 its local workers).  Each level is an independent <code>detail::LockFreeRMALevel</code>, so claiming, publishing and harvesting stay purely one-sided \u2014 composing hierarchically reintroduces no collectives.</p> <p>A node manager plays two roles simultaneously: owner of its local level and claimant of the level above.  It does not compute tasks itself.  Instead each <code>BridgeHop</code> runs, non-blocking, per iteration:</p> <ol> <li>Claim a range from the parent level (subject to backpressure) and republish it    into the child level, queueing a <code>{parent_start, child_len}</code> relay entry.</li> <li>Harvest whatever the child level has confirmed and append it to a relay buffer.</li> <li>Write the available prefix of the front relay entry back to the parent, in as    many partial <code>write_result_range</code> calls as it takes.</li> </ol> <p>Two properties are load-bearing:</p> <ul> <li>Backpressure. Outstanding relayed tasks are capped at   <code>8 \u00d7 parent claim width</code>.  Because the parent's harvest is a contiguous-prefix   scan, relays flush strictly FIFO \u2014 claiming far ahead adds ordering latency,   not just buffering, since a caught-up entry near the back of the queue cannot   flush until everything ahead of it does.</li> <li>Partial flushing. A relay entry is written back incrementally rather than   all-or-nothing.  Since claim width scales with the local worker count, an   all-or-nothing flush would relay nothing until every task of a wide claim   finished \u2014 an artificial barrier whose cost grows with task duration.</li> </ul> <p>Ranks alone on a node skip the local level and claim/compute directly against the level above (<code>run_leaf_leader_worker</code>).  Under <code>max_upper_fanout</code> grouping, a promoted group leader both owns its group's window and claims from it via self-targeted RMA.</p> <p>The constructor ends with an <code>MPI_Barrier</code>: level setup runs a chain of <code>MPI_Comm_split</code> calls whose cost grows with rank count, and without the barrier a caller that starts timing when the manager's constructor returns would measure other ranks still finishing setup.</p> <ul> <li>Ordering: guaranteed, recursively at every level of the relay chain --   see Result ordering.</li> <li>Prioritization: not supported (no priority overload at all).</li> <li>Statistics: not supported.  The <code>Options...</code> pack is accepted and   ignored, and there is no <code>get_statistics()</code>.</li> <li>Extra API: <code>gather_once()</code>, as above.</li> </ul>"},{"location":"implementations/#configuration_2","title":"Configuration","text":"Field Default Description <code>max_tasks</code> <code>8192</code> Reusable ring capacity of each upper-level window. <code>max_local_tasks</code> <code>8192</code> Reusable ring capacity of each node-local window. <code>max_task_count</code> / <code>max_result_count</code> <code>256</code> Per-slot element caps, as for the flat variant. <code>max_local_group_size</code> <code>0</code> <code>&gt; 0</code> splits nodes into smaller local groups. <code>max_upper_fanout</code> <code>-1</code> <code>-1</code> auto (flat at \u2264 32 managers, else smallest power of two \u2265 <code>sqrt(manager_count)</code>), <code>0</code> disabled, <code>&gt; 0</code> explicit. <p>Memory is the flat formula applied per window, so a manager pays for its local window and its share of the upper ones.</p>"},{"location":"implementations/#feature-matrix","title":"Feature matrix","text":"Naive Hierarchical LockFreeRMA Hier. LockFreeRMA Communication Two-sided Two-sided, batched Passive RMA Passive RMA, per level Collectives on the hot path No No No No Ordered results Yes No Yes Yes Arbitrary <code>TaskT</code> / <code>ResultT</code> Yes Yes Yes Yes Variable-length payloads Yes Yes Yes (capped) Yes (capped) Custom structs Yes Yes Yes Yes Prioritization Yes No No (ignored) No Statistics <code>Aggregated</code>, <code>Detailed</code> <code>Aggregated</code>, <code>Detailed</code> <code>Aggregated</code>, <code>Detailed</code> None Node-aware topology No Yes No Yes Incremental insertion Yes Yes Yes Yes Preallocated capacity limit No No <code>max_tasks</code> <code>max_tasks</code>, <code>max_local_tasks</code> Manager load per task 2 messages O(1/batch), per manager O(1/batch), per manager ~3 RMA ops"},{"location":"implementations/#benchmarking","title":"Benchmarking","text":"<p>Measured throughput depends heavily on the machine, the MPI stack and the task duration, so pick a distributor by measuring rather than from a table.  The repository ships drivers for exactly that (build with <code>-DDYNAMPI_BUILD_BENCHMARKS=ON</code>):</p> Benchmark Measures <code>weak_scaling_distribution_rate</code> Task hand-off rate at fixed rank count, across all four full distributors <code>asymptotic_distribution_throughput</code> Throughput vs. task count <code>shutdown_time</code> Cost of finalisation at scale <code>rma_atomic_microbench</code> Raw <code>MPI_Fetch_and_op</code> ceiling for the RMA path <code>pingpong</code>, <code>timer_resolution</code> Baseline latency and clock granularity <pre><code>mpirun -n 64 ./build/benchmark/weak_scaling_distribution_rate -D lockfree_rma -t 100 -d 10\n</code></pre> <p><code>-D</code> selects <code>naive</code>, <code>hierarchical</code>, <code>lockfree_rma</code> or <code>hierarchical_lockfree_rma</code>; <code>-t</code> is the expected task duration in microseconds and <code>-d</code> the measurement window in seconds.  For RMA runs, <code>--max_tasks</code> is both the reusable ring capacity and a benchmark-only publication budget (default 500M, ~19 GiB for this scalar workload).  The library itself has no lifetime task cap.  Lower it to save memory; the driver warns if the budget ends the window early.  Site scripts expose this as <code>MAX_TASKS</code>; plotting helpers live under <code>benchmark/scripts/</code>.</p> <p>For orientation, the design work behind these classes found the flat lock-free RMA distributor plateauing once a single manager window saturated, while the hierarchical topologies kept climbing with node count \u2014 which is the entire motivation for <code>HierarchicalLockFreeRMAWorkDistributor</code>.  Reproduce on your own target before committing to a choice.</p>"},{"location":"install/","title":"Install","text":""},{"location":"install/#installation","title":"Installation","text":"<p>DynaMPI is header-only.  Its only dependency is MPI \u2014 there is nothing to build or link beyond your MPI implementation.</p>"},{"location":"install/#requirements","title":"Requirements","text":"Dependency Minimum C++ compiler C++20: GCC 11+, Clang 14+, MSVC 2022+, Intel oneAPI (IntelLLVM) MPI MPICH 3.4+, Open MPI 4.0+, Intel MPI 2021+, MS-MPI 10+ CMake 3.14+ (only to consume the package or build tests/benchmarks) <p>CI covers Linux (GCC, Clang, Intel), macOS, Windows (MS-MPI), SimGrid/SMPI, and sanitizer builds.</p> <p>RMA distributors and MPI progress</p> <p>The lock-free distributors use passive-target RMA (<code>MPI_Win_lock_all</code> + <code>MPI_Fetch_and_op</code> / <code>MPI_Put</code> / <code>MPI_Get</code>).  Some MPI builds make remote RMA progress only when the two-sided engine is driven; DynaMPI handles this internally, but validate your target stack at scale before relying on them for production runs.</p>"},{"location":"install/#cmake","title":"CMake","text":"<pre><code># As a subdirectory\nadd_subdirectory(path/to/DynaMPI)\ntarget_link_libraries(my_target PRIVATE dynampi)\n</code></pre> <pre><code># Or via FetchContent\ninclude(FetchContent)\nFetchContent_Declare(\n    DynaMPI\n    GIT_REPOSITORY https://github.com/ryanstocks00/DynaMPI.git\n    GIT_TAG main            # pin a tag or commit for reproducible builds\n)\nFetchContent_MakeAvailable(DynaMPI)\ntarget_link_libraries(my_target PRIVATE dynampi)\n</code></pre> <p>The <code>dynampi</code> INTERFACE target adds the include directory, links <code>MPI::MPI_CXX</code>, requires C++20 (<code>cxx_std_20</code>), and defines <code>DYNAMPI_COMMIT_HASH</code> plus the <code>DYNAMPI_VERSION_*</code> macros.</p> <p>When DynaMPI is consumed as a subproject, <code>DYNAMPI_BUILD_TESTS</code>, <code>DYNAMPI_BUILD_BENCHMARKS</code> and <code>DYNAMPI_BUILD_EXAMPLES</code> all default to <code>OFF</code>, so it costs you no extra build time.</p>"},{"location":"install/#without-cmake","title":"Without CMake","text":"<p>Copy <code>include/</code> into your project and add it to the include path.  No macros to define, nothing to link beyond MPI:</p> <pre><code>mpic++ -std=c++20 -I/path/to/DynaMPI/include my_program.cpp\n</code></pre> <p>The only thing you lose is <code>dynampi::version::commit_hash()</code>, which reports <code>\"unknown\"</code> outside a CMake build since nothing else can determine it.</p>"},{"location":"install/#building-the-examples","title":"Building the examples","text":"<pre><code>cmake -B build -DDYNAMPI_BUILD_EXAMPLES=ON\ncmake --build build -j\nmpirun -n 4 ./build/examples/01_index_tasks\n</code></pre> <p>Seven short standalone programs covering the entry point, incremental task insertion, ordering and priorities, custom payload types, the RMA distributors, statistics, and the index parallel-for.  See examples/README.md for what each one demonstrates.  They all run correctly at any rank count, including <code>-n 1</code>.</p>"},{"location":"install/#building-the-tests","title":"Building the tests","text":"<pre><code>git clone https://github.com/ryanstocks00/DynaMPI.git\ncd DynaMPI\ncmake -B build -DDYNAMPI_BUILD_TESTS=ON\ncmake --build build -j\n</code></pre> <pre><code>./build/test/unit_test              # serial unit tests\nmpirun -n 4 ./build/test/mpi_test   # MPI distributor tests\nctest --test-dir build              # everything, as registered\n</code></pre> <p><code>ctest</code> registers the MPI suite at 1, 2, 3, 4, 8, 16 and 64 ranks, since several topology paths only appear at particular rank counts.  Cap that on a small machine with <code>-DDYNAMPI_MAX_MPI_RANK=8</code> (or the <code>DYNAMPI_MAX_MPI_RANK</code> environment variable).</p>"},{"location":"install/#building-the-benchmarks","title":"Building the benchmarks","text":"<pre><code>cmake -B build -DDYNAMPI_BUILD_BENCHMARKS=ON\ncmake --build build -j\n\nmpirun -n 64 ./build/benchmark/weak_scaling_distribution_rate \\\n  -D hierarchical -t 100 -d 10\n</code></pre> <p>Benchmarks fetch <code>cxxopts</code> at configure time for argument parsing.  See Benchmarking for what each driver measures. Site launch and submit scripts live under <code>benchmark/frontier/</code> and <code>benchmark/aurora/</code>; machine-independent helpers, including the plotting scripts, stay in <code>benchmark/scripts/</code> (<code>pip install -r benchmark/scripts/requirements.txt</code>).</p>"},{"location":"lockfree_rma_design/","title":"Lock-Free RMA Design","text":""},{"location":"lockfree_rma_design/#lock-free-rma-design-notes","title":"Lock-Free RMA Design Notes","text":"<p>Design background for the three RMA-based distributors: <code>LockFreeRMAWorkDistributor</code> and <code>HierarchicalLockFreeRMAWorkDistributor</code>.  For the user-facing summary and configuration, see Implementations.</p>"},{"location":"lockfree_rma_design/#why-one-sided-at-all","title":"Why one-sided at all","text":"<p>A two-sided distributor spends one request/response round trip per task, and every one of those messages lands on the manager.  Passive-target RMA replaces the handshake with an atomic on a shared counter: a worker claims work without the manager doing anything, so the manager's involvement drops from \"two messages per task\" to \"occasionally read the window\".</p> <p>The cost is that everything the protocol needs \u2014 task payloads, results, completion status \u2014 has to live in a preallocated window with a fixed layout, and memory visibility has to be established explicitly rather than falling out of message matching.</p> <p>All three distributors open their windows with <code>MPI_Win_lock_all(MPI_MODE_NOCHECK)</code> once at construction and hold that epoch for the distributor's lifetime.  There are no fences, no per-task lock/unlock, and no collectives on the hot path.</p>"},{"location":"lockfree_rma_design/#minimal-index-parallel-for","title":"Minimal index parallel-for","text":"<pre><code>Manager window:\n  offset 0:  head / claim counter (int64)   \u2190 MPI_Fetch_and_op(MPI_SUM) by every rank\n</code></pre> <pre><code>lock_all once\nbroadcast n\n\nwhile true:\n    idx \u2190 fetch_and_op(+1, head); flush\n    if idx &gt;= n: break\n    local.append(idx, worker_function(idx))\n\ngather_sorted(local) on the manager        # one MPI_Gatherv, at the end\n</code></pre> <p>Results are packed as <code>[int64 index][int64 count][data]</code> records, gathered with a single <code>MPI_Gatherv</code>, then sorted by index.  That terminal gather is the design's one collective and its scaling limit.</p>"},{"location":"lockfree_rma_design/#lock-free-rma-window-protocol","title":"Lock-free RMA window protocol","text":"<p><code>LockFreeRMAWorkDistributor</code> generalises this to arbitrary payloads and removes the gather entirely.</p>"},{"location":"lockfree_rma_design/#layout","title":"Layout","text":"<pre><code>[ head ][ total ][ finished ]   control block, 24 bytes, int64 each\n[ task slot 0 ][ task slot 1 ] ... \u00d7 max_tasks\n[ result slot 0 ][ result slot 1 ] ... \u00d7 max_tasks\n[ log 0 ][ log 1 ] ...             int64 each, \u00d7 max_tasks\n</code></pre> <p>Task and result slots are <code>[int64 count][data]</code>, padded to an 8-byte stride so every slot is independently addressable.  Exact offsets are in <code>include/dynampi/impl/lockfree_rma_distributor.hpp</code>; the hierarchical variant reuses the identical layout per level via <code>detail::LockFreeRMALevel</code>.</p> Field Written by Read by Purpose <code>head</code> claimants, <code>fetch_and_op(SUM)</code> manager How far claiming has progressed <code>total</code> manager, <code>fetch_and_op(REPLACE)</code> claimants How many tasks are published <code>finished</code> manager, <code>fetch_and_op(REPLACE)</code> claimants No further tasks will be published task table manager, bulk <code>Put</code> claimants, <code>Get</code> Task payloads result table claimants, bulk <code>Put</code> manager, <code>Get</code> Result payloads completion log claimants, plain <code>Put</code> manager, <code>Get</code> Which claimed ranges are fully written"},{"location":"lockfree_rma_design/#ordering-rules","title":"Ordering rules","text":"<p>Passive-target RMA is non-blocking, so every step names the completion it needs:</p> <ul> <li><code>MPI_Win_flush_local</code> completes an operation at the origin \u2014 enough before   reading the value returned by a <code>fetch_and_op</code> or a <code>Get</code>.</li> <li><code>MPI_Win_flush</code> completes it at the target \u2014 required before another rank   may observe a <code>Put</code> or an atomic.</li> </ul> <p>Two orderings are load-bearing:</p> <ol> <li>Publish: <code>Put(task slots)</code> \u2192 <code>atomic REPLACE(total)</code> \u2192 <code>flush</code>.  <code>total</code> is    the only gate on claiming, so bumping it last means a claimant can never see    a task index whose payload is still in flight.</li> <li>Complete: <code>Put(result data)</code> \u2192 <code>flush</code> \u2192 <code>Put(log[start] = count)</code> \u2192    <code>flush</code>.  The intervening flush is what portably orders data before flag.</li> </ol>"},{"location":"lockfree_rma_design/#why-a-completion-log","title":"Why a completion log","text":"<p>The log turns \"is this result ready?\" into a single contiguous-prefix scan instead of a per-slot poll, and it needs no atomics: <code>fetch_and_op</code> on <code>head</code> already gives a claimant exclusive ownership of its start index, so the log entry at that index has exactly one writer.  A zero entry is the untouched sentinel \u2014 a real entry is always <code>count &gt;= 1</code>.</p> <p>The manager harvests in three round trips regardless of how many batches turn out to be ready:</p> <pre><code>atomic read(head)                       # how far claiming got\nGet(log[frontier .. head))              # one bulk read\nscan forward while log[i] != 0\nGet(result[frontier .. confirmed_end))  # one bulk read\n</code></pre> <p>A gap \u2014 a batch still in flight \u2014 simply stops the scan for that call. Already-complete entries beyond the gap are picked up on a later pass, at the cost of re-scanning that stretch of log, which is cheap because the scan is one bulk <code>Get</code> either way.</p>"},{"location":"lockfree_rma_design/#claiming-under-a-moving-total","title":"Claiming under a moving <code>total</code>","text":"<p>Claims are unconditional <code>fetch_and_add</code>s, so aggregate claim rate can outrun publication and a claimed index can land beyond <code>total</code>.  Claimants therefore carry a <code>pending_start</code>/<code>pending_end</code> remainder, resolved on later iterations as <code>total</code> catches up, and clamp the ready boundary into <code>[start, end]</code> \u2014 clamping to <code>min(end, total)</code> alone can produce a boundary below the claim's own start, which would make a rank write results into a range another claimant owns.</p> <p>Symmetrically, observing <code>finished</code> means \"<code>total</code> will not grow\", not \"stop claiming\": a rank that has seen <code>finished</code> must still be able to claim a genuine unclaimed gap between its cached head and the final <code>total</code>.</p>"},{"location":"lockfree_rma_design/#self-targeted-rma","title":"Self-targeted RMA","text":"<p>The manager reads its own window through the RMA API rather than plain loads. Under <code>MPI_WIN_SEPARATE</code> \u2014 the model MS-MPI always uses \u2014 a window owner is not guaranteed to observe another rank's completed RMA writes through local loads at all.  Going through <code>Get</code>/<code>Fetch_and_op</code> is correct under both memory models.</p> <p>The same reasoning lets a rank be both owner and claimant of a level in the hierarchical variant: a self-targeted RMA operation behaves exactly like a remote one, just over a loopback.</p>"},{"location":"lockfree_rma_design/#progress","title":"Progress","text":"<p>Where a rank must spin on remote state, it calls <code>MPI_Iprobe</code> between polls rather than re-flushing the window.  Every RMA primitive here already flushes its target before returning, so an extra <code>MPI_Win_flush_all</code> would only add contention; <code>MPI_Iprobe</code> drives the two-sided progress engine, which some stacks (MS-MPI in particular) require before self-targeted <code>Get</code>s ever observe a change. Idle ranks additionally back off for tens of microseconds \u2014 staggered by rank on Windows \u2014 so oversubscribed ranks do not flood the window as a thundering herd.</p> <p>Validate RMA progress on your MPI</p> <p>Early <code>MPI_Win_sync</code> + <code>MPI_Fetch_and_op</code> experiments under <code>MPI_Win_lock_all</code> did not make remote atomic updates visible to local loads on some MPICH 4.0 (ch4:ofi) configurations without asynchronous progress. The shipped distributors avoid that pattern and are exercised in CI, but behaviour at scale is stack-dependent \u2014 measure before relying on it.</p>"},{"location":"lockfree_rma_design/#composing-levels","title":"Composing levels","text":"<p><code>HierarchicalLockFreeRMAWorkDistributor</code> instantiates the protocol above once per tree level (<code>detail::LockFreeRMALevel</code>), which is possible precisely because nothing in it is collective: publishing, claiming, writing and harvesting are independent one-sided operations with no requirement that all claimants participate together.</p> <p>The claim and write steps are exposed as separate non-blocking calls (<code>try_claim()</code> / <code>write_result_range()</code>) rather than a single claim-compute-write loop, because a manager does not compute what it claims \u2014 it republishes into the level below and writes results back only once they return.  A leaf worker simply calls both back to back, reassembling the flat class's loop from the same two primitives.</p> <p>Size-1 levels skip <code>MPI_Win_create</code> entirely (some MPI implementations reject window creation on singleton communicators) and fall back to plain loads and stores on the owner's buffer, which is correct because owner and claimant are then the same process.</p>"},{"location":"dynampi/annotated/","title":"Class List","text":"<p>Here are the classes, structs, unions and interfaces with brief descriptions:</p> <ul> <li>namespace dynampi <ul> <li>class BaseWorkDistributor <ul> <li>struct Config </li> </ul> </li> <li>struct CommStatistics </li> <li>class HierarchicalLockFreeRMAWorkDistributor <ul> <li>struct Config </li> <li>struct RunConfig </li> </ul> </li> <li>class HierarchicalWorkDistributor <ul> <li>struct Config </li> <li>struct RunConfig </li> </ul> </li> <li>class LockFreeRMAWorkDistributor <ul> <li>struct Config </li> <li>struct RunConfig </li> <li>struct Statistics </li> </ul> </li> <li>class MPICommunicator </li> <li>class MPIGroup </li> <li>struct MPI_Type </li> <li>struct MPI_Type&lt; std::nullptr_t &gt; </li> <li>struct MPI_Type&lt; std::string &gt; </li> <li>struct MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt; </li> <li>class NaiveWorkDistributor <ul> <li>struct Config </li> <li>struct RunConfig </li> <li>struct Statistics </li> </ul> </li> <li>struct TaskError </li> <li>class TaskFailure </li> <li>class Timer </li> <li>namespace detail <ul> <li>class LockFreeRMALevel <ul> <li>struct ClaimedRange </li> <li>struct Config </li> </ul> </li> </ul> </li> <li>struct enable_prioritization </li> <li>struct has_dynampi_mpi_type </li> <li>struct has_dynampi_mpi_type&lt; U, std::void_t&lt; decltype(MPI_Type&lt; U &gt;::value)&gt; &gt; </li> <li>struct prioritize_tasks_t </li> <li>struct track_statistics </li> <li>struct track_statistics_t </li> <li>namespace version </li> </ul> </li> <li>struct BridgeHop </li> <li>struct PendingRelay </li> <li>struct Statistics </li> <li>struct TaskRequest </li> <li>class TaskErrorLog </li> <li>struct option_value </li> <li>struct option_value&lt; Option, Head, Tail... &gt; </li> <li>namespace std </li> </ul>"},{"location":"dynampi/files/","title":"File List","text":"<p>Here is a list of all files with brief descriptions:</p> <ul> <li>dir include <ul> <li>dir dynampi <ul> <li>dir impl <ul> <li>file base_distributor.hpp </li> <li>file hierarchical_distributor.hpp </li> <li>file hierarchical_lockfree_rma_distributor.hpp </li> <li>file hierarchical_topology_detail.hpp </li> <li>file lockfree_rma_distributor.hpp </li> <li>file naive_distributor.hpp </li> <li>file rma_detail.hpp </li> <li>file variable_batch.hpp </li> </ul> </li> <li>dir mpi <ul> <li>file mpi_communicator.hpp </li> <li>file mpi_error.hpp </li> <li>file mpi_group.hpp </li> <li>file mpi_types.hpp </li> </ul> </li> <li>dir utilities <ul> <li>file assert.hpp </li> <li>file printing.hpp </li> <li>file template_options.hpp </li> <li>file timer.hpp </li> </ul> </li> <li>file dynampi.hpp </li> <li>file task_error.hpp </li> <li>file version.hpp </li> </ul> </li> </ul> </li> </ul>"},{"location":"dynampi/namespacedynampi/","title":"Namespace dynampi","text":"<p>Namespace List &gt; dynampi</p>"},{"location":"dynampi/namespacedynampi/#namespaces","title":"Namespaces","text":"Type Name namespace detail namespace version"},{"location":"dynampi/namespacedynampi/#classes","title":"Classes","text":"Type Name class BaseWorkDistributor &lt;typename TaskT, typename ResultT, Options&gt; struct CommStatistics class HierarchicalLockFreeRMAWorkDistributor &lt;typename TaskT, typename ResultT, Options&gt; class HierarchicalWorkDistributor &lt;typename TaskT, typename ResultT, Options&gt; class LockFreeRMAWorkDistributor &lt;typename TaskT, typename ResultT, Options&gt; class MPICommunicator &lt;Options&gt; class MPIGroup struct MPI_Type &lt;typename T, typename&gt; struct MPI_Type&lt; std::nullptr_t &gt; &lt;&gt; struct MPI_Type&lt; std::string &gt; &lt;&gt; struct MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt; &lt;typename T&gt; class NaiveWorkDistributor &lt;typename TaskT, typename ResultT, Options&gt; struct TaskError class TaskFailure class Timer struct enable_prioritization struct has_dynampi_mpi_type &lt;typename, typename&gt; struct has_dynampi_mpi_type&lt; U, std::void_t&lt; decltype(MPI_Type&lt; U &gt;::value)&gt; &gt; &lt;typename U&gt; struct prioritize_tasks_t struct track_statistics &lt;Mode&gt; struct track_statistics_t"},{"location":"dynampi/namespacedynampi/#public-types","title":"Public Types","text":"Type Name typedef HierarchicalWorkDistributor&lt; TaskT, ResultT, Options... &gt; DynamicWorkDistributor enum StatisticsMode"},{"location":"dynampi/namespacedynampi/#public-attributes","title":"Public Attributes","text":"Type Name constexpr size_t kMaxTaskErrorMessage   = <code>240</code>"},{"location":"dynampi/namespacedynampi/#public-functions","title":"Public Functions","text":"Type Name DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (char, MPI_CHAR)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (std::byte, MPI_BYTE)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (signed char, MPI_SIGNED_CHAR)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (unsigned char, MPI_UNSIGNED_CHAR)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (short, MPI_SHORT)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (unsigned short, MPI_UNSIGNED_SHORT)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (int, MPI_INT)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (unsigned int, MPI_UNSIGNED)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (long, MPI_LONG)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (unsigned long, MPI_UNSIGNED_LONG)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (long long, MPI_LONG_LONG_INT)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (unsigned long long, MPI_UNSIGNED_LONG_LONG)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (float, MPI_FLOAT)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (double, MPI_DOUBLE)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (long double, MPI_LONG_DOUBLE)  DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (bool, MPI_C_BOOL)  std::optional&lt; std::string &gt; OptionalString (Args &amp;&amp;... args)  void _DYNAMPI_FAILBinOp (const A &amp; a, const B &amp; b, const std::string &amp; a_str, const std::string &amp; b_str, const std::string &amp; nop, const std::optional&lt; std::string &gt; &amp; message)  void _DYNAMPI_FAIL_ASSERT (const std::string &amp; condition_str, const std::optional&lt; std::string &gt; &amp; message)  void check_fixed_size_mpi_type (const char * type_role, const char * distributor_name)  int mpi_elements_per_value ()  void mpi_fail (int err, std::string_view command)  std::optional&lt; std::vector&lt; ResultT &gt; &gt; mpi_manager_worker_distribution (size_t n_tasks, std::function&lt; ResultT(size_t)&gt; worker_function, MPI_Comm comm=MPI_COMM_WORLD, int manager_rank=0)  std::ostream &amp; operator&lt;&lt; (std::ostream &amp; os, const std::set&lt; T &gt; &amp; set)  std::ostream &amp; operator&lt;&lt; (std::ostream &amp; os, const std::vector&lt; T &gt; &amp; vec)  std::ostream &amp; operator&lt;&lt; (std::ostream &amp; os, const std::array&lt; T, N &gt; &amp; arr)  std::ostream &amp; operator&lt;&lt; (std::ostream &amp; os, const std::span&lt; T &gt; &amp; vec)  std::ostream &amp; operator&lt;&lt; (std::ostream &amp; os, const std::optional&lt; T &gt; &amp; op)  std::ostream &amp; operator&lt;&lt; (std::ostream &amp; os, const std::tuple&lt; Args... &gt; &amp; tup)  std::ostream &amp; operator&lt;&lt; (std::ostream &amp; os, const std::pair&lt; T, U &gt; &amp; pair)  std::ostream &amp; operator&lt;&lt; (std::ostream &amp; os, const std::byte &amp; b)"},{"location":"dynampi/namespacedynampi/#public-types-documentation","title":"Public Types Documentation","text":""},{"location":"dynampi/namespacedynampi/#typedef-dynamicworkdistributor","title":"typedef DynamicWorkDistributor","text":"<pre><code>using dynampi::DynamicWorkDistributor = typedef HierarchicalWorkDistributor&lt;TaskT, ResultT, Options...&gt;;\n</code></pre>"},{"location":"dynampi/namespacedynampi/#enum-statisticsmode","title":"enum StatisticsMode","text":"<pre><code>enum dynampi::StatisticsMode {\n    None,\n    Aggregated,\n    Detailed\n};\n</code></pre>"},{"location":"dynampi/namespacedynampi/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/namespacedynampi/#variable-kmaxtaskerrormessage","title":"variable kMaxTaskErrorMessage","text":"<pre><code>constexpr size_t dynampi::kMaxTaskErrorMessage;\n</code></pre>"},{"location":"dynampi/namespacedynampi/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    char,\n    MPI_CHAR\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_1","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    std::byte,\n    MPI_BYTE\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_2","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    signed  char,\n    MPI_SIGNED_CHAR\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_3","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    unsigned  char,\n    MPI_UNSIGNED_CHAR\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_4","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    short,\n    MPI_SHORT\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_5","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    unsigned  short,\n    MPI_UNSIGNED_SHORT\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_6","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    int,\n    MPI_INT\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_7","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    unsigned  int,\n    MPI_UNSIGNED\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_8","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    long,\n    MPI_LONG\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_9","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    unsigned  long,\n    MPI_UNSIGNED_LONG\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_10","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    long  long,\n    MPI_LONG_LONG_INT\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_11","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    unsigned  long  long,\n    MPI_UNSIGNED_LONG_LONG\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_12","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    float,\n    MPI_FLOAT\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_13","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    double,\n    MPI_DOUBLE\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_14","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    long  double,\n    MPI_LONG_DOUBLE\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-dynampi_define_primitive_mpi_type_15","title":"function DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>dynampi::DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    bool,\n    MPI_C_BOOL\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-optionalstring","title":"function OptionalString","text":"<pre><code>template&lt;typename... Args&gt;\nstd::optional&lt; std::string &gt; dynampi::OptionalString (\n    Args &amp;&amp;... args\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-_dynampi_failbinop","title":"function _DYNAMPI_FAILBinOp","text":"<pre><code>template&lt;typename A, typename B&gt;\ninline void dynampi::_DYNAMPI_FAILBinOp (\n    const  A &amp; a,\n    const  B &amp; b,\n    const std::string &amp; a_str,\n    const std::string &amp; b_str,\n    const std::string &amp; nop,\n    const std::optional&lt; std::string &gt; &amp; message\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-_dynampi_fail_assert","title":"function _DYNAMPI_FAIL_ASSERT","text":"<pre><code>inline void dynampi::_DYNAMPI_FAIL_ASSERT (\n    const std::string &amp; condition_str,\n    const std::optional&lt; std::string &gt; &amp; message\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-check_fixed_size_mpi_type","title":"function check_fixed_size_mpi_type","text":"<pre><code>template&lt;typename T&gt;\ninline void dynampi::check_fixed_size_mpi_type (\n    const char * type_role,\n    const char * distributor_name\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-mpi_elements_per_value","title":"function mpi_elements_per_value","text":"<pre><code>template&lt;typename T&gt;\ninline int dynampi::mpi_elements_per_value () \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-mpi_fail","title":"function mpi_fail","text":"<pre><code>inline void dynampi::mpi_fail (\n    int err,\n    std::string_view command\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-mpi_manager_worker_distribution","title":"function mpi_manager_worker_distribution","text":"<pre><code>template&lt;typename ResultT, template&lt; typename, typename, typename... &gt; typename T&gt;\nstd::optional&lt; std::vector&lt; ResultT &gt; &gt; dynampi::mpi_manager_worker_distribution (\n    size_t n_tasks,\n    std::function&lt; ResultT ( size_t )&gt; worker_function,\n    MPI_Comm comm=MPI_COMM_WORLD,\n    int manager_rank=0\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-operator","title":"function operator&lt;&lt;","text":"<pre><code>template&lt;typename T&gt;\ninline std::ostream &amp; dynampi::operator&lt;&lt; (\n    std::ostream &amp; os,\n    const std::set&lt; T &gt; &amp; set\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-operator_1","title":"function operator&lt;&lt;","text":"<pre><code>template&lt;typename T&gt;\ninline std::ostream &amp; dynampi::operator&lt;&lt; (\n    std::ostream &amp; os,\n    const std::vector&lt; T &gt; &amp; vec\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-operator_2","title":"function operator&lt;&lt;","text":"<pre><code>template&lt;typename T, std::size_t N&gt;\ninline std::ostream &amp; dynampi::operator&lt;&lt; (\n    std::ostream &amp; os,\n    const std::array&lt; T , N &gt; &amp; arr\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-operator_3","title":"function operator&lt;&lt;","text":"<pre><code>template&lt;typename T&gt;\ninline std::ostream &amp; dynampi::operator&lt;&lt; (\n    std::ostream &amp; os,\n    const std::span&lt; T &gt; &amp; vec\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-operator_4","title":"function operator&lt;&lt;","text":"<pre><code>template&lt;typename T&gt;\ninline std::ostream &amp; dynampi::operator&lt;&lt; (\n    std::ostream &amp; os,\n    const std::optional&lt; T &gt; &amp; op\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-operator_5","title":"function operator&lt;&lt;","text":"<pre><code>template&lt;typename... Args&gt;\ninline std::ostream &amp; dynampi::operator&lt;&lt; (\n    std::ostream &amp; os,\n    const std::tuple&lt; Args... &gt; &amp; tup\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-operator_6","title":"function operator&lt;&lt;","text":"<pre><code>template&lt;typename T, typename U&gt;\ninline std::ostream &amp; dynampi::operator&lt;&lt; (\n    std::ostream &amp; os,\n    const std::pair&lt; T , U &gt; &amp; pair\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi/#function-operator_7","title":"function operator&lt;&lt;","text":"<pre><code>inline std::ostream &amp; dynampi::operator&lt;&lt; (\n    std::ostream &amp; os,\n    const std::byte &amp; b\n) \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/dynampi.hpp</code></p>"},{"location":"dynampi/classdynampi_1_1BaseWorkDistributor/","title":"Class dynampi::BaseWorkDistributor","text":"<p>template &lt;typename TaskT, typename ResultT, typename... Options&gt;</p> <p>ClassList &gt; dynampi &gt; BaseWorkDistributor</p> <ul> <li><code>#include &lt;base_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/classdynampi_1_1BaseWorkDistributor/#classes","title":"Classes","text":"Type Name struct Config"},{"location":"dynampi/classdynampi_1_1BaseWorkDistributor/#protected-types","title":"Protected Types","text":"Type Name typedef std::conditional_t&lt; prioritize_tasks, std::priority_queue&lt; std::pair&lt; double, TaskT &gt; &gt;, std::deque&lt; TaskT &gt; &gt; QueueT"},{"location":"dynampi/classdynampi_1_1BaseWorkDistributor/#protected-static-attributes","title":"Protected Static Attributes","text":"Type Name constexpr bool prioritize_tasks   = <code>[**get\\_option\\_value**](template__options_8hpp.md#function-get_option_value)&amp;lt;[**prioritize\\_tasks\\_t**](structdynampi_1_1prioritize__tasks__t.md), Options...&amp;gt;()</code>"},{"location":"dynampi/classdynampi_1_1BaseWorkDistributor/#protected-types-documentation","title":"Protected Types Documentation","text":""},{"location":"dynampi/classdynampi_1_1BaseWorkDistributor/#typedef-queuet","title":"typedef QueueT","text":"<pre><code>using dynampi::BaseWorkDistributor&lt; TaskT, ResultT, Options &gt;::QueueT =  std::conditional_t&lt;prioritize_tasks, std::priority_queue&lt;std::pair&lt;double, TaskT&gt; &gt;, std::deque&lt;TaskT&gt; &gt;;\n</code></pre>"},{"location":"dynampi/classdynampi_1_1BaseWorkDistributor/#protected-static-attributes-documentation","title":"Protected Static Attributes Documentation","text":""},{"location":"dynampi/classdynampi_1_1BaseWorkDistributor/#variable-prioritize_tasks","title":"variable prioritize_tasks","text":"<pre><code>constexpr bool dynampi::BaseWorkDistributor&lt; TaskT, ResultT, Options &gt;::prioritize_tasks;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/base_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1BaseWorkDistributor_1_1Config/","title":"Struct dynampi::BaseWorkDistributor::Config","text":"<p>ClassList &gt; dynampi &gt; BaseWorkDistributor &gt; Config</p> <ul> <li><code>#include &lt;base_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1BaseWorkDistributor_1_1Config/#public-attributes","title":"Public Attributes","text":"Type Name bool auto_run_workers   = <code>[**true**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code> MPI_Comm comm   = <code>[**MPI\\_COMM\\_WORLD**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code> int manager_rank   = <code>0</code>"},{"location":"dynampi/structdynampi_1_1BaseWorkDistributor_1_1Config/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1BaseWorkDistributor_1_1Config/#variable-auto_run_workers","title":"variable auto_run_workers","text":"<pre><code>bool dynampi::BaseWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::auto_run_workers;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1BaseWorkDistributor_1_1Config/#variable-comm","title":"variable comm","text":"<pre><code>MPI_Comm dynampi::BaseWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::comm;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1BaseWorkDistributor_1_1Config/#variable-manager_rank","title":"variable manager_rank","text":"<pre><code>int dynampi::BaseWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::manager_rank;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/base_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1CommStatistics/","title":"Struct dynampi::CommStatistics","text":"<p>ClassList &gt; dynampi &gt; CommStatistics</p> <ul> <li><code>#include &lt;mpi_communicator.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1CommStatistics/#public-attributes","title":"Public Attributes","text":"Type Name size_t atomic_bytes   = <code>0</code> int atomic_count   = <code>0</code> size_t bytes_received   = <code>0</code> size_t bytes_sent   = <code>0</code> int collective_count   = <code>0</code> int recv_count   = <code>0</code> double recv_time   = <code>0.0</code> int send_count   = <code>0</code> double send_time   = <code>0.0</code>"},{"location":"dynampi/structdynampi_1_1CommStatistics/#public-functions","title":"Public Functions","text":"Type Name double average_receive_size () const double average_send_size () const void reset ()"},{"location":"dynampi/structdynampi_1_1CommStatistics/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1CommStatistics/#variable-atomic_bytes","title":"variable atomic_bytes","text":"<pre><code>size_t dynampi::CommStatistics::atomic_bytes;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1CommStatistics/#variable-atomic_count","title":"variable atomic_count","text":"<pre><code>int dynampi::CommStatistics::atomic_count;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1CommStatistics/#variable-bytes_received","title":"variable bytes_received","text":"<pre><code>size_t dynampi::CommStatistics::bytes_received;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1CommStatistics/#variable-bytes_sent","title":"variable bytes_sent","text":"<pre><code>size_t dynampi::CommStatistics::bytes_sent;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1CommStatistics/#variable-collective_count","title":"variable collective_count","text":"<pre><code>int dynampi::CommStatistics::collective_count;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1CommStatistics/#variable-recv_count","title":"variable recv_count","text":"<pre><code>int dynampi::CommStatistics::recv_count;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1CommStatistics/#variable-recv_time","title":"variable recv_time","text":"<pre><code>double dynampi::CommStatistics::recv_time;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1CommStatistics/#variable-send_count","title":"variable send_count","text":"<pre><code>int dynampi::CommStatistics::send_count;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1CommStatistics/#variable-send_time","title":"variable send_time","text":"<pre><code>double dynampi::CommStatistics::send_time;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1CommStatistics/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/structdynampi_1_1CommStatistics/#function-average_receive_size","title":"function average_receive_size","text":"<pre><code>inline double dynampi::CommStatistics::average_receive_size () const\n</code></pre>"},{"location":"dynampi/structdynampi_1_1CommStatistics/#function-average_send_size","title":"function average_send_size","text":"<pre><code>inline double dynampi::CommStatistics::average_send_size () const\n</code></pre>"},{"location":"dynampi/structdynampi_1_1CommStatistics/#function-reset","title":"function reset","text":"<pre><code>inline void dynampi::CommStatistics::reset () \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_communicator.hpp</code></p>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeRMAWorkDistributor/","title":"Class dynampi::HierarchicalLockFreeRMAWorkDistributor","text":"<p>template &lt;typename TaskT, typename ResultT, typename... Options&gt;</p> <p>ClassList &gt; dynampi &gt; HierarchicalLockFreeRMAWorkDistributor</p> <ul> <li><code>#include &lt;hierarchical_lockfree_rma_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeRMAWorkDistributor/#classes","title":"Classes","text":"Type Name struct Config struct RunConfig"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeRMAWorkDistributor/#public-static-attributes","title":"Public Static Attributes","text":"Type Name const bool ordered   = <code>[**true**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeRMAWorkDistributor/#public-functions","title":"Public Functions","text":"Type Name HierarchicalLockFreeRMAWorkDistributor (std::function&lt; ResultT(TaskT)&gt; worker_function, Config config={})  void finalize ()  std::vector&lt; ResultT &gt; finish_remaining_tasks ()  std::vector&lt; ResultT &gt; gather_once ()  bool has_task_errors () const void insert_task (TaskT task)  void insert_tasks (const std::vector&lt; TaskT &gt; &amp; tasks)  bool is_root_manager () const size_t remaining_tasks_count () const std::vector&lt; ResultT &gt; run_tasks (const RunConfig &amp; config=RunConfig{})  void run_worker ()  std::vector&lt; TaskError &gt; take_task_errors ()  ~HierarchicalLockFreeRMAWorkDistributor ()"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeRMAWorkDistributor/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeRMAWorkDistributor/#variable-ordered","title":"variable ordered","text":"<pre><code>const bool dynampi::HierarchicalLockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::ordered;\n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeRMAWorkDistributor/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeRMAWorkDistributor/#function-hierarchicallockfreermaworkdistributor","title":"function HierarchicalLockFreeRMAWorkDistributor","text":"<pre><code>inline explicit dynampi::HierarchicalLockFreeRMAWorkDistributor::HierarchicalLockFreeRMAWorkDistributor (\n    std::function&lt; ResultT ( TaskT )&gt; worker_function,\n    Config config={}\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeRMAWorkDistributor/#function-finalize","title":"function finalize","text":"<pre><code>inline void dynampi::HierarchicalLockFreeRMAWorkDistributor::finalize () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeRMAWorkDistributor/#function-finish_remaining_tasks","title":"function finish_remaining_tasks","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::HierarchicalLockFreeRMAWorkDistributor::finish_remaining_tasks () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeRMAWorkDistributor/#function-gather_once","title":"function gather_once","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::HierarchicalLockFreeRMAWorkDistributor::gather_once () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeRMAWorkDistributor/#function-has_task_errors","title":"function has_task_errors","text":"<pre><code>inline bool dynampi::HierarchicalLockFreeRMAWorkDistributor::has_task_errors () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeRMAWorkDistributor/#function-insert_task","title":"function insert_task","text":"<pre><code>inline void dynampi::HierarchicalLockFreeRMAWorkDistributor::insert_task (\n    TaskT task\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeRMAWorkDistributor/#function-insert_tasks","title":"function insert_tasks","text":"<pre><code>inline void dynampi::HierarchicalLockFreeRMAWorkDistributor::insert_tasks (\n    const std::vector&lt; TaskT &gt; &amp; tasks\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeRMAWorkDistributor/#function-is_root_manager","title":"function is_root_manager","text":"<pre><code>inline bool dynampi::HierarchicalLockFreeRMAWorkDistributor::is_root_manager () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeRMAWorkDistributor/#function-remaining_tasks_count","title":"function remaining_tasks_count","text":"<pre><code>inline size_t dynampi::HierarchicalLockFreeRMAWorkDistributor::remaining_tasks_count () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeRMAWorkDistributor/#function-run_tasks","title":"function run_tasks","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::HierarchicalLockFreeRMAWorkDistributor::run_tasks (\n    const  RunConfig &amp; config=RunConfig {}\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeRMAWorkDistributor/#function-run_worker","title":"function run_worker","text":"<pre><code>inline void dynampi::HierarchicalLockFreeRMAWorkDistributor::run_worker () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeRMAWorkDistributor/#function-take_task_errors","title":"function take_task_errors","text":"<pre><code>inline std::vector&lt; TaskError &gt; dynampi::HierarchicalLockFreeRMAWorkDistributor::take_task_errors () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalLockFreeRMAWorkDistributor/#function-hierarchicallockfreermaworkdistributor_1","title":"function ~HierarchicalLockFreeRMAWorkDistributor","text":"<pre><code>inline dynampi::HierarchicalLockFreeRMAWorkDistributor::~HierarchicalLockFreeRMAWorkDistributor () \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_lockfree_rma_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1Config/","title":"Struct dynampi::HierarchicalLockFreeRMAWorkDistributor::Config","text":"<p>ClassList &gt; dynampi &gt; HierarchicalLockFreeRMAWorkDistributor &gt; Config</p> <ul> <li><code>#include &lt;hierarchical_lockfree_rma_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1Config/#public-attributes","title":"Public Attributes","text":"Type Name bool auto_run_workers   = <code>[**true**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code> MPI_Comm comm   = <code>[**MPI\\_COMM\\_WORLD**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code> int manager_rank   = <code>0</code> int max_local_group_size   = <code>0</code> int max_local_tasks   = <code>8192</code> int max_pending_rounds   = <code>2</code> int max_result_count   = <code>256</code> int max_task_count   = <code>256</code> int max_tasks   = <code>8192</code> int max_upper_fanout   = <code>-1</code> bool rethrow_task_errors   = <code>[**true**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1Config/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1Config/#variable-auto_run_workers","title":"variable auto_run_workers","text":"<pre><code>bool dynampi::HierarchicalLockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::auto_run_workers;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1Config/#variable-comm","title":"variable comm","text":"<pre><code>MPI_Comm dynampi::HierarchicalLockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::comm;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1Config/#variable-manager_rank","title":"variable manager_rank","text":"<pre><code>int dynampi::HierarchicalLockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::manager_rank;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1Config/#variable-max_local_group_size","title":"variable max_local_group_size","text":"<pre><code>int dynampi::HierarchicalLockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_local_group_size;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1Config/#variable-max_local_tasks","title":"variable max_local_tasks","text":"<pre><code>int dynampi::HierarchicalLockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_local_tasks;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1Config/#variable-max_pending_rounds","title":"variable max_pending_rounds","text":"<pre><code>int dynampi::HierarchicalLockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_pending_rounds;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1Config/#variable-max_result_count","title":"variable max_result_count","text":"<pre><code>int dynampi::HierarchicalLockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_result_count;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1Config/#variable-max_task_count","title":"variable max_task_count","text":"<pre><code>int dynampi::HierarchicalLockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_task_count;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1Config/#variable-max_tasks","title":"variable max_tasks","text":"<pre><code>int dynampi::HierarchicalLockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_tasks;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1Config/#variable-max_upper_fanout","title":"variable max_upper_fanout","text":"<pre><code>int dynampi::HierarchicalLockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_upper_fanout;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1Config/#variable-rethrow_task_errors","title":"variable rethrow_task_errors","text":"<pre><code>bool dynampi::HierarchicalLockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::rethrow_task_errors;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_lockfree_rma_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1RunConfig/","title":"Struct dynampi::HierarchicalLockFreeRMAWorkDistributor::RunConfig","text":"<p>ClassList &gt; dynampi &gt; HierarchicalLockFreeRMAWorkDistributor &gt; RunConfig</p> <ul> <li><code>#include &lt;hierarchical_lockfree_rma_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1RunConfig/#public-attributes","title":"Public Attributes","text":"Type Name bool allow_more_than_target_tasks   = <code>[**true**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code> std::optional&lt; double &gt; max_seconds   = <code>std::nullopt</code> size_t target_num_tasks   = <code>std::numeric\\_limits&amp;lt;[**size\\_t**](namespacedynampi.md#function-check_fixed_size_mpi_type)&amp;gt;[**::max**](namespacedynampi.md#function-check_fixed_size_mpi_type)()</code>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1RunConfig/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1RunConfig/#variable-allow_more_than_target_tasks","title":"variable allow_more_than_target_tasks","text":"<pre><code>bool dynampi::HierarchicalLockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::allow_more_than_target_tasks;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1RunConfig/#variable-max_seconds","title":"variable max_seconds","text":"<pre><code>std::optional&lt;double&gt; dynampi::HierarchicalLockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::max_seconds;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1RunConfig/#variable-target_num_tasks","title":"variable target_num_tasks","text":"<pre><code>size_t dynampi::HierarchicalLockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::target_num_tasks;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_lockfree_rma_distributor.hpp</code></p>"},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/","title":"Class dynampi::HierarchicalWorkDistributor","text":"<p>template &lt;typename TaskT, typename ResultT, typename... Options&gt;</p> <p>ClassList &gt; dynampi &gt; HierarchicalWorkDistributor</p> <ul> <li><code>#include &lt;hierarchical_distributor.hpp&gt;</code></li> </ul> <p>Inherits the following classes: dynampi::BaseWorkDistributor</p>"},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/#classes","title":"Classes","text":"Type Name struct Config struct RunConfig"},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/#public-static-attributes","title":"Public Static Attributes","text":"Type Name const bool ordered   = <code>[**false**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code> constexpr bool prioritize_tasks   = <code>Base::prioritize\\_tasks</code>"},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/#public-functions","title":"Public Functions","text":"Type Name HierarchicalWorkDistributor (std::function&lt; ResultT(TaskT)&gt; worker_function, Config runtime_config=Config{})  void allocate_task_to_child ()  void finalize ()  std::vector&lt; ResultT &gt; finish_remaining_tasks ()  const StatisticsT &amp; get_statistics () const bool has_task_errors () const void insert_task (TaskT task)  void insert_task (const TaskT &amp; task, double priority)  void insert_tasks (Range &amp;&amp; tasks)  void insert_tasks (const std::vector&lt; TaskT &gt; &amp; tasks)  bool is_root_manager () const size_t remaining_tasks_count () const void request_next_batch_if_room ()  std::vector&lt; ResultT &gt; run_tasks (const RunConfig &amp; config=RunConfig{})  void run_worker ()  void send_results_to_parent ()  std::vector&lt; TaskError &gt; take_task_errors ()  ~HierarchicalWorkDistributor ()"},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/#protected-types-inherited-from-dynampibaseworkdistributor","title":"Protected Types inherited from dynampi::BaseWorkDistributor","text":"<p>See dynampi::BaseWorkDistributor</p> Type Name typedef std::conditional_t&lt; prioritize_tasks, std::priority_queue&lt; std::pair&lt; double, TaskT &gt; &gt;, std::deque&lt; TaskT &gt; &gt; QueueT"},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/#protected-static-attributes-inherited-from-dynampibaseworkdistributor","title":"Protected Static Attributes inherited from dynampi::BaseWorkDistributor","text":"<p>See dynampi::BaseWorkDistributor</p> Type Name constexpr bool prioritize_tasks   = <code>[**get\\_option\\_value**](template__options_8hpp.md#function-get_option_value)&amp;lt;[**prioritize\\_tasks\\_t**](structdynampi_1_1prioritize__tasks__t.md), Options...&amp;gt;()</code>"},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/#variable-ordered","title":"variable ordered","text":"<pre><code>const bool dynampi::HierarchicalWorkDistributor&lt; TaskT, ResultT, Options &gt;::ordered;\n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/#variable-prioritize_tasks","title":"variable prioritize_tasks","text":"<pre><code>constexpr bool dynampi::HierarchicalWorkDistributor&lt; TaskT, ResultT, Options &gt;::prioritize_tasks;\n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/#function-hierarchicalworkdistributor","title":"function HierarchicalWorkDistributor","text":"<pre><code>inline explicit dynampi::HierarchicalWorkDistributor::HierarchicalWorkDistributor (\n    std::function&lt; ResultT ( TaskT )&gt; worker_function,\n    Config runtime_config=Config {}\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/#function-allocate_task_to_child","title":"function allocate_task_to_child","text":"<pre><code>inline void dynampi::HierarchicalWorkDistributor::allocate_task_to_child () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/#function-finalize","title":"function finalize","text":"<pre><code>inline void dynampi::HierarchicalWorkDistributor::finalize () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/#function-finish_remaining_tasks","title":"function finish_remaining_tasks","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::HierarchicalWorkDistributor::finish_remaining_tasks () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/#function-get_statistics","title":"function get_statistics","text":"<pre><code>inline const StatisticsT &amp; dynampi::HierarchicalWorkDistributor::get_statistics () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/#function-has_task_errors","title":"function has_task_errors","text":"<pre><code>inline bool dynampi::HierarchicalWorkDistributor::has_task_errors () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/#function-insert_task-12","title":"function insert_task [1/2]","text":"<pre><code>inline void dynampi::HierarchicalWorkDistributor::insert_task (\n    TaskT task\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/#function-insert_task-22","title":"function insert_task [2/2]","text":"<pre><code>inline void dynampi::HierarchicalWorkDistributor::insert_task (\n    const  TaskT &amp; task,\n    double priority\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/#function-insert_tasks-12","title":"function insert_tasks [1/2]","text":"<pre><code>template&lt;typename  Range&gt;\ninline void dynampi::HierarchicalWorkDistributor::insert_tasks (\n    Range &amp;&amp; tasks\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/#function-insert_tasks-22","title":"function insert_tasks [2/2]","text":"<pre><code>inline void dynampi::HierarchicalWorkDistributor::insert_tasks (\n    const std::vector&lt; TaskT &gt; &amp; tasks\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/#function-is_root_manager","title":"function is_root_manager","text":"<pre><code>inline bool dynampi::HierarchicalWorkDistributor::is_root_manager () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/#function-remaining_tasks_count","title":"function remaining_tasks_count","text":"<pre><code>inline size_t dynampi::HierarchicalWorkDistributor::remaining_tasks_count () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/#function-request_next_batch_if_room","title":"function request_next_batch_if_room","text":"<pre><code>inline void dynampi::HierarchicalWorkDistributor::request_next_batch_if_room () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/#function-run_tasks","title":"function run_tasks","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::HierarchicalWorkDistributor::run_tasks (\n    const  RunConfig &amp; config=RunConfig {}\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/#function-run_worker","title":"function run_worker","text":"<pre><code>inline void dynampi::HierarchicalWorkDistributor::run_worker () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/#function-send_results_to_parent","title":"function send_results_to_parent","text":"<pre><code>inline void dynampi::HierarchicalWorkDistributor::send_results_to_parent () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/#function-take_task_errors","title":"function take_task_errors","text":"<pre><code>inline std::vector&lt; TaskError &gt; dynampi::HierarchicalWorkDistributor::take_task_errors () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1HierarchicalWorkDistributor/#function-hierarchicalworkdistributor_1","title":"function ~HierarchicalWorkDistributor","text":"<pre><code>inline dynampi::HierarchicalWorkDistributor::~HierarchicalWorkDistributor () \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1Config/","title":"Struct dynampi::HierarchicalWorkDistributor::Config","text":"<p>ClassList &gt; dynampi &gt; HierarchicalWorkDistributor &gt; Config</p> <ul> <li><code>#include &lt;hierarchical_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1Config/#public-attributes","title":"Public Attributes","text":"Type Name bool auto_run_workers   = <code>[**true**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code> int batch_size_multiplier   = <code>1</code> MPI_Comm comm   = <code>[**MPI\\_COMM\\_WORLD**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code> bool manager_per_node   = <code>[**true**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code> int manager_rank   = <code>0</code> int max_local_group_size   = <code>0</code> int max_upper_fanout   = <code>-1</code> std::optional&lt; int &gt; max_workers_per_manager   = <code>std::nullopt</code> int pipeline_depth   = <code>2</code> bool rethrow_task_errors   = <code>[**true**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code>"},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1Config/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1Config/#variable-auto_run_workers","title":"variable auto_run_workers","text":"<pre><code>bool dynampi::HierarchicalWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::auto_run_workers;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1Config/#variable-batch_size_multiplier","title":"variable batch_size_multiplier","text":"<pre><code>int dynampi::HierarchicalWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::batch_size_multiplier;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1Config/#variable-comm","title":"variable comm","text":"<pre><code>MPI_Comm dynampi::HierarchicalWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::comm;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1Config/#variable-manager_per_node","title":"variable manager_per_node","text":"<pre><code>bool dynampi::HierarchicalWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::manager_per_node;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1Config/#variable-manager_rank","title":"variable manager_rank","text":"<pre><code>int dynampi::HierarchicalWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::manager_rank;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1Config/#variable-max_local_group_size","title":"variable max_local_group_size","text":"<pre><code>int dynampi::HierarchicalWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_local_group_size;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1Config/#variable-max_upper_fanout","title":"variable max_upper_fanout","text":"<pre><code>int dynampi::HierarchicalWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_upper_fanout;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1Config/#variable-max_workers_per_manager","title":"variable max_workers_per_manager","text":"<pre><code>std::optional&lt;int&gt; dynampi::HierarchicalWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_workers_per_manager;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1Config/#variable-pipeline_depth","title":"variable pipeline_depth","text":"<pre><code>int dynampi::HierarchicalWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::pipeline_depth;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1Config/#variable-rethrow_task_errors","title":"variable rethrow_task_errors","text":"<pre><code>bool dynampi::HierarchicalWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::rethrow_task_errors;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1RunConfig/","title":"Struct dynampi::HierarchicalWorkDistributor::RunConfig","text":"<p>ClassList &gt; dynampi &gt; HierarchicalWorkDistributor &gt; RunConfig</p> <ul> <li><code>#include &lt;hierarchical_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1RunConfig/#public-attributes","title":"Public Attributes","text":"Type Name bool allow_more_than_target_tasks   = <code>[**true**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code> std::optional&lt; double &gt; max_seconds   = <code>std::nullopt</code> size_t target_num_tasks   = <code>std::numeric\\_limits&amp;lt;[**size\\_t**](namespacedynampi.md#function-check_fixed_size_mpi_type)&amp;gt;[**::max**](namespacedynampi.md#function-check_fixed_size_mpi_type)()</code>"},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1RunConfig/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1RunConfig/#variable-allow_more_than_target_tasks","title":"variable allow_more_than_target_tasks","text":"<pre><code>bool dynampi::HierarchicalWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::allow_more_than_target_tasks;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1RunConfig/#variable-max_seconds","title":"variable max_seconds","text":"<pre><code>std::optional&lt;double&gt; dynampi::HierarchicalWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::max_seconds;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1RunConfig/#variable-target_num_tasks","title":"variable target_num_tasks","text":"<pre><code>size_t dynampi::HierarchicalWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::target_num_tasks;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_distributor.hpp</code></p>"},{"location":"dynampi/classdynampi_1_1LockFreeRMAWorkDistributor/","title":"Class dynampi::LockFreeRMAWorkDistributor","text":"<p>template &lt;typename TaskT, typename ResultT, typename... Options&gt;</p> <p>ClassList &gt; dynampi &gt; LockFreeRMAWorkDistributor</p> <ul> <li><code>#include &lt;lockfree_rma_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/classdynampi_1_1LockFreeRMAWorkDistributor/#classes","title":"Classes","text":"Type Name struct Config struct RunConfig struct Statistics"},{"location":"dynampi/classdynampi_1_1LockFreeRMAWorkDistributor/#public-types","title":"Public Types","text":"Type Name typedef std::conditional_t&lt; statistics_mode !=StatisticsMode::None, Statistics, std::monostate &gt; StatisticsT"},{"location":"dynampi/classdynampi_1_1LockFreeRMAWorkDistributor/#public-static-attributes","title":"Public Static Attributes","text":"Type Name const bool ordered   = <code>[**true**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code>"},{"location":"dynampi/classdynampi_1_1LockFreeRMAWorkDistributor/#public-functions","title":"Public Functions","text":"Type Name LockFreeRMAWorkDistributor (std::function&lt; ResultT(TaskT)&gt; worker_function, Config config={})  void finalize ()  std::vector&lt; ResultT &gt; finish_remaining_tasks ()  std::vector&lt; ResultT &gt; gather_once ()  const StatisticsT &amp; get_statistics () const bool has_task_errors () const void insert_task (TaskT task)  void insert_task (const TaskT &amp; task, double)  void insert_tasks (const std::vector&lt; TaskT &gt; &amp; tasks)  bool is_root_manager () const int num_workers () const size_t remaining_tasks_count () const std::vector&lt; ResultT &gt; run_tasks (RunConfig config={})  void run_worker ()  std::vector&lt; TaskError &gt; take_task_errors ()  ~LockFreeRMAWorkDistributor ()"},{"location":"dynampi/classdynampi_1_1LockFreeRMAWorkDistributor/#public-types-documentation","title":"Public Types Documentation","text":""},{"location":"dynampi/classdynampi_1_1LockFreeRMAWorkDistributor/#typedef-statisticst","title":"typedef StatisticsT","text":"<pre><code>using dynampi::LockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::StatisticsT =  std::conditional_t&lt;statistics_mode != StatisticsMode::None, Statistics, std::monostate&gt;;\n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeRMAWorkDistributor/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/classdynampi_1_1LockFreeRMAWorkDistributor/#variable-ordered","title":"variable ordered","text":"<pre><code>const bool dynampi::LockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::ordered;\n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeRMAWorkDistributor/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/classdynampi_1_1LockFreeRMAWorkDistributor/#function-lockfreermaworkdistributor","title":"function LockFreeRMAWorkDistributor","text":"<pre><code>inline explicit dynampi::LockFreeRMAWorkDistributor::LockFreeRMAWorkDistributor (\n    std::function&lt; ResultT ( TaskT )&gt; worker_function,\n    Config config={}\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeRMAWorkDistributor/#function-finalize","title":"function finalize","text":"<pre><code>inline void dynampi::LockFreeRMAWorkDistributor::finalize () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeRMAWorkDistributor/#function-finish_remaining_tasks","title":"function finish_remaining_tasks","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::LockFreeRMAWorkDistributor::finish_remaining_tasks () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeRMAWorkDistributor/#function-gather_once","title":"function gather_once","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::LockFreeRMAWorkDistributor::gather_once () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeRMAWorkDistributor/#function-get_statistics","title":"function get_statistics","text":"<pre><code>inline const  StatisticsT &amp; dynampi::LockFreeRMAWorkDistributor::get_statistics () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeRMAWorkDistributor/#function-has_task_errors","title":"function has_task_errors","text":"<pre><code>inline bool dynampi::LockFreeRMAWorkDistributor::has_task_errors () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeRMAWorkDistributor/#function-insert_task-12","title":"function insert_task [1/2]","text":"<pre><code>inline void dynampi::LockFreeRMAWorkDistributor::insert_task (\n    TaskT task\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeRMAWorkDistributor/#function-insert_task-22","title":"function insert_task [2/2]","text":"<pre><code>inline void dynampi::LockFreeRMAWorkDistributor::insert_task (\n    const  TaskT &amp; task,\n    double\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeRMAWorkDistributor/#function-insert_tasks","title":"function insert_tasks","text":"<pre><code>inline void dynampi::LockFreeRMAWorkDistributor::insert_tasks (\n    const std::vector&lt; TaskT &gt; &amp; tasks\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeRMAWorkDistributor/#function-is_root_manager","title":"function is_root_manager","text":"<pre><code>inline bool dynampi::LockFreeRMAWorkDistributor::is_root_manager () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeRMAWorkDistributor/#function-num_workers","title":"function num_workers","text":"<pre><code>inline int dynampi::LockFreeRMAWorkDistributor::num_workers () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeRMAWorkDistributor/#function-remaining_tasks_count","title":"function remaining_tasks_count","text":"<pre><code>inline size_t dynampi::LockFreeRMAWorkDistributor::remaining_tasks_count () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeRMAWorkDistributor/#function-run_tasks","title":"function run_tasks","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::LockFreeRMAWorkDistributor::run_tasks (\n    RunConfig config={}\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeRMAWorkDistributor/#function-run_worker","title":"function run_worker","text":"<pre><code>inline void dynampi::LockFreeRMAWorkDistributor::run_worker () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeRMAWorkDistributor/#function-take_task_errors","title":"function take_task_errors","text":"<pre><code>inline std::vector&lt; TaskError &gt; dynampi::LockFreeRMAWorkDistributor::take_task_errors () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1LockFreeRMAWorkDistributor/#function-lockfreermaworkdistributor_1","title":"function ~LockFreeRMAWorkDistributor","text":"<pre><code>inline dynampi::LockFreeRMAWorkDistributor::~LockFreeRMAWorkDistributor () \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/lockfree_rma_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1LockFreeRMAWorkDistributor_1_1Config/","title":"Struct dynampi::LockFreeRMAWorkDistributor::Config","text":"<p>ClassList &gt; dynampi &gt; LockFreeRMAWorkDistributor &gt; Config</p> <ul> <li><code>#include &lt;lockfree_rma_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1LockFreeRMAWorkDistributor_1_1Config/#public-attributes","title":"Public Attributes","text":"Type Name bool auto_run_workers   = <code>[**true**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code> MPI_Comm comm   = <code>[**MPI\\_COMM\\_WORLD**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code> int manager_rank   = <code>0</code> int max_result_count   = <code>256</code> int max_task_count   = <code>256</code> int max_tasks   = <code>8192</code> bool rethrow_task_errors   = <code>[**true**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code>"},{"location":"dynampi/structdynampi_1_1LockFreeRMAWorkDistributor_1_1Config/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1LockFreeRMAWorkDistributor_1_1Config/#variable-auto_run_workers","title":"variable auto_run_workers","text":"<pre><code>bool dynampi::LockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::auto_run_workers;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1LockFreeRMAWorkDistributor_1_1Config/#variable-comm","title":"variable comm","text":"<pre><code>MPI_Comm dynampi::LockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::comm;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1LockFreeRMAWorkDistributor_1_1Config/#variable-manager_rank","title":"variable manager_rank","text":"<pre><code>int dynampi::LockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::manager_rank;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1LockFreeRMAWorkDistributor_1_1Config/#variable-max_result_count","title":"variable max_result_count","text":"<pre><code>int dynampi::LockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_result_count;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1LockFreeRMAWorkDistributor_1_1Config/#variable-max_task_count","title":"variable max_task_count","text":"<pre><code>int dynampi::LockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_task_count;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1LockFreeRMAWorkDistributor_1_1Config/#variable-max_tasks","title":"variable max_tasks","text":"<pre><code>int dynampi::LockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::max_tasks;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1LockFreeRMAWorkDistributor_1_1Config/#variable-rethrow_task_errors","title":"variable rethrow_task_errors","text":"<pre><code>bool dynampi::LockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::rethrow_task_errors;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/lockfree_rma_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1LockFreeRMAWorkDistributor_1_1RunConfig/","title":"Struct dynampi::LockFreeRMAWorkDistributor::RunConfig","text":"<p>ClassList &gt; dynampi &gt; LockFreeRMAWorkDistributor &gt; RunConfig</p> <ul> <li><code>#include &lt;lockfree_rma_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1LockFreeRMAWorkDistributor_1_1RunConfig/#public-attributes","title":"Public Attributes","text":"Type Name bool allow_more_than_target_tasks   = <code>[**true**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code> std::optional&lt; double &gt; max_seconds   = <code>std::nullopt</code> size_t target_num_tasks   = <code>std::numeric\\_limits&amp;lt;[**size\\_t**](namespacedynampi.md#function-check_fixed_size_mpi_type)&amp;gt;[**::max**](namespacedynampi.md#function-check_fixed_size_mpi_type)()</code>"},{"location":"dynampi/structdynampi_1_1LockFreeRMAWorkDistributor_1_1RunConfig/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1LockFreeRMAWorkDistributor_1_1RunConfig/#variable-allow_more_than_target_tasks","title":"variable allow_more_than_target_tasks","text":"<pre><code>bool dynampi::LockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::allow_more_than_target_tasks;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1LockFreeRMAWorkDistributor_1_1RunConfig/#variable-max_seconds","title":"variable max_seconds","text":"<pre><code>std::optional&lt;double&gt; dynampi::LockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::max_seconds;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1LockFreeRMAWorkDistributor_1_1RunConfig/#variable-target_num_tasks","title":"variable target_num_tasks","text":"<pre><code>size_t dynampi::LockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::target_num_tasks;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/lockfree_rma_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1LockFreeRMAWorkDistributor_1_1Statistics/","title":"Struct dynampi::LockFreeRMAWorkDistributor::Statistics","text":"<p>ClassList &gt; dynampi &gt; LockFreeRMAWorkDistributor &gt; Statistics</p> <ul> <li><code>#include &lt;lockfree_rma_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1LockFreeRMAWorkDistributor_1_1Statistics/#public-attributes","title":"Public Attributes","text":"Type Name const CommStatistics &amp; comm_statistics"},{"location":"dynampi/structdynampi_1_1LockFreeRMAWorkDistributor_1_1Statistics/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1LockFreeRMAWorkDistributor_1_1Statistics/#variable-comm_statistics","title":"variable comm_statistics","text":"<pre><code>const CommStatistics&amp; dynampi::LockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::Statistics::comm_statistics;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/lockfree_rma_distributor.hpp</code></p>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/","title":"Class dynampi::MPICommunicator","text":"<p>template &lt;typename... Options&gt;</p> <p>ClassList &gt; dynampi &gt; MPICommunicator</p> <ul> <li><code>#include &lt;mpi_communicator.hpp&gt;</code></li> </ul>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#public-types","title":"Public Types","text":"Type Name enum Ownership"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#public-static-attributes","title":"Public Static Attributes","text":"Type Name constexpr size_t kMaxRmaChunkBytes   = <code>[**static\\_cast**](namespacedynampi.md#function-check_fixed_size_mpi_type)&amp;lt;[**size\\_t**](namespacedynampi.md#function-check_fixed_size_mpi_type)&amp;gt;(std::numeric\\_limits&amp;lt;[**int**](namespacedynampi.md#function-check_fixed_size_mpi_type)&amp;gt;[**::max**](namespacedynampi.md#function-check_fixed_size_mpi_type)())</code>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#public-functions","title":"Public Functions","text":"Type Name MPICommunicator (MPI_Comm comm, Ownership ownership=Duplicate)  MPICommunicator (const MPICommunicator &amp; other) = delete MPICommunicator (MPICommunicator &amp;&amp; other) noexcept void broadcast (T &amp; data, int root=0)  void fetch_and_op (T &amp; origin, T &amp; result, int target_rank, MPI_Aint target_disp, MPI_Op op, MPI_Win win)  void gather (const T &amp; data, std::vector&lt; T &gt; * result, int root=0)  MPI_Comm get () const void get_bytes (void * dst, size_t n, int target_rank, MPI_Aint target_disp, MPI_Win win)  const CommStatistics &amp; get_statistics () const operator MPI_Comm () const MPICommunicator &amp; operator= (const MPICommunicator &amp; other) = delete MPICommunicator &amp; operator= (MPICommunicator &amp;&amp; other) = delete MPI_Status probe (int source=MPI_ANY_SOURCE, int tag=MPI_ANY_TAG)  void put_bytes (const void * src, size_t n, int target_rank, MPI_Aint target_disp, MPI_Win win)  int rank () const void recv (T &amp; data, int source, int tag=0)  MPI_Status recv_any (T &amp; data, int source=MPI_ANY_SOURCE, int tag=MPI_ANY_TAG)  void recv_empty (int source, int tag=0) Receives 0 elements of type T. Use when the sender used send_empty&lt;T&gt;. void recv_empty_message (int source, int tag=0)  void send (const T &amp; data, int dest, int tag=0)  void send_empty (int dest, int tag=0)  int size () const std::optional&lt; MPICommunicator &gt; split (int color, int key=0) const MPICommunicator split_by_node () const ~MPICommunicator ()"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#public-types-documentation","title":"Public Types Documentation","text":""},{"location":"dynampi/classdynampi_1_1MPICommunicator/#enum-ownership","title":"enum Ownership","text":"<pre><code>enum dynampi::MPICommunicator::Ownership {\n    Reference,\n    Move,\n    Duplicate\n};\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/classdynampi_1_1MPICommunicator/#variable-kmaxrmachunkbytes","title":"variable kMaxRmaChunkBytes","text":"<pre><code>constexpr size_t dynampi::MPICommunicator&lt; Options &gt;::kMaxRmaChunkBytes;\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-mpicommunicator-13","title":"function MPICommunicator [1/3]","text":"<pre><code>inline explicit dynampi::MPICommunicator::MPICommunicator (\n    MPI_Comm comm,\n    Ownership ownership=Duplicate\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-mpicommunicator-23","title":"function MPICommunicator [2/3]","text":"<pre><code>dynampi::MPICommunicator::MPICommunicator (\n    const  MPICommunicator &amp; other\n) = delete\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-mpicommunicator-33","title":"function MPICommunicator [3/3]","text":"<pre><code>inline dynampi::MPICommunicator::MPICommunicator (\n    MPICommunicator &amp;&amp; other\n) noexcept\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-broadcast","title":"function broadcast","text":"<pre><code>template&lt;typename  T&gt;\ninline void dynampi::MPICommunicator::broadcast (\n    T &amp; data,\n    int root=0\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-fetch_and_op","title":"function fetch_and_op","text":"<pre><code>template&lt;typename  T&gt;\ninline void dynampi::MPICommunicator::fetch_and_op (\n    T &amp; origin,\n    T &amp; result,\n    int target_rank,\n    MPI_Aint target_disp,\n    MPI_Op op,\n    MPI_Win win\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-gather","title":"function gather","text":"<pre><code>template&lt;typename  T&gt;\ninline void dynampi::MPICommunicator::gather (\n    const  T &amp; data,\n    std::vector&lt; T &gt; * result,\n    int root=0\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-get","title":"function get","text":"<pre><code>inline MPI_Comm dynampi::MPICommunicator::get () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-get_bytes","title":"function get_bytes","text":"<pre><code>inline void dynampi::MPICommunicator::get_bytes (\n    void * dst,\n    size_t n,\n    int target_rank,\n    MPI_Aint target_disp,\n    MPI_Win win\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-get_statistics","title":"function get_statistics","text":"<pre><code>inline const  CommStatistics &amp; dynampi::MPICommunicator::get_statistics () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-operator-mpi_comm","title":"function operator MPI_Comm","text":"<pre><code>inline dynampi::MPICommunicator::operator MPI_Comm () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-operator","title":"function operator=","text":"<pre><code>MPICommunicator &amp; dynampi::MPICommunicator::operator= (\n    const  MPICommunicator &amp; other\n) = delete\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-operator_1","title":"function operator=","text":"<pre><code>MPICommunicator &amp; dynampi::MPICommunicator::operator= (\n    MPICommunicator &amp;&amp; other\n) = delete\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-probe","title":"function probe","text":"<pre><code>inline MPI_Status dynampi::MPICommunicator::probe (\n    int source=MPI_ANY_SOURCE,\n    int tag=MPI_ANY_TAG\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-put_bytes","title":"function put_bytes","text":"<pre><code>inline void dynampi::MPICommunicator::put_bytes (\n    const  void * src,\n    size_t n,\n    int target_rank,\n    MPI_Aint target_disp,\n    MPI_Win win\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-rank","title":"function rank","text":"<pre><code>inline int dynampi::MPICommunicator::rank () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-recv","title":"function recv","text":"<pre><code>template&lt;typename  T&gt;\ninline void dynampi::MPICommunicator::recv (\n    T &amp; data,\n    int source,\n    int tag=0\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-recv_any","title":"function recv_any","text":"<pre><code>template&lt;typename  T&gt;\ninline MPI_Status dynampi::MPICommunicator::recv_any (\n    T &amp; data,\n    int source=MPI_ANY_SOURCE,\n    int tag=MPI_ANY_TAG\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-recv_empty","title":"function recv_empty","text":"<p>Receives 0 elements of type T. Use when the sender used send_empty&lt;T&gt;. <pre><code>template&lt;typename  T&gt;\ninline void dynampi::MPICommunicator::recv_empty (\n    int source,\n    int tag=0\n) \n</code></pre></p>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-recv_empty_message","title":"function recv_empty_message","text":"<pre><code>inline void dynampi::MPICommunicator::recv_empty_message (\n    int source,\n    int tag=0\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-send","title":"function send","text":"<pre><code>template&lt;typename  T&gt;\ninline void dynampi::MPICommunicator::send (\n    const  T &amp; data,\n    int dest,\n    int tag=0\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-send_empty","title":"function send_empty","text":"<pre><code>template&lt;typename  T&gt;\ninline void dynampi::MPICommunicator::send_empty (\n    int dest,\n    int tag=0\n) \n</code></pre> <p>Sends 0 elements of type T (same type as recv buffer) so that recv_any(T&amp;) can receive any worker message (REQUEST or RESULT) into a single buffer type. </p>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-size","title":"function size","text":"<pre><code>inline int dynampi::MPICommunicator::size () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-split","title":"function split","text":"<pre><code>inline std::optional&lt; MPICommunicator &gt; dynampi::MPICommunicator::split (\n    int color,\n    int key=0\n) const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-split_by_node","title":"function split_by_node","text":"<pre><code>inline MPICommunicator dynampi::MPICommunicator::split_by_node () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPICommunicator/#function-mpicommunicator","title":"function ~MPICommunicator","text":"<pre><code>inline dynampi::MPICommunicator::~MPICommunicator () \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_communicator.hpp</code></p>"},{"location":"dynampi/classdynampi_1_1MPIGroup/","title":"Class dynampi::MPIGroup","text":"<p>ClassList &gt; dynampi &gt; MPIGroup</p> <ul> <li><code>#include &lt;mpi_group.hpp&gt;</code></li> </ul>"},{"location":"dynampi/classdynampi_1_1MPIGroup/#public-functions","title":"Public Functions","text":"Type Name MPIGroup (const MPICommunicator&lt; Options... &gt; &amp; comm)  MPIGroup (const MPIGroup &amp; other) = delete MPIGroup (MPIGroup &amp;&amp; other) noexcept MPI_Group get () const operator MPI_Group () const MPIGroup &amp; operator= (const MPIGroup &amp; other) = delete MPIGroup &amp; operator= (MPIGroup &amp;&amp; other) noexcept int rank () const int size () const int translate_rank (int rank, const MPIGroup &amp; to_group) const void translate_ranks (const MPIGroup &amp; to_group, int n, const int ranks, int translated_ranks) const ~MPIGroup ()"},{"location":"dynampi/classdynampi_1_1MPIGroup/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/classdynampi_1_1MPIGroup/#function-mpigroup-13","title":"function MPIGroup [1/3]","text":"<pre><code>template&lt;typename... Options&gt;\ninline explicit dynampi::MPIGroup::MPIGroup (\n    const  MPICommunicator &lt; Options... &gt; &amp; comm\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPIGroup/#function-mpigroup-23","title":"function MPIGroup [2/3]","text":"<pre><code>dynampi::MPIGroup::MPIGroup (\n    const  MPIGroup &amp; other\n) = delete\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPIGroup/#function-mpigroup-33","title":"function MPIGroup [3/3]","text":"<pre><code>inline dynampi::MPIGroup::MPIGroup (\n    MPIGroup &amp;&amp; other\n) noexcept\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPIGroup/#function-get","title":"function get","text":"<pre><code>inline MPI_Group dynampi::MPIGroup::get () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPIGroup/#function-operator-mpi_group","title":"function operator MPI_Group","text":"<pre><code>inline dynampi::MPIGroup::operator MPI_Group () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPIGroup/#function-operator","title":"function operator=","text":"<pre><code>MPIGroup &amp; dynampi::MPIGroup::operator= (\n    const  MPIGroup &amp; other\n) = delete\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPIGroup/#function-operator_1","title":"function operator=","text":"<pre><code>inline MPIGroup &amp; dynampi::MPIGroup::operator= (\n    MPIGroup &amp;&amp; other\n) noexcept\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPIGroup/#function-rank","title":"function rank","text":"<pre><code>inline int dynampi::MPIGroup::rank () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPIGroup/#function-size","title":"function size","text":"<pre><code>inline int dynampi::MPIGroup::size () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPIGroup/#function-translate_rank","title":"function translate_rank","text":"<pre><code>inline int dynampi::MPIGroup::translate_rank (\n    int rank,\n    const  MPIGroup &amp; to_group\n) const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPIGroup/#function-translate_ranks","title":"function translate_ranks","text":"<pre><code>inline void dynampi::MPIGroup::translate_ranks (\n    const  MPIGroup &amp; to_group,\n    int n,\n    const  int ranks,\n    int translated_ranks\n) const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1MPIGroup/#function-mpigroup","title":"function ~MPIGroup","text":"<pre><code>inline dynampi::MPIGroup::~MPIGroup () \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_group.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1MPI__Type/","title":"Struct dynampi::MPI_Type","text":"<p>template &lt;typename T, typename&gt;</p> <p>ClassList &gt; dynampi &gt; MPI_Type</p> <ul> <li><code>#include &lt;mpi_types.hpp&gt;</code></li> </ul> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_types.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1nullptr__t_01_4/","title":"Struct dynampi::MPI_Type&lt; std::nullptr_t &gt;","text":"<p>template &lt;&gt;</p> <p>ClassList &gt; dynampi &gt; MPI_Type&lt; std::nullptr_t &gt;</p> <ul> <li><code>#include &lt;mpi_types.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1nullptr__t_01_4/#public-static-attributes","title":"Public Static Attributes","text":"Type Name const bool resize_required   = <code>[**false**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code> const MPI_Datatype value   = <code>[**MPI\\_PACKED**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1nullptr__t_01_4/#public-static-functions","title":"Public Static Functions","text":"Type Name int count (const std::nullptr_t &amp;) noexcept void * ptr (std::nullptr_t &amp;) noexcept const void * ptr (const std::nullptr_t &amp;) noexcept void resize (std::nullptr_t &amp;, int new_size) noexcept"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1nullptr__t_01_4/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1nullptr__t_01_4/#variable-resize_required","title":"variable resize_required","text":"<pre><code>const bool dynampi::MPI_Type&lt; std::nullptr_t &gt;::resize_required;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1nullptr__t_01_4/#variable-value","title":"variable value","text":"<pre><code>const MPI_Datatype dynampi::MPI_Type&lt; std::nullptr_t &gt;::value;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1nullptr__t_01_4/#public-static-functions-documentation","title":"Public Static Functions Documentation","text":""},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1nullptr__t_01_4/#function-count","title":"function count","text":"<pre><code>static inline int dynampi::MPI_Type&lt; std::nullptr_t &gt;::count (\n    const std::nullptr_t &amp;\n) noexcept\n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1nullptr__t_01_4/#function-ptr-12","title":"function ptr [1/2]","text":"<pre><code>static inline void * dynampi::MPI_Type&lt; std::nullptr_t &gt;::ptr (\n    std::nullptr_t &amp;\n) noexcept\n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1nullptr__t_01_4/#function-ptr-22","title":"function ptr [2/2]","text":"<pre><code>static inline const  void * dynampi::MPI_Type&lt; std::nullptr_t &gt;::ptr (\n    const std::nullptr_t &amp;\n) noexcept\n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1nullptr__t_01_4/#function-resize","title":"function resize","text":"<pre><code>static inline void dynampi::MPI_Type&lt; std::nullptr_t &gt;::resize (\n    std::nullptr_t &amp;,\n    int new_size\n) noexcept\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_types.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1string_01_4/","title":"Struct dynampi::MPI_Type&lt; std::string &gt;","text":"<p>template &lt;&gt;</p> <p>ClassList &gt; dynampi &gt; MPI_Type&lt; std::string &gt;</p> <ul> <li><code>#include &lt;mpi_types.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1string_01_4/#public-static-attributes","title":"Public Static Attributes","text":"Type Name const bool resize_required   = <code>[**true**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code> const MPI_Datatype value   = <code>[**MPI\\_CHAR**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1string_01_4/#public-static-functions","title":"Public Static Functions","text":"Type Name int count (const std::string &amp; str)  void * ptr (std::string &amp; str) noexcept const void * ptr (const std::string &amp; str) noexcept void resize (std::string &amp; str, int new_size)"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1string_01_4/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1string_01_4/#variable-resize_required","title":"variable resize_required","text":"<pre><code>const bool dynampi::MPI_Type&lt; std::string &gt;::resize_required;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1string_01_4/#variable-value","title":"variable value","text":"<pre><code>const MPI_Datatype dynampi::MPI_Type&lt; std::string &gt;::value;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1string_01_4/#public-static-functions-documentation","title":"Public Static Functions Documentation","text":""},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1string_01_4/#function-count","title":"function count","text":"<pre><code>static inline int dynampi::MPI_Type&lt; std::string &gt;::count (\n    const std::string &amp; str\n) \n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1string_01_4/#function-ptr-12","title":"function ptr [1/2]","text":"<pre><code>static inline void * dynampi::MPI_Type&lt; std::string &gt;::ptr (\n    std::string &amp; str\n) noexcept\n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1string_01_4/#function-ptr-22","title":"function ptr [2/2]","text":"<pre><code>static inline const  void * dynampi::MPI_Type&lt; std::string &gt;::ptr (\n    const std::string &amp; str\n) noexcept\n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1string_01_4/#function-resize","title":"function resize","text":"<pre><code>static inline void dynampi::MPI_Type&lt; std::string &gt;::resize (\n    std::string &amp; str,\n    int new_size\n) \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_types.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1vector_3_01T_01_4_00_01std_1_1enable__if__t_3_01has__dynam0c05b0754f90b71498257126104ee051/","title":"Struct dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;","text":"<p>template &lt;typename T&gt;</p> <p>ClassList &gt; dynampi &gt; MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;</p> <ul> <li><code>#include &lt;mpi_types.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1vector_3_01T_01_4_00_01std_1_1enable__if__t_3_01has__dynam0c05b0754f90b71498257126104ee051/#public-static-attributes","title":"Public Static Attributes","text":"Type Name const bool resize_required   = <code>[**true**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code> const MPI_Datatype value   = <code>[**MPI\\_Type**](structdynampi_1_1MPI__Type.md)&amp;lt;[**T**](namespacedynampi.md#function-check_fixed_size_mpi_type)&amp;gt;::value</code>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1vector_3_01T_01_4_00_01std_1_1enable__if__t_3_01has__dynam0c05b0754f90b71498257126104ee051/#public-static-functions","title":"Public Static Functions","text":"Type Name int count (const std::vector&lt; T &gt; &amp; vec)  void * ptr (std::vector&lt; T &gt; &amp; vec) noexcept const void * ptr (const std::vector&lt; T &gt; &amp; vec) noexcept void resize (std::vector&lt; T &gt; &amp; vec, int new_size)"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1vector_3_01T_01_4_00_01std_1_1enable__if__t_3_01has__dynam0c05b0754f90b71498257126104ee051/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1vector_3_01T_01_4_00_01std_1_1enable__if__t_3_01has__dynam0c05b0754f90b71498257126104ee051/#variable-resize_required","title":"variable resize_required","text":"<pre><code>const bool dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;::resize_required;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1vector_3_01T_01_4_00_01std_1_1enable__if__t_3_01has__dynam0c05b0754f90b71498257126104ee051/#variable-value","title":"variable value","text":"<pre><code>const MPI_Datatype dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;::value;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1vector_3_01T_01_4_00_01std_1_1enable__if__t_3_01has__dynam0c05b0754f90b71498257126104ee051/#public-static-functions-documentation","title":"Public Static Functions Documentation","text":""},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1vector_3_01T_01_4_00_01std_1_1enable__if__t_3_01has__dynam0c05b0754f90b71498257126104ee051/#function-count","title":"function count","text":"<pre><code>static inline int dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;::count (\n    const std::vector&lt; T &gt; &amp; vec\n) \n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1vector_3_01T_01_4_00_01std_1_1enable__if__t_3_01has__dynam0c05b0754f90b71498257126104ee051/#function-ptr-12","title":"function ptr [1/2]","text":"<pre><code>static inline void * dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;::ptr (\n    std::vector&lt; T &gt; &amp; vec\n) noexcept\n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1vector_3_01T_01_4_00_01std_1_1enable__if__t_3_01has__dynam0c05b0754f90b71498257126104ee051/#function-ptr-22","title":"function ptr [2/2]","text":"<pre><code>static inline const  void * dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;::ptr (\n    const std::vector&lt; T &gt; &amp; vec\n) noexcept\n</code></pre>"},{"location":"dynampi/structdynampi_1_1MPI__Type_3_01std_1_1vector_3_01T_01_4_00_01std_1_1enable__if__t_3_01has__dynam0c05b0754f90b71498257126104ee051/#function-resize","title":"function resize","text":"<pre><code>static inline void dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;::resize (\n    std::vector&lt; T &gt; &amp; vec,\n    int new_size\n) \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_types.hpp</code></p>"},{"location":"dynampi/classdynampi_1_1NaiveWorkDistributor/","title":"Class dynampi::NaiveWorkDistributor","text":"<p>template &lt;typename TaskT, typename ResultT, typename... Options&gt;</p> <p>ClassList &gt; dynampi &gt; NaiveWorkDistributor</p> <ul> <li><code>#include &lt;naive_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/classdynampi_1_1NaiveWorkDistributor/#classes","title":"Classes","text":"Type Name struct Config struct RunConfig struct Statistics"},{"location":"dynampi/classdynampi_1_1NaiveWorkDistributor/#public-types","title":"Public Types","text":"Type Name typedef std::conditional_t&lt; statistics_mode !=StatisticsMode::None, Statistics, std::monostate &gt; StatisticsT"},{"location":"dynampi/classdynampi_1_1NaiveWorkDistributor/#public-static-attributes","title":"Public Static Attributes","text":"Type Name const bool ordered   = <code>[**true**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code>"},{"location":"dynampi/classdynampi_1_1NaiveWorkDistributor/#public-functions","title":"Public Functions","text":"Type Name NaiveWorkDistributor (std::function&lt; ResultT(TaskT)&gt; worker_function, Config runtime_config=Config{})  void finalize ()  std::vector&lt; ResultT &gt; finish_remaining_tasks ()  const StatisticsT &amp; get_statistics () const bool has_task_errors () const void insert_task (TaskT task)  void insert_task (const TaskT &amp; task, double priority)  void insert_tasks (const std::vector&lt; TaskT &gt; &amp; tasks)  bool is_root_manager () const size_t remaining_tasks_count () const std::vector&lt; ResultT &gt; run_tasks (RunConfig config=RunConfig{})  void run_worker ()  std::vector&lt; TaskError &gt; take_task_errors ()  ~NaiveWorkDistributor ()"},{"location":"dynampi/classdynampi_1_1NaiveWorkDistributor/#public-types-documentation","title":"Public Types Documentation","text":""},{"location":"dynampi/classdynampi_1_1NaiveWorkDistributor/#typedef-statisticst","title":"typedef StatisticsT","text":"<pre><code>using dynampi::NaiveWorkDistributor&lt; TaskT, ResultT, Options &gt;::StatisticsT =  std::conditional_t&lt;statistics_mode != StatisticsMode::None, Statistics, std::monostate&gt;;\n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveWorkDistributor/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/classdynampi_1_1NaiveWorkDistributor/#variable-ordered","title":"variable ordered","text":"<pre><code>const bool dynampi::NaiveWorkDistributor&lt; TaskT, ResultT, Options &gt;::ordered;\n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveWorkDistributor/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/classdynampi_1_1NaiveWorkDistributor/#function-naiveworkdistributor","title":"function NaiveWorkDistributor","text":"<pre><code>inline explicit dynampi::NaiveWorkDistributor::NaiveWorkDistributor (\n    std::function&lt; ResultT ( TaskT )&gt; worker_function,\n    Config runtime_config=Config {}\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveWorkDistributor/#function-finalize","title":"function finalize","text":"<pre><code>inline void dynampi::NaiveWorkDistributor::finalize () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveWorkDistributor/#function-finish_remaining_tasks","title":"function finish_remaining_tasks","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::NaiveWorkDistributor::finish_remaining_tasks () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveWorkDistributor/#function-get_statistics","title":"function get_statistics","text":"<pre><code>inline const  StatisticsT &amp; dynampi::NaiveWorkDistributor::get_statistics () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveWorkDistributor/#function-has_task_errors","title":"function has_task_errors","text":"<pre><code>inline bool dynampi::NaiveWorkDistributor::has_task_errors () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveWorkDistributor/#function-insert_task-12","title":"function insert_task [1/2]","text":"<pre><code>inline void dynampi::NaiveWorkDistributor::insert_task (\n    TaskT task\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveWorkDistributor/#function-insert_task-22","title":"function insert_task [2/2]","text":"<pre><code>inline void dynampi::NaiveWorkDistributor::insert_task (\n    const  TaskT &amp; task,\n    double priority\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveWorkDistributor/#function-insert_tasks","title":"function insert_tasks","text":"<pre><code>inline void dynampi::NaiveWorkDistributor::insert_tasks (\n    const std::vector&lt; TaskT &gt; &amp; tasks\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveWorkDistributor/#function-is_root_manager","title":"function is_root_manager","text":"<pre><code>inline bool dynampi::NaiveWorkDistributor::is_root_manager () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveWorkDistributor/#function-remaining_tasks_count","title":"function remaining_tasks_count","text":"<pre><code>inline size_t dynampi::NaiveWorkDistributor::remaining_tasks_count () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveWorkDistributor/#function-run_tasks","title":"function run_tasks","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::NaiveWorkDistributor::run_tasks (\n    RunConfig config=RunConfig {}\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveWorkDistributor/#function-run_worker","title":"function run_worker","text":"<pre><code>inline void dynampi::NaiveWorkDistributor::run_worker () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveWorkDistributor/#function-take_task_errors","title":"function take_task_errors","text":"<pre><code>inline std::vector&lt; TaskError &gt; dynampi::NaiveWorkDistributor::take_task_errors () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1NaiveWorkDistributor/#function-naiveworkdistributor_1","title":"function ~NaiveWorkDistributor","text":"<pre><code>inline dynampi::NaiveWorkDistributor::~NaiveWorkDistributor () \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/naive_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1NaiveWorkDistributor_1_1Config/","title":"Struct dynampi::NaiveWorkDistributor::Config","text":"<p>ClassList &gt; dynampi &gt; NaiveWorkDistributor &gt; Config</p> <ul> <li><code>#include &lt;naive_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1NaiveWorkDistributor_1_1Config/#public-attributes","title":"Public Attributes","text":"Type Name bool auto_run_workers   = <code>[**true**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code> MPI_Comm comm   = <code>[**MPI\\_COMM\\_WORLD**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code> int manager_rank   = <code>0</code> bool rethrow_task_errors   = <code>[**true**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code>"},{"location":"dynampi/structdynampi_1_1NaiveWorkDistributor_1_1Config/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1NaiveWorkDistributor_1_1Config/#variable-auto_run_workers","title":"variable auto_run_workers","text":"<pre><code>bool dynampi::NaiveWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::auto_run_workers;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1NaiveWorkDistributor_1_1Config/#variable-comm","title":"variable comm","text":"<pre><code>MPI_Comm dynampi::NaiveWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::comm;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1NaiveWorkDistributor_1_1Config/#variable-manager_rank","title":"variable manager_rank","text":"<pre><code>int dynampi::NaiveWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::manager_rank;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1NaiveWorkDistributor_1_1Config/#variable-rethrow_task_errors","title":"variable rethrow_task_errors","text":"<pre><code>bool dynampi::NaiveWorkDistributor&lt; TaskT, ResultT, Options &gt;::Config::rethrow_task_errors;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/naive_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1NaiveWorkDistributor_1_1RunConfig/","title":"Struct dynampi::NaiveWorkDistributor::RunConfig","text":"<p>ClassList &gt; dynampi &gt; NaiveWorkDistributor &gt; RunConfig</p> <ul> <li><code>#include &lt;naive_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1NaiveWorkDistributor_1_1RunConfig/#public-attributes","title":"Public Attributes","text":"Type Name bool allow_more_than_target_tasks   = <code>[**true**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code> std::optional&lt; double &gt; max_seconds   = <code>std::nullopt</code> size_t target_num_tasks   = <code>std::numeric\\_limits&amp;lt;[**size\\_t**](namespacedynampi.md#function-check_fixed_size_mpi_type)&amp;gt;[**::max**](namespacedynampi.md#function-check_fixed_size_mpi_type)()</code>"},{"location":"dynampi/structdynampi_1_1NaiveWorkDistributor_1_1RunConfig/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1NaiveWorkDistributor_1_1RunConfig/#variable-allow_more_than_target_tasks","title":"variable allow_more_than_target_tasks","text":"<pre><code>bool dynampi::NaiveWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::allow_more_than_target_tasks;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1NaiveWorkDistributor_1_1RunConfig/#variable-max_seconds","title":"variable max_seconds","text":"<pre><code>std::optional&lt;double&gt; dynampi::NaiveWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::max_seconds;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1NaiveWorkDistributor_1_1RunConfig/#variable-target_num_tasks","title":"variable target_num_tasks","text":"<pre><code>size_t dynampi::NaiveWorkDistributor&lt; TaskT, ResultT, Options &gt;::RunConfig::target_num_tasks;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/naive_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1NaiveWorkDistributor_1_1Statistics/","title":"Struct dynampi::NaiveWorkDistributor::Statistics","text":"<p>ClassList &gt; dynampi &gt; NaiveWorkDistributor &gt; Statistics</p> <ul> <li><code>#include &lt;naive_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1NaiveWorkDistributor_1_1Statistics/#public-attributes","title":"Public Attributes","text":"Type Name const CommStatistics &amp; comm_statistics std::vector&lt; size_t &gt; worker_task_counts"},{"location":"dynampi/structdynampi_1_1NaiveWorkDistributor_1_1Statistics/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1NaiveWorkDistributor_1_1Statistics/#variable-comm_statistics","title":"variable comm_statistics","text":"<pre><code>const CommStatistics&amp; dynampi::NaiveWorkDistributor&lt; TaskT, ResultT, Options &gt;::Statistics::comm_statistics;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1NaiveWorkDistributor_1_1Statistics/#variable-worker_task_counts","title":"variable worker_task_counts","text":"<pre><code>std::vector&lt;size_t&gt; dynampi::NaiveWorkDistributor&lt; TaskT, ResultT, Options &gt;::Statistics::worker_task_counts;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/naive_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1TaskError/","title":"Struct dynampi::TaskError","text":"<p>ClassList &gt; dynampi &gt; TaskError</p> <ul> <li><code>#include &lt;task_error.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1TaskError/#public-attributes","title":"Public Attributes","text":"Type Name std::string message int worker_rank   = <code>-1</code>"},{"location":"dynampi/structdynampi_1_1TaskError/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1TaskError/#variable-message","title":"variable message","text":"<pre><code>std::string dynampi::TaskError::message;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1TaskError/#variable-worker_rank","title":"variable worker_rank","text":"<pre><code>int dynampi::TaskError::worker_rank;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/task_error.hpp</code></p>"},{"location":"dynampi/classdynampi_1_1TaskFailure/","title":"Class dynampi::TaskFailure","text":"<p>ClassList &gt; dynampi &gt; TaskFailure</p> <ul> <li><code>#include &lt;task_error.hpp&gt;</code></li> </ul> <p>Inherits the following classes: std::runtime_error</p>"},{"location":"dynampi/classdynampi_1_1TaskFailure/#public-functions","title":"Public Functions","text":"Type Name TaskFailure (TaskError error)  const TaskError &amp; error () noexcept const"},{"location":"dynampi/classdynampi_1_1TaskFailure/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/classdynampi_1_1TaskFailure/#function-taskfailure","title":"function TaskFailure","text":"<pre><code>inline explicit dynampi::TaskFailure::TaskFailure (\n    TaskError error\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1TaskFailure/#function-error","title":"function error","text":"<pre><code>inline const  TaskError &amp; dynampi::TaskFailure::error () noexcept const\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/task_error.hpp</code></p>"},{"location":"dynampi/classdynampi_1_1Timer/","title":"Class dynampi::Timer","text":"<p>ClassList &gt; dynampi &gt; Timer</p> <ul> <li><code>#include &lt;timer.hpp&gt;</code></li> </ul>"},{"location":"dynampi/classdynampi_1_1Timer/#public-types","title":"Public Types","text":"Type Name enum AutoStart"},{"location":"dynampi/classdynampi_1_1Timer/#public-functions","title":"Public Functions","text":"Type Name Timer (AutoStart auto_start=AutoStart::Yes)  std::chrono::duration&lt; double &gt; elapsed () const void reset (AutoStart auto_start=AutoStart::Yes)  void start ()  std::chrono::duration&lt; double &gt; stop ()"},{"location":"dynampi/classdynampi_1_1Timer/#public-types-documentation","title":"Public Types Documentation","text":""},{"location":"dynampi/classdynampi_1_1Timer/#enum-autostart","title":"enum AutoStart","text":"<pre><code>enum dynampi::Timer::AutoStart {\n    Yes,\n    No\n};\n</code></pre>"},{"location":"dynampi/classdynampi_1_1Timer/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/classdynampi_1_1Timer/#function-timer","title":"function Timer","text":"<pre><code>inline explicit dynampi::Timer::Timer (\n    AutoStart auto_start=AutoStart::Yes\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1Timer/#function-elapsed","title":"function elapsed","text":"<pre><code>inline std::chrono::duration&lt; double &gt; dynampi::Timer::elapsed () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1Timer/#function-reset","title":"function reset","text":"<pre><code>inline void dynampi::Timer::reset (\n    AutoStart auto_start=AutoStart::Yes\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1Timer/#function-start","title":"function start","text":"<pre><code>inline void dynampi::Timer::start () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1Timer/#function-stop","title":"function stop","text":"<pre><code>inline std::chrono::duration&lt; double &gt; dynampi::Timer::stop () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1Timer/#friends-documentation","title":"Friends Documentation","text":""},{"location":"dynampi/classdynampi_1_1Timer/#friend-operator","title":"friend operator&lt;&lt;","text":"<pre><code>inline std::ostream &amp; dynampi::Timer::operator&lt;&lt; (\n    std::ostream &amp; os,\n    const  Timer &amp; timer\n) \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/utilities/timer.hpp</code></p>"},{"location":"dynampi/namespacedynampi_1_1detail/","title":"Namespace dynampi::detail","text":"<p>Namespace List &gt; dynampi &gt; detail</p>"},{"location":"dynampi/namespacedynampi_1_1detail/#classes","title":"Classes","text":"Type Name class LockFreeRMALevel &lt;typename TaskT, typename ResultT&gt;"},{"location":"dynampi/namespacedynampi_1_1detail/#public-functions","title":"Public Functions","text":"Type Name void backoff_sleep (std::chrono::microseconds duration)  void check_task_capacity (int64_t start, size_t count, int max_tasks, const char * distributor_name)  void check_variable_batch_read (size_t offset, uint64_t nbytes, size_t buf_size, const char * what)  int mpi_type_size_bytes ()  std::vector&lt; std::byte &gt; pack_variable_batch (const std::vector&lt; T &gt; &amp; items)  void read_bytes (void * dst, size_t dst_capacity, const std::byte * buffer, size_t buffer_size, size_t offset, size_t nbytes)  int64_t read_i64 (const std::byte * buffer, size_t buffer_size, size_t offset)  void read_result_bytes (const std::byte * buffer, size_t buffer_size, size_t offset, T &amp; value, size_t data_bytes)  int resolve_upper_fanout (int manager_count, int configured_fanout)  void rma_wait_idle (MPI_Win, MPI_Comm comm)  constexpr size_t round_up_8 (size_t bytes)  std::optional&lt; Communicator &gt; split_local_worker_communicator (const Communicator &amp; world_comm, int manager_rank, int max_local_group_size)  int sum_subtree_widths_to_group_leader (int local_width, const Communicator &amp; group_comm)  std::vector&lt; T &gt; unpack_variable_batch (const std::vector&lt; std::byte &gt; &amp; buf)  void write_bytes (std::byte * buffer, size_t buffer_size, size_t offset, const void * src, size_t nbytes)  void write_i64 (std::byte * buffer, size_t buffer_size, size_t offset, int64_t value)"},{"location":"dynampi/namespacedynampi_1_1detail/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/namespacedynampi_1_1detail/#function-backoff_sleep","title":"function backoff_sleep","text":"<pre><code>inline void dynampi::detail::backoff_sleep (\n    std::chrono::microseconds duration\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1detail/#function-check_task_capacity","title":"function check_task_capacity","text":"<pre><code>inline void dynampi::detail::check_task_capacity (\n    int64_t start,\n    size_t count,\n    int max_tasks,\n    const  char * distributor_name\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1detail/#function-check_variable_batch_read","title":"function check_variable_batch_read","text":"<pre><code>inline void dynampi::detail::check_variable_batch_read (\n    size_t offset,\n    uint64_t nbytes,\n    size_t buf_size,\n    const  char * what\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1detail/#function-mpi_type_size_bytes","title":"function mpi_type_size_bytes","text":"<pre><code>template&lt;typename T&gt;\ninline int dynampi::detail::mpi_type_size_bytes () \n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1detail/#function-pack_variable_batch","title":"function pack_variable_batch","text":"<pre><code>template&lt;typename T&gt;\nstd::vector&lt; std::byte &gt; dynampi::detail::pack_variable_batch (\n    const std::vector&lt; T &gt; &amp; items\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1detail/#function-read_bytes","title":"function read_bytes","text":"<pre><code>inline void dynampi::detail::read_bytes (\n    void * dst,\n    size_t dst_capacity,\n    const std::byte * buffer,\n    size_t buffer_size,\n    size_t offset,\n    size_t nbytes\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1detail/#function-read_i64","title":"function read_i64","text":"<pre><code>inline int64_t dynampi::detail::read_i64 (\n    const std::byte * buffer,\n    size_t buffer_size,\n    size_t offset\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1detail/#function-read_result_bytes","title":"function read_result_bytes","text":"<pre><code>template&lt;typename T&gt;\ninline void dynampi::detail::read_result_bytes (\n    const std::byte * buffer,\n    size_t buffer_size,\n    size_t offset,\n    T &amp; value,\n    size_t data_bytes\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1detail/#function-resolve_upper_fanout","title":"function resolve_upper_fanout","text":"<pre><code>inline int dynampi::detail::resolve_upper_fanout (\n    int manager_count,\n    int configured_fanout\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1detail/#function-rma_wait_idle","title":"function rma_wait_idle","text":"<pre><code>inline void dynampi::detail::rma_wait_idle (\n    MPI_Win,\n    MPI_Comm comm\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1detail/#function-round_up_8","title":"function round_up_8","text":"<pre><code>inline constexpr  size_t dynampi::detail::round_up_8 (\n    size_t bytes\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1detail/#function-split_local_worker_communicator","title":"function split_local_worker_communicator","text":"<pre><code>template&lt;typename Communicator&gt;\nstd::optional&lt; Communicator &gt; dynampi::detail::split_local_worker_communicator (\n    const  Communicator &amp; world_comm,\n    int manager_rank,\n    int max_local_group_size\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1detail/#function-sum_subtree_widths_to_group_leader","title":"function sum_subtree_widths_to_group_leader","text":"<pre><code>template&lt;typename Communicator&gt;\nint dynampi::detail::sum_subtree_widths_to_group_leader (\n    int local_width,\n    const  Communicator &amp; group_comm\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1detail/#function-unpack_variable_batch","title":"function unpack_variable_batch","text":"<pre><code>template&lt;typename T&gt;\nstd::vector&lt; T &gt; dynampi::detail::unpack_variable_batch (\n    const std::vector&lt; std::byte &gt; &amp; buf\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1detail/#function-write_bytes","title":"function write_bytes","text":"<pre><code>inline void dynampi::detail::write_bytes (\n    std::byte * buffer,\n    size_t buffer_size,\n    size_t offset,\n    const  void * src,\n    size_t nbytes\n) \n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1detail/#function-write_i64","title":"function write_i64","text":"<pre><code>inline void dynampi::detail::write_i64 (\n    std::byte * buffer,\n    size_t buffer_size,\n    size_t offset,\n    int64_t value\n) \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_lockfree_rma_distributor.hpp</code></p>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeRMALevel/","title":"Class dynampi::detail::LockFreeRMALevel","text":"<p>template &lt;typename TaskT, typename ResultT&gt;</p> <p>ClassList &gt; dynampi &gt; detail &gt; LockFreeRMALevel</p> <ul> <li><code>#include &lt;hierarchical_lockfree_rma_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeRMALevel/#classes","title":"Classes","text":"Type Name struct ClaimedRange struct Config"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeRMALevel/#public-functions","title":"Public Functions","text":"Type Name LockFreeRMALevel (Config config, int claim_width=1)  LockFreeRMALevel (const LockFreeRMALevel &amp;) = delete bool check_finished ()  int claim_width () const int comm_size () const bool drained ()  std::vector&lt; ResultT &gt; harvest_ready_results ()  std::vector&lt; ResultT &gt; harvest_ready_results_throttled ()  void idle_wait ()  bool is_owner () const void mark_finished ()  LockFreeRMALevel &amp; operator= (const LockFreeRMALevel &amp;) = delete int64_t owner_available_capacity () const size_t owner_collected_count () const size_t owner_published_count () const int64_t owner_unordered_completed_estimate () const bool publish_tasks (const std::vector&lt; TaskT &gt; &amp; tasks)  void report_task_error (const TaskError &amp; error)  std::vector&lt; TaskError &gt; take_errors ()  ClaimedRange try_claim ()  void write_result_range (int64_t start, const std::vector&lt; ResultT &gt; &amp; results, bool contains_error=false)  ~LockFreeRMALevel ()"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeRMALevel/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeRMALevel/#function-lockfreermalevel-12","title":"function LockFreeRMALevel [1/2]","text":"<pre><code>inline explicit dynampi::detail::LockFreeRMALevel::LockFreeRMALevel (\n    Config config,\n    int claim_width=1\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeRMALevel/#function-lockfreermalevel-22","title":"function LockFreeRMALevel [2/2]","text":"<pre><code>dynampi::detail::LockFreeRMALevel::LockFreeRMALevel (\n    const  LockFreeRMALevel &amp;\n) = delete\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeRMALevel/#function-check_finished","title":"function check_finished","text":"<pre><code>inline bool dynampi::detail::LockFreeRMALevel::check_finished () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeRMALevel/#function-claim_width","title":"function claim_width","text":"<pre><code>inline int dynampi::detail::LockFreeRMALevel::claim_width () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeRMALevel/#function-comm_size","title":"function comm_size","text":"<pre><code>inline int dynampi::detail::LockFreeRMALevel::comm_size () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeRMALevel/#function-drained","title":"function drained","text":"<pre><code>inline bool dynampi::detail::LockFreeRMALevel::drained () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeRMALevel/#function-harvest_ready_results","title":"function harvest_ready_results","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::detail::LockFreeRMALevel::harvest_ready_results () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeRMALevel/#function-harvest_ready_results_throttled","title":"function harvest_ready_results_throttled","text":"<pre><code>inline std::vector&lt; ResultT &gt; dynampi::detail::LockFreeRMALevel::harvest_ready_results_throttled () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeRMALevel/#function-idle_wait","title":"function idle_wait","text":"<pre><code>inline void dynampi::detail::LockFreeRMALevel::idle_wait () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeRMALevel/#function-is_owner","title":"function is_owner","text":"<pre><code>inline bool dynampi::detail::LockFreeRMALevel::is_owner () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeRMALevel/#function-mark_finished","title":"function mark_finished","text":"<pre><code>inline void dynampi::detail::LockFreeRMALevel::mark_finished () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeRMALevel/#function-operator","title":"function operator=","text":"<pre><code>LockFreeRMALevel &amp; dynampi::detail::LockFreeRMALevel::operator= (\n    const  LockFreeRMALevel &amp;\n) = delete\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeRMALevel/#function-owner_available_capacity","title":"function owner_available_capacity","text":"<pre><code>inline int64_t dynampi::detail::LockFreeRMALevel::owner_available_capacity () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeRMALevel/#function-owner_collected_count","title":"function owner_collected_count","text":"<pre><code>inline size_t dynampi::detail::LockFreeRMALevel::owner_collected_count () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeRMALevel/#function-owner_published_count","title":"function owner_published_count","text":"<pre><code>inline size_t dynampi::detail::LockFreeRMALevel::owner_published_count () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeRMALevel/#function-owner_unordered_completed_estimate","title":"function owner_unordered_completed_estimate","text":"<pre><code>inline int64_t dynampi::detail::LockFreeRMALevel::owner_unordered_completed_estimate () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeRMALevel/#function-publish_tasks","title":"function publish_tasks","text":"<pre><code>inline bool dynampi::detail::LockFreeRMALevel::publish_tasks (\n    const std::vector&lt; TaskT &gt; &amp; tasks\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeRMALevel/#function-report_task_error","title":"function report_task_error","text":"<pre><code>inline void dynampi::detail::LockFreeRMALevel::report_task_error (\n    const  TaskError &amp; error\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeRMALevel/#function-take_errors","title":"function take_errors","text":"<pre><code>inline std::vector&lt; TaskError &gt; dynampi::detail::LockFreeRMALevel::take_errors () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeRMALevel/#function-try_claim","title":"function try_claim","text":"<pre><code>inline ClaimedRange dynampi::detail::LockFreeRMALevel::try_claim () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeRMALevel/#function-write_result_range","title":"function write_result_range","text":"<pre><code>inline void dynampi::detail::LockFreeRMALevel::write_result_range (\n    int64_t start,\n    const std::vector&lt; ResultT &gt; &amp; results,\n    bool contains_error=false\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1LockFreeRMALevel/#function-lockfreermalevel","title":"function ~LockFreeRMALevel","text":"<pre><code>inline dynampi::detail::LockFreeRMALevel::~LockFreeRMALevel () \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_lockfree_rma_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1detail_1_1LockFreeRMALevel_1_1ClaimedRange/","title":"Struct dynampi::detail::LockFreeRMALevel::ClaimedRange","text":"<p>ClassList &gt; dynampi &gt; detail &gt; LockFreeRMALevel &gt; ClaimedRange</p> <ul> <li><code>#include &lt;hierarchical_lockfree_rma_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1detail_1_1LockFreeRMALevel_1_1ClaimedRange/#public-attributes","title":"Public Attributes","text":"Type Name int64_t start   = <code>-1</code> std::vector&lt; TaskT &gt; tasks"},{"location":"dynampi/structdynampi_1_1detail_1_1LockFreeRMALevel_1_1ClaimedRange/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1detail_1_1LockFreeRMALevel_1_1ClaimedRange/#variable-start","title":"variable start","text":"<pre><code>int64_t dynampi::detail::LockFreeRMALevel&lt; TaskT, ResultT &gt;::ClaimedRange::start;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1detail_1_1LockFreeRMALevel_1_1ClaimedRange/#variable-tasks","title":"variable tasks","text":"<pre><code>std::vector&lt;TaskT&gt; dynampi::detail::LockFreeRMALevel&lt; TaskT, ResultT &gt;::ClaimedRange::tasks;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_lockfree_rma_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1detail_1_1LockFreeRMALevel_1_1Config/","title":"Struct dynampi::detail::LockFreeRMALevel::Config","text":"<p>ClassList &gt; dynampi &gt; detail &gt; LockFreeRMALevel &gt; Config</p> <ul> <li><code>#include &lt;hierarchical_lockfree_rma_distributor.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structdynampi_1_1detail_1_1LockFreeRMALevel_1_1Config/#public-attributes","title":"Public Attributes","text":"Type Name MPI_Comm comm   = <code>[**MPI\\_COMM\\_NULL**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code> int max_result_count   = <code>256</code> int max_task_count   = <code>256</code> int max_tasks   = <code>8192</code> int owner_rank   = <code>0</code>"},{"location":"dynampi/structdynampi_1_1detail_1_1LockFreeRMALevel_1_1Config/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1detail_1_1LockFreeRMALevel_1_1Config/#variable-comm","title":"variable comm","text":"<pre><code>MPI_Comm dynampi::detail::LockFreeRMALevel&lt; TaskT, ResultT &gt;::Config::comm;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1detail_1_1LockFreeRMALevel_1_1Config/#variable-max_result_count","title":"variable max_result_count","text":"<pre><code>int dynampi::detail::LockFreeRMALevel&lt; TaskT, ResultT &gt;::Config::max_result_count;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1detail_1_1LockFreeRMALevel_1_1Config/#variable-max_task_count","title":"variable max_task_count","text":"<pre><code>int dynampi::detail::LockFreeRMALevel&lt; TaskT, ResultT &gt;::Config::max_task_count;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1detail_1_1LockFreeRMALevel_1_1Config/#variable-max_tasks","title":"variable max_tasks","text":"<pre><code>int dynampi::detail::LockFreeRMALevel&lt; TaskT, ResultT &gt;::Config::max_tasks;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1detail_1_1LockFreeRMALevel_1_1Config/#variable-owner_rank","title":"variable owner_rank","text":"<pre><code>int dynampi::detail::LockFreeRMALevel&lt; TaskT, ResultT &gt;::Config::owner_rank;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_lockfree_rma_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1enable__prioritization/","title":"Struct dynampi::enable_prioritization","text":"<p>ClassList &gt; dynampi &gt; enable_prioritization</p> <ul> <li><code>#include &lt;base_distributor.hpp&gt;</code></li> </ul> <p>Inherits the following classes: dynampi::prioritize_tasks_t</p>"},{"location":"dynampi/structdynampi_1_1enable__prioritization/#public-static-attributes","title":"Public Static Attributes","text":"Type Name constexpr bool value   = <code>[**true**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code>"},{"location":"dynampi/structdynampi_1_1enable__prioritization/#public-static-attributes-inherited-from-dynampiprioritize_tasks_t","title":"Public Static Attributes inherited from dynampi::prioritize_tasks_t","text":"<p>See dynampi::prioritize_tasks_t</p> Type Name constexpr bool value   = <code>[**false**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code>"},{"location":"dynampi/structdynampi_1_1enable__prioritization/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1enable__prioritization/#variable-value","title":"variable value","text":"<pre><code>constexpr bool dynampi::enable_prioritization::value;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/base_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1has__dynampi__mpi__type/","title":"Struct dynampi::has_dynampi_mpi_type","text":"<p>template &lt;typename, typename&gt;</p> <p>ClassList &gt; dynampi &gt; has_dynampi_mpi_type</p> <ul> <li><code>#include &lt;mpi_types.hpp&gt;</code></li> </ul> <p>Inherits the following classes: std::false_type</p> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_types.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1has__dynampi__mpi__type_3_01U_00_01std_1_1void__t_3_01decltype_07MPI__Type_3_01U_01_4_1_1value_08_4_01_4/","title":"Struct dynampi::has_dynampi_mpi_type&lt; U, std::void_t&lt; decltype(MPI_Type&lt; U &gt;::value)&gt; &gt;","text":"<p>template &lt;typename U&gt;</p> <p>ClassList &gt; dynampi &gt; has_dynampi_mpi_type&lt; U, std::void_t&lt; decltype(MPI_Type&lt; U &gt;::value)&gt; &gt;</p> <ul> <li><code>#include &lt;mpi_types.hpp&gt;</code></li> </ul> <p>Inherits the following classes: std::true_type</p> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_types.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1prioritize__tasks__t/","title":"Struct dynampi::prioritize_tasks_t","text":"<p>ClassList &gt; dynampi &gt; prioritize_tasks_t</p> <ul> <li><code>#include &lt;base_distributor.hpp&gt;</code></li> </ul> <p>Inherited by the following classes: dynampi::enable_prioritization</p>"},{"location":"dynampi/structdynampi_1_1prioritize__tasks__t/#public-static-attributes","title":"Public Static Attributes","text":"Type Name constexpr bool value   = <code>[**false**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code>"},{"location":"dynampi/structdynampi_1_1prioritize__tasks__t/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1prioritize__tasks__t/#variable-value","title":"variable value","text":"<pre><code>constexpr bool dynampi::prioritize_tasks_t::value;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/base_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1track__statistics/","title":"Struct dynampi::track_statistics","text":"<p>template &lt;StatisticsMode Mode&gt;</p> <p>ClassList &gt; dynampi &gt; track_statistics</p> <ul> <li><code>#include &lt;mpi_communicator.hpp&gt;</code></li> </ul> <p>Inherits the following classes: dynampi::track_statistics_t</p>"},{"location":"dynampi/structdynampi_1_1track__statistics/#public-static-attributes","title":"Public Static Attributes","text":"Type Name constexpr StatisticsMode value   = <code>[**Mode**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code>"},{"location":"dynampi/structdynampi_1_1track__statistics/#public-static-attributes-inherited-from-dynampitrack_statistics_t","title":"Public Static Attributes inherited from dynampi::track_statistics_t","text":"<p>See dynampi::track_statistics_t</p> Type Name constexpr StatisticsMode value   = <code>StatisticsMode::None</code>"},{"location":"dynampi/structdynampi_1_1track__statistics/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1track__statistics/#variable-value","title":"variable value","text":"<pre><code>constexpr StatisticsMode dynampi::track_statistics&lt; Mode &gt;::value;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_communicator.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1track__statistics__t/","title":"Struct dynampi::track_statistics_t","text":"<p>ClassList &gt; dynampi &gt; track_statistics_t</p> <ul> <li><code>#include &lt;mpi_communicator.hpp&gt;</code></li> </ul> <p>Inherited by the following classes: dynampi::track_statistics</p>"},{"location":"dynampi/structdynampi_1_1track__statistics__t/#public-static-attributes","title":"Public Static Attributes","text":"Type Name constexpr StatisticsMode value   = <code>StatisticsMode::None</code>"},{"location":"dynampi/structdynampi_1_1track__statistics__t/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1track__statistics__t/#variable-value","title":"variable value","text":"<pre><code>constexpr StatisticsMode dynampi::track_statistics_t::value;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_communicator.hpp</code></p>"},{"location":"dynampi/namespacedynampi_1_1version/","title":"Namespace dynampi::version","text":"<p>Namespace List &gt; dynampi &gt; version</p>"},{"location":"dynampi/namespacedynampi_1_1version/#public-attributes","title":"Public Attributes","text":"Type Name constexpr int major   = <code>[**DYNAMPI\\_VERSION\\_MAJOR**](version_8hpp.md#define-dynampi_version_major)</code> constexpr int minor   = <code>[**DYNAMPI\\_VERSION\\_MINOR**](version_8hpp.md#define-dynampi_version_minor)</code> constexpr int patch   = <code>[**DYNAMPI\\_VERSION\\_PATCH**](version_8hpp.md#define-dynampi_version_patch)</code> constexpr std::string_view string   = <code>[**DYNAMPI\\_VERSION\\_STRING**](version_8hpp.md#define-dynampi_version_string)</code>"},{"location":"dynampi/namespacedynampi_1_1version/#public-functions","title":"Public Functions","text":"Type Name constexpr std::string_view commit_hash ()  constexpr std::string_view compile_date ()  constexpr bool is_at_least (int v_major, int v_minor, int v_patch)"},{"location":"dynampi/namespacedynampi_1_1version/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/namespacedynampi_1_1version/#variable-major","title":"variable major","text":"<pre><code>constexpr int dynampi::version::major;\n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1version/#variable-minor","title":"variable minor","text":"<pre><code>constexpr int dynampi::version::minor;\n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1version/#variable-patch","title":"variable patch","text":"<pre><code>constexpr int dynampi::version::patch;\n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1version/#variable-string","title":"variable string","text":"<pre><code>constexpr std::string_view dynampi::version::string;\n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1version/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/namespacedynampi_1_1version/#function-commit_hash","title":"function commit_hash","text":"<pre><code>inline constexpr std::string_view dynampi::version::commit_hash () \n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1version/#function-compile_date","title":"function compile_date","text":"<pre><code>inline constexpr std::string_view dynampi::version::compile_date () \n</code></pre>"},{"location":"dynampi/namespacedynampi_1_1version/#function-is_at_least","title":"function is_at_least","text":"<pre><code>constexpr  bool dynampi::version::is_at_least (\n    int v_major,\n    int v_minor,\n    int v_patch\n) \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/version.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1BridgeHop/","title":"Struct dynampi::HierarchicalLockFreeRMAWorkDistributor::BridgeHop","text":"<p>ClassList &gt; BridgeHop</p>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1BridgeHop/#public-attributes","title":"Public Attributes","text":"Type Name detail::LockFreeRMALevel&lt; TaskT, ResultT &gt; * child bool finish_marked   = <code>[**false**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code> int layers   = <code>1</code> detail::LockFreeRMALevel&lt; TaskT, ResultT &gt; * parent std::deque&lt; PendingRelay &gt; pending_relays int64_t pending_task_count   = <code>0</code> std::vector&lt; ResultT &gt; relay_buffer bool relay_error_pending   = <code>[**false**](namespacedynampi.md#function-check_fixed_size_mpi_type)</code> int64_t total_claimed   = <code>0</code>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1BridgeHop/#public-functions","title":"Public Functions","text":"Type Name BridgeHop (detail::LockFreeRMALevel&lt; TaskT, ResultT &gt; * p, detail::LockFreeRMALevel&lt; TaskT, ResultT &gt; * c)"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1BridgeHop/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1BridgeHop/#variable-child","title":"variable child","text":"<pre><code>detail::LockFreeRMALevel&lt;TaskT, ResultT&gt;* dynampi::HierarchicalLockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::BridgeHop::child;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1BridgeHop/#variable-finish_marked","title":"variable finish_marked","text":"<pre><code>bool dynampi::HierarchicalLockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::BridgeHop::finish_marked;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1BridgeHop/#variable-layers","title":"variable layers","text":"<pre><code>int dynampi::HierarchicalLockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::BridgeHop::layers;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1BridgeHop/#variable-parent","title":"variable parent","text":"<pre><code>detail::LockFreeRMALevel&lt;TaskT, ResultT&gt;* dynampi::HierarchicalLockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::BridgeHop::parent;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1BridgeHop/#variable-pending_relays","title":"variable pending_relays","text":"<pre><code>std::deque&lt;PendingRelay&gt; dynampi::HierarchicalLockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::BridgeHop::pending_relays;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1BridgeHop/#variable-pending_task_count","title":"variable pending_task_count","text":"<pre><code>int64_t dynampi::HierarchicalLockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::BridgeHop::pending_task_count;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1BridgeHop/#variable-relay_buffer","title":"variable relay_buffer","text":"<pre><code>std::vector&lt;ResultT&gt; dynampi::HierarchicalLockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::BridgeHop::relay_buffer;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1BridgeHop/#variable-relay_error_pending","title":"variable relay_error_pending","text":"<pre><code>bool dynampi::HierarchicalLockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::BridgeHop::relay_error_pending;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1BridgeHop/#variable-total_claimed","title":"variable total_claimed","text":"<pre><code>int64_t dynampi::HierarchicalLockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::BridgeHop::total_claimed;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1BridgeHop/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1BridgeHop/#function-bridgehop","title":"function BridgeHop","text":"<pre><code>inline BridgeHop::BridgeHop (\n    detail::LockFreeRMALevel &lt; TaskT , ResultT &gt; * p,\n    detail::LockFreeRMALevel &lt; TaskT , ResultT &gt; * c\n) \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_lockfree_rma_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1PendingRelay/","title":"Struct dynampi::HierarchicalLockFreeRMAWorkDistributor::PendingRelay","text":"<p>ClassList &gt; PendingRelay</p>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1PendingRelay/#public-attributes","title":"Public Attributes","text":"Type Name int64_t child_len int64_t parent_start"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1PendingRelay/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1PendingRelay/#variable-child_len","title":"variable child_len","text":"<pre><code>int64_t dynampi::HierarchicalLockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::PendingRelay::child_len;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalLockFreeRMAWorkDistributor_1_1PendingRelay/#variable-parent_start","title":"variable parent_start","text":"<pre><code>int64_t dynampi::HierarchicalLockFreeRMAWorkDistributor&lt; TaskT, ResultT, Options &gt;::PendingRelay::parent_start;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_lockfree_rma_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1Statistics/","title":"Struct dynampi::HierarchicalWorkDistributor::Statistics","text":"<p>ClassList &gt; Statistics</p>"},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1Statistics/#public-attributes","title":"Public Attributes","text":"Type Name const CommStatistics &amp; comm_statistics std::optional&lt; std::vector&lt; size_t &gt; &gt; worker_task_counts   = <code>{}</code>"},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1Statistics/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1Statistics/#variable-comm_statistics","title":"variable comm_statistics","text":"<pre><code>const CommStatistics&amp; dynampi::HierarchicalWorkDistributor&lt; TaskT, ResultT, Options &gt;::Statistics::comm_statistics;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1Statistics/#variable-worker_task_counts","title":"variable worker_task_counts","text":"<pre><code>std::optional&lt;std::vector&lt;size_t&gt; &gt; dynampi::HierarchicalWorkDistributor&lt; TaskT, ResultT, Options &gt;::Statistics::worker_task_counts;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_distributor.hpp</code></p>"},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1TaskRequest/","title":"Struct dynampi::HierarchicalWorkDistributor::TaskRequest","text":"<p>ClassList &gt; TaskRequest</p>"},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1TaskRequest/#public-attributes","title":"Public Attributes","text":"Type Name std::optional&lt; int &gt; num_tasks_requested   = <code>std::nullopt</code> int worker_rank"},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1TaskRequest/#public-attributes-documentation","title":"Public Attributes Documentation","text":""},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1TaskRequest/#variable-num_tasks_requested","title":"variable num_tasks_requested","text":"<pre><code>std::optional&lt;int&gt; dynampi::HierarchicalWorkDistributor&lt; TaskT, ResultT, Options &gt;::TaskRequest::num_tasks_requested;\n</code></pre>"},{"location":"dynampi/structdynampi_1_1HierarchicalWorkDistributor_1_1TaskRequest/#variable-worker_rank","title":"variable worker_rank","text":"<pre><code>int dynampi::HierarchicalWorkDistributor&lt; TaskT, ResultT, Options &gt;::TaskRequest::worker_rank;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_distributor.hpp</code></p>"},{"location":"dynampi/classdynampi_1_1detail_1_1TaskErrorLog/","title":"Class dynampi::detail::TaskErrorLog","text":"<p>ClassList &gt; TaskErrorLog</p>"},{"location":"dynampi/classdynampi_1_1detail_1_1TaskErrorLog/#public-functions","title":"Public Functions","text":"Type Name bool empty () const void record (TaskError error)  void rethrow_first_if (bool enabled)  size_t size () const std::vector&lt; TaskError &gt; take ()  void warn_if_unreported (const char * distributor) noexcept const"},{"location":"dynampi/classdynampi_1_1detail_1_1TaskErrorLog/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/classdynampi_1_1detail_1_1TaskErrorLog/#function-empty","title":"function empty","text":"<pre><code>inline bool TaskErrorLog::empty () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1TaskErrorLog/#function-record","title":"function record","text":"<pre><code>inline void TaskErrorLog::record (\n    TaskError error\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1TaskErrorLog/#function-rethrow_first_if","title":"function rethrow_first_if","text":"<pre><code>inline void TaskErrorLog::rethrow_first_if (\n    bool enabled\n) \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1TaskErrorLog/#function-size","title":"function size","text":"<pre><code>inline size_t TaskErrorLog::size () const\n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1TaskErrorLog/#function-take","title":"function take","text":"<pre><code>inline std::vector&lt; TaskError &gt; TaskErrorLog::take () \n</code></pre>"},{"location":"dynampi/classdynampi_1_1detail_1_1TaskErrorLog/#function-warn_if_unreported","title":"function warn_if_unreported","text":"<pre><code>inline void TaskErrorLog::warn_if_unreported (\n    const  char * distributor\n) noexcept const\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/task_error.hpp</code></p>"},{"location":"dynampi/structoption__value/","title":"Struct option_value","text":"<p>template &lt;typename Option, typename... Options&gt;</p> <p>ClassList &gt; option_value</p> <ul> <li><code>#include &lt;template_options.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structoption__value/#public-static-attributes","title":"Public Static Attributes","text":"Type Name constexpr decltype(Option::value) value   = <code>Option::value</code>"},{"location":"dynampi/structoption__value/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/structoption__value/#variable-value","title":"variable value","text":"<pre><code>constexpr decltype(Option::value) option_value&lt; Option, Options &gt;::value;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/utilities/template_options.hpp</code></p>"},{"location":"dynampi/structoption__value_3_01Option_00_01Head_00_01Tail_8_8_8_01_4/","title":"Struct option_value&lt; Option, Head, Tail... &gt;","text":"<p>template &lt;typename Option, typename Head, typename... Tail&gt;</p> <p>ClassList &gt; option_value&lt; Option, Head, Tail... &gt;</p> <ul> <li><code>#include &lt;template_options.hpp&gt;</code></li> </ul>"},{"location":"dynampi/structoption__value_3_01Option_00_01Head_00_01Tail_8_8_8_01_4/#public-static-attributes","title":"Public Static Attributes","text":"Type Name constexpr decltype(Option::value) value   = <code>/* multi line expression */</code>"},{"location":"dynampi/structoption__value_3_01Option_00_01Head_00_01Tail_8_8_8_01_4/#public-static-attributes-documentation","title":"Public Static Attributes Documentation","text":""},{"location":"dynampi/structoption__value_3_01Option_00_01Head_00_01Tail_8_8_8_01_4/#variable-value","title":"variable value","text":"<pre><code>constexpr decltype(Option::value) option_value&lt; Option, Head, Tail... &gt;::value;\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/utilities/template_options.hpp</code></p>"},{"location":"dynampi/namespacestd/","title":"Namespace std","text":"<p>Namespace List &gt; std</p> <p>The documentation for this class was generated from the following file <code>[generated]</code></p>"},{"location":"dynampi/dir_d44c64559bbebec7f509842c48db8b23/","title":"Dir /home/runner/work/DynaMPI/DynaMPI/include","text":"<p>FileList &gt; include</p>"},{"location":"dynampi/dir_d44c64559bbebec7f509842c48db8b23/#directories","title":"Directories","text":"Type Name dir dynampi <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/</code></p>"},{"location":"dynampi/dir_f8db417ebd5c3d89eea80c24e3fd4369/","title":"Dir /home/runner/work/DynaMPI/DynaMPI/include/dynampi","text":"<p>FileList &gt; dynampi</p>"},{"location":"dynampi/dir_f8db417ebd5c3d89eea80c24e3fd4369/#files","title":"Files","text":"Type Name file dynampi.hpp file task_error.hpp file version.hpp"},{"location":"dynampi/dir_f8db417ebd5c3d89eea80c24e3fd4369/#directories","title":"Directories","text":"Type Name dir impl dir mpi dir utilities <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/</code></p>"},{"location":"dynampi/dir_87365955bfd9c1869b83a1fbd3fdb221/","title":"Dir /home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl","text":"<p>FileList &gt; dynampi &gt; impl</p>"},{"location":"dynampi/dir_87365955bfd9c1869b83a1fbd3fdb221/#files","title":"Files","text":"Type Name file base_distributor.hpp file hierarchical_distributor.hpp file hierarchical_lockfree_rma_distributor.hpp file hierarchical_topology_detail.hpp file lockfree_rma_distributor.hpp file naive_distributor.hpp file rma_detail.hpp file variable_batch.hpp <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/</code></p>"},{"location":"dynampi/base__distributor_8hpp/","title":"File base_distributor.hpp","text":"<p>FileList &gt; dynampi &gt; impl &gt; base_distributor.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;mpi.h&gt;</code></li> <li><code>#include &lt;deque&gt;</code></li> <li><code>#include &lt;queue&gt;</code></li> <li><code>#include &lt;type_traits&gt;</code></li> <li><code>#include &lt;utility&gt;</code></li> </ul>"},{"location":"dynampi/base__distributor_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi"},{"location":"dynampi/base__distributor_8hpp/#classes","title":"Classes","text":"Type Name class BaseWorkDistributor &lt;typename TaskT, typename ResultT, Options&gt; struct Config struct enable_prioritization struct prioritize_tasks_t <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/base_distributor.hpp</code></p>"},{"location":"dynampi/base__distributor_8hpp_source/","title":"File base_distributor.hpp","text":"<p>File List &gt; dynampi &gt; impl &gt; base_distributor.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2025 QDX Technologies. Authored by Ryan Stocks &lt;ryan.stocks00@gmail.com&gt;\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;mpi.h&gt;\n\n#include &lt;deque&gt;\n#include &lt;queue&gt;\n#include &lt;type_traits&gt;\n#include &lt;utility&gt;\n\nnamespace dynampi {\n\nstruct prioritize_tasks_t {\n  static constexpr bool value = false;\n};\n\nstruct enable_prioritization : public prioritize_tasks_t {\n  static constexpr bool value = true;\n};\n\ntemplate &lt;typename TaskT, typename ResultT, typename... Options&gt;\nclass BaseWorkDistributor {\n public:\n  struct Config {\n    MPI_Comm comm = MPI_COMM_WORLD;\n    int manager_rank = 0;\n    bool auto_run_workers = true;\n  };\n\n protected:\n  static constexpr bool prioritize_tasks = get_option_value&lt;prioritize_tasks_t, Options...&gt;();\n  using QueueT = std::conditional_t&lt;prioritize_tasks, std::priority_queue&lt;std::pair&lt;double, TaskT&gt;&gt;,\n                                    std::deque&lt;TaskT&gt;&gt;;\n};\n\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/hierarchical__distributor_8hpp/","title":"File hierarchical_distributor.hpp","text":"<p>FileList &gt; dynampi &gt; impl &gt; hierarchical_distributor.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;algorithm&gt;</code></li> <li><code>#include &lt;cmath&gt;</code></li> <li><code>#include &lt;deque&gt;</code></li> <li><code>#include &lt;functional&gt;</code></li> <li><code>#include &lt;iterator&gt;</code></li> <li><code>#include &lt;limits&gt;</code></li> <li><code>#include &lt;queue&gt;</code></li> <li><code>#include &lt;ranges&gt;</code></li> <li><code>#include &lt;set&gt;</code></li> <li><code>#include &lt;span&gt;</code></li> <li><code>#include &lt;type_traits&gt;</code></li> <li><code>#include &lt;vector&gt;</code></li> <li><code>#include \"../mpi/mpi_communicator.hpp\"</code></li> <li><code>#include \"../mpi/mpi_types.hpp\"</code></li> <li><code>#include \"dynampi/impl/base_distributor.hpp\"</code></li> <li><code>#include \"dynampi/impl/hierarchical_topology_detail.hpp\"</code></li> <li><code>#include \"dynampi/impl/variable_batch.hpp\"</code></li> <li><code>#include \"dynampi/task_error.hpp\"</code></li> <li><code>#include \"dynampi/utilities/assert.hpp\"</code></li> <li><code>#include \"dynampi/utilities/timer.hpp\"</code></li> </ul>"},{"location":"dynampi/hierarchical__distributor_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi"},{"location":"dynampi/hierarchical__distributor_8hpp/#classes","title":"Classes","text":"Type Name class HierarchicalWorkDistributor &lt;typename TaskT, typename ResultT, Options&gt; struct Config struct RunConfig <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_distributor.hpp</code></p>"},{"location":"dynampi/hierarchical__distributor_8hpp_source/","title":"File hierarchical_distributor.hpp","text":"<p>File List &gt; dynampi &gt; impl &gt; hierarchical_distributor.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2025 QDX Technologies. Authored by Ryan Stocks &lt;ryan.stocks00@gmail.com&gt;\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;algorithm&gt;\n#include &lt;cmath&gt;\n#include &lt;deque&gt;\n#include &lt;functional&gt;\n#include &lt;iterator&gt;\n#include &lt;limits&gt;\n#include &lt;queue&gt;\n#include &lt;ranges&gt;\n#include &lt;set&gt;\n#include &lt;span&gt;\n#include &lt;type_traits&gt;\n#include &lt;vector&gt;\n\n#include \"../mpi/mpi_communicator.hpp\"\n#include \"../mpi/mpi_types.hpp\"\n#include \"dynampi/impl/base_distributor.hpp\"\n#include \"dynampi/impl/hierarchical_topology_detail.hpp\"\n#include \"dynampi/impl/variable_batch.hpp\"\n#include \"dynampi/task_error.hpp\"\n#include \"dynampi/utilities/assert.hpp\"\n#include \"dynampi/utilities/timer.hpp\"\n\nnamespace dynampi {\n\ntemplate &lt;typename TaskT, typename ResultT, typename... Options&gt;\nclass HierarchicalWorkDistributor : public BaseWorkDistributor&lt;TaskT, ResultT, Options...&gt; {\n  using Base = BaseWorkDistributor&lt;TaskT, ResultT, Options...&gt;;\n\n public:\n  struct Config {\n    MPI_Comm comm = MPI_COMM_WORLD;\n    int manager_rank = 0;\n    bool auto_run_workers = true;\n    std::optional&lt;int&gt; max_workers_per_manager = std::nullopt;\n\n    // Tasks requested per subtree leaf. Prefer pipeline_depth for latency\n    // hiding because larger batches reduce load-balancing flexibility.\n    int batch_size_multiplier = 1;\n\n    // Batches owned concurrently. 1 disables prefetching; 2 double-buffers.\n    // Prefetched tasks cannot be reassigned to an idle sibling subtree.\n    int pipeline_depth = 2;\n\n    // Root manager -&gt; node managers -&gt; local workers. The root manager is\n    // excluded from its node's local worker communicator.\n    bool manager_per_node = true;\n\n    // 0 keeps one local group per node; positive values partition it into\n    // contiguous groups of at most this size.\n    int max_local_group_size = 0;\n\n    // &lt;0 selects fanout automatically, 0 keeps a flat leader layer, and &gt;0\n    // recursively caps the number of leader-layer children.\n    int max_upper_fanout = -1;\n\n    // Rethrow worker failures at the root; otherwise collect them for\n    // take_task_errors().\n    bool rethrow_task_errors = true;\n  };\n\n  struct RunConfig {\n    // Stop once we have at least this many results ready to return.\n    size_t target_num_tasks = std::numeric_limits&lt;size_t&gt;::max();\n\n    // If false, strictly clips the return vector to `target_num_tasks`.\n    // Excess results are buffered for the next call.\n    bool allow_more_than_target_tasks = true;\n\n    // Stop if this much time has passed.\n    std::optional&lt;double&gt; max_seconds = std::nullopt;\n  };\n\n  static constexpr bool prioritize_tasks = Base::prioritize_tasks;\n  static const bool ordered = false;\n\n private:\n  typename Base::QueueT m_unallocated_task_queue;\n  std::vector&lt;ResultT&gt; m_results;\n\n  struct TaskRequest {\n    int worker_rank;\n    std::optional&lt;int&gt; num_tasks_requested = std::nullopt;\n  };\n  static constexpr int kMaxTasksRequested = 1'000'000;  // guard against pathological reserve()\n  // FIFO prevents new requests repeatedly jumping older ones.\n  std::queue&lt;TaskRequest&gt; m_free_worker_indices;\n\n  size_t m_tasks_sent_to_child = 0;\n  size_t m_results_received_from_child = 0;\n  size_t m_results_sent_to_parent = 0;\n  size_t m_tasks_received_from_parent = 0;\n  size_t m_tasks_executed = 0;\n\n  bool m_finalized = false;\n  bool m_done = false;\n\n  detail::TaskErrorLog m_task_errors;  // root manager only\n\n  // Batches received during an active round wait here for its boundary.\n  bool m_round_active = false;\n  std::deque&lt;TaskT&gt; m_prefetched_tasks;\n  // Whether the one allowed parent batch request is unanswered.\n  bool m_request_outstanding = false;\n\n  static constexpr StatisticsMode statistics_mode =\n      get_option_value&lt;track_statistics_t, Options...&gt;();\n\n  using MPICommunicator = dynampi::MPICommunicator&lt;track_statistics&lt;statistics_mode&gt;&gt;;\n\n  MPICommunicator m_communicator;  // Global communicator\n  MPIGroup m_world_group;          // Group for the global communicator (for rank translation)\n  std::optional&lt;MPIGroup&gt; m_local_group;  // Intra-node group (Shared Memory, excludes manager)\n\n  // A promoted leader owns one group per level and belongs to one parent\n  // group. The root manager always owns the top group.\n  std::vector&lt;MPIGroup&gt; m_owned_leader_levels;\n  std::optional&lt;MPIGroup&gt; m_leader_parent_group;\n\n  // Number of leaf workers fed by this leader.\n  int m_subtree_leaf_count = 1;\n\n  std::function&lt;ResultT(TaskT)&gt; m_worker_function;\n  Config m_config;\n\n  // Cached parent target to avoid repeated MPI_Group_translate_ranks calls\n  mutable std::optional&lt;int&gt; m_cached_parent_target;\n\n  // --- Topology Helper Methods ---\n\n  inline int max_workers_per_manager() const {\n    const int default_value = std::max(2, static_cast&lt;int&gt;(std::sqrt(m_communicator.size())));\n    const int configured = m_config.max_workers_per_manager.value_or(default_value);\n    return std::max(1, configured);\n  }\n\n  // Builds the k-ary leader layer. Every rank in each input communicator\n  // must reach its matching MPI_Comm_split, including ranks not promoted.\n  void setup_leader_hierarchy(bool is_manager, bool is_node_manager) {\n    const int local_children =\n        (m_local_group &amp;&amp; m_local_group-&gt;rank() == 0) ? (m_local_group-&gt;size() - 1) : 0;\n    m_subtree_leaf_count = std::max(1, local_children);\n\n    const int leader_color = (is_manager || is_node_manager) ? 0 : MPI_UNDEFINED;\n    auto flat_opt = m_communicator.split(leader_color, m_communicator.rank());\n    if (!flat_opt.has_value()) return;\n    MPICommunicator flat_comm = std::move(*flat_opt);\n\n    const int manager_count = flat_comm.size() - 1;  // excludes the root manager\n    const int effective_fanout =\n        detail::resolve_upper_fanout(manager_count, m_config.max_upper_fanout);\n\n    if (manager_count &lt;= effective_fanout) {\n      MPIGroup flat_group(flat_comm);\n      if (is_manager) {\n        m_owned_leader_levels.push_back(std::move(flat_group));\n      } else {\n        m_leader_parent_group.emplace(std::move(flat_group));\n      }\n      return;\n    }\n\n    // Collective over flat_comm; only node managers join the result.\n    auto managers_opt = flat_comm.split(is_manager ? MPI_UNDEFINED : 0, flat_comm.rank());\n\n    bool is_final_round_leader = false;\n    if (!is_manager) {\n      // optional::emplace works around MPICommunicator's deleted move assignment.\n      std::optional&lt;MPICommunicator&gt; round_comm(std::move(*managers_opt));\n      while (true) {\n        if (round_comm-&gt;size() &lt;= effective_fanout) {\n          is_final_round_leader = true;\n          break;\n        }\n        const int color = round_comm-&gt;rank() / effective_fanout;\n        auto group_opt = round_comm-&gt;split(color, round_comm-&gt;rank());\n        MPICommunicator group_comm = std::move(*group_opt);\n        const bool is_group_leader = (group_comm.rank() == 0);\n        const int group_leaf_count =\n            detail::sum_subtree_widths_to_group_leader(m_subtree_leaf_count, group_comm);\n\n        auto leaders_opt =\n            round_comm-&gt;split(is_group_leader ? 0 : MPI_UNDEFINED, round_comm-&gt;rank());\n\n        if (!is_group_leader) {\n          m_leader_parent_group.emplace(std::move(group_comm));\n          break;\n        }\n        m_owned_leader_levels.emplace_back(group_comm);\n        m_subtree_leaf_count = std::max(1, group_leaf_count);\n        round_comm.emplace(std::move(*leaders_opt));\n      }\n    }\n\n    // Collective over the original flat_comm attaches final leaders to root.\n    auto top_opt = flat_comm.split((is_manager || is_final_round_leader) ? 0 : MPI_UNDEFINED,\n                                   flat_comm.rank());\n    if (top_opt.has_value()) {\n      MPIGroup top_group(*top_opt);\n      if (is_manager) {\n        m_owned_leader_levels.push_back(std::move(top_group));\n      } else {\n        m_leader_parent_group.emplace(std::move(top_group));\n      }\n    }\n  }\n\n  inline int get_parent_target() const {\n    if (m_cached_parent_target.has_value()) {\n      return m_cached_parent_target.value();\n    }\n\n    int result;\n    DYNAMPI_ASSERT(!is_root_manager(), \"Root manager should not have a parent\");\n    if (m_config.manager_per_node) {\n      DYNAMPI_ASSERT(m_local_group.has_value() || m_leader_parent_group.has_value(),\n                     \"Local or leader parent group should be present\");\n      if (m_local_group &amp;&amp; m_local_group-&gt;rank() &gt; 0) {\n        result = m_local_group-&gt;translate_rank(0, m_world_group);\n      } else {\n        // Root owns a top group; intermediate groups are owned by rank 0.\n        DYNAMPI_ASSERT(m_leader_parent_group.has_value(),\n                       \"Non-manager leader-layer rank must have a parent group\");\n        int parent_in_group = 0;\n        const int manager_in_group =\n            m_world_group.translate_rank(m_config.manager_rank, *m_leader_parent_group);\n        if (manager_in_group != MPI_UNDEFINED) {\n          parent_in_group = manager_in_group;\n        }\n        result = m_leader_parent_group-&gt;translate_rank(parent_in_group, m_world_group);\n      }\n    } else {\n      int virtual_parent = (virtual_rank() - 1) / max_workers_per_manager();\n      result = virtual_parent == 0 ? m_config.manager_rank : worker_for_idx(virtual_parent - 1);\n    }\n\n    m_cached_parent_target = result;\n    return result;\n  }\n\n  inline int num_direct_children() const {\n    if (m_config.manager_per_node) {\n      int count = 0;\n      if (m_local_group &amp;&amp; m_local_group-&gt;rank() == 0) {\n        count += (m_local_group-&gt;size() - 1);\n      }\n      for (const auto&amp; level : m_owned_leader_levels) {\n        count += (level.size() - 1);\n      }\n      return count;\n    } else {\n      int num_children = 0;\n      int max_children = max_workers_per_manager();\n      for (int i = 0; i &lt; max_children; ++i) {\n        int virtual_child = virtual_rank() * max_children + i + 1;\n        if (virtual_child &lt; m_communicator.size()) {\n          num_children++;\n        }\n      }\n      return num_children;\n    }\n  }\n\n  // Round size is one task per subtree leaf, not per direct manager child.\n  inline int subtree_leaf_count() const {\n    return m_config.manager_per_node ? m_subtree_leaf_count : num_direct_children();\n  }\n\n  // Manager layers between us and the leaf workers, counting ourselves. Each\n  // one buffers a pipelined batch of its own, so the exposure budget has to\n  // grow with it -- see request_next_batch_if_room().\n  inline int subtree_manager_layers() const {\n    if (m_config.manager_per_node) {\n      return 1 + static_cast&lt;int&gt;(m_owned_leader_levels.size());\n    }\n    const int max_children = max_workers_per_manager();\n    int layers = 0;\n    for (int v = virtual_rank(); v * max_children + 1 &lt; m_communicator.size();\n         v = v * max_children + 1) {\n      ++layers;\n    }\n    return std::max(1, layers);\n  }\n\n  bool is_leaf_worker() const {\n    if (m_config.manager_per_node) {\n      if (is_root_manager()) return false;\n\n      if (!m_local_group) return true;\n      if (m_local_group-&gt;rank() &gt; 0) return true;\n\n      return num_direct_children() == 0;\n    } else {\n      return virtual_rank() * max_workers_per_manager() + 1 &gt;= m_communicator.size();\n    }\n  }\n\n  enum Tag : int {\n    TASK = 0,\n    DONE = 1,\n    RESULT = 2,\n    REQUEST = 3,\n    TASK_BATCH = 4,\n    RESULT_BATCH = 5,\n    REQUEST_BATCH = 6,\n    ERROR = 7\n  };\n\n  struct Statistics {\n    const CommStatistics&amp; comm_statistics;\n    std::optional&lt;std::vector&lt;size_t&gt;&gt; worker_task_counts = {};\n  };\n\n  using StatisticsT =\n      std::conditional_t&lt;statistics_mode != StatisticsMode::None, Statistics, std::monostate&gt;;\n\n  StatisticsT _statistics;\n\n  static StatisticsT create_statistics(const MPICommunicator&amp; comm) {\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      return Statistics{.comm_statistics = comm.get_statistics()};\n    } else {\n      return {};\n    }\n  }\n\n public:\n  explicit HierarchicalWorkDistributor(std::function&lt;ResultT(TaskT)&gt; worker_function,\n                                       Config runtime_config = Config{})\n      : m_communicator(runtime_config.comm, MPICommunicator::Duplicate),\n        m_world_group(m_communicator),\n        m_worker_function(worker_function),\n        m_config(runtime_config),\n        _statistics{create_statistics(m_communicator)} {\n    // Managers exchange whole batches as one std::vector&lt;T&gt; message, whose\n    // element count is the number of values -- so a non-resizable payload\n    // spanning more than one datatype element would send a fraction of each.\n    // See check_fixed_size_mpi_type().\n    check_fixed_size_mpi_type&lt;TaskT&gt;(\"task\", \"HierarchicalWorkDistributor\");\n    check_fixed_size_mpi_type&lt;ResultT&gt;(\"result\", \"HierarchicalWorkDistributor\");\n\n    // --- Initialize Topology Groups ---\n    if (m_config.manager_per_node) {\n      // Identify physical/local domains and exclude the root manager from\n      // its node's worker communicator.\n      auto local_comm_opt = detail::split_local_worker_communicator(\n          m_communicator, m_config.manager_rank, m_config.max_local_group_size);\n      if (local_comm_opt.has_value()) {\n        // Extract group from the temporary communicator, then let it be freed\n        m_local_group.emplace(*local_comm_opt);\n      }\n\n      // Build the leader layer: manager + Node Managers (Rank 0 of\n      // the *Local* Comm), optionally grouped into a tree -- see\n      // setup_leader_hierarchy().\n      bool is_manager = (m_communicator.rank() == m_config.manager_rank);\n      // Check if we're rank 0 in the local group (node manager)\n      bool is_node_manager = false;\n      if (m_local_group.has_value()) {\n        int my_local_rank = m_local_group-&gt;rank();\n        is_node_manager = (my_local_rank == 0);\n      }\n\n      setup_leader_hierarchy(is_manager, is_node_manager);\n    }\n\n    // Ensure every rank finishes topology setup before use, matching\n    // HierarchicalLockFreeRMAWorkDistributor's constructor barrier. Without\n    // it, a straggler rank still inside setup_leader_hierarchy()'s\n    // collectives can miss the manager's first request round, folding that\n    // one-time setup latency into the caller's first batch instead of\n    // construction.\n    DYNAMPI_MPI_CHECK(MPI_Barrier, (static_cast&lt;MPI_Comm&gt;(m_communicator)));\n\n    if (m_config.auto_run_workers &amp;&amp; m_communicator.rank() != m_config.manager_rank) {\n      run_worker();\n    }\n  }\n\n  const StatisticsT&amp; get_statistics() const\n    requires(statistics_mode != StatisticsMode::None)\n  {\n    DYNAMPI_ASSERT(is_root_manager(), \"Only the manager can access statistics\");\n    return _statistics;\n  }\n\n  // Tasks that threw, oldest first, removed as they are returned. See\n  // Config::rethrow_task_errors.\n  [[nodiscard]] std::vector&lt;TaskError&gt; take_task_errors() {\n    DYNAMPI_ASSERT(is_root_manager(), \"Only the manager collects task errors\");\n    return m_task_errors.take();\n  }\n\n  bool has_task_errors() const {\n    DYNAMPI_ASSERT(is_root_manager(), \"Only the manager collects task errors\");\n    return !m_task_errors.empty();\n  }\n\n  void run_worker() {\n    DYNAMPI_ASSERT(m_communicator.rank() != m_config.manager_rank,\n                   \"Worker cannot run on the manager rank\");\n    if (is_leaf_worker()) {\n      send_to_parent(nullptr, Tag::REQUEST);\n      while (!m_done) {\n        receive_from_anyone();\n      }\n    } else {\n      run_intermediate_worker();\n    }\n  }\n\n  // One task allocated per leaf, plus one pipelined batch for each manager\n  // layer beneath us, so a leaf worker is never committed to more than\n  // tree-depth tasks and finalization never waits on more than that.\n  //\n  // Counting a flat pipeline_depth batches here instead starves the upper\n  // layers: a group leader fronting 127 leaves got 2 x 127, but its 15 node\n  // managers are each entitled to 2 per leaf (240) and its own 7 leaves take\n  // 7, leaving 7 tasks of headroom where a whole 127-task batch was needed.\n  // It could therefore never hold a pipelined batch, and every round paid the\n  // full request/reply latency up the tree -- 2.97ms per round at 128 nodes\n  // with 1ms tasks, against 1.01ms once the layers are budgeted for. Raising\n  // pipeline_depth globally fixes that but over-provisions the node managers,\n  // whose extra hoarding cannot be handed to an idle sibling.\n  void request_next_batch_if_room() {\n    if (m_done || m_request_outstanding) return;\n    const int prefetch =\n        std::max(1, subtree_leaf_count() * std::max(1, m_config.batch_size_multiplier));\n    const int pipeline_depth = std::max(1, m_config.pipeline_depth);\n    const size_t pipelined_per_layer = static_cast&lt;size_t&gt;(pipeline_depth - 1);\n    const size_t capacity =\n        static_cast&lt;size_t&gt;(prefetch) *\n        (1 + pipelined_per_layer * static_cast&lt;size_t&gt;(subtree_manager_layers()));\n    const size_t uncompleted =\n        m_tasks_received_from_parent - m_results_received_from_child - m_tasks_executed;\n    if (uncompleted + static_cast&lt;size_t&gt;(prefetch) &gt; capacity) {\n      return;\n    }\n    send_to_parent(prefetch, Tag::REQUEST_BATCH);\n    m_request_outstanding = true;\n  }\n\n  void send_results_to_parent() {\n    DYNAMPI_ASSERT(!is_leaf_worker(), \"Leaf workers should not return results directly\");\n    DYNAMPI_ASSERT_NE(m_communicator.rank(), m_config.manager_rank,\n                      \"Manager should not request tasks from itself\");\n    std::vector&lt;ResultT&gt; results = std::move(m_results);\n    m_results.clear();\n\n    send_batch(results, get_parent_target(), Tag::RESULT_BATCH);\n    m_results_sent_to_parent += results.size();\n    request_next_batch_if_room();\n  }\n\n  bool is_root_manager() const { return m_communicator.rank() == m_config.manager_rank; }\n\n  size_t remaining_tasks_count() const {\n    DYNAMPI_ASSERT_EQ(m_communicator.rank(), m_config.manager_rank,\n                      \"Only the manager can check remaining tasks\");\n    return m_unallocated_task_queue.size();\n  }\n\n  void insert_task(TaskT task)\n    requires(!prioritize_tasks)\n  {\n    DYNAMPI_ASSERT_EQ(m_communicator.rank(), m_config.manager_rank,\n                      \"Only the manager can distribute tasks\");\n    m_unallocated_task_queue.push_back(std::move(task));\n    m_tasks_received_from_parent++;\n  }\n  void insert_task(const TaskT&amp; task, double priority)\n    requires(prioritize_tasks)\n  {\n    DYNAMPI_ASSERT_EQ(m_communicator.rank(), m_config.manager_rank,\n                      \"Only the manager can distribute tasks\");\n    m_unallocated_task_queue.emplace(priority, task);\n    m_tasks_received_from_parent++;\n  }\n\n  template &lt;typename Range&gt;\n    requires std::ranges::input_range&lt;Range&gt; &amp;&amp; (!prioritize_tasks)\n  void insert_tasks(Range&amp;&amp; tasks) {\n    DYNAMPI_ASSERT_EQ(m_communicator.rank(), m_config.manager_rank,\n                      \"Only the manager can distribute tasks\");\n    size_t inserted = 0;\n    for (const auto&amp; task : tasks) {\n      m_unallocated_task_queue.push_back(task);\n      ++inserted;\n    }\n    m_tasks_received_from_parent += inserted;\n  }\n  void insert_tasks(const std::vector&lt;TaskT&gt;&amp; tasks)\n    requires(!prioritize_tasks)\n  {\n    insert_tasks(std::span&lt;const TaskT&gt;(tasks));\n  }\n\n  void allocate_task_to_child() {\n    if (m_communicator.size() &gt; 1) {\n      DYNAMPI_ASSERT(!m_free_worker_indices.empty(), \"Cannot allocate task with no free workers\");\n\n      TaskRequest request = m_free_worker_indices.front();\n      m_free_worker_indices.pop();\n\n      int worker_rank = request.worker_rank;\n\n      if (request.num_tasks_requested.has_value()) {\n        const size_t actual_num_tasks = std::min(static_cast&lt;size_t&gt;(*request.num_tasks_requested),\n                                                 m_unallocated_task_queue.size());\n        std::vector&lt;TaskT&gt; tasks = take_tasks_from_queue(actual_num_tasks);\n\n        send_batch(tasks, worker_rank, Tag::TASK_BATCH);\n        m_tasks_sent_to_child += tasks.size();\n      } else {\n        const TaskT task = get_next_task_to_send();\n        m_communicator.send(task, worker_rank, Tag::TASK);\n        m_tasks_sent_to_child++;\n      }\n    } else {\n      // Single-rank mode: the manager runs the task on its own stack. Guarded\n      // all the same -- this path exists so a workload can be debugged\n      // serially, which it cannot be if failures surface differently here.\n      const TaskT task = get_next_task_to_send();\n      ResultT result;\n      auto failure = detail::run_task_guarded(m_worker_function, task, result);\n      if (failure) m_task_errors.record(TaskError{m_communicator.rank(), std::move(*failure)});\n      m_results.emplace_back(std::move(result));\n      m_tasks_executed++;\n    }\n  }\n\n  [[nodiscard]] std::vector&lt;ResultT&gt; run_tasks(const RunConfig&amp; config = RunConfig{}) {\n    DYNAMPI_ASSERT_EQ(m_communicator.rank(), m_config.manager_rank,\n                      \"Only the manager can finish remaining tasks\");\n    Timer timer;\n\n    while (true) {\n      if (m_results.size() &gt;= config.target_num_tasks) break;\n      if (config.max_seconds &amp;&amp; timer.elapsed().count() &gt;= *config.max_seconds) break;\n\n      size_t active_tasks = m_tasks_sent_to_child - m_results_received_from_child;\n      if (m_unallocated_task_queue.empty() &amp;&amp; active_tasks == 0) {\n        break;\n      }\n\n      bool tasks_available = !m_unallocated_task_queue.empty();\n      bool workers_available = !m_free_worker_indices.empty();\n      bool is_single_proc = (m_communicator.size() == 1);\n\n      if (tasks_available &amp;&amp; (is_single_proc || workers_available)) {\n        allocate_task_to_child();\n      } else if (active_tasks &gt; 0 || (tasks_available &amp;&amp; !workers_available)) {\n        receive_from_anyone();\n      }\n    }\n\n    // --- Return Logic ---\n    // Thrown before draining, so results collected so far stay buffered for\n    // whoever catches this and calls again.\n    m_task_errors.rethrow_first_if(m_config.rethrow_task_errors);\n\n    std::vector&lt;ResultT&gt; batch;\n\n    size_t available = m_results.size();\n    size_t count_to_return = available;\n\n    if (!config.allow_more_than_target_tasks) {\n      count_to_return = std::min(available, config.target_num_tasks);\n    }\n\n    batch.reserve(count_to_return);\n    auto end_it = m_results.begin() + count_to_return;\n    std::move(m_results.begin(), end_it, std::back_inserter(batch));\n    m_results.erase(m_results.begin(), end_it);\n\n    m_results_sent_to_parent += batch.size();\n    return batch;\n  }\n\n  [[nodiscard]] std::vector&lt;ResultT&gt; finish_remaining_tasks() {\n    RunConfig cfg;\n    cfg.target_num_tasks = std::numeric_limits&lt;size_t&gt;::max();\n    return run_tasks(cfg);\n  }\n\n  void finalize() {\n    DYNAMPI_ASSERT(!m_finalized, \"Work distribution already finalized\");\n    if (is_root_manager()) {\n      send_done_to_children_when_free();\n    }\n    m_finalized = true;\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      if (is_root_manager()) {\n        _statistics.worker_task_counts = std::vector&lt;size_t&gt;(m_communicator.size(), 0);\n      }\n      m_communicator.gather(m_tasks_executed,\n                            _statistics.worker_task_counts.has_value()\n                                ? &amp;_statistics.worker_task_counts.value()\n                                : nullptr,\n                            m_config.manager_rank);\n    }\n  }\n\n  ~HierarchicalWorkDistributor() {\n    if (!m_finalized) {\n      finalize();\n    }\n    m_task_errors.warn_if_unreported(\"HierarchicalWorkDistributor\");\n    DYNAMPI_ASSERT_EQ(m_results_received_from_child, m_tasks_sent_to_child,\n                      \"All tasks should have been processed by workers before finalizing\");\n    DYNAMPI_ASSERT_EQ(m_results_sent_to_parent, m_tasks_received_from_parent,\n                      \"All results should have been sent to the parent before finalizing\");\n    if (is_leaf_worker())\n      DYNAMPI_ASSERT_EQ(m_results_received_from_child, 0,\n                        \"Leaf workers should not receive results from children\");\n    else if (m_communicator.size() &gt; 1)\n      DYNAMPI_ASSERT_EQ(m_results_received_from_child + m_tasks_executed, m_results_sent_to_parent,\n                        \"Results received from children should match results sent to parent\");\n  }\n\n private:\n  void release_prefetched_tasks() {\n    if constexpr (!prioritize_tasks) {\n      if (m_prefetched_tasks.empty()) return;\n      for (auto&amp; task : m_prefetched_tasks) {\n        m_unallocated_task_queue.push_back(std::move(task));\n      }\n      m_prefetched_tasks.clear();\n    }\n  }\n\n  void wait_for_work_or_flush() {\n    // Flush results while waiting; the parent may need them to declare\n    // exhaustion before sending DONE.\n    while (!m_done &amp;&amp; m_unallocated_task_queue.empty()) {\n      receive_from_anyone();\n      if (!m_results.empty()) send_results_to_parent();\n    }\n  }\n\n  void distribute_current_round() {\n    m_round_active = true;\n    // Finish tasks already in this round even if DONE arrives.\n    while (!m_unallocated_task_queue.empty()) {\n      if (m_free_worker_indices.empty()) {\n        receive_from_anyone();\n      } else {\n        allocate_task_to_child();\n      }\n    }\n    m_round_active = false;\n  }\n\n  void drain_inflight_results_at_shutdown() {\n    // A next-batch request can precede the previous results; drain them.\n    while (m_tasks_sent_to_child &gt; m_results_received_from_child) {\n      receive_from_anyone();  // LCOV_EXCL_LINE -- requires a shutdown/in-flight-result race\n    }\n    if (!m_results.empty()) {\n      send_results_to_parent();  // LCOV_EXCL_LINE -- same race as the drain above\n    }\n  }\n\n  void run_intermediate_worker() {\n    request_next_batch_if_room();\n    while (!m_done) {\n      release_prefetched_tasks();\n      wait_for_work_or_flush();\n      if (m_done) break;\n      distribute_current_round();\n      if (!m_results.empty()) send_results_to_parent();\n      if (m_done) break;\n    }\n    send_done_to_children_when_free();\n    drain_inflight_results_at_shutdown();\n  }\n\n  std::vector&lt;TaskT&gt; take_tasks_from_queue(size_t count) {\n    DYNAMPI_ASSERT_LE(count, m_unallocated_task_queue.size(),\n                      \"Cannot take more tasks than are available\");\n    std::vector&lt;TaskT&gt; tasks;\n    tasks.reserve(count);\n    if constexpr (std::is_same_v&lt;decltype(m_unallocated_task_queue), std::deque&lt;TaskT&gt;&gt;) {\n      auto end = m_unallocated_task_queue.begin() + static_cast&lt;ptrdiff_t&gt;(count);\n      tasks.assign(m_unallocated_task_queue.begin(), end);\n      m_unallocated_task_queue.erase(m_unallocated_task_queue.begin(), end);\n    } else {\n      for (size_t i = 0; i &lt; count; ++i) {\n        tasks.push_back(m_unallocated_task_queue.top().second);\n        m_unallocated_task_queue.pop();\n      }\n    }\n    return tasks;\n  }\n\n  TaskT get_next_task_to_send() {\n    DYNAMPI_ASSERT(is_root_manager() || !is_leaf_worker(),\n                   \"Leaf workers should not send tasks directly\");\n    DYNAMPI_ASSERT(!m_unallocated_task_queue.empty(), \"There should be tasks available to send\");\n    auto tasks = take_tasks_from_queue(1);\n    return std::move(tasks.front());\n  }\n\n  int idx_for_worker(int worker_rank) const {\n    DYNAMPI_ASSERT_NE(worker_rank, m_config.manager_rank,\n                      \"Manager rank should not be used as a worker rank\");\n    return (worker_rank &lt; m_config.manager_rank) ? worker_rank : (worker_rank - 1);\n  }\n\n  int worker_for_idx(int idx) const { return (idx &lt; m_config.manager_rank) ? idx : (idx + 1); }\n\n  int virtual_rank() const {\n    const int rank = m_communicator.rank();\n    return rank == m_config.manager_rank ? 0 : idx_for_worker(rank) + 1;\n  }\n\n  template &lt;typename T&gt;\n  void send_to_parent(const T&amp; data, Tag tag) {\n    const int target = get_parent_target();\n    DYNAMPI_ASSERT_NE(target, -1, \"Root cannot send to parent\");\n\n    m_communicator.send(data, target, tag);\n  }\n\n  template &lt;typename T&gt;\n  void send_batch(const std::vector&lt;T&gt;&amp; values, int rank, Tag tag) {\n    if constexpr (MPI_Type&lt;T&gt;::resize_required) {\n      m_communicator.send(detail::pack_variable_batch(values), rank, tag);\n    } else {\n      m_communicator.send(values, rank, tag);\n    }\n  }\n\n  template &lt;typename T&gt;\n  std::vector&lt;T&gt; receive_batch(MPI_Status status, Tag tag) {\n    const int source = status.MPI_SOURCE;\n    if constexpr (MPI_Type&lt;T&gt;::resize_required) {\n      using message_type = MPI_Type&lt;std::vector&lt;std::byte&gt;&gt;;\n      int count;\n      DYNAMPI_MPI_CHECK(MPI_Get_count, (&amp;status, message_type::value, &amp;count));\n      std::vector&lt;std::byte&gt; packed;\n      message_type::resize(packed, count);\n      m_communicator.recv(packed, source, tag);\n      return detail::unpack_variable_batch&lt;T&gt;(packed);\n    } else {\n      using message_type = MPI_Type&lt;std::vector&lt;T&gt;&gt;;\n      int count;\n      DYNAMPI_MPI_CHECK(MPI_Get_count, (&amp;status, message_type::value, &amp;count));\n      std::vector&lt;T&gt; values;\n      message_type::resize(values, count);\n      m_communicator.recv(values, source, tag);\n      return values;\n    }\n  }\n\n  void send_done_to_children_when_free() {\n    const int direct_children = num_direct_children();\n    // Deduplicate defensively; normally each child has one outstanding request.\n    std::set&lt;int&gt; notified;\n    while (static_cast&lt;int&gt;(notified.size()) &lt; direct_children) {\n      if (m_free_worker_indices.empty()) {\n        receive_from_anyone();\n        continue;\n      }\n      TaskRequest request = m_free_worker_indices.front();\n      m_free_worker_indices.pop();\n\n      if (notified.contains(request.worker_rank)) {\n        continue;  // LCOV_EXCL_LINE -- defensive; see comment above\n      }\n      m_communicator.send(nullptr, request.worker_rank, Tag::DONE);\n      notified.insert(request.worker_rank);\n    }\n  }\n\n  using result_mpi_type = MPI_Type&lt;ResultT&gt;;\n  using task_mpi_type = MPI_Type&lt;TaskT&gt;;\n\n  void receive_result_from(MPI_Status status) {\n    int world_source = status.MPI_SOURCE;\n    if constexpr (result_mpi_type::resize_required) {\n      int count;\n      DYNAMPI_MPI_CHECK(MPI_Get_count, (&amp;status, result_mpi_type::value, &amp;count));\n      ResultT result{};\n      result_mpi_type::resize(result, count);\n      m_communicator.recv(result, world_source, Tag::RESULT);\n      m_results.push_back(std::move(result));\n    } else {\n      m_results.push_back(ResultT{});\n      m_communicator.recv(m_results.back(), world_source, Tag::RESULT);\n    }\n    m_results_received_from_child++;\n    m_free_worker_indices.push(TaskRequest{.worker_rank = world_source});\n  }\n\n  void receive_result_batch_from(MPI_Status status) {\n    std::vector&lt;ResultT&gt; results = receive_batch&lt;ResultT&gt;(status, Tag::RESULT_BATCH);\n    std::move(results.begin(), results.end(), std::back_inserter(m_results));\n    m_results_received_from_child += results.size();\n  }\n\n  void receive_execute_return_task_from(MPI_Status status) {\n    int count;\n    DYNAMPI_MPI_CHECK(MPI_Get_count, (&amp;status, task_mpi_type::value, &amp;count));\n    TaskT message;\n    task_mpi_type::resize(message, count);\n    int world_source = status.MPI_SOURCE;\n    m_communicator.recv(message, world_source, Tag::TASK);\n    m_tasks_received_from_parent++;\n    ResultT result;\n    auto failure = detail::run_task_guarded(m_worker_function, message, result);\n    m_tasks_executed++;\n    // ERROR is advisory; RESULT still carries completion accounting.\n    if (failure) {\n      m_communicator.send(\n          detail::encode_task_error(TaskError{m_communicator.rank(), std::move(*failure)}),\n          world_source, Tag::ERROR);\n    }\n    m_communicator.send(result, world_source, Tag::RESULT);\n    m_results_sent_to_parent++;\n  }\n\n  // Failures are forwarded independently of their placeholder result.\n  void receive_error_from(MPI_Status status) {\n    int world_source = status.MPI_SOURCE;\n    int count;\n    DYNAMPI_MPI_CHECK(MPI_Get_count, (&amp;status, MPI_CHAR, &amp;count));\n    std::string payload;\n    MPI_Type&lt;std::string&gt;::resize(payload, count);\n    m_communicator.recv(payload, world_source, Tag::ERROR);\n\n    if (is_root_manager()) {\n      m_task_errors.record(detail::decode_task_error(payload));\n    } else {\n      send_to_parent(payload, Tag::ERROR);\n    }\n  }\n\n  void receive_task_batch_from(MPI_Status status) {\n    if constexpr (prioritize_tasks) {\n      DYNAMPI_UNIMPLEMENTED(\"Prioritized hierarchical distribution\");\n    } else {\n      std::vector&lt;TaskT&gt; tasks = receive_batch&lt;TaskT&gt;(status, Tag::TASK_BATCH);\n      m_tasks_received_from_parent += tasks.size();\n      m_request_outstanding = false;\n      request_next_batch_if_room();\n      auto&amp; target = m_round_active ? m_prefetched_tasks : m_unallocated_task_queue;\n      for (auto&amp; task : tasks) {\n        target.push_back(std::move(task));\n      }\n    }\n  }\n\n  void receive_request_from(MPI_Status status) {\n    int world_source = status.MPI_SOURCE;\n    m_communicator.recv_empty_message(world_source, Tag::REQUEST);\n    m_free_worker_indices.push(TaskRequest{.worker_rank = world_source});\n  }\n\n  void receive_request_batch_from(MPI_Status status) {\n    int world_source = status.MPI_SOURCE;\n    int request_count;\n    m_communicator.recv(request_count, world_source, Tag::REQUEST_BATCH);\n    DYNAMPI_ASSERT_GT(request_count, 0, \"Invalid request count\");\n    DYNAMPI_ASSERT_LE(request_count, kMaxTasksRequested, \"Request count exceeds maximum allowed\");\n    m_free_worker_indices.push(\n        TaskRequest{.worker_rank = world_source, .num_tasks_requested = request_count});\n  }\n\n  void receive_done_from(MPI_Status status) {\n    int world_source = status.MPI_SOURCE;\n    m_communicator.recv_empty_message(world_source, Tag::DONE);\n    m_done = true;\n  }\n\n  void receive_from_anyone() {\n    DYNAMPI_ASSERT_GT(m_communicator.size(), 1,\n                      \"There should be at least one worker to receive results from\");\n\n    // All protocol messages use world ranks on the global communicator.\n    MPI_Status status = m_communicator.probe();\n\n    DYNAMPI_ASSERT(status.MPI_TAG &gt;= static_cast&lt;int&gt;(Tag::TASK) &amp;&amp;\n                       status.MPI_TAG &lt;= static_cast&lt;int&gt;(Tag::ERROR),\n                   \"Received invalid MPI tag: \" + std::to_string(status.MPI_TAG));\n    Tag tag = static_cast&lt;Tag&gt;(status.MPI_TAG);\n    switch (tag) {\n      case Tag::TASK:\n        return receive_execute_return_task_from(status);\n      case Tag::TASK_BATCH:\n        return receive_task_batch_from(status);\n      case Tag::RESULT:\n        return receive_result_from(status);\n      case Tag::RESULT_BATCH:\n        return receive_result_batch_from(status);\n      case Tag::REQUEST:\n        return receive_request_from(status);\n      case Tag::REQUEST_BATCH:\n        return receive_request_batch_from(status);\n      case Tag::DONE:\n        return receive_done_from(status);\n      case Tag::ERROR:\n        return receive_error_from(status);\n    }\n  }\n};\n\n};  // namespace dynampi\n</code></pre>"},{"location":"dynampi/hierarchical__lockfree__rma__distributor_8hpp/","title":"File hierarchical_lockfree_rma_distributor.hpp","text":"<p>FileList &gt; dynampi &gt; impl &gt; hierarchical_lockfree_rma_distributor.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;algorithm&gt;</code></li> <li><code>#include &lt;cassert&gt;</code></li> <li><code>#include &lt;cstddef&gt;</code></li> <li><code>#include &lt;cstdint&gt;</code></li> <li><code>#include &lt;cstring&gt;</code></li> <li><code>#include &lt;deque&gt;</code></li> <li><code>#include &lt;functional&gt;</code></li> <li><code>#include &lt;iterator&gt;</code></li> <li><code>#include &lt;limits&gt;</code></li> <li><code>#include &lt;optional&gt;</code></li> <li><code>#include &lt;thread&gt;</code></li> <li><code>#include &lt;vector&gt;</code></li> <li><code>#include \"../mpi/mpi_communicator.hpp\"</code></li> <li><code>#include \"../mpi/mpi_group.hpp\"</code></li> <li><code>#include \"../mpi/mpi_types.hpp\"</code></li> <li><code>#include \"dynampi/impl/hierarchical_topology_detail.hpp\"</code></li> <li><code>#include \"dynampi/impl/rma_detail.hpp\"</code></li> <li><code>#include \"dynampi/mpi/mpi_error.hpp\"</code></li> <li><code>#include \"dynampi/task_error.hpp\"</code></li> <li><code>#include \"dynampi/utilities/timer.hpp\"</code></li> </ul>"},{"location":"dynampi/hierarchical__lockfree__rma__distributor_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi namespace detail"},{"location":"dynampi/hierarchical__lockfree__rma__distributor_8hpp/#classes","title":"Classes","text":"Type Name class HierarchicalLockFreeRMAWorkDistributor &lt;typename TaskT, typename ResultT, Options&gt; struct Config struct RunConfig class LockFreeRMALevel &lt;typename TaskT, typename ResultT&gt; struct ClaimedRange struct Config <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_lockfree_rma_distributor.hpp</code></p>"},{"location":"dynampi/hierarchical__lockfree__rma__distributor_8hpp_source/","title":"File hierarchical_lockfree_rma_distributor.hpp","text":"<p>File List &gt; dynampi &gt; impl &gt; hierarchical_lockfree_rma_distributor.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2026 Ryan Stocks\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;algorithm&gt;\n#include &lt;cassert&gt;\n#include &lt;cstddef&gt;\n#include &lt;cstdint&gt;\n#include &lt;cstring&gt;\n#include &lt;deque&gt;\n#include &lt;functional&gt;\n#include &lt;iterator&gt;\n#include &lt;limits&gt;\n#include &lt;optional&gt;\n#include &lt;thread&gt;\n#include &lt;vector&gt;\n\n#include \"../mpi/mpi_communicator.hpp\"\n#include \"../mpi/mpi_group.hpp\"\n#include \"../mpi/mpi_types.hpp\"\n#include \"dynampi/impl/hierarchical_topology_detail.hpp\"\n#include \"dynampi/impl/rma_detail.hpp\"\n#include \"dynampi/mpi/mpi_error.hpp\"\n#include \"dynampi/task_error.hpp\"\n#include \"dynampi/utilities/timer.hpp\"\n\nnamespace dynampi {\n\nnamespace detail {\n\n// One composable level of fetch-and-add claims, task/result storage, and\n// completion logging. Its non-blocking steps let managers relay between levels.\ntemplate &lt;typename TaskT, typename ResultT&gt;\nclass LockFreeRMALevel {\n public:\n  struct Config {\n    MPI_Comm comm = MPI_COMM_NULL;\n    int owner_rank = 0;\n    // Ring capacity; harvested slots are reused modulo max_tasks.\n    int max_tasks = 8192;\n    int max_task_count = 256;\n    int max_result_count = 256;\n  };\n\n  struct ClaimedRange {\n    int64_t start = -1;  // Nothing currently claimable; drained() determines final exhaustion.\n    std::vector&lt;TaskT&gt; tasks;\n  };\n\n  explicit LockFreeRMALevel(Config config, int claim_width = 1)\n      : m_config(config),\n        m_claim_width(std::max(1, claim_width)),\n        m_comm(m_config.comm, MPICommunicator&lt;&gt;::Reference),\n        m_errors_seen(static_cast&lt;size_t&gt;(kMaxRecordedErrors)) {\n    initialize_window();\n  }\n\n  LockFreeRMALevel(const LockFreeRMALevel&amp;) = delete;\n  LockFreeRMALevel&amp; operator=(const LockFreeRMALevel&amp;) = delete;\n\n  ~LockFreeRMALevel() {\n    if (m_window != MPI_WIN_NULL) {\n      DYNAMPI_MPI_CHECK(MPI_Win_unlock_all, (m_window));\n      MPI_Barrier(m_config.comm);\n      DYNAMPI_MPI_CHECK(MPI_Win_free, (&amp;m_window));\n      m_window = MPI_WIN_NULL;\n    }\n  }\n\n  bool is_owner() const { return m_comm.rank() == m_config.owner_rank; }\n  int comm_size() const { return m_comm.size(); }\n  int claim_width() const { return m_claim_width; }\n\n  void idle_wait() {\n    if (m_window == MPI_WIN_NULL) {\n      // Singleton levels have no MPI window.\n      std::this_thread::yield();\n      return;\n    }\n    detail::rma_wait_idle(m_window, m_comm.get());\n  }\n\n  // Capacity must be checked before an irreversible parent claim.\n  int64_t owner_available_capacity() const {\n    assert(is_owner());\n    const int64_t in_flight = m_total_tasks - static_cast&lt;int64_t&gt;(m_owner_collected_count);\n    return static_cast&lt;int64_t&gt;(m_config.max_tasks) - in_flight;\n  }\n\n  // Publishes all tasks or returns false if ring space is temporarily\n  // insufficient. A batch larger than the ring is an error.\n  bool publish_tasks(const std::vector&lt;TaskT&gt;&amp; tasks) {\n    assert(is_owner());\n    if (tasks.empty()) return true;\n    const int64_t count = static_cast&lt;int64_t&gt;(tasks.size());\n    detail::check_task_capacity(0, tasks.size(), m_config.max_tasks, \"LockFreeRMALevel\");\n    if (count &gt; owner_available_capacity()) return false;\n\n    const int64_t start = m_total_tasks;\n    std::vector&lt;std::byte&gt; buffer(tasks.size() * m_task_slot_stride);\n    for (size_t i = 0; i &lt; tasks.size(); ++i) {\n      const TaskT&amp; task = tasks[i];\n      const int elem_count = MPI_Type&lt;TaskT&gt;::count(task);\n      assert(static_cast&lt;size_t&gt;(elem_count) &lt;= m_max_task_count &amp;&amp;\n             \"LockFreeRMALevel: task exceeds max_task_count\");\n      const size_t data_bytes = static_cast&lt;size_t&gt;(elem_count) * m_task_elem;\n      const size_t off = i * m_task_slot_stride;\n      detail::write_i64(buffer.data(), buffer.size(), off + T_COUNT, elem_count);\n      if (data_bytes &gt; 0) {\n        detail::write_bytes(buffer.data(), buffer.size(), off + T_DATA, MPI_Type&lt;TaskT&gt;::ptr(task),\n                            data_bytes);\n      }\n    }\n    put_ring_bytes(buffer.data(), start, count, m_task_slot_stride,\n                   [this](int64_t i) { return task_slot(i); });\n    // Clear stale completion entries before reusing ring slots.\n    std::vector&lt;std::byte&gt; log_clear(static_cast&lt;size_t&gt;(count) * LOG_ENTRY_BYTES, std::byte{0});\n    put_ring_bytes(log_clear.data(), start, count, LOG_ENTRY_BYTES,\n                   [this](int64_t i) { return log_slot(i); });\n\n    if (local_only()) {\n      m_total_tasks += count;\n      local_store_i64(TOTAL_OFF, m_total_tasks);\n      return true;\n    }\n    // Land the payload and log-clear Puts before the counter that makes them\n    // claimable moves; see the same barrier in LockFreeRMAWorkDistributor.\n    // The local_only path above needs none: it writes the window directly.\n    flush_remote();\n    m_total_tasks += count;\n    int64_t total_out = 0;\n    post_fetch_and_op(m_total_tasks, total_out, TOTAL_OFF, MPI_REPLACE);\n    flush_remote();\n    return true;\n  }\n\n  void mark_finished() {\n    assert(is_owner());\n    if (local_only()) {\n      local_store_i64(FINISHED_OFF, 1);\n    } else {\n      int64_t finished_out = 0;\n      post_fetch_and_op(static_cast&lt;int64_t&gt;(1), finished_out, FINISHED_OFF, MPI_REPLACE);\n      flush_remote();\n    }\n    idle_wait();\n  }\n\n  size_t owner_published_count() const {\n    assert(is_owner());\n    return static_cast&lt;size_t&gt;(m_total_tasks);\n  }\n  size_t owner_collected_count() const {\n    assert(is_owner());\n    return m_owner_collected_count;\n  }\n\n  // Completion estimate not blocked by lower-index gaps. It is only a pacing\n  // hint; ordered collection controls result access and ring reuse.\n  int64_t owner_unordered_completed_estimate() const {\n    assert(is_owner());\n    return m_owner_unordered_completed;\n  }\n\n  // Returns the newly completed contiguous result prefix.\n  std::vector&lt;ResultT&gt; harvest_ready_results() {\n    assert(is_owner());\n    const int64_t head_now = atomic_read(HEAD_OFF);\n    // Claims may outrun publication; never scan reused slots beyond this\n    // owner's currently published range.\n    const int64_t head_now_capped = std::min(head_now, m_total_tasks);\n\n    const int64_t frontier = static_cast&lt;int64_t&gt;(m_owner_collected_count);\n    if (head_now_capped &lt;= frontier) return {};\n\n    const int64_t scan_count = head_now_capped - frontier;\n    std::vector&lt;std::byte&gt; log_buf(static_cast&lt;size_t&gt;(scan_count) * LOG_ENTRY_BYTES);\n    get_ring_bytes_local(log_buf.data(), frontier, scan_count, LOG_ENTRY_BYTES,\n                         [this](int64_t i) { return log_slot(i); });\n\n    int64_t confirmed_end = frontier;\n    bool saw_error = false;\n    while (confirmed_end &lt; head_now_capped) {\n      const size_t off = static_cast&lt;size_t&gt;(confirmed_end - frontier) * LOG_ENTRY_BYTES;\n      const int64_t entry = detail::read_i64(log_buf.data(), log_buf.size(), off);\n      if (entry == 0) break;  // gap: not yet written; stop the contiguous prefix here\n      if (entry &lt; 0) saw_error = true;\n      confirmed_end += entry &lt; 0 ? -entry : entry;\n    }\n    // A negative run length indicates at least one task error.\n    if (saw_error) harvest_task_errors();\n\n    // Continue past ordering gaps to estimate total completed work.\n    int64_t unordered_total = confirmed_end - frontier;\n    for (int64_t pos = confirmed_end; pos &lt; head_now_capped;) {\n      const size_t off = static_cast&lt;size_t&gt;(pos - frontier) * LOG_ENTRY_BYTES;\n      const int64_t entry = detail::read_i64(log_buf.data(), log_buf.size(), off);\n      if (entry == 0) {\n        ++pos;\n        continue;\n      }\n      const int64_t run = entry &lt; 0 ? -entry : entry;\n      unordered_total += run;\n      pos += run;\n    }\n    m_owner_unordered_completed = frontier + unordered_total;\n\n    if (confirmed_end &lt;= frontier) return {};\n\n    const int64_t n = confirmed_end - frontier;\n    std::vector&lt;std::byte&gt; result_buf(static_cast&lt;size_t&gt;(n) * m_result_slot_stride);\n    get_ring_bytes_local(result_buf.data(), frontier, n, m_result_slot_stride,\n                         [this](int64_t i) { return result_slot(i); });\n\n    std::vector&lt;ResultT&gt; output;\n    output.reserve(static_cast&lt;size_t&gt;(n));\n    for (int64_t i = 0; i &lt; n; ++i) {\n      const size_t off = static_cast&lt;size_t&gt;(i) * m_result_slot_stride;\n      const int64_t count = detail::read_i64(result_buf.data(), result_buf.size(), off + R_COUNT);\n      ResultT result{};\n      if constexpr (MPI_Type&lt;ResultT&gt;::resize_required)\n        MPI_Type&lt;ResultT&gt;::resize(result, static_cast&lt;int&gt;(count));\n      const size_t data_bytes = count &gt; 0 ? static_cast&lt;size_t&gt;(count) * m_result_elem : size_t{0};\n      detail::read_result_bytes(result_buf.data(), result_buf.size(), off + R_DATA, result,\n                                data_bytes);\n      output.push_back(std::move(result));\n    }\n    m_owner_collected_count = static_cast&lt;size_t&gt;(confirmed_end);\n    return output;\n  }\n\n  // Harvest once, yielding if no result became available.\n  std::vector&lt;ResultT&gt; harvest_ready_results_throttled() {\n    const size_t before = owner_collected_count();\n    auto results = harvest_ready_results();\n    if (owner_collected_count() == before) idle_wait();\n    return results;\n  }\n\n  // Attempts one non-blocking claim. Owners may self-claim when they also\n  // lead a child subtree. start=-1 means no work is currently available.\n  ClaimedRange try_claim() {\n    if (m_pending_start != -1) {\n      const int64_t total = atomic_read(TOTAL_OFF);\n      const int64_t ready = std::min(m_pending_end, total);\n      if (ready &gt; m_pending_start) {\n        const int64_t start = m_pending_start;\n        const int64_t count = ready - m_pending_start;\n        m_pending_start = ready;\n        if (m_pending_start &gt;= m_pending_end || m_seen_finished) m_pending_start = -1;\n        return ClaimedRange{start, read_task_batch(start, count)};\n      }\n      if (m_pending_start &gt;= m_pending_end || m_seen_finished) m_pending_start = -1;\n      return ClaimedRange{};\n    }\n\n    // A stale total must remain refreshable after FINISHED is observed.\n    if (m_cached_head &gt;= m_cached_total &amp;&amp; !m_seen_finished)\n      m_cached_total = atomic_read(TOTAL_OFF);\n\n    if (m_cached_head &lt; m_cached_total) {\n      const int64_t claim = std::min&lt;int64_t&gt;(m_claim_width, m_cached_total - m_cached_head);\n      const int64_t start = fetch_add(HEAD_OFF, claim);\n      const int64_t end = start + claim;\n      m_cached_head = end;\n\n      const int64_t total = (end &lt;= m_cached_total) ? m_cached_total : atomic_read(TOTAL_OFF);\n      // Publication may trail the entire claim, so clamp to its owned range.\n      const int64_t ready = std::clamp(total, start, end);\n      if (ready &lt; end) {\n        m_pending_start = ready;\n        m_pending_end = end;\n      }\n      if (ready &gt; start) {\n        return ClaimedRange{start, read_task_batch(start, ready - start)};\n      }\n    }\n    return ClaimedRange{};\n  }\n\n  // Refreshes FINISHED without charging every try_claim() for the RMA read.\n  bool check_finished() {\n    if (!m_seen_finished &amp;&amp; atomic_read(FINISHED_OFF) != 0) m_seen_finished = true;\n    return m_seen_finished;\n  }\n\n  // True when FINISHED is observed and no published or pending work remains.\n  bool drained() {\n    // Refresh before the pending check so an unpublished remainder can expire.\n    check_finished();\n    if (m_pending_start != -1) return false;\n    if (!m_seen_finished) return false;\n    const int64_t final_total = atomic_read(TOTAL_OFF);\n    if (m_cached_head &gt;= final_total) return true;\n    m_cached_total = final_total;  // real unclaimed work remains; try_claim() will pick it up\n    return false;\n  }\n\n  // Publishes a worker failure before its result range advertises completion.\n  void report_task_error(const TaskError&amp; error) {\n    const int64_t slot = fetch_add(ERROR_COUNT_OFF, 1);\n    if (slot &gt;= kMaxRecordedErrors) return;\n    // Publish payload before readiness; rank+1 preserves zero as the sentinel.\n    std::vector&lt;std::byte&gt; message_bytes(kMaxTaskErrorMessage, std::byte{0});\n    const size_t bytes = std::min(error.message.size(), kMaxTaskErrorMessage - 1);\n    if (bytes &gt; 0) {\n      detail::write_bytes(message_bytes.data(), message_bytes.size(), 0, error.message.data(),\n                          bytes);\n    }\n    const int64_t ready = static_cast&lt;int64_t&gt;(error.worker_rank) + 1;\n    if (local_only()) {\n      // LCOV_EXCL_START -- singleton owned upper group\n      detail::write_bytes(m_window_buffer.data(), m_window_buffer.size(),\n                          static_cast&lt;size_t&gt;(error_slot(slot)) + E_DATA, message_bytes.data(),\n                          message_bytes.size());\n      local_store_i64(error_slot(slot) + static_cast&lt;MPI_Aint&gt;(E_RANK), ready);\n      return;\n      // LCOV_EXCL_STOP\n    }\n    post_put_bytes(message_bytes.data(), message_bytes.size(),\n                   error_slot(slot) + static_cast&lt;MPI_Aint&gt;(E_DATA));\n    flush_remote();\n    post_put_bytes(&amp;ready, sizeof(ready), error_slot(slot) + static_cast&lt;MPI_Aint&gt;(E_RANK));\n    flush_remote();\n  }\n\n  std::vector&lt;TaskError&gt; take_errors() {\n    std::vector&lt;TaskError&gt; taken;\n    taken.swap(m_owner_errors);\n    return taken;\n  }\n\n  // Writes result data before publishing its completion-log entry.\n  void write_result_range(int64_t start, const std::vector&lt;ResultT&gt;&amp; results,\n                          bool contains_error = false) {\n    const int64_t count = static_cast&lt;int64_t&gt;(results.size());\n    assert(count &gt; 0);\n    std::vector&lt;std::byte&gt; buffer(static_cast&lt;size_t&gt;(count) * m_result_slot_stride);\n    for (int64_t i = 0; i &lt; count; ++i) {\n      const ResultT&amp; result = results[static_cast&lt;size_t&gt;(i)];\n      const int elem_count = MPI_Type&lt;ResultT&gt;::count(result);\n      assert(elem_count &gt;= 0);\n      const size_t data_bytes =\n          elem_count &gt; 0 ? static_cast&lt;size_t&gt;(elem_count) * m_result_elem : size_t{0};\n      const size_t off = static_cast&lt;size_t&gt;(i) * m_result_slot_stride;\n      detail::write_i64(buffer.data(), buffer.size(), off + R_COUNT, elem_count);\n      if (data_bytes &gt; 0) {\n        detail::write_bytes(buffer.data(), buffer.size(), off + R_DATA,\n                            MPI_Type&lt;ResultT&gt;::ptr(result), data_bytes);\n      }\n    }\n    const int64_t entry = contains_error ? -count : count;\n    // One log entry covers the range; only result data can cross a ring lap.\n    put_ring_bytes(buffer.data(), start, count, m_result_slot_stride,\n                   [this](int64_t i) { return result_slot(i); });\n    if (local_only()) {\n      local_store_i64(log_slot(start), entry);\n      return;\n    }\n    flush_remote();\n    post_put_bytes(&amp;entry, sizeof(entry), log_slot(start));\n    flush_remote();\n  }\n\n private:\n  // Window layout: controls, task ring, result ring, completion log, errors.\n  static constexpr MPI_Aint HEAD_OFF = 0;\n  static constexpr MPI_Aint TOTAL_OFF = 8;\n  static constexpr MPI_Aint FINISHED_OFF = 16;\n  static constexpr MPI_Aint ERROR_COUNT_OFF = 24;\n  static constexpr size_t CONTROL_BYTES = 32;\n\n  static constexpr int64_t kMaxRecordedErrors = 16;\n  static constexpr size_t E_RANK = 0;\n  static constexpr size_t E_DATA = 8;\n  static constexpr size_t ERROR_SLOT_BYTES = E_DATA + kMaxTaskErrorMessage;\n\n  static constexpr size_t T_COUNT = 0;\n  static constexpr size_t T_DATA = 8;\n  static constexpr size_t R_COUNT = 0;\n  static constexpr size_t R_DATA = 8;\n  static constexpr size_t LOG_ENTRY_BYTES = 8;\n\n  Config m_config;\n  int m_claim_width;\n  MPICommunicator&lt;&gt; m_comm;\n  MPI_Win m_window = MPI_WIN_NULL;\n  std::vector&lt;std::byte&gt; m_window_buffer;\n  alignas(int64_t) std::byte m_peer_window[sizeof(int64_t)]{};\n\n  size_t m_task_elem = 0;\n  size_t m_result_elem = 0;\n  size_t m_max_task_count = 1;\n  size_t m_max_result_count = 1;\n  size_t m_task_slot_stride = 0;\n  size_t m_result_slot_stride = 0;\n  size_t m_task_base = 0;\n  size_t m_result_base = 0;\n  size_t m_log_base = 0;\n  size_t m_error_base = 0;\n\n  int64_t m_total_tasks = 0;\n  size_t m_owner_collected_count = 0;\n  int64_t m_owner_unordered_completed = 0;\n\n  // Claimant-only state (see try_claim()/drained()).\n  int64_t m_cached_head = 0;\n  int64_t m_cached_total = 0;\n  int64_t m_pending_start = -1;\n  int64_t m_pending_end = -1;\n  bool m_seen_finished = false;\n  std::vector&lt;bool&gt; m_errors_seen;        // owner-side: error slots already consumed\n  std::vector&lt;TaskError&gt; m_owner_errors;  // owner-side, drained by take_errors()\n\n  void initialize_window() {\n    check_fixed_size_mpi_type&lt;TaskT&gt;(\"task\", \"LockFreeRMALevel\");\n    check_fixed_size_mpi_type&lt;ResultT&gt;(\"result\", \"LockFreeRMALevel\");\n\n    m_task_elem = static_cast&lt;size_t&gt;(detail::mpi_type_size_bytes&lt;TaskT&gt;());\n    m_result_elem = static_cast&lt;size_t&gt;(detail::mpi_type_size_bytes&lt;ResultT&gt;());\n    m_max_task_count = MPI_Type&lt;TaskT&gt;::resize_required\n                           ? static_cast&lt;size_t&gt;(m_config.max_task_count)\n                           : static_cast&lt;size_t&gt;(mpi_elements_per_value&lt;TaskT&gt;());\n    m_max_result_count = MPI_Type&lt;ResultT&gt;::resize_required\n                             ? static_cast&lt;size_t&gt;(m_config.max_result_count)\n                             : static_cast&lt;size_t&gt;(mpi_elements_per_value&lt;ResultT&gt;());\n\n    m_task_slot_stride = detail::round_up_8(T_DATA + m_max_task_count * m_task_elem);\n    m_result_slot_stride = detail::round_up_8(R_DATA + m_max_result_count * m_result_elem);\n\n    const size_t capacity = static_cast&lt;size_t&gt;(m_config.max_tasks);\n    m_task_base = CONTROL_BYTES;\n    m_result_base = m_task_base + capacity * m_task_slot_stride;\n    m_log_base = m_result_base + capacity * m_result_slot_stride;\n    m_error_base = m_log_base + capacity * LOG_ENTRY_BYTES;\n    const size_t owner_window_bytes =\n        m_error_base + static_cast&lt;size_t&gt;(kMaxRecordedErrors) * ERROR_SLOT_BYTES;\n\n    // Some MPI implementations reject windows on singleton communicators.\n    if (is_owner()) {\n      m_window_buffer.resize(owner_window_bytes);\n    }\n    if (comm_size() == 1) return;\n\n    void* base = nullptr;\n    MPI_Aint bsize = 0;\n    if (is_owner()) {\n      base = m_window_buffer.data();\n      bsize = static_cast&lt;MPI_Aint&gt;(m_window_buffer.size());\n    } else {\n      base = m_peer_window;\n      bsize = static_cast&lt;MPI_Aint&gt;(sizeof(m_peer_window));\n    }\n    DYNAMPI_MPI_CHECK(MPI_Win_create, (base, bsize, 1, MPI_INFO_NULL, m_config.comm, &amp;m_window));\n    DYNAMPI_MPI_CHECK(MPI_Win_lock_all, (MPI_MODE_NOCHECK, m_window));\n  }\n\n  // publish_tasks() prevents live indices from colliding modulo max_tasks.\n  int64_t ring_slot(int64_t index) const {\n    return index % static_cast&lt;int64_t&gt;(m_config.max_tasks);\n  }\n  MPI_Aint task_slot(int64_t index) const {\n    return static_cast&lt;MPI_Aint&gt;(m_task_base +\n                                 static_cast&lt;size_t&gt;(ring_slot(index)) * m_task_slot_stride);\n  }\n  MPI_Aint result_slot(int64_t index) const {\n    return static_cast&lt;MPI_Aint&gt;(m_result_base +\n                                 static_cast&lt;size_t&gt;(ring_slot(index)) * m_result_slot_stride);\n  }\n  MPI_Aint log_slot(int64_t index) const {\n    return static_cast&lt;MPI_Aint&gt;(m_log_base +\n                                 static_cast&lt;size_t&gt;(ring_slot(index)) * LOG_ENTRY_BYTES);\n  }\n  // Error slots are fixed and not ring-indexed.\n  MPI_Aint error_slot(int64_t index) const {\n    return static_cast&lt;MPI_Aint&gt;(m_error_base + static_cast&lt;size_t&gt;(index) * ERROR_SLOT_BYTES);\n  }\n\n  void flush_remote() { DYNAMPI_MPI_CHECK(MPI_Win_flush, (m_config.owner_rank, m_window)); }\n  void flush_local() { DYNAMPI_MPI_CHECK(MPI_Win_flush_local, (m_config.owner_rank, m_window)); }\n\n  // Size-1 levels have no MPI window; owner buffer is the sole storage.\n  bool local_only() const { return m_window == MPI_WIN_NULL; }\n\n  int64_t local_load_i64(MPI_Aint offset) const {\n    return detail::read_i64(m_window_buffer.data(), m_window_buffer.size(),\n                            static_cast&lt;size_t&gt;(offset));\n  }\n  void local_store_i64(MPI_Aint offset, int64_t value) {\n    detail::write_i64(m_window_buffer.data(), m_window_buffer.size(), static_cast&lt;size_t&gt;(offset),\n                      value);\n  }\n\n  void post_fetch_and_op(int64_t in, int64_t&amp; out, MPI_Aint offset, MPI_Op op) {\n    m_comm.fetch_and_op(in, out, m_config.owner_rank, offset, op, m_window);\n  }\n  void post_put_bytes(const void* src, size_t n, MPI_Aint offset) {\n    m_comm.put_bytes(src, n, m_config.owner_rank, offset, m_window);\n  }\n  void post_get_bytes(void* dst, size_t n, MPI_Aint offset) {\n    m_comm.get_bytes(dst, n, m_config.owner_rank, offset, m_window);\n  }\n\n  int64_t atomic_read(MPI_Aint offset) {\n    if (local_only()) return local_load_i64(offset);\n    int64_t in = 0, out;\n    post_fetch_and_op(in, out, offset, MPI_NO_OP);\n    flush_local();\n    return out;\n  }\n  int64_t fetch_add(MPI_Aint offset, int64_t increment) {\n    if (local_only()) {\n      const int64_t out = local_load_i64(offset);\n      local_store_i64(offset, out + increment);\n      return out;\n    }\n    int64_t in = increment, out;\n    post_fetch_and_op(in, out, offset, MPI_SUM);\n    flush_local();\n    return out;\n  }\n\n  void get_bytes_local(void* dst, size_t n, MPI_Aint offset) {\n    if (local_only()) {\n      detail::read_bytes(dst, n, m_window_buffer.data(), m_window_buffer.size(),\n                         static_cast&lt;size_t&gt;(offset), n);\n      return;\n    }\n    post_get_bytes(dst, n, offset);\n    flush_local();\n  }\n\n  // Issues one local or remote chunk; callers flush after all chunks.\n  void put_bytes_owner(const void* src, size_t n, MPI_Aint offset) {\n    if (local_only()) {\n      detail::write_bytes(m_window_buffer.data(), m_window_buffer.size(),\n                          static_cast&lt;size_t&gt;(offset), src, n);\n      return;\n    }\n    post_put_bytes(src, n, offset);\n  }\n\n  // Splits a write when its logical range crosses the ring boundary.\n  template &lt;typename SlotFn&gt;\n  void put_ring_bytes(const void* src, int64_t start, int64_t count, size_t stride,\n                      SlotFn&amp;&amp; slot_fn) {\n    const int64_t ring = static_cast&lt;int64_t&gt;(m_config.max_tasks);\n    const int64_t first_count = std::min(count, ring - ring_slot(start));\n    const auto* bytes = static_cast&lt;const std::byte*&gt;(src);\n    put_bytes_owner(bytes, static_cast&lt;size_t&gt;(first_count) * stride, slot_fn(start));\n    if (first_count &lt; count) {\n      put_bytes_owner(bytes + static_cast&lt;size_t&gt;(first_count) * stride,\n                      static_cast&lt;size_t&gt;(count - first_count) * stride,\n                      slot_fn(start + first_count));\n    }\n  }\n\n  // Read counterpart of put_ring_bytes().\n  template &lt;typename SlotFn&gt;\n  void get_ring_bytes_local(void* dst, int64_t start, int64_t count, size_t stride,\n                            SlotFn&amp;&amp; slot_fn) {\n    const int64_t ring = static_cast&lt;int64_t&gt;(m_config.max_tasks);\n    const int64_t first_count = std::min(count, ring - ring_slot(start));\n    get_bytes_local(dst, static_cast&lt;size_t&gt;(first_count) * stride, slot_fn(start));\n    if (first_count &lt; count) {\n      get_bytes_local(static_cast&lt;std::byte*&gt;(dst) + static_cast&lt;size_t&gt;(first_count) * stride,\n                      static_cast&lt;size_t&gt;(count - first_count) * stride,\n                      slot_fn(start + first_count));\n    }\n  }\n\n  void harvest_task_errors() {\n    const int64_t claimed = std::min(atomic_read(ERROR_COUNT_OFF), kMaxRecordedErrors);\n    if (claimed &lt;= 0) return;  // LCOV_EXCL_LINE -- only reachable on a spurious flag\n    // Read ready slots independently because writers complete out of order.\n    std::vector&lt;std::byte&gt; buf(static_cast&lt;size_t&gt;(claimed) * ERROR_SLOT_BYTES);\n    get_bytes_local(buf.data(), buf.size(), error_slot(0));\n    for (int64_t i = 0; i &lt; claimed; ++i) {\n      if (m_errors_seen[static_cast&lt;size_t&gt;(i)]) continue;\n      const size_t off = static_cast&lt;size_t&gt;(i) * ERROR_SLOT_BYTES;\n      const int64_t ready = detail::read_i64(buf.data(), buf.size(), off + E_RANK);\n      if (ready == 0) continue;  // still in flight; its own flag will bring us back\n      TaskError error;\n      error.worker_rank = static_cast&lt;int&gt;(ready - 1);\n      const char* text = reinterpret_cast&lt;const char*&gt;(buf.data() + off + E_DATA);\n      error.message.assign(text, ::strnlen(text, kMaxTaskErrorMessage - 1));\n      m_owner_errors.push_back(std::move(error));\n      m_errors_seen[static_cast&lt;size_t&gt;(i)] = true;\n    }\n  }\n\n  std::vector&lt;TaskT&gt; read_task_batch(int64_t index, int64_t count) {\n    const size_t bytes = static_cast&lt;size_t&gt;(count) * m_task_slot_stride;\n    std::vector&lt;std::byte&gt; buf(bytes);\n    get_ring_bytes_local(buf.data(), index, count, m_task_slot_stride,\n                         [this](int64_t i) { return task_slot(i); });\n\n    std::vector&lt;TaskT&gt; tasks;\n    tasks.reserve(static_cast&lt;size_t&gt;(count));\n    for (int64_t i = 0; i &lt; count; ++i) {\n      const size_t slot_offset = static_cast&lt;size_t&gt;(i) * m_task_slot_stride;\n      const int64_t elem_count = detail::read_i64(buf.data(), buf.size(), slot_offset + T_COUNT);\n      TaskT task{};\n      if constexpr (MPI_Type&lt;TaskT&gt;::resize_required)\n        MPI_Type&lt;TaskT&gt;::resize(task, static_cast&lt;int&gt;(elem_count));\n      const size_t data_bytes = static_cast&lt;size_t&gt;(elem_count) * m_task_elem;\n      detail::read_result_bytes(buf.data(), buf.size(), slot_offset + T_DATA, task, data_bytes);\n      tasks.push_back(std::move(task));\n    }\n    return tasks;\n  }\n};\n\n}  // namespace detail\n\n// Node-aware tree of one-sided RMA levels. Managers relay claimed tasks down\n// and contiguous result prefixes up. Ordering is preserved across every level;\n// task prioritization and detailed statistics are unsupported.\ntemplate &lt;typename TaskT, typename ResultT, typename... Options&gt;\nclass HierarchicalLockFreeRMAWorkDistributor {\n public:\n  struct Config {\n    MPI_Comm comm = MPI_COMM_WORLD;\n    int manager_rank = 0;\n    bool auto_run_workers = true;\n    int max_tasks = 8192;        // Upper-level ring capacity\n    int max_local_tasks = 8192;  // Per-node ring capacity\n    int max_task_count = 256;\n    int max_result_count = 256;\n    // 0 keeps one local group per node; positive values partition it into\n    // contiguous groups of at most this size.\n    int max_local_group_size = 0;\n    // &lt;0 selects fanout automatically, 0 keeps a flat upper layer, and &gt;0\n    // recursively caps direct upper-level claimants.\n    int max_upper_fanout = -1;\n\n    // Parent claim rounds allowed ahead of child completion.\n    int max_pending_rounds = 2;\n\n    // Rethrow worker failures at the root; otherwise collect them for\n    // take_task_errors().\n    bool rethrow_task_errors = true;\n  };\n\n  struct RunConfig {\n    size_t target_num_tasks = std::numeric_limits&lt;size_t&gt;::max();\n    bool allow_more_than_target_tasks = true;\n    std::optional&lt;double&gt; max_seconds = std::nullopt;\n  };\n\n  static const bool ordered = true;\n\n  explicit HierarchicalLockFreeRMAWorkDistributor(std::function&lt;ResultT(TaskT)&gt; worker_function,\n                                                  Config config = {})\n      : m_config(config),\n        m_world_comm(config.comm, MPICommunicator&lt;&gt;::Duplicate),\n        m_worker_function(std::move(worker_function)) {\n    setup_topology();\n    setup_levels();\n\n    // Ensure every rank finishes communicator and window setup before use.\n    DYNAMPI_MPI_CHECK(MPI_Barrier, (m_world_comm.get()));\n\n    if (m_config.auto_run_workers &amp;&amp; !is_root_manager()) run_worker();\n  }\n\n  // Tasks that threw, oldest first, removed as they are returned. See\n  // Config::rethrow_task_errors.\n  [[nodiscard]] std::vector&lt;TaskError&gt; take_task_errors() {\n    assert(is_root_manager());\n    return m_task_errors.take();\n  }\n\n  bool has_task_errors() const {\n    assert(is_root_manager());\n    return !m_task_errors.empty();\n  }\n\n  ~HierarchicalLockFreeRMAWorkDistributor() {\n    if (!m_finalized) finalize();\n    m_task_errors.warn_if_unreported(\"HierarchicalLockFreeRMAWorkDistributor\");\n    if (!m_solo) {\n      // Destroy every subgroup window before any rank starts another instance.\n      m_parent_level.reset();\n      m_owned_upper_levels.clear();\n      m_local_level.reset();\n      m_upper_comms.clear();\n      m_local_comm.reset();\n      DYNAMPI_MPI_CHECK(MPI_Barrier, (m_world_comm.get()));\n    }\n  }\n\n  bool is_root_manager() const { return m_world_comm.rank() == m_config.manager_rank; }\n\n  size_t remaining_tasks_count() const {\n    assert(is_root_manager());\n    if (m_solo) return m_local_task_store.size() - m_local_collected_count;\n    const auto&amp; top = top_level();\n    return top.owner_published_count() - top.owner_collected_count() - m_results.size();\n  }\n\n  void insert_task(TaskT task) {\n    assert(is_root_manager());\n    insert_tasks(std::vector&lt;TaskT&gt;{std::move(task)});\n  }\n\n  void insert_tasks(const std::vector&lt;TaskT&gt;&amp; tasks) {\n    assert(is_root_manager());\n    if (m_solo) {\n      m_local_task_store.insert(m_local_task_store.end(), tasks.begin(), tasks.end());\n      return;\n    }\n    auto&amp; top = top_level();\n    // This blocking API harvests until enough ring space is available.\n    while (!top.publish_tasks(tasks)) {\n      harvest_top_level(true);\n    }\n  }\n\n  [[nodiscard]] std::vector&lt;ResultT&gt; run_tasks(const RunConfig&amp; config = RunConfig{}) {\n    assert(is_root_manager());\n    Timer timer;\n\n    if (m_solo) {\n      while (m_local_collected_count &lt; m_local_task_store.size()) {\n        if (m_results.size() &gt;= config.target_num_tasks) break;\n        if (config.max_seconds &amp;&amp; timer.elapsed().count() &gt;= *config.max_seconds) break;\n        run_one_solo_task();\n      }\n    } else {\n      auto&amp; top = top_level();\n      while (true) {\n        if (m_results.size() &gt;= config.target_num_tasks) break;\n        if (config.max_seconds &amp;&amp; timer.elapsed().count() &gt;= *config.max_seconds) break;\n        if (top.owner_collected_count() &gt;= top.owner_published_count()) break;\n        harvest_top_level(true);\n      }\n    }\n\n    // Thrown before draining, so results collected so far stay buffered for\n    // whoever catches this and calls again.\n    m_task_errors.rethrow_first_if(m_config.rethrow_task_errors);\n\n    return drain_results(config.allow_more_than_target_tasks ? std::numeric_limits&lt;size_t&gt;::max()\n                                                             : config.target_num_tasks);\n  }\n\n  [[nodiscard]] std::vector&lt;ResultT&gt; finish_remaining_tasks() { return run_tasks({}); }\n\n  // One non-looping harvest snapshot.\n  [[nodiscard]] std::vector&lt;ResultT&gt; gather_once() {\n    assert(is_root_manager());\n    if (m_solo) {\n      while (m_local_collected_count &lt; m_local_task_store.size()) {\n        run_one_solo_task();\n      }\n    } else {\n      harvest_top_level(false);\n    }\n    return drain_results(std::numeric_limits&lt;size_t&gt;::max());\n  }\n\n  void finalize() {\n    assert(!m_finalized);\n    if (is_root_manager()) {\n      if (m_solo) {\n        while (m_local_collected_count &lt; m_local_task_store.size()) {\n          run_one_solo_task();\n        }\n      } else {\n        auto&amp; top = top_level();\n        while (top.owner_collected_count() &lt; top.owner_published_count()) {\n          harvest_top_level(true);\n        }\n        // Each lower level is marked finished when its own parent drains.\n        top.mark_finished();\n      }\n    }\n    m_finalized = true;\n  }\n\n  void run_worker() {\n    assert(!is_root_manager());\n    const bool has_upper_chain = m_parent_level.has_value() || !m_owned_upper_levels.empty();\n    if (m_local_level &amp;&amp; m_local_comm-&gt;size() &gt; 1) {\n      if (has_upper_chain) {\n        run_node_manager();\n      } else {\n        run_local_worker();\n      }\n    } else if (has_upper_chain) {\n      run_leaf_leader_worker();\n    }\n  }\n\n private:\n  using RMALevel = detail::LockFreeRMALevel&lt;TaskT, ResultT&gt;;\n\n  Config m_config;\n  MPICommunicator&lt;&gt; m_world_comm;\n  std::function&lt;ResultT(TaskT)&gt; m_worker_function;\n  const bool m_solo = (m_world_comm.size() == 1);\n\n  std::optional&lt;MPICommunicator&lt;&gt;&gt; m_local_comm;\n  std::optional&lt;detail::LockFreeRMALevel&lt;TaskT, ResultT&gt;&gt; m_local_level;\n\n  // Owns communicators referenced by upper-level RMA objects.\n  std::vector&lt;MPICommunicator&lt;&gt;&gt; m_upper_comms;\n\n  // Top-to-bottom levels owned by this rank. A deque keeps non-movable RMA\n  // levels at stable addresses.\n  std::deque&lt;detail::LockFreeRMALevel&lt;TaskT, ResultT&gt;&gt; m_owned_upper_levels;\n\n  // Immediate parent level, claimed but not owned by this rank.\n  std::optional&lt;detail::LockFreeRMALevel&lt;TaskT, ResultT&gt;&gt; m_parent_level;\n\n  bool m_finalized = false;\n  detail::TaskErrorLog m_task_errors;  // root manager only\n  std::vector&lt;ResultT&gt; m_results;\n\n  std::vector&lt;TaskT&gt; m_local_task_store;\n  size_t m_local_collected_count = 0;\n\n  RMALevel&amp; top_level() {\n    assert(!m_owned_upper_levels.empty());\n    return m_owned_upper_levels.front();\n  }\n\n  const RMALevel&amp; top_level() const {\n    assert(!m_owned_upper_levels.empty());\n    return m_owned_upper_levels.front();\n  }\n\n  void harvest_top_level(bool throttled) {\n    auto&amp; top = top_level();\n    auto results = throttled ? top.harvest_ready_results_throttled() : top.harvest_ready_results();\n    collect_level_errors(top);\n    m_results.insert(m_results.end(), std::make_move_iterator(results.begin()),\n                     std::make_move_iterator(results.end()));\n  }\n\n  void setup_topology() {\n    if (m_solo) return;\n\n    auto local_opt = detail::split_local_worker_communicator(m_world_comm, m_config.manager_rank,\n                                                             m_config.max_local_group_size);\n    if (local_opt.has_value()) m_local_comm.emplace(std::move(*local_opt));\n\n    const bool is_manager = (m_world_comm.rank() == m_config.manager_rank);\n    bool is_node_manager = false;\n    if (m_local_comm.has_value()) is_node_manager = (m_local_comm-&gt;rank() == 0);\n\n    const int leader_color = (is_manager || is_node_manager) ? 0 : MPI_UNDEFINED;\n    auto leader_opt = m_world_comm.split(leader_color, m_world_comm.rank());\n    if (!leader_opt.has_value()) return;  // plain local worker: no upper-chain role at all\n\n    setup_upper_chain(std::move(*leader_opt), is_manager);\n  }\n\n  enum class UpperLevelRole { Owned, Parent };\n\n  void emplace_upper_level(MPICommunicator&lt;&gt; comm, int owner_rank, int claim_width,\n                           UpperLevelRole role) {\n    m_upper_comms.push_back(std::move(comm));\n    typename detail::LockFreeRMALevel&lt;TaskT, ResultT&gt;::Config cfg;\n    cfg.comm = m_upper_comms.back().get();\n    cfg.owner_rank = owner_rank;\n    cfg.max_tasks = m_config.max_tasks;\n    cfg.max_task_count = m_config.max_task_count;\n    cfg.max_result_count = m_config.max_result_count;\n    if (role == UpperLevelRole::Owned) {\n      m_owned_upper_levels.emplace_back(cfg, claim_width);\n    } else {\n      m_parent_level.emplace(cfg, claim_width);\n    }\n  }\n\n  // Builds the k-ary upper RMA chain. Every rank in each input communicator\n  // must reach its matching MPI_Comm_split, including ranks not promoted.\n  void setup_upper_chain(MPICommunicator&lt;&gt; flat_comm, bool is_manager) {\n    MPIGroup world_group(m_world_comm);\n    const int local_children = m_local_comm.has_value() ? std::max(0, m_local_comm-&gt;size() - 1) : 0;\n    // A claim must fit the child ring into which it is republished.\n    const int leaf_claim_width = std::clamp(local_children, 1, m_config.max_local_tasks);\n\n    const int manager_count = flat_comm.size() - 1;\n    const int effective_fanout =\n        detail::resolve_upper_fanout(manager_count, m_config.max_upper_fanout);\n\n    if (manager_count &lt;= effective_fanout) {\n      MPIGroup flat_group(flat_comm);\n      const int owner_rank = world_group.translate_rank(m_config.manager_rank, flat_group);\n      if (flat_comm.rank() == owner_rank) {\n        emplace_upper_level(std::move(flat_comm), owner_rank, leaf_claim_width,\n                            UpperLevelRole::Owned);\n      } else {\n        emplace_upper_level(std::move(flat_comm), owner_rank, leaf_claim_width,\n                            UpperLevelRole::Parent);\n      }\n      return;\n    }\n\n    // Collective over flat_comm; only node managers join the result.\n    auto managers_opt = flat_comm.split(is_manager ? MPI_UNDEFINED : 0, flat_comm.rank());\n\n    bool is_final_round_leader = false;\n    int feed_width = leaf_claim_width;\n    if (!is_manager) {\n      // optional::emplace works around MPICommunicator's deleted move assignment.\n      std::optional&lt;MPICommunicator&lt;&gt;&gt; round_comm(std::move(*managers_opt));\n      while (true) {\n        if (round_comm-&gt;size() &lt;= effective_fanout) {\n          is_final_round_leader = true;\n          break;\n        }\n        const int color = round_comm-&gt;rank() / effective_fanout;\n        auto group_opt = round_comm-&gt;split(color, round_comm-&gt;rank());\n        MPICommunicator&lt;&gt; group_comm = std::move(*group_opt);\n        const bool is_group_leader = (group_comm.rank() == 0);\n        const int group_feed_width =\n            detail::sum_subtree_widths_to_group_leader(feed_width, group_comm);\n\n        auto leaders_opt =\n            round_comm-&gt;split(is_group_leader ? 0 : MPI_UNDEFINED, round_comm-&gt;rank());\n\n        if (!is_group_leader) {\n          emplace_upper_level(std::move(group_comm), 0, feed_width, UpperLevelRole::Parent);\n          break;\n        }\n        emplace_upper_level(std::move(group_comm), 0, feed_width, UpperLevelRole::Owned);\n        // Upper levels share max_tasks as their ring capacity.\n        feed_width = std::clamp(group_feed_width, 1, m_config.max_tasks);\n        round_comm.emplace(std::move(*leaders_opt));\n      }\n    }\n\n    // Collective over the original flat_comm attaches final leaders to root.\n    auto top_opt = flat_comm.split((is_manager || is_final_round_leader) ? 0 : MPI_UNDEFINED,\n                                   flat_comm.rank());\n    if (top_opt.has_value()) {\n      MPIGroup top_group(*top_opt);\n      const int owner_rank = world_group.translate_rank(m_config.manager_rank, top_group);\n      if (is_manager) {\n        emplace_upper_level(std::move(*top_opt), owner_rank, feed_width, UpperLevelRole::Owned);\n      } else {\n        emplace_upper_level(std::move(*top_opt), owner_rank, feed_width, UpperLevelRole::Parent);\n      }\n    }\n  }\n\n  void setup_levels() {\n    // Singleton local groups execute directly against the upper chain.\n    if (m_local_comm.has_value() &amp;&amp; m_local_comm-&gt;size() &gt; 1) {\n      typename detail::LockFreeRMALevel&lt;TaskT, ResultT&gt;::Config local_cfg;\n      local_cfg.comm = m_local_comm-&gt;get();\n      local_cfg.owner_rank = 0;\n      local_cfg.max_tasks = m_config.max_local_tasks;\n      local_cfg.max_task_count = m_config.max_task_count;\n      local_cfg.max_result_count = m_config.max_result_count;\n      m_local_level.emplace(local_cfg);\n    }\n  }\n\n  // Maps child result positions back to their claimed parent ranges.\n  struct PendingRelay {\n    int64_t parent_start;\n    int64_t child_len;\n  };\n  struct BridgeHop {\n    BridgeHop(detail::LockFreeRMALevel&lt;TaskT, ResultT&gt;* p,\n              detail::LockFreeRMALevel&lt;TaskT, ResultT&gt;* c)\n        : parent(p), child(c) {}\n    detail::LockFreeRMALevel&lt;TaskT, ResultT&gt;* parent;\n    detail::LockFreeRMALevel&lt;TaskT, ResultT&gt;* child;\n    std::deque&lt;PendingRelay&gt; pending_relays;\n    std::vector&lt;ResultT&gt; relay_buffer;\n    // Tells the next parent write to read already-published error records.\n    bool relay_error_pending = false;\n    int64_t pending_task_count = 0;\n    int64_t total_claimed = 0;\n    bool finish_marked = false;\n    // Relay layers from this hop's child down to the leaf workers, inclusive.\n    // Each buffers a pipelined round of its own -- see step_bridge_hop().\n    int layers = 1;\n  };\n\n  // Numbers a completed hop chain bottom-up: the hop feeding the local worker\n  // level is layer 1, its feeder layer 2, and so on.\n  static void assign_hop_layers(std::vector&lt;BridgeHop&gt;&amp; hops) {\n    for (size_t i = 0; i &lt; hops.size(); ++i) {\n      hops[i].layers = static_cast&lt;int&gt;(hops.size() - i);\n    }\n  }\n\n  // One non-blocking relay step. Progress excludes local child harvesting so\n  // callers still back off parent polling.\n  bool step_bridge_hop(BridgeHop&amp; hop) {\n    bool made_parent_progress = false;\n\n    // Cache this RMA query so marking the child finished cannot be skipped by\n    // a false-to-true transition between checks.\n    const bool parent_drained = hop.parent-&gt;drained();\n\n    // Pace claims by unordered child completion so an ordering gap does not\n    // starve the subtree. Separately cap the unrelayed backlog.\n    // One claimed round in flight plus one pipelined round for every relay\n    // layer beneath this hop, so a leaf worker is never committed to more\n    // than tree-depth tasks. A flat max_pending_rounds instead gives the\n    // upper hops the same budget as the bottom one, where it is already\n    // spoken for by the layers below -- the two-sided hierarchy measured\n    // 2.97ms per round against 1.00ms once layers were budgeted for.\n    const int64_t max_pending_rounds = m_config.max_pending_rounds;\n    const int64_t pending_cap = static_cast&lt;int64_t&gt;(hop.parent-&gt;claim_width()) *\n                                (1 + (max_pending_rounds - 1) * hop.layers);\n    const int64_t pending_cap_hard = pending_cap * 4;\n    const int64_t uncompleted_claimed =\n        hop.total_claimed - hop.child-&gt;owner_unordered_completed_estimate();\n    const bool claim_paced_ok = uncompleted_claimed &lt; pending_cap;\n    const bool backlog_safe = hop.pending_task_count &lt; pending_cap_hard;\n    // Parent claims cannot be returned, so reserve child capacity first.\n    const bool child_has_room =\n        hop.child-&gt;owner_available_capacity() &gt;= static_cast&lt;int64_t&gt;(hop.parent-&gt;claim_width());\n    if (!parent_drained &amp;&amp; claim_paced_ok &amp;&amp; backlog_safe &amp;&amp; child_has_room) {\n      auto claimed = hop.parent-&gt;try_claim();\n      if (claimed.start != -1) {\n        const int64_t child_len = static_cast&lt;int64_t&gt;(claimed.tasks.size());\n        const bool published = hop.child-&gt;publish_tasks(claimed.tasks);\n        // Safe because this rank is the child's only publisher.\n        assert(published &amp;&amp; \"LockFreeRMALevel: child ring capacity check raced\");\n        (void)published;\n        hop.pending_relays.push_back(PendingRelay{claimed.start, child_len});\n        hop.pending_task_count += child_len;\n        hop.total_claimed += child_len;\n        made_parent_progress = true;\n      }\n    } else if (parent_drained &amp;&amp; !hop.finish_marked) {\n      hop.child-&gt;mark_finished();\n      hop.finish_marked = true;\n      made_parent_progress = true;\n    }\n\n    // Child harvesting does not count as parent progress.\n    auto child_results = hop.child-&gt;harvest_ready_results();\n    // Forward errors alongside the harvested range that exposed them.\n    for (auto&amp; error : hop.child-&gt;take_errors()) {\n      hop.parent-&gt;report_task_error(error);\n      hop.relay_error_pending = true;\n    }\n    if (!child_results.empty()) {\n      hop.relay_buffer.insert(hop.relay_buffer.end(),\n                              std::make_move_iterator(child_results.begin()),\n                              std::make_move_iterator(child_results.end()));\n    }\n\n    // Flush partial prefixes of the oldest relay while preserving FIFO order.\n    size_t consumed = 0;\n    while (!hop.pending_relays.empty() &amp;&amp; consumed &lt; hop.relay_buffer.size()) {\n      auto&amp; front = hop.pending_relays.front();\n      const size_t available = hop.relay_buffer.size() - consumed;\n      const size_t chunk = std::min(available, static_cast&lt;size_t&gt;(front.child_len));\n      std::vector&lt;ResultT&gt; slice(\n          std::make_move_iterator(hop.relay_buffer.begin() + static_cast&lt;ptrdiff_t&gt;(consumed)),\n          std::make_move_iterator(hop.relay_buffer.begin() +\n                                  static_cast&lt;ptrdiff_t&gt;(consumed + chunk)));\n      hop.parent-&gt;write_result_range(front.parent_start, slice, hop.relay_error_pending);\n      hop.relay_error_pending = false;\n      consumed += chunk;\n      hop.pending_task_count -= static_cast&lt;int64_t&gt;(chunk);\n      front.parent_start += static_cast&lt;int64_t&gt;(chunk);\n      front.child_len -= static_cast&lt;int64_t&gt;(chunk);\n      made_parent_progress = true;\n      if (front.child_len == 0) hop.pending_relays.pop_front();\n    }\n    if (consumed &gt; 0) {\n      hop.relay_buffer.erase(hop.relay_buffer.begin(),\n                             hop.relay_buffer.begin() + static_cast&lt;ptrdiff_t&gt;(consumed));\n    }\n    return made_parent_progress;\n  }\n\n  // Completion requires both child shutdown and a fully drained relay.\n  static bool bridge_hop_done(const BridgeHop&amp; hop) {\n    return hop.finish_marked &amp;&amp; hop.pending_relays.empty();\n  }\n\n  // Builds this rank's upper relay chain.\n  std::vector&lt;BridgeHop&gt; build_upper_hops() {\n    std::vector&lt;RMALevel*&gt; chain;\n    chain.push_back(&amp;*m_parent_level);\n    for (auto&amp; level : m_owned_upper_levels) chain.push_back(&amp;level);\n    std::vector&lt;BridgeHop&gt; hops;\n    for (size_t i = 0; i + 1 &lt; chain.size(); ++i) hops.push_back(BridgeHop{chain[i], chain[i + 1]});\n    return hops;\n  }\n\n  RMALevel&amp; last_upper_level() {\n    return m_owned_upper_levels.empty() ? *m_parent_level : m_owned_upper_levels.back();\n  }\n\n  static bool hops_done(const std::vector&lt;BridgeHop&gt;&amp; hops) {\n    for (const auto&amp; hop : hops) {\n      if (!bridge_hop_done(hop)) return false;\n    }\n    return true;\n  }\n\n  // Relay the upper chain into the local worker level.\n  void run_node_manager() {\n    std::vector&lt;BridgeHop&gt; hops = build_upper_hops();\n    hops.push_back(BridgeHop{&amp;last_upper_level(), &amp;*m_local_level});\n    assign_hop_layers(hops);\n\n    while (true) {\n      bool any_progress = false;\n      for (auto&amp; hop : hops) {\n        if (step_bridge_hop(hop)) any_progress = true;\n      }\n      if (hops_done(hops)) break;\n      if (!any_progress) hops.back().child-&gt;idle_wait();\n    }\n  }\n\n  bool claim_and_execute(RMALevel&amp; level) {\n    auto claimed = level.try_claim();\n    if (claimed.start == -1) return false;\n\n    std::vector&lt;ResultT&gt; results;\n    results.reserve(claimed.tasks.size());\n    bool failed = false;\n    for (auto&amp; task : claimed.tasks) {\n      ResultT result;\n      auto failure = detail::run_task_guarded(m_worker_function, std::move(task), result);\n      if (failure) {\n        level.report_task_error(TaskError{m_world_comm.rank(), std::move(*failure)});\n        failed = true;\n      }\n      results.push_back(std::move(result));\n    }\n    level.write_result_range(claimed.start, results, failed);\n    return true;\n  }\n\n  // Claim from the local level and compute inline.\n  void run_local_worker() {\n    while (true) {\n      // Cache this RMA query for a consistent claim and exit decision.\n      const bool is_drained = m_local_level-&gt;drained();\n      if (!is_drained &amp;&amp; claim_and_execute(*m_local_level)) continue;\n      if (is_drained) break;\n      m_local_level-&gt;idle_wait();\n    }\n  }\n\n  // Bridge promoted levels, then compute directly against the terminal one.\n  void run_leaf_leader_worker() {\n    std::vector&lt;BridgeHop&gt; hops = build_upper_hops();\n    assign_hop_layers(hops);\n    RMALevel&amp; terminal = last_upper_level();\n\n    while (true) {\n      bool any_progress = false;\n      for (auto&amp; hop : hops) {\n        if (step_bridge_hop(hop)) any_progress = true;\n      }\n\n      const bool is_drained = terminal.drained();\n      if (!is_drained &amp;&amp; claim_and_execute(terminal)) any_progress = true;\n      if (hops_done(hops) &amp;&amp; is_drained) break;\n      if (!any_progress) terminal.idle_wait();\n    }\n  }\n\n  // Size-one communicator execution.\n  void run_one_solo_task() {\n    ResultT result;\n    auto failure = detail::run_task_guarded(m_worker_function,\n                                            m_local_task_store[m_local_collected_count], result);\n    if (failure) m_task_errors.record(TaskError{m_world_comm.rank(), std::move(*failure)});\n    m_results.push_back(std::move(result));\n    m_local_collected_count++;\n  }\n\n  void collect_level_errors(detail::LockFreeRMALevel&lt;TaskT, ResultT&gt;&amp; level) {\n    for (auto&amp; error : level.take_errors()) m_task_errors.record(std::move(error));\n  }\n\n  std::vector&lt;ResultT&gt; drain_results(size_t limit) {\n    const size_t count = std::min(limit, m_results.size());\n    std::vector&lt;ResultT&gt; output;\n    output.reserve(count);\n    auto end = m_results.begin() + static_cast&lt;ptrdiff_t&gt;(count);\n    std::move(m_results.begin(), end, std::back_inserter(output));\n    m_results.erase(m_results.begin(), end);\n    return output;\n  }\n};\n\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/hierarchical__topology__detail_8hpp/","title":"File hierarchical_topology_detail.hpp","text":"<p>FileList &gt; dynampi &gt; impl &gt; hierarchical_topology_detail.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;cmath&gt;</code></li> <li><code>#include &lt;limits&gt;</code></li> <li><code>#include &lt;optional&gt;</code></li> <li><code>#include &lt;utility&gt;</code></li> <li><code>#include \"../mpi/mpi_communicator.hpp\"</code></li> <li><code>#include \"dynampi/mpi/mpi_error.hpp\"</code></li> </ul>"},{"location":"dynampi/hierarchical__topology__detail_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi namespace detail <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/hierarchical_topology_detail.hpp</code></p>"},{"location":"dynampi/hierarchical__topology__detail_8hpp_source/","title":"File hierarchical_topology_detail.hpp","text":"<p>File List &gt; dynampi &gt; impl &gt; hierarchical_topology_detail.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2026 Ryan Stocks\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n// Topology calculations shared by the two-sided and lock-free RMA\n// hierarchical distributors. Transport-specific level ownership and task\n// movement remain in their respective implementations.\n\n#include &lt;cmath&gt;\n#include &lt;limits&gt;\n#include &lt;optional&gt;\n#include &lt;utility&gt;\n\n#include \"../mpi/mpi_communicator.hpp\"\n#include \"dynampi/mpi/mpi_error.hpp\"\n\nnamespace dynampi::detail {\n\ninline int resolve_upper_fanout(int manager_count, int configured_fanout) {\n  if (configured_fanout &lt; 0) {\n    if (manager_count &lt;= 32) return std::numeric_limits&lt;int&gt;::max();\n    int fanout = 1;\n    const double target = std::sqrt(static_cast&lt;double&gt;(manager_count));\n    while (fanout &lt; target) fanout *= 2;\n    return fanout;\n  }\n  return configured_fanout &gt; 0 ? configured_fanout : std::numeric_limits&lt;int&gt;::max();\n}\n\ntemplate &lt;typename Communicator&gt;\nstd::optional&lt;Communicator&gt; split_local_worker_communicator(const Communicator&amp; world_comm,\n                                                            int manager_rank,\n                                                            int max_local_group_size) {\n  Communicator node_comm = world_comm.split_by_node();\n  std::optional&lt;Communicator&gt; local_domain;\n  if (max_local_group_size &gt; 0 &amp;&amp; node_comm.size() &gt; max_local_group_size) {\n    const int color = node_comm.rank() / max_local_group_size;\n    auto partition = node_comm.split(color, node_comm.rank());\n    local_domain.emplace(std::move(*partition));\n  } else {\n    local_domain.emplace(std::move(node_comm));\n  }\n\n  const int local_color = world_comm.rank() == manager_rank ? MPI_UNDEFINED : 0;\n  return local_domain-&gt;split(local_color, world_comm.rank());\n}\n\n// Valid only on group rank 0. Every group member must call this collectively.\n// Summing the actual widths matters because the root manager is excluded from\n// its local worker communicator, so its node manager fronts one fewer leaf.\ntemplate &lt;typename Communicator&gt;\nint sum_subtree_widths_to_group_leader(int local_width, const Communicator&amp; group_comm) {\n  int group_width = 0;\n  DYNAMPI_MPI_CHECK(MPI_Reduce, (&amp;local_width, &amp;group_width, 1, MPI_INT, MPI_SUM, 0,\n                                 static_cast&lt;MPI_Comm&gt;(group_comm)));\n  return group_width;\n}\n\n}  // namespace dynampi::detail\n</code></pre>"},{"location":"dynampi/lockfree__rma__distributor_8hpp/","title":"File lockfree_rma_distributor.hpp","text":"<p>FileList &gt; dynampi &gt; impl &gt; lockfree_rma_distributor.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;algorithm&gt;</code></li> <li><code>#include &lt;cassert&gt;</code></li> <li><code>#include &lt;cstddef&gt;</code></li> <li><code>#include &lt;cstdint&gt;</code></li> <li><code>#include &lt;cstring&gt;</code></li> <li><code>#include &lt;functional&gt;</code></li> <li><code>#include &lt;limits&gt;</code></li> <li><code>#include &lt;optional&gt;</code></li> <li><code>#include &lt;span&gt;</code></li> <li><code>#include &lt;vector&gt;</code></li> <li><code>#include \"../mpi/mpi_communicator.hpp\"</code></li> <li><code>#include \"../mpi/mpi_types.hpp\"</code></li> <li><code>#include \"dynampi/impl/rma_detail.hpp\"</code></li> <li><code>#include \"dynampi/mpi/mpi_error.hpp\"</code></li> <li><code>#include \"dynampi/task_error.hpp\"</code></li> <li><code>#include \"dynampi/utilities/timer.hpp\"</code></li> </ul>"},{"location":"dynampi/lockfree__rma__distributor_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi"},{"location":"dynampi/lockfree__rma__distributor_8hpp/#classes","title":"Classes","text":"Type Name class LockFreeRMAWorkDistributor &lt;typename TaskT, typename ResultT, Options&gt; struct Config struct RunConfig struct Statistics <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/lockfree_rma_distributor.hpp</code></p>"},{"location":"dynampi/lockfree__rma__distributor_8hpp_source/","title":"File lockfree_rma_distributor.hpp","text":"<p>File List &gt; dynampi &gt; impl &gt; lockfree_rma_distributor.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2026 Ryan Stocks\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;algorithm&gt;\n#include &lt;cassert&gt;\n#include &lt;cstddef&gt;\n#include &lt;cstdint&gt;\n#include &lt;cstring&gt;\n#include &lt;functional&gt;\n#include &lt;limits&gt;\n#include &lt;optional&gt;\n#include &lt;span&gt;\n#include &lt;vector&gt;\n\n#include \"../mpi/mpi_communicator.hpp\"\n#include \"../mpi/mpi_types.hpp\"\n#include \"dynampi/impl/rma_detail.hpp\"\n#include \"dynampi/mpi/mpi_error.hpp\"\n#include \"dynampi/task_error.hpp\"\n#include \"dynampi/utilities/timer.hpp\"\n\nnamespace dynampi {\n\n// ---------------------------------------------------------------------------\n// LockFreeRMAWorkDistributor\n//\n// Lock-free task claiming via fetch-and-add against HEAD_OFF, bounded by a\n// cached read of TOTAL_OFF, one task per claim -- this is a flat distributor.\n// Per task (worker):\n//   fetch_add(HEAD) + flush_local   -- claim; only need origin completion\n//   get(task) + flush_local         -- read payload\n//   compute\n//   put(result) + flush             -- data durable at manager\n//   put(log flag) + flush           -- plain Put (slot is exclusively owned);\n//                                      second flush publishes the flag after data\n//\n// Manager publish: put(tasks) + flush + atomic_set(TOTAL) + flush\n// Manager harvest reads use flush_local only.\n//\n// Results ARE ordered: each write_result_range() writes into the result slot\n// at the claimed index, the same index publish_tasks() assigned in submission\n// order, and harvest_ready_results() only ever advances past a *contiguous*\n// prefix of the completion log -- a gap at index i blocks every result after\n// it from being released, however long ago they finished. So the returned\n// vector always matches submission order, same as NaiveWorkDistributor and\n// for the same reason (see its class comment): the cost of guaranteeing order\n// is that one slow task holds back everything behind it.\n//\n// The task/result/log tables are a ring of Config::max_tasks slots (index %\n// max_tasks), not sized for the run's lifetime total -- publish_tasks()\n// blocks until harvesting has freed enough slots for a batch rather than\n// failing, so window memory stays bounded no matter how many tasks a run\n// publishes over its life. See Config::max_tasks and ring_slot().\n\n// ---------------------------------------------------------------------------\n// Options currently only recognizes track_statistics&lt;...&gt; (no prioritization support in this\n// class).\ntemplate &lt;typename TaskT, typename ResultT, typename... Options&gt;\nclass LockFreeRMAWorkDistributor {\n private:\n  static constexpr StatisticsMode statistics_mode =\n      get_option_value&lt;track_statistics_t, Options...&gt;();\n  using Comm = dynampi::MPICommunicator&lt;track_statistics&lt;statistics_mode&gt;&gt;;\n\n public:\n  struct Config {\n    MPI_Comm comm = MPI_COMM_WORLD;\n    int manager_rank = 0;\n    bool auto_run_workers = true;\n    // Ring capacity: the task/result/log tables are this many slots, reused\n    // via index % max_tasks once a slot's result has been harvested -- not a\n    // lifetime cap. publish_tasks() blocks (harvesting to make room) rather\n    // than failing if the ring is currently full; it only throws if a single\n    // call asks for more than max_tasks tasks at once, which can never fit\n    // regardless of how much harvesting happens. Bounds RMA window memory\n    // independent of how many tasks a run publishes over its lifetime.\n    int max_tasks = 8192;\n    int max_task_count = 256;\n    int max_result_count = 256;\n\n    // If true (default), run_tasks()/finish_remaining_tasks() throw\n    // dynampi::TaskFailure on the manager once a task has thrown. Set false to\n    // recover instead: distribution runs to completion and the failures are\n    // available from take_task_errors().\n    bool rethrow_task_errors = true;\n  };\n\n  struct RunConfig {\n    size_t target_num_tasks = std::numeric_limits&lt;size_t&gt;::max();\n    bool allow_more_than_target_tasks = true;\n    std::optional&lt;double&gt; max_seconds = std::nullopt;\n  };\n\n  // Ordered -- see the class comment above for why.\n  static const bool ordered = true;\n\n  struct Statistics {\n    const CommStatistics&amp; comm_statistics;\n  };\n  using StatisticsT =\n      std::conditional_t&lt;statistics_mode != StatisticsMode::None, Statistics, std::monostate&gt;;\n\n  const StatisticsT&amp; get_statistics() const\n    requires(statistics_mode != StatisticsMode::None)\n  {\n    assert(is_root_manager());\n    return m_statistics;\n  }\n\n  explicit LockFreeRMAWorkDistributor(std::function&lt;ResultT(TaskT)&gt; worker_function,\n                                      Config config = {})\n      : m_config(config),\n        m_comm(config.comm, Comm::Duplicate),\n        m_worker_function(std::move(worker_function)),\n        m_errors_seen(static_cast&lt;size_t&gt;(kMaxRecordedErrors)),\n        m_statistics{make_statistics(m_comm)} {\n    initialize_window();\n    if (m_config.auto_run_workers &amp;&amp; !is_root_manager()) run_worker();\n  }\n\n  // Tasks that threw, oldest first, removed as they are returned. See\n  // Config::rethrow_task_errors.\n  [[nodiscard]] std::vector&lt;TaskError&gt; take_task_errors() {\n    assert(is_root_manager());\n    return m_task_errors.take();\n  }\n\n  bool has_task_errors() const {\n    assert(is_root_manager());\n    return !m_task_errors.empty();\n  }\n\n  ~LockFreeRMAWorkDistributor() {\n    if (!m_finalized) finalize();\n    m_task_errors.warn_if_unreported(\"LockFreeRMAWorkDistributor\");\n    if (m_window != MPI_WIN_NULL) {\n      DYNAMPI_MPI_CHECK(MPI_Win_unlock_all, (m_window));\n      MPI_Barrier(m_comm.get());\n      DYNAMPI_MPI_CHECK(MPI_Win_free, (&amp;m_window));\n      m_window = MPI_WIN_NULL;\n    }\n  }\n\n  bool is_root_manager() const { return m_comm.rank() == m_config.manager_rank; }\n  int num_workers() const { return m_comm.size() - 1; }\n\n  size_t remaining_tasks_count() const {\n    assert(is_root_manager());\n    return static_cast&lt;size_t&gt;(m_total_tasks) - m_returned_count;\n  }\n\n  void insert_task(TaskT task) {\n    assert(is_root_manager());\n    publish_tasks(std::span&lt;const TaskT&gt;(&amp;task, 1));\n  }\n\n  // Priority is accepted but ignored (no prioritization support -- see the\n  // Options note above); exists only for template-shape compatibility with\n  // generic test/benchmark code exercised uniformly across distributors.\n  void insert_task(const TaskT&amp; task, double /*priority*/) {\n    assert(is_root_manager());\n    publish_tasks(std::span&lt;const TaskT&gt;(&amp;task, 1));\n  }\n\n  void insert_tasks(const std::vector&lt;TaskT&gt;&amp; tasks) {\n    assert(is_root_manager());\n    publish_tasks(std::span&lt;const TaskT&gt;(tasks));\n  }\n\n  [[nodiscard]] std::vector&lt;ResultT&gt; run_tasks(RunConfig config = {}) {\n    assert(is_root_manager());\n    Timer timer;\n\n    if (num_workers() == 0) {\n      while (m_collected_count &lt; static_cast&lt;size_t&gt;(m_total_tasks)) {\n        if (m_results.size() &gt;= config.target_num_tasks) break;\n        if (config.max_seconds &amp;&amp; timer.elapsed().count() &gt;= *config.max_seconds) break;\n        run_one_task_locally();\n      }\n    } else {\n      while (true) {\n        if (m_results.size() &gt;= config.target_num_tasks) break;\n        if (config.max_seconds &amp;&amp; timer.elapsed().count() &gt;= *config.max_seconds) break;\n        if (m_collected_count &gt;= static_cast&lt;size_t&gt;(m_total_tasks)) break;\n        const size_t before = m_collected_count;\n        harvest_ready_results();\n        if (m_collected_count == before) detail::rma_wait_idle(m_window, m_comm.get());\n      }\n    }\n\n    // Thrown before draining, so results collected so far stay buffered for\n    // whoever catches this and calls again.\n    m_task_errors.rethrow_first_if(m_config.rethrow_task_errors);\n\n    const size_t limit = config.allow_more_than_target_tasks ? std::numeric_limits&lt;size_t&gt;::max()\n                                                             : config.target_num_tasks;\n    return drain_results(limit);\n  }\n\n  [[nodiscard]] std::vector&lt;ResultT&gt; finish_remaining_tasks() { return run_tasks({}); }\n\n  // One harvest pass, returning whatever is ready. Unlike run_tasks(), does\n  // not retry until everything outstanding is collected -- for callers that\n  // want a snapshot and control back, such as a benchmark measuring an\n  // undisturbed worker spin over a fixed window.\n  [[nodiscard]] std::vector&lt;ResultT&gt; gather_once() {\n    assert(is_root_manager());\n    if (num_workers() == 0) {\n      while (m_collected_count &lt; static_cast&lt;size_t&gt;(m_total_tasks)) run_one_task_locally();\n    } else {\n      harvest_ready_results();\n    }\n    return drain_results(std::numeric_limits&lt;size_t&gt;::max());\n  }\n\n  void finalize() {\n    assert(!m_finalized);\n    if (is_root_manager()) {\n      if (num_workers() == 0) {\n        while (m_collected_count &lt; static_cast&lt;size_t&gt;(m_total_tasks)) run_one_task_locally();\n      } else {\n        // Raised before the harvest, not after: FINISHED only means \"TOTAL\n        // won't grow\", already true here, so workers drain out while the\n        // last results are collected instead of contending the window\n        // throughout. Safe because a worker flushes every result before it\n        // can reach its exit check.\n        atomic_set(FINISHED_OFF, 1);\n        while (m_collected_count &lt; static_cast&lt;size_t&gt;(m_total_tasks)) {\n          const size_t before = m_collected_count;\n          harvest_ready_results();\n          if (m_collected_count == before) detail::rma_wait_idle(m_window, m_comm.get());\n        }\n        detail::rma_wait_idle(m_window, m_comm.get());\n      }\n    }\n    m_finalized = true;\n  }\n\n  void run_worker() {\n    assert(!is_root_manager());\n\n    // One index per claim, gated on a cached TOTAL_OFF (a stale snapshot of\n    // a monotonic counter, so always a safe lower bound). Claims can still\n    // outrun publication, so a claim that lands past TOTAL is held in\n    // `pending` until it is published or FINISHED says it never will be.\n    // Held across iterations rather than spun on inline so the FINISHED\n    // check and idle backoff below keep running meanwhile.\n    int64_t cached_head = 0;\n    int64_t cached_total = 0;\n    int64_t pending = -1;  // claimed but not yet published; -1 = none\n    bool finished_seen = false;\n\n    auto process_one = [&amp;](int64_t index) {\n      std::vector&lt;TaskT&gt; tasks = read_task_batch(index, 1);\n      ResultT result;\n      auto failure = detail::run_task_guarded(m_worker_function, std::move(tasks[0]), result);\n      if (failure) report_task_error(*failure);\n      std::vector&lt;ResultT&gt; results;\n      results.push_back(std::move(result));\n      write_result_range(index, results, failure.has_value());\n    };\n\n    while (true) {\n      bool made_progress = false;\n\n      if (pending != -1) {\n        if (atomic_read(TOTAL_OFF) &gt; pending) {\n          process_one(pending);\n          pending = -1;\n          made_progress = true;\n        } else if (finished_seen) {\n          pending = -1;  // past the end of the run; owes no result\n        }\n      } else {\n        // Claiming is NOT gated on !finished_seen: that flag only means\n        // TOTAL is final, and a real unclaimed gap can still remain below\n        // it (see the exit check below, which records that gap).\n        if (cached_head &gt;= cached_total &amp;&amp; !finished_seen) cached_total = atomic_read(TOTAL_OFF);\n\n        if (cached_head &lt; cached_total) {\n          const int64_t index = fetch_add(HEAD_OFF, 1);\n          cached_head = index + 1;\n\n          // cached_total may be stale enough that this index still landed\n          // past what is published; re-read unless it already covers it.\n          const int64_t total = (index &lt; cached_total) ? cached_total : atomic_read(TOTAL_OFF);\n          if (index &lt; total) {\n            process_one(index);\n            made_progress = true;\n          } else {\n            pending = index;\n          }\n        }\n      }\n\n      if (!finished_seen &amp;&amp; atomic_read(FINISHED_OFF) != 0) finished_seen = true;\n\n      if (pending == -1 &amp;&amp; finished_seen) {\n        const int64_t final_total = atomic_read(TOTAL_OFF);\n        if (cached_head &gt;= final_total) break;\n        cached_total = final_total;\n      }\n      if (!made_progress) detail::rma_wait_idle(m_window, m_comm.get());\n    }\n  }\n\n private:\n  // Window layout: [head][total][finished] then the task table, the result\n  // table, and the completion log, each max_tasks entries wide and reused as\n  // a ring (index % max_tasks, see ring_slot()) -- head and total are\n  // absolute task indices that grow without bound over the run's lifetime,\n  // not ring-relative. No gather-sequence field -- there is no gather\n  // protocol.\n  static constexpr MPI_Aint HEAD_OFF = 0;\n  static constexpr MPI_Aint TOTAL_OFF = 8;\n  static constexpr MPI_Aint FINISHED_OFF = 16;\n  static constexpr MPI_Aint ERROR_COUNT_OFF = 24;\n  static constexpr size_t CONTROL_BYTES = 32;\n\n  // Failed tasks report into a small fixed table rather than through the result\n  // slots, which are sized for ResultT and are routinely far too small to hold\n  // a message. Claimants take a slot with fetch_and_add, so the table fills in\n  // report order; past kMaxRecordedErrors the count still rises (so nothing is\n  // silently lost) but the messages are dropped.\n  static constexpr int64_t kMaxRecordedErrors = 16;\n  static constexpr size_t E_RANK = 0;\n  static constexpr size_t E_DATA = 8;\n  static constexpr size_t ERROR_SLOT_BYTES = E_DATA + kMaxTaskErrorMessage;\n\n  // Task slot: [count][data].\n  static constexpr size_t T_COUNT = 0;\n  static constexpr size_t T_DATA = 8;\n  // Result slot: [count][data]. No valid flag here -- validity lives in the\n  // completion log instead (see write_result_range), one entry per whole\n  // claimed batch rather than one flag per task.\n  static constexpr size_t R_COUNT = 0;\n  static constexpr size_t R_DATA = 8;\n  // Completion log entry: a single int64, 0 = untouched. count &gt; 0 at index\n  // `s` means the claim starting at task index s, covering [s, s+count), is\n  // fully written to the result table. Keyed by the claim's own start index,\n  // which fetch_add already made exclusive -- so no slot-allocation round\n  // trip is needed.\n  static constexpr size_t LOG_ENTRY_BYTES = 8;\n\n  Config m_config;\n  Comm m_comm;\n  std::function&lt;ResultT(TaskT)&gt; m_worker_function;\n\n  MPI_Win m_window = MPI_WIN_NULL;\n  std::vector&lt;std::byte&gt; m_window_buffer;  // manager: control + task table + result table + log\n  alignas(int64_t) std::byte m_worker_window[sizeof(int64_t)]{};\n  bool m_finalized = false;\n\n  size_t m_task_elem = 0;\n  size_t m_result_elem = 0;\n  size_t m_max_task_count = 1;\n  size_t m_max_result_count = 1;\n  size_t m_task_slot_stride = 0;\n  size_t m_result_slot_stride = 0;\n  size_t m_task_base = 0;\n  size_t m_result_base = 0;\n  size_t m_log_base = 0;\n  size_t m_error_base = 0;\n\n  int64_t m_total_tasks = 0;\n  // Doubles as the completion-log scan frontier: the manager only ever\n  // advances it by exactly the number of newly-confirmed results each\n  // harvest, so \"task indices below m_collected_count are fully collected\"\n  // and \"the log/result tables are unscanned above it\" stay equivalent by\n  // construction -- no separate frontier variable needed.\n  size_t m_collected_count = 0;\n  size_t m_returned_count = 0;\n  detail::TaskErrorLog m_task_errors;  // manager only\n  std::vector&lt;bool&gt; m_errors_seen;     // manager only: error slots already consumed\n  std::vector&lt;ResultT&gt; m_results;      // manager only: harvested, ready to return\n  std::vector&lt;TaskT&gt; m_task_store;     // manager only, solo-world fallback\n  StatisticsT m_statistics;\n\n  void initialize_window() {\n    // Window slots are fixed-width, so a non-resizable payload spanning more\n    // than one datatype element would overrun them -- see\n    // check_fixed_size_mpi_type().\n    check_fixed_size_mpi_type&lt;TaskT&gt;(\"task\", \"LockFreeRMA\");\n    check_fixed_size_mpi_type&lt;ResultT&gt;(\"result\", \"LockFreeRMA\");\n\n    m_task_elem = static_cast&lt;size_t&gt;(detail::mpi_type_size_bytes&lt;TaskT&gt;());\n    m_result_elem = static_cast&lt;size_t&gt;(detail::mpi_type_size_bytes&lt;ResultT&gt;());\n    // A fixed-size payload always occupies mpi_elements_per_value&lt;T&gt;() elements\n    // -- 1 for a scalar, but 3 for a struct of three doubles -- so slots are\n    // sized for that rather than for a single element.\n    m_max_task_count = MPI_Type&lt;TaskT&gt;::resize_required\n                           ? static_cast&lt;size_t&gt;(m_config.max_task_count)\n                           : static_cast&lt;size_t&gt;(mpi_elements_per_value&lt;TaskT&gt;());\n    m_max_result_count = MPI_Type&lt;ResultT&gt;::resize_required\n                             ? static_cast&lt;size_t&gt;(m_config.max_result_count)\n                             : static_cast&lt;size_t&gt;(mpi_elements_per_value&lt;ResultT&gt;());\n\n    m_task_slot_stride = detail::round_up_8(T_DATA + m_max_task_count * m_task_elem);\n    m_result_slot_stride = detail::round_up_8(R_DATA + m_max_result_count * m_result_elem);\n\n    const size_t capacity = static_cast&lt;size_t&gt;(m_config.max_tasks);\n    m_task_base = CONTROL_BYTES;\n    m_result_base = m_task_base + capacity * m_task_slot_stride;\n    m_log_base = m_result_base + capacity * m_result_slot_stride;\n    m_error_base = m_log_base + capacity * LOG_ENTRY_BYTES;\n    const size_t manager_window_bytes =\n        m_error_base + static_cast&lt;size_t&gt;(kMaxRecordedErrors) * ERROR_SLOT_BYTES;\n\n    if (num_workers() == 0) return;  // solo world: no RMA window needed at all\n\n    if (is_root_manager()) {\n      m_window_buffer.resize(manager_window_bytes);\n    }\n\n    void* base = nullptr;\n    MPI_Aint bsize = 0;\n    if (is_root_manager()) {\n      base = m_window_buffer.data();\n      bsize = static_cast&lt;MPI_Aint&gt;(m_window_buffer.size());\n    } else {\n      base = m_worker_window;\n      bsize = static_cast&lt;MPI_Aint&gt;(sizeof(m_worker_window));\n    }\n    DYNAMPI_MPI_CHECK(MPI_Win_create, (base, bsize, 1, MPI_INFO_NULL, m_comm.get(), &amp;m_window));\n    DYNAMPI_MPI_CHECK(MPI_Win_lock_all, (MPI_MODE_NOCHECK, m_window));\n  }\n\n  // Ring slot for a task index: reused every max_tasks indices. Callers are\n  // responsible for never having two live (published, not yet harvested)\n  // indices map to the same slot -- see publish_tasks()'s capacity wait.\n  int64_t ring_slot(int64_t index) const {\n    return index % static_cast&lt;int64_t&gt;(m_config.max_tasks);\n  }\n  MPI_Aint task_slot(int64_t index) const {\n    return static_cast&lt;MPI_Aint&gt;(m_task_base +\n                                 static_cast&lt;size_t&gt;(ring_slot(index)) * m_task_slot_stride);\n  }\n  MPI_Aint result_slot(int64_t index) const {\n    return static_cast&lt;MPI_Aint&gt;(m_result_base +\n                                 static_cast&lt;size_t&gt;(ring_slot(index)) * m_result_slot_stride);\n  }\n  MPI_Aint log_slot(int64_t index) const {\n    return static_cast&lt;MPI_Aint&gt;(m_log_base +\n                                 static_cast&lt;size_t&gt;(ring_slot(index)) * LOG_ENTRY_BYTES);\n  }\n  MPI_Aint error_slot(int64_t index) const {\n    return static_cast&lt;MPI_Aint&gt;(m_error_base + static_cast&lt;size_t&gt;(index) * ERROR_SLOT_BYTES);\n  }\n\n  // --- RMA primitives ---\n  //\n  // Passive-target RMA is nonblocking. flush_local completes at the origin\n  // (enough before reading claim/get results); flush_remote completes at the\n  // target (needed before a remote rank can observe Puts / atomics).\n  int manager_rank() const { return m_config.manager_rank; }\n\n  void flush_remote() { DYNAMPI_MPI_CHECK(MPI_Win_flush, (manager_rank(), m_window)); }\n  void flush_local() { DYNAMPI_MPI_CHECK(MPI_Win_flush_local, (manager_rank(), m_window)); }\n\n  void post_fetch_and_op(int64_t in, int64_t&amp; out, MPI_Aint offset, MPI_Op op) {\n    m_comm.fetch_and_op(in, out, manager_rank(), offset, op, m_window);\n  }\n  void post_put_bytes(const void* src, size_t n, MPI_Aint offset) {\n    m_comm.put_bytes(src, n, manager_rank(), offset, m_window);\n  }\n  void post_get_bytes(void* dst, size_t n, MPI_Aint offset) {\n    m_comm.get_bytes(dst, n, manager_rank(), offset, m_window);\n  }\n\n  int64_t atomic_read(MPI_Aint offset) {\n    int64_t in = 0, out;\n    post_fetch_and_op(in, out, offset, MPI_NO_OP);\n    flush_local();\n    return out;\n  }\n  void atomic_set(MPI_Aint offset, int64_t value) {\n    int64_t in = value, out;\n    post_fetch_and_op(in, out, offset, MPI_REPLACE);\n    flush_remote();\n  }\n  int64_t fetch_add(MPI_Aint offset, int64_t increment) {\n    int64_t in = increment, out;\n    post_fetch_and_op(in, out, offset, MPI_SUM);\n    flush_local();\n    return out;\n  }\n\n  void get_bytes_local(void* dst, size_t n, MPI_Aint offset) {\n    post_get_bytes(dst, n, offset);\n    flush_local();\n  }\n\n  // Reads `count` consecutive task indices starting at `start` into `dst`,\n  // splitting into two Gets if the range crosses a ring lap boundary --\n  // slot_fn() (task_slot/result_slot/log_slot) is only contiguous within one\n  // lap. Safe to assume at most one wrap: callers here only ever scan a\n  // range bounded by max_tasks (see publish_tasks()'s capacity wait, which\n  // keeps m_total_tasks - m_collected_count &lt;= max_tasks at all times).\n  template &lt;typename SlotFn&gt;\n  void get_ring_bytes_local(void* dst, int64_t start, int64_t count, size_t stride,\n                            SlotFn&amp;&amp; slot_fn) {\n    const int64_t ring = static_cast&lt;int64_t&gt;(m_config.max_tasks);\n    const int64_t first_count = std::min(count, ring - ring_slot(start));\n    get_bytes_local(dst, static_cast&lt;size_t&gt;(first_count) * stride, slot_fn(start));\n    if (first_count &lt; count) {\n      get_bytes_local(static_cast&lt;std::byte*&gt;(dst) + static_cast&lt;size_t&gt;(first_count) * stride,\n                      static_cast&lt;size_t&gt;(count - first_count) * stride,\n                      slot_fn(start + first_count));\n    }\n  }\n\n  // --- Task publish (manager side) / read (claimant side) ---\n\n  // Three round trips regardless of batch size: one bulk Put for the whole\n  // range's [count][data] slots, a flush to land it, then one atomic_set of\n  // TOTAL_OFF. A per-task publish measured ~18K tasks/s against the ~900K/s\n  // raw ceiling in rma_atomic_microbench.\n  //\n  // TOTAL_OFF is bumped last and only after that flush, so every task's data\n  // is in place at the manager before the counter that makes it claimable\n  // moves: a claimant can never see an index whose payload is still in\n  // flight.\n  void publish_tasks(std::span&lt;const TaskT&gt; tasks) {\n    if (tasks.empty()) return;\n    const int64_t count = static_cast&lt;int64_t&gt;(tasks.size());\n    // A single call can never publish more than the ring holds, however much\n    // harvesting happens -- check with a hypothetical start of 0 purely to\n    // test that absolute bound (real indices grow unboundedly with the ring,\n    // so checking against the true start would spuriously fail once it\n    // first exceeds max_tasks). The capacity wait below handles \"fits in the\n    // ring, just not free yet.\"\n    detail::check_task_capacity(0, tasks.size(), m_config.max_tasks, \"LockFreeRMA\");\n\n    // Block until the ring has room for the whole batch. Waiting for a\n    // partial batch to free up mid-publish would need a second start index,\n    // complicating the single-Put-per-batch design below for no benefit --\n    // this only spins when the caller publishes faster than the ring\n    // drains, an unusual calling pattern to begin with.\n    while (count &gt; static_cast&lt;int64_t&gt;(m_config.max_tasks) -\n                       (m_total_tasks - static_cast&lt;int64_t&gt;(m_collected_count))) {\n      if (num_workers() == 0) {\n        run_one_task_locally();\n      } else {\n        const size_t before = m_collected_count;\n        harvest_ready_results();\n        if (m_collected_count == before) detail::rma_wait_idle(m_window, m_comm.get());\n      }\n    }\n\n    const int64_t start = m_total_tasks;\n    if (num_workers() == 0) {\n      m_task_store.insert(m_task_store.end(), tasks.begin(), tasks.end());\n      m_total_tasks += count;\n      return;\n    }\n    std::vector&lt;std::byte&gt; buffer(tasks.size() * m_task_slot_stride);\n    for (size_t i = 0; i &lt; tasks.size(); ++i) {\n      const TaskT&amp; task = tasks[i];\n      const int elem_count = MPI_Type&lt;TaskT&gt;::count(task);\n      assert(static_cast&lt;size_t&gt;(elem_count) &lt;= m_max_task_count &amp;&amp;\n             \"LockFreeRMA: task exceeds max_task_count\");\n      const size_t data_bytes = static_cast&lt;size_t&gt;(elem_count) * m_task_elem;\n      const size_t off = i * m_task_slot_stride;\n      detail::write_i64(buffer.data(), buffer.size(), off + T_COUNT, elem_count);\n      if (data_bytes &gt; 0) {\n        detail::write_bytes(buffer.data(), buffer.size(), off + T_DATA, MPI_Type&lt;TaskT&gt;::ptr(task),\n                            data_bytes);\n      }\n    }\n    // The ring can wrap mid-batch (task_slot() is only contiguous within one\n    // lap), so a batch crossing the boundary needs two Puts instead of one --\n    // both complete on the flush below, so this costs no extra round trip in\n    // the common (non-wrapping) case and only one extra Put issuance in the\n    // wrapping one.\n    const int64_t ring = static_cast&lt;int64_t&gt;(m_config.max_tasks);\n    const int64_t first_count = std::min(count, ring - ring_slot(start));\n    post_put_bytes(buffer.data(), static_cast&lt;size_t&gt;(first_count) * m_task_slot_stride,\n                   task_slot(start));\n    if (first_count &lt; count) {\n      post_put_bytes(buffer.data() + static_cast&lt;size_t&gt;(first_count) * m_task_slot_stride,\n                     static_cast&lt;size_t&gt;(count - first_count) * m_task_slot_stride,\n                     task_slot(start + first_count));\n    }\n    // Reused slots (every index past the first lap) still hold whatever\n    // completion-log entry their previous occupant's write_result_range()\n    // left behind. Left uncleared, harvest_ready_results()'s scan would read\n    // that stale non-zero entry and treat the brand new index as already\n    // complete before this lap's worker has even claimed it -- confirmed as\n    // a real bug, not a theoretical one (a stress test with a small ring\n    // reproduced exactly this: index i's \"result\" coming back as index\n    // (i - max_tasks)'s, the value the harvester picked up from the\n    // not-yet-cleared log slot's leftover entry). Zeroing here, ahead of the\n    // TOTAL_OFF bump that makes these indices claimable, guarantees the log\n    // entry a claimant's write_result_range() later sets is the only\n    // non-zero value the harvester can ever see for this lap.\n    std::vector&lt;std::byte&gt; log_clear(static_cast&lt;size_t&gt;(count) * LOG_ENTRY_BYTES, std::byte{0});\n    post_put_bytes(log_clear.data(), static_cast&lt;size_t&gt;(first_count) * LOG_ENTRY_BYTES,\n                   log_slot(start));\n    if (first_count &lt; count) {\n      post_put_bytes(log_clear.data() + static_cast&lt;size_t&gt;(first_count) * LOG_ENTRY_BYTES,\n                     static_cast&lt;size_t&gt;(count - first_count) * LOG_ENTRY_BYTES,\n                     log_slot(start + first_count));\n    }\n    // Ordering barrier, the publish-side counterpart to the one in the worker\n    // loop. MPI orders accumulate operations only when they target the same\n    // location, so without this the TOTAL_OFF replace below -- a different\n    // location from either Put -- may reach the manager first, and a claimant\n    // that sees the raised counter can Get a payload slot the Put has not\n    // landed in yet, or write a completion that the log clear then erases.\n    // Issue order alone does not prevent it; only completion at the target\n    // does. Costs one round trip per publish call, not per task.\n    flush_remote();\n    m_total_tasks += count;\n    atomic_set(TOTAL_OFF, m_total_tasks);\n  }\n\n  std::vector&lt;TaskT&gt; read_task_batch(int64_t index, int64_t count) {\n    const size_t bytes = static_cast&lt;size_t&gt;(count) * m_task_slot_stride;\n    std::vector&lt;std::byte&gt; buf(bytes);\n    get_bytes_local(buf.data(), bytes, task_slot(index));\n\n    std::vector&lt;TaskT&gt; tasks;\n    tasks.reserve(static_cast&lt;size_t&gt;(count));\n    for (int64_t i = 0; i &lt; count; ++i) {\n      const size_t slot_offset = static_cast&lt;size_t&gt;(i) * m_task_slot_stride;\n      const int64_t elem_count = detail::read_i64(buf.data(), buf.size(), slot_offset + T_COUNT);\n      TaskT task{};\n      if constexpr (MPI_Type&lt;TaskT&gt;::resize_required)\n        MPI_Type&lt;TaskT&gt;::resize(task, static_cast&lt;int&gt;(elem_count));\n      const size_t data_bytes = static_cast&lt;size_t&gt;(elem_count) * m_task_elem;\n      detail::read_result_bytes(buf.data(), buf.size(), slot_offset + T_DATA, task, data_bytes);\n      tasks.push_back(std::move(task));\n    }\n    return tasks;\n  }\n\n  // --- Result write (claimant side) / harvest (manager side) ---\n\n  // Writes results.size() results starting at task index `start`:\n  //   Put result data -&gt; flush -&gt; Put completion-log flag -&gt; flush.\n  // The log write is a plain Put (not an atomic): fetch_add already gave this\n  // worker exclusive ownership of `start`, so no concurrent writer shares the\n  // slot. The intervening flush is what makes \"data before flag\" portable.\n  // Publishes one failed task into the error table. Must complete before the\n  // log entry that advertises it -- every RMA helper here flushes its target\n  // before returning, so calling this before write_result_range() is enough.\n  void report_task_error(const std::string&amp; message) {\n    const int64_t slot = fetch_add(ERROR_COUNT_OFF, 1);\n    if (slot &gt;= kMaxRecordedErrors) return;\n    // Message first, then the ready word, each flushed separately: fetch_add\n    // hands out the slot before anything is in it, so the count alone never\n    // means a slot can be read. The ready word is rank+1 so that zero stays the\n    // untouched sentinel for rank 0.\n    std::vector&lt;std::byte&gt; message_bytes(kMaxTaskErrorMessage, std::byte{0});\n    const size_t bytes = std::min(message.size(), kMaxTaskErrorMessage - 1);\n    if (bytes &gt; 0) {\n      detail::write_bytes(message_bytes.data(), message_bytes.size(), 0, message.data(), bytes);\n    }\n    const int64_t ready = static_cast&lt;int64_t&gt;(m_comm.rank()) + 1;\n    post_put_bytes(message_bytes.data(), message_bytes.size(),\n                   error_slot(slot) + static_cast&lt;MPI_Aint&gt;(E_DATA));\n    flush_remote();\n    post_put_bytes(&amp;ready, sizeof(ready), error_slot(slot) + static_cast&lt;MPI_Aint&gt;(E_RANK));\n    flush_remote();\n  }\n\n  // Owner-side. Reads whatever error records have appeared since the last call.\n  // Only ever called after the log scan has seen a negative entry, so it costs\n  // nothing on a run with no failures.\n  void harvest_task_errors() {\n    const int64_t claimed = std::min(atomic_read(ERROR_COUNT_OFF), kMaxRecordedErrors);\n    if (claimed &lt;= 0) return;  // LCOV_EXCL_LINE -- only reachable on a spurious flag\n    // Takes ready slots anywhere in the table, not a contiguous frontier:\n    // slots are claimed in fetch_add order but completed out of order, so an\n    // in-flight slot is skipped now and picked up later. Safe because a\n    // claimant flushes its record before the log entry that brings the\n    // manager here.\n    std::vector&lt;std::byte&gt; buf(static_cast&lt;size_t&gt;(claimed) * ERROR_SLOT_BYTES);\n    get_bytes_local(buf.data(), buf.size(), error_slot(0));\n    for (int64_t i = 0; i &lt; claimed; ++i) {\n      if (m_errors_seen[static_cast&lt;size_t&gt;(i)]) continue;\n      const size_t off = static_cast&lt;size_t&gt;(i) * ERROR_SLOT_BYTES;\n      const int64_t ready = detail::read_i64(buf.data(), buf.size(), off + E_RANK);\n      if (ready == 0) continue;  // still in flight; its own flag will bring us back\n      TaskError error;\n      error.worker_rank = static_cast&lt;int&gt;(ready - 1);\n      const char* text = reinterpret_cast&lt;const char*&gt;(buf.data() + off + E_DATA);\n      error.message.assign(text, ::strnlen(text, kMaxTaskErrorMessage - 1));\n      m_task_errors.record(std::move(error));\n      m_errors_seen[static_cast&lt;size_t&gt;(i)] = true;\n    }\n  }\n\n  void write_result_range(int64_t start, const std::vector&lt;ResultT&gt;&amp; results,\n                          bool contains_error = false) {\n    const int64_t count = static_cast&lt;int64_t&gt;(results.size());\n    assert(count &gt; 0);\n    std::vector&lt;std::byte&gt; buffer(static_cast&lt;size_t&gt;(count) * m_result_slot_stride);\n    for (int64_t i = 0; i &lt; count; ++i) {\n      const ResultT&amp; result = results[static_cast&lt;size_t&gt;(i)];\n      const int elem_count = MPI_Type&lt;ResultT&gt;::count(result);\n      assert(elem_count &gt;= 0);\n      const size_t data_bytes =\n          elem_count &gt; 0 ? static_cast&lt;size_t&gt;(elem_count) * m_result_elem : size_t{0};\n      const size_t off = static_cast&lt;size_t&gt;(i) * m_result_slot_stride;\n      detail::write_i64(buffer.data(), buffer.size(), off + R_COUNT, elem_count);\n      if (data_bytes &gt; 0) {\n        detail::write_bytes(buffer.data(), buffer.size(), off + R_DATA,\n                            MPI_Type&lt;ResultT&gt;::ptr(result), data_bytes);\n      }\n    }\n    post_put_bytes(buffer.data(), buffer.size(), result_slot(start));\n    flush_remote();\n    // A negated length marks \"this range produced at least one error\"; the\n    // manager already reads every log entry, so the flag is free, and it only\n    // pays for the error table when something has actually failed.\n    const int64_t entry = contains_error ? -count : count;\n    post_put_bytes(&amp;entry, sizeof(entry), log_slot(start));\n    flush_remote();\n  }\n\n  // Owner-only. Three round trips however many claims are ready: read\n  // HEAD_OFF, bulk-read the log over the unscanned range, bulk-read the\n  // results over whatever contiguous prefix the log confirms. A gap ends\n  // the scan for this pass and is picked up on the next one.\n  //\n  // Uses the RMA API even though the target is this rank: under\n  // MPI_WIN_SEPARATE a window owner is not guaranteed to see remote writes\n  // through local loads. On MS-MPI these self-targeted Gets also need the\n  // two-sided progress engine driven between polls (detail::rma_wait_idle);\n  // flush_all alone leaves the loop spinning on an unchanging HEAD.\n  void harvest_ready_results() {\n    assert(is_root_manager());\n    int64_t head_in = 0, head_now = 0;\n    post_fetch_and_op(head_in, head_now, HEAD_OFF, MPI_NO_OP);\n    flush_local();\n\n    // A claimant can fetch_add HEAD_OFF for an index it hasn't actually been\n    // published yet -- run_worker()'s \"pending\" path deliberately lets\n    // claims outrun publication so a claimant doesn't have to wait for a\n    // fresh TOTAL_OFF read before claiming. Before the ring, scanning that\n    // far ahead was harmless (an unpublished slot's log entry was always\n    // still zero, since nothing had ever touched it); with the ring, that\n    // same slot may hold a stale non-zero entry left over from a prior lap\n    // that this index hasn't reached yet -- confirmed as a real bug (a\n    // small-ring stress test misread stale entries this way, several laps\n    // behind the true completion). Capping at m_total_tasks means the scan\n    // never reads a slot this rank hasn't itself published in the current\n    // lap, so it can never see anything but that lap's own entry.\n    const int64_t head_now_capped = std::min(head_now, m_total_tasks);\n\n    const int64_t frontier = static_cast&lt;int64_t&gt;(m_collected_count);\n    if (head_now_capped &lt;= frontier) return;\n\n    const int64_t scan_count = head_now_capped - frontier;\n    std::vector&lt;std::byte&gt; log_buf(static_cast&lt;size_t&gt;(scan_count) * LOG_ENTRY_BYTES);\n    get_ring_bytes_local(log_buf.data(), frontier, scan_count, LOG_ENTRY_BYTES,\n                         [this](int64_t i) { return log_slot(i); });\n\n    int64_t confirmed_end = frontier;\n    bool saw_error = false;\n    while (confirmed_end &lt; head_now_capped) {\n      const size_t off = static_cast&lt;size_t&gt;(confirmed_end - frontier) * LOG_ENTRY_BYTES;\n      const int64_t entry = detail::read_i64(log_buf.data(), log_buf.size(), off);\n      if (entry == 0) break;  // gap: not yet written; stop the contiguous prefix here\n      if (entry &lt; 0) saw_error = true;\n      confirmed_end += entry &lt; 0 ? -entry : entry;\n    }\n    if (saw_error) harvest_task_errors();\n    if (confirmed_end &lt;= frontier) return;\n\n    const int64_t n = confirmed_end - frontier;\n    std::vector&lt;std::byte&gt; result_buf(static_cast&lt;size_t&gt;(n) * m_result_slot_stride);\n    get_ring_bytes_local(result_buf.data(), frontier, n, m_result_slot_stride,\n                         [this](int64_t i) { return result_slot(i); });\n\n    m_results.reserve(m_results.size() + static_cast&lt;size_t&gt;(n));\n    for (int64_t i = 0; i &lt; n; ++i) {\n      const size_t off = static_cast&lt;size_t&gt;(i) * m_result_slot_stride;\n      const int64_t count = detail::read_i64(result_buf.data(), result_buf.size(), off + R_COUNT);\n      ResultT result{};\n      if constexpr (MPI_Type&lt;ResultT&gt;::resize_required)\n        MPI_Type&lt;ResultT&gt;::resize(result, static_cast&lt;int&gt;(count));\n      const size_t data_bytes = count &gt; 0 ? static_cast&lt;size_t&gt;(count) * m_result_elem : size_t{0};\n      detail::read_result_bytes(result_buf.data(), result_buf.size(), off + R_DATA, result,\n                                data_bytes);\n      m_results.push_back(std::move(result));\n    }\n    m_collected_count = static_cast&lt;size_t&gt;(confirmed_end);\n  }\n\n  void run_one_task_locally() {\n    // Guarded like every other execution path: the solo case exists so a\n    // workload can be debugged without MPI, which it cannot be if failures\n    // surface differently here.\n    ResultT result;\n    auto failure =\n        detail::run_task_guarded(m_worker_function, m_task_store[m_collected_count], result);\n    if (failure) m_task_errors.record(TaskError{m_comm.rank(), std::move(*failure)});\n    m_results.push_back(std::move(result));\n    m_collected_count++;\n  }\n\n  std::vector&lt;ResultT&gt; drain_results(size_t limit) {\n    const size_t count = std::min(limit, m_results.size());\n    std::vector&lt;ResultT&gt; output;\n    output.reserve(count);\n    for (size_t i = 0; i &lt; count; ++i) output.push_back(std::move(m_results[i]));\n    m_results.erase(m_results.begin(), m_results.begin() + static_cast&lt;ptrdiff_t&gt;(count));\n    m_returned_count += count;\n    return output;\n  }\n\n  static StatisticsT make_statistics(const Comm&amp; comm) {\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      return Statistics{.comm_statistics = comm.get_statistics()};\n    } else {\n      return {};\n    }\n  }\n};\n\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/naive__distributor_8hpp/","title":"File naive_distributor.hpp","text":"<p>FileList &gt; dynampi &gt; impl &gt; naive_distributor.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;algorithm&gt;</code></li> <li><code>#include &lt;cassert&gt;</code></li> <li><code>#include &lt;cstdint&gt;</code></li> <li><code>#include &lt;deque&gt;</code></li> <li><code>#include &lt;functional&gt;</code></li> <li><code>#include &lt;limits&gt;</code></li> <li><code>#include &lt;optional&gt;</code></li> <li><code>#include &lt;queue&gt;</code></li> <li><code>#include &lt;stack&gt;</code></li> <li><code>#include &lt;type_traits&gt;</code></li> <li><code>#include &lt;variant&gt;</code></li> <li><code>#include &lt;vector&gt;</code></li> <li><code>#include \"../mpi/mpi_communicator.hpp\"</code></li> <li><code>#include \"../mpi/mpi_types.hpp\"</code></li> <li><code>#include \"dynampi/impl/base_distributor.hpp\"</code></li> <li><code>#include \"dynampi/task_error.hpp\"</code></li> <li><code>#include \"dynampi/utilities/timer.hpp\"</code></li> </ul>"},{"location":"dynampi/naive__distributor_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi"},{"location":"dynampi/naive__distributor_8hpp/#classes","title":"Classes","text":"Type Name class NaiveWorkDistributor &lt;typename TaskT, typename ResultT, Options&gt; struct Config struct RunConfig struct Statistics <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/naive_distributor.hpp</code></p>"},{"location":"dynampi/naive__distributor_8hpp_source/","title":"File naive_distributor.hpp","text":"<p>File List &gt; dynampi &gt; impl &gt; naive_distributor.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2025 QDX Technologies. Authored by Ryan Stocks &lt;ryan.stocks00@gmail.com&gt;\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;algorithm&gt;\n#include &lt;cassert&gt;\n#include &lt;cstdint&gt;\n#include &lt;deque&gt;\n#include &lt;functional&gt;\n#include &lt;limits&gt;\n#include &lt;optional&gt;\n#include &lt;queue&gt;\n#include &lt;stack&gt;\n#include &lt;type_traits&gt;\n#include &lt;variant&gt;\n#include &lt;vector&gt;\n\n#include \"../mpi/mpi_communicator.hpp\"\n#include \"../mpi/mpi_types.hpp\"\n#include \"dynampi/impl/base_distributor.hpp\"\n#include \"dynampi/task_error.hpp\"\n#include \"dynampi/utilities/timer.hpp\"\n\nnamespace dynampi {\n\ntemplate &lt;typename TaskT, typename ResultT, typename... Options&gt;\nclass NaiveWorkDistributor {\n public:\n  struct Config {\n    MPI_Comm comm = MPI_COMM_WORLD;\n    int manager_rank = 0;\n    bool auto_run_workers = true;\n\n    // If true (default), run_tasks()/finish_remaining_tasks() throw\n    // dynampi::TaskFailure on the manager once a task has thrown. Set false to\n    // recover instead: distribution runs to completion and the failures are\n    // available from take_task_errors().\n    bool rethrow_task_errors = true;\n  };\n\n  struct RunConfig {\n    // Stop once we have at least this many contiguous results ready to return.\n    size_t target_num_tasks = std::numeric_limits&lt;size_t&gt;::max();\n\n    // If false, strictly clips the return vector to `target_num_tasks`.\n    // Excess results remain in the internal buffer for the next call.\n    bool allow_more_than_target_tasks = true;\n\n    // Stop if this much time has passed.\n    std::optional&lt;double&gt; max_seconds = std::nullopt;\n  };\n\n  static const bool ordered = true;\n\n private:\n  static constexpr bool prioritize_tasks = get_option_value&lt;prioritize_tasks_t, Options...&gt;();\n  static constexpr StatisticsMode statistics_mode =\n      get_option_value&lt;track_statistics_t, Options...&gt;();\n\n  using QueueT = std::conditional_t&lt;prioritize_tasks, std::priority_queue&lt;std::pair&lt;double, TaskT&gt;&gt;,\n                                    std::deque&lt;TaskT&gt;&gt;;\n  using MPICommunicator = dynampi::MPICommunicator&lt;track_statistics&lt;statistics_mode&gt;&gt;;\n\n  // --- Member Variables ---\n  Config m_config;\n  MPICommunicator m_communicator;\n  std::function&lt;ResultT(TaskT)&gt; m_worker_function;\n\n  QueueT m_unallocated_task_queue;\n\n  // State tracking\n  std::vector&lt;int64_t&gt; m_worker_current_task_indices;  // Maps worker_idx -&gt; task_id\n  std::stack&lt;int&gt; m_free_worker_ranks;\n\n  // Transient Storage:\n  // We use a vector to store results by task ID, with a bitmap to track validity.\n  // Items are marked invalid as soon as they become contiguous and ready to return.\n  std::vector&lt;ResultT&gt; m_pending_results;\n  std::vector&lt;bool&gt; m_pending_results_valid;\n\n  // Counters\n  size_t m_tasks_sent = 0;        // Total tasks ever sent (acts as the unique ID for the next task)\n  size_t m_front_result_idx = 0;  // The task ID of the result at the front of the vector (index 0)\n  size_t m_known_contiguous_results =\n      0;  // Number of contiguous valid results starting from m_front_result_idx\n  bool m_finalized = false;\n\n  detail::TaskErrorLog m_task_errors;  // manager only\n\n  enum Tag : int { TASK = 0, DONE = 1, RESULT = 2, REQUEST = 3, ERROR = 4 };\n\n public:\n  struct Statistics {\n    const CommStatistics&amp; comm_statistics;\n    // Tasks dispatched to each worker, indexed by worker index (this rank's\n    // communicator rank with the manager removed -- see rank_to_worker_idx).\n    std::vector&lt;size_t&gt; worker_task_counts;\n  };\n\n  using StatisticsT =\n      std::conditional_t&lt;statistics_mode != StatisticsMode::None, Statistics, std::monostate&gt;;\n\n private:\n  StatisticsT m_statistics;\n\n public:\n  explicit NaiveWorkDistributor(std::function&lt;ResultT(TaskT)&gt; worker_function,\n                                Config runtime_config = Config{})\n      : m_config(runtime_config),\n        m_communicator(runtime_config.comm, MPICommunicator::Duplicate),\n        m_worker_function(worker_function),\n        m_statistics{create_statistics(m_communicator)} {\n    if (is_root_manager()) {\n      m_worker_current_task_indices.resize(num_workers(), -1);\n    }\n\n    if (m_config.auto_run_workers &amp;&amp; !is_root_manager()) {\n      run_worker();\n    }\n\n    if constexpr (statistics_mode &gt;= StatisticsMode::Aggregated) {\n      if (is_root_manager()) m_statistics.worker_task_counts.resize(m_communicator.size(), 0);\n    }\n  }\n\n  ~NaiveWorkDistributor() {\n    if (!m_finalized) finalize();\n    m_task_errors.warn_if_unreported(\"NaiveWorkDistributor\");\n  }\n\n  // --- Main Interface ---\n\n  [[nodiscard]] std::vector&lt;ResultT&gt; run_tasks(RunConfig config = RunConfig{}) {\n    assert(is_root_manager() &amp;&amp; \"Only the manager can distribute tasks\");\n\n    Timer timer;\n\n    // We loop until one of the exit conditions is met.\n    while (true) {\n      // --- 1. Check Exit Conditions ---\n\n      // A. Have we collected enough contiguous results?\n      if (m_known_contiguous_results &gt;= config.target_num_tasks) {\n        break;\n      }\n\n      // B. Time limit check\n      if (config.max_seconds &amp;&amp; timer.elapsed().count() &gt;= *config.max_seconds) {\n        break;\n      }\n\n      // C. Total exhaustion check\n      if (m_unallocated_task_queue.empty() &amp;&amp; active_worker_count() == 0) {\n        break;\n      }\n\n      // --- 2. Action Logic (Send vs Receive) ---\n\n      // Priority: Keep workers busy\n      if (!m_unallocated_task_queue.empty() &amp;&amp; !m_free_worker_ranks.empty()) {\n        send_next_task_to_worker(m_free_worker_ranks.top());\n        m_free_worker_ranks.pop();\n      } else {\n        // Single process mode fallback\n        if (num_workers() == 0 &amp;&amp; !m_unallocated_task_queue.empty()) {\n          run_task_locally();\n        }\n        // Standard MPI wait\n        else if (active_worker_count() &gt; 0) {\n          process_incoming_message();\n        }\n      }\n    }\n\n    // --- 3. Return Logic ---\n    // Thrown before draining, so the results collected so far stay buffered for\n    // whoever catches this and calls again.\n    m_task_errors.rethrow_first_if(m_config.rethrow_task_errors);\n\n    size_t limit = std::numeric_limits&lt;size_t&gt;::max();\n    if (!config.allow_more_than_target_tasks) {\n      limit = config.target_num_tasks;\n    }\n\n    return collect_available_results(limit);\n  }\n\n  [[nodiscard]] std::vector&lt;ResultT&gt; finish_remaining_tasks() {\n    RunConfig cfg;\n    cfg.target_num_tasks = std::numeric_limits&lt;size_t&gt;::max();\n    return run_tasks(cfg);\n  }\n\n  void finalize() {\n    assert(!m_finalized &amp;&amp; \"Work distribution already finalized\");\n    if (is_root_manager()) {\n      broadcast_done();\n    }\n    m_finalized = true;\n  }\n\n  // --- Public Accessors ---\n\n  bool is_root_manager() const { return m_communicator.rank() == m_config.manager_rank; }\n\n  size_t remaining_tasks_count() const {\n    assert(is_root_manager() &amp;&amp; \"Only the manager can check remaining tasks\");\n    return m_unallocated_task_queue.size();\n  }\n\n  const StatisticsT&amp; get_statistics() const\n    requires(statistics_mode != StatisticsMode::None)\n  {\n    assert(is_root_manager() &amp;&amp; \"Only the manager can access statistics\");\n    return m_statistics;\n  }\n\n  // Tasks that threw, oldest first, removed from the distributor as they are\n  // returned. With Config::rethrow_task_errors left true this only ever holds\n  // failures that arrived after the last throw; with it false it holds all of\n  // them.\n  [[nodiscard]] std::vector&lt;TaskError&gt; take_task_errors() {\n    assert(is_root_manager() &amp;&amp; \"Only the manager collects task errors\");\n    return m_task_errors.take();\n  }\n\n  bool has_task_errors() const {\n    assert(is_root_manager() &amp;&amp; \"Only the manager collects task errors\");\n    return !m_task_errors.empty();\n  }\n\n  // --- Task Insertion ---\n\n  void insert_task(TaskT task)\n    requires(!prioritize_tasks)\n  {\n    assert(is_root_manager());\n    m_unallocated_task_queue.push_back(std::move(task));\n  }\n\n  void insert_task(const TaskT&amp; task, double priority)\n    requires(prioritize_tasks)\n  {\n    assert(is_root_manager());\n    m_unallocated_task_queue.emplace(priority, task);\n  }\n\n  void insert_tasks(const std::vector&lt;TaskT&gt;&amp; tasks)\n    requires(!prioritize_tasks)\n  {\n    assert(is_root_manager());\n    for (const auto&amp; t : tasks) m_unallocated_task_queue.push_back(t);\n  }\n\n  // --- Worker Logic ---\n\n  void run_worker() {\n    assert(!is_root_manager());\n    using task_type = MPI_Type&lt;TaskT&gt;;\n\n    // Handshake: send REQUEST as 0 elements of ResultT so manager can recv_any(ResultT&amp;) for both\n    // REQUEST and RESULT\n    m_communicator.template send_empty&lt;ResultT&gt;(m_config.manager_rank, Tag::REQUEST);\n\n    while (true) {\n      MPI_Status status = m_communicator.probe();\n\n      if (status.MPI_TAG == Tag::DONE) {\n        m_communicator.recv_empty_message(m_config.manager_rank, Tag::DONE);\n        break;\n      }\n\n      int count;\n      DYNAMPI_MPI_CHECK(MPI_Get_count, (&amp;status, task_type::value, &amp;count));\n      TaskT message;\n      task_type::resize(message, count);\n      m_communicator.recv(message, m_config.manager_rank, Tag::TASK);\n\n      ResultT result;\n      auto failure = detail::run_task_guarded(m_worker_function, std::move(message), result);\n\n      // ERROR is a pure notification and travels alongside the placeholder\n      // result, never instead of it: the manager marks a worker free when its\n      // RESULT lands, so a failure that skipped the result would have to\n      // duplicate that bookkeeping -- and a failure that sent both while ERROR\n      // also completed the task would push this worker onto the free stack\n      // twice.\n      if (failure) {\n        m_communicator.send(*failure, m_config.manager_rank, Tag::ERROR);\n      }\n      m_communicator.send(result, m_config.manager_rank, Tag::RESULT);\n    }\n  }\n\n private:\n  // --- Helpers ---\n\n  int num_workers() const { return m_communicator.size() - 1; }\n\n  size_t active_worker_count() const {\n    return static_cast&lt;size_t&gt;(num_workers()) - m_free_worker_ranks.size();\n  }\n\n  int rank_to_worker_idx(int rank) const {\n    return (rank &lt; m_config.manager_rank) ? rank : (rank - 1);\n  }\n\n  int worker_idx_to_rank(int idx) const { return (idx &lt; m_config.manager_rank) ? idx : (idx + 1); }\n\n  TaskT pop_next_task() {\n    TaskT task;\n    if constexpr (prioritize_tasks) {\n      task = m_unallocated_task_queue.top().second;\n      m_unallocated_task_queue.pop();\n    } else {\n      task = std::move(m_unallocated_task_queue.front());\n      m_unallocated_task_queue.pop_front();\n    }\n    return task;\n  }\n\n  void run_task_locally() {\n    TaskT task = pop_next_task();\n    // Store result directly in vector (using relative indexing)\n    int64_t task_id = static_cast&lt;int64_t&gt;(m_tasks_sent);\n    ensure_result_capacity(task_id - m_front_result_idx + 1);\n    size_t vector_idx = task_id - m_front_result_idx;\n    // Guarded even here, where the task runs on the manager's own stack: the\n    // single-rank path exists so a workload can be debugged serially, and it\n    // is no use for that if it reports failures differently from the real run.\n    ResultT result;\n    auto failure = detail::run_task_guarded(m_worker_function, std::move(task), result);\n    if (failure) m_task_errors.record(TaskError{m_communicator.rank(), std::move(*failure)});\n    m_pending_results[vector_idx] = std::move(result);\n    m_pending_results_valid[vector_idx] = true;\n    m_tasks_sent++;\n    update_contiguous_results_count(task_id);\n  }\n\n  void send_next_task_to_worker(int worker_rank) {\n    TaskT task = pop_next_task();\n    int64_t task_id = static_cast&lt;int64_t&gt;(m_tasks_sent);\n\n    m_worker_current_task_indices[rank_to_worker_idx(worker_rank)] = task_id;\n    if constexpr (statistics_mode &gt;= StatisticsMode::Aggregated) {\n      m_statistics.worker_task_counts[rank_to_worker_idx(worker_rank)]++;\n    }\n\n    m_communicator.send(task, worker_rank, Tag::TASK);\n    m_tasks_sent++;\n  }\n\n  void process_incoming_message() {\n    MPI_Status status = m_communicator.probe(MPI_ANY_SOURCE, MPI_ANY_TAG);\n    int source = status.MPI_SOURCE;\n    if (status.MPI_TAG == Tag::ERROR) {\n      // Notification only -- the matching RESULT is what frees this worker.\n      handle_error_message(source, status);\n      return;\n    }\n    if (status.MPI_TAG == Tag::RESULT) {\n      handle_result_message(source, status);\n    } else {\n      DYNAMPI_ASSERT_EQ(status.MPI_TAG, Tag::REQUEST, \"Unexpected tag received\");\n      m_communicator.template recv_empty&lt;ResultT&gt;(source, Tag::REQUEST);\n    }\n    m_free_worker_ranks.push(source);\n  }\n\n  void handle_result_message(int source, MPI_Status&amp; probe_status) {\n    using result_type = MPI_Type&lt;ResultT&gt;;\n    int count;\n    DYNAMPI_MPI_CHECK(MPI_Get_count, (&amp;probe_status, result_type::value, &amp;count));\n\n    ResultT result_data;\n    result_type::resize(result_data, count);\n    m_communicator.recv(result_data, source, Tag::RESULT);\n\n    store_completed_task(source, std::move(result_data));\n  }\n\n  // A worker reporting a failed task. The placeholder result that follows keeps\n  // the ordered result sequence contiguous, so a caller that recovers still\n  // gets one (default-constructed) entry per task and the indices of the\n  // surviving results do not shift.\n  void handle_error_message(int source, MPI_Status&amp; probe_status) {\n    int count;\n    DYNAMPI_MPI_CHECK(MPI_Get_count, (&amp;probe_status, MPI_CHAR, &amp;count));\n    std::string message;\n    MPI_Type&lt;std::string&gt;::resize(message, count);\n    m_communicator.recv(message, source, Tag::ERROR);\n\n    m_task_errors.record(TaskError{source, std::move(message)});\n  }\n\n  void store_completed_task(int source, ResultT result) {\n    int worker_idx = rank_to_worker_idx(source);\n    int64_t task_id = m_worker_current_task_indices[worker_idx];\n    m_worker_current_task_indices[worker_idx] = -1;\n\n    // Store in vector (using relative indexing)\n    size_t vector_idx = task_id - m_front_result_idx;\n    ensure_result_capacity(vector_idx + 1);\n    m_pending_results[vector_idx] = std::move(result);\n    m_pending_results_valid[vector_idx] = true;\n    update_contiguous_results_count(task_id);\n  }\n\n  std::vector&lt;ResultT&gt; collect_available_results(size_t limit) {\n    std::vector&lt;ResultT&gt; batch;\n    size_t num_results_to_return = std::min(limit, m_known_contiguous_results);\n    if (num_results_to_return == 0) {\n      return batch;\n    }\n\n    batch.reserve(num_results_to_return);\n    // Extract from the beginning of the vectors (which contain contiguous results starting from\n    // m_front_result_idx)\n    batch.insert(batch.end(), std::make_move_iterator(m_pending_results.begin()),\n                 std::make_move_iterator(m_pending_results.begin() + num_results_to_return));\n\n    // Erase the collected results from the beginning\n    m_pending_results_valid.erase(m_pending_results_valid.begin(),\n                                  m_pending_results_valid.begin() + num_results_to_return);\n    m_pending_results.erase(m_pending_results.begin(),\n                            m_pending_results.begin() + num_results_to_return);\n\n    // Update counters: increment m_front_result_idx to reflect the new starting point,\n    // and decrement the contiguous count. The vectors now use relative indexing\n    // where index 0 corresponds to task_id = m_front_result_idx.\n    m_front_result_idx += num_results_to_return;\n    m_known_contiguous_results -= num_results_to_return;\n\n    return batch;\n  }\n\n  void broadcast_done() {\n    for (int i = 0; i &lt; num_workers(); i++) {\n      m_communicator.send(nullptr, worker_idx_to_rank(i), Tag::DONE);\n    }\n  }\n\n  void ensure_result_capacity(size_t required_size) {\n    // required_size is relative to m_front_result_idx\n    if (m_pending_results.size() &lt; required_size) {\n      m_pending_results.resize(required_size);\n      m_pending_results_valid.resize(required_size, false);\n    }\n  }\n\n  // Updates m_known_contiguous_results when a new result arrives.\n  // If the result extends the contiguous sequence, increment and check forward.\n  void update_contiguous_results_count(int64_t task_id) {\n    int64_t expected_task_id =\n        static_cast&lt;int64_t&gt;(m_front_result_idx + m_known_contiguous_results);\n\n    // Only update if this result extends the contiguous sequence\n    if (task_id == expected_task_id) {\n      // Extend the contiguous sequence forward as far as possible\n      // Use relative indexing for vector access\n      size_t vector_idx = expected_task_id - m_front_result_idx;\n      while (vector_idx &lt; m_pending_results.size() &amp;&amp; m_pending_results_valid[vector_idx]) {\n        m_known_contiguous_results++;\n        vector_idx++;\n      }\n    }\n  }\n\n  static StatisticsT create_statistics(const MPICommunicator&amp; comm) {\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      return Statistics{comm.get_statistics(), {}};\n    } else {\n      return {};\n    }\n  }\n};\n\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/rma__detail_8hpp/","title":"File rma_detail.hpp","text":"<p>FileList &gt; dynampi &gt; impl &gt; rma_detail.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;algorithm&gt;</code></li> <li><code>#include &lt;cassert&gt;</code></li> <li><code>#include &lt;chrono&gt;</code></li> <li><code>#include &lt;cstddef&gt;</code></li> <li><code>#include &lt;cstdint&gt;</code></li> <li><code>#include &lt;limits&gt;</code></li> <li><code>#include &lt;stdexcept&gt;</code></li> <li><code>#include &lt;string&gt;</code></li> <li><code>#include &lt;thread&gt;</code></li> <li><code>#include \"../mpi/mpi_communicator.hpp\"</code></li> <li><code>#include \"../mpi/mpi_types.hpp\"</code></li> <li><code>#include \"dynampi/mpi/mpi_error.hpp\"</code></li> <li><code>#include \"dynampi/task_error.hpp\"</code></li> </ul>"},{"location":"dynampi/rma__detail_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi namespace detail <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/rma_detail.hpp</code></p>"},{"location":"dynampi/rma__detail_8hpp_source/","title":"File rma_detail.hpp","text":"<p>File List &gt; dynampi &gt; impl &gt; rma_detail.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2026 Ryan Stocks\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n// Byte-packing, capacity-checking and idle-wait helpers shared by the\n// passive-target RMA distributors (LockFreeRMAWorkDistributor and\n// HierarchicalLockFreeRMAWorkDistributor). Both lay their windows out as flat\n// byte buffers with 8-byte-aligned records, so the pack/unpack and bounds\n// logic is identical between them and lives here rather than in either one.\n\n#include &lt;algorithm&gt;\n#include &lt;cassert&gt;\n#include &lt;chrono&gt;\n#include &lt;cstddef&gt;\n#include &lt;cstdint&gt;\n#include &lt;limits&gt;\n#include &lt;stdexcept&gt;\n#include &lt;string&gt;\n#include &lt;thread&gt;\n\n// Declared directly; &lt;windows.h&gt;'s min/max/ERROR macros collide with the\n// distributor headers that include this one.\n#if defined(_WIN32) &amp;&amp; defined(_MSC_VER)\nextern \"C\" __declspec(dllimport) unsigned int __stdcall timeBeginPeriod(unsigned int);\n#pragma comment(lib, \"winmm\")\n#endif\n\n#include \"../mpi/mpi_communicator.hpp\"\n#include \"../mpi/mpi_types.hpp\"\n#include \"dynampi/mpi/mpi_error.hpp\"\n#include \"dynampi/task_error.hpp\"\n\nnamespace dynampi {\n\nnamespace detail {\n\ninline void check_task_capacity(int64_t start, size_t count, int max_tasks,\n                                const char* distributor_name) {\n  const bool invalid = start &lt; 0 || max_tasks &lt; 0 ||\n                       static_cast&lt;uint64_t&gt;(start) &gt; static_cast&lt;uint64_t&gt;(max_tasks) ||\n                       count &gt; static_cast&lt;uint64_t&gt;(max_tasks) - static_cast&lt;uint64_t&gt;(start);\n  if (invalid) {\n    throw std::length_error(std::string(distributor_name) + \": exceeded max_tasks capacity\");\n  }\n}\n\n// Byte size of a single element of the MPI datatype backing T (e.g. 4 for int,\n// 4 for the element type of std::vector&lt;int&gt;).\ntemplate &lt;typename T&gt;\ninline int mpi_type_size_bytes() {\n  int size = 0;\n  DYNAMPI_MPI_CHECK(MPI_Type_size, (MPI_Type&lt;T&gt;::value, &amp;size));\n  return size;\n}\n\ninline constexpr size_t round_up_8(size_t bytes) { return (bytes + 7) &amp; ~static_cast&lt;size_t&gt;(7); }\n\n#if defined(__GNUC__)\n[[gnu::noinline]]\n#endif\ninline void write_bytes(std::byte* buffer, size_t buffer_size, size_t offset, const void* src,\n                        size_t nbytes) {\n  if (nbytes == 0) return;\n  // Runtime range gate (not assert-only): GCC 14 -Wstringop-overflow treats an\n  // unconstrained size_t length as possibly near SIZE_MAX and false-positives\n  // on fortified memcpy into buffer+offset under -Werror. Cap against\n  // ptrdiff_t max so the length is proven below the \"maximum object size\".\n  constexpr size_t kMaxObjectSize = static_cast&lt;size_t&gt;(std::numeric_limits&lt;std::ptrdiff_t&gt;::max());\n  if (nbytes &gt; kMaxObjectSize || offset &gt; buffer_size || nbytes &gt; buffer_size - offset) {\n    DYNAMPI_FAIL(\"write_bytes out of range\");  // LCOV_EXCL_LINE\n  }\n  const auto* in = static_cast&lt;const std::byte*&gt;(src);\n  std::copy_n(in, nbytes, buffer + offset);\n}\n\n// Bounds-checked copy out of a sized source buffer into a sized destination.\ninline void read_bytes(void* dst, size_t dst_capacity, const std::byte* buffer, size_t buffer_size,\n                       size_t offset, size_t nbytes) {\n  if (nbytes == 0) return;\n  constexpr size_t kMaxObjectSize = static_cast&lt;size_t&gt;(std::numeric_limits&lt;std::ptrdiff_t&gt;::max());\n  if (nbytes &gt; kMaxObjectSize || nbytes &gt; dst_capacity || offset &gt; buffer_size ||\n      nbytes &gt; buffer_size - offset) {\n    DYNAMPI_FAIL(\"read_bytes out of range\");  // LCOV_EXCL_LINE\n  }\n  const size_t copy_n = std::min(nbytes, dst_capacity);\n  std::copy_n(buffer + offset, copy_n, static_cast&lt;std::byte*&gt;(dst));\n}\n\ninline int64_t read_i64(const std::byte* buffer, size_t buffer_size, size_t offset) {\n  int64_t value{};\n  read_bytes(&amp;value, sizeof(value), buffer, buffer_size, offset, sizeof(int64_t));\n  return value;\n}\n\ninline void write_i64(std::byte* buffer, size_t buffer_size, size_t offset, int64_t value) {\n  write_bytes(buffer, buffer_size, offset, &amp;value, sizeof(int64_t));\n}\n\ntemplate &lt;typename T&gt;\ninline void read_result_bytes(const std::byte* buffer, size_t buffer_size, size_t offset, T&amp; value,\n                              size_t data_bytes) {\n  if (data_bytes == 0) return;\n  if constexpr (MPI_Type&lt;T&gt;::resize_required) {\n    read_bytes(MPI_Type&lt;T&gt;::ptr(value), data_bytes, buffer, buffer_size, offset, data_bytes);\n  } else {\n    assert(data_bytes == sizeof(T));\n    read_bytes(&amp;value, sizeof(T), buffer, buffer_size, offset, data_bytes);\n  }\n}\n\ninline void backoff_sleep(std::chrono::microseconds duration) {\n#if defined(_WIN32) &amp;&amp; defined(_MSC_VER)\n  // MSVC's sleep_for rounds any wait up to the global timer tick (~15.6 ms by\n  // default), so every rma_wait_idle backoff was ~300x too long: the MS-MPI\n  // ctest run took ~350 s. Raise the resolution to 1 ms once. It can't go\n  // lower: below ~1 ms, whether sleeping or spinning, the idle poll loop\n  // starves MS-MPI's passive-target progress and livelocks at &gt;= 4 ranks.\n  [[maybe_unused]] static const auto timer_res = ::timeBeginPeriod(1);\n#endif\n  std::this_thread::sleep_for(duration);\n}\n\n// Drive progress while a rank spins on one-sided completion. Under\n// MPI_WIN_SEPARATE (MS-MPI always) the two-sided engine must run for remote RMA\n// state to advance; primitives already flush their target, so MPI_Iprobe drives\n// progress here without the extra contention of another MPI_Win_flush_all.\ninline void rma_wait_idle(MPI_Win /*window*/, MPI_Comm comm) {\n  int flag = 0;\n  DYNAMPI_MPI_CHECK(MPI_Iprobe, (MPI_ANY_SOURCE, MPI_ANY_TAG, comm, &amp;flag, MPI_STATUS_IGNORE));\n  backoff_sleep(std::chrono::microseconds(50));\n}\n\n}  // namespace detail\n\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/variable__batch_8hpp/","title":"File variable_batch.hpp","text":"<p>FileList &gt; dynampi &gt; impl &gt; variable_batch.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;algorithm&gt;</code></li> <li><code>#include &lt;cstdint&gt;</code></li> <li><code>#include &lt;stdexcept&gt;</code></li> <li><code>#include &lt;vector&gt;</code></li> <li><code>#include \"dynampi/mpi/mpi_types.hpp\"</code></li> </ul>"},{"location":"dynampi/variable__batch_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi namespace detail <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/impl/variable_batch.hpp</code></p>"},{"location":"dynampi/variable__batch_8hpp_source/","title":"File variable_batch.hpp","text":"<p>File List &gt; dynampi &gt; impl &gt; variable_batch.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2026 Ryan Stocks\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;algorithm&gt;\n#include &lt;cstdint&gt;\n#include &lt;stdexcept&gt;\n#include &lt;vector&gt;\n\n#include \"dynampi/mpi/mpi_types.hpp\"\n\nnamespace dynampi::detail {\n\n// MPI_Type&lt;std::vector&lt;T&gt;&gt; (see mpi_types.hpp) assumes T is a fixed-size\n// value: it ships the outer vector as `count() = size() * elements_per_T`\n// contiguous datatype elements, with elements_per_T a single compile-time\n// constant. That model has no way to describe a *batch of variable-length T*\n// (e.g. TaskT/ResultT = std::vector&lt;std::byte&gt;, one serialized message per\n// task): each element can have a different byte length, and MPI_Type&lt;T&gt;::ptr\n// for such a T doesn't even point at contiguous element storage across the\n// batch (it's a vector of separately heap-allocated std::vectors). Using the\n// generic path for that case silently sends/receives raw std::vector control\n// blocks (pointer/size/capacity) as if they were payload bytes -- garbage\n// across process boundaries, and exactly the kind of corruption that shows\n// up later as a bad-free when the receiving vector's destructor runs on\n// those bogus pointers.\n//\n// So the hierarchical distributors' TASK_BATCH/RESULT_BATCH transfer (see\n// hierarchical_distributor.hpp)\n// packs such batches manually instead, as a flat std::vector&lt;std::byte&gt;\n// (which *is* representable via the fixed-size path) with a length-prefixed\n// layout:\n//   [uint64_t n_items]\n//   [uint64_t byte_len_0] .. [uint64_t byte_len_{n-1}]\n//   [bytes of item 0] .. [bytes of item {n-1}]\n\ntemplate &lt;typename T&gt;\nstd::vector&lt;std::byte&gt; pack_variable_batch(const std::vector&lt;T&gt;&amp; items) {\n  using ItemType = MPI_Type&lt;T&gt;;\n  int elem_bytes = 0;\n  MPI_Type_size(ItemType::value, &amp;elem_bytes);\n\n  const uint64_t n_items = items.size();\n  std::vector&lt;uint64_t&gt; byte_lens(n_items);\n  uint64_t total_bytes = 0;\n  for (uint64_t i = 0; i &lt; n_items; ++i) {\n    const uint64_t len =\n        static_cast&lt;uint64_t&gt;(ItemType::count(items[i])) * static_cast&lt;uint64_t&gt;(elem_bytes);\n    byte_lens[i] = len;\n    total_bytes += len;\n  }\n\n  std::vector&lt;std::byte&gt; buf(sizeof(uint64_t) * (1 + n_items) + total_bytes);\n  size_t offset = 0;\n  // std::copy_n rather than memcpy throughout this file: Codacy/Flawfinder\n  // flags every memcpy as CWE-120 regardless of prior range checks or a\n  // destination sized immediately above it (see rma_detail.hpp's\n  // write_bytes()/read_bytes() for the same convention).\n  std::copy_n(reinterpret_cast&lt;const std::byte*&gt;(&amp;n_items), sizeof(uint64_t), buf.data() + offset);\n  offset += sizeof(uint64_t);\n  if (n_items &gt; 0) {\n    std::copy_n(reinterpret_cast&lt;const std::byte*&gt;(byte_lens.data()), sizeof(uint64_t) * n_items,\n                buf.data() + offset);\n  }\n  offset += sizeof(uint64_t) * n_items;\n  for (uint64_t i = 0; i &lt; n_items; ++i) {\n    if (byte_lens[i] &gt; 0) {\n      const auto* src = static_cast&lt;const std::byte*&gt;(ItemType::ptr(items[i]));\n      std::copy_n(src, byte_lens[i], buf.data() + offset);\n    }\n    offset += byte_lens[i];\n  }\n  return buf;\n}\n\n// Bounds-checks a read of `nbytes` at `offset` against `buf_size` before the\n// caller's copy touches it. `buf` is wire data from another rank (a\n// version-skewed peer, a TaskT/ResultT mismatch between the two sides, or\n// plain corruption could all hand this function a header that overstates\n// what's actually there), so unlike pack_variable_batch's writes -- into a\n// buffer this function sizes itself, right above each copy, and so is\n// provably big enough -- every read length here has to be checked against\n// what was actually received before touching it.\ninline void check_variable_batch_read(size_t offset, uint64_t nbytes, size_t buf_size,\n                                      const char* what) {\n  if (offset &gt; buf_size || nbytes &gt; static_cast&lt;uint64_t&gt;(buf_size - offset)) {\n    throw std::runtime_error(std::string(\"dynampi: truncated variable-batch buffer reading \") +\n                             what);\n  }\n}\n\ntemplate &lt;typename T&gt;\nstd::vector&lt;T&gt; unpack_variable_batch(const std::vector&lt;std::byte&gt;&amp; buf) {\n  using ItemType = MPI_Type&lt;T&gt;;\n  int elem_bytes = 0;\n  MPI_Type_size(ItemType::value, &amp;elem_bytes);\n\n  const size_t buf_size = buf.size();\n  size_t offset = 0;\n  uint64_t n_items = 0;\n  check_variable_batch_read(offset, sizeof(uint64_t), buf_size, \"item count\");\n  std::copy_n(buf.data() + offset, sizeof(uint64_t), reinterpret_cast&lt;std::byte*&gt;(&amp;n_items));\n  offset += sizeof(uint64_t);\n\n  // Bound n_items against the remaining buffer (each item needs at least one\n  // 8-byte length-table slot) via division, not multiplication, and before\n  // allocating anything sized by it: a corrupted or truncated header could\n  // otherwise claim an enormous count, overflowing sizeof(uint64_t) * n_items\n  // itself or driving an allocation far larger than the actual message.\n  if (n_items &gt; static_cast&lt;uint64_t&gt;(buf_size - offset) / sizeof(uint64_t)) {\n    throw std::runtime_error(\"dynampi: truncated variable-batch buffer reading length table\");\n  }\n\n  std::vector&lt;uint64_t&gt; byte_lens(n_items);\n  if (n_items &gt; 0) {\n    std::copy_n(buf.data() + offset, sizeof(uint64_t) * n_items,\n                reinterpret_cast&lt;std::byte*&gt;(byte_lens.data()));\n  }\n  offset += sizeof(uint64_t) * n_items;\n\n  std::vector&lt;T&gt; items(n_items);\n  for (uint64_t i = 0; i &lt; n_items; ++i) {\n    const uint64_t len = byte_lens[i];\n    const int count_in_elements =\n        elem_bytes &gt; 0 ? static_cast&lt;int&gt;(len / static_cast&lt;uint64_t&gt;(elem_bytes)) : 0;\n    ItemType::resize(items[i], count_in_elements);\n    if (len &gt; 0) {\n      check_variable_batch_read(offset, len, buf_size, \"item payload\");\n      auto* dst = static_cast&lt;std::byte*&gt;(ItemType::ptr(items[i]));\n      std::copy_n(buf.data() + offset, len, dst);\n    }\n    offset += len;\n  }\n  return items;\n}\n\n}  // namespace dynampi::detail\n</code></pre>"},{"location":"dynampi/dir_70f9944cf42b7c83e40875b744e30ff7/","title":"Dir /home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi","text":"<p>FileList &gt; dynampi &gt; mpi</p>"},{"location":"dynampi/dir_70f9944cf42b7c83e40875b744e30ff7/#files","title":"Files","text":"Type Name file mpi_communicator.hpp file mpi_error.hpp file mpi_group.hpp file mpi_types.hpp <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/</code></p>"},{"location":"dynampi/mpi__communicator_8hpp/","title":"File mpi_communicator.hpp","text":"<p>FileList &gt; dynampi &gt; mpi &gt; mpi_communicator.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;mpi.h&gt;</code></li> <li><code>#include &lt;algorithm&gt;</code></li> <li><code>#include &lt;cstddef&gt;</code></li> <li><code>#include &lt;limits&gt;</code></li> <li><code>#include &lt;optional&gt;</code></li> <li><code>#include &lt;variant&gt;</code></li> <li><code>#include \"dynampi/mpi/mpi_group.hpp\"</code></li> <li><code>#include \"dynampi/mpi/mpi_types.hpp\"</code></li> <li><code>#include \"dynampi/utilities/assert.hpp\"</code></li> <li><code>#include \"dynampi/utilities/template_options.hpp\"</code></li> <li><code>#include \"mpi_error.hpp\"</code></li> </ul>"},{"location":"dynampi/mpi__communicator_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi"},{"location":"dynampi/mpi__communicator_8hpp/#classes","title":"Classes","text":"Type Name struct CommStatistics class MPICommunicator &lt;Options&gt; struct track_statistics &lt;Mode&gt; struct track_statistics_t <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_communicator.hpp</code></p>"},{"location":"dynampi/mpi__communicator_8hpp_source/","title":"File mpi_communicator.hpp","text":"<p>File List &gt; dynampi &gt; mpi &gt; mpi_communicator.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2025 QDX Technologies. Authored by Ryan Stocks &lt;ryan.stocks00@gmail.com&gt;\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;mpi.h&gt;\n\n#include &lt;algorithm&gt;\n#include &lt;cstddef&gt;\n#include &lt;limits&gt;\n#include &lt;optional&gt;\n#include &lt;variant&gt;\n\n#include \"dynampi/mpi/mpi_group.hpp\"\n#include \"dynampi/mpi/mpi_types.hpp\"\n#include \"dynampi/utilities/assert.hpp\"\n#include \"dynampi/utilities/template_options.hpp\"\n#include \"mpi_error.hpp\"\n\nnamespace dynampi {\n\nenum class StatisticsMode {\n  None,\n  Aggregated,\n  Detailed,\n};\n\nstruct track_statistics_t {\n  static constexpr StatisticsMode value = StatisticsMode::None;\n};\n\ntemplate &lt;StatisticsMode Mode = StatisticsMode::Detailed&gt;\nstruct track_statistics : public track_statistics_t {\n  static constexpr StatisticsMode value = Mode;\n};\n\nstruct CommStatistics {\n  int send_count = 0;\n  int recv_count = 0;\n  int collective_count = 0;\n  int atomic_count = 0;\n  size_t bytes_sent = 0;\n  size_t bytes_received = 0;\n  size_t atomic_bytes = 0;\n  // Seconds spent inside the MPI calls that increment send_count / recv_count.\n  // Only accumulated under StatisticsMode::Detailed -- Aggregated deliberately\n  // skips the clock reads so counting costs nothing in the hot path.\n  double send_time = 0.0;\n  double recv_time = 0.0;\n\n  void reset() {\n    send_count = 0;\n    recv_count = 0;\n    collective_count = 0;\n    atomic_count = 0;\n    bytes_sent = 0;\n    bytes_received = 0;\n    atomic_bytes = 0;\n    send_time = 0.0;\n    recv_time = 0.0;\n  }\n\n  double average_send_size() const {\n    if (send_count == 0) return 0.0;\n    return static_cast&lt;double&gt;(bytes_sent) / send_count;\n  }\n\n  double average_receive_size() const {\n    if (recv_count == 0) return 0.0;\n    return static_cast&lt;double&gt;(bytes_received) / recv_count;\n  }\n};\n\ntemplate &lt;typename... Options&gt;\nclass MPICommunicator {\n public:\n  enum Ownership {\n    Reference,  // The communicator is not owned by this class and should not be freed.\n    Move,       // The communicator is moved into this class and will be freed in the destructor.\n    Duplicate,  // The communicator is duplicated by this class and will be freed in the destructor.\n  };\n\n private:\n  MPI_Comm m_comm;\n  Ownership m_ownership;\n\n  static constexpr StatisticsMode statistics_mode =\n      get_option_value&lt;track_statistics_t, Options...&gt;();\n  using StatisticsT =\n      std::conditional_t&lt;statistics_mode != StatisticsMode::None, CommStatistics, std::monostate&gt;;\n\n  StatisticsT _statistics;\n\n  static constexpr bool track_time = statistics_mode == StatisticsMode::Detailed;\n\n  // Runs `op`, accumulating its wall time into `accumulator` under Detailed.\n  // Compiles to a bare call to `op` in every other mode, so Aggregated pays no\n  // clock reads at all.\n  template &lt;typename F&gt;\n  inline void timed(double CommStatistics::* accumulator, F&amp;&amp; op) {\n    if constexpr (track_time) {\n      const double start = MPI_Wtime();\n      op();\n      _statistics.*accumulator += MPI_Wtime() - start;\n    } else {\n      op();\n    }\n  }\n\n public:\n  explicit MPICommunicator(MPI_Comm comm, Ownership ownership = Duplicate)\n      : m_comm(comm), m_ownership(ownership) {\n    if (m_ownership == Duplicate) {\n      DYNAMPI_MPI_CHECK(MPI_Comm_dup, (comm, &amp;m_comm));\n    }\n  }\n\n  MPICommunicator(const MPICommunicator&amp; other) = delete;\n  MPICommunicator&amp; operator=(const MPICommunicator&amp; other) = delete;\n  MPICommunicator(MPICommunicator&amp;&amp; other) noexcept\n      : m_comm(other.m_comm),\n        m_ownership(other.m_ownership),\n        _statistics(std::move(other._statistics)) {\n    other.m_comm = MPI_COMM_NULL;\n    other.m_ownership = Reference;\n  }\n  MPICommunicator&amp; operator=(MPICommunicator&amp;&amp; other) = delete;\n\n  ~MPICommunicator() {\n    if (m_ownership != Reference) {\n      MPI_Comm_free(&amp;m_comm);\n    }\n  }\n\n  MPICommunicator split_by_node() const {\n    MPI_Comm node_comm;\n    DYNAMPI_MPI_CHECK(MPI_Comm_split_type,\n                      (m_comm, MPI_COMM_TYPE_SHARED, 0, MPI_INFO_NULL, &amp;node_comm));\n    return MPICommunicator(node_comm, Move);\n  }\n\n  std::optional&lt;MPICommunicator&gt; split(int color, int key = 0) const {\n    MPI_Comm new_comm;\n    DYNAMPI_MPI_CHECK(MPI_Comm_split, (m_comm, color, key, &amp;new_comm));\n    if (new_comm == MPI_COMM_NULL) {\n      return std::nullopt;\n    }\n    assert(color != MPI_UNDEFINED &amp;&amp; \"Undefined color should not result in a valid communicator\");\n    return MPICommunicator(new_comm, Move);\n  }\n\n  operator MPI_Comm() const { return m_comm; }\n\n  const CommStatistics&amp; get_statistics() const\n    requires(statistics_mode != StatisticsMode::None)\n  {\n    return _statistics;\n  }\n\n  int rank() const {\n    int rank;\n    DYNAMPI_MPI_CHECK(MPI_Comm_rank, (m_comm, &amp;rank));\n    return rank;\n  }\n\n  int size() const {\n    int size;\n    DYNAMPI_MPI_CHECK(MPI_Comm_size, (m_comm, &amp;size));\n    return size;\n  }\n\n  template &lt;typename T&gt;\n  inline void send(const T&amp; data, int dest, int tag = 0) {\n    using mpi_type = MPI_Type&lt;T&gt;;\n    timed(&amp;CommStatistics::send_time, [&amp;] {\n      DYNAMPI_MPI_CHECK(MPI_Send, (mpi_type::ptr(data), mpi_type::count(data), mpi_type::value,\n                                   dest, tag, m_comm));\n    });\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      _statistics.send_count++;\n      int size;\n      MPI_Type_size(mpi_type::value, &amp;size);\n      _statistics.bytes_sent += mpi_type::count(data) * size;\n    }\n  }\n\n  inline MPI_Status probe(int source = MPI_ANY_SOURCE, int tag = MPI_ANY_TAG) {\n    MPI_Status status;\n    DYNAMPI_MPI_CHECK(MPI_Probe, (source, tag, m_comm, &amp;status));\n    return status;\n  }\n\n  template &lt;typename T&gt;\n  inline void recv(T&amp; data, int source, int tag = 0) {\n    using mpi_type = MPI_Type&lt;T&gt;;\n    MPI_Status status;\n    timed(&amp;CommStatistics::recv_time, [&amp;] {\n      DYNAMPI_MPI_CHECK(MPI_Recv, (mpi_type::ptr(data), mpi_type::count(data), mpi_type::value,\n                                   source, tag, m_comm, &amp;status));\n    });\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      _statistics.recv_count++;\n      int actual_count;\n      DYNAMPI_MPI_CHECK(MPI_Get_count, (&amp;status, mpi_type::value, &amp;actual_count));\n      int size;\n      MPI_Type_size(mpi_type::value, &amp;size);\n      _statistics.bytes_received += actual_count * size;\n    }\n  }\n\n  // Receive with MPI_ANY_SOURCE/MPI_ANY_TAG and return status\n  template &lt;typename T&gt;\n  inline MPI_Status recv_any(T&amp; data, int source = MPI_ANY_SOURCE, int tag = MPI_ANY_TAG) {\n    using mpi_type = MPI_Type&lt;T&gt;;\n    MPI_Status status;\n    timed(&amp;CommStatistics::recv_time, [&amp;] {\n      DYNAMPI_MPI_CHECK(MPI_Recv, (mpi_type::ptr(data), mpi_type::count(data), mpi_type::value,\n                                   source, tag, m_comm, &amp;status));\n    });\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      _statistics.recv_count++;\n      int actual_count;\n      DYNAMPI_MPI_CHECK(MPI_Get_count, (&amp;status, mpi_type::value, &amp;actual_count));\n      int size;\n      MPI_Type_size(mpi_type::value, &amp;size);\n      _statistics.bytes_received += actual_count * size;\n    }\n    return status;\n  }\n\n  template &lt;typename T&gt;\n  inline void broadcast(T&amp; data, int root = 0) {\n    using mpi_type = MPI_Type&lt;T&gt;;\n    if constexpr (mpi_type::resize_required) {\n      int size = mpi_type::count(data);\n      broadcast(size, root);\n      if (rank() != root) {\n        mpi_type::resize(data, size);\n      }\n    }\n    DYNAMPI_MPI_CHECK(MPI_Bcast,\n                      (mpi_type::ptr(data), mpi_type::count(data), mpi_type::value, root, m_comm));\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      _statistics.collective_count++;\n    }\n  }\n\n  inline void recv_empty_message(int source, int tag = 0) {\n    using mpi_type = MPI_Type&lt;std::nullptr_t&gt;;\n    timed(&amp;CommStatistics::recv_time, [&amp;] {\n      DYNAMPI_MPI_CHECK(MPI_Recv, (nullptr, mpi_type::count(nullptr), mpi_type::value, source, tag,\n                                   m_comm, MPI_STATUS_IGNORE));\n    });\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      _statistics.recv_count++;\n    }\n  }\n\n  template &lt;typename T&gt;\n  inline void send_empty(int dest, int tag = 0) {\n    using mpi_type = MPI_Type&lt;T&gt;;\n    timed(&amp;CommStatistics::send_time,\n          [&amp;] { DYNAMPI_MPI_CHECK(MPI_Send, (nullptr, 0, mpi_type::value, dest, tag, m_comm)); });\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      _statistics.send_count++;\n    }\n  }\n\n  template &lt;typename T&gt;\n  inline void recv_empty(int source, int tag = 0) {\n    using mpi_type = MPI_Type&lt;T&gt;;\n    timed(&amp;CommStatistics::recv_time, [&amp;] {\n      DYNAMPI_MPI_CHECK(MPI_Recv,\n                        (nullptr, 0, mpi_type::value, source, tag, m_comm, MPI_STATUS_IGNORE));\n    });\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      _statistics.recv_count++;\n    }\n  }\n\n  template &lt;typename T&gt;\n  inline void gather(const T&amp; data, std::vector&lt;T&gt;* result, int root = 0) {\n    DYNAMPI_ASSERT_EQ(result != nullptr, root == rank(),\n                      \"Gather result must be provided only on the root rank\");\n    using mpi_type = MPI_Type&lt;T&gt;;\n    DYNAMPI_MPI_CHECK(MPI_Gather, (mpi_type::ptr(data), mpi_type::count(data), mpi_type::value,\n                                   result == nullptr ? nullptr : result-&gt;data(),\n                                   mpi_type::count(data), mpi_type::value, root, m_comm));\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      _statistics.collective_count++;\n    }\n  }\n\n  // --- One-sided RMA (window is owned by the caller) ---\n  //\n  // Displacements are byte offsets. The target rank's window must have\n  // been created with disp_unit == 1 when using put_bytes/get_bytes.\n  //\n  // put_bytes/get_bytes chunk transfers larger than INT_MAX so MPI's plain\n  // `int` count argument never silently wraps. Statistics count one logical\n  // transfer (send_count/recv_count += 1, bytes = total n), not one entry\n  // per chunk.\n\n  static constexpr size_t kMaxRmaChunkBytes = static_cast&lt;size_t&gt;(std::numeric_limits&lt;int&gt;::max());\n\n  inline void put_bytes(const void* src, size_t n, int target_rank, MPI_Aint target_disp,\n                        MPI_Win win) {\n    if (n == 0) return;\n    const auto* bytes = static_cast&lt;const std::byte*&gt;(src);\n    timed(&amp;CommStatistics::send_time, [&amp;] {\n      size_t done = 0;\n      while (done &lt; n) {\n        const size_t chunk = std::min(kMaxRmaChunkBytes, n - done);\n        DYNAMPI_MPI_CHECK(MPI_Put, (bytes + done, static_cast&lt;int&gt;(chunk), MPI_BYTE, target_rank,\n                                    target_disp + static_cast&lt;MPI_Aint&gt;(done),\n                                    static_cast&lt;int&gt;(chunk), MPI_BYTE, win));\n        done += chunk;\n      }\n    });\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      _statistics.send_count++;\n      _statistics.bytes_sent += n;\n    }\n  }\n\n  inline void get_bytes(void* dst, size_t n, int target_rank, MPI_Aint target_disp, MPI_Win win) {\n    if (n == 0) return;\n    auto* bytes = static_cast&lt;std::byte*&gt;(dst);\n    timed(&amp;CommStatistics::recv_time, [&amp;] {\n      size_t done = 0;\n      while (done &lt; n) {\n        const size_t chunk = std::min(kMaxRmaChunkBytes, n - done);\n        DYNAMPI_MPI_CHECK(MPI_Get, (bytes + done, static_cast&lt;int&gt;(chunk), MPI_BYTE, target_rank,\n                                    target_disp + static_cast&lt;MPI_Aint&gt;(done),\n                                    static_cast&lt;int&gt;(chunk), MPI_BYTE, win));\n        done += chunk;\n      }\n    });\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      _statistics.recv_count++;\n      _statistics.bytes_received += n;\n    }\n  }\n\n  // RMA atomic: counted separately from point-to-point send/recv.\n  template &lt;typename T&gt;\n  inline void fetch_and_op(T&amp; origin, T&amp; result, int target_rank, MPI_Aint target_disp, MPI_Op op,\n                           MPI_Win win) {\n    using mpi_type = MPI_Type&lt;T&gt;;\n    static_assert(!mpi_type::resize_required, \"fetch_and_op requires a fixed-size type\");\n    DYNAMPI_MPI_CHECK(MPI_Fetch_and_op, (mpi_type::ptr(origin), mpi_type::ptr(result),\n                                         mpi_type::value, target_rank, target_disp, op, win));\n    if constexpr (statistics_mode != StatisticsMode::None) {\n      int size = 0;\n      MPI_Type_size(mpi_type::value, &amp;size);\n      const size_t bytes = static_cast&lt;size_t&gt;(mpi_type::count(origin)) * static_cast&lt;size_t&gt;(size);\n      _statistics.atomic_count++;\n      _statistics.atomic_bytes += bytes;\n    }\n  }\n\n  [[nodiscard]] MPI_Comm get() const { return m_comm; }\n};\n\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/mpi__error_8hpp/","title":"File mpi_error.hpp","text":"<p>FileList &gt; dynampi &gt; mpi &gt; mpi_error.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;mpi.h&gt;</code></li> <li><code>#include &lt;string&gt;</code></li> <li><code>#include &lt;stdexcept&gt;</code></li> </ul>"},{"location":"dynampi/mpi__error_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi"},{"location":"dynampi/mpi__error_8hpp/#macros","title":"Macros","text":"Type Name define DYNAMPI_MPI_CHECK (func, args) <code>/* multi line expression */</code>"},{"location":"dynampi/mpi__error_8hpp/#macro-definition-documentation","title":"Macro Definition Documentation","text":""},{"location":"dynampi/mpi__error_8hpp/#define-dynampi_mpi_check","title":"define DYNAMPI_MPI_CHECK","text":"<pre><code>#define DYNAMPI_MPI_CHECK (\n    func,\n    args\n) `/* multi line expression */`\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_error.hpp</code></p>"},{"location":"dynampi/mpi__error_8hpp_source/","title":"File mpi_error.hpp","text":"<p>File List &gt; dynampi &gt; mpi &gt; mpi_error.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2025 QDX Technologies. Authored by Ryan Stocks &lt;ryan.stocks00@gmail.com&gt;\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;mpi.h&gt;\n\n#include &lt;string&gt;\n\n// cppcheck-suppress preprocessorErrorDirective\n#if __has_include(&lt;source_location&gt;)\n#include &lt;source_location&gt;\n#if defined(__cpp_lib_source_location)\n#define DYNAMPI_HAS_SOURCE_LOCATION\n#endif\n#elif __has_include(&lt;experimental/source_location&gt;)\n#include &lt;experimental/source_location&gt;\n#define DYNAMPI_HAS_SOURCE_LOCATION\n#endif\n#include &lt;stdexcept&gt;\n\n#define DYNAMPI_MPI_CHECK(func, args)      \\\n  do {                                     \\\n    int err = func args;                   \\\n    if (err != MPI_SUCCESS) {              \\\n      dynampi::mpi_fail(err, #func #args); \\\n    }                                      \\\n  } while (false)\n\nnamespace dynampi {\ninline void mpi_fail(int err, std::string_view command\n#ifdef DYNAMPI_HAS_SOURCE_LOCATION\n                     ,\n                     std::source_location loc = std::source_location::current()\n#endif\n) {\n  char error_string[MPI_MAX_ERROR_STRING];\n  int length_of_error_string;\n  MPI_Error_string(err, error_string, &amp;length_of_error_string);\n  throw std::runtime_error(std::string(\"MPI error in \") + std::string(command) + \": \" +\n                           std::string(error_string, length_of_error_string)\n#ifdef DYNAMPI_HAS_SOURCE_LOCATION\n                           + \" at \" + loc.file_name() + \":\" + std::to_string(loc.line())\n#endif\n  );\n}\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/mpi__group_8hpp/","title":"File mpi_group.hpp","text":"<p>FileList &gt; dynampi &gt; mpi &gt; mpi_group.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;mpi.h&gt;</code></li> <li><code>#include \"dynampi/utilities/assert.hpp\"</code></li> <li><code>#include \"mpi_error.hpp\"</code></li> </ul>"},{"location":"dynampi/mpi__group_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi"},{"location":"dynampi/mpi__group_8hpp/#classes","title":"Classes","text":"Type Name class MPIGroup <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_group.hpp</code></p>"},{"location":"dynampi/mpi__group_8hpp_source/","title":"File mpi_group.hpp","text":"<p>File List &gt; dynampi &gt; mpi &gt; mpi_group.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2025 QDX Technologies. Authored by Ryan Stocks &lt;ryan.stocks00@gmail.com&gt;\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;mpi.h&gt;\n\n#include \"dynampi/utilities/assert.hpp\"\n#include \"mpi_error.hpp\"\n\nnamespace dynampi {\n\n// Forward declaration\ntemplate &lt;typename... Options&gt;\nclass MPICommunicator;\n\nclass MPIGroup {\n private:\n  MPI_Group m_group;\n\n public:\n  // Create from a communicator (extracts the group)\n  template &lt;typename... Options&gt;\n  explicit MPIGroup(const MPICommunicator&lt;Options...&gt;&amp; comm) {\n    DYNAMPI_MPI_CHECK(MPI_Comm_group, (comm.get(), &amp;m_group));\n  }\n\n  // Non-copyable\n  MPIGroup(const MPIGroup&amp; other) = delete;\n  MPIGroup&amp; operator=(const MPIGroup&amp; other) = delete;\n\n  // Movable\n  MPIGroup(MPIGroup&amp;&amp; other) noexcept : m_group(other.m_group) { other.m_group = MPI_GROUP_NULL; }\n  MPIGroup&amp; operator=(MPIGroup&amp;&amp; other) noexcept {\n    if (this != &amp;other) {\n      if (m_group != MPI_GROUP_NULL) {\n        MPI_Group_free(&amp;m_group);\n      }\n      m_group = other.m_group;\n      other.m_group = MPI_GROUP_NULL;\n    }\n    return *this;\n  }\n\n  ~MPIGroup() {\n    if (m_group != MPI_GROUP_NULL) {\n      MPI_Group_free(&amp;m_group);\n    }\n  }\n\n  // Translate ranks from this group to another group\n  void translate_ranks(const MPIGroup&amp; to_group, int n, const int ranks[],\n                       int translated_ranks[]) const {\n    DYNAMPI_MPI_CHECK(MPI_Group_translate_ranks,\n                      (m_group, n, ranks, to_group.m_group, translated_ranks));\n  }\n\n  // Convenience method for single rank translation\n  int translate_rank(int rank, const MPIGroup&amp; to_group) const {\n    int translated_rank;\n    translate_ranks(to_group, 1, &amp;rank, &amp;translated_rank);\n    return translated_rank;\n  }\n\n  // Get the size of this group\n  int size() const {\n    int size;\n    DYNAMPI_MPI_CHECK(MPI_Group_size, (m_group, &amp;size));\n    return size;\n  }\n\n  // Get the rank of the calling process in this group (MPI_UNDEFINED if not in group)\n  int rank() const {\n    int rank;\n    DYNAMPI_MPI_CHECK(MPI_Group_rank, (m_group, &amp;rank));\n    return rank;\n  }\n\n  operator MPI_Group() const { return m_group; }\n\n  [[nodiscard]] MPI_Group get() const { return m_group; }\n};\n\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/mpi__types_8hpp/","title":"File mpi_types.hpp","text":"<p>FileList &gt; dynampi &gt; mpi &gt; mpi_types.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;mpi.h&gt;</code></li> <li><code>#include &lt;cassert&gt;</code></li> <li><code>#include &lt;cstddef&gt;</code></li> <li><code>#include &lt;stdexcept&gt;</code></li> <li><code>#include &lt;string&gt;</code></li> <li><code>#include &lt;type_traits&gt;</code></li> <li><code>#include &lt;vector&gt;</code></li> </ul>"},{"location":"dynampi/mpi__types_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi"},{"location":"dynampi/mpi__types_8hpp/#classes","title":"Classes","text":"Type Name struct MPI_Type &lt;typename T, typename&gt; struct MPI_Type&lt; std::nullptr_t &gt; &lt;&gt; struct MPI_Type&lt; std::string &gt; &lt;&gt; struct MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt; &lt;typename T&gt; struct has_dynampi_mpi_type &lt;typename, typename&gt; struct has_dynampi_mpi_type&lt; U, std::void_t&lt; decltype(MPI_Type&lt; U &gt;::value)&gt; &gt; &lt;typename U&gt;"},{"location":"dynampi/mpi__types_8hpp/#macros","title":"Macros","text":"Type Name define DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (type, mpi_type) <code>/* multi line expression */</code>"},{"location":"dynampi/mpi__types_8hpp/#macro-definition-documentation","title":"Macro Definition Documentation","text":""},{"location":"dynampi/mpi__types_8hpp/#define-dynampi_define_primitive_mpi_type","title":"define DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE","text":"<pre><code>#define DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (\n    type,\n    mpi_type\n) `/* multi line expression */`\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/mpi/mpi_types.hpp</code></p>"},{"location":"dynampi/mpi__types_8hpp_source/","title":"File mpi_types.hpp","text":"<p>File List &gt; dynampi &gt; mpi &gt; mpi_types.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2025 QDX Technologies\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;mpi.h&gt;\n\n#include &lt;cassert&gt;\n#include &lt;cstddef&gt;\n#include &lt;stdexcept&gt;\n#include &lt;string&gt;\n#include &lt;type_traits&gt;\n#include &lt;vector&gt;\n\nnamespace dynampi {\n\ntemplate &lt;typename T, typename = void&gt;\nstruct MPI_Type {\n  static_assert(sizeof(T) == 0,\n                \"dynampi::MPI_Type&lt;T&gt; is not defined for this T. \"\n                \"Provide a specialization or use a supported primitive.\");\n};\n\n#define DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(type, mpi_type)         \\\n  template &lt;&gt;                                                     \\\n  struct MPI_Type&lt;type, void&gt; {                                   \\\n    inline static const MPI_Datatype value = mpi_type;            \\\n    inline static const bool resize_required = false;             \\\n    static int count(const type&amp;) noexcept { return 1; }          \\\n    static void resize(type&amp;, int new_size) noexcept {            \\\n      (void)new_size;                                             \\\n      assert(new_size == 1);                                      \\\n    }                                                             \\\n    static void* ptr(type&amp; t) noexcept { return &amp;t; }             \\\n    static const void* ptr(const type&amp; t) noexcept { return &amp;t; } \\\n  }\n\n// Primitives\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(char, MPI_CHAR);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(std::byte, MPI_BYTE);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(signed char, MPI_SIGNED_CHAR);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(unsigned char, MPI_UNSIGNED_CHAR);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(short, MPI_SHORT);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(unsigned short, MPI_UNSIGNED_SHORT);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(int, MPI_INT);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(unsigned int, MPI_UNSIGNED);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(long, MPI_LONG);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(unsigned long, MPI_UNSIGNED_LONG);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(long long, MPI_LONG_LONG_INT);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(unsigned long long, MPI_UNSIGNED_LONG_LONG);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(float, MPI_FLOAT);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(double, MPI_DOUBLE);\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(long double, MPI_LONG_DOUBLE);\n#if defined(MPI_CXX_BOOL)\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(bool, MPI_CXX_BOOL);\n#else\n// Fallback for when MPI_CXX_BOOL is not available (e.g. Microsoft-MPI)\nDYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE(bool, MPI_C_BOOL);\n#endif\n\ntemplate &lt;&gt;\nstruct MPI_Type&lt;std::nullptr_t&gt; {\n  inline static const MPI_Datatype value = MPI_PACKED;\n  inline static const bool resize_required = false;\n\n  static int count(const std::nullptr_t&amp;) noexcept { return 0; }\n  static void resize(std::nullptr_t&amp;, int new_size) noexcept {\n    (void)new_size;  // No-op, nullptr cannot be resized\n  }\n  static void* ptr(std::nullptr_t&amp;) noexcept { return nullptr; }\n  static const void* ptr(const std::nullptr_t&amp;) noexcept { return nullptr; }\n};\n\n// Helper trait: is there a dynampi::MPI_Type&lt;U&gt; specialization?\ntemplate &lt;typename, typename = void&gt;\nstruct has_dynampi_mpi_type : std::false_type {};\ntemplate &lt;typename U&gt;\nstruct has_dynampi_mpi_type&lt;U, std::void_t&lt;decltype(MPI_Type&lt;U&gt;::value)&gt;&gt; : std::true_type {};\n\n// How many elements of MPI_Type&lt;T&gt;::value make up one whole T.\n//\n// 1 for the primitives, whose datatype already covers the object. For a\n// fixed-size aggregate described by its scalar element type -- e.g. a struct of\n// three doubles declaring MPI_DOUBLE -- it is 3, and anything that sizes a\n// buffer \"per value\" has to account for that: the fixed-width slots in the RMA\n// window tables, and the element count of a std::vector&lt;T&gt; batch message.\n//\n// Derived from sizeof(T) rather than from count(), so it needs no instance and\n// is a stable property of the type. Genuinely variable-length types have no\n// such constant and report 1, since they size their buffers from count()\n// instead. Queried once per T and cached; MPI_Type_size needs MPI_Init, which\n// has necessarily run by the time any value is being moved.\ntemplate &lt;typename T&gt;\ninline int mpi_elements_per_value() {\n  if constexpr (MPI_Type&lt;T&gt;::resize_required) {\n    return 1;\n  } else {\n    static const int elements = [] {\n      int element_bytes = 0;\n      if (MPI_Type_size(MPI_Type&lt;T&gt;::value, &amp;element_bytes) != MPI_SUCCESS || element_bytes &lt;= 0) {\n        return 1;  // LCOV_EXCL_LINE -- only reachable if MPI_Type_size fails on a live datatype\n      }\n      const size_t per_value = sizeof(T) / static_cast&lt;size_t&gt;(element_bytes);\n      return per_value &gt; 0 ? static_cast&lt;int&gt;(per_value) : 1;\n    }();\n    return elements;\n  }\n}\n\n// std::vector&lt;T&gt; specialization (contiguous storage). Excludes vector&lt;bool&gt;.\ntemplate &lt;typename T&gt;\nstruct MPI_Type&lt;std::vector&lt;T&gt;, std::enable_if_t&lt;has_dynampi_mpi_type&lt;T&gt;::value&gt;&gt; {\n  inline static const MPI_Datatype value = MPI_Type&lt;T&gt;::value;\n  inline static const bool resize_required = true;\n\n  // Counts are in elements of `value`, not in T, so a T spanning several\n  // elements (a fixed-size aggregate) contributes all of them -- otherwise a\n  // batch message would carry only the first element of each value. count()\n  // and resize() are inverses in those units, which is exactly how every call\n  // site pairs them: MPI_Get_count -&gt; resize -&gt; recv.\n  static int count(const std::vector&lt;T&gt;&amp; vec) {\n    // Traditional MPI calls take 'int' counts; very large vectors require MPI-4 large-count APIs.\n    // Caller responsibility if vec.size() exceeds INT_MAX.\n    return static_cast&lt;int&gt;(vec.size()) * mpi_elements_per_value&lt;T&gt;();\n  }\n  static void resize(std::vector&lt;T&gt;&amp; vec, int new_size) {\n    vec.resize(static_cast&lt;size_t&gt;(new_size) / static_cast&lt;size_t&gt;(mpi_elements_per_value&lt;T&gt;()));\n  }\n  static void* ptr(std::vector&lt;T&gt;&amp; vec) noexcept { return vec.data(); }\n  static const void* ptr(const std::vector&lt;T&gt;&amp; vec) noexcept { return vec.data(); }\n\n  static_assert(!std::is_same_v&lt;bool, T&gt;,\n                \"dynampi::MPI_Type&lt;std::vector&lt;bool&gt;&gt; is not supported: \"\n                \"std::vector&lt;bool&gt; is bit-packed and not contiguous. \"\n                \"Use std::vector&lt;unsigned char&gt; or a custom container.\");\n};\n\n// Verifies what a fixed-size (resize_required == false) payload must satisfy\n// for mpi_elements_per_value&lt;T&gt;() to describe it: the object has to be a whole\n// number of datatype elements, so that per-value buffer sizing is exact.\n//\n// A struct of three doubles declaring MPI_DOUBLE satisfies this (24 = 3 x 8); a\n// struct of a double and an int declaring MPI_DOUBLE does not, and there is no\n// element count that would let a fixed-width slot or a batch message describe\n// it. Such a type needs a datatype that really covers it (MPI_Type_create_struct\n// with count() == 1) or MPI_BYTE with count() == sizeof(T).\n//\n// Throws rather than asserts: a mismatch here moves truncated data instead of\n// failing any existing size check, and NDEBUG builds are exactly where that\n// does the most damage.\ntemplate &lt;typename T&gt;\ninline void check_fixed_size_mpi_type(const char* type_role, const char* distributor_name) {\n  if constexpr (!MPI_Type&lt;T&gt;::resize_required) {\n    int element_bytes = 0;\n    // Check NULL before MPI_Type_size: with the default MPI_ERRORS_ARE_FATAL\n    // handler many implementations abort on an invalid datatype rather than\n    // returning an error code, so a C++ throw would never be reachable.\n    if (MPI_Type&lt;T&gt;::value == MPI_DATATYPE_NULL ||\n        MPI_Type_size(MPI_Type&lt;T&gt;::value, &amp;element_bytes) != MPI_SUCCESS || element_bytes &lt;= 0) {\n      throw std::invalid_argument(std::string(distributor_name) + \": could not query the MPI \" +\n                                  \"datatype of its \" + type_role + \" type\");\n    }\n    if (sizeof(T) % static_cast&lt;size_t&gt;(element_bytes) != 0) {\n      throw std::invalid_argument(\n          std::string(distributor_name) + \": the \" + type_role + \" type is \" +\n          std::to_string(sizeof(T)) +\n          \" bytes, which is not a whole number of its MPI datatype's elements (\" +\n          std::to_string(element_bytes) +\n          \" bytes each). Use a datatype that covers the whole object with count() == 1, or \"\n          \"MPI_BYTE \"\n          \"with count() == sizeof(T).\");\n    }\n  }\n}\n\n// std::string specialization\ntemplate &lt;&gt;\nstruct MPI_Type&lt;std::string&gt; {\n  inline static const MPI_Datatype value = MPI_CHAR;\n  inline static const bool resize_required = true;\n\n  static int count(const std::string&amp; str) { return static_cast&lt;int&gt;(str.size()); }\n  static void resize(std::string&amp; str, int new_size) { str.resize(static_cast&lt;size_t&gt;(new_size)); }\n  static void* ptr(std::string&amp; str) noexcept { return str.data(); }\n  static const void* ptr(const std::string&amp; str) noexcept { return str.data(); }\n};\n\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/dir_23e51883c93568b92bc8806003dcc116/","title":"Dir /home/runner/work/DynaMPI/DynaMPI/include/dynampi/utilities","text":"<p>FileList &gt; dynampi &gt; utilities</p>"},{"location":"dynampi/dir_23e51883c93568b92bc8806003dcc116/#files","title":"Files","text":"Type Name file assert.hpp file printing.hpp file template_options.hpp file timer.hpp <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/utilities/</code></p>"},{"location":"dynampi/assert_8hpp/","title":"File assert.hpp","text":"<p>FileList &gt; dynampi &gt; utilities &gt; assert.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;mpi.h&gt;</code></li> <li><code>#include &lt;exception&gt;</code></li> <li><code>#include &lt;iostream&gt;</code></li> <li><code>#include &lt;optional&gt;</code></li> <li><code>#include &lt;sstream&gt;</code></li> <li><code>#include &lt;stdexcept&gt;</code></li> <li><code>#include &lt;string&gt;</code></li> <li><code>#include &lt;string_view&gt;</code></li> <li><code>#include \"printing.hpp\"</code></li> </ul>"},{"location":"dynampi/assert_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi"},{"location":"dynampi/assert_8hpp/#macros","title":"Macros","text":"Type Name define DYNAMPI_ASSERT (condition, ...) <code>/* multi line expression */</code> define DYNAMPI_ASSERT_BIN_OP (a, b, op, nop, ...) <code>/* multi line expression */</code> define DYNAMPI_ASSERT_EQ (expr, val, ...) <code>[**DYNAMPI\\_ASSERT\\_BIN\\_OP**](assert_8hpp.md#define-dynampi_assert_bin_op)(expr, val, ==, !=, \\_\\_VA\\_ARGS\\_\\_)</code> define DYNAMPI_ASSERT_GE (expr, val, ...) <code>[**DYNAMPI\\_ASSERT\\_BIN\\_OP**](assert_8hpp.md#define-dynampi_assert_bin_op)(expr, val, &amp;gt;=, &amp;lt;, \\_\\_VA\\_ARGS\\_\\_)</code> define DYNAMPI_ASSERT_GT (expr, val, ...) <code>[**DYNAMPI\\_ASSERT\\_BIN\\_OP**](assert_8hpp.md#define-dynampi_assert_bin_op)(expr, val, &amp;gt;, &amp;lt;=, \\_\\_VA\\_ARGS\\_\\_)</code> define DYNAMPI_ASSERT_LE (expr, val, ...) <code>[**DYNAMPI\\_ASSERT\\_BIN\\_OP**](assert_8hpp.md#define-dynampi_assert_bin_op)(expr, val, &amp;lt;=, &amp;gt;, \\_\\_VA\\_ARGS\\_\\_)</code> define DYNAMPI_ASSERT_LT (expr, val, ...) <code>[**DYNAMPI\\_ASSERT\\_BIN\\_OP**](assert_8hpp.md#define-dynampi_assert_bin_op)(expr, val, &amp;lt;, &amp;gt;=, \\_\\_VA\\_ARGS\\_\\_)</code> define DYNAMPI_ASSERT_NE (expr, val, ...) <code>[**DYNAMPI\\_ASSERT\\_BIN\\_OP**](assert_8hpp.md#define-dynampi_assert_bin_op)(expr, val, !=, ==, \\_\\_VA\\_ARGS\\_\\_)</code> define DYNAMPI_FAIL (...) <code>/* multi line expression */</code> define DYNAMPI_HAS_BUILTIN (x) <code>\\_\\_has\\_builtin(x)</code> define DYNAMPI_UNIMPLEMENTED (...) <code>[**DYNAMPI\\_FAIL**](assert_8hpp.md#define-dynampi_fail)(\"DYNAMPI\\_UNIMPLEMENTED\")</code> define DYNAMPI_UNREACHABLE () <code>\\_\\_builtin\\_unreachable()</code>"},{"location":"dynampi/assert_8hpp/#macro-definition-documentation","title":"Macro Definition Documentation","text":""},{"location":"dynampi/assert_8hpp/#define-dynampi_assert","title":"define DYNAMPI_ASSERT","text":"<pre><code>#define DYNAMPI_ASSERT (\n    condition,\n    ...\n) `/* multi line expression */`\n</code></pre>"},{"location":"dynampi/assert_8hpp/#define-dynampi_assert_bin_op","title":"define DYNAMPI_ASSERT_BIN_OP","text":"<pre><code>#define DYNAMPI_ASSERT_BIN_OP (\n    a,\n    b,\n    op,\n    nop,\n    ...\n) `/* multi line expression */`\n</code></pre>"},{"location":"dynampi/assert_8hpp/#define-dynampi_assert_eq","title":"define DYNAMPI_ASSERT_EQ","text":"<pre><code>#define DYNAMPI_ASSERT_EQ (\n    expr,\n    val,\n    ...\n) `DYNAMPI_ASSERT_BIN_OP (expr, val, ==, !=, __VA_ARGS__)`\n</code></pre>"},{"location":"dynampi/assert_8hpp/#define-dynampi_assert_ge","title":"define DYNAMPI_ASSERT_GE","text":"<pre><code>#define DYNAMPI_ASSERT_GE (\n    expr,\n    val,\n    ...\n) `DYNAMPI_ASSERT_BIN_OP (expr, val, &gt;=, &lt;, __VA_ARGS__)`\n</code></pre>"},{"location":"dynampi/assert_8hpp/#define-dynampi_assert_gt","title":"define DYNAMPI_ASSERT_GT","text":"<pre><code>#define DYNAMPI_ASSERT_GT (\n    expr,\n    val,\n    ...\n) `DYNAMPI_ASSERT_BIN_OP (expr, val, &gt;, &lt;=, __VA_ARGS__)`\n</code></pre>"},{"location":"dynampi/assert_8hpp/#define-dynampi_assert_le","title":"define DYNAMPI_ASSERT_LE","text":"<pre><code>#define DYNAMPI_ASSERT_LE (\n    expr,\n    val,\n    ...\n) `DYNAMPI_ASSERT_BIN_OP (expr, val, &lt;=, &gt;, __VA_ARGS__)`\n</code></pre>"},{"location":"dynampi/assert_8hpp/#define-dynampi_assert_lt","title":"define DYNAMPI_ASSERT_LT","text":"<pre><code>#define DYNAMPI_ASSERT_LT (\n    expr,\n    val,\n    ...\n) `DYNAMPI_ASSERT_BIN_OP (expr, val, &lt;, &gt;=, __VA_ARGS__)`\n</code></pre>"},{"location":"dynampi/assert_8hpp/#define-dynampi_assert_ne","title":"define DYNAMPI_ASSERT_NE","text":"<pre><code>#define DYNAMPI_ASSERT_NE (\n    expr,\n    val,\n    ...\n) `DYNAMPI_ASSERT_BIN_OP (expr, val, !=, ==, __VA_ARGS__)`\n</code></pre>"},{"location":"dynampi/assert_8hpp/#define-dynampi_fail","title":"define DYNAMPI_FAIL","text":"<pre><code>#define DYNAMPI_FAIL (\n    ...\n) `DYNAMPI_ASSERT (false, __VA_ARGS__); \\ DYNAMPI_UNREACHABLE ()`\n</code></pre>"},{"location":"dynampi/assert_8hpp/#define-dynampi_has_builtin","title":"define DYNAMPI_HAS_BUILTIN","text":"<pre><code>#define DYNAMPI_HAS_BUILTIN (\n    x\n) `__has_builtin(x)`\n</code></pre>"},{"location":"dynampi/assert_8hpp/#define-dynampi_unimplemented","title":"define DYNAMPI_UNIMPLEMENTED","text":"<pre><code>#define DYNAMPI_UNIMPLEMENTED (\n    ...\n) `DYNAMPI_FAIL (\"DYNAMPI_UNIMPLEMENTED\")`\n</code></pre>"},{"location":"dynampi/assert_8hpp/#define-dynampi_unreachable","title":"define DYNAMPI_UNREACHABLE","text":"<pre><code>#define DYNAMPI_UNREACHABLE (\n\n) `__builtin_unreachable()`\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/utilities/assert.hpp</code></p>"},{"location":"dynampi/assert_8hpp_source/","title":"File assert.hpp","text":"<p>File List &gt; dynampi &gt; utilities &gt; assert.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2025 QDX Technologies. Authored by Ryan Stocks &lt;ryan.stocks00@gmail.com&gt;\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;mpi.h&gt;\n\n#include &lt;exception&gt;\n#ifndef _MSC_VER\n#define DYNAMPI_HAS_BUILTIN(x) __has_builtin(x)\n#else\n#define DYNAMPI_HAS_BUILTIN(x) 0\n#endif\n\n#ifndef NDEBUG\n#include &lt;iostream&gt;\n#include &lt;optional&gt;\n\n// cppcheck-suppress preprocessorErrorDirective\n#if __has_include(&lt;source_location&gt;)\n#include &lt;source_location&gt;\n#if defined(__cpp_lib_source_location)\n#define DYNAMPI_HAS_SOURCE_LOCATION\n#endif\n#elif __has_include(&lt;experimental/source_location&gt;)\n#include &lt;experimental/source_location&gt;\nnamespace std {\nusing source_location = std::experimental::source_location;\n}\n#define DYNAMPI_HAS_SOURCE_LOCATION\n#endif\n\n#include &lt;sstream&gt;\n#include &lt;stdexcept&gt;\n#include &lt;string&gt;\n#include &lt;string_view&gt;\n\n#include \"printing.hpp\"\n#endif\n\nnamespace dynampi {\n\n#ifndef NDEBUG\ntemplate &lt;typename... Args&gt;\nstd::optional&lt;std::string&gt; OptionalString(Args &amp;&amp;...args) {\n  if constexpr (sizeof...(args) == 0) {\n    return std::nullopt;\n  } else {\n    std::stringstream ss;\n    (ss &lt;&lt; ... &lt;&lt; args);\n    return ss.str();\n  }\n}\n\n#define DYNAMPI_ASSERT(condition, ...)                                                 \\\n  do {                                                                                 \\\n    if (!(condition))                                                                  \\\n      dynampi::_DYNAMPI_FAIL_ASSERT(#condition, dynampi::OptionalString(__VA_ARGS__)); \\\n  } while (false)\n\ninline void _DYNAMPI_FAIL_ASSERT(const std::string &amp;condition_str,\n                                 const std::optional&lt;std::string&gt; &amp;message\n#ifdef DYNAMPI_HAS_SOURCE_LOCATION\n                                 ,\n                                 const std::source_location &amp;loc = std::source_location::current()\n#endif\n) {\n  if (!std::uncaught_exceptions()) {\n    std::stringstream ss;\n#ifdef DYNAMPI_HAS_SOURCE_LOCATION\n    std::string_view s = loc.file_name();\n    std::string_view filename = s.substr(s.find_last_of('/') + 1);\n#endif\n    int rank = 0;\n    MPI_Comm_rank(MPI_COMM_WORLD, &amp;rank);\n    ss &lt;&lt; \"DynaMPI assertion failed on rank \" &lt;&lt; rank &lt;&lt; \": \" &lt;&lt; condition_str\n       &lt;&lt; (message ? \" \" + *message : \"\")\n\n#ifdef DYNAMPI_HAS_SOURCE_LOCATION\n       &lt;&lt; \"\\n in \" &lt;&lt; loc.function_name() &lt;&lt; \" at \" &lt;&lt; filename &lt;&lt; \":\" &lt;&lt; loc.line()\n#endif\n       &lt;&lt; std::endl;\n    std::cerr &lt;&lt; ss.str();\n    throw std::runtime_error(ss.str());\n  }\n}\n\n#define DYNAMPI_ASSERT_BIN_OP(a, b, op, nop, ...)                        \\\n  do {                                                                   \\\n    const auto A = a;                                                    \\\n    const auto B = b;                                                    \\\n    if (!((A)op(B)))                                                     \\\n      dynampi::_DYNAMPI_FAILBinOp((A), (B), (#a), (#b), (#nop),          \\\n                                  dynampi::OptionalString(__VA_ARGS__)); \\\n  } while (false)\n\ntemplate &lt;typename A, typename B&gt;\ninline void _DYNAMPI_FAILBinOp(const A &amp;a, const B &amp;b, const std::string &amp;a_str,\n                               const std::string &amp;b_str, const std::string &amp;nop,\n                               const std::optional&lt;std::string&gt; &amp;message\n#ifdef DYNAMPI_HAS_SOURCE_LOCATION\n                               ,\n                               const std::source_location &amp;loc = std::source_location::current()\n#endif\n) {\n  std::stringstream ss;\n  ss &lt;&lt; a &lt;&lt; \" \" &lt;&lt; nop &lt;&lt; \" \" &lt;&lt; b;\n  dynampi::_DYNAMPI_FAIL_ASSERT(a_str + \" \" + nop + \" \" + b_str,\n                                message ? (ss.str() + \" \" + *message) : ss.str()\n#ifdef DYNAMPI_HAS_SOURCE_LOCATION\n                                    ,\n                                loc\n#endif\n  );\n}\n\n#else\n#define DYNAMPI_ASSERT(condition, ...) \\\n  do {                                 \\\n  } while (false)\n#define DYNAMPI_ASSERT_BIN_OP(a, b, op, nop, ...) \\\n  do {                                            \\\n  } while (false)\n#endif\n\n#define DYNAMPI_FAIL(...)             \\\n  DYNAMPI_ASSERT(false, __VA_ARGS__); \\\n  DYNAMPI_UNREACHABLE()  // LCOV_EXCL_LINE\n\n#define DYNAMPI_UNIMPLEMENTED(...) DYNAMPI_FAIL(\"DYNAMPI_UNIMPLEMENTED\")\n\n#define DYNAMPI_ASSERT_GE(expr, val, ...) DYNAMPI_ASSERT_BIN_OP(expr, val, &gt;=, &lt;, __VA_ARGS__)\n#define DYNAMPI_ASSERT_LE(expr, val, ...) DYNAMPI_ASSERT_BIN_OP(expr, val, &lt;=, &gt;, __VA_ARGS__)\n#define DYNAMPI_ASSERT_GT(expr, val, ...) DYNAMPI_ASSERT_BIN_OP(expr, val, &gt;, &lt;=, __VA_ARGS__)\n#define DYNAMPI_ASSERT_LT(expr, val, ...) DYNAMPI_ASSERT_BIN_OP(expr, val, &lt;, &gt;=, __VA_ARGS__)\n#define DYNAMPI_ASSERT_EQ(expr, val, ...) DYNAMPI_ASSERT_BIN_OP(expr, val, ==, !=, __VA_ARGS__)\n#define DYNAMPI_ASSERT_NE(expr, val, ...) DYNAMPI_ASSERT_BIN_OP(expr, val, !=, ==, __VA_ARGS__)\n\n#if defined(_MSC_VER) &amp;&amp; !defined(__clang__)  // MSVC\n#define DYNAMPI_UNREACHABLE() __assume(false)\n#else  // GCC, Clang\n#define DYNAMPI_UNREACHABLE() __builtin_unreachable()\n#endif\n\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/printing_8hpp/","title":"File printing.hpp","text":"<p>FileList &gt; dynampi &gt; utilities &gt; printing.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;array&gt;</code></li> <li><code>#include &lt;cstddef&gt;</code></li> <li><code>#include &lt;cstdint&gt;</code></li> <li><code>#include &lt;iostream&gt;</code></li> <li><code>#include &lt;optional&gt;</code></li> <li><code>#include &lt;set&gt;</code></li> <li><code>#include &lt;span&gt;</code></li> <li><code>#include &lt;tuple&gt;</code></li> <li><code>#include &lt;utility&gt;</code></li> <li><code>#include &lt;vector&gt;</code></li> </ul>"},{"location":"dynampi/printing_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/utilities/printing.hpp</code></p>"},{"location":"dynampi/printing_8hpp_source/","title":"File printing.hpp","text":"<p>File List &gt; dynampi &gt; utilities &gt; printing.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2025 QDX Technologies. Authored by Ryan Stocks &lt;ryan.stocks00@gmail.com&gt;\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;array&gt;\n#include &lt;cstddef&gt;\n#include &lt;cstdint&gt;\n#include &lt;iostream&gt;\n#include &lt;optional&gt;\n#include &lt;set&gt;\n#include &lt;span&gt;\n#include &lt;tuple&gt;\n#include &lt;utility&gt;\n#include &lt;vector&gt;\n\nnamespace dynampi {\n\ntemplate &lt;typename T&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::set&lt;T&gt;&amp; set);\ntemplate &lt;typename T&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::vector&lt;T&gt;&amp; vec);\ntemplate &lt;typename T, std::size_t N&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::array&lt;T, N&gt;&amp; arr);\ntemplate &lt;typename T&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::span&lt;T&gt;&amp; vec);\ntemplate &lt;typename T&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::optional&lt;T&gt;&amp; op);\ntemplate &lt;typename... Args&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::tuple&lt;Args...&gt;&amp; tup);\ntemplate &lt;typename T, typename U&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::pair&lt;T, U&gt;&amp; pair);\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::byte&amp; b);\n\n// --------------- IMPLEMENTATIONS ---------------\n\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::byte&amp; b) {\n  return os &lt;&lt; static_cast&lt;uint32_t&gt;(b);\n}\n\ntemplate &lt;typename T&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::span&lt;T&gt;&amp; vec) {\n  os &lt;&lt; \"[\";\n  for (std::size_t i = 0; i &lt; vec.size(); i++) {\n    os &lt;&lt; vec[i];\n    if (i &lt; vec.size() - 1) {\n      os &lt;&lt; \", \";\n    }\n  }\n  return os &lt;&lt; \"]\";\n}\n\ntemplate &lt;typename T&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::vector&lt;T&gt;&amp; vec) {\n  return os &lt;&lt; std::span&lt;const T&gt;(vec.data(), vec.size());\n}\n\ntemplate &lt;typename T, std::size_t N&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::array&lt;T, N&gt;&amp; arr) {\n  os &lt;&lt; \"[\";\n  for (std::size_t i = 0; i &lt; arr.size(); i++) {\n    os &lt;&lt; arr[i];\n    if (i &lt; arr.size() - 1) {\n      os &lt;&lt; \", \";\n    }\n  }\n  return os &lt;&lt; \"]\";\n}\n\ntemplate &lt;typename T&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::set&lt;T&gt;&amp; set) {\n  os &lt;&lt; \"{\";\n  auto it = set.begin();\n  while (it != set.end()) {\n    os &lt;&lt; *it;\n    ++it;\n    if (it != set.end()) {\n      os &lt;&lt; \", \";\n    }\n  }\n  return os &lt;&lt; \"}\";\n}\n\ntemplate &lt;typename T, typename U&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::pair&lt;T, U&gt;&amp; pair) {\n  return os &lt;&lt; \"(\" &lt;&lt; pair.first &lt;&lt; \", \" &lt;&lt; pair.second &lt;&lt; \")\";\n}\n\ntemplate &lt;typename T&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::optional&lt;T&gt;&amp; op) {\n  if (op.has_value()) {\n    return os &lt;&lt; \"Some(\" &lt;&lt; op.value() &lt;&lt; \")\";\n  }\n  return os &lt;&lt; \"None\";\n}\n\ntemplate &lt;typename... Args&gt;\ninline std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const std::tuple&lt;Args...&gt;&amp; tup) {\n  os &lt;&lt; \"(\";\n  std::apply(\n      [&amp;os](const Args&amp;... args) {\n        std::size_t i = 0;\n        ((os &lt;&lt; args &lt;&lt; (++i &lt; sizeof...(Args) ? \", \" : \"\")), ...);\n      },\n      tup);\n  return os &lt;&lt; \")\";\n}\n\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/template__options_8hpp/","title":"File template_options.hpp","text":"<p>FileList &gt; dynampi &gt; utilities &gt; template_options.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;type_traits&gt;</code></li> </ul>"},{"location":"dynampi/template__options_8hpp/#classes","title":"Classes","text":"Type Name struct option_value &lt;typename Option, Options&gt; struct option_value&lt; Option, Head, Tail... &gt; &lt;typename Option, typename Head, Tail&gt;"},{"location":"dynampi/template__options_8hpp/#public-functions","title":"Public Functions","text":"Type Name consteval decltype(Option::value) get_option_value ()"},{"location":"dynampi/template__options_8hpp/#public-functions-documentation","title":"Public Functions Documentation","text":""},{"location":"dynampi/template__options_8hpp/#function-get_option_value","title":"function get_option_value","text":"<pre><code>template&lt;typename Option, typename... Options&gt;\nconsteval decltype(Option::value) get_option_value () \n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/utilities/template_options.hpp</code></p>"},{"location":"dynampi/template__options_8hpp_source/","title":"File template_options.hpp","text":"<p>File List &gt; dynampi &gt; utilities &gt; template_options.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2025 QDX Technologies. Authored by Ryan Stocks &lt;ryan.stocks00@gmail.com&gt;\n * SPDX-License-Identifier: Apache-2.0\n */\n#pragma once\n\n#include &lt;type_traits&gt;\n\ntemplate &lt;typename Option, typename... Options&gt;\nstruct option_value {\n  static constexpr decltype(Option::value) value = Option::value;\n};\n\ntemplate &lt;typename Option, typename Head, typename... Tail&gt;\nstruct option_value&lt;Option, Head, Tail...&gt; {\n  static constexpr decltype(Option::value) value = [] {\n    if constexpr (std::is_base_of_v&lt;Option, Head&gt;) {\n      // Use Head\u2019s value only if it really is derived from Option\n      return static_cast&lt;decltype(Option::value)&gt;(Head::value);\n    } else {\n      return option_value&lt;Option, Tail...&gt;::value;\n    }\n  }();\n};\n\ntemplate &lt;typename Option, typename... Options&gt;\nconsteval decltype(Option::value) get_option_value() {\n  return option_value&lt;Option, Options...&gt;::value;\n}\n</code></pre>"},{"location":"dynampi/timer_8hpp/","title":"File timer.hpp","text":"<p>FileList &gt; dynampi &gt; utilities &gt; timer.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;cassert&gt;</code></li> <li><code>#include &lt;chrono&gt;</code></li> <li><code>#include &lt;optional&gt;</code></li> <li><code>#include &lt;ostream&gt;</code></li> </ul>"},{"location":"dynampi/timer_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi"},{"location":"dynampi/timer_8hpp/#classes","title":"Classes","text":"Type Name class Timer <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/utilities/timer.hpp</code></p>"},{"location":"dynampi/timer_8hpp_source/","title":"File timer.hpp","text":"<p>File List &gt; dynampi &gt; utilities &gt; timer.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2025 QDX Technologies. Authored by Ryan Stocks &lt;ryan.stocks00@gmail.com&gt;\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;cassert&gt;\n#include &lt;chrono&gt;\n#include &lt;optional&gt;\n#include &lt;ostream&gt;\n\nnamespace dynampi {\n\nclass Timer {\n  std::optional&lt;std::chrono::time_point&lt;std::chrono::high_resolution_clock&gt;&gt; _start_time;\n  std::chrono::nanoseconds _elapsed_time{0};\n\n public:\n  enum class AutoStart { Yes, No };\n\n  explicit Timer(AutoStart auto_start = AutoStart::Yes) {\n    if (auto_start == AutoStart::Yes) {\n      start();\n    }\n  }\n\n  void start() {\n    assert(!_start_time.has_value() &amp;&amp; \"Timer already started\");\n    _start_time = std::chrono::high_resolution_clock::now();\n  }\n\n  std::chrono::duration&lt;double&gt; stop() {\n    assert(_start_time.has_value() &amp;&amp; \"Timer not started\");\n    auto end_time = std::chrono::high_resolution_clock::now();\n    // GCC 12 -O2 -Wmaybe-uninitialized false-positives on optional's inactive\n    // storage when stop() is inlined across a branch (e.g. shutdown_time).\n#if defined(__GNUC__) &amp;&amp; !defined(__clang__)\n#pragma GCC diagnostic push\n#pragma GCC diagnostic ignored \"-Wmaybe-uninitialized\"\n#endif\n    _elapsed_time +=\n        std::chrono::duration_cast&lt;std::chrono::nanoseconds&gt;(end_time - _start_time.value());\n#if defined(__GNUC__) &amp;&amp; !defined(__clang__)\n#pragma GCC diagnostic pop\n#endif\n    _start_time.reset();\n    return std::chrono::duration&lt;double&gt;(_elapsed_time);\n  }\n\n  void reset(AutoStart auto_start = AutoStart::Yes) {\n    _start_time.reset();\n    _elapsed_time = std::chrono::nanoseconds{0};\n    if (auto_start == AutoStart::Yes) {\n      start();\n    }\n  }\n\n  [[nodiscard]] std::chrono::duration&lt;double&gt; elapsed() const {\n    if (_start_time.has_value()) {\n      auto current_elapsed =\n          _elapsed_time + std::chrono::duration_cast&lt;std::chrono::nanoseconds&gt;(\n                              std::chrono::high_resolution_clock::now() - _start_time.value());\n      return std::chrono::duration&lt;double&gt;(current_elapsed);\n    }\n    return std::chrono::duration&lt;double&gt;(_elapsed_time);\n  }\n\n  friend std::ostream&amp; operator&lt;&lt;(std::ostream&amp; os, const Timer&amp; timer) {\n    return os &lt;&lt; timer.elapsed().count() &lt;&lt; \" seconds\";\n  }\n};\n\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/dynampi_8hpp/","title":"File dynampi.hpp","text":"<p>FileList &gt; dynampi &gt; dynampi.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;mpi.h&gt;</code></li> <li><code>#include &lt;cassert&gt;</code></li> <li><code>#include &lt;functional&gt;</code></li> <li><code>#include &lt;optional&gt;</code></li> <li><code>#include &lt;vector&gt;</code></li> <li><code>#include \"dynampi/impl/hierarchical_distributor.hpp\"</code></li> <li><code>#include \"dynampi/impl/naive_distributor.hpp\"</code></li> <li><code>#include \"dynampi/version.hpp\"</code></li> </ul>"},{"location":"dynampi/dynampi_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/dynampi.hpp</code></p>"},{"location":"dynampi/dynampi_8hpp_source/","title":"File dynampi.hpp","text":"<p>File List &gt; dynampi &gt; dynampi.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2025 QDX Technologies. Authored by Ryan Stocks &lt;ryan.stocks00@gmail.com&gt;\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;mpi.h&gt;\n\n#include &lt;cassert&gt;\n#include &lt;functional&gt;\n#include &lt;optional&gt;\n#include &lt;vector&gt;\n\n#include \"dynampi/impl/hierarchical_distributor.hpp\"\n#include \"dynampi/impl/naive_distributor.hpp\"\n#include \"dynampi/version.hpp\"\n\nnamespace dynampi {\n\ntemplate &lt;typename ResultT,\n          template &lt;typename, typename, typename...&gt; typename T = HierarchicalWorkDistributor&gt;\nstd::optional&lt;std::vector&lt;ResultT&gt;&gt; mpi_manager_worker_distribution(\n    size_t n_tasks, std::function&lt;ResultT(size_t)&gt; worker_function, MPI_Comm comm = MPI_COMM_WORLD,\n    int manager_rank = 0) {\n  T&lt;size_t, ResultT&gt; distributor(worker_function, {.comm = comm, .manager_rank = manager_rank});\n  if (distributor.is_root_manager()) {\n    for (size_t i = 0; i &lt; n_tasks; ++i) {\n      distributor.insert_task(i);\n    }\n    return distributor.finish_remaining_tasks();\n  }\n  return {};\n}\n\ntemplate &lt;typename TaskT, typename ResultT, typename... Options&gt;\nusing DynamicWorkDistributor = HierarchicalWorkDistributor&lt;TaskT, ResultT, Options...&gt;;\n\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/task__error_8hpp/","title":"File task_error.hpp","text":"<p>FileList &gt; dynampi &gt; task_error.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;cstdio&gt;</code></li> <li><code>#include &lt;exception&gt;</code></li> <li><code>#include &lt;optional&gt;</code></li> <li><code>#include &lt;stdexcept&gt;</code></li> <li><code>#include &lt;string&gt;</code></li> <li><code>#include &lt;utility&gt;</code></li> <li><code>#include &lt;vector&gt;</code></li> </ul>"},{"location":"dynampi/task__error_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi namespace detail"},{"location":"dynampi/task__error_8hpp/#classes","title":"Classes","text":"Type Name struct TaskError class TaskFailure <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/task_error.hpp</code></p>"},{"location":"dynampi/task__error_8hpp_source/","title":"File task_error.hpp","text":"<p>File List &gt; dynampi &gt; task_error.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2026 Ryan Stocks\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;cstdio&gt;\n#include &lt;exception&gt;\n#include &lt;optional&gt;\n#include &lt;stdexcept&gt;\n#include &lt;string&gt;\n#include &lt;utility&gt;\n#include &lt;vector&gt;\n\nnamespace dynampi {\n\n// Longest task-error message carried back to the manager. The RMA distributors\n// report errors through fixed-width slots in a preallocated window, so the\n// limit has to be a compile-time constant; the two-sided distributors truncate\n// to the same length so a failing task reports identically whichever\n// distributor ran it.\ninline constexpr size_t kMaxTaskErrorMessage = 240;\n\n// One task that threw, as reported by the rank that ran it.\nstruct TaskError {\n  // Rank within the distributor's own (duplicated) communicator.\n  int worker_rank = -1;\n  // what() of the escaping exception, truncated to kMaxTaskErrorMessage.\n  std::string message;\n};\n\n// Thrown on the manager by run_tasks()/finish_remaining_tasks() when a task\n// threw and Config::rethrow_task_errors is set. Never thrown by finalize() or\n// a destructor -- see BaseWorkDistributor's error accessors.\nclass TaskFailure : public std::runtime_error {\n public:\n  explicit TaskFailure(TaskError error)\n      : std::runtime_error(\"dynampi: task on rank \" + std::to_string(error.worker_rank) +\n                           \" threw: \" + error.message),\n        m_error(std::move(error)) {}\n\n  const TaskError&amp; error() const noexcept { return m_error; }\n\n private:\n  TaskError m_error;\n};\n\nnamespace detail {\n\n// Manager-side record of the tasks that threw, shared by every distributor.\n//\n// Each error surfaces exactly once: rethrow() hands back the oldest one and\n// drops it, so a caller that catches and calls run_tasks() again sees the next\n// one rather than the same one forever, and take() returns whatever has not\n// been thrown yet.\nclass TaskErrorLog {\n public:\n  void record(TaskError error) { m_errors.push_back(std::move(error)); }\n\n  bool empty() const { return m_errors.empty(); }\n  size_t size() const { return m_errors.size(); }\n\n  std::vector&lt;TaskError&gt; take() {\n    std::vector&lt;TaskError&gt; taken;\n    taken.swap(m_errors);\n    return taken;\n  }\n\n  // Call at a point where throwing is safe -- never from finalize() or a\n  // destructor.\n  void rethrow_first_if(bool enabled) {\n    if (!enabled || m_errors.empty()) return;\n    TaskError error = std::move(m_errors.front());\n    m_errors.erase(m_errors.begin());\n    throw TaskFailure(std::move(error));\n  }\n\n  // Destructors cannot throw, and dropping a task failure without a word is\n  // exactly the silent-corruption case this whole mechanism exists to avoid.\n  void warn_if_unreported(const char* distributor) const noexcept;\n\n private:\n  std::vector&lt;TaskError&gt; m_errors;\n};\n\n// Wire form for the tree distributors: \"&lt;rank&gt;\\n&lt;message&gt;\".\n//\n// A leaf's failure has to reach the root through however many managers sit\n// in between, and each hop only knows the child it received from -- so the\n// originating rank travels in the payload rather than being read off\n// MPI_SOURCE. The flat distributors, where the reporting rank is the\n// originating rank, send the bare message instead.\ninline std::string encode_task_error(const TaskError&amp; error) {\n  return std::to_string(error.worker_rank) + \"\\n\" + error.message;\n}\n\ninline TaskError decode_task_error(const std::string&amp; payload) {\n  const size_t split = payload.find('\\n');\n  if (split == std::string::npos) return TaskError{-1, payload};\n  TaskError error;\n  try {\n    error.worker_rank = std::stoi(payload.substr(0, split));\n  } catch (const std::exception&amp;) {\n    error.worker_rank = -1;\n  }\n  error.message = payload.substr(split + 1);\n  return error;\n}\n\ninline std::string truncate_task_error_message(const char* what) {\n  std::string message = what == nullptr ? std::string(\"unknown exception\") : std::string(what);\n  if (message.size() &gt; kMaxTaskErrorMessage) {\n    message.resize(kMaxTaskErrorMessage - 3);\n    message += \"...\";\n  }\n  return message;\n}\n\n// Runs worker_function(task), writing its result into `out`. Returns\n// std::nullopt on success, or the failure message if the task threw.\n//\n// A throwing task must not take the job down with it, and it must not leave the\n// protocol short one result either: every distributor's completion accounting\n// (contiguous result prefixes, sent-vs-received counters, completion-log\n// ranges) assumes exactly one result per dispatched task. So a failed task\n// yields a default-constructed ResultT as a placeholder and the message travels\n// to the manager out of band.\ninline void TaskErrorLog::warn_if_unreported(const char* distributor) const noexcept {\n  if (m_errors.empty()) return;\n  std::fprintf(\n      stderr, \"dynampi: %s destroyed with %zu unreported task error(s); first was on rank %d: %s\\n\",\n      distributor, m_errors.size(), m_errors.front().worker_rank, m_errors.front().message.c_str());\n}\n\ntemplate &lt;typename ResultT, typename F, typename TaskT&gt;\nstd::optional&lt;std::string&gt; run_task_guarded(F&amp; worker_function, TaskT&amp;&amp; task, ResultT&amp; out) {\n  try {\n    out = worker_function(std::forward&lt;TaskT&gt;(task));\n    return std::nullopt;\n  } catch (const std::exception&amp; e) {\n    out = ResultT{};\n    return truncate_task_error_message(e.what());\n  } catch (...) {\n    out = ResultT{};\n    return std::string(\"unknown exception (not derived from std::exception)\");\n  }\n}\n\n}  // namespace detail\n\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/version_8hpp/","title":"File version.hpp","text":"<p>FileList &gt; dynampi &gt; version.hpp</p> <p>Go to the source code of this file</p> <ul> <li><code>#include &lt;string_view&gt;</code></li> <li><code>#include &lt;tuple&gt;</code></li> </ul>"},{"location":"dynampi/version_8hpp/#namespaces","title":"Namespaces","text":"Type Name namespace dynampi namespace version"},{"location":"dynampi/version_8hpp/#macros","title":"Macros","text":"Type Name define DYNAMPI_COMMIT_HASH <code>\"unknown\"</code> define DYNAMPI_STR (x) <code>[**DYNAMPI\\_STR\\_HELPER**](version_8hpp.md#define-dynampi_str_helper)(x)</code> define DYNAMPI_STR_HELPER (x) <code>#x</code> define DYNAMPI_VERSION_MAJOR <code>0</code> define DYNAMPI_VERSION_MINOR <code>0</code> define DYNAMPI_VERSION_PATCH <code>1</code> define DYNAMPI_VERSION_STRING <code>/* multi line expression */</code>"},{"location":"dynampi/version_8hpp/#macro-definition-documentation","title":"Macro Definition Documentation","text":""},{"location":"dynampi/version_8hpp/#define-dynampi_commit_hash","title":"define DYNAMPI_COMMIT_HASH","text":"<pre><code>#define DYNAMPI_COMMIT_HASH `\"unknown\"`\n</code></pre>"},{"location":"dynampi/version_8hpp/#define-dynampi_str","title":"define DYNAMPI_STR","text":"<pre><code>#define DYNAMPI_STR (\n    x\n) `DYNAMPI_STR_HELPER (x)`\n</code></pre>"},{"location":"dynampi/version_8hpp/#define-dynampi_str_helper","title":"define DYNAMPI_STR_HELPER","text":"<pre><code>#define DYNAMPI_STR_HELPER (\n    x\n) `#x`\n</code></pre>"},{"location":"dynampi/version_8hpp/#define-dynampi_version_major","title":"define DYNAMPI_VERSION_MAJOR","text":"<pre><code>#define DYNAMPI_VERSION_MAJOR `0`\n</code></pre>"},{"location":"dynampi/version_8hpp/#define-dynampi_version_minor","title":"define DYNAMPI_VERSION_MINOR","text":"<pre><code>#define DYNAMPI_VERSION_MINOR `0`\n</code></pre>"},{"location":"dynampi/version_8hpp/#define-dynampi_version_patch","title":"define DYNAMPI_VERSION_PATCH","text":"<pre><code>#define DYNAMPI_VERSION_PATCH `1`\n</code></pre>"},{"location":"dynampi/version_8hpp/#define-dynampi_version_string","title":"define DYNAMPI_VERSION_STRING","text":"<pre><code>#define DYNAMPI_VERSION_STRING `\"v\" DYNAMPI_STR ( DYNAMPI_VERSION_MAJOR ) \".\" DYNAMPI_STR ( DYNAMPI_VERSION_MINOR ) \".\" DYNAMPI_STR ( \\ DYNAMPI_VERSION_PATCH )`\n</code></pre> <p>The documentation for this class was generated from the following file <code>/home/runner/work/DynaMPI/DynaMPI/include/dynampi/version.hpp</code></p>"},{"location":"dynampi/version_8hpp_source/","title":"File version.hpp","text":"<p>File List &gt; dynampi &gt; version.hpp</p> <p>Go to the documentation of this file</p> <pre><code>/*\n * SPDX-FileCopyrightText: 2026 Ryan Stocks\n * SPDX-License-Identifier: Apache-2.0\n */\n\n#pragma once\n\n#include &lt;string_view&gt;\n#include &lt;tuple&gt;\n\n// Version macros are normally supplied by the `dynampi` CMake target. The\n// fallbacks below keep every header usable with a plain `-I include` build (no\n// CMake, no -D flags). The top-level CMakeLists.txt parses these three #define\n// lines to derive project(DynaMPI VERSION ...), so the fallbacks are the single\n// source of truth and cannot drift from the CMake version -- keep the literal\n// `#define DYNAMPI_VERSION_&lt;part&gt; &lt;integer&gt;` shape when bumping them.\n#ifndef DYNAMPI_VERSION_MAJOR\n#define DYNAMPI_VERSION_MAJOR 0\n#endif\n#ifndef DYNAMPI_VERSION_MINOR\n#define DYNAMPI_VERSION_MINOR 0\n#endif\n#ifndef DYNAMPI_VERSION_PATCH\n#define DYNAMPI_VERSION_PATCH 1\n#endif\n\n// Set by CMake from `git rev-parse HEAD` (with a `-dirty` suffix when the tree\n// has uncommitted changes). Builds without CMake have no way to know it.\n#ifndef DYNAMPI_COMMIT_HASH\n#define DYNAMPI_COMMIT_HASH \"unknown\"\n#endif\n\nnamespace dynampi {\n\nnamespace version {\n\ninline constexpr int major = DYNAMPI_VERSION_MAJOR;\ninline constexpr int minor = DYNAMPI_VERSION_MINOR;\ninline constexpr int patch = DYNAMPI_VERSION_PATCH;\n\n// Macros for compile-time version string\n#define DYNAMPI_STR_HELPER(x) #x\n#define DYNAMPI_STR(x) DYNAMPI_STR_HELPER(x)\n#define DYNAMPI_VERSION_STRING                                                                   \\\n  \"v\" DYNAMPI_STR(DYNAMPI_VERSION_MAJOR) \".\" DYNAMPI_STR(DYNAMPI_VERSION_MINOR) \".\" DYNAMPI_STR( \\\n      DYNAMPI_VERSION_PATCH)\n\ninline constexpr std::string_view string = DYNAMPI_VERSION_STRING;\n\n[[nodiscard]] constexpr bool is_at_least(int v_major, int v_minor, int v_patch) {\n  return std::tie(major, minor, patch) &gt;= std::tie(v_major, v_minor, v_patch);\n}\n\n[[nodiscard]] inline constexpr std::string_view compile_date() { return __DATE__ \" \" __TIME__; }\n\n[[nodiscard]] inline constexpr std::string_view commit_hash() { return DYNAMPI_COMMIT_HASH; }\n\n}  // namespace version\n\n}  // namespace dynampi\n</code></pre>"},{"location":"dynampi/namespaces/","title":"Namespace List","text":"<p>Here is a list of all namespaces with brief descriptions:</p> <ul> <li>namespace dynampi <ul> <li>namespace detail </li> <li>namespace version </li> </ul> </li> <li>namespace std </li> </ul>"},{"location":"dynampi/classes/","title":"Class Index","text":""},{"location":"dynampi/classes/#b","title":"b","text":"<ul> <li>BaseWorkDistributor (dynampi)</li> <li>BridgeHop</li> </ul>"},{"location":"dynampi/classes/#c","title":"c","text":"<ul> <li>ClaimedRange (dynampi::detail::LockFreeRMALevel)</li> <li>CommStatistics (dynampi)</li> <li>Config (dynampi::BaseWorkDistributor)</li> <li>Config (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>Config (dynampi::HierarchicalWorkDistributor)</li> <li>Config (dynampi::LockFreeRMAWorkDistributor)</li> <li>Config (dynampi::NaiveWorkDistributor)</li> <li>Config (dynampi::detail::LockFreeRMALevel)</li> </ul>"},{"location":"dynampi/classes/#e","title":"e","text":"<ul> <li>enable_prioritization (dynampi)</li> </ul>"},{"location":"dynampi/classes/#h","title":"h","text":"<ul> <li>has_dynampi_mpi_type (dynampi)</li> <li>has_dynampi_mpi_type&lt; U, std::void_t&lt; decltype(MPI_Type&lt; U &gt;::value)&gt; &gt; (dynampi)</li> <li>HierarchicalLockFreeRMAWorkDistributor (dynampi)</li> <li>HierarchicalWorkDistributor (dynampi)</li> </ul>"},{"location":"dynampi/classes/#l","title":"l","text":"<ul> <li>LockFreeRMALevel (dynampi::detail)</li> <li>LockFreeRMAWorkDistributor (dynampi)</li> </ul>"},{"location":"dynampi/classes/#m","title":"m","text":"<ul> <li>MPI_Type (dynampi)</li> <li>MPI_Type&lt; std::nullptr_t &gt; (dynampi)</li> <li>MPI_Type&lt; std::string &gt; (dynampi)</li> <li>MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt; (dynampi)</li> <li>MPICommunicator (dynampi)</li> <li>MPIGroup (dynampi)</li> </ul>"},{"location":"dynampi/classes/#n","title":"n","text":"<ul> <li>NaiveWorkDistributor (dynampi)</li> </ul>"},{"location":"dynampi/classes/#o","title":"o","text":"<ul> <li>option_value</li> <li>option_value&lt; Option, Head, Tail... &gt;</li> </ul>"},{"location":"dynampi/classes/#p","title":"p","text":"<ul> <li>PendingRelay</li> <li>prioritize_tasks_t (dynampi)</li> </ul>"},{"location":"dynampi/classes/#r","title":"r","text":"<ul> <li>RunConfig (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>RunConfig (dynampi::HierarchicalWorkDistributor)</li> <li>RunConfig (dynampi::LockFreeRMAWorkDistributor)</li> <li>RunConfig (dynampi::NaiveWorkDistributor)</li> </ul>"},{"location":"dynampi/classes/#s","title":"s","text":"<ul> <li>Statistics (dynampi::LockFreeRMAWorkDistributor)</li> <li>Statistics (dynampi::NaiveWorkDistributor)</li> <li>Statistics</li> </ul>"},{"location":"dynampi/classes/#t","title":"t","text":"<ul> <li>TaskError (dynampi)</li> <li>TaskErrorLog</li> <li>TaskFailure (dynampi)</li> <li>TaskRequest</li> <li>Timer (dynampi)</li> <li>track_statistics (dynampi)</li> <li>track_statistics_t (dynampi)</li> </ul>"},{"location":"dynampi/hierarchy/","title":"Class Hierarchy","text":"<p>This inheritance list is sorted roughly, but not completely, alphabetically:</p> <ul> <li>class dynampi::BaseWorkDistributor </li> <li>class dynampi::HierarchicalLockFreeRMAWorkDistributor </li> <li>class dynampi::LockFreeRMAWorkDistributor </li> <li>class dynampi::MPICommunicator </li> <li>class dynampi::MPIGroup </li> <li>class dynampi::NaiveWorkDistributor </li> <li>class dynampi::Timer </li> <li>class dynampi::detail::LockFreeRMALevel </li> <li>class dynampi::detail::TaskErrorLog </li> <li>struct dynampi::BaseWorkDistributor::Config </li> <li>struct dynampi::CommStatistics </li> <li>struct dynampi::HierarchicalLockFreeRMAWorkDistributor::Config </li> <li>struct dynampi::HierarchicalLockFreeRMAWorkDistributor::RunConfig </li> <li>struct dynampi::HierarchicalWorkDistributor::Config </li> <li>struct dynampi::HierarchicalWorkDistributor::RunConfig </li> <li>struct dynampi::LockFreeRMAWorkDistributor::Config </li> <li>struct dynampi::LockFreeRMAWorkDistributor::RunConfig </li> <li>struct dynampi::LockFreeRMAWorkDistributor::Statistics </li> <li>struct dynampi::MPI_Type </li> <li>struct dynampi::MPI_Type&lt; std::nullptr_t &gt; </li> <li>struct dynampi::MPI_Type&lt; std::string &gt; </li> <li>struct dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt; </li> <li>struct dynampi::NaiveWorkDistributor::Config </li> <li>struct dynampi::NaiveWorkDistributor::RunConfig </li> <li>struct dynampi::NaiveWorkDistributor::Statistics </li> <li>struct dynampi::TaskError </li> <li>struct dynampi::detail::LockFreeRMALevel::ClaimedRange </li> <li>struct dynampi::detail::LockFreeRMALevel::Config </li> <li>struct dynampi::prioritize_tasks_t <ul> <li>struct dynampi::enable_prioritization </li> </ul> </li> <li>struct dynampi::track_statistics_t <ul> <li>struct dynampi::track_statistics </li> </ul> </li> <li>struct dynampi::HierarchicalLockFreeRMAWorkDistributor::BridgeHop </li> <li>struct dynampi::HierarchicalLockFreeRMAWorkDistributor::PendingRelay </li> <li>struct dynampi::HierarchicalWorkDistributor::Statistics </li> <li>struct dynampi::HierarchicalWorkDistributor::TaskRequest </li> <li>struct option_value </li> <li>struct option_value&lt; Option, Head, Tail... &gt; </li> <li>class std::runtime_error <ul> <li>class dynampi::TaskFailure </li> </ul> </li> <li>class std::false_type <ul> <li>struct dynampi::has_dynampi_mpi_type </li> </ul> </li> <li>class std::true_type <ul> <li>struct dynampi::has_dynampi_mpi_type&lt; U, std::void_t&lt; decltype(MPI_Type&lt; U &gt;::value)&gt; &gt; </li> </ul> </li> </ul>"},{"location":"dynampi/modules/","title":"Modules","text":"<p>No modules found.</p>"},{"location":"dynampi/pages/","title":"Related Pages","text":"<p>Here is a list of all related documentation pages:</p>"},{"location":"dynampi/class_members/","title":"Class Members","text":""},{"location":"dynampi/class_members/#a","title":"a","text":"<ul> <li>auto_run_workers (dynampi::BaseWorkDistributor::Config, dynampi::HierarchicalLockFreeRMAWorkDistributor::Config, dynampi::HierarchicalWorkDistributor::Config, dynampi::LockFreeRMAWorkDistributor::Config, dynampi::NaiveWorkDistributor::Config)</li> <li>atomic_bytes (dynampi::CommStatistics)</li> <li>atomic_count (dynampi::CommStatistics)</li> <li>average_receive_size (dynampi::CommStatistics)</li> <li>average_send_size (dynampi::CommStatistics)</li> <li>assign_hop_layers (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>allow_more_than_target_tasks (dynampi::HierarchicalLockFreeRMAWorkDistributor::RunConfig, dynampi::HierarchicalWorkDistributor::RunConfig, dynampi::LockFreeRMAWorkDistributor::RunConfig, dynampi::NaiveWorkDistributor::RunConfig)</li> <li>allocate_task_to_child (dynampi::HierarchicalWorkDistributor)</li> <li>atomic_read (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>atomic_set (dynampi::LockFreeRMAWorkDistributor)</li> <li>active_worker_count (dynampi::NaiveWorkDistributor)</li> <li>AutoStart (dynampi::Timer)</li> </ul>"},{"location":"dynampi/class_members/#b","title":"b","text":"<ul> <li>bytes_received (dynampi::CommStatistics)</li> <li>bytes_sent (dynampi::CommStatistics)</li> <li>bridge_hop_done (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>build_upper_hops (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>Base (dynampi::HierarchicalWorkDistributor)</li> <li>batch_size_multiplier (dynampi::HierarchicalWorkDistributor::Config)</li> <li>broadcast (dynampi::MPICommunicator)</li> <li>broadcast_done (dynampi::NaiveWorkDistributor)</li> <li>BridgeHop (dynampi::HierarchicalLockFreeRMAWorkDistributor::BridgeHop)</li> </ul>"},{"location":"dynampi/class_members/#c","title":"c","text":"<ul> <li>comm (dynampi::BaseWorkDistributor::Config, dynampi::HierarchicalLockFreeRMAWorkDistributor::Config, dynampi::HierarchicalWorkDistributor::Config, dynampi::LockFreeRMAWorkDistributor::Config, dynampi::NaiveWorkDistributor::Config, dynampi::detail::LockFreeRMALevel::Config)</li> <li>collective_count (dynampi::CommStatistics)</li> <li>claim_and_execute (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>collect_level_errors (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>create_statistics (dynampi::HierarchicalWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>CONTROL_BYTES (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>Comm (dynampi::LockFreeRMAWorkDistributor)</li> <li>comm_statistics (dynampi::LockFreeRMAWorkDistributor::Statistics, dynampi::NaiveWorkDistributor::Statistics, dynampi::HierarchicalWorkDistributor::Statistics)</li> <li>count (dynampi::MPI_Type&lt; std::nullptr_t &gt;, dynampi::MPI_Type&lt; std::string &gt;, dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;)</li> <li>collect_available_results (dynampi::NaiveWorkDistributor)</li> <li>check_finished (dynampi::detail::LockFreeRMALevel)</li> <li>claim_width (dynampi::detail::LockFreeRMALevel)</li> <li>comm_size (dynampi::detail::LockFreeRMALevel)</li> <li>child (dynampi::HierarchicalLockFreeRMAWorkDistributor::BridgeHop)</li> <li>child_len (dynampi::HierarchicalLockFreeRMAWorkDistributor::PendingRelay)</li> </ul>"},{"location":"dynampi/class_members/#d","title":"d","text":"<ul> <li>drain_results (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::LockFreeRMAWorkDistributor)</li> <li>distribute_current_round (dynampi::HierarchicalWorkDistributor)</li> <li>drain_inflight_results_at_shutdown (dynampi::HierarchicalWorkDistributor)</li> <li>drained (dynampi::detail::LockFreeRMALevel)</li> </ul>"},{"location":"dynampi/class_members/#e","title":"e","text":"<ul> <li>emplace_upper_level (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>ERROR_COUNT_OFF (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>ERROR_SLOT_BYTES (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>E_DATA (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>E_RANK (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>error_slot (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>ensure_result_capacity (dynampi::NaiveWorkDistributor)</li> <li>error (dynampi::TaskFailure)</li> <li>elapsed (dynampi::Timer)</li> <li>empty (dynampi::detail::TaskErrorLog)</li> </ul>"},{"location":"dynampi/class_members/#f","title":"f","text":"<ul> <li>finalize (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>finish_remaining_tasks (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>FINISHED_OFF (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>fetch_add (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>flush_local (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>flush_remote (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>fetch_and_op (dynampi::MPICommunicator)</li> <li>finish_marked (dynampi::HierarchicalLockFreeRMAWorkDistributor::BridgeHop)</li> </ul>"},{"location":"dynampi/class_members/#g","title":"g","text":"<ul> <li>gather_once (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::LockFreeRMAWorkDistributor)</li> <li>get_next_task_to_send (dynampi::HierarchicalWorkDistributor)</li> <li>get_parent_target (dynampi::HierarchicalWorkDistributor)</li> <li>get_statistics (dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::MPICommunicator, dynampi::NaiveWorkDistributor)</li> <li>get_bytes_local (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>get_ring_bytes_local (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>gather (dynampi::MPICommunicator)</li> <li>get (dynampi::MPICommunicator, dynampi::MPIGroup)</li> <li>get_bytes (dynampi::MPICommunicator)</li> </ul>"},{"location":"dynampi/class_members/#h","title":"h","text":"<ul> <li>HierarchicalLockFreeRMAWorkDistributor (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>harvest_top_level (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>has_task_errors (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>hops_done (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>HierarchicalWorkDistributor (dynampi::HierarchicalWorkDistributor)</li> <li>HEAD_OFF (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>harvest_ready_results (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>harvest_task_errors (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>handle_error_message (dynampi::NaiveWorkDistributor)</li> <li>handle_result_message (dynampi::NaiveWorkDistributor)</li> <li>harvest_ready_results_throttled (dynampi::detail::LockFreeRMALevel)</li> </ul>"},{"location":"dynampi/class_members/#i","title":"i","text":"<ul> <li>insert_task (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>insert_tasks (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>is_root_manager (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>idx_for_worker (dynampi::HierarchicalWorkDistributor)</li> <li>is_leaf_worker (dynampi::HierarchicalWorkDistributor)</li> <li>initialize_window (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>idle_wait (dynampi::detail::LockFreeRMALevel)</li> <li>is_owner (dynampi::detail::LockFreeRMALevel)</li> </ul>"},{"location":"dynampi/class_members/#k","title":"k","text":"<ul> <li>kMaxTasksRequested (dynampi::HierarchicalWorkDistributor)</li> <li>kMaxRecordedErrors (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>kMaxRmaChunkBytes (dynampi::MPICommunicator)</li> </ul>"},{"location":"dynampi/class_members/#l","title":"l","text":"<ul> <li>last_upper_level (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>LOG_ENTRY_BYTES (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>LockFreeRMAWorkDistributor (dynampi::LockFreeRMAWorkDistributor)</li> <li>log_slot (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>LockFreeRMALevel (dynampi::detail::LockFreeRMALevel)</li> <li>local_load_i64 (dynampi::detail::LockFreeRMALevel)</li> <li>local_only (dynampi::detail::LockFreeRMALevel)</li> <li>local_store_i64 (dynampi::detail::LockFreeRMALevel)</li> <li>layers (dynampi::HierarchicalLockFreeRMAWorkDistributor::BridgeHop)</li> </ul>"},{"location":"dynampi/class_members/#m","title":"m","text":"<ul> <li>manager_rank (dynampi::BaseWorkDistributor::Config, dynampi::HierarchicalLockFreeRMAWorkDistributor::Config, dynampi::HierarchicalWorkDistributor::Config, dynampi::LockFreeRMAWorkDistributor::Config, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor::Config)</li> <li>max_local_group_size (dynampi::HierarchicalLockFreeRMAWorkDistributor::Config, dynampi::HierarchicalWorkDistributor::Config)</li> <li>max_local_tasks (dynampi::HierarchicalLockFreeRMAWorkDistributor::Config)</li> <li>max_pending_rounds (dynampi::HierarchicalLockFreeRMAWorkDistributor::Config)</li> <li>max_result_count (dynampi::HierarchicalLockFreeRMAWorkDistributor::Config, dynampi::LockFreeRMAWorkDistributor::Config, dynampi::detail::LockFreeRMALevel::Config)</li> <li>max_task_count (dynampi::HierarchicalLockFreeRMAWorkDistributor::Config, dynampi::LockFreeRMAWorkDistributor::Config, dynampi::detail::LockFreeRMALevel::Config)</li> <li>max_tasks (dynampi::HierarchicalLockFreeRMAWorkDistributor::Config, dynampi::LockFreeRMAWorkDistributor::Config, dynampi::detail::LockFreeRMALevel::Config)</li> <li>max_upper_fanout (dynampi::HierarchicalLockFreeRMAWorkDistributor::Config, dynampi::HierarchicalWorkDistributor::Config)</li> <li>max_seconds (dynampi::HierarchicalLockFreeRMAWorkDistributor::RunConfig, dynampi::HierarchicalWorkDistributor::RunConfig, dynampi::LockFreeRMAWorkDistributor::RunConfig, dynampi::NaiveWorkDistributor::RunConfig)</li> <li>m_config (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_finalized (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>m_local_collected_count (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>m_local_comm (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>m_local_level (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>m_local_task_store (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>m_owned_upper_levels (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>m_parent_level (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>m_results (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor)</li> <li>m_solo (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>m_task_errors (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>m_upper_comms (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>m_worker_function (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>m_world_comm (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>MPICommunicator (dynampi::HierarchicalWorkDistributor, dynampi::MPICommunicator, dynampi::NaiveWorkDistributor)</li> <li>manager_per_node (dynampi::HierarchicalWorkDistributor::Config)</li> <li>max_workers_per_manager (dynampi::HierarchicalWorkDistributor::Config, dynampi::HierarchicalWorkDistributor)</li> <li>m_cached_parent_target (dynampi::HierarchicalWorkDistributor)</li> <li>m_communicator (dynampi::HierarchicalWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>m_done (dynampi::HierarchicalWorkDistributor)</li> <li>m_free_worker_indices (dynampi::HierarchicalWorkDistributor)</li> <li>m_leader_parent_group (dynampi::HierarchicalWorkDistributor)</li> <li>m_local_group (dynampi::HierarchicalWorkDistributor)</li> <li>m_owned_leader_levels (dynampi::HierarchicalWorkDistributor)</li> <li>m_prefetched_tasks (dynampi::HierarchicalWorkDistributor)</li> <li>m_request_outstanding (dynampi::HierarchicalWorkDistributor)</li> <li>m_results_received_from_child (dynampi::HierarchicalWorkDistributor)</li> <li>m_results_sent_to_parent (dynampi::HierarchicalWorkDistributor)</li> <li>m_round_active (dynampi::HierarchicalWorkDistributor)</li> <li>m_subtree_leaf_count (dynampi::HierarchicalWorkDistributor)</li> <li>m_tasks_executed (dynampi::HierarchicalWorkDistributor)</li> <li>m_tasks_received_from_parent (dynampi::HierarchicalWorkDistributor)</li> <li>m_tasks_sent_to_child (dynampi::HierarchicalWorkDistributor)</li> <li>m_unallocated_task_queue (dynampi::HierarchicalWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>m_world_group (dynampi::HierarchicalWorkDistributor)</li> <li>m_collected_count (dynampi::LockFreeRMAWorkDistributor)</li> <li>m_comm (dynampi::LockFreeRMAWorkDistributor, dynampi::MPICommunicator, dynampi::detail::LockFreeRMALevel)</li> <li>m_error_base (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_errors_seen (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_log_base (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_max_result_count (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_max_task_count (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_result_base (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_result_elem (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_result_slot_stride (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_returned_count (dynampi::LockFreeRMAWorkDistributor)</li> <li>m_statistics (dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>m_task_base (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_task_elem (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_task_slot_stride (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_task_store (dynampi::LockFreeRMAWorkDistributor)</li> <li>m_total_tasks (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_window (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_window_buffer (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_worker_window (dynampi::LockFreeRMAWorkDistributor)</li> <li>make_statistics (dynampi::LockFreeRMAWorkDistributor)</li> <li>m_ownership (dynampi::MPICommunicator)</li> <li>MPIGroup (dynampi::MPIGroup)</li> <li>m_group (dynampi::MPIGroup)</li> <li>m_free_worker_ranks (dynampi::NaiveWorkDistributor)</li> <li>m_front_result_idx (dynampi::NaiveWorkDistributor)</li> <li>m_known_contiguous_results (dynampi::NaiveWorkDistributor)</li> <li>m_pending_results (dynampi::NaiveWorkDistributor)</li> <li>m_pending_results_valid (dynampi::NaiveWorkDistributor)</li> <li>m_tasks_sent (dynampi::NaiveWorkDistributor)</li> <li>m_worker_current_task_indices (dynampi::NaiveWorkDistributor)</li> <li>message (dynampi::TaskError)</li> <li>m_error (dynampi::TaskFailure)</li> <li>m_cached_head (dynampi::detail::LockFreeRMALevel)</li> <li>m_cached_total (dynampi::detail::LockFreeRMALevel)</li> <li>m_claim_width (dynampi::detail::LockFreeRMALevel)</li> <li>m_owner_collected_count (dynampi::detail::LockFreeRMALevel)</li> <li>m_owner_errors (dynampi::detail::LockFreeRMALevel)</li> <li>m_owner_unordered_completed (dynampi::detail::LockFreeRMALevel)</li> <li>m_peer_window (dynampi::detail::LockFreeRMALevel)</li> <li>m_pending_end (dynampi::detail::LockFreeRMALevel)</li> <li>m_pending_start (dynampi::detail::LockFreeRMALevel)</li> <li>m_seen_finished (dynampi::detail::LockFreeRMALevel)</li> <li>mark_finished (dynampi::detail::LockFreeRMALevel)</li> <li>m_errors (dynampi::detail::TaskErrorLog)</li> </ul>"},{"location":"dynampi/class_members/#n","title":"n","text":"<ul> <li>num_direct_children (dynampi::HierarchicalWorkDistributor)</li> <li>num_workers (dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>NaiveWorkDistributor (dynampi::NaiveWorkDistributor)</li> <li>num_tasks_requested (dynampi::HierarchicalWorkDistributor::TaskRequest)</li> </ul>"},{"location":"dynampi/class_members/#o","title":"o","text":"<ul> <li>ordered (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>Ownership (dynampi::MPICommunicator)</li> <li>operator MPI_Comm (dynampi::MPICommunicator)</li> <li>operator= (dynampi::MPICommunicator, dynampi::MPIGroup, dynampi::detail::LockFreeRMALevel)</li> <li>operator MPI_Group (dynampi::MPIGroup)</li> <li>owner_rank (dynampi::detail::LockFreeRMALevel::Config)</li> <li>owner_available_capacity (dynampi::detail::LockFreeRMALevel)</li> <li>owner_collected_count (dynampi::detail::LockFreeRMALevel)</li> <li>owner_published_count (dynampi::detail::LockFreeRMALevel)</li> <li>owner_unordered_completed_estimate (dynampi::detail::LockFreeRMALevel)</li> </ul>"},{"location":"dynampi/class_members/#p","title":"p","text":"<ul> <li>prioritize_tasks (dynampi::BaseWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>pipeline_depth (dynampi::HierarchicalWorkDistributor::Config)</li> <li>post_fetch_and_op (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>post_get_bytes (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>post_put_bytes (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>publish_tasks (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>probe (dynampi::MPICommunicator)</li> <li>put_bytes (dynampi::MPICommunicator)</li> <li>ptr (dynampi::MPI_Type&lt; std::nullptr_t &gt;, dynampi::MPI_Type&lt; std::string &gt;, dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;)</li> <li>pop_next_task (dynampi::NaiveWorkDistributor)</li> <li>process_incoming_message (dynampi::NaiveWorkDistributor)</li> <li>put_bytes_owner (dynampi::detail::LockFreeRMALevel)</li> <li>put_ring_bytes (dynampi::detail::LockFreeRMALevel)</li> <li>parent (dynampi::HierarchicalLockFreeRMAWorkDistributor::BridgeHop)</li> <li>pending_relays (dynampi::HierarchicalLockFreeRMAWorkDistributor::BridgeHop)</li> <li>pending_task_count (dynampi::HierarchicalLockFreeRMAWorkDistributor::BridgeHop)</li> <li>parent_start (dynampi::HierarchicalLockFreeRMAWorkDistributor::PendingRelay)</li> </ul>"},{"location":"dynampi/class_members/#q","title":"q","text":"<ul> <li>QueueT (dynampi::BaseWorkDistributor, dynampi::NaiveWorkDistributor)</li> </ul>"},{"location":"dynampi/class_members/#r","title":"r","text":"<ul> <li>recv_count (dynampi::CommStatistics)</li> <li>recv_time (dynampi::CommStatistics)</li> <li>reset (dynampi::CommStatistics, dynampi::Timer)</li> <li>RMALevel (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>rethrow_task_errors (dynampi::HierarchicalLockFreeRMAWorkDistributor::Config, dynampi::HierarchicalWorkDistributor::Config, dynampi::LockFreeRMAWorkDistributor::Config, dynampi::NaiveWorkDistributor::Config)</li> <li>remaining_tasks_count (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>run_leaf_leader_worker (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>run_local_worker (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>run_node_manager (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>run_one_solo_task (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>run_tasks (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>run_worker (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>receive_batch (dynampi::HierarchicalWorkDistributor)</li> <li>receive_done_from (dynampi::HierarchicalWorkDistributor)</li> <li>receive_error_from (dynampi::HierarchicalWorkDistributor)</li> <li>receive_execute_return_task_from (dynampi::HierarchicalWorkDistributor)</li> <li>receive_from_anyone (dynampi::HierarchicalWorkDistributor)</li> <li>receive_request_batch_from (dynampi::HierarchicalWorkDistributor)</li> <li>receive_request_from (dynampi::HierarchicalWorkDistributor)</li> <li>receive_result_batch_from (dynampi::HierarchicalWorkDistributor)</li> <li>receive_result_from (dynampi::HierarchicalWorkDistributor)</li> <li>receive_task_batch_from (dynampi::HierarchicalWorkDistributor)</li> <li>release_prefetched_tasks (dynampi::HierarchicalWorkDistributor)</li> <li>request_next_batch_if_room (dynampi::HierarchicalWorkDistributor)</li> <li>result_mpi_type (dynampi::HierarchicalWorkDistributor)</li> <li>run_intermediate_worker (dynampi::HierarchicalWorkDistributor)</li> <li>R_COUNT (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>R_DATA (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>read_task_batch (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>report_task_error (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>result_slot (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>ring_slot (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>run_one_task_locally (dynampi::LockFreeRMAWorkDistributor)</li> <li>rank (dynampi::MPICommunicator, dynampi::MPIGroup)</li> <li>recv (dynampi::MPICommunicator)</li> <li>recv_any (dynampi::MPICommunicator)</li> <li>recv_empty (dynampi::MPICommunicator)</li> <li>recv_empty_message (dynampi::MPICommunicator)</li> <li>resize (dynampi::MPI_Type&lt; std::nullptr_t &gt;, dynampi::MPI_Type&lt; std::string &gt;, dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;)</li> <li>resize_required (dynampi::MPI_Type&lt; std::nullptr_t &gt;, dynampi::MPI_Type&lt; std::string &gt;, dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;)</li> <li>rank_to_worker_idx (dynampi::NaiveWorkDistributor)</li> <li>run_task_locally (dynampi::NaiveWorkDistributor)</li> <li>relay_buffer (dynampi::HierarchicalLockFreeRMAWorkDistributor::BridgeHop)</li> <li>relay_error_pending (dynampi::HierarchicalLockFreeRMAWorkDistributor::BridgeHop)</li> <li>record (dynampi::detail::TaskErrorLog)</li> <li>rethrow_first_if (dynampi::detail::TaskErrorLog)</li> </ul>"},{"location":"dynampi/class_members/#s","title":"s","text":"<ul> <li>send_count (dynampi::CommStatistics)</li> <li>send_time (dynampi::CommStatistics)</li> <li>setup_levels (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>setup_topology (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>setup_upper_chain (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>step_bridge_hop (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>StatisticsT (dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::MPICommunicator, dynampi::NaiveWorkDistributor)</li> <li>send_batch (dynampi::HierarchicalWorkDistributor)</li> <li>send_done_to_children_when_free (dynampi::HierarchicalWorkDistributor)</li> <li>send_results_to_parent (dynampi::HierarchicalWorkDistributor)</li> <li>send_to_parent (dynampi::HierarchicalWorkDistributor)</li> <li>setup_leader_hierarchy (dynampi::HierarchicalWorkDistributor)</li> <li>statistics_mode (dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::MPICommunicator, dynampi::NaiveWorkDistributor)</li> <li>subtree_leaf_count (dynampi::HierarchicalWorkDistributor)</li> <li>subtree_manager_layers (dynampi::HierarchicalWorkDistributor)</li> <li>send (dynampi::MPICommunicator)</li> <li>send_empty (dynampi::MPICommunicator)</li> <li>size (dynampi::MPICommunicator, dynampi::MPIGroup, dynampi::detail::TaskErrorLog)</li> <li>split (dynampi::MPICommunicator)</li> <li>split_by_node (dynampi::MPICommunicator)</li> <li>send_next_task_to_worker (dynampi::NaiveWorkDistributor)</li> <li>store_completed_task (dynampi::NaiveWorkDistributor)</li> <li>start (dynampi::Timer, dynampi::detail::LockFreeRMALevel::ClaimedRange)</li> <li>stop (dynampi::Timer)</li> </ul>"},{"location":"dynampi/class_members/#t","title":"t","text":"<ul> <li>target_num_tasks (dynampi::HierarchicalLockFreeRMAWorkDistributor::RunConfig, dynampi::HierarchicalWorkDistributor::RunConfig, dynampi::LockFreeRMAWorkDistributor::RunConfig, dynampi::NaiveWorkDistributor::RunConfig)</li> <li>take_task_errors (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>top_level (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>Tag (dynampi::HierarchicalWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>take_tasks_from_queue (dynampi::HierarchicalWorkDistributor)</li> <li>task_mpi_type (dynampi::HierarchicalWorkDistributor)</li> <li>TOTAL_OFF (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>T_COUNT (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>T_DATA (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>task_slot (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>timed (dynampi::MPICommunicator)</li> <li>track_time (dynampi::MPICommunicator)</li> <li>translate_rank (dynampi::MPIGroup)</li> <li>translate_ranks (dynampi::MPIGroup)</li> <li>TaskFailure (dynampi::TaskFailure)</li> <li>Timer (dynampi::Timer)</li> <li>tasks (dynampi::detail::LockFreeRMALevel::ClaimedRange)</li> <li>take_errors (dynampi::detail::LockFreeRMALevel)</li> <li>try_claim (dynampi::detail::LockFreeRMALevel)</li> <li>total_claimed (dynampi::HierarchicalLockFreeRMAWorkDistributor::BridgeHop)</li> <li>take (dynampi::detail::TaskErrorLog)</li> </ul>"},{"location":"dynampi/class_members/#u","title":"u","text":"<ul> <li>UpperLevelRole (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>update_contiguous_results_count (dynampi::NaiveWorkDistributor)</li> </ul>"},{"location":"dynampi/class_members/#v","title":"v","text":"<ul> <li>virtual_rank (dynampi::HierarchicalWorkDistributor)</li> <li>value (dynampi::MPI_Type&lt; std::nullptr_t &gt;, dynampi::MPI_Type&lt; std::string &gt;, dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;, dynampi::enable_prioritization, dynampi::prioritize_tasks_t, dynampi::track_statistics, dynampi::track_statistics_t, option_value, option_value&lt; Option, Head, Tail... &gt;)</li> </ul>"},{"location":"dynampi/class_members/#w","title":"w","text":"<ul> <li>wait_for_work_or_flush (dynampi::HierarchicalWorkDistributor)</li> <li>worker_for_idx (dynampi::HierarchicalWorkDistributor)</li> <li>write_result_range (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>worker_task_counts (dynampi::NaiveWorkDistributor::Statistics, dynampi::HierarchicalWorkDistributor::Statistics)</li> <li>worker_idx_to_rank (dynampi::NaiveWorkDistributor)</li> <li>worker_rank (dynampi::TaskError, dynampi::HierarchicalWorkDistributor::TaskRequest)</li> <li>warn_if_unreported (dynampi::detail::TaskErrorLog)</li> </ul>"},{"location":"dynampi/class_members/#_1","title":"~","text":"<ul> <li>~HierarchicalLockFreeRMAWorkDistributor (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>~HierarchicalWorkDistributor (dynampi::HierarchicalWorkDistributor)</li> <li>~LockFreeRMAWorkDistributor (dynampi::LockFreeRMAWorkDistributor)</li> <li>~MPICommunicator (dynampi::MPICommunicator)</li> <li>~MPIGroup (dynampi::MPIGroup)</li> <li>~NaiveWorkDistributor (dynampi::NaiveWorkDistributor)</li> <li>~LockFreeRMALevel (dynampi::detail::LockFreeRMALevel)</li> </ul>"},{"location":"dynampi/class_members/#_","title":"_","text":"<ul> <li>_statistics (dynampi::HierarchicalWorkDistributor, dynampi::MPICommunicator)</li> <li>_elapsed_time (dynampi::Timer)</li> <li>_start_time (dynampi::Timer)</li> </ul>"},{"location":"dynampi/class_member_functions/","title":"Class Member Functions","text":""},{"location":"dynampi/class_member_functions/#a","title":"a","text":"<ul> <li>average_receive_size (dynampi::CommStatistics)</li> <li>average_send_size (dynampi::CommStatistics)</li> <li>assign_hop_layers (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>allocate_task_to_child (dynampi::HierarchicalWorkDistributor)</li> <li>atomic_read (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>atomic_set (dynampi::LockFreeRMAWorkDistributor)</li> <li>active_worker_count (dynampi::NaiveWorkDistributor)</li> </ul>"},{"location":"dynampi/class_member_functions/#b","title":"b","text":"<ul> <li>bridge_hop_done (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>build_upper_hops (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>broadcast (dynampi::MPICommunicator)</li> <li>broadcast_done (dynampi::NaiveWorkDistributor)</li> <li>BridgeHop (dynampi::HierarchicalLockFreeRMAWorkDistributor::BridgeHop)</li> </ul>"},{"location":"dynampi/class_member_functions/#c","title":"c","text":"<ul> <li>claim_and_execute (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>collect_level_errors (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>create_statistics (dynampi::HierarchicalWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>count (dynampi::MPI_Type&lt; std::nullptr_t &gt;, dynampi::MPI_Type&lt; std::string &gt;, dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;)</li> <li>collect_available_results (dynampi::NaiveWorkDistributor)</li> <li>check_finished (dynampi::detail::LockFreeRMALevel)</li> <li>claim_width (dynampi::detail::LockFreeRMALevel)</li> <li>comm_size (dynampi::detail::LockFreeRMALevel)</li> </ul>"},{"location":"dynampi/class_member_functions/#d","title":"d","text":"<ul> <li>drain_results (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::LockFreeRMAWorkDistributor)</li> <li>distribute_current_round (dynampi::HierarchicalWorkDistributor)</li> <li>drain_inflight_results_at_shutdown (dynampi::HierarchicalWorkDistributor)</li> <li>drained (dynampi::detail::LockFreeRMALevel)</li> </ul>"},{"location":"dynampi/class_member_functions/#e","title":"e","text":"<ul> <li>emplace_upper_level (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>error_slot (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>ensure_result_capacity (dynampi::NaiveWorkDistributor)</li> <li>error (dynampi::TaskFailure)</li> <li>elapsed (dynampi::Timer)</li> <li>empty (dynampi::detail::TaskErrorLog)</li> </ul>"},{"location":"dynampi/class_member_functions/#f","title":"f","text":"<ul> <li>finalize (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>finish_remaining_tasks (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>fetch_add (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>flush_local (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>flush_remote (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>fetch_and_op (dynampi::MPICommunicator)</li> </ul>"},{"location":"dynampi/class_member_functions/#g","title":"g","text":"<ul> <li>gather_once (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::LockFreeRMAWorkDistributor)</li> <li>get_next_task_to_send (dynampi::HierarchicalWorkDistributor)</li> <li>get_parent_target (dynampi::HierarchicalWorkDistributor)</li> <li>get_statistics (dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::MPICommunicator, dynampi::NaiveWorkDistributor)</li> <li>get_bytes_local (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>get_ring_bytes_local (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>gather (dynampi::MPICommunicator)</li> <li>get (dynampi::MPICommunicator, dynampi::MPIGroup)</li> <li>get_bytes (dynampi::MPICommunicator)</li> </ul>"},{"location":"dynampi/class_member_functions/#h","title":"h","text":"<ul> <li>HierarchicalLockFreeRMAWorkDistributor (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>harvest_top_level (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>has_task_errors (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>hops_done (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>HierarchicalWorkDistributor (dynampi::HierarchicalWorkDistributor)</li> <li>harvest_ready_results (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>harvest_task_errors (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>handle_error_message (dynampi::NaiveWorkDistributor)</li> <li>handle_result_message (dynampi::NaiveWorkDistributor)</li> <li>harvest_ready_results_throttled (dynampi::detail::LockFreeRMALevel)</li> </ul>"},{"location":"dynampi/class_member_functions/#i","title":"i","text":"<ul> <li>insert_task (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>insert_tasks (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>is_root_manager (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>idx_for_worker (dynampi::HierarchicalWorkDistributor)</li> <li>is_leaf_worker (dynampi::HierarchicalWorkDistributor)</li> <li>initialize_window (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>idle_wait (dynampi::detail::LockFreeRMALevel)</li> <li>is_owner (dynampi::detail::LockFreeRMALevel)</li> </ul>"},{"location":"dynampi/class_member_functions/#l","title":"l","text":"<ul> <li>last_upper_level (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>LockFreeRMAWorkDistributor (dynampi::LockFreeRMAWorkDistributor)</li> <li>log_slot (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>LockFreeRMALevel (dynampi::detail::LockFreeRMALevel)</li> <li>local_load_i64 (dynampi::detail::LockFreeRMALevel)</li> <li>local_only (dynampi::detail::LockFreeRMALevel)</li> <li>local_store_i64 (dynampi::detail::LockFreeRMALevel)</li> </ul>"},{"location":"dynampi/class_member_functions/#m","title":"m","text":"<ul> <li>max_workers_per_manager (dynampi::HierarchicalWorkDistributor)</li> <li>make_statistics (dynampi::LockFreeRMAWorkDistributor)</li> <li>manager_rank (dynampi::LockFreeRMAWorkDistributor)</li> <li>MPICommunicator (dynampi::MPICommunicator)</li> <li>MPIGroup (dynampi::MPIGroup)</li> <li>mark_finished (dynampi::detail::LockFreeRMALevel)</li> </ul>"},{"location":"dynampi/class_member_functions/#n","title":"n","text":"<ul> <li>num_direct_children (dynampi::HierarchicalWorkDistributor)</li> <li>num_workers (dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>NaiveWorkDistributor (dynampi::NaiveWorkDistributor)</li> </ul>"},{"location":"dynampi/class_member_functions/#o","title":"o","text":"<ul> <li>operator MPI_Comm (dynampi::MPICommunicator)</li> <li>operator= (dynampi::MPICommunicator, dynampi::MPIGroup, dynampi::detail::LockFreeRMALevel)</li> <li>operator MPI_Group (dynampi::MPIGroup)</li> <li>owner_available_capacity (dynampi::detail::LockFreeRMALevel)</li> <li>owner_collected_count (dynampi::detail::LockFreeRMALevel)</li> <li>owner_published_count (dynampi::detail::LockFreeRMALevel)</li> <li>owner_unordered_completed_estimate (dynampi::detail::LockFreeRMALevel)</li> </ul>"},{"location":"dynampi/class_member_functions/#p","title":"p","text":"<ul> <li>post_fetch_and_op (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>post_get_bytes (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>post_put_bytes (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>publish_tasks (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>probe (dynampi::MPICommunicator)</li> <li>put_bytes (dynampi::MPICommunicator)</li> <li>ptr (dynampi::MPI_Type&lt; std::nullptr_t &gt;, dynampi::MPI_Type&lt; std::string &gt;, dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;)</li> <li>pop_next_task (dynampi::NaiveWorkDistributor)</li> <li>process_incoming_message (dynampi::NaiveWorkDistributor)</li> <li>put_bytes_owner (dynampi::detail::LockFreeRMALevel)</li> <li>put_ring_bytes (dynampi::detail::LockFreeRMALevel)</li> </ul>"},{"location":"dynampi/class_member_functions/#r","title":"r","text":"<ul> <li>reset (dynampi::CommStatistics, dynampi::Timer)</li> <li>remaining_tasks_count (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>run_leaf_leader_worker (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>run_local_worker (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>run_node_manager (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>run_one_solo_task (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>run_tasks (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>run_worker (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>receive_batch (dynampi::HierarchicalWorkDistributor)</li> <li>receive_done_from (dynampi::HierarchicalWorkDistributor)</li> <li>receive_error_from (dynampi::HierarchicalWorkDistributor)</li> <li>receive_execute_return_task_from (dynampi::HierarchicalWorkDistributor)</li> <li>receive_from_anyone (dynampi::HierarchicalWorkDistributor)</li> <li>receive_request_batch_from (dynampi::HierarchicalWorkDistributor)</li> <li>receive_request_from (dynampi::HierarchicalWorkDistributor)</li> <li>receive_result_batch_from (dynampi::HierarchicalWorkDistributor)</li> <li>receive_result_from (dynampi::HierarchicalWorkDistributor)</li> <li>receive_task_batch_from (dynampi::HierarchicalWorkDistributor)</li> <li>release_prefetched_tasks (dynampi::HierarchicalWorkDistributor)</li> <li>request_next_batch_if_room (dynampi::HierarchicalWorkDistributor)</li> <li>run_intermediate_worker (dynampi::HierarchicalWorkDistributor)</li> <li>read_task_batch (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>report_task_error (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>result_slot (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>ring_slot (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>run_one_task_locally (dynampi::LockFreeRMAWorkDistributor)</li> <li>rank (dynampi::MPICommunicator, dynampi::MPIGroup)</li> <li>recv (dynampi::MPICommunicator)</li> <li>recv_any (dynampi::MPICommunicator)</li> <li>recv_empty (dynampi::MPICommunicator)</li> <li>recv_empty_message (dynampi::MPICommunicator)</li> <li>resize (dynampi::MPI_Type&lt; std::nullptr_t &gt;, dynampi::MPI_Type&lt; std::string &gt;, dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;)</li> <li>rank_to_worker_idx (dynampi::NaiveWorkDistributor)</li> <li>run_task_locally (dynampi::NaiveWorkDistributor)</li> <li>record (dynampi::detail::TaskErrorLog)</li> <li>rethrow_first_if (dynampi::detail::TaskErrorLog)</li> </ul>"},{"location":"dynampi/class_member_functions/#s","title":"s","text":"<ul> <li>setup_levels (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>setup_topology (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>setup_upper_chain (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>step_bridge_hop (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>send_batch (dynampi::HierarchicalWorkDistributor)</li> <li>send_done_to_children_when_free (dynampi::HierarchicalWorkDistributor)</li> <li>send_results_to_parent (dynampi::HierarchicalWorkDistributor)</li> <li>send_to_parent (dynampi::HierarchicalWorkDistributor)</li> <li>setup_leader_hierarchy (dynampi::HierarchicalWorkDistributor)</li> <li>subtree_leaf_count (dynampi::HierarchicalWorkDistributor)</li> <li>subtree_manager_layers (dynampi::HierarchicalWorkDistributor)</li> <li>send (dynampi::MPICommunicator)</li> <li>send_empty (dynampi::MPICommunicator)</li> <li>size (dynampi::MPICommunicator, dynampi::MPIGroup, dynampi::detail::TaskErrorLog)</li> <li>split (dynampi::MPICommunicator)</li> <li>split_by_node (dynampi::MPICommunicator)</li> <li>send_next_task_to_worker (dynampi::NaiveWorkDistributor)</li> <li>store_completed_task (dynampi::NaiveWorkDistributor)</li> <li>start (dynampi::Timer)</li> <li>stop (dynampi::Timer)</li> </ul>"},{"location":"dynampi/class_member_functions/#t","title":"t","text":"<ul> <li>take_task_errors (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>top_level (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>take_tasks_from_queue (dynampi::HierarchicalWorkDistributor)</li> <li>task_slot (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>timed (dynampi::MPICommunicator)</li> <li>translate_rank (dynampi::MPIGroup)</li> <li>translate_ranks (dynampi::MPIGroup)</li> <li>TaskFailure (dynampi::TaskFailure)</li> <li>Timer (dynampi::Timer)</li> <li>take_errors (dynampi::detail::LockFreeRMALevel)</li> <li>try_claim (dynampi::detail::LockFreeRMALevel)</li> <li>take (dynampi::detail::TaskErrorLog)</li> </ul>"},{"location":"dynampi/class_member_functions/#u","title":"u","text":"<ul> <li>update_contiguous_results_count (dynampi::NaiveWorkDistributor)</li> </ul>"},{"location":"dynampi/class_member_functions/#v","title":"v","text":"<ul> <li>virtual_rank (dynampi::HierarchicalWorkDistributor)</li> </ul>"},{"location":"dynampi/class_member_functions/#w","title":"w","text":"<ul> <li>wait_for_work_or_flush (dynampi::HierarchicalWorkDistributor)</li> <li>worker_for_idx (dynampi::HierarchicalWorkDistributor)</li> <li>write_result_range (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>worker_idx_to_rank (dynampi::NaiveWorkDistributor)</li> <li>warn_if_unreported (dynampi::detail::TaskErrorLog)</li> </ul>"},{"location":"dynampi/class_member_functions/#_1","title":"~","text":"<ul> <li>~HierarchicalLockFreeRMAWorkDistributor (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>~HierarchicalWorkDistributor (dynampi::HierarchicalWorkDistributor)</li> <li>~LockFreeRMAWorkDistributor (dynampi::LockFreeRMAWorkDistributor)</li> <li>~MPICommunicator (dynampi::MPICommunicator)</li> <li>~MPIGroup (dynampi::MPIGroup)</li> <li>~NaiveWorkDistributor (dynampi::NaiveWorkDistributor)</li> <li>~LockFreeRMALevel (dynampi::detail::LockFreeRMALevel)</li> </ul>"},{"location":"dynampi/class_member_variables/","title":"Class Member Variables","text":""},{"location":"dynampi/class_member_variables/#a","title":"a","text":"<ul> <li>auto_run_workers (dynampi::BaseWorkDistributor::Config, dynampi::HierarchicalLockFreeRMAWorkDistributor::Config, dynampi::HierarchicalWorkDistributor::Config, dynampi::LockFreeRMAWorkDistributor::Config, dynampi::NaiveWorkDistributor::Config)</li> <li>atomic_bytes (dynampi::CommStatistics)</li> <li>atomic_count (dynampi::CommStatistics)</li> <li>allow_more_than_target_tasks (dynampi::HierarchicalLockFreeRMAWorkDistributor::RunConfig, dynampi::HierarchicalWorkDistributor::RunConfig, dynampi::LockFreeRMAWorkDistributor::RunConfig, dynampi::NaiveWorkDistributor::RunConfig)</li> </ul>"},{"location":"dynampi/class_member_variables/#b","title":"b","text":"<ul> <li>bytes_received (dynampi::CommStatistics)</li> <li>bytes_sent (dynampi::CommStatistics)</li> <li>batch_size_multiplier (dynampi::HierarchicalWorkDistributor::Config)</li> </ul>"},{"location":"dynampi/class_member_variables/#c","title":"c","text":"<ul> <li>comm (dynampi::BaseWorkDistributor::Config, dynampi::HierarchicalLockFreeRMAWorkDistributor::Config, dynampi::HierarchicalWorkDistributor::Config, dynampi::LockFreeRMAWorkDistributor::Config, dynampi::NaiveWorkDistributor::Config, dynampi::detail::LockFreeRMALevel::Config)</li> <li>collective_count (dynampi::CommStatistics)</li> <li>CONTROL_BYTES (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>comm_statistics (dynampi::LockFreeRMAWorkDistributor::Statistics, dynampi::NaiveWorkDistributor::Statistics, dynampi::HierarchicalWorkDistributor::Statistics)</li> <li>child (dynampi::HierarchicalLockFreeRMAWorkDistributor::BridgeHop)</li> <li>child_len (dynampi::HierarchicalLockFreeRMAWorkDistributor::PendingRelay)</li> </ul>"},{"location":"dynampi/class_member_variables/#e","title":"e","text":"<ul> <li>ERROR_COUNT_OFF (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>ERROR_SLOT_BYTES (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>E_DATA (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>E_RANK (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> </ul>"},{"location":"dynampi/class_member_variables/#f","title":"f","text":"<ul> <li>FINISHED_OFF (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>finish_marked (dynampi::HierarchicalLockFreeRMAWorkDistributor::BridgeHop)</li> </ul>"},{"location":"dynampi/class_member_variables/#h","title":"h","text":"<ul> <li>HEAD_OFF (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> </ul>"},{"location":"dynampi/class_member_variables/#k","title":"k","text":"<ul> <li>kMaxTasksRequested (dynampi::HierarchicalWorkDistributor)</li> <li>kMaxRecordedErrors (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>kMaxRmaChunkBytes (dynampi::MPICommunicator)</li> </ul>"},{"location":"dynampi/class_member_variables/#l","title":"l","text":"<ul> <li>LOG_ENTRY_BYTES (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>layers (dynampi::HierarchicalLockFreeRMAWorkDistributor::BridgeHop)</li> </ul>"},{"location":"dynampi/class_member_variables/#m","title":"m","text":"<ul> <li>manager_rank (dynampi::BaseWorkDistributor::Config, dynampi::HierarchicalLockFreeRMAWorkDistributor::Config, dynampi::HierarchicalWorkDistributor::Config, dynampi::LockFreeRMAWorkDistributor::Config, dynampi::NaiveWorkDistributor::Config)</li> <li>max_local_group_size (dynampi::HierarchicalLockFreeRMAWorkDistributor::Config, dynampi::HierarchicalWorkDistributor::Config)</li> <li>max_local_tasks (dynampi::HierarchicalLockFreeRMAWorkDistributor::Config)</li> <li>max_pending_rounds (dynampi::HierarchicalLockFreeRMAWorkDistributor::Config)</li> <li>max_result_count (dynampi::HierarchicalLockFreeRMAWorkDistributor::Config, dynampi::LockFreeRMAWorkDistributor::Config, dynampi::detail::LockFreeRMALevel::Config)</li> <li>max_task_count (dynampi::HierarchicalLockFreeRMAWorkDistributor::Config, dynampi::LockFreeRMAWorkDistributor::Config, dynampi::detail::LockFreeRMALevel::Config)</li> <li>max_tasks (dynampi::HierarchicalLockFreeRMAWorkDistributor::Config, dynampi::LockFreeRMAWorkDistributor::Config, dynampi::detail::LockFreeRMALevel::Config)</li> <li>max_upper_fanout (dynampi::HierarchicalLockFreeRMAWorkDistributor::Config, dynampi::HierarchicalWorkDistributor::Config)</li> <li>max_seconds (dynampi::HierarchicalLockFreeRMAWorkDistributor::RunConfig, dynampi::HierarchicalWorkDistributor::RunConfig, dynampi::LockFreeRMAWorkDistributor::RunConfig, dynampi::NaiveWorkDistributor::RunConfig)</li> <li>m_config (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_finalized (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>m_local_collected_count (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>m_local_comm (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>m_local_level (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>m_local_task_store (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>m_owned_upper_levels (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>m_parent_level (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>m_results (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor)</li> <li>m_solo (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>m_task_errors (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>m_upper_comms (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>m_worker_function (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>m_world_comm (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>manager_per_node (dynampi::HierarchicalWorkDistributor::Config)</li> <li>max_workers_per_manager (dynampi::HierarchicalWorkDistributor::Config)</li> <li>m_cached_parent_target (dynampi::HierarchicalWorkDistributor)</li> <li>m_communicator (dynampi::HierarchicalWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>m_done (dynampi::HierarchicalWorkDistributor)</li> <li>m_free_worker_indices (dynampi::HierarchicalWorkDistributor)</li> <li>m_leader_parent_group (dynampi::HierarchicalWorkDistributor)</li> <li>m_local_group (dynampi::HierarchicalWorkDistributor)</li> <li>m_owned_leader_levels (dynampi::HierarchicalWorkDistributor)</li> <li>m_prefetched_tasks (dynampi::HierarchicalWorkDistributor)</li> <li>m_request_outstanding (dynampi::HierarchicalWorkDistributor)</li> <li>m_results_received_from_child (dynampi::HierarchicalWorkDistributor)</li> <li>m_results_sent_to_parent (dynampi::HierarchicalWorkDistributor)</li> <li>m_round_active (dynampi::HierarchicalWorkDistributor)</li> <li>m_subtree_leaf_count (dynampi::HierarchicalWorkDistributor)</li> <li>m_tasks_executed (dynampi::HierarchicalWorkDistributor)</li> <li>m_tasks_received_from_parent (dynampi::HierarchicalWorkDistributor)</li> <li>m_tasks_sent_to_child (dynampi::HierarchicalWorkDistributor)</li> <li>m_unallocated_task_queue (dynampi::HierarchicalWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>m_world_group (dynampi::HierarchicalWorkDistributor)</li> <li>m_collected_count (dynampi::LockFreeRMAWorkDistributor)</li> <li>m_comm (dynampi::LockFreeRMAWorkDistributor, dynampi::MPICommunicator, dynampi::detail::LockFreeRMALevel)</li> <li>m_error_base (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_errors_seen (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_log_base (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_max_result_count (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_max_task_count (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_result_base (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_result_elem (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_result_slot_stride (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_returned_count (dynampi::LockFreeRMAWorkDistributor)</li> <li>m_statistics (dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>m_task_base (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_task_elem (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_task_slot_stride (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_task_store (dynampi::LockFreeRMAWorkDistributor)</li> <li>m_total_tasks (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_window (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_window_buffer (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>m_worker_window (dynampi::LockFreeRMAWorkDistributor)</li> <li>m_ownership (dynampi::MPICommunicator)</li> <li>m_group (dynampi::MPIGroup)</li> <li>m_free_worker_ranks (dynampi::NaiveWorkDistributor)</li> <li>m_front_result_idx (dynampi::NaiveWorkDistributor)</li> <li>m_known_contiguous_results (dynampi::NaiveWorkDistributor)</li> <li>m_pending_results (dynampi::NaiveWorkDistributor)</li> <li>m_pending_results_valid (dynampi::NaiveWorkDistributor)</li> <li>m_tasks_sent (dynampi::NaiveWorkDistributor)</li> <li>m_worker_current_task_indices (dynampi::NaiveWorkDistributor)</li> <li>message (dynampi::TaskError)</li> <li>m_error (dynampi::TaskFailure)</li> <li>m_cached_head (dynampi::detail::LockFreeRMALevel)</li> <li>m_cached_total (dynampi::detail::LockFreeRMALevel)</li> <li>m_claim_width (dynampi::detail::LockFreeRMALevel)</li> <li>m_owner_collected_count (dynampi::detail::LockFreeRMALevel)</li> <li>m_owner_errors (dynampi::detail::LockFreeRMALevel)</li> <li>m_owner_unordered_completed (dynampi::detail::LockFreeRMALevel)</li> <li>m_peer_window (dynampi::detail::LockFreeRMALevel)</li> <li>m_pending_end (dynampi::detail::LockFreeRMALevel)</li> <li>m_pending_start (dynampi::detail::LockFreeRMALevel)</li> <li>m_seen_finished (dynampi::detail::LockFreeRMALevel)</li> <li>m_errors (dynampi::detail::TaskErrorLog)</li> </ul>"},{"location":"dynampi/class_member_variables/#n","title":"n","text":"<ul> <li>num_tasks_requested (dynampi::HierarchicalWorkDistributor::TaskRequest)</li> </ul>"},{"location":"dynampi/class_member_variables/#o","title":"o","text":"<ul> <li>ordered (dynampi::HierarchicalLockFreeRMAWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>owner_rank (dynampi::detail::LockFreeRMALevel::Config)</li> </ul>"},{"location":"dynampi/class_member_variables/#p","title":"p","text":"<ul> <li>prioritize_tasks (dynampi::BaseWorkDistributor, dynampi::HierarchicalWorkDistributor, dynampi::NaiveWorkDistributor)</li> <li>pipeline_depth (dynampi::HierarchicalWorkDistributor::Config)</li> <li>parent (dynampi::HierarchicalLockFreeRMAWorkDistributor::BridgeHop)</li> <li>pending_relays (dynampi::HierarchicalLockFreeRMAWorkDistributor::BridgeHop)</li> <li>pending_task_count (dynampi::HierarchicalLockFreeRMAWorkDistributor::BridgeHop)</li> <li>parent_start (dynampi::HierarchicalLockFreeRMAWorkDistributor::PendingRelay)</li> </ul>"},{"location":"dynampi/class_member_variables/#r","title":"r","text":"<ul> <li>recv_count (dynampi::CommStatistics)</li> <li>recv_time (dynampi::CommStatistics)</li> <li>rethrow_task_errors (dynampi::HierarchicalLockFreeRMAWorkDistributor::Config, dynampi::HierarchicalWorkDistributor::Config, dynampi::LockFreeRMAWorkDistributor::Config, dynampi::NaiveWorkDistributor::Config)</li> <li>R_COUNT (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>R_DATA (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>resize_required (dynampi::MPI_Type&lt; std::nullptr_t &gt;, dynampi::MPI_Type&lt; std::string &gt;, dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;)</li> <li>relay_buffer (dynampi::HierarchicalLockFreeRMAWorkDistributor::BridgeHop)</li> <li>relay_error_pending (dynampi::HierarchicalLockFreeRMAWorkDistributor::BridgeHop)</li> </ul>"},{"location":"dynampi/class_member_variables/#s","title":"s","text":"<ul> <li>send_count (dynampi::CommStatistics)</li> <li>send_time (dynampi::CommStatistics)</li> <li>statistics_mode (dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::MPICommunicator, dynampi::NaiveWorkDistributor)</li> <li>start (dynampi::detail::LockFreeRMALevel::ClaimedRange)</li> </ul>"},{"location":"dynampi/class_member_variables/#t","title":"t","text":"<ul> <li>target_num_tasks (dynampi::HierarchicalLockFreeRMAWorkDistributor::RunConfig, dynampi::HierarchicalWorkDistributor::RunConfig, dynampi::LockFreeRMAWorkDistributor::RunConfig, dynampi::NaiveWorkDistributor::RunConfig)</li> <li>TOTAL_OFF (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>T_COUNT (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>T_DATA (dynampi::LockFreeRMAWorkDistributor, dynampi::detail::LockFreeRMALevel)</li> <li>track_time (dynampi::MPICommunicator)</li> <li>tasks (dynampi::detail::LockFreeRMALevel::ClaimedRange)</li> <li>total_claimed (dynampi::HierarchicalLockFreeRMAWorkDistributor::BridgeHop)</li> </ul>"},{"location":"dynampi/class_member_variables/#v","title":"v","text":"<ul> <li>value (dynampi::MPI_Type&lt; std::nullptr_t &gt;, dynampi::MPI_Type&lt; std::string &gt;, dynampi::MPI_Type&lt; std::vector&lt; T &gt;, std::enable_if_t&lt; has_dynampi_mpi_type&lt; T &gt;::value &gt; &gt;, dynampi::enable_prioritization, dynampi::prioritize_tasks_t, dynampi::track_statistics, dynampi::track_statistics_t, option_value, option_value&lt; Option, Head, Tail... &gt;)</li> </ul>"},{"location":"dynampi/class_member_variables/#w","title":"w","text":"<ul> <li>worker_task_counts (dynampi::NaiveWorkDistributor::Statistics, dynampi::HierarchicalWorkDistributor::Statistics)</li> <li>worker_rank (dynampi::TaskError, dynampi::HierarchicalWorkDistributor::TaskRequest)</li> </ul>"},{"location":"dynampi/class_member_variables/#_","title":"_","text":"<ul> <li>_statistics (dynampi::HierarchicalWorkDistributor, dynampi::MPICommunicator)</li> <li>_elapsed_time (dynampi::Timer)</li> <li>_start_time (dynampi::Timer)</li> </ul>"},{"location":"dynampi/class_member_typedefs/","title":"Class Member Typedefs","text":""},{"location":"dynampi/class_member_typedefs/#b","title":"b","text":"<ul> <li>Base (dynampi::HierarchicalWorkDistributor)</li> </ul>"},{"location":"dynampi/class_member_typedefs/#c","title":"c","text":"<ul> <li>Comm (dynampi::LockFreeRMAWorkDistributor)</li> </ul>"},{"location":"dynampi/class_member_typedefs/#m","title":"m","text":"<ul> <li>MPICommunicator (dynampi::HierarchicalWorkDistributor, dynampi::NaiveWorkDistributor)</li> </ul>"},{"location":"dynampi/class_member_typedefs/#q","title":"q","text":"<ul> <li>QueueT (dynampi::BaseWorkDistributor, dynampi::NaiveWorkDistributor)</li> </ul>"},{"location":"dynampi/class_member_typedefs/#r","title":"r","text":"<ul> <li>RMALevel (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> <li>result_mpi_type (dynampi::HierarchicalWorkDistributor)</li> </ul>"},{"location":"dynampi/class_member_typedefs/#s","title":"s","text":"<ul> <li>StatisticsT (dynampi::HierarchicalWorkDistributor, dynampi::LockFreeRMAWorkDistributor, dynampi::MPICommunicator, dynampi::NaiveWorkDistributor)</li> </ul>"},{"location":"dynampi/class_member_typedefs/#t","title":"t","text":"<ul> <li>task_mpi_type (dynampi::HierarchicalWorkDistributor)</li> </ul>"},{"location":"dynampi/class_member_enums/","title":"Class Member Enums","text":""},{"location":"dynampi/class_member_enums/#a","title":"a","text":"<ul> <li>AutoStart (dynampi::Timer)</li> </ul>"},{"location":"dynampi/class_member_enums/#o","title":"o","text":"<ul> <li>Ownership (dynampi::MPICommunicator)</li> </ul>"},{"location":"dynampi/class_member_enums/#t","title":"t","text":"<ul> <li>Tag (dynampi::HierarchicalWorkDistributor, dynampi::NaiveWorkDistributor)</li> </ul>"},{"location":"dynampi/class_member_enums/#u","title":"u","text":"<ul> <li>UpperLevelRole (dynampi::HierarchicalLockFreeRMAWorkDistributor)</li> </ul>"},{"location":"dynampi/namespace_members/","title":"Namespace Members","text":""},{"location":"dynampi/namespace_members/#b","title":"b","text":"<ul> <li>backoff_sleep (dynampi::detail)</li> </ul>"},{"location":"dynampi/namespace_members/#c","title":"c","text":"<ul> <li>check_fixed_size_mpi_type (dynampi)</li> <li>check_task_capacity (dynampi::detail)</li> <li>check_variable_batch_read (dynampi::detail)</li> <li>commit_hash (dynampi::version)</li> <li>compile_date (dynampi::version)</li> </ul>"},{"location":"dynampi/namespace_members/#d","title":"d","text":"<ul> <li>DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (dynampi)</li> <li>DynamicWorkDistributor (dynampi)</li> <li>decode_task_error (dynampi::detail)</li> </ul>"},{"location":"dynampi/namespace_members/#e","title":"e","text":"<ul> <li>encode_task_error (dynampi::detail)</li> </ul>"},{"location":"dynampi/namespace_members/#i","title":"i","text":"<ul> <li>is_at_least (dynampi::version)</li> </ul>"},{"location":"dynampi/namespace_members/#k","title":"k","text":"<ul> <li>kMaxTaskErrorMessage (dynampi)</li> </ul>"},{"location":"dynampi/namespace_members/#m","title":"m","text":"<ul> <li>mpi_type_size_bytes (dynampi::detail)</li> <li>major (dynampi::version)</li> <li>minor (dynampi::version)</li> <li>mpi_elements_per_value (dynampi)</li> <li>mpi_fail (dynampi)</li> <li>mpi_manager_worker_distribution (dynampi)</li> </ul>"},{"location":"dynampi/namespace_members/#o","title":"o","text":"<ul> <li>OptionalString (dynampi)</li> <li>operator&lt;&lt; (dynampi)</li> </ul>"},{"location":"dynampi/namespace_members/#p","title":"p","text":"<ul> <li>pack_variable_batch (dynampi::detail)</li> <li>patch (dynampi::version)</li> </ul>"},{"location":"dynampi/namespace_members/#r","title":"r","text":"<ul> <li>read_bytes (dynampi::detail)</li> <li>read_i64 (dynampi::detail)</li> <li>read_result_bytes (dynampi::detail)</li> <li>resolve_upper_fanout (dynampi::detail)</li> <li>rma_wait_idle (dynampi::detail)</li> <li>round_up_8 (dynampi::detail)</li> <li>run_task_guarded (dynampi::detail)</li> </ul>"},{"location":"dynampi/namespace_members/#s","title":"s","text":"<ul> <li>StatisticsMode (dynampi)</li> <li>split_local_worker_communicator (dynampi::detail)</li> <li>sum_subtree_widths_to_group_leader (dynampi::detail)</li> <li>string (dynampi::version)</li> </ul>"},{"location":"dynampi/namespace_members/#t","title":"t","text":"<ul> <li>truncate_task_error_message (dynampi::detail)</li> </ul>"},{"location":"dynampi/namespace_members/#u","title":"u","text":"<ul> <li>unpack_variable_batch (dynampi::detail)</li> </ul>"},{"location":"dynampi/namespace_members/#w","title":"w","text":"<ul> <li>write_bytes (dynampi::detail)</li> <li>write_i64 (dynampi::detail)</li> </ul>"},{"location":"dynampi/namespace_members/#_","title":"_","text":"<ul> <li>_DYNAMPI_FAILBinOp (dynampi)</li> <li>_DYNAMPI_FAIL_ASSERT (dynampi)</li> </ul>"},{"location":"dynampi/namespace_member_functions/","title":"Namespace Member Functions","text":""},{"location":"dynampi/namespace_member_functions/#b","title":"b","text":"<ul> <li>backoff_sleep (dynampi::detail)</li> </ul>"},{"location":"dynampi/namespace_member_functions/#c","title":"c","text":"<ul> <li>check_fixed_size_mpi_type (dynampi)</li> <li>check_task_capacity (dynampi::detail)</li> <li>check_variable_batch_read (dynampi::detail)</li> <li>commit_hash (dynampi::version)</li> <li>compile_date (dynampi::version)</li> </ul>"},{"location":"dynampi/namespace_member_functions/#d","title":"d","text":"<ul> <li>DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (dynampi)</li> <li>decode_task_error (dynampi::detail)</li> </ul>"},{"location":"dynampi/namespace_member_functions/#e","title":"e","text":"<ul> <li>encode_task_error (dynampi::detail)</li> </ul>"},{"location":"dynampi/namespace_member_functions/#i","title":"i","text":"<ul> <li>is_at_least (dynampi::version)</li> </ul>"},{"location":"dynampi/namespace_member_functions/#m","title":"m","text":"<ul> <li>mpi_type_size_bytes (dynampi::detail)</li> <li>mpi_elements_per_value (dynampi)</li> <li>mpi_fail (dynampi)</li> <li>mpi_manager_worker_distribution (dynampi)</li> </ul>"},{"location":"dynampi/namespace_member_functions/#o","title":"o","text":"<ul> <li>OptionalString (dynampi)</li> <li>operator&lt;&lt; (dynampi)</li> </ul>"},{"location":"dynampi/namespace_member_functions/#p","title":"p","text":"<ul> <li>pack_variable_batch (dynampi::detail)</li> </ul>"},{"location":"dynampi/namespace_member_functions/#r","title":"r","text":"<ul> <li>read_bytes (dynampi::detail)</li> <li>read_i64 (dynampi::detail)</li> <li>read_result_bytes (dynampi::detail)</li> <li>resolve_upper_fanout (dynampi::detail)</li> <li>rma_wait_idle (dynampi::detail)</li> <li>round_up_8 (dynampi::detail)</li> <li>run_task_guarded (dynampi::detail)</li> </ul>"},{"location":"dynampi/namespace_member_functions/#s","title":"s","text":"<ul> <li>split_local_worker_communicator (dynampi::detail)</li> <li>sum_subtree_widths_to_group_leader (dynampi::detail)</li> </ul>"},{"location":"dynampi/namespace_member_functions/#t","title":"t","text":"<ul> <li>truncate_task_error_message (dynampi::detail)</li> </ul>"},{"location":"dynampi/namespace_member_functions/#u","title":"u","text":"<ul> <li>unpack_variable_batch (dynampi::detail)</li> </ul>"},{"location":"dynampi/namespace_member_functions/#w","title":"w","text":"<ul> <li>write_bytes (dynampi::detail)</li> <li>write_i64 (dynampi::detail)</li> </ul>"},{"location":"dynampi/namespace_member_functions/#_","title":"_","text":"<ul> <li>_DYNAMPI_FAILBinOp (dynampi)</li> <li>_DYNAMPI_FAIL_ASSERT (dynampi)</li> </ul>"},{"location":"dynampi/namespace_member_variables/","title":"Namespace Member Variables","text":""},{"location":"dynampi/namespace_member_variables/#k","title":"k","text":"<ul> <li>kMaxTaskErrorMessage (dynampi)</li> </ul>"},{"location":"dynampi/namespace_member_variables/#m","title":"m","text":"<ul> <li>major (dynampi::version)</li> <li>minor (dynampi::version)</li> </ul>"},{"location":"dynampi/namespace_member_variables/#p","title":"p","text":"<ul> <li>patch (dynampi::version)</li> </ul>"},{"location":"dynampi/namespace_member_variables/#s","title":"s","text":"<ul> <li>string (dynampi::version)</li> </ul>"},{"location":"dynampi/namespace_member_typedefs/","title":"Namespace Member Typedefs","text":""},{"location":"dynampi/namespace_member_typedefs/#d","title":"d","text":"<ul> <li>DynamicWorkDistributor (dynampi)</li> </ul>"},{"location":"dynampi/namespace_member_enums/","title":"Namespace Member Enums","text":""},{"location":"dynampi/namespace_member_enums/#s","title":"s","text":"<ul> <li>StatisticsMode (dynampi)</li> </ul>"},{"location":"dynampi/functions/","title":"Functions","text":""},{"location":"dynampi/functions/#g","title":"g","text":"<ul> <li>get_option_value (template_options.hpp)</li> </ul>"},{"location":"dynampi/macros/","title":"Macros","text":""},{"location":"dynampi/macros/#d","title":"d","text":"<ul> <li>DYNAMPI_MPI_CHECK (mpi_error.hpp)</li> <li>DYNAMPI_DEFINE_PRIMITIVE_MPI_TYPE (mpi_types.hpp)</li> <li>DYNAMPI_ASSERT (assert.hpp)</li> <li>DYNAMPI_ASSERT_BIN_OP (assert.hpp)</li> <li>DYNAMPI_ASSERT_EQ (assert.hpp)</li> <li>DYNAMPI_ASSERT_GE (assert.hpp)</li> <li>DYNAMPI_ASSERT_GT (assert.hpp)</li> <li>DYNAMPI_ASSERT_LE (assert.hpp)</li> <li>DYNAMPI_ASSERT_LT (assert.hpp)</li> <li>DYNAMPI_ASSERT_NE (assert.hpp)</li> <li>DYNAMPI_FAIL (assert.hpp)</li> <li>DYNAMPI_HAS_BUILTIN (assert.hpp)</li> <li>DYNAMPI_UNIMPLEMENTED (assert.hpp)</li> <li>DYNAMPI_UNREACHABLE (assert.hpp)</li> <li>DYNAMPI_COMMIT_HASH (version.hpp)</li> <li>DYNAMPI_STR (version.hpp)</li> <li>DYNAMPI_STR_HELPER (version.hpp)</li> <li>DYNAMPI_VERSION_MAJOR (version.hpp)</li> <li>DYNAMPI_VERSION_MINOR (version.hpp)</li> <li>DYNAMPI_VERSION_PATCH (version.hpp)</li> <li>DYNAMPI_VERSION_STRING (version.hpp)</li> </ul>"},{"location":"dynampi/variables/","title":"Variables","text":"<p>Nothing related to Variables found.</p>"},{"location":"dynampi/links/","title":"Links","text":"<ul> <li>Related Pages</li> <li>Modules</li> <li>Class List</li> <li>Namespace ListNamespace List</li> <li>Namespace Members</li> <li>Namespace Member Functions</li> <li>Namespace Member Variables</li> <li>Namespace Member Typedefs</li> <li>Namespace Member Enumerations</li> <li>Class Index</li> <li>Class Hierarchy</li> <li>Class Members</li> <li>Class Member Functions</li> <li>Class Member Variables</li> <li>Class Member Typedefs</li> <li>Class Member Enumerations</li> <li>Files</li> <li>File Variables</li> <li>File Functions</li> <li>File Macros</li> </ul>"}]}